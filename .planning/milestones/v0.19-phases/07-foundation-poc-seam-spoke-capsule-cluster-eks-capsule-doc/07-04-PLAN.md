---
phase: 07-foundation-poc-seam-spoke-capsule-cluster-eks-capsule-doc
plan: 04
type: execute
wave: 2
depends_on:
  - "07-00"
files_modified:
  - scripts/preflight.sh
  - .gitignore
  - .gitleaks.toml
autonomous: true
requirements:
  - POC-03
  - POC-04
requirements_addressed:
  - POC-03
  - POC-04

must_haves:
  truths:
    - "scripts/preflight.sh defines a check_port_strict() helper that uses fail (NOT warn) when a port is in use — D-15 inverts the v0.18 check_port semantics"
    - "scripts/preflight.sh invokes check_port_strict 30443 unconditionally (Discretion default; Pitfall 4 mitigation)"
    - "Existing v0.18 check_port() (warn-only) function preserved verbatim — no v0.18 regression"
    - "v0.18 warn-loop over 6443 6444 6445 8080 8081 8082 still uses check_port (NOT check_port_strict) — preserved verbatim"
    - "scripts/preflight.sh contains a # ---------- Network / ports (PRE-15: POC-reserved) ---------- section header"
    - ".gitignore extends Phase 6 D-16 expansion with tenant-kubeconfig globs (karyon-tenants/, *.tenant.kubeconfig, tenants/**/access.yaml, tenants/**/admin.yaml) — append-only; Phase 6 globs preserved verbatim"
    - ".gitleaks.toml gains a [[rules]] block with id=kubeconfig-bearer-token + multi-line (?ms) regex matching kind: Config + users + token shape (D-14)"
    - ".gitleaks.toml has [rules.allowlist] paths entry for tests/fixtures/gitleaks/synthetic-kubeconfig.yaml (Pitfall 5 mitigation — fixture is committable)"
    - "Existing [allowlist] block in .gitleaks.toml preserved verbatim (Phase 1 D-11 contract)"
    - "tests/bats/preflight-pre15.bats turns GREEN; tests/bats/repo-hygiene-04-poc.bats (including live gitleaks invocations) turns GREEN"
  artifacts:
    - path: "scripts/preflight.sh"
      provides: "v0.19 check_port_strict() helper + PRE-15 section + check_port_strict 30443 invocation"
      contains: "check_port_strict()"
      contains_2: "check_port_strict 30443"
      contains_3: "PRE-15"
    - path: ".gitignore"
      provides: "Phase 7 D-14 tenant-kubeconfig globs (append-only extension of Phase 6 D-16 expansion)"
      contains: "karyon-tenants/"
      contains_2: "*.tenant.kubeconfig"
      contains_3: "tenants/**/access.yaml"
      contains_4: "tenants/**/admin.yaml"
    - path: ".gitleaks.toml"
      provides: "First positive [[rules]] block: kubeconfig-bearer-token (multi-line (?ms) regex) + per-fixture path allowlist"
      contains: "id = \"kubeconfig-bearer-token\""
      contains_2: "(?ms)"
      contains_3: "tests/fixtures/gitleaks/synthetic-kubeconfig"
      contains_4: "v0.19"
  key_links:
    - from: "scripts/preflight.sh check_port_strict 30443 invocation"
      to: "scripts/poc/capsule/create-cluster.sh preflight gate (Plan 03)"
      via: "preflight.sh exit code"
      pattern: "fail-fast on 30443 in use"
    - from: ".gitleaks.toml [[rules]] kubeconfig-bearer-token"
      to: "tests/fixtures/gitleaks/synthetic-kubeconfig.yaml (Plan 00 fixture)"
      via: "gitleaks detect --no-git --source FIXTURE positive case"
      pattern: "(?ms) regex matches kind: Config + users + token shape"
    - from: ".gitleaks.toml [rules.allowlist].paths"
      to: "tests/fixtures/gitleaks/synthetic-kubeconfig.yaml"
      via: "per-rule path allowlist"
      pattern: "allowlist excludes only the synthetic fixture from the rule"
---

<objective>
Land two coupled defensive pieces: (1) the `check_port_strict()` fail-fast inversion in `scripts/preflight.sh` (POC-04 / D-15) so port 30443 collision is caught at preflight time, and (2) the tenant-kubeconfig leak defense (POC-03 / D-14): extended `.gitignore` globs + first positive gitleaks rule + per-fixture path allowlist.

Purpose: Per ROADMAP success criteria 4 + 6 + REQUIREMENTS.md POC-03 + POC-04. POC-04's literal contract ("fails fast if the port is already in use") inverts v0.18's `check_port` warn pattern; D-15 says solve via a separate `check_port_strict()` helper (preserves the v0.18 warn surface verbatim while adding the strict variant for POC-reserved ports). POC-03's leak defense closes the loop on P29 — the gitleaks rule lands here, fires on the Plan 00 positive fixture, and stays silent on the negative fixture; the path-allowlist mechanism lets the synthetic fixture live in the repo without breaking pre-commit.

Output: 3 modified files. `scripts/preflight.sh` gains the strict helper + invocation; `.gitignore` gains 4 tenant-kubeconfig globs; `.gitleaks.toml` gains the kubeconfig-bearer-token rule + path allowlist. Both Plan 00 bats files (`preflight-pre15.bats` + `repo-hygiene-04-poc.bats`, including live gitleaks invocations) turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-foundation-poc-seam-spoke-capsule-cluster-eks-capsule-doc/07-CONTEXT.md
@.planning/phases/07-foundation-poc-seam-spoke-capsule-cluster-eks-capsule-doc/07-RESEARCH.md
@.planning/phases/07-foundation-poc-seam-spoke-capsule-cluster-eks-capsule-doc/07-PATTERNS.md

# Self-references for the 3 modified files
@scripts/preflight.sh
@.gitignore
@.gitleaks.toml

# Plan 00 contracts this plan must satisfy
@tests/bats/preflight-pre15.bats
@tests/bats/repo-hygiene-04-poc.bats
@tests/fixtures/gitleaks/synthetic-kubeconfig.yaml
@tests/fixtures/gitleaks/benign-configmap.yaml

<interfaces>
<!-- scripts/preflight.sh existing structure (preserve everything except the targeted ports section): -->

From scripts/preflight.sh lines 175-190 (current "Network / ports" section):
```bash
# ---------- 5. Network / ports ----------
section "Network / ports (k3d will want these)"

check_port() {
  local port="$1"
  if ss -ltn 2>/dev/null | awk 'NR>1 {print $4}' | grep -qE "[:.]${port}$"; then
    warn "Port ${port} is in use"
    ss -ltnp 2>/dev/null | awk -v p=":${port}" '$4 ~ p {print "      " $0}' | head -2
  else
    pass "Port ${port} free"
  fi
}

for p in 6443 6444 6445 8080 8081 8082; do
  check_port "$p"
done
```

Section 6 (line 192) starts: `# ---------- 6. Existing container / k8s state ----------`. The new strict-helper + PRE-15 section MUST be inserted BETWEEN sections 5 and 6.

<!-- .gitleaks.toml current state (lines 1-18 — preserve verbatim above the new [[rules]] block): -->

From .gitleaks.toml:
```toml
# .gitleaks.toml
# Allow-rules for gitleaks pre-commit hook (REPO-04) and CI gitleaks-scan job
# (REPO-07, see Plan 07). ...

[allowlist]
description = "False positives: placeholder tokens and CONTEXT.md SHAs"
regexes = [
  '''ghp_REPLACE_WITH_PAT_WITH_repo_SCOPE''',
]
paths = [
  '''^\.env\.example$''',
]
```

<!-- .gitignore Phase 6 D-16 section (preserve verbatim above the new Phase 7 D-14 block): -->

From .gitignore lines 9-22:
```
# Phase 6 D-16 expansion ──────────────────────────────────

# Kubeconfig artifacts (fix-coredns / register-spokes scratch)
kubeconfig*
*.kubeconfig

# TLS material (anything generated locally)
*.pem
*.key
*.crt

# Flux scratch from bootstrap retries
flux-system/identity*
```

The new Phase 7 D-14 block appends after these existing globs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend scripts/preflight.sh with check_port_strict() helper + PRE-15 section + 30443 invocation</name>
  <files>scripts/preflight.sh</files>
  <read_first>
    - scripts/preflight.sh (current state — preserve every byte outside the targeted ports section; specifically preserve check_port() warn body lines 178-187 + the for-loop over 6443 6444 6445 8080 8081 8082 lines 188-190)
    - .planning/phases/07-foundation-poc-seam-spoke-capsule-cluster-eks-capsule-doc/07-PATTERNS.md (lines 523-611 — preflight extension pattern with verbatim helper)
    - .planning/phases/07-foundation-poc-seam-spoke-capsule-cluster-eks-capsule-doc/07-RESEARCH.md (lines 570-611 — Pattern 5 with separate-helper rationale)
    - .planning/phases/07-foundation-poc-seam-spoke-capsule-cluster-eks-capsule-doc/07-CONTEXT.md (D-15 — fail-fast unconditional Discretion default)
    - tests/bats/preflight-pre15.bats (Plan 00 contract)
  </read_first>
  <action>
Modify `scripts/preflight.sh` to add the `check_port_strict()` helper and the PRE-15 section that invokes it on 30443. The existing `check_port()` warn-only function and the v0.18 warn-loop MUST be preserved verbatim — D-15 explicitly preserves the v0.18 surface unchanged.

**Insertion point:** Between line 190 (`done` of the v0.18 warn-loop) and line 192 (start of section 6). The current ports section ends at:

```bash
for p in 6443 6444 6445 8080 8081 8082; do
  check_port "$p"
done
```

After this `done`, append the PRE-15 block. Use Edit tool to make the targeted change.

**Insert this block immediately after the existing `done` line of the v0.18 ports loop:**

```bash

# v0.19 POC-04 / D-15: strict-mode port check — fails fast (does NOT warn) when
# the port is in use. Used for POC-reserved ports where a stale listener would
# block k3d cluster create. UNCHANGED check_port() preserved above for v0.18 ports.
check_port_strict() {
  local port="$1"
  if ss -ltn 2>/dev/null | awk 'NR>1 {print $4}' | grep -qE "[:.]${port}$"; then
    fail "Port ${port} is in use elsewhere on the host (POC-04 reserved).
      Inspect: ss -ltnp | awk '\$4 ~ /:${port}\$/{print}'
      Fix: free the port (kill the listener) or stop the offending service before continuing"
    ss -ltnp 2>/dev/null | awk -v p=":${port}" '$4 ~ p {print "      " $0}' | head -2
  else
    pass "Port ${port} free (POC-04 fail-fast)"
  fi
}

# ---------- 5b. Network / ports (PRE-15: POC-reserved) ----------
section "Network / ports (PRE-15: POC-reserved)"
check_port_strict 30443
```

**Critical invariants (D-15 / POC-04):**

- The existing `check_port()` function (lines 178-187 in current preflight.sh) MUST be preserved BYTE-FOR-BYTE — no edits to its body, no comments added inside it
- The existing for-loop `for p in 6443 6444 6445 8080 8081 8082` MUST be preserved BYTE-FOR-BYTE
- The new helper MUST be named `check_port_strict()` (P30 disambiguation; bats greps the literal function name)
- The invocation MUST be the literal `check_port_strict 30443` (bats greps this exact string)
- The new section header MUST contain the literal `PRE-15` (bats greps for greppable section marker)
- The body of `check_port_strict()` MUST use `fail` (NOT `warn`) — bats `awk` extract + grep enforces this
- All other sections (PRE-01 through section 11) MUST be preserved verbatim — no v0.18 regression
- Section numbering: existing has 5 (Network/ports) → 6 (Existing state) → 7 (Tools) → ... → 11 (systemd-resolved). The new PRE-15 section is "5b" semantically but the bats only checks for the literal `PRE-15` text presence — no need to renumber existing sections

After editing, the v0.18 sections 5 (lines 175-190) and 6+ (lines 192+) are still in place; the only insertion is the strict helper + invocation block between them.

Use Edit tool — make the single targeted insertion. Do NOT rewrite the entire file.
  </action>
  <verify>
    <automated>bash -n scripts/preflight.sh && bats tests/bats/preflight-pre15.bats 2>&1 | tail -25 && bats tests/bats/preflight-pre*.bats --filter 'PRE-0|PRE-1' 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/preflight.sh` exists; `bash -n scripts/preflight.sh` exits 0 (syntax valid)
    - `grep -F 'check_port_strict()' scripts/preflight.sh` matches (function defined)
    - `grep -F 'check_port_strict 30443' scripts/preflight.sh` matches (invocation)
    - `grep -F 'PRE-15' scripts/preflight.sh` matches (section header)
    - `grep -F 'check_port()' scripts/preflight.sh` matches (v0.18 warn function preserved)
    - `grep -F 'for p in 6443 6444 6445 8080 8081 8082' scripts/preflight.sh` matches (v0.18 warn-loop preserved)
    - `awk '/^check_port_strict\(\)/{flag=1} flag; /^}/{if(flag){flag=0; exit}}' scripts/preflight.sh | grep -F -- 'fail '` matches (D-15 — uses fail, not warn)
    - `awk '/^check_port\(\)/{flag=1} flag; /^}/{if(flag){flag=0; exit}}' scripts/preflight.sh | grep -F -- 'warn '` matches (v0.18 — uses warn, not fail)
    - `bats tests/bats/preflight-pre15.bats` exits 0 (all 6 tests GREEN)
    - All v0.18 preflight bats (preflight-pre*.bats other than pre15) still GREEN — no regression
    - The new helper appears AFTER the existing `for p in 6443 ...` loop (verify by line ordering: `grep -nE 'for p in 6443|^check_port_strict' scripts/preflight.sh` shows for-loop line < check_port_strict line)
  </acceptance_criteria>
  <done>scripts/preflight.sh extended with check_port_strict + PRE-15 30443 invocation; v0.18 warn-only check_port + warn-loop preserved verbatim; preflight-pre15.bats GREEN; all v0.18 preflight tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Extend .gitignore with tenant-kubeconfig globs (D-14) + add first positive gitleaks rule + per-fixture allowlist to .gitleaks.toml</name>
  <files>.gitignore, .gitleaks.toml</files>
  <read_first>
    - .gitignore (current state — preserve every byte; append-only edit per D-14)
    - .gitleaks.toml (current state — preserve [allowlist] block verbatim per Phase 1 D-11 contract; append [[rules]] block)
    - .planning/phases/07-foundation-poc-seam-spoke-capsule-cluster-eks-capsule-doc/07-PATTERNS.md (lines 626-722 — .gitignore + .gitleaks.toml patterns)
    - .planning/phases/07-foundation-poc-seam-spoke-capsule-cluster-eks-capsule-doc/07-RESEARCH.md (lines 619-704 — Pattern 6 multi-line gitleaks rule + fixtures)
    - .planning/phases/07-foundation-poc-seam-spoke-capsule-cluster-eks-capsule-doc/07-CONTEXT.md (D-14 — gitleaks rule + .gitignore globs by end of Phase 7)
    - tests/bats/repo-hygiene-04-poc.bats (Plan 00 contract — multi-literal greps + live gitleaks fires-on-positive + does-NOT-fire-on-negative)
    - tests/fixtures/gitleaks/synthetic-kubeconfig.yaml (Plan 00 positive fixture — must be allowlisted by path)
    - tests/fixtures/gitleaks/benign-configmap.yaml (Plan 00 negative fixture)
  </read_first>
  <action>
Make 2 file edits. Both are append-only — preserve all existing content verbatim.

**Edit 1: Append Phase 7 D-14 expansion to `.gitignore`**

After the existing Phase 6 D-16 expansion section (which ends after line 22 `flux-system/identity*`), append a new Phase 7 D-14 block. Use Edit tool to append; do NOT rewrite the file.

```
# Phase 7 D-14 expansion (v0.19 POC-03) ──────────────────
# Tenant kubeconfig globs (Phase 10 ${TMPDIR:-/tmp}/karyon-tenants/ destination):
karyon-tenants/
*.tenant.kubeconfig
tenants/**/access.yaml
tenants/**/admin.yaml
```

**Edit 2: Append the first positive `[[rules]]` block + per-fixture allowlist to `.gitleaks.toml`**

The current file is allowlist-only. Append the new rule block AFTER the existing `[allowlist]` block. Preserve lines 1-17 verbatim (per Phase 1 D-11 contract — DO NOT modify the existing `[allowlist]` block).

```toml

# Phase 7 v0.19 POC-03: kubeconfig-shaped artifact + bearer token regex.
# gitleaks 8.30.1 supports multi-line regex via (?ms) and [\r\n] character classes.
# minVersion = "v8.25.0" satisfied by the project's pinned 8.30.1.
# Ref: .planning/research/PITFALLS.md §P29; tests/fixtures/gitleaks/synthetic-kubeconfig.yaml
# is the bats positive fixture (path-allowlisted below so it can live in the repo).

[[rules]]
id = "kubeconfig-bearer-token"
description = "Kubernetes kubeconfig with embedded bearer token (kind: Config + users.token)"
regex = '''(?ms)^kind:\s*Config[\s\S]*?users:[\s\S]*?token:\s*[A-Za-z0-9._-]{32,}'''
tags = ["kubernetes", "kubeconfig", "bearer-token", "v0.19"]

[rules.allowlist]
description = "Phase 7 synthetic fixtures (verified inert; positive-test only)"
paths = [
  '''^tests/fixtures/gitleaks/synthetic-kubeconfig\.yaml$''',
]
```

**Critical invariants for both files:**

- `.gitignore`:
  - APPEND-ONLY edit; existing Phase 6 D-16 globs preserved verbatim (Phase 4 + Phase 6 inheritance)
  - All 4 D-14 tenant-kubeconfig globs present: `karyon-tenants/`, `*.tenant.kubeconfig`, `tenants/**/access.yaml`, `tenants/**/admin.yaml`
- `.gitleaks.toml`:
  - APPEND-ONLY edit; existing `[allowlist]` block (lines 7-14) preserved verbatim (Phase 1 D-11 contract)
  - New rule literal `id = "kubeconfig-bearer-token"`
  - Multi-line regex uses `(?ms)` flag (gitleaks 8.30.1 supports this; satisfies minVersion v8.25.0)
  - Regex anchors on `^kind:\s*Config[\s\S]*?users:[\s\S]*?token:\s*[A-Za-z0-9._-]{32,}` — catches kubeconfig + bearer-token shape
  - Tags include `"v0.19"` (bats greps for this literal)
  - `[rules.allowlist].paths` allowlists EXACTLY `^tests/fixtures/gitleaks/synthetic-kubeconfig\.yaml$` — narrowest possible path-pattern (Pitfall 5 mitigation)
  - The `[rules.allowlist]` block is per-rule (NOT global); applies only to the `kubeconfig-bearer-token` rule

After editing, verify the gitleaks rule fires on the Plan 00 positive fixture and stays silent on the Plan 00 negative fixture (both fixtures already exist from Plan 00 — Wave 0 Nyquist gate).
  </action>
  <verify>
    <automated>bats tests/bats/repo-hygiene-04-poc.bats 2>&1; gitleaks detect --no-banner --no-git --source tests/fixtures/gitleaks/synthetic-kubeconfig.yaml --config .gitleaks.toml --redact --verbose; echo "exit=$?"; gitleaks detect --no-banner --no-git --source tests/fixtures/gitleaks/benign-configmap.yaml --config .gitleaks.toml --redact --verbose; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains all 4 new D-14 globs: `grep -F 'karyon-tenants/' .gitignore`, `grep -F '*.tenant.kubeconfig' .gitignore`, `grep -F 'tenants/**/access.yaml' .gitignore`, `grep -F 'tenants/**/admin.yaml' .gitignore` — all match
    - `.gitignore` preserves Phase 6 D-16 globs: `grep -F 'kubeconfig*' .gitignore`, `grep -F '*.pem' .gitignore`, `grep -F '*.key' .gitignore`, `grep -F '*.crt' .gitignore` — all match (no regression)
    - `.gitleaks.toml` contains the new rule: `grep -F 'id = "kubeconfig-bearer-token"' .gitleaks.toml` matches
    - `.gitleaks.toml` rule uses multi-line `(?ms)`: `grep -F '(?ms)' .gitleaks.toml` matches
    - `.gitleaks.toml` rule references `kind:`, `users:`, `token:`: `grep -F 'kind:' .gitleaks.toml`, `grep -F 'users:' .gitleaks.toml`, `grep -F 'token:' .gitleaks.toml` — all match
    - `.gitleaks.toml` rule has v0.19 tag: `grep -F 'v0.19' .gitleaks.toml` matches
    - `.gitleaks.toml` allowlists positive fixture by path: `grep -F 'tests/fixtures/gitleaks/synthetic-kubeconfig' .gitleaks.toml` matches
    - `.gitleaks.toml` preserves Phase 1 [allowlist]: `grep -F 'ghp_REPLACE_WITH_PAT_WITH_repo_SCOPE' .gitleaks.toml` matches (no regression)
    - Live test: `gitleaks detect --no-banner --no-git --source tests/fixtures/gitleaks/synthetic-kubeconfig.yaml --config .gitleaks.toml --redact --verbose` exits non-zero (rule fires on positive fixture)
    - Live test: `gitleaks detect --no-banner --no-git --source tests/fixtures/gitleaks/benign-configmap.yaml --config .gitleaks.toml --redact --verbose` exits 0 (rule does NOT fire on negative fixture)
    - `bats tests/bats/repo-hygiene-04-poc.bats` exits 0 (all 11+ tests GREEN, including live gitleaks invocations)
    - `bats tests/bats/repo-hygiene-01-static.bats` and `repo-hygiene-02-taskfile.bats` and `repo-hygiene-03*.bats` still GREEN (no v0.18 regression)
    - The synthetic fixture (`tests/fixtures/gitleaks/synthetic-kubeconfig.yaml`) is committable to git WITHOUT triggering the project's pre-commit gitleaks hook (the path-allowlist excludes it from the new rule)
  </acceptance_criteria>
  <done>.gitignore + .gitleaks.toml extended with D-14 leak defense; rule fires on positive fixture, silent on negative; per-fixture allowlist functioning; Phase 1 + Phase 6 inheritance preserved verbatim; repo-hygiene-04-poc.bats GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator (WSL host) → preflight gate | check_port_strict 30443 reads listener state via `ss -ltn` (read-only host network probe); fails fast on collision |
| Repo author → git commit (pre-commit) | gitleaks scans staged content using .gitleaks.toml rules + allowlists; new positive rule catches kubeconfig-shaped artifacts; per-fixture allowlist excludes synthetic-kubeconfig.yaml |
| Repo author → git history (post-push CI) | Same .gitleaks.toml; rule applies to entire history (gitleaks 8.30.1 multi-line scan covers all commits) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-D-15 / T-7-P-39 | Denial of Service (port collision) | scripts/preflight.sh check_port_strict 30443 | mitigate | Unconditional fail-fast (D-15 + Discretion default). On 30443 collision, preflight exits 1 BEFORE Plan 03's create-cluster.sh attempts k3d cluster create (which would partially succeed and leave a half-built cluster). Existing v0.18 check_port (warn-only) preserved for ports 6443-8082 — no regression. |
| T-7-D-14 / T-7-P-29 | Information Disclosure (tenant kubeconfig leak) | .gitignore + .gitleaks.toml | mitigate | 4-glob .gitignore extension matches Phase 10's `${TMPDIR:-/tmp}/karyon-tenants/` output paths. Multi-line gitleaks rule catches `kind: Config + users + token` shape with `[A-Za-z0-9._-]{32,}` token-length anchor (matches JWT/SA-token family). Per-fixture path allowlist (Pitfall 5 mitigation) excludes `tests/fixtures/gitleaks/synthetic-kubeconfig.yaml` from the rule so the fixture is committable. |
| T-7-Pitfall-5 | False Positive (rule fires on innocuous YAML) | .gitleaks.toml [[rules]] kubeconfig-bearer-token | mitigate | Plan 00 negative fixture `tests/fixtures/gitleaks/benign-configmap.yaml` exercises "rule does NOT fire on innocuous YAML"; bats live test asserts `gitleaks detect` exits 0. Token regex `[A-Za-z0-9._-]{32,}` requires 32+ chars (avoids short-identifier false-positives). gitleaks built-in entropy heuristic provides additional double-classification filter. |
| T-7-D-14-inherit-01 | Tampering (regress Phase 1 allowlist) | .gitleaks.toml [allowlist] block | mitigate | Append-only edit; lines 7-14 (existing [allowlist] with ghp_REPLACE placeholder + .env.example path) preserved verbatim. Bats `tests/bats/repo-hygiene-04-poc.bats` greps for `ghp_REPLACE_WITH_PAT_WITH_repo_SCOPE` to confirm Phase 1 D-11 contract. |
| T-7-D-14-inherit-02 | Tampering (regress Phase 6 .gitignore globs) | .gitignore | mitigate | Append-only edit; Phase 6 D-16 expansion globs (kubeconfig*, *.kubeconfig, *.pem, *.key, *.crt, flux-system/identity*) preserved verbatim. Bats greps each Phase 6 glob to confirm Phase 6 inheritance. |
| T-7-Sentinel-self-invalidation | Tampering | .gitleaks.toml comment block | accept | The comment block above the new [[rules]] mentions `kind: Config` and `token` literals in narrative form; the gitleaks rule's regex requires the line to start with `^kind:\s*Config` — comment indentation prevents false self-match. The bats grep for `kind:` and `token:` literals catches both rule body and comment occurrences (which is fine — the bats only asserts presence, not uniqueness). No mitigation needed beyond reviewing. |
</threat_model>

<verification>
- `bash -n scripts/preflight.sh` exits 0
- `bats tests/bats/preflight-pre15.bats` exits 0
- `bats tests/bats/repo-hygiene-04-poc.bats` exits 0 (including 2 live gitleaks invocations)
- `bats tests/bats/preflight-pre*.bats` (all PRE-* tests) shows zero v0.18 regressions
- `bats tests/bats/repo-hygiene-*.bats` shows zero v0.18 regressions (Phase 6 + Phase 1 inheritance preserved)
- `bats tests/bats/` (full suite) zero failures
- Live: `gitleaks detect --no-banner --no-git --source tests/fixtures/gitleaks/synthetic-kubeconfig.yaml --config .gitleaks.toml` exits non-zero (positive fires)
- Live: `gitleaks detect --no-banner --no-git --source tests/fixtures/gitleaks/benign-configmap.yaml --config .gitleaks.toml` exits 0 (negative silent)
- Live: `gitleaks git --pre-commit --staged --config .gitleaks.toml` (project's pre-commit invocation) does NOT flag the synthetic fixture (path-allowlist functioning)
</verification>

<success_criteria>
- 3 modified files (scripts/preflight.sh, .gitignore, .gitleaks.toml)
- POC-03 + POC-04 contracts satisfied at file-shape AND live-gitleaks levels
- v0.18 invariants preserved verbatim (check_port warn-only; check_port loop ports; .gitignore Phase 6 globs; .gitleaks.toml [allowlist])
- Both Plan 00 bats files (preflight-pre15.bats + repo-hygiene-04-poc.bats) GREEN
- Live gitleaks behavior: positive fires, negative silent, synthetic fixture path-allowlisted
- Plan 03's create-cluster.sh preflight gate now backed by 30443 fail-fast
</success_criteria>

<output>
After completion, create `.planning/phases/07-foundation-poc-seam-spoke-capsule-cluster-eks-capsule-doc/07-04-SUMMARY.md` documenting:
- POC-03 + POC-04 deliverables shipped
- 3 modified files (preflight.sh check_port_strict + 30443 invocation; .gitignore D-14 globs; .gitleaks.toml first positive rule + per-fixture allowlist)
- D-15 fail-fast inversion attestation: check_port_strict uses fail (not warn); existing check_port preserved verbatim
- gitleaks rule shape: id="kubeconfig-bearer-token", multi-line (?ms), tags=["v0.19"], allowlist-by-path for synthetic-kubeconfig.yaml only
- Live gitleaks evidence: positive fixture caught (exit 1), negative fixture silent (exit 0)
- v0.18 inheritance preserved verbatim: check_port warn-only function + warn-loop, [allowlist] ghp placeholder + .env.example path, Phase 6 D-16 .gitignore globs
- Bats coverage: preflight-pre15.bats GREEN; repo-hygiene-04-poc.bats GREEN; v0.18 preflight + repo-hygiene tests GREEN (no regression)
</output>
</content>
</invoke>