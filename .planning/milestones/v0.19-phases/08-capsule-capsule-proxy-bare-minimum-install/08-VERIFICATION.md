---
phase: 08-capsule-capsule-proxy-bare-minimum-install
verified: 2026-04-29T22:30:00Z
status: passed_with_overrides
score: 10/10 must-haves verified (6 live-VERIFIED + 4 deferred-with-override)
overrides_applied: 4
re_verification:
  previous_status: gaps_found
  previous_score: 6/10
  gaps_closed:
    - "G-03 (BL-01) — proxy HelmRelease cert source flipped per D-08-11 (controller-less capsule-proxy-certgen Job)"
    - "G-04 (WR-05) — outer Kustomization split per D-08-12 (hub-targeted capsule.yaml + spoke-targeted capsule-spoke.yaml)"
    - "WR-01 — capsule-install-04-static-no-umbrella.bats vacuous grep replaced with yq recursive-descent path assertion"
    - "WR-02 — capsule-install-04-static-no-umbrella.bats vacuous grep replaced with yq path positive assertion"
    - "WR-03 — capsule-install-06-live-helm.bats fragile regex replaced with kubectl jsonpath column-anchored Ready=True parsing"
    - "WR-04 — capsule-install-09-live-adr-004.bats silent-pass-on-any-non-zero replaced with NotFound vs transient distinction"
    - "Phase 7 P40/P18 invariant evolution recorded — spoke-targeted Ks carry kubeConfig; hub-targeted Ks must NOT"
  gaps_remaining: []
  regressions: []
overrides:
  - must_have: "Both Flux v2 HelmReleases (capsule:0.12.4 operator + capsule-proxy:0.12.0) reach Ready=True"
    reason: "Push gate deferred to Phase 11 VAL-05 per D-08-13 (08-CONTEXT.md lines 108-112) — Path B framing accepted by milestone owner via interactive prompt 2026-04-29. Phase 11 VAL-05 owns the first-push event (one-shot history scan + first-push gate). Phase 8 ships the structural fix (D-08-11 cert flip + D-08-12 split-K architecture); the runtime install evidence is gated on VAL-05. Live observation confirms the deferral is honored: HEAD=0b8f3fc7 vs origin/main=32e0b2fa (35 commits ahead, NOT pushed). The hub-flux GitRepository is reconciling against pre-Phase-8 source where pocs/capsule/kustomization.yaml resources is [] (no objects to apply yet)."
    accepted_by: "milestone owner via /gsd-plan-phase 8 --gaps interactive prompt 2026-04-29"
    accepted_at: "2026-04-29"
  - must_have: "capsule-controller-manager pod is Running on spoke-capsule (CAP-01 secondary signal)"
    reason: "Push gate deferred to Phase 11 VAL-05 per D-08-13 — pod cannot exist until operator HR reconciles, which requires the push event. Live: spoke-capsule capsule-system ns has no resources (kubectl --context=k3d-spoke-capsule -n capsule-system get pods returns 'No resources found in capsule-system namespace.'). Resolution path: Phase 11 VAL-05 push lands → outer K applies HR + OCIRepo to hub → helm-controller installs chart on spoke via HR.kubeConfig → pod becomes Running. The structural enablers are in place (HR shape valid on hub per server-side dry-run; D-08-12 architecture routes correctly)."
    accepted_by: "milestone owner via /gsd-plan-phase 8 --gaps interactive prompt 2026-04-29"
    accepted_at: "2026-04-29"
  - must_have: "≥7 capsule.clastix.io CRDs registered on spoke-capsule (CAP-03)"
    reason: "Push gate deferred to Phase 11 VAL-05 per D-08-13 — CRDs land via the operator chart's install.crds=CreateReplace policy, which only fires once the HR reconciles. Live: kubectl --context=k3d-spoke-capsule get crds | grep -c capsule.clastix.io returns 0. Same resolution chain as the operator pod must-have."
    accepted_by: "milestone owner via /gsd-plan-phase 8 --gaps interactive prompt 2026-04-29"
    accepted_at: "2026-04-29"
  - must_have: "NodePort 30443 reachable from WSL host — TLS handshake + HTTP code [1-5][0-9][0-9] (CAP-02)"
    reason: "Push gate deferred to Phase 11 VAL-05 per D-08-13 — proxy pod cannot start without the chart-rendered TLS Secret, which is materialized by the capsule-proxy-certgen Job that only runs after the proxy HR reconciles. Live: curl -sk https://127.0.0.1:30443/healthz --max-time 5 returns code 000 (TLS handshake fails / port closed; exit RC=35). The G-03 BL-01 cert-source defect (which would have caused this to fail even after push) is now CLOSED per D-08-11 — helm template renders 0 Issuer + 0 Certificate + capsule-proxy-certgen Job. Resolution: Phase 11 VAL-05 push lands → proxy HR reconciles → certgen Job produces Secret → proxy pod mounts TLS volume → NodePort 30443 reachable."
    accepted_by: "milestone owner via /gsd-plan-phase 8 --gaps interactive prompt 2026-04-29"
    accepted_at: "2026-04-29"
gaps: []
deferred:
  - truth: "HelmReleases Ready=True; operator pod Running; CRDs registered; NodePort 30443 reachable"
    addressed_in: "Phase 11 VAL-05"
    evidence: "ROADMAP.md Phase 11 VAL-05: 'One-shot history gitleaks scan post-Phase-10 first push returns 0 findings' — this success criterion is the first-push event. D-08-13 (08-CONTEXT.md lines 108-112) explicitly assigns first-push ownership to VAL-05. The 4 push-gate-downstream must-haves resolve naturally once the push lands and outer K applies the now-corrected HR + OCIRepo CRs to hub-flux."
human_verification: []
---

# Phase 8: Capsule + capsule-proxy Bare-Minimum Install — Verification Report (Re-verification)

**Phase Goal:** Capsule operator and capsule-proxy run on spoke-capsule via two separate Flux HelmReleases reconciled hub-only; CRDs are visible and the proxy is reachable from the WSL host
**Verified:** 2026-04-29T22:30:00Z
**Status:** `passed_with_overrides`
**Re-verification:** Yes — after gap closure (plan 08-04)

## Re-verification Summary

| Aspect | Previous (initial) | Current (post-08-04) |
| ------ | ------------------ | -------------------- |
| Status | `gaps_found` | `passed_with_overrides` |
| Score | 6/10 truths verified | 10/10 (6 VERIFIED live + 4 DEFERRED-WITH-OVERRIDE) |
| BLOCKER gaps | G-03 (cert source), G-04 (architectural seam) | NONE — both closed by plan 08-04 |
| Push-gate gaps | G-01/G-02 (recommendation: gap-fix) | DEFERRED-WITH-OVERRIDE per D-08-13 (4 entries) |
| Static bats | 38/38 | 64/64 GREEN (no regressions; +6 from gap-close evolution) |

**Plan 08-04 commits:** `75e5b78` (D-08-11 cert flip + WR-01/02), `a9af679` (D-08-12 split-K + Phase 7 invariant evolution), `36f1cfd` (WR-03/04 live-bats hardening)

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                                                | Status     | Evidence                                                                                                                                                                                                                                                                       |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Two SEPARATE Flux HelmReleases reconciled hub-only — `capsule:0.12.4` operator + `capsule-proxy:0.12.0` (NOT umbrella `proxy.enabled: true`)                                                         | VERIFIED   | Both yaml files exist with correct shape; static bat 64/64 GREEN. WR-01 fix (yq recursive-descent path) confirms anti-umbrella invariant on operator HR.                                                                                                                       |
| 2   | Both HelmReleases report `Ready=True` via `flux get helmrelease`                                                                                                                                     | PASSED (override) | DEFERRED per D-08-13 — push gate not yet satisfied (HEAD=0b8f3fc7 vs origin/main=32e0b2fa, 35 commits ahead; `flux get helmrelease -A --context k3d-hub-flux` reports no HelmRelease objects). Structural blockers (G-03 cert + G-04 architecture) are CLOSED by plan 08-04 — push event in Phase 11 VAL-05 will trigger natural resolution. Override accepted by milestone owner 2026-04-29. |
| 3   | `capsule-controller-manager` pod is `Running` on spoke-capsule                                                                                                                                       | PASSED (override) | DEFERRED per D-08-13 — `kubectl --context=k3d-spoke-capsule -n capsule-system get pods` returns "No resources found in capsule-system namespace." Downstream of must-have #2; same resolution chain. Override accepted by milestone owner 2026-04-29.                                |
| 4   | ≥7 `capsule.clastix.io` CRDs registered on spoke-capsule                                                                                                                                             | PASSED (override) | DEFERRED per D-08-13 — `kubectl --context=k3d-spoke-capsule get crds \| grep -c capsule.clastix.io` returns 0. Downstream of must-have #2. Override accepted by milestone owner 2026-04-29.                                                                                          |
| 5   | NodePort 30443 reachable from WSL host (TLS handshake + HTTP `[1-5][0-9][0-9]`)                                                                                                                      | PASSED (override) | DEFERRED per D-08-13 — `curl -sk https://127.0.0.1:30443/healthz --max-time 5` returns code 000 (TLS handshake fails / port closed; RC=35). G-03 BL-01 cert-source defect that would have blocked this AFTER push is now CLOSED per D-08-11 (helm-template renders 0 Issuer + 0 Certificate + capsule-proxy-certgen Job). Override accepted by milestone owner 2026-04-29. |
| 6   | ADR-004 invariant preserved — `kubectl --context=k3d-spoke-capsule -n flux-system get pods` returns no flux controllers                                                                              | VERIFIED   | `kubectl --context=k3d-spoke-capsule get pods -n flux-system` returns "No resources found in flux-system namespace." WR-04 fix (08-04) hardened the bats to distinguish NotFound from transient errors.                                                                       |
| 7   | FOLDED CARRYOVER — outer `poc-capsule` Kustomization on hub-flux reports `Ready=True` against a post-Phase-7 source revision                                                                         | VERIFIED   | `flux get kustomization poc-capsule -n flux-system --context k3d-hub-flux` reports `Ready=True` with `Applied revision: main@sha1:32e0b2fa` (post-Phase-7 close-out source revision). Note: post-08-04 source revision lands at Phase 11 VAL-05 push.                          |
| 8   | Phase 7 P31 isolation regression — `tests/bats/poc-isolation-01-static.bats` GREEN                                                                                                                   | VERIFIED   | All 64 static bats GREEN end-to-end. Phase 8 introduced ZERO `spoke-capsule` mentions in 6 v0.18 scripts.                                                                                                                                                                  |
| 9   | Phase 7 P40/P18 invariant — outer Flux Kustomization carries `secretRef.name: spoke-capsule-kubeconfig` AND `key: value.yaml` (EVOLVED per D-08-12: split into hub-targeted + spoke-targeted) | VERIFIED   | **EVOLVED form**: `clusters/hub-flux/pocs/capsule.yaml` (hub-targeted) has NO `spec.kubeConfig` (yq returns `null`); `clusters/hub-flux/pocs/capsule-spoke.yaml` (spoke-targeted, NEW) has full `spec.kubeConfig.secretRef={name:spoke-capsule-kubeconfig,key:value.yaml}` + `spec.path: ./pocs/capsule/spoke`. Pocs aggregator references both. Bats `poc-mount-01-static.bats` updated to assert split form (10/10 ok). |
| 10  | Pitfall 9 anti-pattern lint — proxy HelmRelease `dependsOn` block has NO `wait:` field                                                                                                               | VERIFIED   | `yq eval '.spec.dependsOn[0] \| has("wait")' pocs/capsule/proxy/helmrelease.yaml` returns `false`. Static bats `capsule-install-04` test 2 GREEN.                                                                                                                                                |

**Score:** 10/10 truths verified (6 VERIFIED live + 4 PASSED-with-override per D-08-13 push-gate deferral)

### Required Artifacts

| Artifact                                                | Expected                                                                                  | Status     | Details                                                                                                                                                                                                       |
| ------------------------------------------------------- | ----------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pocs/capsule/operator/ocirepository.yaml`              | Flux v1 OCIRepository pinning `capsule:0.12.4`                                            | VERIFIED   | yq invariants pass (apiVersion source.toolkit.fluxcd.io/v1, ref.tag 0.12.4, etc.)                                                                                                                              |
| `pocs/capsule/operator/helmrelease.yaml`                | Flux v2 HelmRelease (built-in certgen, per-hook failurePolicy, kubeConfig)                | VERIFIED   | All yq shape invariants pass. Server-side dry-run on hub-flux returns "namespaces capsule-system not found" (NOT "no matches for kind") — confirms HR will land on hub once outer K applies it post-push.       |
| `pocs/capsule/operator/kustomization.yaml`              | Local Kustomize aggregator                                                                | VERIFIED   | resources: [ocirepository.yaml, helmrelease.yaml]                                                                                                                                                              |
| `pocs/capsule/proxy/ocirepository.yaml`                 | Flux v1 OCIRepository pinning `capsule-proxy:0.12.0`                                      | VERIFIED   | yq invariants pass                                                                                                                                                                                            |
| `pocs/capsule/proxy/helmrelease.yaml`                   | Flux v2 HelmRelease (dependsOn operator, NodePort 30443, options.additionalSANs, **D-08-11 cert flip**) | VERIFIED   | All shape invariants pass. **G-03 CLOSED per D-08-11**: `.spec.values.options.generateCertificates == true` AND `.spec.values.certManager.generateCertificates == false`. helm-template renders 0 cert-manager.io Issuer/Certificate, 2 Jobs (capsule-proxy-certgen + capsule-proxy-crds). Comment block at lines 60-77 cleanly attributes D-08-11 + 08-VERIFICATION.md G-03 + 08-REVIEW.md BL-01 + helm-template evidence. |
| `pocs/capsule/proxy/kustomization.yaml`                 | Local Kustomize aggregator                                                                | VERIFIED   | Mirrors operator subdir aggregator                                                                                                                                                                             |
| `pocs/capsule/kustomization.yaml`                       | Inner aggregator `resources: [operator/, proxy/]`, Phase 7 head comment preserved         | VERIFIED   | REQ-CAPCLU-02 + Pitfall 7 attribution preserved + Phase 8 update note appended                                                                                                                                |
| `clusters/hub-flux/pocs/capsule.yaml` (HUB-TARGETED)    | Outer Flux Kustomization with NO `spec.kubeConfig` (D-08-12 hub-targeted)                 | VERIFIED   | **G-04 CLOSED per D-08-12**: `yq eval '.spec.kubeConfig'` returns `null`. Head comment evolved to attribute D-08-12 + cite server-side dry-run evidence + record Phase 7 invariant evolution. `spec` ends with `sourceRef.name: flux-system`. |
| `clusters/hub-flux/pocs/capsule-spoke.yaml` (NEW, SPOKE-TARGETED) | Outer Flux Kustomization with full `spec.kubeConfig.secretRef={name,key}` + `spec.path: ./pocs/capsule/spoke` (D-08-12 spoke-targeted) | VERIFIED   | NEW file (created in commit `a9af679`). yq verifies: `.spec.kubeConfig.secretRef.name == "spoke-capsule-kubeconfig"` AND `.key == "value.yaml"` AND `.spec.path == "./pocs/capsule/spoke"` AND `.metadata.name == "poc-capsule-spoke"`. Phase 7 P40/P18 invariant inherited (split form). |
| `clusters/hub-flux/pocs/kustomization.yaml`             | Pocs aggregator referencing both `capsule.yaml` AND `capsule-spoke.yaml`                  | VERIFIED   | `yq eval '.resources'` returns `[capsule.yaml, capsule-spoke.yaml]` (length 2). Head comment attributes D-08-12 split-path pattern.                                                                            |
| `pocs/capsule/spoke/kustomization.yaml` (NEW, PLACEHOLDER) | Pitfall-7 placeholder for spoke-targeted resources (Phase 9 populates)                  | VERIFIED   | NEW file. `yq eval '.resources'` returns `[]`. Head comment attributes D-08-12 + Pitfall-7 defense + Phase 9 forward-pointer.                                                                                  |
| `tests/bats/capsule-install-{01..05}-static-*.bats`     | 5 static contract bats files                                                              | VERIFIED   | 39/39 GREEN end-to-end (capsule-install-04 expanded from 5 → 6 tests per WR-01/WR-02 fixes + new D-08-11 cert flag pair test).                                                                                  |
| `tests/bats/capsule-install-{06..10}-live-*.bats`       | 5 live observability bats files                                                           | VERIFIED   | All exist with cluster-info gates. 06 hardened with WR-03 jsonpath; 09 hardened with WR-04 NotFound distinction.                                                                                              |
| `tests/bats/poc-mount-01-static.bats` + `poc-cluster-01-static.bats` | Phase 7 invariant evolution recorded (split-K assertions per D-08-12)         | VERIFIED   | Both files updated (Rule 1 deviation auto-fix in commit `a9af679` for poc-cluster-01-static.bats). 10/10 + 12/12 GREEN.                                                                                       |

### Key Link Verification

| From                                          | To                                              | Via                                            | Status      | Details                                                                                                                          |
| --------------------------------------------- | ----------------------------------------------- | ---------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------- |
| operator/helmrelease.yaml                     | operator/ocirepository.yaml                     | spec.chartRef.kind=OCIRepository + name=capsule | WIRED       | yq verifies                                                                                                                      |
| proxy/helmrelease.yaml                        | proxy/ocirepository.yaml                        | spec.chartRef.kind=OCIRepository + name=capsule-proxy | WIRED       | yq verifies                                                                                                                      |
| operator/helmrelease.yaml                     | spoke-capsule-kubeconfig Secret                 | spec.kubeConfig.secretRef.name + key=value.yaml | WIRED       | P18/P40 honored on the HR itself (HR.kubeConfig redirects helm-install to spoke; Phase 7 invariant unchanged)                    |
| proxy/helmrelease.yaml                        | spoke-capsule-kubeconfig Secret                 | spec.kubeConfig.secretRef.name + key=value.yaml | WIRED       | Same                                                                                                                            |
| proxy/helmrelease.yaml                        | operator/helmrelease.yaml                       | spec.dependsOn[0].name=capsule + namespace=capsule-system | WIRED       | yq verifies; Pitfall 9 honored (no `wait:` field)                                                                                |
| pocs/capsule/kustomization.yaml               | operator/ + proxy/                              | resources: [operator/, proxy/]                 | WIRED       | static bats GREEN                                                                                                                |
| **clusters/hub-flux/pocs/capsule.yaml (HUB)** | **pocs/capsule/operator/ + pocs/capsule/proxy/ (HR + OCIRepo CRs land on HUB)** | **kustomize-controller flux-system in-cluster SA (NO kubeConfig)** | **WIRED (G-04 CLOSED)** | **D-08-12 architectural seam fix**: outer K has no `spec.kubeConfig`; CRs apply to hub where helm-controller exists. Server-side dry-run confirms HR shape valid on hub-flux. |
| **clusters/hub-flux/pocs/capsule-spoke.yaml (SPOKE)** | **pocs/capsule/spoke/ (Phase 9 Tenant CR landing pad)** | **kustomize-controller impersonating spoke via spoke-capsule-kubeconfig** | **WIRED** | NEW spoke-targeted K. Reconciles `pocs/capsule/spoke/kustomization.yaml` (resources: []) — Pitfall-7 placeholder until Phase 9. |
| pocs/capsule/proxy/helmrelease.yaml (D-08-11) | capsule-proxy-certgen Job (controller-less)     | options.generateCertificates=true + certManager.generateCertificates=false | WIRED (G-03 CLOSED) | helm-template confirms: 0 Issuer + 0 Certificate + 2 Jobs (capsule-proxy-certgen named) renders correctly                       |

### Data-Flow Trace (Level 4)

| Artifact                                | Data Variable                       | Source                                                                       | Produces Real Data | Status        |
| --------------------------------------- | ----------------------------------- | ---------------------------------------------------------------------------- | ------------------ | ------------- |
| `pocs/capsule/operator/helmrelease.yaml` | HelmRelease CR install state         | hub-flux helm-controller (post-push, post-reconcile)                         | Pending push (D-08-13 deferral) | DEFERRED — but G-04 architectural blocker now CLOSED, so reconcile path is unblocked |
| `pocs/capsule/proxy/helmrelease.yaml`    | HelmRelease CR install state + TLS Secret | hub-flux helm-controller + capsule-proxy-certgen Job (controller-less Secret materialization) | Pending push (D-08-13 deferral) | DEFERRED — but G-03 cert-source blocker now CLOSED per D-08-11; Secret will materialize from certgen Job, not absent cert-manager controller |
| `clusters/hub-flux/pocs/capsule.yaml` (HUB) | Kustomization Ready=True             | hub-flux kustomize-controller (currently against pre-Phase-8 source `32e0b2fa`) | YES — currently Ready=True against empty `pocs/capsule/kustomization.yaml: resources: []`. Once push lands, will apply HR + OCIRepo to hub-flux successfully (G-04 architecture corrected). | FLOWING (will continue post-push) |
| `clusters/hub-flux/pocs/capsule-spoke.yaml` (SPOKE) | Kustomization Ready=True             | hub-flux kustomize-controller post-push (impersonating spoke via kubeConfig) | Pending push — Pitfall-7 placeholder ensures Ready=True against empty `pocs/capsule/spoke/`. | DEFERRED |

### Behavioral Spot-Checks

| Behavior                                                                        | Command                                                                                                          | Result                                                                                                                                                                                                              | Status |
| ------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| All 64 static bats GREEN (post-08-04 expansion)                                 | `bats tests/bats/capsule-install-0[12345]-static-*.bats tests/bats/poc-{mount,isolation,cluster}-01-static.bats` | 64/64 ok, 0 failures                                                                                                                                                                                                | PASS   |
| **G-03 CLOSED**: D-08-11 cert flag pair on proxy HR                             | `yq eval '.spec.values.options.generateCertificates'` && `yq eval '.spec.values.certManager.generateCertificates'` | `true` / `false`                                                                                                                                                                                                    | PASS   |
| **G-03 CLOSED**: helm-template renders certgen Job (not Issuer/Certificate CRs) | `helm template capsule-proxy oci://ghcr.io/projectcapsule/charts/capsule-proxy --version 0.12.0 --set options.generateCertificates=true --set certManager.generateCertificates=false` | 0 cert-manager.io Issuer + 0 Certificate + 2 Jobs (capsule-proxy-certgen + capsule-proxy-crds). Job name `capsule-proxy-certgen` matches production releaseName.                                                    | PASS   |
| **G-04 CLOSED**: hub-targeted K has no kubeConfig                               | `yq eval '.spec.kubeConfig' clusters/hub-flux/pocs/capsule.yaml`                                                | `null`                                                                                                                                                                                                              | PASS   |
| **G-04 CLOSED**: spoke-targeted K exists with full kubeConfig+key+path          | `yq eval` on capsule-spoke.yaml                                                                                  | `secretRef.name=spoke-capsule-kubeconfig`, `key=value.yaml`, `path=./pocs/capsule/spoke`                                                                                                                            | PASS   |
| **G-04 CLOSED**: pocs aggregator references both                                 | `yq eval '.resources' clusters/hub-flux/pocs/kustomization.yaml`                                                | length 2; `[capsule.yaml, capsule-spoke.yaml]`                                                                                                                                                                       | PASS   |
| **G-04 CLOSED**: server-side dry-run on hub succeeds (only ns missing)          | `kubectl --context=k3d-hub-flux apply --dry-run=server -f pocs/capsule/operator/helmrelease.yaml`               | "Error from server (NotFound): namespaces \"capsule-system\" not found" (NOT "no matches for kind HelmRelease"). HR shape is valid on hub.                                                                          | PASS   |
| **D-08-13 honored**: push gate not yet cleared                                  | `git rev-parse HEAD; git rev-parse origin/main; git log --oneline origin/main..HEAD \| wc -l`                   | HEAD=`0b8f3fc7`; origin/main=`32e0b2fa`; 35 commits ahead of origin. Push event remains gated on Phase 11 VAL-05.                                                                                                  | INFO   |
| **D-08-13 honored**: live HRs absent (push not satisfied)                       | `flux get helmrelease -A --context k3d-hub-flux`                                                                | "no HelmRelease objects found in any namespace" — must-haves #2/#3/#4/#5 are PASSED-with-override per D-08-13 deferral.                                                                                              | INFO   |
| ADR-004 invariant: no Flux on spoke                                             | `kubectl --context=k3d-spoke-capsule get pods -n flux-system`                                                  | "No resources found in flux-system namespace."                                                                                                                                                                       | PASS   |
| Outer poc-capsule Kustomization Ready=True (folded carryover)                   | `flux get kustomization poc-capsule -n flux-system --context k3d-hub-flux`                                      | Ready=True; revision `main@sha1:32e0b2fa` (post-Phase-7 source); MESSAGE: "Applied revision: main@sha1:32e0b2fa"                                                                                                    | PASS   |

### Requirements Coverage

| Requirement | Source Plan(s)                                | Description                                                                                                                                                                                                                                                                                                                  | Status     | Evidence                                                                                                                                                              |
| ----------- | --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CAP-01      | 08-00, 08-01, 08-03, **08-04**                | Capsule operator (`capsule:0.12.4`) installed via Flux HelmRelease on hub-flux, reconciling to spoke-capsule via `spec.kubeConfig` — HelmRelease `Ready=True` and `capsule-controller-manager` pod Running on spoke                                                                                                              | SATISFIED (structural) + DEFERRED (runtime) | Yaml shape verified (64/64 static bats GREEN). G-04 architectural seam CLOSED per D-08-12 — HR shape now applies to hub-flux correctly (server-side dry-run confirmed). Runtime install evidence (HR Ready=True + pod Running) deferred to Phase 11 VAL-05 per D-08-13 (override accepted). |
| CAP-02      | 08-00, 08-02, 08-03, **08-04**                | capsule-proxy (separate HelmRelease, NOT umbrella) installed with `service.type: NodePort + service.nodePort: 30443` and `additionalSANs: [127.0.0.1, localhost]` — reachable via `curl -k https://127.0.0.1:30443/healthz`                                                                                                  | SATISFIED (structural) + DEFERRED (runtime) | Yaml shape verified. G-03 BL-01 cert-source blocker CLOSED per D-08-11 — helm-template confirms controller-less certgen Job materializes Secret. G-04 architecture CLOSED. Runtime probe deferred to Phase 11 VAL-05 (override accepted). |
| CAP-03      | 08-00, 08-01, 08-02, 08-03                    | Capsule CRDs (Tenant, CapsuleConfiguration, GlobalTenantResource, TenantResource, ResourcePool, ResourcePoolClaim, TenantOwner) installed and visible on spoke-capsule                                                                                                                                                       | SATISFIED (structural) + DEFERRED (runtime) | Yaml shape configures `install.crds: CreateReplace` correctly. Operator HR architecture corrected (G-04 closed) — CRDs will register on spoke once HR reconciles. Live `kubectl get crds` count deferred to Phase 11 VAL-05 (override accepted). |

**Orphaned requirements:** None — all 3 Phase 8 REQ-IDs (CAP-01, CAP-02, CAP-03) claimed by plans 08-00..08-04. REQUIREMENTS.md Phase 8 mapping confirmed.

### Anti-Patterns Found

| File                                              | Line(s) | Pattern                                                                                                                            | Previous Severity | Current Severity | Disposition |
| ------------------------------------------------- | ------- | ---------------------------------------------------------------------------------------------------------------------------------- | ----------------- | ---------------- | ----------- |
| `pocs/capsule/proxy/helmrelease.yaml`             | 75-76 (prior) | `certManager.generateCertificates: true` chosen with comment claiming "works WITHOUT cert-manager controller installed" | BLOCKER (BL-01 / G-03) | **RESOLVED**     | **CLOSED in commit `75e5b78`** — D-08-11 flip applied. Lines 60-77 of the new file cleanly attribute D-08-11 + 08-VERIFICATION.md G-03 + 08-REVIEW.md BL-01 + helm-template evidence. Comment block rewritten (no internal contradiction; no fallback framing). |
| `tests/bats/capsule-install-04-static-no-umbrella.bats` | 15-21 (prior) | `grep -F 'proxy.enabled: true'` — vacuous dotted-key literal lint                                                                | WARNING (WR-01)   | **RESOLVED**     | **CLOSED in commit `75e5b78`** — replaced with `yq eval '[.. \| select(has("proxy")) \| .proxy.enabled] \| any_c(. == true)'` (mikefarah yq syntax) recursive descent path negative assertion. |
| `tests/bats/capsule-install-04-static-no-umbrella.bats` | 30-34 (prior) | `grep -F 'certManager.generateCertificates: true'` — vacuous dotted-key lint                                                       | WARNING (WR-02)   | **RESOLVED**     | **CLOSED in commit `75e5b78`** — replaced with `yq eval '(.spec.values.certManager.generateCertificates // false) == false'` positive assertion on operator HR. New D-08-11 cert flag pair test added for proxy HR (test 4). |
| `tests/bats/capsule-install-06-live-helm.bats`    | 23, 36 (prior) | Regex `<name>[[:space:]]+.*True` admits MESSAGE-column false positives                                                            | WARNING (WR-03)   | **RESOLVED**     | **CLOSED in commit `36f1cfd`** — replaced with `kubectl ... -o jsonpath='{.status.conditions[?(@.type=="Ready")].status}'` column-anchored parsing. Both CAP-01 + CAP-02 tests reframed. |
| `tests/bats/capsule-install-09-live-adr-004.bats` | 16-24 (prior) | Silent-pass-on-any-non-zero-kubectl-exit (transient apiserver errors → silent pass)                                              | WARNING (WR-04)   | **RESOLVED**     | **CLOSED in commit `36f1cfd`** — explicit `kubectl get ns flux-system -o jsonpath='{.status.phase}' 2>&1` + `grep -F 'NotFound'` discrimination. NotFound → PASS; transient → FAIL. |
| `pocs/capsule/proxy/helmrelease.yaml`             | 68-76 (prior) | Block comment internally inconsistent (claims chart works without controller, then describes alternative as "fallback")           | WARNING (WR-06)   | **RESOLVED**     | **CLOSED in commit `75e5b78`** — comment block rewritten to attribute D-08-11 + cite helm-template evidence. No more fallback framing. |
| `pocs/capsule/operator/helmrelease.yaml`, `pocs/capsule/proxy/helmrelease.yaml` | 37-40, 39-42 | HR `spec.kubeConfig` redirect assumes HR resides on hub; outer K applied CRs to spoke | BLOCKER (G-04 / WR-05) | **RESOLVED**     | **CLOSED in commit `a9af679`** — D-08-12 split-path architecture implemented. Hub-targeted K (capsule.yaml) drops kubeConfig; new spoke-targeted K (capsule-spoke.yaml) carries it for Phase 9 Tenant CRs. Server-side dry-run on hub-flux confirms HR shape valid: returns "namespaces capsule-system not found" (expected ns-existence failure), NOT "no matches for kind HelmRelease". Phase 7 P40/P18 invariant evolution recorded in head comments + bats updated. |
| `tests/bats/capsule-install-08-live-proxy-reachable.bats` | 28-31 | bash-only `/dev/tcp` magic with explicit `bash -c` wrapper                                                                       | INFO (IN-01)      | UNCHANGED        | Not in 08-04 scope; informational. Could be addressed in future cleanup. |
| `pocs/capsule/proxy/helmrelease.yaml`             | 63 (now 82) | `options.capsuleConfigurationName: default` references CR not committed in Phase 8                                              | INFO (IN-02)      | UNCHANGED        | Chart auto-creates `CapsuleConfiguration/default`; not independently verified. Phase 9 owns explicit CR landing. |
| `tests/bats/capsule-install-06-live-helm.bats`    | 21, 33 (prior) | 3 × 30s = 90s retry budget tight for first install with cold image cache                                                          | INFO (IN-03)      | UNCHANGED        | Same retry budget retained in WR-03 fix. May flake on cold installs; not a correctness defect. |
| `pocs/capsule/operator/kustomization.yaml`, `pocs/capsule/proxy/kustomization.yaml` | 10 | `namespace: capsule-system` directive unconditionally rewrites resource namespaces                                                 | INFO (IN-04)      | UNCHANGED        | Non-intuitive Kustomize override; not in 08-04 scope. |

**Summary:** 2 BLOCKERs CLOSED (G-03/BL-01, G-04/WR-05); 4 WARNINGs CLOSED (WR-01, WR-02, WR-03, WR-04, WR-06); 4 INFO unchanged (out of 08-04 scope; informational). Zero new anti-patterns introduced by 08-04 commits.

### Human Verification Required

None — all gap-close items are programmatically verifiable. The deferred push-gate-downstream truths (must-haves #2/#3/#4/#5) will become observable once Phase 11 VAL-05 push lands, at which point a future re-verification (or VAL-05 verifier) will confirm: HR Ready=True, operator pod Running, ≥7 CRDs registered, NodePort 30443 reachable.

### Gaps Summary

**Re-verification result: All Phase 8 BLOCKER + WARNING gaps from the prior verification are now CLOSED by plan 08-04.**

- **G-03 (BL-01) — cert source incompatible with D-08-04**: CLOSED per D-08-11 in commit `75e5b78`. Proxy HelmRelease lines 60-77 now carry `options.generateCertificates: true` + `certManager.generateCertificates: false` with a clean comment block citing helm-template evidence. Verified live: `helm template capsule-proxy oci://ghcr.io/projectcapsule/charts/capsule-proxy --version 0.12.0 --set options.generateCertificates=true --set certManager.generateCertificates=false` renders 0 cert-manager.io Issuer + 0 Certificate + 2 Jobs (capsule-proxy-certgen using kube-webhook-certgen image, controller-less). Once Phase 11 VAL-05 push lands, the proxy pod will materialize its TLS Secret directly via the certgen Job — no absent controller dependency.

- **G-04 (WR-05) — Architectural seam defect (outer K → spoke applies HR CRs to wrong cluster)**: CLOSED per D-08-12 in commit `a9af679`. The outer Flux Kustomization is now split: `clusters/hub-flux/pocs/capsule.yaml` (hub-targeted, `spec.kubeConfig` REMOVED, applies HR + OCIRepo to hub-flux where helm-controller exists) + `clusters/hub-flux/pocs/capsule-spoke.yaml` (NEW, spoke-targeted, full `spec.kubeConfig.secretRef={name:spoke-capsule-kubeconfig,key:value.yaml}` block, `spec.path: ./pocs/capsule/spoke` for Phase 9 Tenant CRs). The pocs aggregator references both. Pitfall-7 placeholder `pocs/capsule/spoke/kustomization.yaml` (`resources: []`) ensures the spoke-targeted K reconciles successfully against an empty path until Phase 9 populates it. Server-side dry-run of operator HR against hub-flux returns "namespaces capsule-system not found" (NOT "no matches for kind HelmRelease") — confirms HR shape is valid on hub. Phase 7 P40/P18 invariant evolution recorded in head comments of capsule.yaml + capsule-spoke.yaml: "Spoke-targeted Kustomizations MUST include the entire spec.kubeConfig block; hub-targeted Kustomizations MUST NOT include it."

- **WR-01 + WR-02 (vacuous bats greps)**: CLOSED per commit `75e5b78`. `tests/bats/capsule-install-04-static-no-umbrella.bats` now uses yq recursive-descent path assertions (mikefarah yq syntax: `[.. | select(has("proxy")) | .proxy.enabled] | any_c(. == true)`) instead of impossible dotted-key literal greps. Test count expanded from 5 → 6 (added D-08-11 cert flag pair positive assertion).

- **WR-03 (Ready=True regex MESSAGE-column false positives)**: CLOSED per commit `36f1cfd`. `tests/bats/capsule-install-06-live-helm.bats` now uses `kubectl ... -o jsonpath='{.status.conditions[?(@.type=="Ready")].status}'` column-anchored parsing for both CAP-01 and CAP-02 HR Ready tests.

- **WR-04 (NotFound vs transient kubectl error confusion)**: CLOSED per commit `36f1cfd`. `tests/bats/capsule-install-09-live-adr-004.bats` now uses explicit `kubectl get ns flux-system -o jsonpath='{.status.phase}' 2>&1` + `grep -F 'NotFound'` discrimination — namespace truly absent → PASS; transient apiserver/RBAC error → FAIL.

- **G-01 / G-02 (push gate)**: PASSED with override per D-08-13 (08-CONTEXT.md lines 108-112). Plan 08-04 deliberately excludes a `git push` step. Phase 11 VAL-05 owns the first-push event. The 4 push-gate-downstream must-haves (#2/#3/#4/#5) are documented as DEFERRED-WITH-OVERRIDE in the frontmatter, with milestone-owner acceptance recorded. Live state confirms deferral honored: HEAD=`0b8f3fc7` is 35 commits ahead of origin/main=`32e0b2fa`; hub-flux GitRepository revision is `32e0b2fa` (pre-Phase-8 close-out source); no HelmReleases reconciled yet; outer poc-capsule Kustomization Ready=True against empty pre-Phase-8 `resources: []`.

**Phase 7 invariant evolution disposition:** Recorded directly in tree (in capsule.yaml + capsule-spoke.yaml head comments + plan 08-04 SUMMARY's `tech-stack.patterns` block + bats `poc-mount-01-static.bats` + `poc-cluster-01-static.bats` updated assertions). The evolved invariant is now the load-bearing form: the silent-misroute fault is "absence of `spec.kubeConfig` on a SPOKE-targeted Kustomization", not "absence of `spec.kubeConfig` per se". Phase 9 inherits this evolved form when populating `pocs/capsule/spoke/`.

**Phase 11 VAL-05 forward expectation:** When the push lands, Phase 11 verifier should observe: outer K `poc-capsule` Ready=True against post-Phase-8 source revision; outer K `poc-capsule-spoke` Ready=True against post-Phase-8 source revision; both HelmReleases (`capsule`, `capsule-proxy`) Ready=True on hub-flux; capsule-controller-manager pod Running on spoke-capsule (capsule-system); ≥7 capsule.clastix.io CRDs registered on spoke; capsule-proxy pod Running with TLS Secret materialized from capsule-proxy-certgen Job; NodePort 30443 reachable from WSL host with TLS handshake + HTTP `[1-5][0-9][0-9]`. None of these require additional Phase 8 work — all structural enablers are in tree.

---

_Verified: 2026-04-29T22:30:00Z_
_Verifier: Claude (gsd-verifier) — re-verification after gap-close (plan 08-04)_
