---
phase: 09-tenants-flux-multi-tenancy-lockdown
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/bats/tenants-01-static-tenant-cr.bats
  - tests/bats/tenants-02-static-p27.bats
  - tests/bats/tenants-03-static-lockdown.bats
  - tests/bats/tenants-04-static-dependson.bats
  - tests/bats/tenants-05-static-split-path.bats
  - tests/bats/tenants-06-live-tenant-cr.bats
  - tests/bats/tenants-07-live-impersonate.bats
  - tests/bats/tenants-08-live-quota-limit.bats
  - tests/bats/tenants-09-live-inner-k-ready.bats
  - tests/bats/tenants-10-live-lockdown.bats
  - tests/bats/tenants-11-live-dependson.bats
  - tests/bats/tenants-12-live-health-check.bats
  - tests/fixtures/p27-broken/tenant-broken.yaml
autonomous: true
requirements: [TEN-01, TEN-02, TEN-03, TEN-04, TEN-05, TEN-06]

must_haves:
  truths:
    - "All 12 Phase 9 bats files exist and are RED at end of plan (per D-09-08; mirrors Phase 7 / Phase 8 Wave 0 pattern)"
    - "Static bats fail with file-not-found errors (target manifest yaml does not exist yet — populated by Plans 09-01 + 09-02 + 09-03)"
    - "Live bats are cluster-info gated and skip when k3d-spoke-capsule unreachable, fail when reachable but Tenants/CRs absent"
    - "Synthetic broken-fixture for P27 falsifier exists at tests/fixtures/p27-broken/tenant-broken.yaml (yq lint MUST flag this fixture as missing serviceAccountName)"
    - "All bats files preserve Phase 7+8 conventions: load 'test_helper'; cluster-info gating per file; 3×30s retry on jsonpath assertions"
  artifacts:
    - path: "tests/bats/tenants-01-static-tenant-cr.bats"
      provides: "TEN-01 Tenant CR yaml shape contract (apiVersion v1beta2 + Tenant kind + alpha/bravo names + spec.forceTenantPrefix top-level + owners[SA combined-name + User]) — RESEARCH Gap 4 corrections"
      contains: "apiVersion == \"capsule.clastix.io/v1beta2\""
    - path: "tests/bats/tenants-02-static-p27.bats"
      provides: "TEN-04 P27 LOAD-BEARING — yq-based lint that every Kustomization in pocs/capsule/tenants/*.yaml has non-null spec.serviceAccountName + negative falsifier against tests/fixtures/p27-broken/"
      contains: "spec.serviceAccountName"
    - path: "tests/bats/tenants-03-static-lockdown.bats"
      provides: "TEN-05 lockdown — FLUX PATCH SURFACE comment block preserved verbatim; KARYON SPOKES + POC MOUNT sentinels present; patches: block contains 3 lockdown flags + 4 patch targets (kustomize-controller, helm-controller, notification-controller, flux-system Kustomization)"
      contains: "--no-cross-namespace-refs=true"
    - path: "tests/bats/tenants-04-static-dependson.bats"
      provides: "TEN-06 P32 — capsule-spoke.yaml dependsOn[0].name=poc-capsule + spec.wait=true (TOP-LEVEL per RESEARCH Gap 9); per-tenant inner Ks dependsOn[0].name=poc-capsule + namespace=flux-system"
      contains: "dependsOn"
    - path: "tests/bats/tenants-05-static-split-path.bats"
      provides: "D-08-12 inheritance regression — capsule.yaml NO kubeConfig + serviceAccountName=kustomize-controller; capsule-spoke.yaml HAS kubeConfig with key=value.yaml (P40/P18)"
      contains: "spec.kubeConfig"
    - path: "tests/bats/tenants-06-live-tenant-cr.bats"
      provides: "TEN-01 live — kubectl get tenants alpha bravo reports forceTenantPrefix=true; gitops-reconciler SA exists in tenant-{alpha,bravo} ns on spoke-capsule; 3×30s retry"
      contains: "k3d-spoke-capsule"
    - path: "tests/bats/tenants-07-live-impersonate.bats"
      provides: "TEN-02 live — kubectl --as=alpha create namespace alpha-app1 succeeds (idempotent) + capsule.clastix.io/tenant=alpha label stamped; mirror for bravo"
      contains: "--as=alpha"
    - path: "tests/bats/tenants-08-live-quota-limit.bats"
      provides: "TEN-03 live — ResourceQuota + LimitRange auto-materialize in alpha-app1/bravo-app1 within 10s of namespace creation (capsule-{alpha,bravo}-* names)"
      contains: "resourcequota"
    - path: "tests/bats/tenants-09-live-inner-k-ready.bats"
      provides: "TEN-04 reconcile + Secret-mirror — tenant inner Ks Ready=True against empty placeholder paths (skip on pre-push GitRepository ArtifactFailed); spoke-capsule-kubeconfig Secret exists in tenant-{alpha,bravo} ns on hub (D-09-03a per RESEARCH Gap 1)"
      contains: "Ready"
    - path: "tests/bats/tenants-10-live-lockdown.bats"
      provides: "TEN-05 live — kustomize-controller args contain 3 flags; helm-controller args contain 2 flags; notification-controller args contain 1 flag; all 3 controllers availableReplicas==replicas (no CrashLoopBackOff); flux-system Kustomization Ready=True (escape hatch validation)"
      contains: "kustomize-controller"
    - path: "tests/bats/tenants-11-live-dependson.bats"
      provides: "TEN-06 live — poc-capsule-spoke dependsOn[0].name=poc-capsule + Ready=True post-lockdown"
      contains: "poc-capsule-spoke"
    - path: "tests/bats/tenants-12-live-health-check.bats"
      provides: "v0.18 regression gate — task health-check exits 0 post-lockdown; targeted assertions for kustomize-controller + spoke-apps + spoke-ml Kustomizations Ready"
      contains: "task health-check"
    - path: "tests/fixtures/p27-broken/tenant-broken.yaml"
      provides: "P27 negative falsifier — synthetic Kustomization without spec.serviceAccountName for tenants-02 to lint against"
      contains: "kind: Kustomization"
  key_links:
    - from: "tests/bats/tenants-02-static-p27.bats"
      to: "tests/fixtures/p27-broken/tenant-broken.yaml"
      via: "yq lint loop"
      pattern: "p27-broken"
    - from: "tests/bats/tenants-12-live-health-check.bats"
      to: "scripts/health-check.sh (via Taskfile.yml)"
      via: "task health-check invocation"
      pattern: "task health-check"
---

<objective>
Plan 09-00 — Wave 0 RED bats scaffold.

Land all 12 Phase 9 bats files (5 static + 7 live, 1 health-check) in `tests/bats/` per D-09-08 + VALIDATION.md per-task verification map, plus a synthetic broken P27 fixture for the negative falsifier. All bats are RED at end of plan: static bats fail because target manifest yaml does not exist yet (populated by 09-01..09-03); live bats are cluster-info gated and skip when k3d-spoke-capsule is unreachable, fail when reachable but Tenants/CRs absent. Mirrors Phase 7 Plan 07-00 / Phase 8 Plan 08-00 Wave 0 RED scaffold pattern.

Purpose: Nyquist gate. Every TEN-01..06 contract has a falsifier in place BEFORE any tenant yaml lands. Subsequent plans turn each bats GREEN exactly once when the corresponding manifest is committed.

Output: 12 new bats files + 1 fixture, all committed, all RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-CONTEXT.md
@.planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-RESEARCH.md
@.planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-VALIDATION.md
@.planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-PATTERNS.md
@tests/bats/test_helper.bash
@tests/bats/capsule-install-01-static-helmrelease.bats
@tests/bats/capsule-install-04-static-no-umbrella.bats
@tests/bats/capsule-install-06-live-helm.bats
@tests/bats/capsule-install-07-live-crds.bats
@tests/bats/capsule-install-10-live-flux-observable.bats
@tests/bats/poc-mount-01-static.bats
@tests/bats/poc-isolation-01-static.bats

<interfaces>
<!-- Key bats patterns the executor will mirror. Extracted from existing capsule-install-* and poc-* bats. -->
<!-- Executor uses these directly — no codebase exploration needed. -->

From tests/bats/test_helper.bash:
```bash
# REPO_ROOT resolves the top of the repo from any bats suite.
REPO_ROOT="$(cd "$(dirname "${BATS_TEST_FILENAME:-$0}")/../.." && pwd)"
```

From tests/bats/capsule-install-01-static-helmrelease.bats (yq compound assertion pattern):
```bash
load 'test_helper'
setup() {
  OPERATOR_HR="${REPO_ROOT}/pocs/capsule/operator/helmrelease.yaml"
}
@test "CAP-01 / D-08-06: ... interval 5m" {
  [ -f "$OPERATOR_HR" ]
  run yq eval '.apiVersion == "..." and .kind == "..." and .metadata.name == "..."' "$OPERATOR_HR"
  [ "$status" -eq 0 ]
  [ "$output" = "true" ]
}
```

From tests/bats/capsule-install-06-live-helm.bats (cluster-info gate + 3×30s retry pattern):
```bash
load 'test_helper'
setup() {
  if ! kubectl --context=k3d-hub-flux cluster-info >/dev/null 2>&1; then
    skip "k3d-hub-flux cluster not reachable; skipping live bats"
  fi
  if ! kubectl --context=k3d-spoke-capsule cluster-info >/dev/null 2>&1; then
    skip "k3d-spoke-capsule cluster not reachable; skipping live bats"
  fi
}
@test "CAP-01 live (3×30s retry, jsonpath column-anchored)" {
  local i ready
  for i in 1 2 3; do
    ready=$(kubectl --context=k3d-hub-flux -n capsule-system get helmrelease capsule \
      -o jsonpath='{.status.conditions[?(@.type=="Ready")].status}' 2>/dev/null || true)
    [[ "$ready" == "True" ]] && return 0
    [[ $i -lt 3 ]] && sleep 30
  done
  echo "Last observed Ready condition: '$ready'"
  return 1
}
```

From tests/bats/capsule-install-10-live-flux-observable.bats (pre-push skip pattern):
```bash
@test "FOLDED CARRYOVER: post-Phase-7 push gate — git log origin/main..main is empty" {
  if ! git -C "${REPO_ROOT}" rev-parse origin/main >/dev/null 2>&1; then
    skip "origin/main not tracked locally; skipping push-gate"
  fi
  run bash -c "cd '${REPO_ROOT}' && git log origin/main..main --oneline"
  [ "$status" -eq 0 ]
  if [ -n "$output" ]; then
    skip "Local commits not pushed to origin/main yet; deferred-to-VAL-05"
  fi
}
```

From tests/bats/poc-mount-01-static.bats (sentinel preservation grep pattern):
```bash
@test "POC-01 / D-09: # KARYON POC MOUNT sentinel exists in clusters/hub-flux/flux-system/kustomization.yaml" {
  [ -f "$HUB_FS_KUST" ]
  run grep -F -- '# KARYON POC MOUNT' "$HUB_FS_KUST"
  [ "$status" -eq 0 ]
}
```

From tests/bats/capsule-install-04-static-no-umbrella.bats (yq path negative assertion):
```bash
@test "Anti-umbrella: operator HelmRelease has NO values.proxy.enabled=true at any depth" {
  run yq eval '[.. | select(has("proxy")) | .proxy.enabled] | any_c(. == true)' "$OPERATOR_HR"
  [ "$status" -eq 0 ]
  [ "$output" = "false" ]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write 5 static bats files (TEN-01 + TEN-04 P27 + TEN-05 lockdown + TEN-06 dependsOn + D-08-12 split-path) + P27 negative fixture</name>
  <files>
    tests/bats/tenants-01-static-tenant-cr.bats,
    tests/bats/tenants-02-static-p27.bats,
    tests/bats/tenants-03-static-lockdown.bats,
    tests/bats/tenants-04-static-dependson.bats,
    tests/bats/tenants-05-static-split-path.bats,
    tests/fixtures/p27-broken/tenant-broken.yaml
  </files>
  <read_first>
    - .planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-CONTEXT.md (D-09-08 Wave 0 pattern + bats coverage list lines 401-413; D-09-02 Tenant CR shape; D-09-05 lockdown patches)
    - .planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-RESEARCH.md §"Bats Test Coverage Recommendations" lines 1213-1269 + §Gap 4 (Tenant CR shape lines 311-372) + §Gap 5 (CapsuleConfiguration lines 408-453) + §Gap 2 (per-controller flag matrix lines 188-238)
    - .planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-VALIDATION.md lines 41-50 (per-task verification map; static bats targets) + lines 67-81 (Wave 0 requirements)
    - .planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-PATTERNS.md "Bats Tests" section lines 736-893 (file-by-file analog patterns)
    - tests/bats/test_helper.bash (load 'test_helper' + REPO_ROOT pattern)
    - tests/bats/capsule-install-01-static-helmrelease.bats (yq compound assertion analog for tenants-01)
    - tests/bats/capsule-install-04-static-no-umbrella.bats (yq path-recursive-negative analog; multi-doc pattern adapter for tenants-02 P27 lint)
    - tests/bats/poc-mount-01-static.bats (sentinel grep + split-path verification analogs for tenants-03 lockdown + tenants-05 split-path)
  </read_first>
  <action>
Create the 5 static bats files + 1 fixture. Each bats file MUST:
1. Start with `#!/usr/bin/env bats` shebang
2. Include header comment block: `# tests/bats/tenants-NN-static-...bats` + `# Phase 9 / Wave 0 (D-09-08 Nyquist gate)` + `# TEN-XX — <one-line>` + `# Plans 09-01..09-03 land the yaml that turns these GREEN.`
3. Include `load 'test_helper'`
4. Use `setup()` to set REPO_ROOT-relative file paths
5. Use `[ -f "$FILE" ]` guard before yq/grep
6. Use `run yq eval '...' "$FILE"` for compound assertions; check `[ "$status" -eq 0 ]` AND `[ "$output" = "true" ]`
7. Use `run grep -F -- 'literal text' "$FILE"` for sentinel/literal grep; `[ "$status" -eq 0 ]`

**File 1: `tests/bats/tenants-01-static-tenant-cr.bats` (TEN-01)** — RESEARCH §"Bats Coverage" lines 1215-1225 + Gap 4 lines 311-372:
- setup(): `ALPHA_TENANT="${REPO_ROOT}/pocs/capsule/spoke/tenants/alpha/tenant.yaml"` + BRAVO_TENANT mirror
- 8 @test blocks (4 alpha + 4 bravo):
  - "TEN-01 / D-09-02 (alpha): apiVersion v1beta2 + kind Tenant + name alpha + forceTenantPrefix TOP-LEVEL=true" — yq: `.apiVersion == "capsule.clastix.io/v1beta2" and .kind == "Tenant" and .metadata.name == "alpha" and .spec.forceTenantPrefix == true`
  - "TEN-01 / RESEARCH Gap 4 (alpha): owners[] contains kind=ServiceAccount + name=system:serviceaccount:tenant-alpha:gitops-reconciler (combined-name format)" — yq: `.spec.owners | map(select(.kind == "ServiceAccount" and .name == "system:serviceaccount:tenant-alpha:gitops-reconciler")) | length == 1`
  - "TEN-01 / D-09-02 (alpha): owners[] contains kind=User + name=alpha (TEN-02 contract)" — yq: `.spec.owners | map(select(.kind == "User" and .name == "alpha")) | length == 1`
  - "TEN-03 / D-09-02 (alpha): resourceQuotas.items[0].hard non-empty AND limitRanges.items[0].limits non-empty (auto-materialization shape)" — yq: `(.spec.resourceQuotas.items[0].hard | length) > 0 and (.spec.limitRanges.items[0].limits | length) > 0`
- Mirror 4 tests for bravo (substitute alpha → bravo throughout)

**File 2: `tests/bats/tenants-02-static-p27.bats` (TEN-04 P27 LOAD-BEARING)** — RESEARCH §"Bats Coverage" lines 1227-1242 + PATTERNS lines 782-810:
- setup(): `TENANTS_DIR="${REPO_ROOT}/pocs/capsule/tenants"` + `BROKEN_FIXTURE="${REPO_ROOT}/tests/fixtures/p27-broken/tenant-broken.yaml"`
- @test "P27 LOAD-BEARING / TEN-04: every Kustomization in pocs/capsule/tenants/*.yaml has non-null spec.serviceAccountName" — multi-doc yq loop:
```bash
[ -d "$TENANTS_DIR" ]
shopt -s nullglob
for f in "$TENANTS_DIR"/*.yaml; do
  [ -f "$f" ] || continue
  doc_count=$(yq eval 'select(.kind == "Kustomization" and .apiVersion == "kustomize.toolkit.fluxcd.io/v1") | .spec.serviceAccountName' "$f" | grep -cv '^---$' || true)
  if [ "$doc_count" -eq 0 ]; then continue; fi
  while IFS= read -r sa; do
    [ "$sa" != "null" ] && [ -n "$sa" ] || { echo "P27 violation in $f: serviceAccountName is '$sa'"; return 1; }
  done < <(yq eval 'select(.kind == "Kustomization" and .apiVersion == "kustomize.toolkit.fluxcd.io/v1") | .spec.serviceAccountName' "$f")
done
```
- @test "P27 negative falsifier: synthetic broken-fixture (no serviceAccountName) MUST FAIL the lint" — invokes the same loop on `$BROKEN_FIXTURE` and asserts `[ "$status" -ne 0 ]` (NOTE: cannot use `return 1` inside @test for negative test; use `run bash -c '<lint loop>'` and assert non-zero exit)

**File 3: `tests/bats/tenants-03-static-lockdown.bats` (TEN-05 lockdown)** — RESEARCH §"Bats Coverage" lines 1244-1253 + Gap 2 (per-controller matrix):
- setup(): `HUB_FS_KUST="${REPO_ROOT}/clusters/hub-flux/flux-system/kustomization.yaml"`
- 9 @test blocks:
  - "FLUX PATCH SURFACE comment block preserved verbatim (head lines 1-13)" — `grep -F -- 'FLUX PATCH SURFACE'` AND `grep -F -- 'Edits here SURVIVE re-bootstrap'` AND `grep -F -- 'See: docs/flux-hub-spoke.md'`
  - "KARYON SPOKES MOUNT sentinel preserved" — `grep -F -- '# KARYON SPOKES MOUNT'` AND `grep -F -- '- ../spokes'`
  - "KARYON POC MOUNT sentinel preserved" — `grep -F -- '# KARYON POC MOUNT'` AND `grep -F -- '- ../pocs'`
  - "KARYON FLUX LOCKDOWN PATCHES sentinel exists (NEW per D-09-05)" — `grep -F -- '# KARYON FLUX LOCKDOWN PATCHES'`
  - "patches: block exists at top level" — `yq eval '.patches | length > 0'` returns true
  - "TEN-05 / D-09-05: --no-cross-namespace-refs=true literal present" — `grep -F -- '--no-cross-namespace-refs=true'`
  - "TEN-05 / D-09-05: --no-remote-bases=true literal present" — `grep -F -- '--no-remote-bases=true'`
  - "TEN-05 / D-09-05: --default-service-account=default literal present" — `grep -F -- '--default-service-account=default'`
  - "TEN-05 / D-09-05 / RESEARCH Gap 2: kustomize-controller Deployment patch exists" — `yq eval '.patches | map(select(.target.kind == "Deployment" and .target.name == "kustomize-controller")) | length == 1'` returns true
  - "TEN-05 / RESEARCH Gap 2: helm-controller Deployment patch exists (extends CONTEXT.md narrowing per upstream canonical)" — `yq eval '.patches | map(select(.target.kind == "Deployment" and .target.name == "helm-controller")) | length == 1'` returns true
  - "TEN-05 / D-09-05: notification-controller Deployment patch exists" — `yq eval '.patches | map(select(.target.kind == "Deployment" and .target.name == "notification-controller")) | length == 1'` returns true
  - "TEN-05 / D-09-05a LOAD-BEARING: flux-system Kustomization patch exists with serviceAccountName: kustomize-controller" — `yq eval '.patches | map(select(.target.kind == "Kustomization" and .target.name == "flux-system")) | length == 1'` returns true

**File 4: `tests/bats/tenants-04-static-dependson.bats` (TEN-06 P32 dependsOn)** — RESEARCH §"Bats Coverage" lines 1255-1261 + RESEARCH §Gap 9 wait:true correction:
- setup(): `CAPSULE_SPOKE="${REPO_ROOT}/clusters/hub-flux/pocs/capsule-spoke.yaml"` + `TENANTS_DIR="${REPO_ROOT}/pocs/capsule/tenants"`
- 4 @test blocks for capsule-spoke.yaml:
  - "TEN-06 / D-09-06: capsule-spoke.yaml dependsOn[0].name == poc-capsule" — yq: `.spec.dependsOn[0].name == "poc-capsule"`
  - "TEN-06 / RESEARCH Gap 9: capsule-spoke.yaml spec.wait == true (TOP-LEVEL, NOT inside dependsOn entry)" — yq: `.spec.wait == true`
  - "TEN-06 / D-09-06: capsule-spoke.yaml spec.timeout == 10m" — yq: `.spec.timeout == "10m"`
  - "TEN-06 / D-09-05: capsule-spoke.yaml spec.serviceAccountName == kustomize-controller (explicit clarity)" — yq: `.spec.serviceAccountName == "kustomize-controller"`
- 2 @test blocks for per-tenant inner Ks under pocs/capsule/tenants/:
  - "TEN-06 / D-09-06: every Kustomization in pocs/capsule/tenants/*.yaml has dependsOn[0].name=poc-capsule + namespace=flux-system" — multi-doc yq loop similar to file 2 P27 pattern
  - "TEN-06 / RESEARCH Gap 9: every Kustomization in pocs/capsule/tenants/*.yaml has spec.wait == true (TOP-LEVEL)" — multi-doc yq loop

**File 5: `tests/bats/tenants-05-static-split-path.bats` (D-08-12 inheritance regression)** — RESEARCH §"Bats Coverage" lines 1263-1269 + PATTERNS lines 870-893:
- setup(): `POCS_CAPSULE="${REPO_ROOT}/clusters/hub-flux/pocs/capsule.yaml"` + `POCS_CAPSULE_SPOKE="${REPO_ROOT}/clusters/hub-flux/pocs/capsule-spoke.yaml"`
- 4 @test blocks:
  - "D-08-12 / hub-targeted: capsule.yaml has NO spec.kubeConfig (Phase 8 split-path invariant)" — yq: `.spec.kubeConfig == null`
  - "D-09-05a / hub-targeted: capsule.yaml has spec.serviceAccountName == kustomize-controller (escape hatch under lockdown)" — yq: `.spec.serviceAccountName == "kustomize-controller"`
  - "D-08-12 / spoke-targeted: capsule-spoke.yaml has spec.kubeConfig.secretRef.name == spoke-capsule-kubeconfig (Phase 7 P40/P18)" — yq: `.spec.kubeConfig.secretRef.name == "spoke-capsule-kubeconfig"`
  - "P40/P18 LOAD-BEARING: capsule-spoke.yaml has spec.kubeConfig.secretRef.key == value.yaml (silent-misroute defense)" — yq: `.spec.kubeConfig.secretRef.key == "value.yaml"`

**File 6: `tests/fixtures/p27-broken/tenant-broken.yaml` (P27 negative falsifier fixture)**:
```yaml
# tests/fixtures/p27-broken/tenant-broken.yaml
# P27 NEGATIVE FALSIFIER FIXTURE — Phase 9 D-09-08 / TEN-04
# This file is intentionally BROKEN: it's a Flux Kustomization CR that omits
# spec.serviceAccountName, which would allow the K to run with the kubeConfig
# Secret's principal (cluster-admin via spoke-capsule-kubeconfig) and bypass
# Capsule's tenant boundary entirely (P27 root cause).
#
# tests/bats/tenants-02-static-p27.bats lints this fixture and MUST FAIL
# on the missing serviceAccountName field. If the bats test ever PASSES this
# fixture, the P27 lint is broken.
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: tenant-broken-no-sa
  namespace: tenant-broken
spec:
  interval: 5m
  path: ./pocs/capsule/tenants/broken-app
  prune: true
  sourceRef:
    kind: GitRepository
    name: tenant-broken-source
  kubeConfig:
    secretRef:
      name: spoke-capsule-kubeconfig
      key: value.yaml
  # NOTE: spec.serviceAccountName INTENTIONALLY OMITTED — P27 falsifier
  dependsOn:
    - name: poc-capsule
      namespace: flux-system
```

After creating the 6 files, run static bats and verify ALL 5 fail (RED) for the expected reason: target manifest yaml does not exist yet. The P27 negative falsifier inside file 2 should PASS (proves the lint catches missing SA).
  </action>
  <verify>
    <automated>cd "${REPO_ROOT:-/home/rich/code/gsd/karyon}" && bats tests/bats/tenants-{01,02,03,04,05}-static-*.bats 2>&1 | tail -30; rc=$?; [ -f tests/bats/tenants-01-static-tenant-cr.bats ] && [ -f tests/bats/tenants-02-static-p27.bats ] && [ -f tests/bats/tenants-03-static-lockdown.bats ] && [ -f tests/bats/tenants-04-static-dependson.bats ] && [ -f tests/bats/tenants-05-static-split-path.bats ] && [ -f tests/fixtures/p27-broken/tenant-broken.yaml ]; echo "Static bats RED as expected: rc=$rc (non-zero is correct at Wave 0)"</automated>
  </verify>
  <done>
    All 5 static bats files exist + p27-broken fixture exists. Files start with `#!/usr/bin/env bats`. Each file uses `load 'test_helper'`. yq compound assertions match RESEARCH §Gap 4/5/2 yaml shapes (forceTenantPrefix top-level; combined-name SA owners; helm-controller in lockdown patches; flux-system K escape hatch). Static bats are RED (non-zero exit) because target manifest yaml does not exist yet — this is the EXPECTED Wave 0 state per D-09-08.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write 7 live bats files (TEN-01..06 live + v0.18 health-check regression)</name>
  <files>
    tests/bats/tenants-06-live-tenant-cr.bats,
    tests/bats/tenants-07-live-impersonate.bats,
    tests/bats/tenants-08-live-quota-limit.bats,
    tests/bats/tenants-09-live-inner-k-ready.bats,
    tests/bats/tenants-10-live-lockdown.bats,
    tests/bats/tenants-11-live-dependson.bats,
    tests/bats/tenants-12-live-health-check.bats
  </files>
  <read_first>
    - .planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-CONTEXT.md (D-09-07 verifier path lines 234-249; D-09-09 plan ordering lines 270-279)
    - .planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-RESEARCH.md §"Bats Test Coverage" lines 1271-1316 + §Gap 7 (impersonation prereqs lines 499-532) + §Gap 11 (health-check subset lines 681-724)
    - .planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-VALIDATION.md lines 47-53 (live-bats per-task verification)
    - .planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-PATTERNS.md "Bats Tests" section lines 897-1049 (file-by-file analog patterns)
    - tests/bats/capsule-install-06-live-helm.bats (cluster-info gate + 3×30s retry analog for tenants-06/10/11)
    - tests/bats/capsule-install-07-live-crds.bats (kubectl get list iterator analog for tenants-08)
    - tests/bats/capsule-install-10-live-flux-observable.bats (pre-push GitRepository skip analog for tenants-09)
    - tests/bats/capsule-install-09-live-adr-004.bats (ADR-004 invariant analog for tenants-12)
    - scripts/health-check.sh (the v0.18 regression gate — to understand what task health-check exercises)
  </read_first>
  <action>
Create the 7 live bats files. Each MUST:
1. Start with `#!/usr/bin/env bats` shebang + Phase 9 Wave 0 D-09-08 Nyquist gate header
2. Include `load 'test_helper'`
3. Have a `setup()` that runs cluster-info gate: `if ! kubectl --context=k3d-{spoke-capsule|hub-flux} cluster-info >/dev/null 2>&1; then skip "..."; fi`
4. Use `run kubectl ...` or local var + 3-attempt loop with 30s sleep on jsonpath assertions
5. Reference the RESEARCH-corrected yaml shapes (forceTenantPrefix top-level; combined-name SA; helm-controller in lockdown)

**File 1: `tests/bats/tenants-06-live-tenant-cr.bats` (TEN-01 live)** — RESEARCH §"Bats Coverage" lines 1273-1278 + Gap 4 corrections:
- setup(): cluster-info gate on `k3d-spoke-capsule`
- 6 @test blocks (3 alpha + 3 bravo):
  - "TEN-01 / RESEARCH Gap 4 (alpha): kubectl get tenant alpha reports forceTenantPrefix=true (TOP-LEVEL, 3×30s retry)"
    ```bash
    local i ftp
    for i in 1 2 3; do
      ftp=$(kubectl --context=k3d-spoke-capsule get tenant alpha -o jsonpath='{.spec.forceTenantPrefix}' 2>/dev/null || true)
      [[ "$ftp" == "true" ]] && return 0
      [[ $i -lt 3 ]] && sleep 30
    done
    echo "Last observed forceTenantPrefix: '$ftp'"
    return 1
    ```
  - "TEN-01 / D-09-02 (alpha): tenant-alpha namespace exists on spoke-capsule" — `kubectl --context=k3d-spoke-capsule get namespace tenant-alpha`
  - "TEN-01 / D-09-02 (alpha): gitops-reconciler ServiceAccount exists in tenant-alpha namespace" — `kubectl --context=k3d-spoke-capsule -n tenant-alpha get sa gitops-reconciler`
- Mirror 3 tests for bravo

**File 2: `tests/bats/tenants-07-live-impersonate.bats` (TEN-02 — kubectl --as=alpha)** — RESEARCH §Gap 7:
- setup(): cluster-info gate on `k3d-spoke-capsule`
- 4 @test blocks:
  - "TEN-02 / D-09-07 (alpha): kubectl --as=alpha can create namespace alpha-app1 (idempotent — apply, NOT create)"
    ```bash
    # Idempotent: pipe via apply -f - so re-runs don't fail with AlreadyExists
    run bash -c "kubectl --context=k3d-spoke-capsule --as=alpha create namespace alpha-app1 --dry-run=client -o yaml | kubectl --context=k3d-spoke-capsule --as=alpha apply -f -"
    [ "$status" -eq 0 ]
    ```
  - "TEN-02 / D-09-07 (alpha): namespace alpha-app1 has capsule.clastix.io/tenant=alpha label (auto-stamped by Capsule webhook)"
    ```bash
    local i label
    for i in 1 2 3; do
      label=$(kubectl --context=k3d-spoke-capsule get namespace alpha-app1 -o jsonpath='{.metadata.labels.capsule\.clastix\.io/tenant}' 2>/dev/null || true)
      [[ "$label" == "alpha" ]] && return 0
      [[ $i -lt 3 ]] && sleep 30
    done
    echo "Last observed label: '$label'"
    return 1
    ```
  - Mirror for bravo: "kubectl --as=bravo can create namespace bravo-app1" + label assertion

**File 3: `tests/bats/tenants-08-live-quota-limit.bats` (TEN-03 — auto-materialization)** — RESEARCH §"Bats Coverage" lines 1285-1289:
- setup(): cluster-info gate on `k3d-spoke-capsule`
- 4 @test blocks:
  - "TEN-03 / D-09-04 (alpha): ResourceQuota auto-materializes in alpha-app1 namespace (capsule-alpha-* name, 3×30s poll)"
    ```bash
    local i names
    for i in 1 2 3; do
      names=$(kubectl --context=k3d-spoke-capsule -n alpha-app1 get resourcequota -o jsonpath='{.items[*].metadata.name}' 2>/dev/null || true)
      [[ "$names" == capsule-alpha-* ]] && return 0
      [[ $i -lt 3 ]] && sleep 10
    done
    echo "Last observed resourcequota names: '$names'"
    return 1
    ```
  - "TEN-03 / D-09-04 (alpha): LimitRange auto-materializes in alpha-app1 namespace" — same pattern with `get limitrange`
  - Mirror for bravo (resourcequota + limitrange in bravo-app1 namespace)

**File 4: `tests/bats/tenants-09-live-inner-k-ready.bats` (TEN-04 reconcile + Secret-mirror)** — RESEARCH §"Bats Coverage" lines 1291-1293 + §Gap 1 + §Gap 10 pre-push skip:
- setup(): cluster-info gate on `k3d-hub-flux`
- 4 @test blocks:
  - "RESEARCH Gap 1 / D-09-03a (alpha): spoke-capsule-kubeconfig Secret mirrored to tenant-alpha namespace on hub" — `kubectl --context=k3d-hub-flux -n tenant-alpha get secret spoke-capsule-kubeconfig`
  - "RESEARCH Gap 1 / D-09-03a (bravo): spoke-capsule-kubeconfig Secret mirrored to tenant-bravo namespace on hub" — mirror
  - "TEN-04 / D-09-03 (alpha): tenant-alpha-app Kustomization Ready=True (skip if GitRepository pre-push ArtifactFailed)"
    ```bash
    # Pre-push skip: if GitRepository tenant-alpha-source is ArtifactFailed (revision main missing — pre-Phase-11 push state), skip
    local gr_reason
    gr_reason=$(kubectl --context=k3d-hub-flux -n tenant-alpha get gitrepository tenant-alpha-source -o jsonpath='{.status.conditions[?(@.type=="Ready")].reason}' 2>/dev/null || true)
    if [[ "$gr_reason" == "ArtifactFailed" || "$gr_reason" == "GitOperationFailed" ]]; then
      skip "pre-push state: tenant-alpha-source GitRepository is $gr_reason (deferred to Phase 11 VAL-05)"
    fi
    local i ready
    for i in 1 2 3; do
      ready=$(kubectl --context=k3d-hub-flux -n tenant-alpha get kustomization tenant-alpha-app -o jsonpath='{.status.conditions[?(@.type=="Ready")].status}' 2>/dev/null || true)
      [[ "$ready" == "True" ]] && return 0
      [[ $i -lt 3 ]] && sleep 30
    done
    echo "Last observed Ready: '$ready'"
    return 1
    ```
  - Mirror for bravo

**File 5: `tests/bats/tenants-10-live-lockdown.bats` (TEN-05 live)** — RESEARCH §"Bats Coverage" lines 1295-1301 + §Gap 2 controller-restart caveat:
- setup(): cluster-info gate on `k3d-hub-flux`
- 8 @test blocks:
  - "TEN-05 / D-09-05: kustomize-controller args contain --no-cross-namespace-refs=true"
    ```bash
    run bash -c "kubectl --context=k3d-hub-flux -n flux-system get deployment kustomize-controller -o jsonpath='{.spec.template.spec.containers[0].args}' | grep -F -- '--no-cross-namespace-refs=true'"
    [ "$status" -eq 0 ]
    ```
  - "TEN-05 / D-09-05: kustomize-controller args contain --no-remote-bases=true" — same pattern
  - "TEN-05 / D-09-05: kustomize-controller args contain --default-service-account=default" — same pattern
  - "TEN-05 / RESEARCH Gap 2: helm-controller args contain --no-cross-namespace-refs=true AND --default-service-account=default (extends CONTEXT.md narrowing)"
  - "TEN-05 / D-09-05: notification-controller args contain --no-cross-namespace-refs=true"
  - "TEN-05 / RESEARCH Gap 2 (3 controllers no CrashLoopBackOff): kustomize-controller availableReplicas == replicas (2-min poll)"
    ```bash
    local i avail desired
    for i in 1 2 3 4; do
      avail=$(kubectl --context=k3d-hub-flux -n flux-system get deployment kustomize-controller -o jsonpath='{.status.availableReplicas}' 2>/dev/null || echo 0)
      desired=$(kubectl --context=k3d-hub-flux -n flux-system get deployment kustomize-controller -o jsonpath='{.spec.replicas}' 2>/dev/null || echo 1)
      [[ "$avail" == "$desired" ]] && [[ "$avail" -gt 0 ]] && return 0
      [[ $i -lt 4 ]] && sleep 30
    done
    echo "Last observed kustomize-controller availableReplicas=$avail desired=$desired"
    return 1
    ```
  - Mirror for helm-controller + notification-controller availableReplicas == replicas
  - "TEN-05 / D-09-05a LOAD-BEARING: flux-system Kustomization reports Ready=True (escape hatch validation, 3×30s retry)"

**File 6: `tests/bats/tenants-11-live-dependson.bats` (TEN-06 live)** — RESEARCH §"Bats Coverage" lines 1303-1306:
- setup(): cluster-info gate on `k3d-hub-flux`
- 3 @test blocks:
  - "TEN-06 / D-09-06: poc-capsule-spoke Kustomization has dependsOn[0].name == poc-capsule"
    ```bash
    run kubectl --context=k3d-hub-flux -n flux-system get kustomization poc-capsule-spoke -o jsonpath='{.spec.dependsOn[0].name}'
    [ "$status" -eq 0 ]
    [ "$output" = "poc-capsule" ]
    ```
  - "TEN-06 / RESEARCH Gap 9: poc-capsule-spoke Kustomization has spec.wait == true (TOP-LEVEL)" — `... -o jsonpath='{.spec.wait}'` == "true"
  - "TEN-06 / D-09-06: poc-capsule-spoke Kustomization Ready=True (3×30s retry)" — same retry pattern as file 5

**File 7: `tests/bats/tenants-12-live-health-check.bats` (v0.18 regression gate)** — RESEARCH §Gap 11:
- setup(): cluster-info gate on `k3d-hub-flux` AND `k3d-spoke-apps` AND `k3d-spoke-ml` (full v0.18 baseline)
- 3 @test blocks:
  - "v0.18 regression: task health-check exits 0 (D-09-09 + RESEARCH Gap 11 belt-and-suspenders)"
    ```bash
    run bash -c "cd '${REPO_ROOT}' && task health-check"
    [ "$status" -eq 0 ]
    ```
  - "RESEARCH Gap 11: spoke-apps Kustomization Ready=True (cross-cluster lockdown impact regression)"
    ```bash
    local ready
    ready=$(kubectl --context=k3d-hub-flux -n flux-system get kustomization spoke-apps -o jsonpath='{.status.conditions[?(@.type=="Ready")].status}' 2>/dev/null || true)
    [[ "$ready" == "True" ]]
    ```
  - "RESEARCH Gap 11: spoke-ml Kustomization Ready=True (cross-cluster lockdown impact regression)" — mirror

After creating the 7 files, run live bats and verify all SKIP (cluster not yet patched/populated) — that is the expected Wave 0 RED state.
  </action>
  <verify>
    <automated>cd "${REPO_ROOT:-/home/rich/code/gsd/karyon}" && bats tests/bats/tenants-{06,07,08,09,10,11,12}-live-*.bats 2>&1 | tail -40; rc=$?; for f in tests/bats/tenants-06-live-tenant-cr.bats tests/bats/tenants-07-live-impersonate.bats tests/bats/tenants-08-live-quota-limit.bats tests/bats/tenants-09-live-inner-k-ready.bats tests/bats/tenants-10-live-lockdown.bats tests/bats/tenants-11-live-dependson.bats tests/bats/tenants-12-live-health-check.bats; do [ -f "$f" ] || { echo "MISSING: $f"; exit 1; }; done; echo "Live bats files present. Wave 0 RED state expected (skip when cluster down OR fail when cluster up but yaml absent)."</automated>
  </verify>
  <done>
    All 7 live bats files exist. Each file has cluster-info gate via `kubectl --context=... cluster-info >/dev/null 2>&1` to auto-skip when target cluster unreachable. 3×30s retry pattern used on all jsonpath Ready/condition assertions. Live bats are RED (skip when cluster not up; fail when up but yaml absent) — the EXPECTED Wave 0 state per D-09-08.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Validate Wave 0 RED state + frontmatter Nyquist compliance + commit</name>
  <files>
    .planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-VALIDATION.md
  </files>
  <read_first>
    - .planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-VALIDATION.md (current frontmatter; check `nyquist_compliant: false` + `wave_0_complete: false` flags)
    - .planning/phases/07-foundation-poc-seam-spoke-capsule-cluster-eks-capsule-doc/07-VERIFICATION.md (analog for VALIDATION frontmatter flip pattern post-Wave-0)
  </read_first>
  <action>
1. Run the full bats suite to confirm RED state:
   ```bash
   cd "${REPO_ROOT:-/home/rich/code/gsd/karyon}" && bats tests/bats/tenants-*.bats 2>&1 | tail -50
   ```
   Expected output: static bats (01-05) FAIL with "file not found" or yq returns null; live bats (06-12) SKIP because k3d-spoke-capsule cluster-info gate triggers (cluster not yet patched) OR FAIL because cluster up but Tenants absent.

2. Verify all 13 expected files exist:
   ```bash
   for f in tests/bats/tenants-{01,02,03,04,05}-static-*.bats tests/bats/tenants-{06,07,08,09,10,11,12}-live-*.bats tests/fixtures/p27-broken/tenant-broken.yaml; do
     [ -f "$f" ] || { echo "MISSING: $f"; exit 1; }
   done
   echo "All 13 Wave 0 files present"
   ```

3. Update `.planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-VALIDATION.md` frontmatter:
   - Change `nyquist_compliant: false` → `nyquist_compliant: true`
   - Change `wave_0_complete: false` → `wave_0_complete: true`
   - Update line 6 `created: 2026-04-29` → preserve date; do NOT touch
   - Below `## Wave 0 Requirements` heading, mark every checkbox `- [ ]` → `- [x]` for the 12 bats files + the fixture (mirror VALIDATION.md lines 67-81 pattern)

4. Commit. Use `gsd-sdk query commit` per the planner workflow:
   ```bash
   gsd-sdk query commit "test(09-00): wave 0 RED bats scaffold — 12 bats files + p27 negative fixture (TEN-01..06; D-09-08 Nyquist gate)" \
     tests/bats/tenants-01-static-tenant-cr.bats \
     tests/bats/tenants-02-static-p27.bats \
     tests/bats/tenants-03-static-lockdown.bats \
     tests/bats/tenants-04-static-dependson.bats \
     tests/bats/tenants-05-static-split-path.bats \
     tests/bats/tenants-06-live-tenant-cr.bats \
     tests/bats/tenants-07-live-impersonate.bats \
     tests/bats/tenants-08-live-quota-limit.bats \
     tests/bats/tenants-09-live-inner-k-ready.bats \
     tests/bats/tenants-10-live-lockdown.bats \
     tests/bats/tenants-11-live-dependson.bats \
     tests/bats/tenants-12-live-health-check.bats \
     tests/fixtures/p27-broken/tenant-broken.yaml \
     .planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-VALIDATION.md
   ```

5. Verify commit landed via `git log -1 --oneline`. NO PUSH per D-08-13 (push-gate deferred to Phase 11 VAL-05).
  </action>
  <verify>
    <automated>cd "${REPO_ROOT:-/home/rich/code/gsd/karyon}" && grep -F 'nyquist_compliant: true' .planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-VALIDATION.md && grep -F 'wave_0_complete: true' .planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-VALIDATION.md && git log -1 --oneline | grep -F '09-00'</automated>
  </verify>
  <done>
    All 13 Wave 0 files committed. VALIDATION.md frontmatter `nyquist_compliant: true` + `wave_0_complete: true`. Wave 0 checkboxes flipped to `[x]`. Bats suite runs RED (static fail with file-not-found / yaml absent; live skip on cluster-info gate). NO push to origin/main per D-08-13.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bats lint surface (yq + grep) | NO trust crossing — bats files are committed yaml/bash; their failure modes are deterministic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-P27 (referenced; Plan 09-02 + 09-03 own primary mitigation) | Tampering | tests/bats/tenants-02-static-p27.bats | mitigate | The yq lint asserts `spec.serviceAccountName` is set + non-null on every tenant inner Kustomization. The negative falsifier (tests/fixtures/p27-broken/tenant-broken.yaml) proves the lint catches missing SA. If a future tenant K is added without `serviceAccountName`, the static bats RED-fails before merge — primary structural defense. |
| T-9-LOCKDOWN-self-block | Denial of Service | tests/bats/tenants-10-live-lockdown.bats availableReplicas check | mitigate | The 3-controller availableReplicas==replicas + 2-minute poll catches CrashLoopBackOff from misapplied flag (RESEARCH §Gap 2 line 235-236 — controllers exit non-zero on unknown flag). If a Phase 9 patch ships a typo, this bats RED-fails immediately post-Plan 09-03 reconcile. |
| T-9-WAVE-0-FALSE-GREEN | Spoofing (false test pass) | tests/bats/tenants-02-static-p27.bats negative falsifier | mitigate | The synthetic broken-fixture lint test proves the lint actually catches missing SA (not just absence of any input). Wave 0 RED state acceptance criterion: the negative falsifier's `[ "$status" -ne 0 ]` PASSES while all positive lints FAIL — proves the lint logic is correct before any tenant yaml lands. |
| T-9-IMPERSONATION (referenced; Plans 09-01 + 09-02 own primary mitigation) | Elevation of Privilege | tests/bats/tenants-07-live-impersonate.bats | accept (for Wave 0) | TEN-02 verifier path (D-09-07) uses cluster-admin local kubeconfig + `--as=alpha` impersonation per RESEARCH §Gap 7. The bats setup gate skips when k3d-spoke-capsule unreachable. Wave 0 RED state is intentional; live PASS happens after Plan 09-01 lands Tenant CRs. |
</threat_model>

<verification>
At end of plan, run:
```bash
cd "${REPO_ROOT:-/home/rich/code/gsd/karyon}" && bats tests/bats/tenants-*.bats 2>&1 | tail -50
```

Expected: 12 bats files + 1 fixture exist. Static bats RED (file-not-found errors on target manifest yaml). Live bats SKIP (cluster-info gate fires when k3d-spoke-capsule unreachable; if reachable, live bats fail because Tenants/CRs absent — this is also RED-acceptable).

P27 negative falsifier (inside tenants-02): the synthetic broken-fixture test PASSES (proves lint catches missing SA).

VALIDATION.md frontmatter: `nyquist_compliant: true` + `wave_0_complete: true`.

NO push to origin/main per D-08-13.
</verification>

<success_criteria>
- All 12 Phase 9 bats files exist under `tests/bats/tenants-*.bats`
- P27 negative falsifier fixture exists at `tests/fixtures/p27-broken/tenant-broken.yaml`
- All 5 static bats files reference RESEARCH-corrected yaml shapes (forceTenantPrefix top-level; combined-name SA owners; helm-controller in lockdown patches; flux-system K escape hatch; spec.wait top-level)
- All 7 live bats files have cluster-info gate via `kubectl --context=... cluster-info >/dev/null 2>&1`
- 3×30s retry pattern present on all jsonpath Ready/condition assertions
- VALIDATION.md frontmatter: `nyquist_compliant: true` + `wave_0_complete: true`
- All 12 Wave 0 checkboxes flipped to `[x]` in VALIDATION.md
- Single commit landed locally; NO push to origin (D-08-13 deferral)
</success_criteria>

<output>
After completion, create `.planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-00-SUMMARY.md` per the GSD summary template documenting:
- 13 files created (12 bats + 1 fixture)
- Wave 0 RED state confirmed (static fail; live skip)
- VALIDATION.md frontmatter flipped to nyquist_compliant: true / wave_0_complete: true
- Forward-pointer: Plan 09-01 turns tenants-01/06 GREEN by landing Tenant CRs; Plan 09-02 turns tenants-02/04/09 GREEN by landing per-tenant inner Ks; Plan 09-03 turns tenants-03/10 GREEN by landing lockdown patches; Plan 09-04 verifier reruns full suite + Phase 7/8 invariants
</output>
</content>
</invoke>