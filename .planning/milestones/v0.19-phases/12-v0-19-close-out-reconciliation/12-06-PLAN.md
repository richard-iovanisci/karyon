---
phase: 12
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
autonomous: true
requirements: []
tags: [bookkeeping, roadmap, verification, close-out, v0.19]

must_haves:
  truths:
    - "ROADMAP.md Progress table is consistent with Phase Summary checkboxes (verified at HEAD 83b81bb5 per RESEARCH.md Pitfall 1; this plan VERIFIES the Progress table)"
    - "Phase 11 row: 8/8 plans Complete 2026-05-07"
    - "Phase 12 row: currently `Planned 0/7 —` (set by planner-init commit aca6600; will be flipped to Complete 7/7 by Plan 12-07 in Wave 2)"
    - "No within-document contradiction between Phase Summary (lines 39-44) and Progress table (lines 153-167)"
    - "ROADMAP.md narrative count for v0.19 closed-out REQs at lines 44, 141, 143 reports `18 v0.19 mandatory` (not the stale `15`) — matches REQUIREMENTS.md after Plan 12-01 closes its narrative drift at line 86 (same Wave 1)"
  artifacts:
    - path: .planning/ROADMAP.md
      provides: "Verified-consistent milestone roadmap (Progress table verified; narrative count corrected at 3 sites per RESEARCH.md Pattern §C + roadmap-narrative-drift fix)"
      contains: "12. v0.19 Close-out Reconciliation"
  key_links:
    - from: .planning/ROADMAP.md
      to: .planning/STATE.md
      via: "Plan 12-02 updated STATE.md to reflect Phase 12 EXECUTING; this plan confirms ROADMAP doesn't contradict"
      pattern: "Phase 12.*Planned"
    - from: .planning/ROADMAP.md
      to: .planning/REQUIREMENTS.md
      via: "Plan 12-01 Task 3 corrects REQUIREMENTS.md line 86 from `15 stale` to `18 v0.19 mandatory`; this plan corrects the 3 ROADMAP.md sites of the same drift (lines 44, 141, 143) so audit re-run reads a self-consistent narrative"
      pattern: "18 v0.19 mandatory"
---

<objective>
Verify the Progress table consistency in `.planning/ROADMAP.md` AND correct
the "15 stale" roadmap-narrative drift at 3 sites (lines 44, 141, 143) to
match the actual `[ ]` count of 18 v0.19 mandatory REQ-IDs. This is Cluster
B of the Phase 12 close-out reconciliation (RESEARCH.md §"Wave / Parallelism
Opportunity" Wave 1).

Purpose: RESEARCH.md Pitfall 1 + 12-PATTERNS.md §C explicitly note that
commit `83b81bb docs(roadmap): add gap closure phase 12 (v0.19 close-out
reconciliation)` already updated the Progress table after the audit ran
(audit HEAD 8415ab66; current HEAD 83b81bb5+). The planner-init commit
`aca6600 docs(12): create phase plan (7 plans; 6 Wave-1 + 1 Wave-2 gate)`
then advanced the Phase 12 row plan-count from `0/0` to `0/7` (live state at
planning time). The audit's recommendation #4 ("Refresh ROADMAP.md Progress
table") is therefore ALREADY SATISFIED at the live HEAD — Task 1 below
verifies this.

The narrative count `15 stale` (lines 44, 141, 143) is a roadmap-narrative
drift from the original gap-closure write — the actual REQUIREMENTS.md
`[ ]` count for v0.19 mandatory IDs is 18 (POC-01..04 + CAPCLU-01..04 +
EKSDOC-01 + CAP-01..03 + TEN-01..06 = 4+4+1+3+6 = 18). Plan 12-01 Task 3
corrects the same drift in REQUIREMENTS.md line 86; Task 2 below corrects
the 3 ROADMAP.md sites so the audit re-run (Plan 12-07 Task 2 in Wave 2)
reads a self-consistent narrative across REQUIREMENTS.md + ROADMAP.md.

Output: Verification record for the Progress table (no Progress table
edits expected) + 3 narrative-text edits at ROADMAP.md lines 44, 141, 143.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/v0.19-MILESTONE-AUDIT.md
@.planning/phases/12-v0-19-close-out-reconciliation/12-RESEARCH.md
@.planning/phases/12-v0-19-close-out-reconciliation/12-PATTERNS.md
</context>

<truths>
- Live ROADMAP Progress table (verified during research + post-planner-init commit aca6600):
  | Row | Phase | Plans | Status | Completed |
  | --- | ----- | ----- | ------ | --------- |
  | 7 | Foundation: POC Seam + spoke-capsule + EKS Doc | 6/6 | Complete | 2026-04-29 |
  | 8 | Capsule + capsule-proxy Install | 5/5 | Complete | 2026-04-30 |
  | 9 | Tenants + Flux Multi-Tenancy Lockdown | 5/5 | Complete | 2026-04-30 |
  | 10 | Tenant Kubeconfigs + Proxy Round-trip | 3/3 | Complete | 2026-05-05 |
  | 11 | Validation + Graduation ADR-008 | 8/8 | Complete | 2026-05-07 |
  | 12 | v0.19 Close-out Reconciliation | 0/7 | Planned | — |
- Live HEAD Phase Summary checkboxes (verified):
  - Phase 7-11: all `[x]`
  - Phase 12: `[ ]` (gap-closure phase in progress)
- This means SC2 is ALREADY SATISFIED at HEAD for the Progress table; no Progress table edits are needed.
- Plan 12-07 (Wave 2) will perform the Phase 12 self-close flip (Phase Summary checkbox `[ ] → [x]`; Progress table Status `Planned → Complete`; plans `0/7 → 7/7`; Completed cell `— → 2026-05-11`). That flip happens AFTER the audit re-run passes, so it lives in Wave 2.
- The `0/7` (not `0/0`) plan count was established by the planner-init commit `aca6600 docs(12): create phase plan (7 plans; 6 Wave-1 + 1 Wave-2 gate)` after the original gap-closure ROADMAP write. Verification greps below MUST match the live `0/7` state, NOT the historical `0/0` text that briefly existed pre-planner-init.
- The "15 stale" wording at ROADMAP.md lines 44, 141, 143 is roadmap-narrative drift — the literal `[ ]` count was always 18 (POC-01..04 + CAPCLU-01..04 + EKSDOC-01 + CAP-01..03 + TEN-01..06 = 4+4+1+3+6 = 18). The original gap-closure scribe miscounted; this plan corrects all 3 sites.
- Plan 12-01 Task 3 corrects the same drift in REQUIREMENTS.md line 86 (file-disjoint, same Wave 1).
</truths>

<tasks>

<task type="auto">
  <name>Task 1: Verify ROADMAP.md Phase Summary + Progress table consistency (grep-only; no edits)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md Phase Summary lines 39-44 (Phase 7-12 checkboxes)
    - .planning/ROADMAP.md Progress table lines 153-167 (rows for all 12 phases)
    - .planning/v0.19-MILESTONE-AUDIT.md §"ROADMAP.md self-contradiction" (lines 198-201 — the audit-reported issue that was subsequently fixed by commit 83b81bb)
    - .planning/phases/12-v0-19-close-out-reconciliation/12-RESEARCH.md Pitfall 1 + 12-PATTERNS.md §C (research confirms HEAD already-fixed credit)
  </read_first>
  <action>
    Run the following grep verifications (use Bash tool):

    ```
    # Phase Summary checkboxes — expect Phase 7-11 [x], Phase 12 [ ]
    grep -E "^- \[x\] \*\*Phase (7|8|9|10|11):" .planning/ROADMAP.md | wc -l
    # Expected: 5

    grep -E "^- \[ \] \*\*Phase 12:" .planning/ROADMAP.md | wc -l
    # Expected: 1

    # Progress table — expect Phase 7-11 Complete; Phase 12 Planned 0/7
    grep -c "^| 7\. .*| v0.19 | 6/6 | Complete | 2026-04-29 |$" .planning/ROADMAP.md
    # Expected: 1

    grep -c "^| 8\. .*| v0.19 | 5/5 | Complete | 2026-04-30 |$" .planning/ROADMAP.md
    # Expected: 1

    grep -c "^| 9\. .*| v0.19 | 5/5 | Complete | 2026-04-30 |$" .planning/ROADMAP.md
    # Expected: 1

    grep -c "^| 10\. .*| v0.19 | 3/3 | Complete | 2026-05-05 |$" .planning/ROADMAP.md
    # Expected: 1

    grep -c "^| 11\. .*| v0.19 | 8/8 | Complete | 2026-05-07 |$" .planning/ROADMAP.md
    # Expected: 1

    grep -c "^| 12\. .*| v0.19 | 0/7 | Planned | — |$" .planning/ROADMAP.md
    # Expected: 1
    ```

    If ALL the above checks return their expected values, write a one-line verification record to `.planning/phases/12-v0-19-close-out-reconciliation/12-06-SUMMARY.md` Notable Observations section confirming Pitfall 1 held and no Progress-table edits were made. Record the grep results verbatim in the SUMMARY.

    If ANY check fails (returns unexpected value), DO NOT silently auto-correct. Instead, document the surfaced contradiction in the SUMMARY's Notable Observations and add a Wave-1 follow-up edit to this plan (revision mode) using 12-PATTERNS.md §A same-line edit pattern. Do NOT edit the Progress table if checks pass.

    Task 1 is verification-only for the Progress table. Task 2 is narrative-edit only (text outside the Progress table).
  </action>
  <verify>
    <automated>
test $(grep -cE "^- \[x\] \*\*Phase (7|8|9|10|11):" .planning/ROADMAP.md) -eq 5 && \
test $(grep -cE "^- \[ \] \*\*Phase 12:" .planning/ROADMAP.md) -eq 1 && \
test $(grep -cE "^\| 11\. .*\| v0.19 \| 8/8 \| Complete \| 2026-05-07 \|$" .planning/ROADMAP.md) -eq 1 && \
test $(grep -cE "^\| 12\. .*\| v0.19 \| 0/7 \| Planned \| — \|$" .planning/ROADMAP.md) -eq 1
    </automated>
  </verify>
  <acceptance_criteria>
    - All 7 grep checks in the action block return their expected values (note: Phase 12 row has `0/7` plans live, NOT `0/0` — set by planner-init commit aca6600)
    - No Progress-table edits made by this task: the Progress-table portion of `.planning/ROADMAP.md` (lines 153-174) is byte-identical pre-Task-1 vs post-Task-1
    - Plan SUMMARY records the verification results verbatim with `grep -nE` outputs for reproducibility
  </acceptance_criteria>
  <done>
    Progress table verified consistent; no Progress-table edits required; SUMMARY captures the verification record (date, grep commands, expected vs actual counts).
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct "15 stale" roadmap-narrative drift at 3 ROADMAP.md sites (lines 44, 141, 143) — "15" → "18 v0.19 mandatory"</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md line 44 (Phase 12 Phase Summary bullet — currently says "15 REQs")
    - .planning/ROADMAP.md lines 141, 143 (Phase 12 Requirements line + Success Criterion 1 — both currently say "15 stale")
    - .planning/REQUIREMENTS.md lines 30-63 (count source: 18 v0.19 mandatory IDs across POC/CAPCLU/EKSDOC/CAP/TEN families)
    - .planning/phases/12-v0-19-close-out-reconciliation/12-01-PLAN.md Task 3 (file-disjoint sibling — corrects the same drift in REQUIREMENTS.md line 86)
  </read_first>
  <action>
    Three Edit tool calls against ROADMAP.md, one per site. Each `old_string` is the verbatim current line; each `new_string` corrects the count from "15" to "18 v0.19 mandatory" (with the same explicit family breakdown that Plan 12-01 Task 3 uses in REQUIREMENTS.md).

    **Edit 1 — ROADMAP.md line 44 (Phase 12 Phase Summary bullet):**

    `old_string`:
    `- [ ] **Phase 12: v0.19 Close-out Reconciliation** — Gap closure phase created by `/gsd-plan-milestone-gaps` 2026-05-11. Reconcile REQUIREMENTS.md stale checkboxes (15 REQs), refresh ROADMAP Progress table, update STATE.md, supersede stale Phase 11 verification artifacts, close 4 Nyquist VALIDATION ledgers, resolve TEN-01 `forceTenantPrefix` spec deviation per D-11-07, archive Phase 7 carryover todo. (Replaces the original Phase 12 `capsule-addon-fluxcd` trial; addon trial deferred to v0.20.)`

    `new_string`:
    `- [ ] **Phase 12: v0.19 Close-out Reconciliation** — Gap closure phase created by `/gsd-plan-milestone-gaps` 2026-05-11. Reconcile REQUIREMENTS.md stale checkboxes (18 v0.19 mandatory REQs: POC-01..04 + CAPCLU-01..04 + EKSDOC-01 + CAP-01..03 + TEN-01..06 = 4+4+1+3+6 = 18), refresh ROADMAP Progress table, update STATE.md, supersede stale Phase 11 verification artifacts, close 4 Nyquist VALIDATION ledgers, resolve TEN-01 `forceTenantPrefix` spec deviation per D-11-07, archive Phase 7 carryover todo. (Replaces the original Phase 12 `capsule-addon-fluxcd` trial; addon trial deferred to v0.20.)`

    Difference: `stale checkboxes (15 REQs)` → `stale checkboxes (18 v0.19 mandatory REQs: POC-01..04 + CAPCLU-01..04 + EKSDOC-01 + CAP-01..03 + TEN-01..06 = 4+4+1+3+6 = 18)`. All other bytes identical.

    NOTE: Plan 12-07 Task 3 Edit 1 will later flip this same line from `[ ]` to `[x]` and rewrite the body with the post-close summary. Task 2 of this plan only fixes the count — the close-out body rewrite is 12-07's responsibility.

    **Edit 2 — ROADMAP.md line 141 (Phase 12 Requirements line in Phase Details):**

    `old_string`:
    `**Requirements**: No new REQ-IDs (book-keeping phase). Closes the 15 stale `[ ]` checkboxes for POC-01..04, CAPCLU-01..04, EKSDOC-01, CAP-01..03, TEN-01..06 against the existing implementation.`

    `new_string`:
    `**Requirements**: No new REQ-IDs (book-keeping phase). Closes the 18 v0.19 mandatory `[ ]` checkboxes for POC-01..04 + CAPCLU-01..04 + EKSDOC-01 + CAP-01..03 + TEN-01..06 (= 4+4+1+3+6 = 18) against the existing implementation.`

    Difference: `15 stale` → `18 v0.19 mandatory`; family list switched from comma-separated to `+`-separated with explicit arithmetic `(= 4+4+1+3+6 = 18)`. All other bytes identical.

    **Edit 3 — ROADMAP.md line 143 (Phase 12 Success Criterion 1):**

    `old_string`:
    `  1. `.planning/REQUIREMENTS.md`: 15 stale `[ ]` checkboxes flipped to `[x]` for POC-01..04, CAPCLU-01..04, EKSDOC-01, CAP-01..03, TEN-01..06 against the audit's WIRED evidence. TEN-01 spec text amended to reflect `forceTenantPrefix: false` per Plan 11-07 D-11-07 live repair, OR an ADR-008 amendment records the deviation explicitly.`

    `new_string`:
    `  1. `.planning/REQUIREMENTS.md`: 18 v0.19 mandatory `[ ]` checkboxes flipped to `[x]` for POC-01..04 + CAPCLU-01..04 + EKSDOC-01 + CAP-01..03 + TEN-01..06 (= 4+4+1+3+6 = 18) against the audit's WIRED evidence. TEN-01 spec text amended to reflect `forceTenantPrefix: false` per Plan 11-07 D-11-07 live repair, OR an ADR-008 amendment records the deviation explicitly.`

    Difference: `15 stale` → `18 v0.19 mandatory`; family list switched from comma-separated to `+`-separated with explicit arithmetic. All other bytes identical.

    These three edits MUST land Wave 1, before Plan 12-07 Task 2's audit re-run, so the audit reads a self-consistent narrative across REQUIREMENTS.md (corrected by 12-01 Task 3) + ROADMAP.md (corrected here).
  </action>
  <verify>
    <automated>
test $(grep -c "18 v0.19 mandatory REQs" .planning/ROADMAP.md) -ge 1 && \
test $(grep -c "18 v0.19 mandatory \`\[ \]\` checkboxes" .planning/ROADMAP.md) -ge 2 && \
test $(grep -c "15 stale \`\[ \]\` checkboxes" .planning/ROADMAP.md) -eq 0 && \
test $(grep -c "15 REQs" .planning/ROADMAP.md) -eq 0
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "18 v0.19 mandatory" .planning/ROADMAP.md` returns 3 (one per edited site)
    - `grep -c "4+4+1+3+6 = 18" .planning/ROADMAP.md` returns 3 (explicit arithmetic present at all 3 sites for traceability)
    - `grep -c "15 stale \`\[ \]\` checkboxes" .planning/ROADMAP.md` returns 0 (lines 141, 143 narrative drift eliminated)
    - `grep -c "stale checkboxes (15 REQs)" .planning/ROADMAP.md` returns 0 (line 44 narrative drift eliminated)
    - Each of the 3 lines still references the correct family list (POC-01..04, CAPCLU-01..04, EKSDOC-01, CAP-01..03, TEN-01..06) and the correct downstream pointers (Plan 11-07 D-11-07; audit's WIRED evidence; ADR-008 amendment alternative) — no content lost, only count corrected
    - Phase 12 Phase Summary checkbox at line 44 is STILL `[ ]` (Plan 12-07 will flip to `[x]` in Wave 2 — this task only fixes the count)
  </acceptance_criteria>
  <done>
    ROADMAP.md narrative count reports `18 v0.19 mandatory` at all 3 sites (lines 44, 141, 143); the "15 stale" / "15 REQs" roadmap-narrative drift is eliminated. Combined with Plan 12-01 Task 3 (REQUIREMENTS.md line 86), the audit re-run at Plan 12-07 Task 2 will read a self-consistent narrative.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Verification + narrative-text edits; no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06-01 | N/A | ROADMAP.md grep verification + narrative edits | accept | No new threats — documentation reconciliation only; existing v0.19 security posture per `11-VERIFICATION.md` §VAL-01 through VAL-05 is unchanged. No code paths, auth, or data flows touched. |
</threat_model>

<verification>
- Progress table unchanged: Task 1's 7 grep checks all match expected counts; no Progress-table row edits.
- ROADMAP narrative count corrected at 3 sites: `grep -c "18 v0.19 mandatory" .planning/ROADMAP.md` returns 3.
- "15 stale" / "15 REQs" wording eliminated: `grep -c "15 stale \`\[ \]\` checkboxes" .planning/ROADMAP.md` returns 0; `grep -c "stale checkboxes (15 REQs)" .planning/ROADMAP.md` returns 0.
- SUMMARY records the Task 1 verification timestamp + grep commands AND the Task 2 byte-diff for each of the 3 edit sites.
</verification>

<success_criteria>
This plan satisfies ROADMAP Phase 12 Success Criterion 2 ("`.planning/ROADMAP.md` Progress table refreshed to match the Phase Summary checkboxes — no within-document contradiction") by VERIFICATION (Task 1 — the refresh was applied by commit 83b81bb + planner-init commit aca6600 prior to Phase 12 execution) AND closes the cross-document narrative drift (Task 2 — "15 stale" → "18 v0.19 mandatory" at 3 sites) so the audit re-run (Plan 12-07 Task 2 in Wave 2) reads a self-consistent narrative against REQUIREMENTS.md (which Plan 12-01 Task 3 corrects at line 86).
</success_criteria>

<output>
After completion, create `.planning/phases/12-v0-19-close-out-reconciliation/12-06-SUMMARY.md` per `~/.claude/get-shit-done/templates/summary.md`. requirements-completed: [] (no REQ-IDs). Notable Observations: record verbatim grep results from Task 1 + byte diffs from Task 2's 3 narrative edits for traceability.
</output>
</content>
</invoke>