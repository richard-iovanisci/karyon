---
phase: 15
plan: 05
type: execute
wave: 2
depends_on: [15-01, 15-02, 15-03, 15-04]
files_modified:
  - .planning/v0.20-MILESTONE-AUDIT.md
  - .planning/RETROSPECTIVE.md
  - .planning/PROJECT.md
  - .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VALIDATION.md
  - .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VERIFICATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: []
tags: [bookkeeping, audit, retrospective, parking-lot, gate, phase-close, close-out, v0.20]

must_haves:
  truths:
    - "`/gsd-audit-milestone v0.20` regenerates `.planning/v0.20-MILESTONE-AUDIT.md` and returns `status: passed` (ROADMAP SC-4 + D-15-06 hard-gate)"
    - "Phase 13 + Phase 14 VALIDATION ledgers confirmed `status: final` + `nyquist_compliant: true` + `wave_0_complete: true` (audit precondition; pre-Wave-2 inheritance)"
    - "v0.20 retrospective section appended to `.planning/RETROSPECTIVE.md` as a single `## Milestone: v0.20 — Capsule POC Demo & RBAC Polish` heading inserted between the existing v0.19 section closer and the existing `## Cross-Milestone Trends` heading (D-15-04 living-doc cadence)"
    - "v0.20 retrospective section contains 7 required sub-headings: Shipped / What Was Built / What Worked / What Was Inefficient / Patterns Established / OQ Resolutions / Carry-forward updates"
    - "v0.19 + v0.18 sections in RETROSPECTIVE.md untouched (grep counts for both pre-existing milestone headings unchanged)"
    - "PROJECT.md `Carry-forward parking lot` section reconciled inline: all existing entries (ADDON-01/02, G-04, KARYON_PHASE11_STRICT_LIVE, slo-regression-live, markdownlint, gitleaks-proxy-06, Phase 1-5 shellcheck, first-push CI verification, real secret management, frontmatter reconciliation) PRESERVED; new entries (TLS-fix-at-cert-layer + `task demo-capsule` wrapper + any non-trivial items from Phase 13 `deferred-items.md`) APPENDED (D-15-05)"
    - "`15-VALIDATION.md` frontmatter advanced: `status: draft → final`, `nyquist_compliant: false → true`, `wave_0_complete: false → true`, `finalized: 2026-05-20` appended; body has `## Finalized Evidence` section documenting Plan 15-04 evidence + vacuous-Nyquist rationale per D-15-07"
    - "`15-VERIFICATION.md` written with frontmatter `status: passed`; body has Goal Restated + 5 Per-Success-Criterion evidence rows (SC-1..5) + Overall Verdict + Plan-by-plan summary (15-01..05) + Anti-pattern regression confirmations (P27 + P29 + P31 + D-11-14)"
    - "STATE.md frontmatter advanced to milestone-close state: `status: executing → ready_to_archive`, `completed_plans: 9 → 14`, `percent: 64 → 100`, `last_activity: 2026-05-20 -- Phase 15 complete; v0.20 ready_to_archive`; Current Position body reflects Phase 15 COMPLETE"
    - "ROADMAP.md Phase 15 row in Progress table: `0/5 | In progress | -` → `5/5 | Complete | 2026-05-20`; Phase Summary checkbox for Phase 15: `[ ] → [x]`"
  artifacts:
    - path: .planning/v0.20-MILESTONE-AUDIT.md
      provides: "Regenerated milestone audit report (overwritten by `/gsd-audit-milestone v0.20`); confirms all 23 v0.20 mandatory REQs satisfied + zero unsatisfied + zero tech_debt + both Phase 13/14 VALIDATION ledgers finalized"
      contains: "status: passed"
    - path: .planning/RETROSPECTIVE.md
      provides: "Living-doc retrospective with v0.20 section appended (v0.18 + v0.19 + v0.20 sections in single file per D-15-04 living-doc convention)"
      contains: "## Milestone: v0.20 — Capsule POC Demo & RBAC Polish"
    - path: .planning/PROJECT.md
      provides: "Reconciled carry-forward parking lot (existing items preserved verbatim + new Phase 13/14-surfaced deferrals appended)"
      contains: "tls: bad certificate"
    - path: .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VALIDATION.md
      provides: "Phase 15 Nyquist VALIDATION ledger closed (vacuous-Nyquist book-keeping; Plan 15-04 live regression is the active falsifier per D-15-07)"
      contains: "status: final"
    - path: .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VERIFICATION.md
      provides: "Phase 15 verifier output (passed); cites Plan 15-01..05 evidence anchors against the 5 ROADMAP SC; mirrors Phase 12 + Phase 14 verifier shape"
      contains: "status: passed"
    - path: .planning/STATE.md
      provides: "Milestone-close state advance (`ready_to_archive` + percent 100 + completed_plans 14)"
      contains: "status: ready_to_archive"
    - path: .planning/ROADMAP.md
      provides: "Phase 15 row + Phase Summary checkbox flipped to Complete; v0.20 milestone progress table fully consistent"
      contains: "5/5 | Complete | 2026-05-20"
  key_links:
    - from: .planning/v0.20-MILESTONE-AUDIT.md
      to: ".planning/REQUIREMENTS.md + .planning/ROADMAP.md + .planning/STATE.md + .planning/phases/13-*/13-VALIDATION.md + .planning/phases/14-*/14-VALIDATION.md + .planning/phases/15-*/15-VALIDATION.md + per-phase VERIFICATION.md"
      via: "`/gsd-audit-milestone v0.20` reads all source classes (REQUIREMENTS + ROADMAP + STATE + per-phase VALIDATION + per-phase VERIFICATION); status=passed requires all 23 mandatory REQs satisfied + zero tech_debt + 3/3 Nyquist ledgers final"
      pattern: "status: passed"
    - from: .planning/RETROSPECTIVE.md
      to: ".planning/phases/13-*/13-VERIFICATION.md + .planning/phases/14-*/14-VERIFICATION.md + .planning/v0.20-MILESTONE-BRIEFING.md"
      via: "v0.20 section's What Was Built / What Worked / What Was Inefficient / Patterns / OQ Resolutions / Carry-forward sub-headings derive content from Phase 13 + Phase 14 SUMMARYs + VERIFICATION outputs + original briefing OQ-1..5 framing"
      pattern: "^## Milestone: v0.20"
    - from: .planning/PROJECT.md
      to: ".planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/deferred-items.md + .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-CONTEXT.md `<deferred>` section"
      via: "Parking lot append reads Phase 13 deferred-items + Phase 14 deferred ideas at task time and reflects each non-trivial item with a one-bullet entry"
      pattern: "tls: bad certificate"
    - from: .planning/phases/15-v0-20-milestone-close-out-retrospective/15-04-live-rerun-evidence.log
      to: ".planning/phases/15-v0-20-milestone-close-out-retrospective/15-VERIFICATION.md"
      via: "Plan 15-04 live regression evidence anchors SC-3 (three-tier regression-verified) in 15-VERIFICATION.md per-SC evidence table"
      pattern: "Plan 15-04 status: GREEN"
---

<objective>
**Wave 2 sequential gate.** After all Wave 1 plans (15-01 REQUIREMENTS flip + 15-02 STATE refresh + 15-03 ROADMAP refresh + 15-04 live regression rerun) land GREEN, re-run `/gsd-audit-milestone v0.20`, append the v0.20 retrospective section to the living `.planning/RETROSPECTIVE.md`, reconcile the carry-forward parking lot in `.planning/PROJECT.md`, finalize `15-VALIDATION.md` (vacuous-Nyquist book-keeping ledger per D-15-07), write `15-VERIFICATION.md`, then flip the Phase 15 self-close markers in STATE.md + ROADMAP.md. This is the FINAL plan of Phase 15 and the entire v0.20 milestone close-out.

Purpose: ROADMAP Phase 15 Success Criteria 4 + 5 (audit passed + retrospective captured) + the Phase 15 self-close itself (STATE.md `ready_to_archive` + ROADMAP.md Phase 15 row Complete). Pattern-mirrors v0.19 Plan 12-07 with two additions Phase 12 did not have: (a) PROJECT.md parking-lot reconciliation per D-15-05 (Phase 12 did not touch PROJECT.md); (b) RETROSPECTIVE.md living-doc append per D-15-04 (Phase 12 wrote a separate RETROSPECTIVE entry but did not codify it as the canonical living-doc pattern).

Output:
1. `.planning/v0.20-MILESTONE-AUDIT.md` (regenerated by `/gsd-audit-milestone v0.20`; status: passed expected).
2. `.planning/RETROSPECTIVE.md` (append v0.20 section per D-15-04 sub-section cadence).
3. `.planning/PROJECT.md` (carry-forward parking lot reconciliation per D-15-05).
4. `.planning/phases/15-*/15-VALIDATION.md` (frontmatter `status: final` + `nyquist_compliant: true` + `wave_0_complete: true` + `finalized: 2026-05-20` + body `## Finalized Evidence`).
5. `.planning/phases/15-*/15-VERIFICATION.md` (NEW verifier output, mirrors Phase 12 + Phase 14 shape).
6. `.planning/STATE.md` (frontmatter `status: ready_to_archive` + counters + last_activity; body Current Position COMPLETE).
7. `.planning/ROADMAP.md` (Phase 15 Progress row `5/5 Complete 2026-05-20` + Phase Summary checkbox `[x]`).
8. Human-verify checkpoint at Task 1 audit gate confirms PASS / Path-X / Path-Y per D-15-06.

`autonomous: false` because Task 1 (audit gate) is a `checkpoint:human-verify` gate per D-15-06 — audit failure (anything other than `passed`) is a HARD BLOCK that halts the plan; operator must approve the audit outcome before Tasks 2-7 proceed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/RETROSPECTIVE.md
@.planning/phases/15-v0-20-milestone-close-out-retrospective/15-CONTEXT.md
@.planning/phases/15-v0-20-milestone-close-out-retrospective/15-RESEARCH.md
@.planning/phases/15-v0-20-milestone-close-out-retrospective/15-PATTERNS.md
@.planning/phases/15-v0-20-milestone-close-out-retrospective/15-VALIDATION.md
@.planning/phases/15-v0-20-milestone-close-out-retrospective/15-04-live-rerun-evidence.log
@.planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VERIFICATION.md
@.planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VALIDATION.md
@.planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/deferred-items.md
@.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VERIFICATION.md
@.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VALIDATION.md
@.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-CONTEXT.md
@.planning/milestones/v0.19-phases/12-v0-19-close-out-reconciliation/12-07-PLAN.md
@.planning/milestones/v0.19-phases/12-v0-19-close-out-reconciliation/12-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Audit gate — `/gsd-audit-milestone v0.20` + Wave 1 pre-flight + Path-X (passed) / Path-Y (failed) Contingency per D-15-06</name>
  <what-built>
    Wave 1 (Plans 15-01, 15-02, 15-03, 15-04) has landed: REQUIREMENTS.md has 23 v0.20 mandatory `[x]` checkboxes + 5 OQ lock-in citations (Plan 15-01); STATE.md is mid-phase consistent (Plan 15-02); ROADMAP.md Phase 14 row is `4/4 Complete` and Phase 15 row reflects in-progress mid-phase state (Plan 15-03); Plan 15-04 evidence log exists with `Plan 15-04 status: GREEN` (live bats GREEN + cluster-admin denials + `task rebuild` SLO < 230s + post-rebuild health-check exit 0).

    This task: (a) verifies Wave 1 closure with exact-count grep gates; (b) invokes `/gsd-audit-milestone v0.20` to regenerate `.planning/v0.20-MILESTONE-AUDIT.md`; (c) presents the audit output for operator approval per D-15-06 Path-X (passed) vs Path-Y (failed) Contingency branches. This is the SOLE blocking gate of the plan — if audit fails, Tasks 2-7 do NOT execute.
  </what-built>
  <how-to-verify>
    **Step 1 — Wave 1 pre-flight verification (exact-count grep gates; HALT if any fail).**

    Run these grep checks in Bash; ALL must return their expected values (use `-eq`, not `-ge`):

    ```bash
    # Plan 15-01: 11 v0.20 mandatory REQ-IDs flipped
    test $(grep -v '^#' .planning/REQUIREMENTS.md | grep -cE "^- \[x\] \*\*(DEMO|RUNBOOK)-") -eq 11

    # Plan 15-01: combined v0.20 mandatory [x] count is 23
    test $(grep -v '^#' .planning/REQUIREMENTS.md | grep -cE "^- \[x\] \*\*(RBAC|SEED|DEMO|RUNBOOK|DELIVERY)-") -eq 23

    # Plan 15-01: 5 OQ lock-in citations landed (RBAC-01, SEED-03, DELIVERY-01, RUNBOOK-05)
    test $(grep -c "Phase 13 D-13-01" .planning/REQUIREMENTS.md) -ge 1
    test $(grep -c "Phase 13 D-13-02" .planning/REQUIREMENTS.md) -ge 1
    test $(grep -c "Phase 13 D-13-03" .planning/REQUIREMENTS.md) -ge 1
    test $(grep -c "Phase 14 D-14-02" .planning/REQUIREMENTS.md) -ge 1

    # Plan 15-02: STATE.md frontmatter mid-phase state
    test $(grep -cE "^last_activity: 2026-05-20" .planning/STATE.md) -eq 1
    test $(grep -c "^  total_plans: 14$" .planning/STATE.md) -eq 1

    # Plan 15-03: ROADMAP.md Phase 14 row Complete
    test $(grep -cE "^- \[x\] \*\*Phase 14:" .planning/ROADMAP.md) -eq 1
    test $(grep -cE "^\| 14\. .* \| v0.20 \| 4/4 \| Complete" .planning/ROADMAP.md) -eq 1

    # Plan 15-04: live-rerun evidence log exists with GREEN summary
    test -f .planning/phases/15-v0-20-milestone-close-out-retrospective/15-04-live-rerun-evidence.log
    test $(grep -c "Plan 15-04 status: GREEN" .planning/phases/15-v0-20-milestone-close-out-retrospective/15-04-live-rerun-evidence.log) -eq 1
    test $(grep -c "RED (not ok): 0" .planning/phases/15-v0-20-milestone-close-out-retrospective/15-04-live-rerun-evidence.log) -eq 1

    # Phase 13 + Phase 14 VALIDATION ledgers ALREADY final (precondition; not flipped by Phase 15)
    test $(grep -c "^status: final$" .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VALIDATION.md) -eq 1
    test $(grep -c "^nyquist_compliant: true$" .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VALIDATION.md) -eq 1
    test $(grep -c "^wave_0_complete: true$" .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VALIDATION.md) -eq 1
    test $(grep -c "^status: final$" .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VALIDATION.md) -eq 1
    test $(grep -c "^nyquist_compliant: true$" .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VALIDATION.md) -eq 1
    test $(grep -c "^wave_0_complete: true$" .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VALIDATION.md) -eq 1
    ```

    If ANY check fails: HALT. Wave 1 has a regression; surface in SUMMARY which check failed and refer the affected Plan 15-NN to revision mode before Plan 15-05 can resume.

    **Step 2 — Invoke `/gsd-audit-milestone v0.20`.**

    Run the audit:

    ```
    /gsd-audit-milestone v0.20
    ```

    This regenerates `.planning/v0.20-MILESTONE-AUDIT.md`. Read the frontmatter:

    ```bash
    head -20 .planning/v0.20-MILESTONE-AUDIT.md
    ```

    Capture the FULL audit output verbatim into the Plan 15-05 SUMMARY's Notable Observations section (per D-15-06 audit-output-inline requirement). The verbatim output is the evidence anchor for Plan 15-05's SC-4 closure.

    **Step 3 — Path-X (audit returns `passed`):** APPROVE; proceed to Task 2. Required: frontmatter `status: passed`; `gaps.requirements: []` (all 23 v0.20 mandatory satisfied; ADDON-01/02 stay deferred to v0.21 — if they appear in `gaps.requirements`, the audit logic includes Future Requirements and Path-X-with-tech-debt applies per Plan 12-07 Contingency 1 / RESEARCH OQ Pitfall 5); `tech_debt: []`; both Phase 13 + Phase 14 VALIDATION ledgers reported finalized in the audit's Nyquist section.

    **Step 4 — Path-Y (audit returns anything other than `passed`, e.g., `tech_debt > 0`, `gaps_found`, REQ unsatisfied, VALIDATION ledger not final):** HALT. Plan 15-05 does NOT proceed to Tasks 2-7. Orchestrator MUST either:
    - (a) insert a remediation plan (e.g., `/gsd-insert-phase 14.1` if Phase 14 regression; or a new Plan 15-06 if a Wave 1 plan needs revision); land it; rerun audit; restart Plan 15-05 from Task 1.
    - (b) if the audit is `tech_debt` solely due to ADDON-01/02 (Future Requirements), apply Path-X-with-tech-debt per Plan 12-07 Contingency 1: acknowledge ADDON-01/02 as acceptable v0.21-deferred tech_debt and approve to proceed (SC-4 satisfied with lenient reading). Cite the exact ADDON entries in the resume signal.

    The milestone is NOT marked `ready_to_archive` until the audit returns `passed` (strict) OR `tech_debt` with ONLY ADDON-01/02 entries (lenient + operator approval).

    **Step 5 — Operator decision recorded in resume signal.**

    Operator types one of:
    - `approved` — audit returned `passed` cleanly; proceed.
    - `path-x` — audit returned `tech_debt` only for ADDON-01/02; operator accepts as v0.21-deferred; proceed.
    - `path-y` — audit returned non-acceptable failure; HALT; surface remediation pathway in SUMMARY.

    Any other text triggers revision mode on the affected plan.
  </how-to-verify>
  <resume-signal>
    Type `approved` (audit passed strict) OR `path-x` (tech_debt for ADDON-01/02 only; accepted as v0.21-deferred) OR `path-y` (audit failed; HALT + describe remediation). Any other text triggers revision mode.
  </resume-signal>
  <acceptance_criteria>
    - All 17 Wave 1 pre-flight grep gates return their expected values (exact-count `-eq` semantics; no over-counts)
    - `.planning/v0.20-MILESTONE-AUDIT.md` regenerated (file exists; `git diff --stat` shows changes since prior HEAD)
    - Audit frontmatter `status:` is either `passed` (Path-X primary) OR `tech_debt` with ONLY ADDON-01/02 in `gaps.requirements` (Path-X-with-tech-debt per Contingency 1)
    - Operator resume signal received: `approved` OR `path-x` (Tasks 2-7 proceed) OR `path-y` (HALT; Tasks 2-7 do NOT execute)
    - SUMMARY's Notable Observations section contains the verbatim audit output (per D-15-06 inline-audit-output requirement)
    - If Path-Y triggered: SUMMARY surfaces the specific audit failure mode + recommended remediation pathway
    - **Threat ref T-15-04 (LOW):** Audit `passed` but `/gsd-complete-milestone v0.20` precondition fails — operator inspects audit output at this checkpoint as the mitigation
  </acceptance_criteria>
  <done>
    Audit gate cleared (Path-X primary OR Path-X-with-tech-debt + operator approval). Tasks 2-7 proceed. If Path-Y triggered, plan halts and orchestrator inserts remediation per D-15-06 hard-block contract.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append v0.20 retrospective section to `.planning/RETROSPECTIVE.md` using Append-after-anchor idiom (D-15-04)</name>
  <files>.planning/RETROSPECTIVE.md</files>
  <read_first>
    - .planning/RETROSPECTIVE.md (full file — confirms v0.18 section at lines 5-58, v0.19 section at lines 62-106, `## Cross-Milestone Trends` at line 110; v0.19 section ends with "Cost Observations" sub-section closing at line 106 + `---` H2 separator at line 108)
    - .planning/phases/15-v0-20-milestone-close-out-retrospective/15-CONTEXT.md §"Retrospective Shape (Success Criterion 5)" D-15-04 (sub-section cadence + content guidance)
    - .planning/phases/15-v0-20-milestone-close-out-retrospective/15-RESEARCH.md §"Retrospective Shape" (verbatim sub-section content for v0.20)
    - .planning/phases/15-v0-20-milestone-close-out-retrospective/15-PATTERNS.md §"Retrospective append shape" (Phase 12 + RETROSPECTIVE.md v0.19 section excerpt as the analog)
    - .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VERIFICATION.md (source for "What Was Built" Phase 13 bullets)
    - .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VERIFICATION.md (source for "What Was Built" Phase 14 bullets + "What Was Inefficient" friction notes)
    - .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-CONTEXT.md (D-13-01..03 for OQ Resolutions table)
    - .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-CONTEXT.md (D-14-01..03 for OQ Resolutions table)
    - .planning/v0.20-MILESTONE-BRIEFING.md (briefing OQ-1..5 recommended defaults; for OQ Resolutions citations)
  </read_first>
  <action>
    Use the Edit tool ONCE against `.planning/RETROSPECTIVE.md` with the v0.19 section's CLOSING line (line 108 — the `---` H2 separator immediately AFTER the v0.19 "Cost Observations" sub-section closes at line 106) as the unique `old_string` anchor. This is the **Append-after-anchor idiom** per PATTERNS — the `old_string` matches a multi-line window that uniquely identifies the insertion point (v0.19 section's closing block) and the `new_string` replaces it with the same window PLUS the new v0.20 section.

    **`old_string`** (verbatim — lines 106-110 from current RETROSPECTIVE.md HEAD; the closing 2 lines of v0.19's Cost Observations + the `---` separator + blank line + `## Cross-Milestone Trends` opener — this 5-line window is unique because "Cost Observations" appears exactly twice (once each for v0.18 and v0.19) but ONLY the v0.19 instance is immediately followed by `## Cross-Milestone Trends`):

    ```
    - **Notable:** Parallel worktree execution (Phase 12 Wave 1) and `--auto` mode (Phase 11 + Phase 12 execute) cut wall-time meaningfully; the cost trade-off was the Phase 12 SDK quirks (counter overshoot in `phase.complete`, audit-open frontmatter parse) that needed manual correction.

    ---

    ## Cross-Milestone Trends
    ```

    **`new_string`** (verbatim — identical 5-line window PLUS the new v0.20 section inserted between the `---` separator and the `## Cross-Milestone Trends` heading):

    ```
    - **Notable:** Parallel worktree execution (Phase 12 Wave 1) and `--auto` mode (Phase 11 + Phase 12 execute) cut wall-time meaningfully; the cost trade-off was the Phase 12 SDK quirks (counter overshoot in `phase.complete`, audit-open frontmatter parse) that needed manual correction.

    ---

    ## Milestone: v0.20 — Capsule POC Demo & RBAC Polish

    **Shipped:** 2026-05-20
    **Phases:** 3 (13-15) | **Plans:** 14 | **Tasks:** ~32 | **Timeline:** 2026-05-19 → 2026-05-20

    ### What Was Built

    - `tenant-workload-editor` ClusterRole + per-tenant human-tenant-owner ServiceAccounts + Tenant CR additive `spec.owners[]` co-ownership (Phase 13 RBAC-01..03; D-13-01 read-only Secrets policy)
    - Platform-owner SA + dual-subject ClusterRoleBinding + `issue-platform-owner-kubeconfig.sh` + `new-tenant.sh` provisioning helper (Phase 13 RBAC-04..06)
    - `GlobalTenantResource` CR + hello-world Pod + Service propagation across all tenant namespaces within 60s (Phase 13 SEED-01..03; D-13-02 Pod+Service shape; FQCI `docker.io/library/nginx:alpine` per Pitfall 13-P1)
    - GitOps delivery under `pocs/capsule/` + `docs/poc-capsule.md` platform-owner H2 append (Phase 13 DELIVERY-01..03; D-13-03 — NO `task seed-capsule-demo` Taskfile entry, declined for P31 isolation)
    - Two-perspective demo verification (platform-owner kubeconfig + tenant-owner kubeconfigs via capsule-proxy NodePort 30443) end-to-end bats GREEN against `spoke-capsule` (Phase 14 DEMO-01..06)
    - `docs/poc-capsule-demo.md` 6-act demo runbook + pre-demo checklist + Known Noise appendix + `docs/capsule-on-eks.md` three-tier-on-EKS append (Phase 14 RUNBOOK-01..05; D-14-01 TLS noise = document-only; D-14-02 marquee tenant = `charlie`; D-14-03 provision style = live)
    - OQ-1..5 resolutions landed against falsifiable evidence: TLS noise document-only, Secrets policy read-only, hello-world Pod+Service, GitOps delivery (no Taskfile), `charlie` marquee tenant with live provisioning

    ### What Worked

    - **Lean / additive milestone framing held** (per `v0.20-MILESTONE-BRIEFING.md`): 3-phase decomposition + anti-scope guard rejected every "while we're at it" addition during discuss-phase; ADDON-01/02 + G-04 + KARYON_PHASE11_STRICT_LIVE all stayed deferred to v0.21
    - **Additive-only constraint** preserved alpha + bravo Flux reconciliation across all three phases (RBAC-03 + DELIVERY-03 regression gates GREEN at Phase 15 close); existing Flux SA owner pattern intact; `spec.owners[]` additive append only
    - **Wave 0 RED bats Nyquist gate** carried forward verbatim from v0.18 + v0.19 to Phase 13 (19 NEW bats files locked the RBAC + SEED + DELIVERY contracts BEFORE Plan 13-01 implementation) and Phase 14 (11 NEW bats files locked DEMO + RUNBOOK contracts BEFORE Plan 14-01 implementation)
    - **UAT cold-read caught actual runbook friction** (Phase 14 Plan 14-03 auto-attested-with-carry-forward UAT pattern): the runbook was executable cold in ≤ 15 min, and Plan 15-04 live regression rerun + Plan 15-05 audit gate retired the UAT carry-forward cleanly
    - **Phase 12 close-out template scaled cleanly to Phase 15 compressed form** (5 plans / 2 waves vs Phase 12's 7 plans / 2 waves) — supersedure-of-prior-VERIFICATION work was unnecessary because Phase 13 + Phase 14 VERIFICATION ledgers were already final at Phase 15 open
    - **Three-tier permission model** as a load-bearing architectural concept proved falsifiable end-to-end via 3 distinct `kubectl auth can-i '*' '*' = no` denials (platform-owner + alpha-owner + bravo-owner) — the conceptual model maps 1:1 to falsifiable bats + visceral kubectl assertions

    ### What Was Inefficient

    - **capsule-controller TLS noise** (`tls: bad certificate` 1Hz chatter) treated as cosmetic per OQ-1 D-14-01 (document-only); cert-rotation layer fix deferred to graduation. The right architectural fix is cert-manager wiring or proper CA distribution, but neither is in scope for a POC demo
    - **`spoke-capsule` cluster state-recovery dance** (Phase 14 destroy-poc → Plan 15-04 pre-flight needed `flux resume kustomization poc-capsule-spoke{,-rbac,-tenants}` recovery + `task fix-dns-poc-capsule` invocation): the rebuild + resume + DNS-fix sequence is operator-knowledge-heavy and would benefit from a `task spoke-capsule-recover` wrapper — but that wrapper itself was declined for P31 isolation, so the workaround is documented-only
    - **`origin/main` push gap** surfaced as a Plan 14-03 carry-forward (UAT cold-run blocked by `origin/main@49e264a2` lag); Plan 15-04 Task 1 pre-flight resolved it via mandatory `git fetch` + `HEAD..origin/main count == 0` gate. Future milestones: build a pre-Wave-2 push-currency check into the close-out template
    - **Two `delivery-05-live-slo-regression.bats` execution paths** (env-gated bats vs direct `time task rebuild`) both satisfied DELIVERY-02 but added ambiguity about which is canonical; Plan 15-04 chose the direct path for explicit wall-clock measurement clarity

    ### Patterns Established

    - **Three-tier permission model** as a falsifiable architectural articulation: Tier 1 (Cloud Ops, out-of-scope/talk-track only) / Tier 2 (Karyon Platform Team, platform-owner role; `kubectl auth can-i '*' '*' = no` + Tenant CR CRUD CAN + co-owner LIST/WATCH/GET/LOGS/EXEC) / Tier 3 (Tenant Devs, tenant-owner role via capsule-proxy NodePort 30443; `kubectl auth can-i '*' '*' = no` + own-tenant-only LIST + cross-tenant Forbidden + `kubectl create secret` Forbidden)
    - **Co-owner ClusterRoleBinding pattern** for platform-owner LIST/WATCH/GET/LOGS/EXEC across every tenant namespace (additive append to existing `spec.owners[]` — preserves v0.19 Flux SA owner)
    - **`new-tenant.sh` provisioning template** (per OQ-5 D-14-02 + D-14-03 — marquee tenant `charlie` with live provisioning style) — default-includes `capsule-platform-owners` in every newly-created Tenant CR's `spec.owners[]`
    - **6-act demo runbook cadence** with executable pre-demo checklist + Known Noise appendix + production-translation forward-pointer to `docs/capsule-on-eks.md` (three-tier-on-EKS mapping)
    - **Phase 15 5-plan compressed close-out** (vs Phase 12's 7-plan template): omits Plan 12-03 (Phase-11 VERIFICATION supersedure) entirely; collapses Plan 12-04 (4 VALIDATION ledgers close) into Plan 15-05 Task 4 since only the Phase 15 ledger needs flipping (Phase 13 + Phase 14 ledgers were already final at phase-open)
    - **Living-doc retrospective convention** (D-15-04): single `.planning/RETROSPECTIVE.md` with sequential `## Milestone: vX.YZ` section appends rather than per-milestone standalone files — preserves cross-milestone scroll-comparison for future readers
    - **Audit-gate + Path-X/Path-Y Contingency** (D-15-06; inherited from Plan 12-07): hard-block on audit failure; lenient Path-X-with-tech-debt acceptable ONLY for ADDON-pattern Future-Requirements drift

    ### OQ Resolutions

    | OQ | Briefing-recommended default | Landed Decision | Phase / D-code |
    |----|------------------------------|----------------|----------------|
    | OQ-1 (TLS noise treatment) | document-only | document-only (cert-rotation-layer fix deferred to graduation) | Phase 14 D-14-01 |
    | OQ-2 (`tenant-workload-editor` Secrets policy) | read-only | read-only (`get`/`list`/`watch` only; explicit deny on `create`/`update`/`patch`/`delete`) | Phase 13 D-13-01 |
    | OQ-3 (hello-world workload shape) | Pod + Service | Pod + Service (FQCI `docker.io/library/nginx:alpine` per Pitfall 13-P1) | Phase 13 D-13-02 |
    | OQ-4 (delivery path) | GitOps `pocs/capsule/` | GitOps `pocs/capsule/` (NO `task seed-capsule-demo` Taskfile entry — declined for P31 isolation) | Phase 13 D-13-03 |
    | OQ-5 (new-tenant name + provision style) | `charlie` + live | `charlie` + live (single canonical name across runbook + `new-tenant.sh` + bats fixtures) | Phase 14 D-14-02 + D-14-03 |

    ### Carry-forward updates

    - **Closed by v0.20:** (none) — v0.20 was purely additive; no v0.19 carry-forward items were closed. ADDON-01/02 stay deferred to v0.21; G-04 PostBuild workaround stays; KARYON_PHASE11_STRICT_LIVE remains optional; slo-regression-live env-gate stays; markdownlint + gitleaks-proxy-06 + Phase 1-5 shellcheck + first-push CI + real secret management + frontmatter reconciliation all stay deferred per `v0.20-MILESTONE-BRIEFING.md` "Out of Scope".
    - **Added to parking lot by v0.20:** (a) Fix `tls: bad certificate` capsule-controller chatter at the cert-rotation layer (cert-manager wiring or proper CA distribution) — was OQ-1 `document-only` per D-14-01; cross-linked to ADR-008 (DEFER) and `docs/poc-capsule-demo.md` Known Noise appendix; revisit at capsule graduation or production EKS cut. (b) `task demo-capsule` Taskfile wrapper — tempting ergonomics but breaks P31 isolation (would add a `spoke-capsule` mention to the default-path Taskfile); revisit at graduation or in a POC-ergonomics milestone.

    ### Key Lessons

    1. **Lean / additive milestone framing scales.** v0.20's 3-phase decomposition (RBAC + SEED + DELIVERY → DEMO + RUNBOOK → close-out) with anti-scope guard active during discuss-phase shipped 23 mandatory REQs across 14 plans without any "while we're at it" scope creep. Future lean milestones should preserve the briefing's "in-scope / out-of-scope" lists as enforceable contracts during discuss-phase.
    2. **Three-tier permission model is the right articulation for Capsule POC demos.** Tier 1/2/3 maps cleanly to Cloud-Ops / Platform-Team / Tenant-Devs roles and lets each tier's ceiling be falsified by a distinct `kubectl auth can-i '*' '*' = no` assertion + a positive control. The articulation is reusable for any future multi-tenancy demo (Capsule, vCluster, kiosk, etc.).
    3. **Phase 12 close-out template compresses cleanly when no prior-VERIFICATION supersedure work is needed.** v0.19 Phase 12 needed 7 plans because Plan 12-03 superseded Phase 11's pre-fix VERIFICATION artifact; v0.20 Phase 15 needed only 5 plans because Phase 13 + Phase 14 VERIFICATION ledgers were already final at phase-open. Future close-out phases should estimate plan count by counting (a) artifact reconciliation surfaces + (b) VERIFICATION supersedure needs + (c) live regression rerun gates separately, not by analogy to prior milestones.
    4. **Living-doc retrospective is the right convention.** Single `.planning/RETROSPECTIVE.md` with sequential `## Milestone: vX.YZ` sections preserves cross-milestone scroll-comparison (a future reader compares v0.18 → v0.19 → v0.20 patterns + lessons in one file). Per-milestone standalone files would fragment that signal.
    5. **OQ-resolution table is the cleanest format for retrospective OQ closure.** The 4-column table (OQ # | briefing default | landed decision | Phase/D-code) makes the briefing-vs-landed delta scannable and auditable without prose. Future milestones with open questions should adopt this table format verbatim.

    ### Cost Observations

    - **Model mix:** Opus 4.7 (1M context) for orchestration + planning + most executor agents; occasional Sonnet for shorter executor agents. No deviation from v0.18 + v0.19 pattern.
    - **Sessions:** ~2-3 sessions across 2 days (2026-05-19 → 2026-05-20); STATE.md + per-phase CONTEXT.md + per-plan SUMMARY.md anchored cross-session handoffs.
    - **Notable:** Phase 15 compressed close-out (5 plans / 2 waves) executed in significantly less wall-time than Phase 12 (7 plans / 2 waves) — the no-supersedure-needed shortcut is real. Plan 15-04 (live regression rerun) was the longest single plan due to `task rebuild` cold-run wall-time (~3-4 min), comparable to Phase 11 VAL-04 + VAL-05 combined.

    ---

    ## Cross-Milestone Trends
    ```

    The Edit succeeds because the 5-line `old_string` window (last bullet of v0.19 Cost Observations + blank + `---` + blank + `## Cross-Milestone Trends`) is globally unique in RETROSPECTIVE.md — only the v0.19 section's Cost Observations is followed by `## Cross-Milestone Trends`; the v0.18 section's Cost Observations is followed by the v0.19 section header instead.
  </action>
  <verify>
    <automated>test $(grep -c "^## Milestone: v0.20 — Capsule POC Demo & RBAC Polish$" .planning/RETROSPECTIVE.md) -eq 1 && test $(grep -c "^## Milestone: v0.19 — Capsule Multi-Tenancy POC$" .planning/RETROSPECTIVE.md) -eq 1 && test $(grep -c "^## Milestone: v0.18 — karyon Lab v1$" .planning/RETROSPECTIVE.md) -eq 1 && test $(grep -c "^### OQ Resolutions$" .planning/RETROSPECTIVE.md) -eq 1 && test $(grep -c "^### Carry-forward updates$" .planning/RETROSPECTIVE.md) -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Milestone: v0.20 — Capsule POC Demo & RBAC Polish$" .planning/RETROSPECTIVE.md` returns 1 (single v0.20 section added)
    - `grep -c "^## Milestone: v0.19 — Capsule Multi-Tenancy POC$" .planning/RETROSPECTIVE.md` returns 1 (v0.19 section preserved untouched)
    - `grep -c "^## Milestone: v0.18 — karyon Lab v1$" .planning/RETROSPECTIVE.md` returns 1 (v0.18 section preserved untouched)
    - `grep -c "^## Cross-Milestone Trends$" .planning/RETROSPECTIVE.md` returns 1 (Trends section preserved AFTER the new v0.20 section)
    - All 7 required sub-headings present at least once each within the v0.20 section: `Shipped` / `What Was Built` / `What Worked` / `What Was Inefficient` / `Patterns Established` / `OQ Resolutions` / `Carry-forward updates`
    - `grep -c "Phase 14 D-14-01" .planning/RETROSPECTIVE.md` returns 1 (OQ-1 citation in OQ Resolutions table)
    - `grep -c "Phase 13 D-13-01" .planning/RETROSPECTIVE.md` returns 1 (OQ-2 citation in OQ Resolutions table)
    - `grep -c "Phase 13 D-13-02" .planning/RETROSPECTIVE.md` returns 1 (OQ-3 citation in OQ Resolutions table)
    - `grep -c "Phase 13 D-13-03" .planning/RETROSPECTIVE.md` returns 1 (OQ-4 citation in OQ Resolutions table)
    - `grep -c "Phase 14 D-14-02 + D-14-03" .planning/RETROSPECTIVE.md` returns 1 (OQ-5 citation in OQ Resolutions table)
    - **Threat ref T-15-06 (LOW):** Retrospective append did NOT overwrite v0.19 — verified by the v0.19 section's `## Milestone: v0.19 — Capsule Multi-Tenancy POC$` heading grep returning 1
  </acceptance_criteria>
  <done>
    v0.20 retrospective section appended to `.planning/RETROSPECTIVE.md` with all 7 required sub-headings + OQ resolutions table + carry-forward updates; v0.18 + v0.19 sections untouched; `## Cross-Milestone Trends` heading preserved after the new section. Living-doc cadence per D-15-04 honored.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reconcile carry-forward parking lot in `.planning/PROJECT.md` — append Phase 13/14-surfaced deferrals (D-15-05)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md lines 87-99 (current `**Carry-forward parking lot**` section — confirms all 10 existing bullets verbatim: ADDON-01/02 line 89, G-04 line 90, KARYON_PHASE11_STRICT_LIVE line 91, slo-regression-live line 92, markdownlint line 93, gitleaks-proxy-06 line 94, Phase 1-5 shellcheck line 95, first-push CI line 96, real secret management line 97, frontmatter reconciliation line 98; section ends at blank line 99 + `### Out of Scope` H3 at line 100)
    - .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/deferred-items.md (Phase 13-surfaced deferrals — read FIRST at task time; append any non-trivial items as additional parking lot bullets)
    - .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-CONTEXT.md §`<deferred>` (Phase 14 deferred ideas — read for TLS-fix-at-cert-layer + `task demo-capsule` framing)
    - .planning/phases/15-v0-20-milestone-close-out-retrospective/15-CONTEXT.md §"Carry-Forward Parking Lot Reconciliation" D-15-05 (verbatim append guidance)
    - .planning/phases/15-v0-20-milestone-close-out-retrospective/15-PATTERNS.md §"PROJECT.md parking-lot reconciliation" (additive bullet append idiom; no Phase 12 analog — author from CONTEXT D-15-05 directly)
  </read_first>
  <action>
    Use the Edit tool ONCE against `.planning/PROJECT.md` with the last existing parking-lot bullet (line 98 — `Stale frontmatter reconciliation pass`) PLUS the trailing blank line + `### Out of Scope` H3 opener as the unique 3-line `old_string` anchor window (per PATTERNS Edit-tool unique-old_string convention). The `new_string` replaces this 3-line window with the same anchor PLUS the new bullets appended between the last existing bullet and the blank line.

    **Before Edit — read Phase 13 deferred-items.md** (if it exists) to identify any non-trivial items beyond TLS-fix + `task demo-capsule`. Common items to anticipate (verify at task time): nothing additional, OR Phase 13-specific RBAC narrowing edge cases that were deferred. If `deferred-items.md` is absent or empty, only the two D-15-05-mandated new bullets land.

    **`old_string`** (verbatim — lines 98-100 from current PROJECT.md HEAD; trailing blank + `### Out of Scope` H3 is the unique anchor because `### Out of Scope` appears exactly once in PROJECT.md):

    ```
    - Stale frontmatter reconciliation pass (v0.18 close carryover)

    ### Out of Scope
    ```

    **`new_string`** (verbatim — identical anchor window PLUS new bullets inserted between the last existing bullet and the blank line):

    ```
    - Stale frontmatter reconciliation pass (v0.18 close carryover)
    - Fix the `tls: bad certificate` capsule-controller chatter at the cert-rotation layer (cert-manager wiring / proper CA distribution) — was OQ-1 `document-only` in v0.20 per Phase 14 D-14-01; cross-linked to ADR-008 (DEFER) and `docs/poc-capsule-demo.md` Known Noise appendix. Revisit at capsule graduation or production EKS cut. (v0.20 close carryover)
    - `task demo-capsule` Taskfile wrapper for one-shot demo execution — tempting ergonomics but breaks P31 isolation (would add a `spoke-capsule` mention to the default-path Taskfile, which the v0.19 P31 contract forbids). Revisit at graduation or in a POC-ergonomics milestone. (v0.20 close carryover)

    ### Out of Scope
    ```

    **If Phase 13 `deferred-items.md` contains additional non-trivial items at task time:** insert each as an additional bullet between the `task demo-capsule` line and the blank line, preserving the same bullet-prefix idiom (`- <description> ... (v0.20 close carryover)`). DO NOT inline trivial items (typos, copy-edit notes, etc.) — only items that constitute future architectural / scope decisions.

    The Edit succeeds because the 3-line `old_string` window (last existing bullet + blank + `### Out of Scope`) is unique in PROJECT.md (the H3 `### Out of Scope` appears exactly once).
  </action>
  <verify>
    <automated>test $(grep -c "Fix the \`tls: bad certificate\` capsule-controller chatter" .planning/PROJECT.md) -eq 1 && test $(grep -c "\`task demo-capsule\` Taskfile wrapper" .planning/PROJECT.md) -eq 1 && test $(grep -c "^### Out of Scope$" .planning/PROJECT.md) -eq 1 && test $(grep -c "ADDON-01 / ADDON-02 deferred from v0.19 Phase 12" .planning/PROJECT.md) -eq 1 && test $(grep -c "Stale frontmatter reconciliation pass (v0.18 close carryover)" .planning/PROJECT.md) -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Fix the \`tls: bad certificate\` capsule-controller chatter" .planning/PROJECT.md` returns 1 (new TLS-fix bullet present)
    - `grep -c "\`task demo-capsule\` Taskfile wrapper" .planning/PROJECT.md` returns 1 (new task-demo-capsule bullet present)
    - All 10 pre-existing bullets preserved verbatim:
      - `grep -c "ADDON-01 / ADDON-02 deferred from v0.19 Phase 12" .planning/PROJECT.md` returns 1
      - `grep -c "G-04 architectural decision / split-Kustomization refactor" .planning/PROJECT.md` returns 1
      - `grep -c "KARYON_PHASE11_STRICT_LIVE=1" .planning/PROJECT.md` returns 1
      - `grep -c "slo-regression-live \`KARYON_REBUILD_APPROVED\`" .planning/PROJECT.md` returns 1
      - `grep -c "Pre-existing markdownlint failures" .planning/PROJECT.md` returns 1
      - `grep -c "Pre-existing gitleaks finding in \`proxy-06\`" .planning/PROJECT.md` returns 1
      - `grep -c "Phase 1-5 shellcheck warning cleanup (SC2034 / SC2155)" .planning/PROJECT.md` returns 1
      - `grep -c "First-push CI verification" .planning/PROJECT.md` returns 1
      - `grep -c "Real secret management (Vault / SOPS / age)" .planning/PROJECT.md` returns 1
      - `grep -c "Stale frontmatter reconciliation pass (v0.18 close carryover)" .planning/PROJECT.md` returns 1
    - `grep -c "^### Out of Scope$" .planning/PROJECT.md` returns 1 (Out of Scope H3 preserved exactly once)
    - `grep -c "(v0.20 close carryover)" .planning/PROJECT.md` returns at least 2 (new bullets carry the citation)
    - Parking lot section overall bullet count increased by at least 2 (verify with `awk '/\*\*Carry-forward parking lot\*\*/,/### Out of Scope/' .planning/PROJECT.md | grep -c "^- "` returns at least 12)
    - **Threat ref T-15-01 (LOW):** Edit-tool ambiguity mitigated — `old_string` anchor `### Out of Scope` is globally unique in PROJECT.md (verified)
  </acceptance_criteria>
  <done>
    Carry-forward parking lot reconciled inline: all 10 pre-existing bullets preserved; 2 new D-15-05-mandated bullets (TLS-fix-at-cert-layer + `task demo-capsule` wrapper) appended; any Phase 13 deferred-items.md non-trivial items also appended. v0.21 milestone briefing process can read PROJECT.md and seed the next milestone's scope-vs-defer decisions cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 4: Close `15-VALIDATION.md` Nyquist ledger — frontmatter advance + `## Finalized Evidence` body section (D-15-07)</name>
  <files>.planning/phases/15-v0-20-milestone-close-out-retrospective/15-VALIDATION.md</files>
  <read_first>
    - .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VALIDATION.md (current frontmatter at lines 1-8; body §"Wave 0 Requirements" + §"Validation Sign-Off" — confirms vacuous-Nyquist rationale already documented in body; only frontmatter values need flipping)
    - .planning/phases/15-v0-20-milestone-close-out-retrospective/15-04-live-rerun-evidence.log (Plan 15-04 evidence — extract: bats GREEN count + `task rebuild` duration + 3 `auth can-i` denials + `task health-check` exit 0 + `Plan 15-04 status: GREEN` literal)
    - .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VALIDATION.md frontmatter (Phase 14 finalized-evidence shape — `finalized: 2026-05-XX` + body `## Finalized Evidence` section precedent; verify shape at task time)
    - .planning/milestones/v0.19-phases/12-v0-19-close-out-reconciliation/12-VALIDATION.md frontmatter (Phase 12 close-out vacuous-Nyquist precedent — confirms `finalized_evidence:` frontmatter field is acceptable inline ALT to body `## Finalized Evidence` section; D-15-07 uses the body-section form per CONTEXT mandate)
    - .planning/phases/15-v0-20-milestone-close-out-retrospective/15-CONTEXT.md §"Nyquist / Wave 0 RED Bats — Not Applicable to Book-Keeping Phase" D-15-07 (vacuous-Nyquist rationale verbatim text)
  </read_first>
  <action>
    Use the Edit tool TWICE against `.planning/phases/15-v0-20-milestone-close-out-retrospective/15-VALIDATION.md`.

    **Edit 1 — Frontmatter advance (lines 1-8).**

    `old_string` (verbatim — current frontmatter block from HEAD):

    ```
    ---
    phase: 15
    slug: v0-20-milestone-close-out-retrospective
    status: draft
    nyquist_compliant: false
    wave_0_complete: false
    created: 2026-05-20
    ---
    ```

    `new_string` (advanced frontmatter — flip 3 fields + append `finalized: 2026-05-20`; preserve `phase` + `slug` + `created` byte-identical):

    ```
    ---
    phase: 15
    slug: v0-20-milestone-close-out-retrospective
    status: final
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-05-20
    finalized: 2026-05-20
    ---
    ```

    Diff: `status: draft → final`; `nyquist_compliant: false → true`; `wave_0_complete: false → true`; new line `finalized: 2026-05-20` appended before closing `---`. All other bytes identical.

    **Edit 2 — Body `## Finalized Evidence` section append.**

    Append a new H2 section AFTER the existing `## Validation Sign-Off` section (last section in the file per current HEAD). Use the file's terminal line (`**Approval:** pending — finalized at Plan 15-05 close (sets `status: final`, `nyquist_compliant: true`, `wave_0_complete: true`).`) as the unique `old_string` anchor.

    `old_string` (verbatim — terminal line of file):

    ```
    **Approval:** pending — finalized at Plan 15-05 close (sets `status: final`, `nyquist_compliant: true`, `wave_0_complete: true`).
    ```

    `new_string` (same anchor line + 2 blank lines + new `## Finalized Evidence` H2 section appended; trailing newline preserved):

    ```
    **Approval:** finalized 2026-05-20 by Plan 15-05 — frontmatter sets `status: final`, `nyquist_compliant: true`, `wave_0_complete: true`.

    ---

    ## Finalized Evidence

    Phase 15 finalized 2026-05-20 per D-15-07 + Plan 15-05 Task 4. This section documents the evidence anchors that satisfy the Nyquist gate for a book-keeping / close-out phase.

    ### Why no Wave 0 RED bats scaffold (vacuous-Nyquist for the no-new-behavior axis)

    Phase 15 introduced ZERO new behavior — it mutates only `.planning/*.md` artifacts (Plans 15-01..03, 15-05) plus the Plan 15-04 live-rerun evidence log. No production code paths added; no new CRDs, ClusterRoles, controllers, or scripts. Per CONTEXT D-15-07 + Phase 12 precedent (`.planning/milestones/v0.19-phases/12-v0-19-close-out-reconciliation/12-VALIDATION.md`), the Nyquist gate is satisfied **vacuously** for the no-new-behavior axis: zero new behavior implies zero new falsifiers required.

    ### Active falsifiers (non-vacuous Nyquist coverage for the regression-gate axis)

    Plan 15-04's live regression rerun acts as the **non-vacuous** falsifier for Phase 15's SC-3 (three-tier model regression-verified) claim. Evidence captured in `.planning/phases/15-v0-20-milestone-close-out-retrospective/15-04-live-rerun-evidence.log`:

    - **Phase 13 + Phase 14 live + static bats subset** (~207 @tests target) re-run verbatim against live `spoke-capsule`; result: 0 REDs, all GREEN (per evidence-log `RED (not ok): 0` line + `Plan 15-04 status: GREEN` footer). These bats are the Wave 0 RED scaffolds originally authored during Phases 13 + 14 — they remain the canonical falsifiers for RBAC-01..06 + SEED-01..03 + DELIVERY-01..03 + DEMO-01..06 + RUNBOOK-01..03/05 contracts.
    - **`kubectl auth can-i '*' '*' = no` for 3 distinct kubeconfigs** (platform-owner + alpha-owner + bravo-owner) — direct visceral falsifier for the Tier 2 + Tier 3 ceiling claims (RBAC-02 + RBAC-04). Captured at Plan 15-04 Task 3.
    - **`time task rebuild` cold-run wall-clock < 230s** — direct visceral falsifier for the DELIVERY-02 SLO claim (v0.18 baseline preserved). Captured at Plan 15-04 Task 5.
    - **`task health-check` exit 0 post-rebuild** — direct visceral falsifier for the DELIVERY-03 regression claim (additive-only delivery did not break alpha/bravo Flux reconciliation). Captured at Plan 15-04 Task 5.

    ### Audit-gate cross-verification

    `/gsd-audit-milestone v0.20` re-run at Plan 15-05 Task 1 returns `status: passed` (per `.planning/v0.20-MILESTONE-AUDIT.md`; full output captured in `15-05-SUMMARY.md` Notable Observations). The audit independently verifies that Phase 13 + Phase 14 VALIDATION ledgers have `status: final` + `nyquist_compliant: true` + `wave_0_complete: true` — confirming the pre-Wave-2 inheritance the entire Phase 15 plan-set relied on.

    ### Wave 0 sign-off rationale

    `wave_0_complete: true` set in frontmatter because no Wave 0 was required per the phase character (book-keeping; D-15-07). `nyquist_compliant: true` set in frontmatter because every Wave-1 plan's `<acceptance_criteria>` is grep-verifiable + was verified during execution (per the 15-VALIDATION.md §"Per-Task Verification Map" rows for Plans 15-01..04), AND Plan 15-04's live regression rerun serves as the active falsifier for the SC-3 regression-gate axis (non-vacuous Nyquist coverage).
    ```

    Both Edits succeed because:
    - Edit 1's `old_string` matches the unique 8-line frontmatter block (only one `---...---` block opens the file).
    - Edit 2's `old_string` matches the unique terminal `**Approval:** pending` line (appears exactly once in the file).
  </action>
  <verify>
    <automated>test $(grep -c "^status: final$" .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VALIDATION.md) -eq 1 && test $(grep -c "^nyquist_compliant: true$" .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VALIDATION.md) -eq 1 && test $(grep -c "^wave_0_complete: true$" .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VALIDATION.md) -eq 1 && test $(grep -c "^finalized: 2026-05-20$" .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VALIDATION.md) -eq 1 && test $(grep -c "^## Finalized Evidence$" .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VALIDATION.md) -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^status: final$" .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VALIDATION.md` returns 1
    - `grep -c "^nyquist_compliant: true$" .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VALIDATION.md` returns 1
    - `grep -c "^wave_0_complete: true$" .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VALIDATION.md` returns 1
    - `grep -c "^finalized: 2026-05-20$" .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VALIDATION.md` returns 1
    - `grep -c "^## Finalized Evidence$" .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VALIDATION.md` returns 1
    - `grep -c "vacuous-Nyquist" .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VALIDATION.md` returns at least 1
    - `grep -c "Plan 15-04" .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VALIDATION.md` returns at least 2 (vacuous + active-falsifier references)
    - `grep -c "RED (not ok): 0" .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VALIDATION.md` returns 1 (cross-reference to evidence log)
    - No frontmatter pre-existing fields lost (`grep -c "^phase: 15$" ...` returns 1; `grep -c "^slug: v0-20-milestone-close-out-retrospective$" ...` returns 1; `grep -c "^created: 2026-05-20$" ...` returns 1)
  </acceptance_criteria>
  <done>
    `15-VALIDATION.md` frontmatter advanced to closed-ledger state (`status: final`, `nyquist_compliant: true`, `wave_0_complete: true`, `finalized: 2026-05-20`); body has new `## Finalized Evidence` H2 section documenting vacuous-Nyquist rationale + Plan 15-04 active falsifiers + audit-gate cross-verification per D-15-07.
  </done>
</task>

<task type="auto">
  <name>Task 5: Write `15-VERIFICATION.md` — verifier output mirroring Phase 12 + Phase 14 shape</name>
  <files>.planning/phases/15-v0-20-milestone-close-out-retrospective/15-VERIFICATION.md</files>
  <read_first>
    - .planning/milestones/v0.19-phases/12-v0-19-close-out-reconciliation/12-VERIFICATION.md (full file — Phase 12 verifier shape: frontmatter + Goal Achievement table + per-SC evidence + Plan-by-plan summary; mirrors per PATTERNS analog mapping)
    - .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VERIFICATION.md (Phase 14 verifier shape — most-recent precedent for the verifier output format)
    - .planning/ROADMAP.md §"Phase 15" Success Criteria 1-5 (verbatim SC text for the per-criterion evidence rows)
    - .planning/phases/15-v0-20-milestone-close-out-retrospective/15-CONTEXT.md (D-15-01..07 for plan-by-plan summary anchors)
    - .planning/phases/15-v0-20-milestone-close-out-retrospective/15-04-live-rerun-evidence.log (SC-3 evidence anchor for the per-SC table row)
    - .planning/v0.20-MILESTONE-AUDIT.md (regenerated by Plan 15-05 Task 1; SC-4 evidence anchor for the per-SC table row)
    - .planning/RETROSPECTIVE.md (Plan 15-05 Task 2 output; SC-5 evidence anchor for the per-SC table row)
    - .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VERIFICATION.md (Phase 13 ROADMAP-SC reference shape — confirms format)
  </read_first>
  <action>
    Create a NEW file at `.planning/phases/15-v0-20-milestone-close-out-retrospective/15-VERIFICATION.md`. Use the Write tool (file does not exist yet).

    File content (full body):

    ```markdown
    ---
    phase: 15-v0-20-milestone-close-out-retrospective
    verified: 2026-05-20T00:00:00Z
    status: passed
    score: 5/5 ROADMAP Success Criteria verified; plan-level must-haves verified for Plans 15-01..05
    overrides_applied: 0
    re_verification: false
    ---

    # Phase 15: v0.20 Milestone Close-out + Retrospective — Verification Report

    ## Goal (restated from ROADMAP)

    Reconcile v0.20 milestone artifacts to match implementation reality, flip all REQ checkboxes against live evidence, run the milestone audit, capture lessons learned, and prepare the milestone for clean archival via `/gsd-complete-milestone`. Pattern-mirrors v0.19 Phase 12 close-out at smaller scale.

    ## Goal Achievement

    ### ROADMAP Success Criteria (5/5)

    | # | Success Criterion | Status | Evidence |
    |---|-------------------|--------|----------|
    | 1 | REQUIREMENTS.md fully reconciled — all 23 v0.20 mandatory `[ ]` checkboxes flipped to `[x]` with phase/plan citations; Traceability table reflects final phase mapping; any spec deviations captured inline | PASS | Plan 15-01 (`15-01-SUMMARY.md`): 11 DEMO + RUNBOOK flips landed; 4 OQ lock-in amendments inline (RBAC-01 → Phase 13 D-13-01 read-only Secrets; SEED-03 → Phase 13 D-13-02 Pod+Service; DELIVERY-01 → Phase 13 D-13-03 GitOps; RUNBOOK-05 → Phase 14 D-14-02 charlie + D-14-03 live); 11 Traceability rows flipped `Pending → Complete`. Final state: `grep -cE "^- \[x\] \*\*(RBAC\|SEED\|DEMO\|RUNBOOK\|DELIVERY)-" .planning/REQUIREMENTS.md` returns 23. |
    | 2 | STATE.md and ROADMAP.md consistent — STATE.md `last_activity` advanced past Phase 14 close-out; Performance Metrics By-Phase populated for Phases 13/14/15; ROADMAP.md Progress table refreshed; Phase Summary checkboxes flipped | PASS | Plan 15-02 (`15-02-SUMMARY.md`): STATE.md frontmatter mid-phase advanced. Plan 15-03 (`15-03-SUMMARY.md`): ROADMAP.md Phase 14 row `0/4 Not started → 4/4 Complete 2026-05-19`; Phase Summary Phase 14 `[ ] → [x]`. Plan 15-05 Task 6 + Task 7 (this plan) finalized: STATE.md `status: ready_to_archive` + `percent: 100` + `completed_plans: 14`; ROADMAP.md Phase 15 row `5/5 Complete 2026-05-20` + Phase Summary `[x]`. |
    | 3 | Three-tier model regression-verified — full Phase 13 + Phase 14 bats suite re-run GREEN against live `spoke-capsule`; `kubectl auth can-i '*' '*'` returns `no` for both platform-owner and tenant-workload-editor identities; alpha + bravo Flux reconciliation continues unbroken; v0.18 `task rebuild` SLO < 230s preserved | PASS | Plan 15-04 (`15-04-SUMMARY.md` + `15-04-live-rerun-evidence.log`): bats subset ~207 @tests GREEN with 0 REDs (per evidence log `RED (not ok): 0` + `Plan 15-04 status: GREEN`); 3/3 `kubectl auth can-i '*' '*' = no` denials captured (platform-owner + alpha-owner + bravo-owner); `task rebuild` cold-run exit 0 with wall-clock < 230s (DELIVERY-02 SLO PASS); `task health-check` exit 0 post-rebuild (DELIVERY-03 regression gate PASS — alpha/bravo Flux unbroken). |
    | 4 | Milestone audit passes — `/gsd-audit-milestone v0.20` returns `passed` with zero `tech_debt`, all 23 REQs satisfied, Nyquist VALIDATION ledgers closed for Phases 13/14 with `status: final` + `nyquist_compliant: true` + `wave_0_complete: true` | PASS | Plan 15-05 Task 1 (this plan): `/gsd-audit-milestone v0.20` regenerated `.planning/v0.20-MILESTONE-AUDIT.md` with `status: passed` (verbatim audit output captured in `15-05-SUMMARY.md` Notable Observations per D-15-06 inline-output requirement). Phase 13 + Phase 14 VALIDATION ledgers confirmed `status: final` + `nyquist_compliant: true` + `wave_0_complete: true` during pre-flight gate (Task 1 Step 1 grep checks). Operator approval: `<resume signal recorded in 15-05-SUMMARY>` (approved / path-x). |
    | 5 | Retrospective captured — `.planning/RETROSPECTIVE.md` v0.20 section documents lessons + OQ-1..5 resolutions + deviations + carry-forward parking lot updates + suggested patterns for future lean milestones; milestone is `ready_to_archive` | PASS | Plan 15-05 Task 2 (this plan): v0.20 retrospective section appended to `.planning/RETROSPECTIVE.md` (D-15-04 living-doc cadence); 7 required sub-headings present; OQ-1..5 resolutions table cites Phase 13 D-13-01/02/03 + Phase 14 D-14-01/02/03. Plan 15-05 Task 3: carry-forward parking lot reconciled in `.planning/PROJECT.md` (D-15-05 — 10 existing entries preserved + 2 new entries appended). Plan 15-05 Task 6: STATE.md `status: ready_to_archive`. |

    **Overall verdict:** **passed** — all 5 ROADMAP Success Criteria GREEN with concrete evidence anchors; milestone v0.20 is `ready_to_archive`.

    ## Plan-by-plan Summary

    | Plan | Wave | Status | Key Outcomes |
    |------|------|--------|--------------|
    | 15-01 | 1 | COMPLETE | 11 v0.20 mandatory `[ ] → [x]` flips (DEMO-01..06 + RUNBOOK-01..05); 4 OQ lock-in amendments inline; 11 Traceability table rows `Pending → Complete`. |
    | 15-02 | 1 | COMPLETE | STATE.md frontmatter advanced to mid-phase; Performance Metrics By-Phase rows added for Phase 13/14/15; Current Position body reflects Phase 15 EXECUTING. |
    | 15-03 | 1 | COMPLETE | ROADMAP.md Phase 14 row `0/4 Not started → 4/4 Complete 2026-05-19`; Phase Summary Phase 14 `[ ] → [x]`. |
    | 15-04 | 1 | COMPLETE | Live regression rerun: ~207 @tests GREEN with 0 REDs; 3/3 `auth can-i '*' '*' = no` denials; `task rebuild` SLO < 230s + exit 0; `task health-check` exit 0 post-rebuild. RUNBOOK-04 Plan 14-03 UAT carry-forward RETIRED. |
    | 15-05 | 2 | COMPLETE | `/gsd-audit-milestone v0.20` returns `passed`; v0.20 retrospective appended to `.planning/RETROSPECTIVE.md`; carry-forward parking lot reconciled in `.planning/PROJECT.md`; `15-VALIDATION.md` finalized; `15-VERIFICATION.md` written; STATE.md `ready_to_archive` + `percent: 100`; ROADMAP.md Phase 15 `5/5 Complete 2026-05-20` + `[x]`. |

    ## Anti-Pattern Regression Confirmations

    All v0.18 + v0.19 load-bearing invariants preserved across v0.20 — verified by Plan 15-04 live regression rerun + Plan 15-05 audit gate.

    | Invariant | Source | Verification Method | Result |
    |-----------|--------|---------------------|--------|
    | **P27** — every tenant Flux Kustomization MUST set `spec.serviceAccountName` explicitly | v0.19 Phase 9 contract; v0.19 Phase 11 falsifier | Plan 15-04 bats subset includes Phase 13 + Phase 14 P27 static greps; `task health-check` exit 0 post-rebuild confirms alpha + bravo Flux SAs unchanged | PASS |
    | **P29** — tenant kubeconfigs MUST NOT land in git | v0.19 Phase 7 `.gitignore` + `.gitleaks.toml` rule + Phase 11 VAL-05 one-shot history scan | Plan 15-04 mints all 3 kubeconfigs to `/tmp/karyon-tenants/` (outside repo); evidence log redacts kubeconfig contents (per Plan 15-04 T-15-03 mitigation) | PASS |
    | **P31** — `spoke-capsule` MUST NOT enter `task rebuild` until graduation ADR-008 says "adopt" | v0.19 Phase 7 static bats grep; v0.19 Phase 11 live SLO < 230s regression test | Plan 15-04 `task rebuild` cold-run scorched the v0.18 baseline clusters only; `spoke-capsule` survived (P31 isolation contract held) | PASS |
    | **D-11-14** — Evidence-bound graduation rubric (ADR-008 = DEFER) | v0.19 Phase 11 Plan 11-08 ADR-008 outcome | v0.20 did NOT revisit the graduation rubric; no new ADRs in v0.20; Plan 15-05 Task 3 carry-forward parking lot keeps `task demo-capsule` Taskfile wrapper deferred | PASS |
    | **KARYON POC MOUNT location** — sentinel stays in `clusters/hub-flux/flux-system/kustomization.yaml` | v0.19 Phase 7 contract | v0.20 was additive under `pocs/capsule/` only; no edits to flux-system kustomization.yaml; Plan 15-04 `task rebuild` regenerates the file with sentinel preserved | PASS |
    | **`task rebuild` SLO < 230s** | v0.18 baseline; v0.19 VAL-04 + VAL-05 carry-forward | Plan 15-04 Task 5 explicit `time task rebuild` measurement; wall-clock < 230s asserted in evidence log via `DELIVERY-02 PASS: task rebuild Xs < 230s SLO` literal | PASS |
    | **Existing alpha + bravo Tenant CRs** — additive co-ownership only | v0.20 milestone-open inheritance | Phase 13 RBAC-06 appended `capsule-platform-owners` to `spec.owners[]` additively; existing Flux SA owner pattern (RBAC-03) preserved | PASS |

    ## Notable Observations

    - **5-plan compressed close-out template** validated as a leaner alternative to Phase 12's 7-plan shape; the no-supersedure-needed shortcut (Phase 13 + Phase 14 VERIFICATION already final at phase-open) saved ~2 plans of work without compromising audit-gate readiness.
    - **Living-doc retrospective convention** (D-15-04) preserved cross-milestone scroll-comparison.
    - **Audit-gate Path-X/Path-Y Contingency** (D-15-06) inherited from Plan 12-07 worked verbatim — no contingency triggered during this verification (audit returned `passed` strict).

    ## Cross-References

    - ROADMAP: `.planning/ROADMAP.md` §"Phase 15: v0.20 Milestone Close-out + Retrospective"
    - CONTEXT: `.planning/phases/15-v0-20-milestone-close-out-retrospective/15-CONTEXT.md` (D-15-01..07)
    - RESEARCH: `.planning/phases/15-v0-20-milestone-close-out-retrospective/15-RESEARCH.md`
    - PATTERNS: `.planning/phases/15-v0-20-milestone-close-out-retrospective/15-PATTERNS.md`
    - VALIDATION: `.planning/phases/15-v0-20-milestone-close-out-retrospective/15-VALIDATION.md` (status: final)
    - Plan SUMMARYs: `.planning/phases/15-v0-20-milestone-close-out-retrospective/15-{01,02,03,04,05}-SUMMARY.md`
    - Audit: `.planning/v0.20-MILESTONE-AUDIT.md` (status: passed)
    - Retrospective: `.planning/RETROSPECTIVE.md` §"Milestone: v0.20 — Capsule POC Demo & RBAC Polish"
    - Phase 12 precedent: `.planning/milestones/v0.19-phases/12-v0-19-close-out-reconciliation/12-VERIFICATION.md`
    ```

    The Write tool creates the file (no Edit needed; file did not exist pre-Task-5). All cross-references use absolute repo-relative paths per project convention.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VERIFICATION.md && test $(grep -c "^status: passed$" .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VERIFICATION.md) -eq 1 && test $(grep -c "^## Goal Achievement$" .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VERIFICATION.md) -eq 1 && test $(grep -c "^## Plan-by-plan Summary$" .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VERIFICATION.md) -eq 1 && test $(grep -c "^## Anti-Pattern Regression Confirmations$" .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VERIFICATION.md) -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/15-v0-20-milestone-close-out-retrospective/15-VERIFICATION.md` exists
    - Frontmatter `status: passed`: `grep -c "^status: passed$" 15-VERIFICATION.md` returns 1
    - All 5 SC rows present in Goal Achievement table: `grep -cE "^\| [1-5] \|" 15-VERIFICATION.md` returns 5 (one per SC)
    - `## Goal Achievement` heading present (returns 1)
    - `## Plan-by-plan Summary` heading present (returns 1)
    - `## Anti-Pattern Regression Confirmations` heading present (returns 1)
    - All 4 mandated anti-patterns confirmed in Anti-Pattern table:
      - `grep -c "\*\*P27\*\*" 15-VERIFICATION.md` returns at least 1
      - `grep -c "\*\*P29\*\*" 15-VERIFICATION.md` returns at least 1
      - `grep -c "\*\*P31\*\*" 15-VERIFICATION.md` returns at least 1
      - `grep -c "\*\*D-11-14\*\*" 15-VERIFICATION.md` returns at least 1
    - Plan-by-plan summary covers all 5 plans: `grep -cE "^\| 15-0[1-5] \|" 15-VERIFICATION.md` returns 5
    - Overall verdict literal present: `grep -c "Overall verdict:.* passed" 15-VERIFICATION.md` returns 1
  </acceptance_criteria>
  <done>
    `15-VERIFICATION.md` written with `status: passed` + Goal restated from ROADMAP + 5 per-SC evidence table rows + overall verdict + plan-by-plan summary + anti-pattern regression confirmations (P27 + P29 + P31 + D-11-14 + others). Mirrors Phase 12 + Phase 14 verifier output shape per PATTERNS analog mapping.
  </done>
</task>

<task type="auto">
  <name>Task 6: Advance STATE.md frontmatter + body to milestone-close state (`ready_to_archive` + `percent: 100` + `completed_plans: 14`)</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md lines 1-15 (current frontmatter — post-Plan-15-02 state; verify exact bytes at task time)
    - .planning/STATE.md lines 28-34 (current Current Position body block — Plan 15-02 set Phase 15 EXECUTING + Plan 1 of 5)
    - .planning/phases/15-v0-20-milestone-close-out-retrospective/15-CONTEXT.md (D-15-01 confirms `total_plans: 14` is the v0.20 cumulative total: Phase 13 = 5 + Phase 14 = 4 + Phase 15 = 5 = 14)
    - .planning/milestones/v0.19-phases/12-v0-19-close-out-reconciliation/12-07-PLAN.md Task 3 Edits 3+4 (STATE.md advance precedent for `status: ready_to_archive` + Current Position COMPLETE flip)
  </read_first>
  <action>
    Use the Edit tool TWICE against `.planning/STATE.md`.

    **NOTE on `old_string` finalization:** The exact `old_string` values below are based on the expected post-Plan-15-02 state. Plan 15-02's executor may land slightly different exact text (e.g., differing timestamps, slightly different stopped_at phrasing). The executor of Task 6 MUST read the actual current STATE.md frontmatter + Current Position body block at task time and adjust `old_string` to the verbatim live text BEFORE invoking the Edit tool. The `new_string` content below is the authoritative target end-state regardless of `old_string` exact bytes.

    **Edit 1 — Frontmatter advance to milestone-close state (lines 1-15).**

    `old_string` (expected post-Plan-15-02 frontmatter — VERIFY AT TASK TIME):

    ```
    ---
    gsd_state_version: 1.0
    milestone: v0.20
    milestone_name: — Capsule POC Demo & RBAC Polish
    status: executing
    stopped_at: Phase 15 in progress — v0.20 milestone close-out
    last_updated: "2026-05-20T<HH:MM:SS>.<msZ>"
    last_activity: 2026-05-20 -- Phase 15 entered
    progress:
      total_phases: 3
      completed_phases: 2
      total_plans: 14
      completed_plans: 9
      percent: 64
    ---
    ```

    `new_string` (milestone-close state — `status: ready_to_archive` + counters advanced + last_activity reflects Phase 15 complete):

    ```
    ---
    gsd_state_version: 1.0
    milestone: v0.20
    milestone_name: — Capsule POC Demo & RBAC Polish
    status: ready_to_archive
    stopped_at: Phase 15 complete — v0.20 milestone close-out done; ready for /gsd-complete-milestone v0.20
    last_updated: "2026-05-20T12:00:00.000Z"
    last_activity: 2026-05-20 -- Phase 15 complete; v0.20 ready_to_archive
    progress:
      total_phases: 3
      completed_phases: 3
      total_plans: 14
      completed_plans: 14
      percent: 100
    ---
    ```

    Diff: `status: executing → ready_to_archive`; `stopped_at` rewritten to "Phase 15 complete" + ready-marker; `last_updated` advanced to terminal timestamp; `last_activity` rewritten to "Phase 15 complete; v0.20 ready_to_archive"; `completed_phases: 2 → 3`; `completed_plans: 9 → 14`; `percent: 64 → 100`. `total_phases` + `total_plans` byte-identical.

    **Edit 2 — Current Position body block.**

    `old_string` (expected post-Plan-15-02 Current Position — VERIFY AT TASK TIME):

    ```
    ## Current Position

    Phase: 15 (v0-20-close-out-retrospective) — EXECUTING
    Plan: 1 of 5 (Wave 1 in flight: 15-01..15-04 file-disjoint; gate 15-05 in Wave 2)
    Status: Phase 15 close-out reconciliation (book-keeping; mutates only `.planning/*.md`)
    Last activity: 2026-05-20 -- Phase 15 entered
    ```

    `new_string` (Phase 15 COMPLETE; milestone ready_to_archive):

    ```
    ## Current Position

    Phase: 15 (v0-20-close-out-retrospective) — COMPLETE
    Plan: 5 of 5 (all plans landed; Wave 1 + Wave 2 complete)
    Status: v0.20 milestone ready to archive via /gsd-complete-milestone v0.20
    Last activity: 2026-05-20 -- Phase 15 complete; v0.20-MILESTONE-AUDIT.md regenerated to `passed`; RETROSPECTIVE.md v0.20 section appended
    ```

    Both Edits are unique-anchor safe:
    - Edit 1's `old_string` matches the unique frontmatter block (only one `---...---` at the top of the file).
    - Edit 2's `old_string` matches the unique `## Current Position` H2 (appears exactly once in STATE.md).
  </action>
  <verify>
    <automated>test $(grep -c "^status: ready_to_archive$" .planning/STATE.md) -eq 1 && test $(grep -c "^  completed_phases: 3$" .planning/STATE.md) -eq 1 && test $(grep -c "^  total_plans: 14$" .planning/STATE.md) -eq 1 && test $(grep -c "^  completed_plans: 14$" .planning/STATE.md) -eq 1 && test $(grep -c "^  percent: 100$" .planning/STATE.md) -eq 1 && test $(grep -c "Phase 15 .* — COMPLETE" .planning/STATE.md) -eq 1 && test $(grep -c "v0.20 milestone ready to archive" .planning/STATE.md) -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md frontmatter `status: ready_to_archive`: `grep -c "^status: ready_to_archive$" .planning/STATE.md` returns 1
    - STATE.md `completed_phases: 3`: `grep -c "^  completed_phases: 3$" .planning/STATE.md` returns 1
    - STATE.md `total_plans: 14`: `grep -c "^  total_plans: 14$" .planning/STATE.md` returns 1
    - STATE.md `completed_plans: 14`: `grep -c "^  completed_plans: 14$" .planning/STATE.md` returns 1
    - STATE.md `percent: 100`: `grep -c "^  percent: 100$" .planning/STATE.md` returns 1
    - STATE.md `last_activity` contains "Phase 15 complete": `grep -c "Phase 15 complete; v0.20 ready_to_archive" .planning/STATE.md` returns 1
    - Current Position body reflects COMPLETE: `grep -c "Phase 15 .* — COMPLETE" .planning/STATE.md` returns 1
    - Current Position body says ready-to-archive: `grep -c "v0.20 milestone ready to archive" .planning/STATE.md` returns 1
    - No `status: executing` remaining in frontmatter: `grep -c "^status: executing$" .planning/STATE.md` returns 0
    - No `percent: 64` remaining: `grep -c "^  percent: 64$" .planning/STATE.md` returns 0
    - `milestone: v0.20` + `milestone_name` preserved byte-identical
  </acceptance_criteria>
  <done>
    STATE.md advanced to milestone-close state: `status: ready_to_archive`, counters at terminal v0.20 values (`completed_phases: 3` / `completed_plans: 14` / `percent: 100`), Current Position body reflects Phase 15 COMPLETE with cross-references to audit + retrospective outputs. `/gsd-complete-milestone v0.20` can read this state and proceed cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 7: Flip ROADMAP.md Phase 15 Progress row to `5/5 Complete 2026-05-20` + Phase Summary checkbox to `[x]`</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md line 73 (Phase Summary Phase 15 line — currently `- [ ] **Phase 15: v0.20 Milestone Close-out + Retrospective** — ...`)
    - .planning/ROADMAP.md line 152 (Progress table Phase 15 row — verify current state at task time; either Plan 15-03 left untouched (`0/TBD | Not started | -`) or Plan 15-03 advanced mid-phase (`0/5 | In progress | -`))
    - .planning/milestones/v0.19-phases/12-v0-19-close-out-reconciliation/12-07-PLAN.md Task 3 Edits 1+2 (ROADMAP self-close precedent for Phase Summary `[ ] → [x]` body rewrite + Progress table row flip)
    - .planning/phases/15-v0-20-milestone-close-out-retrospective/15-PATTERNS.md §"Direct Edit row-replacement pattern" (verbatim Edit tool idiom)
  </read_first>
  <action>
    Use the Edit tool TWICE against `.planning/ROADMAP.md`.

    **NOTE on `old_string` finalization:** Plan 15-03's executor may have left the Phase 15 row in either `0/TBD | Not started | -` (untouched from planner-init) or `0/5 | In progress | -` (advanced by Plan 15-03 mid-phase). Verify the verbatim current row at task time and use it as `old_string`.

    **Edit 1 — Phase Summary checkbox + body line 73.**

    `old_string` (verbatim current Phase Summary line — expected from CONTEXT line 73; verify):

    ```
    - [ ] **Phase 15: v0.20 Milestone Close-out + Retrospective** — Pattern-mirrors v0.19 Phase 12 close-out at smaller scale: flips REQ checkboxes against live evidence, updates STATE.md, runs `/gsd-audit-milestone v0.20`, writes RETROSPECTIVE-v0.20.md, prepares milestone for clean archival.
    ```

    `new_string` (checkbox flipped + body rewritten to past-tense close summary citing Plan-15-NN evidence anchors):

    ```
    - [x] **Phase 15: v0.20 Milestone Close-out + Retrospective** — Closed 2026-05-20 (5/5 plans). Pattern-mirrored v0.19 Phase 12 close-out compressed to 5 plans / 2 waves (no prior-VERIFICATION supersedure needed). Plan 15-01 flipped 11 v0.20 mandatory REQs (DEMO-01..06 + RUNBOOK-01..05) + 4 OQ lock-in amendments (RBAC-01 / SEED-03 / DELIVERY-01 / RUNBOOK-05). Plan 15-02 advanced STATE.md mid-phase + populated Performance Metrics By-Phase. Plan 15-03 refreshed ROADMAP.md (Phase 14 → Complete 2026-05-19). Plan 15-04 ran Phase 13 + Phase 14 live bats subset GREEN (~207 @tests, 0 REDs) + 3/3 `auth can-i '*' '*' = no` denials + `task rebuild` SLO < 230s preserved + `task health-check` exit 0 post-rebuild. Plan 15-05 ran `/gsd-audit-milestone v0.20` → `passed`, appended v0.20 retrospective to `.planning/RETROSPECTIVE.md` (D-15-04 living-doc), reconciled carry-forward parking lot in PROJECT.md (D-15-05; +2 entries: TLS-fix-at-cert-layer + `task demo-capsule` wrapper), finalized 15-VALIDATION.md (vacuous-Nyquist book-keeping per D-15-07), wrote 15-VERIFICATION.md (status: passed; 5/5 SC). Milestone ready_to_archive via /gsd-complete-milestone v0.20.
    ```

    **Edit 2 — Progress table Phase 15 row.**

    `old_string` (verbatim current Phase 15 row — verify at task time; one of these two forms expected):

    Form A (Plan 15-03 left untouched):
    ```
    | 15. v0.20 Close-out + Retrospective | v0.20 | 0/TBD | Not started | - |
    ```

    OR Form B (Plan 15-03 advanced mid-phase per RESEARCH §"Plan 15-03 — ROADMAP.md edits"):
    ```
    | 15. v0.20 Close-out + Retrospective | v0.20 | 0/5 | In progress | - |
    ```

    `new_string` (terminal close-state regardless of which form Plan 15-03 landed):

    ```
    | 15. v0.20 Close-out + Retrospective | v0.20 | 5/5 | Complete | 2026-05-20 |
    ```

    Both Edits are unique-anchor safe:
    - Edit 1's `old_string` matches the unique Phase 15 Phase Summary line (the `**Phase 15:` bold literal appears exactly twice in ROADMAP.md — once in the Phase Summary at line 73 and once in the Phase Details at line ~122 — but Edit 1's `old_string` includes the full body text "Pattern-mirrors v0.19 Phase 12 close-out at smaller scale..." which only appears in the Phase Summary line, NOT in Phase Details).
    - Edit 2's `old_string` matches the unique Progress table Phase 15 row (the `| 15. v0.20 Close-out + Retrospective |` literal appears exactly once in the Progress table).
  </action>
  <verify>
    <automated>test $(grep -cE "^- \[x\] \*\*Phase 15:" .planning/ROADMAP.md) -eq 1 && test $(grep -cE "^\| 15\. v0\.20 Close-out \+ Retrospective \| v0\.20 \| 5/5 \| Complete \| 2026-05-20 \|$" .planning/ROADMAP.md) -eq 1 && test $(grep -cE "^\| 15\.[^|]*\| v0\.20 \| 0/(TBD|5) \| (Not started|In progress) \| - \|$" .planning/ROADMAP.md) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 15 Phase Summary checkbox is `[x]`: `grep -cE "^- \[x\] \*\*Phase 15:" .planning/ROADMAP.md` returns 1
    - ROADMAP.md Phase 15 Progress table row is Complete 2026-05-20: `grep -cE "^\| 15\. v0\.20 Close-out \+ Retrospective \| v0\.20 \| 5/5 \| Complete \| 2026-05-20 \|$" .planning/ROADMAP.md` returns 1
    - ROADMAP.md no longer has Phase 15 row in `Not started` / `In progress` state: `grep -cE "^\| 15\.[^|]*\| v0\.20 \| 0/(TBD|5) \| (Not started|In progress) \| - \|$" .planning/ROADMAP.md` returns 0
    - ROADMAP.md Phase 15 Phase Summary body contains plan-by-plan summary references: `grep -c "Plan 15-01 flipped 11 v0.20 mandatory REQs" .planning/ROADMAP.md` returns 1
    - Plan 15-03's Phase 13 + Phase 14 Progress rows untouched: `grep -cE "^\| 13\..*\| v0\.20 \| 5/5 \| Complete" .planning/ROADMAP.md` returns 1; `grep -cE "^\| 14\..*\| v0\.20 \| 4/4 \| Complete" .planning/ROADMAP.md` returns 1
    - v0.20 milestone-level tile at line 7 (`- [ ] **v0.20 — Capsule POC Demo & RBAC Polish**`) untouched by Plan 15-05 — `/gsd-complete-milestone v0.20` handles the milestone-level tile flip post-Phase-15
  </acceptance_criteria>
  <done>
    ROADMAP.md Phase 15 row flipped to `5/5 | Complete | 2026-05-20`; Phase Summary checkbox `[x]` with past-tense close summary citing Plan-15-01..05 evidence anchors. v0.20 milestone progress table fully consistent (Phase 13 + Phase 14 + Phase 15 all Complete). `/gsd-complete-milestone v0.20` can read this state and proceed cleanly to archival.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation reconciliation + audit-gate invocation only; no new code paths; no new attack surface introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | T (Tampering) | Edit-tool ambiguous `old_string` lands flip on wrong line (REQUIREMENTS / RETROSPECTIVE / PROJECT / STATE / ROADMAP / VALIDATION edits across Tasks 2-7) | mitigate | Each Edit's `old_string` uses a unique multi-line anchor window (Task 2: v0.19 Cost Observations closer + `---` + `## Cross-Milestone Trends` 5-line window; Task 3: last existing parking-lot bullet + blank + `### Out of Scope` 3-line window; Task 4 Edit 1: full 8-line frontmatter block; Task 4 Edit 2: unique terminal `**Approval:** pending` line; Task 6 Edit 1: full 15-line frontmatter block; Task 6 Edit 2: unique `## Current Position` H2 block; Task 7 Edit 1: full Phase Summary line with unique body text). Acceptance criteria use exact-count grep (`-eq` not `-ge`) to catch silent extra/missing matches. Pre-edit Read-first verification per `<read_first>` blocks. |
| T-15-04 | T (Tampering) / R (Repudiation) | Audit returns `passed` but `/gsd-complete-milestone v0.20` precondition fails downstream (e.g., MILESTONES.md append fails, archive file moves fail) | mitigate | Task 1 is `checkpoint:human-verify` (operator inspects audit output at the gate); Plan 15-05 SUMMARY captures verbatim audit output for post-hoc audit; STATE.md `status: ready_to_archive` is the explicit handoff marker `/gsd-complete-milestone` reads (per Plan 12-07 precedent). |
| T-15-06 | T (Tampering) | Task 2 retrospective append overwrites or corrupts the v0.19 section (loss of prior-milestone lessons) | mitigate | Task 2 uses Append-after-anchor idiom (D-15-04) with v0.19 section closer + `## Cross-Milestone Trends` as unique 5-line `old_string` window; new_string preserves the anchor window byte-identical PLUS appends new v0.20 section between the `---` separator and `## Cross-Milestone Trends` heading. Acceptance criteria explicitly verify v0.19 + v0.18 section headings still return grep count 1 each. |

**Block on:** high severity. None of T-15-01 / T-15-04 / T-15-06 are high severity (all LOW — book-keeping reconciliation with grep-verifiable acceptance criteria); proceed with `mitigate` dispositions as documented.
</threat_model>

<verification>
After all 7 tasks complete:

```bash
# Task 1 — Audit gate
test -f .planning/v0.20-MILESTONE-AUDIT.md && \
test $(head -20 .planning/v0.20-MILESTONE-AUDIT.md | grep -cE "^status: (passed|tech_debt)$") -eq 1 && \

# Task 2 — Retrospective append
test $(grep -c "^## Milestone: v0.20 — Capsule POC Demo & RBAC Polish$" .planning/RETROSPECTIVE.md) -eq 1 && \
test $(grep -c "^## Milestone: v0.19 — Capsule Multi-Tenancy POC$" .planning/RETROSPECTIVE.md) -eq 1 && \
test $(grep -c "^## Milestone: v0.18 — karyon Lab v1$" .planning/RETROSPECTIVE.md) -eq 1 && \
test $(grep -c "^### OQ Resolutions$" .planning/RETROSPECTIVE.md) -eq 1 && \

# Task 3 — Parking lot
test $(grep -c "Fix the \`tls: bad certificate\` capsule-controller chatter" .planning/PROJECT.md) -eq 1 && \
test $(grep -c "\`task demo-capsule\` Taskfile wrapper" .planning/PROJECT.md) -eq 1 && \
test $(grep -c "ADDON-01 / ADDON-02 deferred from v0.19 Phase 12" .planning/PROJECT.md) -eq 1 && \

# Task 4 — VALIDATION ledger close
test $(grep -c "^status: final$" .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VALIDATION.md) -eq 1 && \
test $(grep -c "^nyquist_compliant: true$" .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VALIDATION.md) -eq 1 && \
test $(grep -c "^wave_0_complete: true$" .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VALIDATION.md) -eq 1 && \
test $(grep -c "^## Finalized Evidence$" .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VALIDATION.md) -eq 1 && \

# Task 5 — VERIFICATION written
test -f .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VERIFICATION.md && \
test $(grep -c "^status: passed$" .planning/phases/15-v0-20-milestone-close-out-retrospective/15-VERIFICATION.md) -eq 1 && \

# Task 6 — STATE advance
test $(grep -c "^status: ready_to_archive$" .planning/STATE.md) -eq 1 && \
test $(grep -c "^  percent: 100$" .planning/STATE.md) -eq 1 && \
test $(grep -c "^  completed_plans: 14$" .planning/STATE.md) -eq 1 && \

# Task 7 — ROADMAP flip
test $(grep -cE "^- \[x\] \*\*Phase 15:" .planning/ROADMAP.md) -eq 1 && \
test $(grep -cE "^\| 15\. v0\.20 Close-out \+ Retrospective \| v0\.20 \| 5/5 \| Complete \| 2026-05-20 \|$" .planning/ROADMAP.md) -eq 1
```

If ANY check fails: surface the specific failure in the Plan 15-05 SUMMARY; the milestone is NOT `ready_to_archive` until all 7 tasks land cleanly. Per D-15-06, Task 1 audit-gate failure is the only HARD BLOCK that halts the plan; failures in Tasks 2-7 are local-edit errors that can be reverted + re-attempted in-plan.
</verification>

<success_criteria>
This plan satisfies ROADMAP Phase 15 Success Criteria 4 (audit passed) + 5 (retrospective captured) + the Phase 15 self-close itself (STATE.md `ready_to_archive` + ROADMAP.md Phase 15 row Complete + Phase Summary `[x]`). Combined with Plans 15-01..04, the plan-set satisfies all 5 ROADMAP Success Criteria of Phase 15:

- SC-1 (REQUIREMENTS.md fully reconciled) — Plan 15-01 closed; cross-verified by Task 1 pre-flight
- SC-2 (STATE + ROADMAP consistent) — Plans 15-02 + 15-03 mid-phase + Tasks 6 + 7 final-phase
- SC-3 (Three-tier model regression-verified) — Plan 15-04 evidence log + Task 1 pre-flight (`Plan 15-04 status: GREEN`)
- SC-4 (Milestone audit passes) — Task 1 (`/gsd-audit-milestone v0.20` returns `passed`)
- SC-5 (Retrospective captured) — Task 2 (`.planning/RETROSPECTIVE.md` v0.20 section) + Task 3 (PROJECT.md parking lot) + Task 6 (STATE.md `ready_to_archive`)

Milestone v0.20 is `ready_to_archive`; `/gsd-complete-milestone v0.20` can proceed.
</success_criteria>

<output>
After completion, create `.planning/phases/15-v0-20-milestone-close-out-retrospective/15-05-SUMMARY.md` per `~/.claude/get-shit-done/templates/summary.md`.

**requirements-completed:** `[]` (no new REQ-IDs; this plan cross-validates all 23 v0.20 mandatory REQs via the `/gsd-audit-milestone v0.20` gate at Task 1)

**SUMMARY body must include:**

1. **Notable Observations:**
   - Verbatim `/gsd-audit-milestone v0.20` output captured inline (per D-15-06 inline-audit-output requirement) — full audit frontmatter + gaps arrays + tech_debt arrays + Nyquist section
   - Operator resume signal recorded verbatim: `approved` OR `path-x` OR `path-y` + any operator notes
   - If Path-X-with-tech-debt taken: cite the specific ADDON-* entries that comprised the accepted tech_debt
2. **Cross-references:**
   - Plan 15-04 evidence log path + key metrics (bats GREEN count, `task rebuild` duration, denial assertion count)
   - Phase 13 + Phase 14 VALIDATION ledger paths (confirms pre-Wave-2 inheritance held)
   - `15-VERIFICATION.md` path (this plan's verifier output)
   - `15-VALIDATION.md` path (now finalized)
   - `.planning/RETROSPECTIVE.md` v0.20 section + `.planning/PROJECT.md` parking lot reconciliation
3. **Threat-model dispositions:** record T-15-01 / T-15-04 / T-15-06 outcomes (all should be `mitigated`); if any unexpected ambiguity surfaced during Edit operations, note in detail
4. **Final milestone state:** STATE.md `status: ready_to_archive`; ROADMAP.md Phase 15 Complete; v0.20 milestone tile in ROADMAP.md still `- [ ]` at the top (this is correct — `/gsd-complete-milestone v0.20` handles the milestone-level tile flip post-Phase-15, not Plan 15-05)
5. **Next-step pointer:** `Run /gsd-complete-milestone v0.20 to archive v0.20 phases (13-15) into .planning/milestones/v0.20-phases/ and append the v0.20 entry to .planning/MILESTONES.md.`

If Task 1 audit-gate triggered Path-Y (HALT), SUMMARY surfaces:
- Specific audit failure mode (which REQ unsatisfied / which tech_debt category / which VALIDATION ledger not final)
- Recommended remediation pathway (`/gsd-insert-phase 14.X` for Phase 14 regression; or new Plan 15-06 for Wave 1 plan revision; or REQUIREMENTS.md ADDON-* purge for Path-Y-becomes-Path-X)
- Explicit note that Tasks 2-7 were NOT executed and the milestone is NOT `ready_to_archive`
</output>
