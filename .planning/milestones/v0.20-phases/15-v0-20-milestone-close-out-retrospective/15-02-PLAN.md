---
phase: 15
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
autonomous: true
requirements: []
tags: [bookkeeping, state, close-out, v0.20]

must_haves:
  truths:
    - "STATE.md frontmatter `status` reverted from premature `completed` to `executing` (Wave 1 mid-phase state)"
    - "STATE.md frontmatter `last_activity` advanced to `2026-05-20 -- Phase 15 entered`"
    - "STATE.md frontmatter `total_plans: 14` (Phase 13: 5 + Phase 14: 4 + Phase 15: 5)"
    - "STATE.md frontmatter `completed_plans: 9` (Phase 13: 5 + Phase 14: 4; pre-Plan-15 close)"
    - "STATE.md frontmatter `percent: 64` (9/14 mid-phase; Plan 15-05 advances to 100)"
    - "STATE.md `Current Position` body reflects Phase 15 EXECUTING Wave 1 in flight"
    - "STATE.md Performance Metrics By-Phase rows for Phase 13 (5 plans), Phase 14 (4 plans), Phase 15 (5 plans) populated"
    - "STATE.md Velocity block appends `Total plans completed (v0.20): 14`"
  artifacts:
    - path: .planning/STATE.md
      provides: "STATE ledger advanced to Phase 15 EXECUTING (mid-phase Wave 1 state)"
      contains: "Phase 15 entered"
  key_links:
    - from: .planning/STATE.md
      to: .planning/ROADMAP.md
      via: "Performance Metrics By-Phase plan counts match ROADMAP Progress table (Plan 15-03 enforces ROADMAP side)"
      pattern: "Phase 1[345].*\\| [0-9]+ \\|"
---

<objective>
Advance STATE.md from the premature `status: completed` / `percent: 100` state (set when Phase 14 marked complete) to the correct mid-phase Wave-1 state (`status: executing`, `completed_plans: 9`, `total_plans: 14`, `percent: 64`); update Current Position body to reflect Phase 15 EXECUTING; populate the Performance Metrics By-Phase TBD rows for Phases 13/14/15; append a `Total plans completed (v0.20): 14` velocity line under v0.19.

Purpose: SC-2 of Phase 15 — "STATE.md and ROADMAP.md consistent" — STATE.md side. The current state has `status: completed` and `percent: 100` PREMATURELY (set at Phase 14 close before Phase 15 even started — per Pitfall 15-P2). This plan reverts to the correct mid-phase Wave-1 state; Plan 15-05 (Wave 2) advances to the final `status: ready_to_archive` / `percent: 100` state.

Output: Updated `.planning/STATE.md` with frontmatter reverted to mid-phase, Current Position block flipped, Performance Metrics By-Phase rows populated, Velocity counter appended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-v0-20-milestone-close-out-retrospective/15-CONTEXT.md
@.planning/phases/15-v0-20-milestone-close-out-retrospective/15-RESEARCH.md
@.planning/phases/15-v0-20-milestone-close-out-retrospective/15-PATTERNS.md
@.planning/milestones/v0.19-phases/12-v0-19-close-out-reconciliation/12-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revert STATE.md frontmatter from premature `completed` to mid-phase `executing` (Wave 1 entry state)</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md lines 1-15 (current frontmatter — confirms `status: completed`, `total_plans: 9`, `completed_plans: 9`, `percent: 100` premature state)
    - .planning/phases/15-v0-20-milestone-close-out-retrospective/15-RESEARCH.md §"Plan 15-02 — STATE.md edits" (target frontmatter values + arithmetic rationale)
    - .planning/phases/15-v0-20-milestone-close-out-retrospective/15-PATTERNS.md §"Frontmatter block-edit pattern" (verbatim Plan 12-02 Task 1 block-edit shape)
    - .planning/milestones/v0.19-phases/12-v0-19-close-out-reconciliation/12-02-PLAN.md Task 1 (analog block-edit pattern with old_string ↔ new_string verbatim)
  </read_first>
  <action>
    Use ONE Edit tool call to rewrite the entire frontmatter block (STATE.md lines 1-15).

    `old_string` (lines 1-15 verbatim from current STATE.md):
    ```
    ---
    gsd_state_version: 1.0
    milestone: v0.20
    milestone_name: — Capsule POC Demo & RBAC Polish
    status: completed
    stopped_at: Phase 15 context gathered
    last_updated: "2026-05-20T09:10:11.765Z"
    last_activity: 2026-05-20 -- Phase 14 marked complete
    progress:
      total_phases: 3
      completed_phases: 2
      total_plans: 9
      completed_plans: 9
      percent: 100
    ---
    ```

    `new_string`:
    ```
    ---
    gsd_state_version: 1.0
    milestone: v0.20
    milestone_name: — Capsule POC Demo & RBAC Polish
    status: executing
    stopped_at: Phase 15 in flight — v0.20 close-out reconciliation (Wave 1)
    last_updated: "2026-05-20T12:00:00.000Z"
    last_activity: 2026-05-20 -- Phase 15 entered (Wave 1: 15-01 REQUIREMENTS / 15-02 STATE / 15-03 ROADMAP / 15-04 live regression rerun in flight)
    progress:
      total_phases: 3
      completed_phases: 2
      total_plans: 14
      completed_plans: 9
      percent: 64
    ---
    ```

    Arithmetic rationale:
    - `status: completed → executing` — premature `completed` was set at Phase 14 close; Phase 15 is now in flight (Pitfall 15-P2).
    - `total_plans: 9 → 14` — Phase 13: 5 plans + Phase 14: 4 plans + Phase 15: 5 plans (per D-15-01) = 14. Verified via `ls .planning/phases/{13,14}-*/[01]*-PLAN.md | wc -l = 9` plus Phase 15 plans authored as part of this planning cycle.
    - `completed_plans: 9 → 9` — UNCHANGED at Wave 1 entry (Phase 13: 5 done + Phase 14: 4 done; no Phase 15 plans complete yet at this point of execution).
    - `percent: 100 → 64` — 9/14 = 64.28...% → 64% (per `gsd_state_version: 1.0` integer convention).
    - `completed_phases: 2` — UNCHANGED (Phase 13 + Phase 14); Plan 15-05 self-close advances to 3.
    - `last_updated` advanced to a fresh ISO8601 timestamp; planner may use the actual run-time value when this task executes.

    Plan 15-05 Task 3 (Wave 2 self-close) will advance to:
    - `status: executing → ready_to_archive`
    - `completed_phases: 2 → 3`
    - `completed_plans: 9 → 14`
    - `percent: 64 → 100`
  </action>
  <verify>
    <automated>test $(grep -c "^status: executing$" .planning/STATE.md) -eq 1 && test $(grep -c "^  total_plans: 14$" .planning/STATE.md) -eq 1 && test $(grep -c "^  completed_plans: 9$" .planning/STATE.md) -eq 1 && test $(grep -c "^  percent: 64$" .planning/STATE.md) -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^status: executing$" .planning/STATE.md` returns 1
    - `grep -c "^status: completed$" .planning/STATE.md` returns 0
    - `grep -c "^  total_plans: 14$" .planning/STATE.md` returns 1
    - `grep -c "^  completed_plans: 9$" .planning/STATE.md` returns 1
    - `grep -c "^  percent: 64$" .planning/STATE.md` returns 1
    - `grep -c "^  percent: 100$" .planning/STATE.md` returns 0
    - `grep -c "^last_activity: 2026-05-20 -- Phase 15 entered" .planning/STATE.md` returns 1
    - `grep -c "stopped_at: Phase 15 in flight" .planning/STATE.md` returns 1
    - `grep -c "^milestone: v0.20$" .planning/STATE.md` returns 1 (UNCHANGED)
    - `grep -c "^  completed_phases: 2$" .planning/STATE.md` returns 1 (UNCHANGED at Wave 1)
  </acceptance_criteria>
  <done>
    STATE.md frontmatter at mid-phase Wave-1 state: `executing` / `total_plans: 14` / `completed_plans: 9` / `percent: 64`. Plan 15-05 self-close will advance to `ready_to_archive` / `completed_phases: 3` / `completed_plans: 14` / `percent: 100`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md Current Position block + Performance Metrics By-Phase table + Velocity v0.20 line</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md lines 28-34 (current Current Position block — confirms `Phase: 14 — COMPLETE` text)
    - .planning/STATE.md lines 36-42 (Velocity block — confirms `v0.18: 35` and `v0.19: 18` totals present; v0.20 line absent)
    - .planning/STATE.md lines 72-78 (Performance Metrics By-Phase v0.20 table — confirms `TBD` cells for Phase 13/14/15)
    - .planning/phases/15-v0-20-milestone-close-out-retrospective/15-RESEARCH.md §"Plan 15-02 — STATE.md edits" (verbatim Current Position target text + Performance Metrics By-Phase target values)
    - .planning/phases/15-v0-20-milestone-close-out-retrospective/15-PATTERNS.md §"Body block + Pending Todos edit pattern" (Plan 12-02 Task 2 verbatim Edit idiom)
    - .planning/milestones/v0.19-phases/12-v0-19-close-out-reconciliation/12-02-PLAN.md Task 2 (analog body-block + table-row edits)
  </read_first>
  <action>
    Three independent Edit calls (each `old_string` is unique because the surrounding context anchors it).

    **Edit 1 — Current Position block (STATE.md lines 28-34):**

    `old_string`:
    ```
    ## Current Position

    Phase: 14 — COMPLETE
    Plan: 1 of 4
    Status: Phase 14 complete
    Last activity: 2026-05-20 -- Phase 14 marked complete
    ```

    `new_string`:
    ```
    ## Current Position

    Phase: 15 (v0-20-close-out-retrospective) — EXECUTING
    Plan: 1 of 5 (Wave 1: 15-01 REQUIREMENTS / 15-02 STATE / 15-03 ROADMAP / 15-04 live regression rerun — file-disjoint parallel; Wave 2: 15-05 audit + retrospective + parking-lot + VALIDATION + VERIFICATION)
    Status: Phase 15 close-out reconciliation (book-keeping; mutates `.planning/*.md` + writes 15-04 evidence log)
    Last activity: 2026-05-20 -- Phase 15 entered
    ```

    **Edit 2 — Velocity v0.20 line append (STATE.md line 40 area — after `Total plans completed (v0.19): 18`):**

    `old_string`:
    `- Total plans completed (v0.19): 18`

    `new_string`:
    `- Total plans completed (v0.19): 18
- Total plans completed (v0.20): 14`

    (This appends a new line after the v0.19 line; the v0.19 line is unique within STATE.md so the anchor is unambiguous.)

    **Edit 3 — Performance Metrics By-Phase v0.20 table population (STATE.md lines 72-78):**

    `old_string`:
    ```
    | Phase | Plans | Total | Avg/Plan |
    |-------|-------|-------|----------|
    | 13 (RBAC + SEED + DELIVERY) | TBD | — | — |
    | 14 (DEMO + RUNBOOK) | TBD | — | — |
    | 15 (Close-out + Retrospective) | TBD | — | — |
    ```

    `new_string`:
    ```
    | Phase | Plans | Total | Avg/Plan |
    |-------|-------|-------|----------|
    | 13 (RBAC + SEED + DELIVERY) | 5 | - | - |
    | 14 (DEMO + RUNBOOK) | 4 | - | - |
    | 15 (Close-out + Retrospective) | 5 | - | - |
    ```

    Plan counts verified via:
    - Phase 13 = 5: `ls .planning/phases/13-*/[01]*-PLAN.md | wc -l` returns 5 (13-00..13-04)
    - Phase 14 = 4: `ls .planning/phases/14-*/[01]*-PLAN.md | wc -l` returns 4 (14-00..14-03)
    - Phase 15 = 5: per D-15-01 (15-01..15-05; this plan is one of them)

    `Total` and `Avg/Plan` cells stay `-` per the v0.18 + v0.19 precedent (timing data not collected by the orchestrator — Pitfall 15-P9: avoid mis-incrementing).
  </action>
  <verify>
    <automated>test $(grep -c "^Phase: 15 (v0-20-close-out-retrospective) — EXECUTING$" .planning/STATE.md) -eq 1 && test $(grep -c "^- Total plans completed (v0.20): 14$" .planning/STATE.md) -eq 1 && test $(grep -cE "^\| 13 \(RBAC \+ SEED \+ DELIVERY\) \| 5 \|" .planning/STATE.md) -eq 1 && test $(grep -cE "^\| 14 \(DEMO \+ RUNBOOK\) \| 4 \|" .planning/STATE.md) -eq 1 && test $(grep -cE "^\| 15 \(Close-out \+ Retrospective\) \| 5 \|" .planning/STATE.md) -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^Phase: 15 (v0-20-close-out-retrospective) — EXECUTING$" .planning/STATE.md` returns 1
    - `grep -c "^Phase: 14 — COMPLETE$" .planning/STATE.md` returns 0 (replaced)
    - `grep -c "Wave 1: 15-01 REQUIREMENTS / 15-02 STATE" .planning/STATE.md` returns 1
    - `grep -c "^- Total plans completed (v0.20): 14$" .planning/STATE.md` returns 1
    - `grep -c "^- Total plans completed (v0.19): 18$" .planning/STATE.md` returns 1 (unchanged — v0.19 line preserved)
    - `grep -c "^- Total plans completed (v0.18): 35$" .planning/STATE.md` returns 1 (unchanged — v0.18 line preserved)
    - `grep -cE "^\| 13 \(RBAC \+ SEED \+ DELIVERY\) \| 5 \|" .planning/STATE.md` returns 1
    - `grep -cE "^\| 14 \(DEMO \+ RUNBOOK\) \| 4 \|" .planning/STATE.md` returns 1
    - `grep -cE "^\| 15 \(Close-out \+ Retrospective\) \| 5 \|" .planning/STATE.md` returns 1
    - `grep -cE "^\| 1[345].*\| TBD \|" .planning/STATE.md` returns 0 (no TBD cells remain in v0.20 By-Phase table)
  </acceptance_criteria>
  <done>
    Current Position block reflects Phase 15 EXECUTING with explicit Wave 1 plan listing; v0.20 velocity line appended (14 plans); Performance Metrics By-Phase v0.20 table populated for Phases 13/14/15 with `5/4/5` plan counts respectively; no `TBD` cells remain for v0.20 phases.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation reconciliation only; no new attack surface introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-a | T (Tampering) | Frontmatter block-edit lands in wrong YAML location (e.g., overwrites unrelated key) | mitigate | `old_string` = entire 15-line frontmatter block verbatim — unambiguous; pre-edit Read confirms target window matches `old_string` byte-for-byte |
| T-15-01 (default doc-only) | I (Information disclosure) | STATE.md edits expose secrets | accept | No secrets in STATE.md; book-keeping only; existing v0.20 security posture unchanged |
| T-15-09 | T (Tampering) | Velocity counters mis-incremented (off-by-one risk per Pitfall 15-P9) | mitigate | Plan counts derived from authoritative `ls .planning/phases/{13,14}-*/[01]*-PLAN.md` source; acceptance_criteria use exact-equality grep gates |
</threat_model>

<verification>
After both tasks complete:

```bash
# Frontmatter state (mid-phase Wave 1)
test $(grep -c "^status: executing$" .planning/STATE.md) -eq 1 && \
test $(grep -c "^  total_plans: 14$" .planning/STATE.md) -eq 1 && \
test $(grep -c "^  completed_plans: 9$" .planning/STATE.md) -eq 1 && \
test $(grep -c "^  percent: 64$" .planning/STATE.md) -eq 1 && \

# Body state
test $(grep -c "^Phase: 15 (v0-20-close-out-retrospective) — EXECUTING$" .planning/STATE.md) -eq 1 && \

# Velocity
test $(grep -c "^- Total plans completed (v0.20): 14$" .planning/STATE.md) -eq 1 && \

# Performance Metrics By-Phase
test $(grep -cE "^\| 1[345].*\| [0-9]+ \|" .planning/STATE.md) -eq 3 && \
test $(grep -cE "^\| 1[345].*\| TBD \|" .planning/STATE.md) -eq 0
```
</verification>

<success_criteria>
- STATE.md frontmatter at mid-phase Wave-1 state (`executing` / `64%`)
- Current Position block reflects Phase 15 EXECUTING + Wave 1 plan listing
- Velocity v0.20 line appended (14 plans)
- Performance Metrics By-Phase v0.20 table fully populated (no `TBD` cells)
- Plan 15-05 has the correct starting state to advance to `ready_to_archive` / `percent: 100`
</success_criteria>

<output>
After completion, create `.planning/phases/15-v0-20-milestone-close-out-retrospective/15-02-SUMMARY.md` per `~/.claude/get-shit-done/templates/summary.md`. requirements-completed: [] (book-keeping; no REQ-IDs).

SUMMARY body must note: STATE.md final state advance (`ready_to_archive` / `percent: 100`) is OWNED BY Plan 15-05 Task 3 (Wave 2 self-close); Plan 15-02 deliberately stops at mid-phase state per the v0.19 Plan 12-02 → Plan 12-07 STATE.md trajectory.
</output>
