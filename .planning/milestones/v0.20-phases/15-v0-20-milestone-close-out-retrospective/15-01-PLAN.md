---
phase: 15
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DEMO-01, DEMO-02, DEMO-03, DEMO-04, DEMO-05, DEMO-06, RUNBOOK-01, RUNBOOK-02, RUNBOOK-03, RUNBOOK-04, RUNBOOK-05]
tags: [bookkeeping, requirements, close-out, v0.20]

must_haves:
  truths:
    - "11 v0.20 mandatory `[ ]` checkboxes (DEMO-01..06 + RUNBOOK-01..05) flipped to `[x]` in REQUIREMENTS.md"
    - "Combined v0.20 mandatory `[x]` count is 23 (12 already-flipped Phase 13 RBAC/SEED/DELIVERY + 11 Phase 14 DEMO/RUNBOOK)"
    - "OQ-2 lock-in amendment landed inline on RBAC-01 with literal citation `Phase 13 D-13-01`"
    - "OQ-3 lock-in amendment landed inline on SEED-03 with literal citation `Phase 13 D-13-02`"
    - "OQ-4 lock-in amendment landed inline on DELIVERY-01 with literal citation `Phase 13 D-13-03`"
    - "OQ-5 lock-in amendment landed inline on RUNBOOK-05 with literal citation `Phase 14 D-14-02`"
    - "Traceability table rows for DEMO-01..06 + RUNBOOK-01..05 changed from `Pending` to `Complete`"
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: "v0.20 REQ ledger with 23/23 mandatory checkboxes complete + 5 OQ lock-in amendments inline"
      contains: "Phase 13 D-13-01"
  key_links:
    - from: .planning/REQUIREMENTS.md
      to: .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VERIFICATION.md
      via: "11 DEMO + RUNBOOK flips cite Phase 14 live + static bats evidence"
      pattern: "^- \\[x\\] \\*\\*(DEMO|RUNBOOK)-"
    - from: .planning/REQUIREMENTS.md
      to: .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VERIFICATION.md
      via: "4 OQ-2/3/4/5 lock-in amendments cite Phase 13 + Phase 14 D-codes inline"
      pattern: "Phase 1[34] D-1[34]-0[123]"
---

<objective>
Flip the 11 remaining v0.20 mandatory `[ ]` checkboxes (DEMO-01..06 + RUNBOOK-01..05) to `[x]` in REQUIREMENTS.md, fold the 4 OQ-2/3/4/5 lock-in amendments inline next to the affected REQ text (mirroring v0.19 Plan 12-01 TEN-01 `forceTenantPrefix: false` pattern), and reconcile the Traceability table footer rows from `Pending` to `Complete`.

Purpose: SC-1 of Phase 15 — "REQUIREMENTS.md fully reconciled" — with phase/plan citations and inline OQ lock-ins (D-15-03). After this plan, `grep -cE "^- \[x\] \*\*(RBAC|SEED|DEMO|RUNBOOK|DELIVERY)-" .planning/REQUIREMENTS.md` returns exactly 23, and the `Pending` cells in the Traceability table for the 11 flipped REQs become `Complete`.

Output: Updated `.planning/REQUIREMENTS.md` with 11 checkbox flips, 4 inline OQ lock-in amendments (RBAC-01, SEED-03, DELIVERY-01, RUNBOOK-05), and 11 Traceability table row updates. Plus the SUMMARY.md frontmatter `requirements-completed: [DEMO-01..06, RUNBOOK-01..05]` to satisfy the audit 3-source matrix (per Pitfall 15-P6).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-v0-20-milestone-close-out-retrospective/15-CONTEXT.md
@.planning/phases/15-v0-20-milestone-close-out-retrospective/15-RESEARCH.md
@.planning/phases/15-v0-20-milestone-close-out-retrospective/15-PATTERNS.md
@.planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VERIFICATION.md
@.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VERIFICATION.md
@.planning/milestones/v0.19-phases/12-v0-19-close-out-reconciliation/12-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip 11 v0.20 mandatory checkboxes (DEMO-01..06 + RUNBOOK-01..05) from [ ] to [x] with one Edit call per REQ-ID</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 60-80 (DEMO + RUNBOOK section — confirms current `[ ]` state and exact line contents)
    - .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VERIFICATION.md (Phase 13 RBAC/SEED/DELIVERY GREEN; 12 REQs already flipped at Phase 15 open)
    - .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VERIFICATION.md (Phase 14 DEMO/RUNBOOK GREEN; evidence for the 11 to-be-flipped REQs)
    - .planning/phases/15-v0-20-milestone-close-out-retrospective/15-PATTERNS.md §"Plan 15-01 — REQUIREMENTS.md" (verbatim Edit-tool pattern: `old_string` = `- [ ] **<ID>**`; `new_string` = `- [x] **<ID>**`)
    - .planning/milestones/v0.19-phases/12-v0-19-close-out-reconciliation/12-01-PLAN.md Task 1 (the analog 17-line bulk flip pattern with exact-count grep gate)
  </read_first>
  <action>
    Use the Edit tool 11 times — one call per REQ-ID — to flip the same-line checkbox. Each `old_string` uses the bold-ID literal as the unique anchor (per PATTERNS §"Edit tool unique-old_string convention"). For Task 1 we ONLY flip the checkbox; Task 2 handles the 4 OQ inline amendments.

    Treat any REQ whose Task 2 amendment changes the same line as still requiring this Task 1 flip first (the Task 2 amendment uses the post-Task-1 line as its `old_string`). Order: Task 1 → Task 2 — sequential within this plan.

    **DEMO-01 (line 64) — no OQ amend:**
    `old_string`: `- [ ] **DEMO-01** — Platform-owner kubeconfig (`capsule-platform-owners` group)`
    `new_string`: `- [x] **DEMO-01** — Platform-owner kubeconfig (`capsule-platform-owners` group)`
    (Surrounding text on each `old_string` may be expanded if the 8-char tail isn't unique — verified to be unique against REQUIREMENTS.md HEAD via grep.)

    **DEMO-02 (line 65) — no OQ amend:**
    `old_string`: `- [ ] **DEMO-02** — Tenant-owner kubeconfig (minted via`
    `new_string`: `- [x] **DEMO-02** — Tenant-owner kubeconfig (minted via`

    **DEMO-03 (line 66) — no OQ amend:**
    `old_string`: `- [ ] **DEMO-03** — Tenant-owner can `kubectl get / kubectl logs`
    `new_string`: `- [x] **DEMO-03** — Tenant-owner can `kubectl get / kubectl logs`

    **DEMO-04 (line 67) — no OQ amend:**
    `old_string`: `- [ ] **DEMO-04** — Tenant-owner is REJECTED on `kubectl create secret``
    `new_string`: `- [x] **DEMO-04** — Tenant-owner is REJECTED on `kubectl create secret``

    **DEMO-05 (line 68) — no OQ amend:**
    `old_string`: `- [ ] **DEMO-05** — Tenant-owner is REJECTED on cross-tenant access`
    `new_string`: `- [x] **DEMO-05** — Tenant-owner is REJECTED on cross-tenant access`

    **DEMO-06 (line 69) — no OQ amend:**
    `old_string`: `- [ ] **DEMO-06** — Platform-owner is REJECTED on cluster-admin-only actions`
    `new_string`: `- [x] **DEMO-06** — Platform-owner is REJECTED on cluster-admin-only actions`

    **RUNBOOK-01 (line 75) — no OQ amend:**
    `old_string`: `- [ ] **RUNBOOK-01** — `docs/poc-capsule-demo.md` exists as a 6-act walkthrough`
    `new_string`: `- [x] **RUNBOOK-01** — `docs/poc-capsule-demo.md` exists as a 6-act walkthrough`

    **RUNBOOK-02 (line 76) — no OQ amend:**
    `old_string`: `- [ ] **RUNBOOK-02** — Runbook includes a pre-demo checklist`
    `new_string`: `- [x] **RUNBOOK-02** — Runbook includes a pre-demo checklist`

    **RUNBOOK-03 (line 77) — no OQ amend (RESEARCH OQ-1 table: no substantive amend needed):**
    `old_string`: `- [ ] **RUNBOOK-03** — Runbook includes known-noise notes`
    `new_string`: `- [x] **RUNBOOK-03** — Runbook includes known-noise notes`

    **RUNBOOK-04 (line 78) — no OQ amend; carry-forward from Plan 14-03 retired by Plan 15-04 + 15-05 audit gate:**
    `old_string`: `- [ ] **RUNBOOK-04** — Runbook is executable end-to-end against a `spoke-capsule``
    `new_string`: `- [x] **RUNBOOK-04** — Runbook is executable end-to-end against a `spoke-capsule``

    **RUNBOOK-05 (line 79) — Task 2 follows with OQ-5 amend:**
    `old_string`: `- [ ] **RUNBOOK-05** — New-tenant name selection (per OQ-5: "charlie"`
    `new_string`: `- [x] **RUNBOOK-05** — New-tenant name selection (per OQ-5: "charlie"`

    All 11 Edit calls preserve every other byte of the line exactly (only `[ ]` → `[x]`).
  </action>
  <verify>
    <automated>test $(grep -v '^#' .planning/REQUIREMENTS.md | grep -cE "^- \[x\] \*\*(DEMO|RUNBOOK)-") -eq 11</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' .planning/REQUIREMENTS.md | grep -cE "^- \[x\] \*\*DEMO-0[1-6]\*\*"` returns 6
    - `grep -v '^#' .planning/REQUIREMENTS.md | grep -cE "^- \[x\] \*\*RUNBOOK-0[1-5]\*\*"` returns 5
    - `grep -v '^#' .planning/REQUIREMENTS.md | grep -cE "^- \[ \] \*\*(DEMO|RUNBOOK)-"` returns 0
    - `grep -v '^#' .planning/REQUIREMENTS.md | grep -cE "^- \[x\] \*\*(RBAC|SEED|DEMO|RUNBOOK|DELIVERY)-"` returns 23
    - `grep -v '^#' .planning/REQUIREMENTS.md | grep -cE "^- \[ \] \*\*(RBAC|SEED|DEMO|RUNBOOK|DELIVERY)-"` returns 0
    - Future Requirements `[ ]` lines (ADDON-01/02 in Future Requirements section, ~line 98+) unchanged: `grep -c "^- \*\*ADDON-0[12]\*\*" .planning/REQUIREMENTS.md` returns 2
  </acceptance_criteria>
  <done>
    All 11 v0.20 mandatory REQ-IDs flipped to `[x]`; combined v0.20 mandatory `[x]` count is 23; Future Requirements section unchanged; no narrative-count drift introduced.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply 4 OQ lock-in amendments inline (RBAC-01, SEED-03, DELIVERY-01, RUNBOOK-05) per D-15-03</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 45 (RBAC-01), 58 (SEED-03), 79 (RUNBOOK-05), 85 (DELIVERY-01) — confirm exact post-Task-1 wording
    - .planning/phases/15-v0-20-milestone-close-out-retrospective/15-RESEARCH.md §"Open Question Deviation Table" (verbatim amend text per OQ + cited D-code)
    - .planning/phases/15-v0-20-milestone-close-out-retrospective/15-PATTERNS.md §"Inline spec-deviation amendment pattern" (mirrors Plan 12-01 Task 2 TEN-01 exemplar)
    - .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-CONTEXT.md (D-13-01 Secrets policy = read-only; D-13-02 Pod + Service shape; D-13-03 GitOps delivery)
    - .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-CONTEXT.md (D-14-02 marquee name = charlie; D-14-03 provision style = live)
  </read_first>
  <action>
    Use the Edit tool 4 times — one call per OQ amendment — combining the (already-flipped) checkbox state with the inline lock-in citation. Each `old_string` uses the FULL post-Task-1 REQ line; `new_string` injects the verbatim lock-in literal with the cited D-code.

    **Amend 1 — RBAC-01 (line 45) — OQ-2 Secrets policy = read-only (Phase 13 D-13-01):**

    `old_string` (entire post-Task-1 line 45):
    `- [x] **RBAC-01** — A \`tenant-workload-editor\` ClusterRole exists with scope narrower than upstream k8s \`edit\`: NO Secret create / edit, NO RoleBinding, NO ResourceQuota, NO LimitRange. Final Secrets policy (read-only / no-access / upstream-edit-equivalent) resolved in discuss-phase per OQ-2.`

    `new_string`:
    `- [x] **RBAC-01** — A \`tenant-workload-editor\` ClusterRole exists with scope narrower than upstream k8s \`edit\`: NO Secret create / edit, NO RoleBinding, NO ResourceQuota, NO LimitRange. Final Secrets policy resolved as **read-only** per Phase 13 D-13-01 (Secrets \`get\`/\`list\`/\`watch\` only; explicit deny on \`create\`/\`update\`/\`patch\`/\`delete\`).`

    **Amend 2 — SEED-03 (line 58) — OQ-3 hello-world shape = Pod + Service (Phase 13 D-13-02):**

    `old_string` (entire post-Task-1 line 58):
    `- [x] **SEED-03** — Hello-world workload shape is the OQ-3-resolved choice (Pod + Service / ConfigMap-only / Deployment + Service + ConfigMap) — final selection lightweight enough to land alongside ≤230s SLO and visually meaningful enough for the demo narrative.`

    `new_string`:
    `- [x] **SEED-03** — Hello-world workload shape resolved as **Pod + Service** per Phase 13 D-13-02 (FQCI \`docker.io/library/nginx:alpine\` per Pitfall 13-P1; lightweight enough to land alongside ≤230s SLO and visually meaningful enough for the demo narrative).`

    **Amend 3 — DELIVERY-01 (line 85) — OQ-4 delivery = GitOps `pocs/capsule/`; NO Taskfile (Phase 13 D-13-03):**

    `old_string` (entire post-Task-1 line 85):
    `- [x] **DELIVERY-01** — New demo resources (\`tenant-workload-editor\` ClusterRole, \`GlobalTenantResource\` CR, hello-world workload manifest) land via the OQ-4-resolved delivery mechanism — either GitOps under \`pocs/capsule/\` (production-correct, slower because of push-and-reconcile cycle) or a \`task seed-capsule-demo\` Taskfile entry (fast, self-contained). Single canonical path documented in RUNBOOK-01.`

    `new_string`:
    `- [x] **DELIVERY-01** — New demo resources (\`tenant-workload-editor\` ClusterRole, \`GlobalTenantResource\` CR, hello-world workload manifest) land via **GitOps under \`pocs/capsule/\`** per Phase 13 D-13-03 (production-correct, slower because of push-and-reconcile cycle; NO \`task seed-capsule-demo\` Taskfile entry — declined for P31 isolation). Single canonical path documented in RUNBOOK-01.`

    **Amend 4 — RUNBOOK-05 (line 79) — OQ-5 marquee name = charlie; provision style = live (Phase 14 D-14-02 + D-14-03):**

    `old_string` (entire post-Task-1 line 79):
    `- [x] **RUNBOOK-05** — New-tenant name selection (per OQ-5: "charlie" / "demo" / "team-foo" / other) is reflected verbatim in the runbook's act-3 "provision a new tenant live" step, with the chosen name canonical across runbook + delivery mechanism + any scripted helpers.`

    `new_string`:
    `- [x] **RUNBOOK-05** — New-tenant name selected as **\`charlie\`** per Phase 14 D-14-02 (provision style = **live** per Phase 14 D-14-03); appears verbatim in the runbook's act-3 "provision a new tenant live" step, canonical across runbook + delivery mechanism + scripted helpers (\`scripts/poc/capsule/new-tenant.sh\`).`

    Each Edit's `old_string` is unambiguous because the bold REQ-ID literal (`**RBAC-01**`, `**SEED-03**`, `**DELIVERY-01**`, `**RUNBOOK-05**`) appears exactly once in REQUIREMENTS.md.
  </action>
  <verify>
    <automated>test $(grep -c "Phase 13 D-13-01" .planning/REQUIREMENTS.md) -ge 1 && test $(grep -c "Phase 13 D-13-02" .planning/REQUIREMENTS.md) -ge 1 && test $(grep -c "Phase 13 D-13-03" .planning/REQUIREMENTS.md) -ge 1 && test $(grep -c "Phase 14 D-14-02" .planning/REQUIREMENTS.md) -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RBAC-01.*Phase 13 D-13-01" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "SEED-03.*Phase 13 D-13-02" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "DELIVERY-01.*Phase 13 D-13-03" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "RUNBOOK-05.*Phase 14 D-14-02" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "Phase 14 D-14-03" .planning/REQUIREMENTS.md` returns 1 (RUNBOOK-05 provision style)
    - `grep -c "read-only" .planning/REQUIREMENTS.md` returns at least 2 (the new RBAC-01 amend literal + the pre-existing options paren in RBAC-06 was unchanged; verify before counting)
    - `grep -c "FQCI" .planning/REQUIREMENTS.md` returns 1 (SEED-03 amend literal)
    - `grep -c "NO \`task seed-capsule-demo\` Taskfile entry" .planning/REQUIREMENTS.md` returns 1 (DELIVERY-01 amend literal)
    - All 4 amended lines still match `^- \[x\] \*\*(RBAC|SEED|DELIVERY|RUNBOOK)-` (checkbox state preserved through amend)
    - 11-flip count from Task 1 acceptance preserved: `grep -v '^#' .planning/REQUIREMENTS.md | grep -cE "^- \[x\] \*\*(DEMO|RUNBOOK)-"` still returns 11
  </acceptance_criteria>
  <done>
    4 inline OQ lock-in amendments landed (RBAC-01, SEED-03, DELIVERY-01, RUNBOOK-05) with literal D-code citations; no checkbox states regressed; RUNBOOK-03 has no substantive amend per RESEARCH OQ-1 table (recommended-default already in REQ text).
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Traceability table — flip 11 DEMO + RUNBOOK rows from `Pending` to `Complete` (mirrors Plan 12-01 Task 3 idiom)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 136-160 (Traceability table — confirms current `Pending` cells for DEMO-* and RUNBOOK-*)
    - .planning/phases/15-v0-20-milestone-close-out-retrospective/15-PATTERNS.md §"Narrative-drift correction pattern" (Plan 12-01 Task 3 row-by-row Edit idiom)
    - .planning/milestones/v0.19-phases/12-v0-19-close-out-reconciliation/12-01-PLAN.md Task 3 (verbatim row-replacement template)
  </read_first>
  <action>
    Use the Edit tool 11 times — one call per DEMO/RUNBOOK Traceability row — to flip the `Pending` cell to `Complete`. Each `old_string` uses the verbatim row literal (the REQ-ID + Phase column makes each row unique).

    Current state (verified via Read of REQUIREMENTS.md lines 147-157):
    - Line 147: `| DEMO-01 | Phase 14 | Pending |`
    - Line 148: `| DEMO-02 | Phase 14 | Pending |`
    - Line 149: `| DEMO-03 | Phase 14 | Pending |`
    - Line 150: `| DEMO-04 | Phase 14 | Pending |`
    - Line 151: `| DEMO-05 | Phase 14 | Pending |`
    - Line 152: `| DEMO-06 | Phase 14 | Pending |`
    - Line 153: `| RUNBOOK-01 | Phase 14 | Pending |`
    - Line 154: `| RUNBOOK-02 | Phase 14 | Pending |`
    - Line 155: `| RUNBOOK-03 | Phase 14 | Pending |`
    - Line 156: `| RUNBOOK-04 | Phase 14 | Pending |`
    - Line 157: `| RUNBOOK-05 | Phase 14 | Pending |`

    For each row, the Edit pattern is:
    `old_string`: `| <REQ-ID> | Phase 14 | Pending |`
    `new_string`: `| <REQ-ID> | Phase 14 | Complete |`

    Repeat for all 11 REQ-IDs (DEMO-01..06, RUNBOOK-01..05). Each `old_string` is unique because the REQ-ID column makes the row globally unique within the Traceability table.

    DO NOT touch the existing 12 Phase 13 rows (RBAC-01..06, SEED-01..03, DELIVERY-01..03) at lines 138-146 + 158-160 — those already say `Complete`.
  </action>
  <verify>
    <automated>test $(grep -cE "^\| (DEMO|RUNBOOK)-0[1-6]? \| Phase 14 \| Complete \|" .planning/REQUIREMENTS.md) -eq 11</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\| DEMO-0[1-6] \| Phase 14 \| Complete \|" .planning/REQUIREMENTS.md` returns 6
    - `grep -cE "^\| RUNBOOK-0[1-5] \| Phase 14 \| Complete \|" .planning/REQUIREMENTS.md` returns 5
    - `grep -cE "^\| (DEMO|RUNBOOK)-.* \| Phase 14 \| Pending \|" .planning/REQUIREMENTS.md` returns 0
    - `grep -cE "^\| (RBAC|SEED|DELIVERY)-.* \| Phase 13 \| Complete \|" .planning/REQUIREMENTS.md` returns 12 (unchanged — pre-existing Phase 13 rows)
    - No Traceability table rows reference Phase 15 (Phase 15 has no own REQ-IDs)
    - REQUIREMENTS.md total `Complete` Traceability cells = 23 (12 Phase 13 + 11 Phase 14)
  </acceptance_criteria>
  <done>
    All 11 DEMO + RUNBOOK Traceability rows flipped from `Pending` to `Complete`; 12 pre-existing Phase 13 `Complete` rows untouched; final state: 23/23 v0.20 mandatory REQs Complete in both the checkbox list AND the Traceability table (the audit's 3-source matrix has both sides aligned).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation reconciliation only; no new attack surface introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-a | T (Tampering) | Edit tool ambiguous `old_string` lands flip on wrong REQ line | mitigate | Each `old_string` uses the bold REQ-ID literal (`**DEMO-01**`, etc.) as unique anchor (PATTERNS §"Edit tool unique-old_string convention"); acceptance_criteria use exact-count grep (`-eq` not `-ge`) to catch silent extra/missing matches |
| T-15-05 | T (Tampering) | OQ lock-in amend introduces line-number shifts breaking downstream grep-based citation patterns in Plan 15-02..15-05 | mitigate | All downstream acceptance_criteria use `grep -c '<literal>'` patterns (line-number-independent), NEVER `sed -n '<N>p'` (Pitfall 15-P4); verified against Plan 15-02..15-05 drafts before commit |
| T-15-01 (default doc-only) | I (Information disclosure) | REQUIREMENTS.md edits expose secrets | accept | No secrets in REQ text; book-keeping only; existing v0.20 security posture per `14-VERIFICATION.md` unchanged |
</threat_model>

<verification>
After all 3 tasks complete:

```bash
# Final state assertions (all must pass)
test $(grep -v '^#' .planning/REQUIREMENTS.md | grep -cE "^- \[x\] \*\*(RBAC|SEED|DEMO|RUNBOOK|DELIVERY)-") -eq 23 && \
test $(grep -v '^#' .planning/REQUIREMENTS.md | grep -cE "^- \[ \] \*\*(RBAC|SEED|DEMO|RUNBOOK|DELIVERY)-") -eq 0 && \
test $(grep -cE "^\| (RBAC|SEED|DEMO|RUNBOOK|DELIVERY)-.* \| Phase 1[34] \| Complete \|" .planning/REQUIREMENTS.md) -eq 23 && \
test $(grep -c "Phase 13 D-13-01" .planning/REQUIREMENTS.md) -ge 1 && \
test $(grep -c "Phase 13 D-13-02" .planning/REQUIREMENTS.md) -ge 1 && \
test $(grep -c "Phase 13 D-13-03" .planning/REQUIREMENTS.md) -ge 1 && \
test $(grep -c "Phase 14 D-14-02" .planning/REQUIREMENTS.md) -ge 1 && \
test $(grep -c "Phase 14 D-14-03" .planning/REQUIREMENTS.md) -ge 1
```
</verification>

<success_criteria>
- 11 `[ ] → [x]` flips landed (DEMO-01..06, RUNBOOK-01..05)
- 4 OQ lock-in amendments landed inline (RBAC-01 / SEED-03 / DELIVERY-01 / RUNBOOK-05) with literal D-code citations
- 11 Traceability table rows changed from `Pending` to `Complete` (DEMO + RUNBOOK)
- ZERO v0.20 mandatory `[ ]` checkboxes remain
- Future Requirements (ADDON-01/02) untouched
- All verification grep gates pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-v0-20-milestone-close-out-retrospective/15-01-SUMMARY.md` per `~/.claude/get-shit-done/templates/summary.md`.

**Critical (per Pitfall 15-P6):** SUMMARY frontmatter MUST set `requirements-completed: [DEMO-01, DEMO-02, DEMO-03, DEMO-04, DEMO-05, DEMO-06, RUNBOOK-01, RUNBOOK-02, RUNBOOK-03, RUNBOOK-04, RUNBOOK-05]` to satisfy the audit's 3-source matrix.

SUMMARY body should include a "No-deviation cases" section listing each of the 11 REQs against live evidence with "no deviation" (DEMO-01..06, RUNBOOK-01/02/04) or "OQ lock-in: <citation>" (RBAC-01 OQ-2, SEED-03 OQ-3, DELIVERY-01 OQ-4, RUNBOOK-03 OQ-1 anchor, RUNBOOK-05 OQ-5).
</output>
