---
phase: 13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/bats/rbac-01-static-tenant-workload-editor.bats
  - tests/bats/rbac-02-static-platform-owner-sa.bats
  - tests/bats/rbac-03-static-tenant-co-ownership.bats
  - tests/bats/rbac-04-static-human-owner-sa.bats
  - tests/bats/rbac-05-live-tenant-owner-grant-matrix.bats
  - tests/bats/rbac-06-live-tenant-kubeconfig.bats
  - tests/bats/rbac-07-live-flux-sa-preservation.bats
  - tests/bats/rbac-08-live-platform-owner-grant-matrix.bats
  - tests/bats/rbac-09-live-platform-owner-kubeconfig.bats
  - tests/bats/rbac-10-live-tenant-crud.bats
  - tests/bats/seed-01-static-globaltenantresource.bats
  - tests/bats/seed-02-live-existing-tenants.bats
  - tests/bats/seed-03-live-fresh-tenant.bats
  - tests/bats/seed-04-live-shape.bats
  - tests/bats/delivery-01-static-mount-sentinel.bats
  - tests/bats/delivery-02-static-paths.bats
  - tests/bats/delivery-03-live-additive-only.bats
  - tests/bats/delivery-04-live-flux-reconcile.bats
  - tests/bats/delivery-05-live-slo-regression.bats
autonomous: true
requirements:
  - RBAC-01
  - RBAC-02
  - RBAC-03
  - RBAC-04
  - RBAC-05
  - RBAC-06
  - SEED-01
  - SEED-02
  - SEED-03
  - DELIVERY-01
  - DELIVERY-02
  - DELIVERY-03
tags:
  - capsule
  - bats
  - wave0
  - nyquist

must_haves:
  truths:
    - "18 NEW bats files exist under tests/bats/ covering RBAC + SEED + DELIVERY contracts"
    - "All 18 NEW static + live bats files are RED before any Phase 13 implementation lands"
    - "Wave 0 bats lock the falsifier matrix: ClusterRole shape, platform-owner SA + dual-subject CRB, Tenant CR additive co-ownership, GTR + Pod + Service inline shape, KARYON POC MOUNT sentinel preservation, file-layout boundaries, additive-only Tenant CR diff"
    - "6 inherited bats (Phase 7 P31, Phase 8 ADR-004, Phase 9 P32, Phase 10 PROXY-01, Phase 11 D-11-02 + D-11-03) still GREEN — Wave 0 verifies inheritance reachability"
    - "Live cluster state preflight verified — CapsuleConfiguration.userGroups contains system:serviceaccounts + system:authenticated (Phase 11 D-11-02 inheritance)"
  artifacts:
    - path: "tests/bats/rbac-01-static-tenant-workload-editor.bats"
      provides: "RBAC-01 ClusterRole shape static contract (positive verbs + 4 denial categories + Pitfall 13-P3 aggregate-to anti-pattern)"
      contains: "tenant-workload-editor"
    - path: "tests/bats/rbac-02-static-platform-owner-sa.bats"
      provides: "D-13-06 SA file shape + dual-subject CRB"
      contains: "platform-owner"
    - path: "tests/bats/rbac-03-static-tenant-co-ownership.bats"
      provides: "D-13-05 + D-13-07 owner additions to alpha + bravo (Group + SA + human-tenant-owner with explicit clusterRoles)"
      contains: "capsule-platform-owners"
    - path: "tests/bats/rbac-04-static-human-owner-sa.bats"
      provides: "Per-tenant human-tenant-owner SA file shape"
      contains: "human-tenant-owner"
    - path: "tests/bats/rbac-05-live-tenant-owner-grant-matrix.bats"
      provides: "RBAC-01 live grant matrix — positive grants + 4 explicit denials"
    - path: "tests/bats/rbac-06-live-tenant-kubeconfig.bats"
      provides: "RBAC-02 live kubeconfig auth can-i '*' '*' no + DEMO-04 create-secret Forbidden"
    - path: "tests/bats/rbac-07-live-flux-sa-preservation.bats"
      provides: "RBAC-03 Flux SA owner pattern regression gate"
    - path: "tests/bats/rbac-08-live-platform-owner-grant-matrix.bats"
      provides: "RBAC-04 platform-owner ceiling — auth can-i '*' '*' no + nodes/csr/clusterrolebinding Forbidden + positive Tenant CR grants"
    - path: "tests/bats/rbac-09-live-platform-owner-kubeconfig.bats"
      provides: "RBAC-04 + RBAC-06 live kubeconfig LIST tenants + LIST namespaces through capsule-proxy"
    - path: "tests/bats/rbac-10-live-tenant-crud.bats"
      provides: "RBAC-05 Tenant CRUD via platform-owner kubeconfig (create/get/patch/delete)"
    - path: "tests/bats/seed-01-static-globaltenantresource.bats"
      provides: "SEED-01 GTR + Pod + Service inline shape with FQCI nginx image"
      contains: "GlobalTenantResource"
    - path: "tests/bats/seed-02-live-existing-tenants.bats"
      provides: "SEED-01 propagation to alpha-app1 + tenant-alpha + bravo-app1 + tenant-bravo"
    - path: "tests/bats/seed-03-live-fresh-tenant.bats"
      provides: "SEED-02 fresh-tenant propagation ≤60s falsifier (uses new-tenant.sh phase13-fresh)"
    - path: "tests/bats/seed-04-live-shape.bats"
      provides: "SEED-03 Pod + Service only — no Deployment / no ReplicaSet / no extra ConfigMap"
    - path: "tests/bats/delivery-01-static-mount-sentinel.bats"
      provides: "KARYON POC MOUNT sentinel preservation in clusters/hub-flux/flux-system/kustomization.yaml"
      contains: "KARYON POC MOUNT"
    - path: "tests/bats/delivery-02-static-paths.bats"
      provides: "D-13-13 file layout + DELIVERY-01 + DELIVERY-02 P31 (no Taskfile.yml edits, no v0.18 script regression)"
    - path: "tests/bats/delivery-03-live-additive-only.bats"
      provides: "DELIVERY-03 additive-only Tenant CR diff against Phase 12 base SHA"
    - path: "tests/bats/delivery-04-live-flux-reconcile.bats"
      provides: "DELIVERY-03 existing Flux Ks (poc-capsule-spoke + poc-capsule-spoke-rbac + poc-capsule-spoke-tenants) Ready=True after Phase 13"
    - path: "tests/bats/delivery-05-live-slo-regression.bats"
      provides: "DELIVERY-02 task rebuild SLO <230s regression (KARYON_REBUILD_APPROVED gated)"
  key_links:
    - from: "tests/bats/rbac-*-static-*.bats"
      to: "pocs/capsule/spoke/rbac/* and pocs/capsule/spoke/tenant-crs/*"
      via: "grep + yq path assertions"
      pattern: "REPO_ROOT/pocs/capsule/spoke/rbac/(tenant-workload-editor|platform-owner-sa|platform-owner)\\.yaml"
    - from: "tests/bats/rbac-*-live-*.bats"
      to: "k3d-spoke-capsule cluster (kubectl auth can-i + kubeconfig mint)"
      via: "issue-tenant-kubeconfig.sh + issue-platform-owner-kubeconfig.sh"
      pattern: "kubectl --kubeconfig=.*auth can-i"
    - from: "tests/bats/seed-*-static-*.bats"
      to: "pocs/capsule/spoke/global-resources/hello-world.yaml"
      via: "grep + yq path assertions"
      pattern: "REPO_ROOT/pocs/capsule/spoke/global-resources/hello-world\\.yaml"
    - from: "tests/bats/delivery-01-static-*.bats"
      to: "clusters/hub-flux/flux-system/kustomization.yaml"
      via: "grep -F + grep -c"
      pattern: "KARYON POC MOUNT"
---

<objective>
Plan 13-00 — Wave 0 RED bats scaffold. Lands ALL 18 NEW bats files covering RBAC-01..06 + SEED-01..03 + DELIVERY-01..03 contracts as failing tests BEFORE any implementation. Plus a live-state preflight task that re-verifies Phase 11 D-11-02 CapsuleConfiguration.userGroups patch is still live (no drift).

Purpose: Lock the falsifier matrix in advance per Nyquist gate (D-09-08 inheritance from Phases 7/8/9/10/11). Every executor task in Wave 1+ has a pre-existing automated test command before implementation lands.

Output: 18 NEW bats files + 1 commit + cluster state preflight log. All 18 bats RED end-of-plan (static tests fail because target files don't exist yet; live tests skip because target resources don't exist yet but scaffold is in place).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-CONTEXT.md
@.planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-RESEARCH.md
@.planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-PATTERNS.md
@.planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VALIDATION.md

# Analog bats files (read for naming + assertion shape conventions)
@tests/bats/capsule-platform-owner-01-static-rbac.bats
@tests/bats/capsule-install-04-static-no-umbrella.bats
@tests/bats/proxy-02-static-leak-defenses.bats
@tests/bats/negative-rbac-03-webhook-policy.bats
@tests/bats/capsule-install-09-live-adr-004.bats
@tests/bats/tenants-08-live-quota-limit.bats
@tests/bats/phase5-02-live.bats
@tests/bats/poc-isolation-01-static.bats
@tests/bats/test_helper

<interfaces>
<!-- Key types/literals executor needs. From PATTERNS.md + analog bats files. -->

## bats test_helper exports (from tests/bats/test_helper):
```
REPO_ROOT   # absolute repo root
pass / warn / fail / info / have   # helper functions inherited from preflight-lib (NOT used in static bats; only echo-style)
```

## Cluster gate idiom (from capsule-install-09-live-adr-004.bats lines 10-14):
```bash
setup() {
  if ! kubectl --context=k3d-spoke-capsule cluster-info >/dev/null 2>&1; then
    skip "k3d-spoke-capsule cluster not reachable; skipping live bats"
  fi
}
```

## setup_file kubeconfig mint idiom (from negative-rbac-03-webhook-policy.bats lines 21-34):
```bash
setup_file() {
  if ! kubectl --context=k3d-spoke-capsule cluster-info >/dev/null 2>&1; then
    skip "k3d-spoke-capsule cluster not reachable; skipping live bats"
  fi
  export KARYON_BATS_TMPDIR="${TMPDIR:-/tmp}/karyon-tenants/bats-${BATS_RUN_TMPDIR##*/}"
  mkdir -p "$KARYON_BATS_TMPDIR"
  bash "${REPO_ROOT}/scripts/poc/capsule/issue-tenant-kubeconfig.sh" alpha human-tenant-owner \
    --duration=2h --write-to "${KARYON_BATS_TMPDIR}/alpha.kubeconfig" >/dev/null 2>&1
  export ALPHA_KUBECONFIG="${KARYON_BATS_TMPDIR}/alpha.kubeconfig"
}
```

## Forbidden literals (grep -v '^#' filtering) idiom (from poc-isolation-01-static.bats):
```bash
@test "X: file has zero <literal> mentions (comments excluded)" {
  [ -f "$FILE" ]
  run bash -c "grep -v '^[[:space:]]*#' '$FILE' | grep -F -- '<literal>'"
  [ "$status" -ne 0 ]
}
```

## Phase 13 file path conventions:
- ClusterRole:        pocs/capsule/spoke/rbac/tenant-workload-editor.yaml
- Platform-owner SA:  pocs/capsule/spoke/rbac/platform-owner-sa.yaml
- Platform-owner CRB: pocs/capsule/spoke/rbac/platform-owner.yaml (EDITED — subjects[] extended)
- RBAC kustomization: pocs/capsule/spoke/rbac/kustomization.yaml (EDITED — resources appended)
- Human-owner SA:     pocs/capsule/spoke/tenants/{alpha,bravo}/human-owner-sa.yaml
- Tenant kustomization: pocs/capsule/spoke/tenants/{alpha,bravo}/kustomization.yaml (EDITED)
- Tenant CR:          pocs/capsule/spoke/tenant-crs/{alpha,bravo}.yaml (EDITED — owners[] + containerRegistries.allowed += docker.io)
- GTR + Pod + Service: pocs/capsule/spoke/global-resources/hello-world.yaml
- GTR kustomization:  pocs/capsule/spoke/global-resources/kustomization.yaml
- Spoke kustomization: pocs/capsule/spoke/kustomization.yaml (EDITED — global-resources/ appended)
- Issue platform-owner kc: scripts/poc/capsule/issue-platform-owner-kubeconfig.sh
- New-tenant script:  scripts/poc/capsule/new-tenant.sh
- Issue-tenant-kc:    scripts/poc/capsule/issue-tenant-kubeconfig.sh (EDITED — usage hint expanded)

## Phase 13 expected literals (all bats must grep-assert against these):
- ClusterRole name:                       capsule-tenant-workload-editor  (label) / tenant-workload-editor (RoleRef name per D-13-04)
  NOTE: Per D-13-04 the resource name is exactly `tenant-workload-editor` (used in Tenant.spec.owners[].clusterRoles[]).
- Platform-owner SA name:                 platform-owner (in capsule-system ns)
- Per-tenant human SA name:               human-tenant-owner (in tenant-<tenant> ns)
- Platform-owner Group:                   capsule-platform-owners
- GTR name:                               hello-world-seeded
- GTR apiVersion:                         capsule.clastix.io/v1beta2
- GTR kind:                               GlobalTenantResource
- match-all selector:                     tenantSelector.matchLabels: {}
- Container image (FQCI required):        docker.io/library/nginx:alpine
- Container registry allowlist additive:  docker.io
- Provenance label:                       karyon.io/managed-by: capsule-global-tenant-resource
- KARYON POC MOUNT sentinel file:         clusters/hub-flux/flux-system/kustomization.yaml
- Existing Tenant CR owners[] entries to preserve (RBAC-03 regression gate):
    - system:serviceaccount:flux-system:flux-reconciler
    - system:serviceaccount:tenant-alpha:gitops-reconciler  (and bravo)
    - User alpha  (and bravo)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight live state verification (CapsuleConfiguration.userGroups inheritance + cluster reachability)</name>
  <files>(no file output — STATE / inspection only; informational stdout)</files>
  <read_first>
    - .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-CONTEXT.md (Risks section — capsule-proxy LIST filter wiring inheritance; CapsuleConfiguration `userGroups` live-state verification)
    - .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-RESEARCH.md ("Runtime State Inventory" section — userGroups verification 2026-05-19)
    - pocs/capsule/spoke/config/capsuleconfig.yaml (Git source of truth for CapsuleConfiguration — should mirror live state)
  </read_first>
  <action>
    Run the following preflight probes against `k3d-spoke-capsule` to confirm v0.19 inheritance still holds before any Phase 13 implementation lands. Capture stdout + stderr to a tmpdir log for inclusion in the plan SUMMARY.

    ```bash
    # Probe 1: cluster reachable
    kubectl --context=k3d-spoke-capsule cluster-info

    # Probe 2: CapsuleConfiguration userGroups inheritance (Phase 11 D-11-02 — REQUIRED for capsule-proxy LIST filter to recognize SA-token identities)
    kubectl --context=k3d-spoke-capsule get capsuleconfiguration default \
      -o jsonpath='{.spec.userGroups}'
    # Expected output: ["capsule.clastix.io","system:serviceaccounts","system:authenticated"]

    # Probe 3: GlobalTenantResource CRD installed (Capsule v0.12.4 — D-13-11 requires this)
    kubectl --context=k3d-spoke-capsule get crd globaltenantresources.capsule.clastix.io \
      -o jsonpath='{.spec.versions[?(@.storage==true)].name}'
    # Expected output: v1beta2

    # Probe 4: Existing alpha + bravo Tenants present + Flux SA owners preserved (RBAC-03 baseline)
    kubectl --context=k3d-spoke-capsule get tenant alpha bravo \
      -o jsonpath='{range .items[*]}{.metadata.name}: {range .spec.owners[*]}{.name}{","}{end}{"\n"}{end}'
    # Expected: alpha and bravo each list system:serviceaccount:flux-system:flux-reconciler, system:serviceaccount:tenant-<t>:gitops-reconciler, User alpha/bravo

    # Probe 5: capsule-proxy Secret has tls.crt key (Phase 10 D-10-06 + P1 inheritance; new platform-owner script reads same key)
    kubectl --context=k3d-spoke-capsule -n capsule-system get secret capsule-proxy \
      -o jsonpath='{.data}' | jq 'keys'
    # Expected: ["ca","tls.crt","tls.key"]   (per RESEARCH finding #7 — `ca` key also present; Phase 13 mirrors Phase 10's tls.crt verbatim)

    # Probe 6: capsule-platform-owner ClusterRole + ClusterRoleBinding exist (D-13-06 extension target)
    kubectl --context=k3d-spoke-capsule get clusterrole capsule-platform-owner -o name
    kubectl --context=k3d-spoke-capsule get clusterrolebinding capsule-platform-owner \
      -o jsonpath='{.subjects}'
    # Expected: ClusterRole exists; CRB subjects[] contains Group: capsule-platform-owners
    ```

    Tee all output to `/tmp/13-00-preflight.log`. If any probe fails OR returns drift from the expected output, FAIL FAST: emit "BLOCKER: Phase 11 D-11-02 / Phase 10 D-10-06 inheritance does not match live state — investigate before Plan 13-01 lands implementation. Output saved to /tmp/13-00-preflight.log." and exit 1.

    Per the quality gate: this preflight is non-destructive (read-only kubectl gets) — no cluster mutation, no resource creation, no script invocation.
  </action>
  <verify>
    <automated>kubectl --context=k3d-spoke-capsule cluster-info >/dev/null 2>&1 && test -f /tmp/13-00-preflight.log && grep -F 'v1beta2' /tmp/13-00-preflight.log && grep -F 'system:serviceaccounts' /tmp/13-00-preflight.log</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/13-00-preflight.log` exists and contains output from probes 1–6
    - Probe 2 output contains all three groups: `capsule.clastix.io`, `system:serviceaccounts`, `system:authenticated`
    - Probe 3 output is exactly `v1beta2`
    - Probe 5 output contains `tls.crt`
    - Probe 6 output contains `capsule-platform-owners`
    - If ANY probe fails, blocker emitted and task exits 1
  </acceptance_criteria>
  <done>Live state baseline verified — Phase 13 implementation can proceed against a known-stable Phase 11 inheritance baseline. Preflight log captured for SUMMARY citation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Land Wave 0 RED bats files — RBAC static (4 files) + RBAC live (6 files) + SEED static (1 file) + SEED live (3 files) + DELIVERY static (2 files) + DELIVERY live (3 files) = 19 files total (one of them is delivery-02-static which also covers DELIVERY-02 P31 static, complementing the inherited poc-isolation-01-static.bats)</name>
  <files>
    tests/bats/rbac-01-static-tenant-workload-editor.bats,
    tests/bats/rbac-02-static-platform-owner-sa.bats,
    tests/bats/rbac-03-static-tenant-co-ownership.bats,
    tests/bats/rbac-04-static-human-owner-sa.bats,
    tests/bats/rbac-05-live-tenant-owner-grant-matrix.bats,
    tests/bats/rbac-06-live-tenant-kubeconfig.bats,
    tests/bats/rbac-07-live-flux-sa-preservation.bats,
    tests/bats/rbac-08-live-platform-owner-grant-matrix.bats,
    tests/bats/rbac-09-live-platform-owner-kubeconfig.bats,
    tests/bats/rbac-10-live-tenant-crud.bats,
    tests/bats/seed-01-static-globaltenantresource.bats,
    tests/bats/seed-02-live-existing-tenants.bats,
    tests/bats/seed-03-live-fresh-tenant.bats,
    tests/bats/seed-04-live-shape.bats,
    tests/bats/delivery-01-static-mount-sentinel.bats,
    tests/bats/delivery-02-static-paths.bats,
    tests/bats/delivery-03-live-additive-only.bats,
    tests/bats/delivery-04-live-flux-reconcile.bats,
    tests/bats/delivery-05-live-slo-regression.bats
  </files>
  <read_first>
    - .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-RESEARCH.md ("Validation Architecture" section — exhaustive per-REQ test mapping; "Code Examples" section for can-i loop pattern + LIST filter test)
    - .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-PATTERNS.md (all bats sections lines 619-1067 — every bats file has a specific analog + grep/yq pattern excerpt)
    - tests/bats/capsule-platform-owner-01-static-rbac.bats (analog for static yq path + grep -qx assertions)
    - tests/bats/capsule-install-04-static-no-umbrella.bats (analog for yq array walk + `any_c(. == true)` denial assertion)
    - tests/bats/proxy-02-static-leak-defenses.bats (analog for grep -F literal-loop pattern)
    - tests/bats/negative-rbac-03-webhook-policy.bats (analog for setup_file kubeconfig mint + teardown_file cleanup; idempotent kubectl delete --ignore-not-found pattern)
    - tests/bats/capsule-install-09-live-adr-004.bats (analog for cluster gate setup() + jsonpath state probe; NotFound vs transient error distinguishing)
    - tests/bats/tenants-08-live-quota-limit.bats (analog for 3×10s poll loop pattern for live wait-for-state)
    - tests/bats/phase5-02-live.bats (analog for env-gated destructive rebuild + elapsed seconds extraction)
    - tests/bats/poc-isolation-01-static.bats (analog for `grep -v '^#' | grep -F` filtering pattern — Wave 0 of Plan 13-00 MUST use this filter for any grep -c or `! grep` static assertion, per planner-antipatterns "self-invalidating grep gate" rule)
    - tests/bats/test_helper (load + REPO_ROOT export contract)
  </read_first>
  <action>
    Create 19 NEW bats files at the paths listed above. Every file follows the conventions in `<interfaces>` above + uses the exact analogs in PATTERNS.md.

    CRITICAL GLOBAL RULES — apply to EVERY bats file:
    - Use `#!/usr/bin/env bats` shebang
    - `load 'test_helper'` (provides REPO_ROOT)
    - Static tests: setup() defines file path vars only; NO cluster gate
    - Live tests: setup() runs cluster gate (`kubectl --context=k3d-spoke-capsule cluster-info >/dev/null 2>&1 || skip "..."`)
    - For grep counts and `! grep` assertions on YAML/script files, filter comments first: `bash -c "grep -v '^[[:space:]]*#' '$FILE' | grep ..."`. This avoids the "self-invalidating grep gate" anti-pattern (planner-antipatterns) where the bats header itself contains the prohibited literal.
    - Live tests minting kubeconfigs use `setup_file` + `teardown_file` with `KARYON_BATS_TMPDIR="${TMPDIR:-/tmp}/karyon-tenants/bats-${BATS_RUN_TMPDIR##*/}"` (per Phase 7 D-14 tmpdir-root contract; covered by existing `karyon-tenants/` gitignore)
    - Every `@test` description follows the pattern: `"<REQ-ID> / <decision-tag>: <human-readable assertion>"` — bats `-x` summary makes the REQ traceability immediate
    - Comments in bats files: prefix WITH `#` so test files are themselves Wave 0 documentation (purpose: traceability for the executor in Wave 1+ implementation)

    === Static tests (10 files; sub-second) ===

    1. **`tests/bats/rbac-01-static-tenant-workload-editor.bats`** — assertions:
       - File `pocs/capsule/spoke/rbac/tenant-workload-editor.yaml` exists
       - `yq` asserts `.kind == "ClusterRole"` AND `.metadata.name == "tenant-workload-editor"`
       - `yq` asserts `.metadata.labels."karyon.io/role" == "capsule-tenant-workload-editor"`
       - `yq` POSITIVE: for each `(group, resource, verb)` in the D-13-04 positive grant matrix (see RESEARCH §"kubectl auth can-i matrix coverage"), assert the rule exists.
         Use: `run yq eval '[.rules[] | select(.apiGroups | contains([""]) and .resources | contains(["pods"])) | .verbs] | flatten | any_c(. == "create")' "$F"; [ "$output" = "true" ]`
         Cover: `pods` create/list/watch/get/update/patch/delete; `pods/log` get; `pods/exec` create; `pods/portforward` create; `deployments` apps/v1 CRUD; `services` "" CRUD; `configmaps` "" CRUD; `persistentvolumeclaims` "" get/list/watch; `secrets` "" get/list/watch
       - `yq` DENIAL: secrets create/update/patch/delete MUST be absent. Use `[ "$output" = "false" ]` on `any_c(. == "create")` for secrets.
       - `yq` DENIAL: `rolebindings`, `roles` (rbac.authorization.k8s.io), `resourcequotas`, `limitranges`, `nodes`, `customresourcedefinitions`, `tenants.capsule.clastix.io` — each MUST be absent from rules[]
       - Anti-pattern (Pitfall 13-P3): `! grep -F 'aggregate-to-' "$F"` (no aggregation label)
       - Anti-pattern: NO `secrets` `create`/`update`/`patch`/`delete` (4 separate yq tests)

    2. **`tests/bats/rbac-02-static-platform-owner-sa.bats`** — assertions:
       - File `pocs/capsule/spoke/rbac/platform-owner-sa.yaml` exists
       - `grep -F 'kind: ServiceAccount'` AND `grep -F 'name: platform-owner'` AND `grep -F 'namespace: capsule-system'` in the file
       - File `pocs/capsule/spoke/rbac/platform-owner.yaml` exists (existing — checked for ClusterRoleBinding subjects[] extension)
       - `yq` asserts CRB subjects[] contains BOTH:
         - `{kind: Group, name: capsule-platform-owners}` (existing — preserve)
         - `{kind: ServiceAccount, name: platform-owner, namespace: capsule-system}` (new — D-13-06)
         Use: `run yq eval '[.subjects[] | select(.kind == "Group" and .name == "capsule-platform-owners")] | length' "$RBAC"; [ "$output" = "1" ]` (existing); same for SA path.
       - `yq` asserts kustomization.yaml `resources:` list contains `platform-owner-sa.yaml`

    3. **`tests/bats/rbac-03-static-tenant-co-ownership.bats`** — assertions (alpha + bravo):
       - Files `pocs/capsule/spoke/tenant-crs/{alpha,bravo}.yaml` exist
       - For each, `yq` asserts `.spec.owners[]` CONTAINS each entry:
         - **Preserved (RBAC-03 regression gate):** `system:serviceaccount:flux-system:flux-reconciler`, `system:serviceaccount:tenant-<t>:gitops-reconciler`, `User <t>`
         - **NEW:** `system:serviceaccount:tenant-<t>:human-tenant-owner` with `clusterRoles: ["tenant-workload-editor"]`
         - **NEW:** `Group capsule-platform-owners` (no explicit clusterRoles)
         - **NEW:** `system:serviceaccount:capsule-system:platform-owner` (no explicit clusterRoles)
       - For each, `yq` asserts `.spec.containerRegistries.allowed` contains `docker.io` (per Pitfall 13-P1 + RESEARCH finding #6)
       - Anti-pattern: assert no entry has `clusterRoles: []` (empty array — Pitfall 13-P8)
         Use: `run yq eval '[.spec.owners[] | select(has("clusterRoles") and (.clusterRoles | length == 0))] | length' "$F"; [ "$output" = "0" ]`

    4. **`tests/bats/rbac-04-static-human-owner-sa.bats`** — assertions (alpha + bravo):
       - Files `pocs/capsule/spoke/tenants/{alpha,bravo}/human-owner-sa.yaml` exist
       - For each, `grep -F 'kind: ServiceAccount'`, `name: human-tenant-owner`, `namespace: tenant-<t>`
       - For each, kustomization.yaml `resources:` contains `human-owner-sa.yaml` (yq path)

    5. **`tests/bats/seed-01-static-globaltenantresource.bats`** — assertions:
       - File `pocs/capsule/spoke/global-resources/hello-world.yaml` exists
       - File `pocs/capsule/spoke/global-resources/kustomization.yaml` exists
       - `grep -F 'apiVersion: capsule.clastix.io/v1beta2'` AND `grep -F 'kind: GlobalTenantResource'`
       - `grep -F 'matchLabels: {}'` (match-all selector per A1 + RESEARCH Pattern 1)
       - `grep -F 'karyon.io/managed-by: capsule-global-tenant-resource'` (provenance label)
       - `grep -F 'image: docker.io/library/nginx:alpine'` (FQCI required per Pitfall 13-P1)
       - `yq eval-all` asserts presence of: Pod (kind: Pod) in `rawItems[]`, Service (kind: Service) in `rawItems[]`
       - SEED-03 boundary anti-patterns: `! grep -F 'kind: Deployment'` AND `! grep -F 'kind: ReplicaSet'` (NO Deployment / ReplicaSet); ConfigMap absence asserted via `! yq` on rawItems[] kind list (an extra ConfigMap is denied — per D-13-02 OQ-3 Pod+Service ONLY)
       - Parent `pocs/capsule/spoke/kustomization.yaml` `resources:` list contains `global-resources/` (yq path)

    6. **`tests/bats/delivery-01-static-mount-sentinel.bats`** — assertions:
       - File `clusters/hub-flux/flux-system/kustomization.yaml` exists
       - `grep -F 'KARYON POC MOUNT'` returns 0 (sentinel present)
       - `grep -c 'KARYON POC MOUNT'` returns exactly 1 (sentinel NOT duplicated — `grep -v '^[[:space:]]*#'` filtering required if any comments in source contain the literal; assert filtered count == 1, NOT zero)
         Use: `run bash -c "grep -v '^[[:space:]]*#' '$F' | grep -c 'KARYON POC MOUNT'"; [ "$output" -eq 1 ]`  (because the sentinel line itself starts with `#` so filtering it out would give 0; sentinel-recognized line may be a non-comment marker — confirm by reading the live file. If the sentinel IS a `# KARYON POC MOUNT` comment, the bats must use a different filter — e.g., assert `grep -c 'KARYON POC MOUNT' "$F" -eq 1` WITHOUT comment-stripping. Read clusters/hub-flux/flux-system/kustomization.yaml first to confirm and adjust.)
       - `grep -F '../pocs'` returns 0 (resource line present)

    7. **`tests/bats/delivery-02-static-paths.bats`** — assertions (file existence + P31 isolation):
       - File-existence loop: every NEW or EDITED file from Plan 13-01..13-03 exists under `pocs/capsule/spoke/` or `scripts/poc/capsule/` (anchor to the file_modified list across plans)
       - `Taskfile.yml` exists
       - Forbidden-literals loop in `Taskfile.yml` (using `bash -c "grep -v '^[[:space:]]*#' '$F' | grep -F -- 'X'"` filter, then `[ "$status" -ne 0 ]`):
         - `seed-capsule-demo`
         - `issue-platform-owner-kubeconfig`
         - `new-tenant` (Phase 13 task target — per D-13-14)
       - Forbidden-literals loop in every v0.18 default-path script (`scripts/preflight.sh`, `scripts/create-clusters.sh`, `scripts/register-spokes-for-flux.sh`, `scripts/health-check.sh`, `scripts/destroy.sh`, `scripts/delete-clusters.sh`, `scripts/rebuild.sh`):
         - `tenant-workload-editor`
         - `platform-owner`
         - `human-tenant-owner`
         - `GlobalTenantResource`
         - `hello-world`
         - `scripts/poc/capsule/issue-platform-owner-kubeconfig`
         - `scripts/poc/capsule/new-tenant`
       - This is intentionally a SUPERSET of `poc-isolation-01-static.bats` to cover Phase 13's specific NEW literals (the Phase 7 file only catches `spoke-capsule` + `register-poc-cluster` + `scripts/poc/`).

    === Live tests (9 files; cluster gate, mostly skip pending implementation) ===

    8. **`tests/bats/rbac-05-live-tenant-owner-grant-matrix.bats`** — RBAC-01 live grant matrix:
       - `setup_file`: mint alpha kubeconfig via `bash scripts/poc/capsule/issue-tenant-kubeconfig.sh alpha human-tenant-owner --duration=2h --write-to "$KARYON_BATS_TMPDIR/alpha.kubeconfig"`; export `ALPHA_KUBECONFIG`. Mint bravo similarly. Currently RED — the script doesn't accept human-tenant-owner yet AND the SA doesn't exist yet — `setup_file` itself fails, marking all @tests in the file as failing.
       - For each positive verb in D-13-04 grant matrix, `@test` runs `kubectl --kubeconfig=$ALPHA_KUBECONFIG auth can-i <verb> <resource> -n tenant-alpha`; assert `[ "$output" = "yes" ]`
         Cover: `create pods`, `get pods`, `list pods`, `watch pods`, `create deployments`, `create services`, `create configmaps`, `list events`, `get pods/log`, `create pods/exec`, `get persistentvolumeclaims`, `list persistentvolumeclaims`, `get secrets`, `list secrets`, `watch secrets`
       - For each denial verb, assert `[ "$output" = "no" ]`:
         `create secrets`, `update secrets`, `patch secrets`, `delete secrets`, `create rolebindings`, `update roles`, `create resourcequotas`, `create limitranges`, `create namespaces`, `create tenants.capsule.clastix.io`, `get nodes`, `create clusterrolebinding`
       - Mirror for bravo kubeconfig

    9. **`tests/bats/rbac-06-live-tenant-kubeconfig.bats`** — RBAC-02 + DEMO-04 boundary:
       - `setup_file`: mint alpha kubeconfig (as above)
       - `@test "RBAC-02: kubectl auth can-i '*' '*' returns no"` — `kubectl --kubeconfig=$ALPHA_KUBECONFIG auth can-i '*' '*'`; assert `[ "$output" = "no" ]`
       - `@test "RBAC-01 positive: kubectl get pods -n tenant-alpha succeeds"` — assert exit 0
       - `@test "RBAC-01 boundary (D-13-01): kubectl create secret ... -n tenant-alpha returns Forbidden"` — `kubectl --kubeconfig=$ALPHA_KUBECONFIG create secret generic foo --from-literal=k=v -n tenant-alpha 2>&1`; assert `grep -F 'Forbidden\|forbidden\|cannot create resource'` (use permissive grep per Pitfall 11-P3 lesson — variants of Forbidden text)

    10. **`tests/bats/rbac-07-live-flux-sa-preservation.bats`** — RBAC-03 regression gate:
       - cluster gate in setup()
       - `@test "RBAC-03: existing gitops-reconciler SA owner preserved on alpha"` — `kubectl get tenant alpha -o jsonpath='{.spec.owners[?(@.name=="system:serviceaccount:tenant-alpha:gitops-reconciler")].name}'`; assert output equals the exact name string
       - `@test "RBAC-03: existing flux-reconciler SA owner preserved on alpha"` — same pattern for `system:serviceaccount:flux-system:flux-reconciler`
       - Mirror for bravo
       - `@test "RBAC-03: poc-capsule-spoke-tenants Flux Kustomization Ready=True"` — `kubectl --context=k3d-hub-flux get kustomization poc-capsule-spoke-tenants -n flux-system -o jsonpath='{.status.conditions[?(@.type=="Ready")].status}'`; assert `[ "$output" = "True" ]`
       - WR-04 fix pattern (distinguish NotFound from transient): `if echo "$ns_check" | grep -qF 'NotFound'; then fail; fi`

    11. **`tests/bats/rbac-08-live-platform-owner-grant-matrix.bats`** — RBAC-04 ceiling:
       - `setup_file`: mint platform-owner kubeconfig via `bash scripts/poc/capsule/issue-platform-owner-kubeconfig.sh --duration=2h --write-to "$KARYON_BATS_TMPDIR/po.kubeconfig"`; export `PO_KUBECONFIG`. Currently RED — the script doesn't exist yet.
       - Positive: `create tenants.capsule.clastix.io`, `list tenants.capsule.clastix.io`, `get tenants.capsule.clastix.io`, `list namespaces`, `get customresourcedefinitions`
       - Denial: `'*' '*'` → no; `get nodes` → no; `get csr` → no; `create clusterrolebinding` → no; `create namespaces` → no (per existing capsule-platform-owner ClusterRole shape — namespaces verbs are GLW only)

    12. **`tests/bats/rbac-09-live-platform-owner-kubeconfig.bats`** — RBAC-06 co-owner LIST through proxy:
       - `setup_file`: mint platform-owner kubeconfig (as above)
       - `@test "RBAC-06: get tenants returns alpha + bravo"` — assert both present in output
       - `@test "RBAC-06: get namespaces returns tenant-alpha + tenant-bravo + alpha-app1 + bravo-app1"` — assert all 4 present
       - `@test "RBAC-06 (filter holds): get namespaces does NOT return kube-system"` — assert kube-system absent (capsule-proxy filter)
       - `@test "RBAC-04 (forbidden): get nodes returns Forbidden"` — assert exit non-zero AND stderr/stdout grep 'Forbidden\|forbidden'
       - `@test "RBAC-06 (positive co-owner): get pods -n tenant-alpha succeeds"` — exit 0

    13. **`tests/bats/rbac-10-live-tenant-crud.bats`** — RBAC-05 Tenant CRUD via platform-owner kubeconfig:
       - `setup_file`: mint platform-owner kubeconfig
       - `setup`: ensure tenant `phase13-fresh` does not pre-exist (`kubectl delete tenant phase13-fresh --ignore-not-found`)
       - `@test "RBAC-05 (create): new-tenant.sh phase13-fresh succeeds"` — `bash scripts/poc/capsule/new-tenant.sh phase13-fresh --kubeconfig=$PO_KUBECONFIG`; exit 0
       - `@test "RBAC-05 (get): kubectl get tenant phase13-fresh succeeds"` — exit 0 with non-empty output
       - `@test "RBAC-05 (patch): kubectl patch tenant phase13-fresh ... succeeds"` — e.g., add a label patch
       - `@test "RBAC-05 (delete): kubectl delete tenant phase13-fresh succeeds"` — exit 0
       - `teardown_file`: idempotent cleanup `kubectl delete tenant phase13-fresh --ignore-not-found --wait=false`

    14. **`tests/bats/seed-02-live-existing-tenants.bats`** — SEED-01 propagation:
       - cluster gate
       - For each ns in `alpha-app1, tenant-alpha, bravo-app1, tenant-bravo`:
         - `@test "SEED-01 (<ns>): hello-world Pod Ready within 30s"` — 3×10s poll loop on `.status.conditions[?(@.type=="Ready")].status == "True"`
         - `@test "SEED-01 (<ns>): hello-world Pod carries karyon.io/managed-by=capsule-global-tenant-resource label"` — `kubectl get pod hello-world -n <ns> -o jsonpath='{.metadata.labels.karyon\.io/managed-by}'` == `capsule-global-tenant-resource`
         - `@test "SEED-01 (<ns>): hello-world Service exists with port 80"` — `kubectl get svc hello-world -n <ns> -o jsonpath='{.spec.ports[0].port}'` == `80`

    15. **`tests/bats/seed-03-live-fresh-tenant.bats`** — SEED-02 fresh-tenant ≤60s falsifier:
       - `setup_file`: mint platform-owner kubeconfig; ensure `phase13-fresh` does not pre-exist
       - `setup`: invoke `bash scripts/poc/capsule/new-tenant.sh phase13-fresh --kubeconfig=$PO_KUBECONFIG`; wait for `Tenant.Status.Namespaces` to be non-empty (Pitfall 13-P2; 3×10s poll)
       - `@test "SEED-02: hello-world Pod Ready in tenant-phase13-fresh within 60s"` — `kubectl wait pod/hello-world --for=condition=Ready -n tenant-phase13-fresh --timeout=60s`; assert exit 0
       - `teardown_file`: `kubectl --kubeconfig=$PO_KUBECONFIG delete tenant phase13-fresh --ignore-not-found --wait=false`; then `kubectl wait --for=delete namespace tenant-phase13-fresh --timeout=30s || true` (Pitfall 13-P6 — pruningOnDelete unmaterialization)

    16. **`tests/bats/seed-04-live-shape.bats`** — SEED-03 shape:
       - cluster gate
       - For each ns in `tenant-alpha, alpha-app1`:
         - `@test "SEED-03 (<ns>): zero Deployments materialized by GTR"` — `kubectl get deployment -n <ns> -l karyon.io/managed-by=capsule-global-tenant-resource --no-headers | wc -l` == `0`
         - `@test "SEED-03 (<ns>): zero ReplicaSets materialized by GTR"` — same for replicaset
         - `@test "SEED-03 (<ns>): zero ConfigMaps materialized by GTR"` — same for configmap with label selector
         - `@test "SEED-03 (<ns>): exactly 1 Pod hello-world materialized by GTR"` — `kubectl get pod hello-world -n <ns>` exit 0
         - `@test "SEED-03 (<ns>): exactly 1 Service hello-world materialized by GTR"` — same for svc

    17. **`tests/bats/delivery-03-live-additive-only.bats`** — DELIVERY-03 additive-only Tenant CR diff:
       - For each tenant `alpha, bravo`:
         - `@test "DELIVERY-03 (<tenant>): no spec.owners[] removals from Phase 12 base"` — compute base SHA from `git rev-parse HEAD@{20}` OR from `.planning/STATE.md` Performance Metrics last v0.19 commit (executor picks the right base; document chosen approach in commit message). Then `git diff <base> -- pocs/capsule/spoke/tenant-crs/<tenant>.yaml | grep -E '^-[^-]' | grep -F 'name:'` returns 0 lines (only additions to owners[] names; no removal-of-name lines)
       - The exact `git diff` shape: `^-[^-]` matches removal lines (excluding the `^---` diff header); planner accepts an alternative implementation using `yq` over current vs old file content as long as it asserts "current owners[] is superset of old owners[]"
       - Currently RED — Phase 13 implementation hasn't landed; the diff is empty against current HEAD, but post-implementation a non-additive change would fail. This bats is therefore "GREEN-by-design" until Plan 13-01/13-02 lands (the diff is empty pre-implementation, which yields zero deletions). The bats becomes meaningful AFTER Plan 13-02 commits.

    18. **`tests/bats/delivery-04-live-flux-reconcile.bats`** — DELIVERY-03 Flux Ks Ready=True:
       - cluster gate
       - For each Kustomization in `poc-capsule-spoke, poc-capsule-spoke-rbac, poc-capsule-spoke-tenants`:
         - `@test "DELIVERY-03 (<K>): Ready=True"` — `kubectl --context=k3d-hub-flux get kustomization <K> -n flux-system -o jsonpath='{.status.conditions[?(@.type=="Ready")].status}'` == `True`

    19. **`tests/bats/delivery-05-live-slo-regression.bats`** — DELIVERY-02 SLO regression:
       - `setup()`: if `${KARYON_REBUILD_APPROVED:-}` != `1`, `skip "DELIVERY-02 SLO regression — set KARYON_REBUILD_APPROVED=1 to run (destructive)"`
       - `LOG_FILE="/tmp/karyon-rebuild.log"`
       - `@test "DELIVERY-02 / Phase 13 SLO: rebuild elapsed seconds <= 230 (v0.18 baseline preserved)"` — extract elapsed via `grep -E '^rebuild elapsed seconds: [0-9]+' "$LOG_FILE" | awk '{print $4}' | tail -1`; assert `<= 230`

    === End of bats ===

    After creating all 19 files, run `bats tests/bats/{rbac,seed,delivery}-*.bats -x 2>&1 | tee /tmp/13-00-wave0-red.log`. Capture the RED-count summary. Expected: every static test is RED (target files don't exist yet), every live test either skips (cluster gate fails — should not happen since preflight passed) OR is RED via setup_file failing (script doesn't exist).

    Per D-13-15 + Nyquist gate: do NOT alter any inherited bats. The 6 inherited bats files (poc-isolation-01-static.bats, proxy-04-live-issue.bats, capsule-install-09-live-adr-004.bats, tenants-04-static-dependson.bats, push-gate-02-static-capsuleconfig.bats, push-gate-03-static-tenant-ns-label.bats) are re-run by Plan 13-04 verifier and MUST remain GREEN throughout Phase 13.
  </action>
  <verify>
    <automated>test $(ls -1 tests/bats/{rbac,seed,delivery}-{01,02,03,04,05,06,07,08,09,10}-*.bats 2>/dev/null | wc -l) -ge 18 && bats tests/bats/{rbac,seed,delivery}-*.bats -x 2>&1 | tee /tmp/13-00-wave0-red.log | grep -E '^[0-9]+ tests' && bats tests/bats/poc-isolation-01-static.bats tests/bats/capsule-install-09-live-adr-004.bats tests/bats/tenants-04-static-dependson.bats 2>&1 | grep -E '0 failures|not ok' </automated>
  </verify>
  <acceptance_criteria>
    - All 19 NEW bats files exist at the paths listed in `<files>`
    - Each file starts with `#!/usr/bin/env bats` and `load 'test_helper'`
    - Static bats files contain `[ -f "$FILE" ]` existence assertions for their target file(s)
    - Live bats files contain cluster gate setup (`kubectl --context=k3d-spoke-capsule cluster-info ... skip`)
    - `bats tests/bats/{rbac,seed,delivery}-*-static-*.bats` runs and produces a RED-count > 0 (target YAML files don't exist yet)
    - `bats tests/bats/{rbac,seed,delivery}-*-live-*.bats` runs and produces either skip-summary or RED-count > 0 (target scripts + resources don't exist yet)
    - 6 inherited bats files (poc-isolation-01-static.bats, capsule-install-09-live-adr-004.bats, tenants-04-static-dependson.bats, push-gate-02-static-capsuleconfig.bats, push-gate-03-static-tenant-ns-label.bats, proxy-04-live-issue.bats) continue to be GREEN (re-running them in this commit shows 0 new failures)
    - No bats file uses `grep -c X` without `grep -v '^[[:space:]]*#'` filter when the file under test could contain `X` in a comment (anti-pattern guard)
    - Wave 0 log saved to `/tmp/13-00-wave0-red.log`
  </acceptance_criteria>
  <done>19 NEW bats files committed; Wave 0 RED state locked; falsifier matrix is in place for Plan 13-01..13-03 implementation to flip to GREEN incrementally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → repo | NEW bats files materialize specific RBAC contracts; if test logic is wrong, it lies to executors about implementation state |
| bats process → kubectl context | Live bats reach into k3d-spoke-capsule cluster; if cluster reachable but in unexpected state, false-positive risk |
| Wave 0 bats → Wave 1+ executor | Wave 0 defines the falsifier matrix; if Wave 0 misses a contract, Wave 1+ executor lands implementation without coverage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | Tenant kubeconfig in git (RBAC-02 / RBAC-04 live bats mint kubeconfigs) | mitigate | `setup_file` writes to `${TMPDIR}/karyon-tenants/bats-*` tmpdir; existing Phase 7 `.gitignore` glob `karyon-tenants/` covers; `.gitleaks.toml` kubeconfig-bearer-token rule still active. Inherited from Phase 7 D-14 + Phase 10 D-10-08. |
| T-13-04 | Elevation of Privilege | tenant-workload-editor ClusterRole too broad | mitigate | rbac-01-static-tenant-workload-editor.bats grep-asserts 4 explicit denials (secrets CUD, rolebindings, resourcequotas, limitranges); Pitfall 13-P3 anti-pattern guard for `aggregate-to-`; rbac-05-live grant matrix tests denial verbs return Forbidden via kubectl auth can-i |
| T-13-05 | Denial of Service | Flux SA owner pattern regression (RBAC-03) | mitigate | rbac-07-live-flux-sa-preservation.bats asserts existing owners[] entries (flux-reconciler, gitops-reconciler) preserved verbatim; delivery-03-live-additive-only.bats asserts no removals from spec.owners[] in Tenant CR diff against Phase 12 base SHA |
| T-13-06 | Elevation of Privilege | GTR misconfiguration propagates outside intended tenants | mitigate | seed-01-static asserts `matchLabels: {}` (canonical k8s match-all per A1); seed-02-live verifies propagation to exactly the 4 expected tenant namespaces; seed-04-live-shape asserts no extra resources (no Deployment / ReplicaSet / extra ConfigMap — would indicate scope creep) |
| T-13-07 | Denial of Service / Scope Creep | P31 isolation regression | mitigate | delivery-02-static-paths.bats enforces Phase 13 NEW literals NOT in Taskfile.yml + v0.18 default-path scripts; inherits Phase 7 poc-isolation-01-static.bats |
</threat_model>

<verification>
- All 19 bats files RED for non-implemented targets (static fails because target file doesn't exist; live fails or skips because target script/resource doesn't exist)
- `bats tests/bats/poc-isolation-01-static.bats` (Phase 7 inheritance) GREEN
- `bats tests/bats/capsule-install-09-live-adr-004.bats` (Phase 8 inheritance) GREEN
- `bats tests/bats/tenants-04-static-dependson.bats` (Phase 9 inheritance) GREEN
- `bats tests/bats/push-gate-02-static-capsuleconfig.bats` + `bats tests/bats/push-gate-03-static-tenant-ns-label.bats` (Phase 11 inheritance) GREEN
- `bats tests/bats/proxy-04-live-issue.bats` (Phase 10 inheritance — PROXY-01..03 regression) GREEN
- `/tmp/13-00-preflight.log` captured + cited in SUMMARY
- `/tmp/13-00-wave0-red.log` captured + cited in SUMMARY
- VALIDATION.md frontmatter flipped: `wave_0_complete: true`, `nyquist_compliant: true` (executor edits .planning/phases/.../13-VALIDATION.md as part of this commit OR Plan 13-04 verifier flips — planner discretion; recommended in Plan 13-00 since the wave 0 is complete here)
</verification>

<success_criteria>
- 19 NEW bats files committed to `tests/bats/`
- Preflight log captured at `/tmp/13-00-preflight.log` and inheritance baseline verified
- Wave 0 RED log captured at `/tmp/13-00-wave0-red.log`
- Inherited bats GREEN
- `13-VALIDATION.md` frontmatter flipped (or queued for Plan 13-04 to flip)
- Git commit message: `test(13-00): wave 0 RED bats scaffold (19 files; RBAC + SEED + DELIVERY contracts locked pre-implementation)`
</success_criteria>

<output>
After completion, create `.planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-00-SUMMARY.md` documenting:
- 19 bats files landed (file list + REQ coverage)
- Preflight inheritance verified (Phase 11 D-11-02 userGroups + Phase 10 D-10-06 capsule-proxy Secret keys)
- Wave 0 RED counts per category (static / live)
- Inherited bats GREEN-confirmed
- Any deviations from PATTERNS.md per-file conventions (planner discretion notes)
- Cross-references: D-13-15 (Wave 0 contract); Phase 9 D-09-08 (Wave 0 pattern inheritance)
</output>
</content>
</invoke>