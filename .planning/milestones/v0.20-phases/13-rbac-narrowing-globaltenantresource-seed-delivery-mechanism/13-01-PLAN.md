---
phase: 13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism
plan: 01
type: execute
wave: 1
depends_on:
  - "13-00"
files_modified:
  - pocs/capsule/spoke/rbac/tenant-workload-editor.yaml
  - pocs/capsule/spoke/rbac/kustomization.yaml
  - pocs/capsule/spoke/tenants/alpha/human-owner-sa.yaml
  - pocs/capsule/spoke/tenants/bravo/human-owner-sa.yaml
  - pocs/capsule/spoke/tenants/alpha/kustomization.yaml
  - pocs/capsule/spoke/tenants/bravo/kustomization.yaml
  - pocs/capsule/spoke/tenant-crs/alpha.yaml
  - pocs/capsule/spoke/tenant-crs/bravo.yaml
  - scripts/poc/capsule/issue-tenant-kubeconfig.sh
autonomous: true
requirements:
  - RBAC-01
  - RBAC-02
  - RBAC-03
tags:
  - capsule
  - rbac
  - tenant-workload-editor

must_haves:
  truths:
    - "A `tenant-workload-editor` ClusterRole exists with the D-13-04 grant matrix (positive verbs + 4 denial categories absent)"
    - "Per-tenant `human-tenant-owner` ServiceAccounts exist in tenant-alpha + tenant-bravo namespaces"
    - "Each tenant CR has the human-tenant-owner SA listed in spec.owners[] with explicit `clusterRoles: [tenant-workload-editor]` (Pitfall 13-P8 — non-empty array, not `[]`)"
    - "Existing Flux + GitOps SA owner entries preserved character-for-character (RBAC-03 regression gate)"
    - "Each tenant CR has `docker.io` appended to containerRegistries.allowed (additive — preserves Phase 9 N7 fixture)"
    - "Tenant-owner kubeconfig minted with `bash issue-tenant-kubeconfig.sh <t> human-tenant-owner` results in `kubectl auth can-i '*' '*'` → no"
    - "kubectl create secret returns Forbidden under the human-tenant-owner kubeconfig (RBAC-01 + D-13-01 boundary)"
  artifacts:
    - path: "pocs/capsule/spoke/rbac/tenant-workload-editor.yaml"
      provides: "D-13-04 ClusterRole — narrower than upstream k8s edit"
      contains: "tenant-workload-editor"
      min_lines: 50
    - path: "pocs/capsule/spoke/tenants/alpha/human-owner-sa.yaml"
      provides: "Per-tenant human SA (RBAC-01 binding target via Capsule auto-injection)"
      contains: "human-tenant-owner"
    - path: "pocs/capsule/spoke/tenants/bravo/human-owner-sa.yaml"
      provides: "Bravo mirror"
      contains: "human-tenant-owner"
    - path: "pocs/capsule/spoke/tenant-crs/alpha.yaml"
      provides: "EDITED — additive spec.owners[] (human-tenant-owner with explicit clusterRoles) + containerRegistries.allowed += docker.io"
      contains: "human-tenant-owner"
    - path: "pocs/capsule/spoke/tenant-crs/bravo.yaml"
      provides: "Bravo mirror"
      contains: "human-tenant-owner"
    - path: "scripts/poc/capsule/issue-tenant-kubeconfig.sh"
      provides: "EDITED — usage hint + error message expanded to enumerate `human-tenant-owner` alongside `gitops-reconciler`"
      contains: "human-tenant-owner"
  key_links:
    - from: "pocs/capsule/spoke/tenant-crs/alpha.yaml"
      to: "pocs/capsule/spoke/rbac/tenant-workload-editor.yaml"
      via: "Capsule auto-injected RoleBinding per (owner, clusterRole) pair per tenant namespace"
      pattern: "spec.owners[].clusterRoles\\[\\] contains 'tenant-workload-editor'"
    - from: "pocs/capsule/spoke/tenants/alpha/human-owner-sa.yaml"
      to: "pocs/capsule/spoke/tenant-crs/alpha.yaml"
      via: "spec.owners[].name = system:serviceaccount:tenant-alpha:human-tenant-owner combined-name format"
      pattern: "system:serviceaccount:tenant-alpha:human-tenant-owner"
    - from: "scripts/poc/capsule/issue-tenant-kubeconfig.sh"
      to: "human-tenant-owner SA + tenant-workload-editor ClusterRole"
      via: "TokenRequest mints SA token bound to ClusterRole via Capsule-injected RoleBinding"
      pattern: "bash scripts/poc/capsule/issue-tenant-kubeconfig.sh alpha human-tenant-owner"
---

<objective>
Plan 13-01 — Land Tier 3 (tenant-developer) RBAC narrowing artifacts.

Purpose: Implement the D-13-04 `tenant-workload-editor` ClusterRole (narrower than upstream k8s `edit`), the per-tenant `human-tenant-owner` ServiceAccount in tenant-alpha + tenant-bravo, and the additive Tenant CR spec.owners[] entries that bind the human SAs to the new ClusterRole via Capsule's auto-injected RoleBindings. Plus a minor edit to `issue-tenant-kubeconfig.sh` to expand the `<owner>` enumeration hint.

Output: 6 NEW YAML files + 3 EDITED YAML files + 1 EDITED bash script. Wave 0 RED bats `rbac-01-static`, `rbac-03-static`, `rbac-04-static`, `rbac-05-live`, `rbac-06-live`, `rbac-07-live` flip to GREEN. RBAC-01 + RBAC-02 + RBAC-03 satisfied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-CONTEXT.md
@.planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-RESEARCH.md
@.planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-PATTERNS.md
@.planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-00-SUMMARY.md

# Analogs to mirror (REQUIRED reading)
@pocs/capsule/spoke/rbac/platform-owner.yaml
@pocs/capsule/spoke/rbac/helm-controller-sa.yaml
@pocs/capsule/spoke/rbac/kustomization.yaml
@pocs/capsule/spoke/tenants/alpha/sa.yaml
@pocs/capsule/spoke/tenants/alpha/kustomization.yaml
@pocs/capsule/spoke/tenant-crs/alpha.yaml
@pocs/capsule/spoke/tenant-crs/bravo.yaml
@scripts/poc/capsule/issue-tenant-kubeconfig.sh

<interfaces>
<!-- Key types/literals from PATTERNS.md + RESEARCH.md -->

## D-13-04 ClusterRole grant matrix (exhaustive — bats rbac-01-static enforces):
```yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: tenant-workload-editor
  labels:
    karyon.io/poc: capsule
    karyon.io/role: capsule-tenant-workload-editor
rules:
  # Workload management
  - apiGroups: [""]
    resources: [pods]
    verbs: [get, list, watch, create, update, patch, delete, deletecollection]
  - apiGroups: [""]
    resources: [pods/log]
    verbs: [get, list, watch]
  - apiGroups: [""]
    resources: [pods/exec, pods/portforward]
    verbs: [create]
  # Workload lifecycle
  - apiGroups: [apps]
    resources: [deployments, replicasets, statefulsets, daemonsets]
    verbs: [get, list, watch, create, update, patch, delete, deletecollection]
  - apiGroups: [batch]
    resources: [jobs, cronjobs]
    verbs: [get, list, watch, create, update, patch, delete, deletecollection]
  # Service exposure
  - apiGroups: [""]
    resources: [services, endpoints]
    verbs: [get, list, watch, create, update, patch, delete, deletecollection]
  - apiGroups: [discovery.k8s.io]
    resources: [endpointslices]
    verbs: [get, list, watch, create, update, patch, delete, deletecollection]
  # Workload configuration
  - apiGroups: [""]
    resources: [configmaps]
    verbs: [get, list, watch, create, update, patch, delete, deletecollection]
  # Read-only positive grants
  - apiGroups: [""]
    resources: [persistentvolumeclaims]
    verbs: [get, list, watch]
  - apiGroups: [""]
    resources: [events]
    verbs: [get, list, watch]
  - apiGroups: [events.k8s.io]
    resources: [events]
    verbs: [get, list, watch]
  # Secrets — D-13-01 read-only ONLY
  - apiGroups: [""]
    resources: [secrets]
    verbs: [get, list, watch]
# NO clusterRole aggregation labels (Pitfall 13-P3)
```

## D-13-05 Per-tenant human-tenant-owner SA shape (from sa.yaml analog):
```yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: human-tenant-owner
  namespace: tenant-alpha   # (or tenant-bravo for bravo mirror)
  labels:
    karyon.io/poc: capsule
    karyon.io/role: capsule-tenant-workload-editor
```

## Tenant CR spec.owners[] additive shape (per D-13-05 + RESEARCH Pattern 2):
```yaml
# EXISTING entries preserved verbatim — DO NOT REORDER OR REMOVE
- kind: ServiceAccount
  name: system:serviceaccount:flux-system:flux-reconciler
- kind: ServiceAccount
  name: system:serviceaccount:tenant-alpha:gitops-reconciler
- kind: User
  name: alpha
# === NEW (Plan 13-01 — additive only) ===
- kind: ServiceAccount
  name: system:serviceaccount:tenant-alpha:human-tenant-owner
  clusterRoles:
    - tenant-workload-editor    # EXPLICIT override per D-13-05; non-empty (Pitfall 13-P8)
```
Note: Plan 13-02 will add additional entries (Group capsule-platform-owners + SA platform-owner@capsule-system). Do NOT add those yet — they are Plan 13-02's responsibility.

## Tenant CR containerRegistries.allowed additive (per RESEARCH finding #6 + Pitfall 13-P1):
```yaml
containerRegistries:
  allowed:
    - registry.example.io     # EXISTING (Phase 9 N7 fixture — do NOT remove)
    - docker.io               # NEW (additive; required for FQCI nginx Pod propagation by GTR)
```

## scripts/poc/capsule/issue-tenant-kubeconfig.sh edit target (analog lines 167-178):
The ONLY edit is the help-text hint that lists available SA values. Find the existing `fail` block (around line 175) that says:
```
or pass a known SA name (currently only 'gitops-reconciler').
```
Replace with:
```
or pass a known SA name (currently 'gitops-reconciler' or 'human-tenant-owner').
```
Also update any Usage block (around lines 67-69) that documents the `<owner>` arg to mention both SA names. DO NOT modify any other section. The SA-exists check logic at line 167 already works for ANY owner name passed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Land tenant-workload-editor ClusterRole + per-tenant human-owner SAs + kustomization edits</name>
  <files>
    pocs/capsule/spoke/rbac/tenant-workload-editor.yaml,
    pocs/capsule/spoke/rbac/kustomization.yaml,
    pocs/capsule/spoke/tenants/alpha/human-owner-sa.yaml,
    pocs/capsule/spoke/tenants/bravo/human-owner-sa.yaml,
    pocs/capsule/spoke/tenants/alpha/kustomization.yaml,
    pocs/capsule/spoke/tenants/bravo/kustomization.yaml
  </files>
  <read_first>
    - pocs/capsule/spoke/rbac/platform-owner.yaml (ClusterRole + ClusterRoleBinding head-comment + label pattern — analog for Tier 2; mirror the head-comment narrative shape for Tier 3 ClusterRole)
    - pocs/capsule/spoke/rbac/helm-controller-sa.yaml (analog for SA-in-capsule-system shape; even though Tier 3 SA lives in tenant-<t> ns, the file shape is identical — single SA doc, labels, namespace)
    - pocs/capsule/spoke/rbac/kustomization.yaml (current resources list — append-after pattern with `# NEW (Phase 13)` annotation)
    - pocs/capsule/spoke/tenants/alpha/sa.yaml (per-tenant SA file analog — head comment cites TEN-01 / D-09-02; mirror cites RBAC-01 / RBAC-02 / D-13-05)
    - pocs/capsule/spoke/tenants/alpha/kustomization.yaml (sortOptions.order: fifo preservation; append human-owner-sa.yaml after sa.yaml)
    - .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-RESEARCH.md "Standard Stack" + "Code Examples" §"Tenant CR additive owner edit via yq" + "k8s 1.34 upstream `edit` ClusterRole" reference
  </read_first>
  <behavior>
    - Test 1: `bats tests/bats/rbac-01-static-tenant-workload-editor.bats` → GREEN. ClusterRole file exists with `kind: ClusterRole`, `metadata.name: tenant-workload-editor`, positive verb matrix present, 4 explicit denials absent, no `aggregate-to-` labels.
    - Test 2: `bats tests/bats/rbac-04-static-human-owner-sa.bats` → GREEN. Both `human-owner-sa.yaml` files exist with correct ns + name; kustomization.yaml resources list contains `human-owner-sa.yaml`.
    - Test 3: `bats tests/bats/poc-isolation-01-static.bats` (Phase 7 inheritance) → still GREEN (no spoke-capsule mentions added to v0.18 default-path scripts).
    - Test 4: Static rbac/kustomization.yaml ordering preserved — namespace.yaml first, tenant-workload-editor.yaml appended.
  </behavior>
  <action>
    Create the ClusterRole + 2 SA files + edit the 3 kustomization.yaml files.

    **1. CREATE `pocs/capsule/spoke/rbac/tenant-workload-editor.yaml`** — mirror the platform-owner.yaml head comment shape but adapted to Tier 3:

    ```yaml
    # pocs/capsule/spoke/rbac/tenant-workload-editor.yaml
    # Capsule tenant-workload-editor ClusterRole (Phase 13 — Tier 3 RBAC narrowing).
    #
    # REQ: RBAC-01 (v0.20 — narrower than upstream k8s `edit`)
    # Decision: D-13-04 (positive grants + 4 explicit denials)
    # Threat: T-13-04 (Elevation of Privilege via overbroad tenant role)
    #
    # Intent: replace upstream k8s `admin` / `edit` for human-exercised tenant
    # owners. Narrower than `edit` by removing Secret CUD (read-only per D-13-01)
    # + RoleBindings + ResourceQuotas + LimitRanges.
    #
    # The Flux SA owner pattern (RBAC-03) is preserved on alpha + bravo with
    # implicit-default `clusterRoles: [admin, capsule-namespace-deleter]`; only
    # the new `human-tenant-owner` SA per tenant binds to THIS ClusterRole via
    # Capsule's auto-injected RoleBinding `capsule-<tenant>-<index>-tenant-workload-editor`.
    #
    # Anti-pattern (Pitfall 13-P3): DO NOT add any rbac.authorization.k8s.io/
    # aggregate-to-* label — would silently inherit upstream `edit` Secret CUD.
    #
    # Local POC usage:
    #   kubectl --context=k3d-spoke-capsule auth can-i create pods \
    #     -n tenant-alpha \
    #     --as=system:serviceaccount:tenant-alpha:human-tenant-owner
    #
    # Production translation: bind via tenant.spec.owners[] with explicit
    # clusterRoles: [tenant-workload-editor]. Capsule v0.12.4 auto-injects
    # one RoleBinding per (owner, clusterRole, namespace) tuple.
    apiVersion: rbac.authorization.k8s.io/v1
    kind: ClusterRole
    metadata:
      name: tenant-workload-editor
      labels:
        karyon.io/poc: capsule
        karyon.io/role: capsule-tenant-workload-editor
    rules:
      # Workload management — full CRUD on pods + log/exec/portforward subresources
      - apiGroups: [""]
        resources: ["pods"]
        verbs: ["get", "list", "watch", "create", "update", "patch", "delete", "deletecollection"]
      - apiGroups: [""]
        resources: ["pods/log"]
        verbs: ["get", "list", "watch"]
      - apiGroups: [""]
        resources: ["pods/exec", "pods/portforward"]
        verbs: ["create"]
      # Workload lifecycle — apps + batch
      - apiGroups: ["apps"]
        resources: ["deployments", "replicasets", "statefulsets", "daemonsets"]
        verbs: ["get", "list", "watch", "create", "update", "patch", "delete", "deletecollection"]
      - apiGroups: ["batch"]
        resources: ["jobs", "cronjobs"]
        verbs: ["get", "list", "watch", "create", "update", "patch", "delete", "deletecollection"]
      # Service exposure
      - apiGroups: [""]
        resources: ["services", "endpoints"]
        verbs: ["get", "list", "watch", "create", "update", "patch", "delete", "deletecollection"]
      - apiGroups: ["discovery.k8s.io"]
        resources: ["endpointslices"]
        verbs: ["get", "list", "watch", "create", "update", "patch", "delete", "deletecollection"]
      # Workload configuration
      - apiGroups: [""]
        resources: ["configmaps"]
        verbs: ["get", "list", "watch", "create", "update", "patch", "delete", "deletecollection"]
      # Storage references — read-only (PVC creation is platform-team concern)
      - apiGroups: [""]
        resources: ["persistentvolumeclaims"]
        verbs: ["get", "list", "watch"]
      # Debugging — events read-only
      - apiGroups: [""]
        resources: ["events"]
        verbs: ["get", "list", "watch"]
      - apiGroups: ["events.k8s.io"]
        resources: ["events"]
        verbs: ["get", "list", "watch"]
      # Secrets — D-13-01 read-only ONLY (NO create/update/patch/delete)
      - apiGroups: [""]
        resources: ["secrets"]
        verbs: ["get", "list", "watch"]
    # Implicit denials (not in rules[]):
    #   - rolebindings, roles (rbac.authorization.k8s.io)  -- RBAC-01 explicit denial #2
    #   - resourcequotas (""), limitranges ("")            -- RBAC-01 explicit denials #3 and #4
    #   - nodes, csr, clusterrolebinding, clusterrole      -- Tier-1/Tier-2 surface
    #   - tenants.capsule.clastix.io, customresourcedefinitions  -- Tier-2 surface
    #   - networkpolicies.networking.k8s.io                -- defense in depth
    ```

    **2. CREATE `pocs/capsule/spoke/tenants/alpha/human-owner-sa.yaml`** — mirror sa.yaml structure:

    ```yaml
    # pocs/capsule/spoke/tenants/alpha/human-owner-sa.yaml
    # Per-tenant ServiceAccount registered as Tenant owner via the combined name
    # format `system:serviceaccount:tenant-alpha:human-tenant-owner` in
    # tenant-crs/alpha.yaml spec.owners[].
    #
    # REQ: RBAC-01 / RBAC-02 (human-exercised tenant-owner identity)
    # Decision: D-13-05 (binds to tenant-workload-editor via explicit clusterRoles in owner entry)
    # Threat: T-13-04 mitigation (narrower than edit; bound via Capsule auto-injection)
    #
    # Distinct from sibling `sa.yaml` (gitops-reconciler — Flux automation owner with
    # implicit-default clusterRoles: [admin, capsule-namespace-deleter]). human-tenant-owner
    # is the operator-bound SA used to mint tenant-owner kubeconfigs via:
    #   bash scripts/poc/capsule/issue-tenant-kubeconfig.sh alpha human-tenant-owner
    apiVersion: v1
    kind: ServiceAccount
    metadata:
      name: human-tenant-owner
      namespace: tenant-alpha
      labels:
        karyon.io/poc: capsule
        karyon.io/role: capsule-tenant-workload-editor
    ```

    **3. CREATE `pocs/capsule/spoke/tenants/bravo/human-owner-sa.yaml`** — same content with `s/tenant-alpha/tenant-bravo/g` (and `alpha → bravo` in the head comment).

    **4. EDIT `pocs/capsule/spoke/rbac/kustomization.yaml`** — append `tenant-workload-editor.yaml` to `resources:` list. Preserve `namespace.yaml` FIRST (codex §2 HIGH #1 — Phase 11 invariant) and existing head comment. Result (target shape):

    ```yaml
    # pocs/capsule/spoke/rbac/kustomization.yaml
    # (existing head comment preserved verbatim — DO NOT MODIFY)
    apiVersion: kustomize.config.k8s.io/v1beta1
    kind: Kustomization
    resources:
      - namespace.yaml
      - helm-controller-sa.yaml
      - platform-owner.yaml
      # NEW (Phase 13 D-13-04): Tier 3 RBAC narrowing — narrower than upstream k8s edit
      - tenant-workload-editor.yaml
    ```
    Plan 13-02 will additionally append `platform-owner-sa.yaml` here. This Plan 13-01 only adds `tenant-workload-editor.yaml`.

    **5. EDIT `pocs/capsule/spoke/tenants/alpha/kustomization.yaml`** — append `human-owner-sa.yaml` after `sa.yaml`:

    ```yaml
    apiVersion: kustomize.config.k8s.io/v1beta1
    kind: Kustomization
    sortOptions:
      order: fifo
    resources:
      - namespace.yaml
      - sa.yaml
      # NEW (Phase 13 D-13-05): human-tenant-owner SA for RBAC-01 binding target
      - human-owner-sa.yaml
    ```

    **6. EDIT `pocs/capsule/spoke/tenants/bravo/kustomization.yaml`** — same pattern (mirror of alpha).

    POST-LANDING verification:
    ```bash
    # The new ClusterRole + SAs land via Flux reconcile (poc-capsule-spoke-rbac for ClusterRole; poc-capsule-spoke for tenant SAs).
    # Trigger a force-reconcile on the rbac K and the spoke K, then verify resources exist:
    flux --context=k3d-hub-flux reconcile kustomization poc-capsule-spoke-rbac --with-source
    flux --context=k3d-hub-flux reconcile kustomization poc-capsule-spoke --with-source

    # Wait briefly + verify:
    kubectl --context=k3d-spoke-capsule get clusterrole tenant-workload-editor -o name
    kubectl --context=k3d-spoke-capsule -n tenant-alpha get sa human-tenant-owner -o name
    kubectl --context=k3d-spoke-capsule -n tenant-bravo get sa human-tenant-owner -o name

    # Run static bats:
    bats tests/bats/rbac-01-static-tenant-workload-editor.bats tests/bats/rbac-04-static-human-owner-sa.bats -x
    ```
    If any resource is missing OR static bats RED, FAIL FAST.
  </action>
  <verify>
    <automated>bats tests/bats/rbac-01-static-tenant-workload-editor.bats tests/bats/rbac-04-static-human-owner-sa.bats tests/bats/poc-isolation-01-static.bats -x</automated>
  </verify>
  <acceptance_criteria>
    - `pocs/capsule/spoke/rbac/tenant-workload-editor.yaml` exists, contains `kind: ClusterRole`, `name: tenant-workload-editor`, all D-13-04 positive verb groupings, NO `secrets` `create`/`update`/`patch`/`delete` verbs, NO `aggregate-to-` label
    - `pocs/capsule/spoke/tenants/{alpha,bravo}/human-owner-sa.yaml` both exist, contain `kind: ServiceAccount`, `name: human-tenant-owner`, `namespace: tenant-<tenant>`
    - `pocs/capsule/spoke/rbac/kustomization.yaml` contains `tenant-workload-editor.yaml` in `resources:` list (after platform-owner.yaml)
    - `pocs/capsule/spoke/tenants/{alpha,bravo}/kustomization.yaml` both contain `human-owner-sa.yaml` in `resources:` list (after `sa.yaml`)
    - `bats tests/bats/rbac-01-static-tenant-workload-editor.bats` exits 0
    - `bats tests/bats/rbac-04-static-human-owner-sa.bats` exits 0
    - `bats tests/bats/poc-isolation-01-static.bats` exits 0 (no v0.18 regression)
    - kubectl-live verification: `kubectl get clusterrole tenant-workload-editor` exits 0; both `kubectl -n tenant-{alpha,bravo} get sa human-tenant-owner` exit 0
  </acceptance_criteria>
  <done>Tier 3 RBAC artifacts landed. ClusterRole + 2 human SAs reconciled on spoke-capsule. Static bats GREEN. Phase 7 P31 regression gate still GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add human-tenant-owner SA entries to alpha + bravo Tenant CR spec.owners[] + add docker.io to containerRegistries.allowed + edit issue-tenant-kubeconfig.sh hint</name>
  <files>
    pocs/capsule/spoke/tenant-crs/alpha.yaml,
    pocs/capsule/spoke/tenant-crs/bravo.yaml,
    scripts/poc/capsule/issue-tenant-kubeconfig.sh
  </files>
  <read_first>
    - pocs/capsule/spoke/tenant-crs/alpha.yaml (current spec.owners[] entries — DO NOT REORDER OR REMOVE: flux-reconciler SA, gitops-reconciler SA, User alpha; current containerRegistries.allowed: [registry.example.io])
    - pocs/capsule/spoke/tenant-crs/bravo.yaml (mirror of alpha)
    - scripts/poc/capsule/issue-tenant-kubeconfig.sh (lines 167-178 SA-exists check; line 174-175 fail hint with available SAs; usage block lines 67-69 documenting `<owner>` arg)
    - .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-RESEARCH.md "Pitfall 13-P1" + "Pitfall 13-P8" + "Code Examples — Tenant CR additive owner edit via yq"
    - tests/bats/rbac-03-static-tenant-co-ownership.bats (from Plan 13-00 — drives the assertion shape: existing owners[] preserved + new SA entry with explicit clusterRoles + containerRegistries.allowed contains docker.io)
  </read_first>
  <behavior>
    - Test 1: Static `bats tests/bats/rbac-03-static-tenant-co-ownership.bats` will turn PARTIALLY GREEN for the Phase 13-01 entries it asserts (the human-tenant-owner SA + containerRegistries.allowed += docker.io). Plan 13-02 turns the rest GREEN (Group + platform-owner SA entries).
      NOTE: rbac-03-static asserts ALL co-ownership entries land. After Plan 13-01 it will still be RED because Group + platform-owner-SA entries are missing. Plan 13-01 commits should NOT assert this static GREEN — only `delivery-03-live-additive-only.bats` and the existing-entry-preservation portion are testable here.
    - Test 2: Live `bats tests/bats/rbac-07-live-flux-sa-preservation.bats` → GREEN. Flux SA owner pattern preserved verbatim on alpha + bravo.
    - Test 3: Live `bats tests/bats/rbac-05-live-tenant-owner-grant-matrix.bats` → GREEN. After Flux reconciles, Capsule auto-injects a `capsule-<t>-<index>-tenant-workload-editor` RoleBinding per tenant namespace; minted human-tenant-owner kubeconfig satisfies the can-i positive + denial matrix.
    - Test 4: Live `bats tests/bats/rbac-06-live-tenant-kubeconfig.bats` → GREEN. Minted human-tenant-owner kubeconfig: auth can-i '*' '*' returns no; get pods works; create secret returns Forbidden.
    - Test 5: Phase 9 N7 negative-RBAC fixture (registry rejection of a DIFFERENT arbitrary registry) is NOT regressed — `bats tests/bats/negative-rbac-*.bats` continues to pass.
  </behavior>
  <action>
    **1. EDIT `pocs/capsule/spoke/tenant-crs/alpha.yaml`** — APPEND to `spec.owners[]` (after existing User alpha entry) AND APPEND `docker.io` to `containerRegistries.allowed[]`. Use the additive-only diff shape from RESEARCH Pattern 2:

    Before edit (current state — existing lines 24-38 owners[]; lines 72-74 containerRegistries):
    ```yaml
    spec:
      forceTenantPrefix: false
      owners:
        - kind: ServiceAccount
          name: system:serviceaccount:flux-system:flux-reconciler
        - kind: ServiceAccount
          name: system:serviceaccount:tenant-alpha:gitops-reconciler
        - kind: User
          name: alpha
      # ... (resourceQuotas, limitRanges, namespaceOptions intact) ...
      containerRegistries:
        allowed:
          - registry.example.io
    ```

    After edit (additive only — append below User alpha + append below registry.example.io):
    ```yaml
    spec:
      forceTenantPrefix: false
      owners:
        # === Existing entries preserved verbatim (RBAC-03 regression gate) ===
        - kind: ServiceAccount
          name: system:serviceaccount:flux-system:flux-reconciler
        - kind: ServiceAccount
          name: system:serviceaccount:tenant-alpha:gitops-reconciler
        - kind: User
          name: alpha
        # === NEW (Phase 13 D-13-05): Tier 3 human-exercised tenant owner ===
        # Binds via Capsule auto-injected RoleBinding capsule-alpha-<index>-tenant-workload-editor.
        # Pitfall 13-P8: clusterRoles MUST be non-empty (NEVER `clusterRoles: []` — that
        # removes all bindings; OMIT field for Capsule default, OR list explicit roles).
        - kind: ServiceAccount
          name: system:serviceaccount:tenant-alpha:human-tenant-owner
          clusterRoles:
            - tenant-workload-editor
      # ... (resourceQuotas, limitRanges, namespaceOptions intact) ...
      containerRegistries:
        allowed:
          - registry.example.io
          # NEW (Phase 13 D-13-12 + Pitfall 13-P1): required for FQCI nginx Pod
          # propagation by GTR. Phase 9 N7 negative-RBAC fixture continues to assert
          # rejection of a different registry — additive.
          - docker.io
    ```

    Implementation: use `yq -i` to add the new owner entry append + `docker.io` to allowed list:

    ```bash
    yq -i '.spec.owners += [{
      "kind": "ServiceAccount",
      "name": "system:serviceaccount:tenant-alpha:human-tenant-owner",
      "clusterRoles": ["tenant-workload-editor"]
    }]' pocs/capsule/spoke/tenant-crs/alpha.yaml

    yq -i '.spec.containerRegistries.allowed += ["docker.io"]' pocs/capsule/spoke/tenant-crs/alpha.yaml
    ```

    After `yq` edit, MANUALLY inspect the result with `diff` against pre-edit content to verify:
    - No existing line removed
    - Only 4-5 NEW lines added in owners[] (the SA entry block + comment header)
    - Exactly 1 new line added in containerRegistries.allowed[]
    - Comment headers above the new entries can be inserted via `sed` after `yq` finishes (yq normalizes YAML and may collapse comments — verify and re-add the explanatory comments via `sed -i`).

    **Plan 13-01 explicitly leaves OUT** the `Group capsule-platform-owners` and `SA platform-owner@capsule-system` entries — those are Plan 13-02's responsibility. Adding them here would either:
    1. Force Plan 13-02 to deal with them as already-landed (preventing the additive-co-ownership separation), OR
    2. Cause Plan 13-02's static bats `rbac-03-static-tenant-co-ownership.bats` to assert state that Plan 13-01 supplied prematurely.

    **2. EDIT `pocs/capsule/spoke/tenant-crs/bravo.yaml`** — IDENTICAL pattern with `s/tenant-alpha/tenant-bravo/g`. Replace `system:serviceaccount:tenant-alpha:human-tenant-owner` with `system:serviceaccount:tenant-bravo:human-tenant-owner` and the comment block `capsule-alpha-<index>` with `capsule-bravo-<index>`.

    **3. EDIT `scripts/poc/capsule/issue-tenant-kubeconfig.sh`** — find the existing error hint around line 175 (in the SA-exists check):

    Before (CURRENT — verbatim line 175):
    ```bash
         Hint: run /gsd-execute-phase 9 first if Phase 9 has not landed yet,
               or pass a known SA name (currently only 'gitops-reconciler').
    ```

    After (expanded enum):
    ```bash
         Hint: run /gsd-execute-phase 9 first if Phase 9 has not landed yet,
               or pass a known SA name (currently 'gitops-reconciler' or
               'human-tenant-owner' — Phase 13 RBAC-02 default).
    ```

    Also find the Usage block (around lines 67-69 — search for the `--write-to` flag documentation and locate the `<owner>` arg description). The Usage line for `<owner>` likely says something like:
    ```bash
    #   <owner>     Tenant owner SA name (e.g., gitops-reconciler)
    ```
    Replace with:
    ```bash
    #   <owner>     Tenant owner SA name; one of:
    #               - gitops-reconciler  (Flux automation; Phase 10 default)
    #               - human-tenant-owner (RBAC-02 human-exercised; Phase 13 default)
    ```

    DO NOT modify ANY other section. The script's `set -euo pipefail`, stdout/stderr contract, REPO_ROOT pinning, preflight-lib source, helper shadows, TokenRequest mint, TLS read, kubeconfig heredoc — ALL preserved verbatim. The SA-exists check at line 167 already works for any SA name in the tenant namespace.

    POST-LANDING verification:
    ```bash
    # Force Flux reconcile of tenant-crs:
    flux --context=k3d-hub-flux reconcile kustomization poc-capsule-spoke-tenants --with-source

    # Capsule auto-injects RoleBinding for the new human-tenant-owner owner entry. Verify:
    kubectl --context=k3d-spoke-capsule -n tenant-alpha get rolebinding \
      -l app.kubernetes.io/managed-by=capsule \
      | grep -E 'capsule-alpha-[0-9]+-tenant-workload-editor'
    # Expect exactly 1 binding matching the pattern.

    # Mint a kubeconfig for alpha human-tenant-owner and run the can-i matrix:
    bash scripts/poc/capsule/issue-tenant-kubeconfig.sh alpha human-tenant-owner \
      --duration=1h --write-to "$(mktemp -d)/alpha-human.kubeconfig" 2>&1 | tail -5

    KUBECONFIG=/tmp/karyon-tenants/alpha-human.kubeconfig kubectl auth can-i '*' '*'  # → no
    KUBECONFIG=/tmp/karyon-tenants/alpha-human.kubeconfig kubectl auth can-i create pods -n tenant-alpha  # → yes
    KUBECONFIG=/tmp/karyon-tenants/alpha-human.kubeconfig kubectl auth can-i create secrets -n tenant-alpha  # → no

    # Run live bats:
    bats tests/bats/rbac-05-live-tenant-owner-grant-matrix.bats \
         tests/bats/rbac-06-live-tenant-kubeconfig.bats \
         tests/bats/rbac-07-live-flux-sa-preservation.bats \
         tests/bats/proxy-04-live-issue.bats -x
    ```
    Verify all green. `proxy-04-live-issue.bats` (Phase 10 inheritance) MUST stay green — the issue-tenant-kubeconfig.sh edit is hint-only.
  </action>
  <verify>
    <automated>bats tests/bats/rbac-05-live-tenant-owner-grant-matrix.bats tests/bats/rbac-06-live-tenant-kubeconfig.bats tests/bats/rbac-07-live-flux-sa-preservation.bats tests/bats/proxy-04-live-issue.bats -x</automated>
  </verify>
  <acceptance_criteria>
    - `pocs/capsule/spoke/tenant-crs/alpha.yaml` contains `system:serviceaccount:tenant-alpha:human-tenant-owner` AND `clusterRoles:` block with `tenant-workload-editor`
    - `pocs/capsule/spoke/tenant-crs/alpha.yaml` `containerRegistries.allowed` list contains `docker.io` AND `registry.example.io` (both present; additive)
    - `pocs/capsule/spoke/tenant-crs/bravo.yaml` mirrors alpha with `tenant-bravo` substitutions
    - Existing owners[] entries (flux-reconciler, gitops-reconciler, User alpha/bravo) preserved verbatim — verified via `yq` or `git diff`
    - `scripts/poc/capsule/issue-tenant-kubeconfig.sh` Usage block + error hint now enumerate `human-tenant-owner` alongside `gitops-reconciler`
    - `bats tests/bats/rbac-05-live-tenant-owner-grant-matrix.bats` exits 0
    - `bats tests/bats/rbac-06-live-tenant-kubeconfig.bats` exits 0
    - `bats tests/bats/rbac-07-live-flux-sa-preservation.bats` exits 0 (RBAC-03 regression gate GREEN)
    - `bats tests/bats/proxy-04-live-issue.bats` exits 0 (Phase 10 PROXY-01..03 regression — issue-tenant-kubeconfig.sh hint edit does NOT regress)
    - kubectl-live: a freshly-minted human-tenant-owner kubeconfig returns `no` to `auth can-i '*' '*'` AND returns `no` to `auth can-i create secrets -n tenant-<t>`
  </acceptance_criteria>
  <done>Tier 3 RBAC fully wired. Tenant CR additions reconciled. Capsule auto-injected RoleBinding present. Tenant-owner kubeconfig minted via `human-tenant-owner` SA satisfies RBAC-01 + RBAC-02 + D-13-01 boundaries. RBAC-03 regression gate GREEN. issue-tenant-kubeconfig.sh hint edit non-regressing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git repo → spoke-capsule cluster (via Flux) | Tier 3 ClusterRole + Tenant CR edits land via Flux reconcile; misconfigured ClusterRole grants too much |
| Tenant kubeconfig holder → tenant namespaces (via kubectl) | RBAC fence enforced by apiserver SubjectAccessReview against the Capsule-injected RoleBinding |
| Issue-tenant-kubeconfig.sh user → Tenant kubeconfig file | Hint-only edit; structural shape (TokenRequest, TLS embed, stdout YAML) unchanged from Phase 10 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Elevation of Privilege | tenant-workload-editor ClusterRole too broad | mitigate | D-13-04 grant matrix has 4 explicit denial categories + no aggregate-to-* label (Pitfall 13-P3). Static bats `rbac-01` enforces; live bats `rbac-05` verifies via auth can-i matrix. Defense-in-depth: PVCs are read-only (no PVC create), NetworkPolicy not granted at all. |
| T-13-05 | Denial of Service | Flux SA owner pattern regression (RBAC-03 — existing `gitops-reconciler` owner with implicit-default clusterRoles: [admin, capsule-namespace-deleter] disturbed) | mitigate | Edits are APPEND-ONLY to spec.owners[]; static bats `rbac-03` and `delivery-03-live` assert no removals. Live bats `rbac-07-live` checks `kubectl get tenant ... -o jsonpath='{.spec.owners[?(@.name=="...gitops-reconciler")]}'` returns the entry verbatim. WR-04 pattern distinguishes NotFound from transient errors. |
| T-13-01 | Information Disclosure | Tenant kubeconfig leak in git (RBAC-02 minted kubeconfig) | mitigate | Phase 7 D-14 gitleaks rule + `.gitignore` glob `karyon-tenants/` cover; Phase 10 D-10-08 tmpdir-root realpath check enforced by `issue-tenant-kubeconfig.sh`. Plan 13-01 does NOT alter the leak-defense surface. |

</threat_model>

<verification>
- Static `bats tests/bats/rbac-01-static-tenant-workload-editor.bats` → GREEN
- Static `bats tests/bats/rbac-04-static-human-owner-sa.bats` → GREEN
- Live `bats tests/bats/rbac-05-live-tenant-owner-grant-matrix.bats` → GREEN (positive + denial matrix)
- Live `bats tests/bats/rbac-06-live-tenant-kubeconfig.bats` → GREEN (auth can-i '*' '*' = no; create secret Forbidden)
- Live `bats tests/bats/rbac-07-live-flux-sa-preservation.bats` → GREEN (RBAC-03)
- Live `bats tests/bats/proxy-04-live-issue.bats` → GREEN (Phase 10 PROXY-01..03 regression)
- Static `bats tests/bats/poc-isolation-01-static.bats` → GREEN (Phase 7 P31 regression)
- Capsule auto-injected RoleBinding `capsule-<tenant>-<index>-tenant-workload-editor` present in tenant-alpha + tenant-bravo (live)
- Existing `bats tests/bats/negative-rbac-*.bats` (Phase 9 + 11 negative-RBAC suite) GREEN — confirming N7 fixture not regressed by docker.io addition (N7 tests a DIFFERENT arbitrary registry)
</verification>

<success_criteria>
- 4 NEW files: tenant-workload-editor.yaml, alpha/human-owner-sa.yaml, bravo/human-owner-sa.yaml, plus an additional asymmetric edit to alpha/kustomization.yaml + bravo/kustomization.yaml (already counted as "EDITED")
- 4 EDITED files: rbac/kustomization.yaml (1 line appended), 2× tenants/kustomization.yaml (1 line each), 2× tenant-crs (alpha + bravo — yq-appended), 1× issue-tenant-kubeconfig.sh (Usage + error hint)
- 6 Wave 0 bats GREEN (rbac-01, rbac-04, rbac-05, rbac-06, rbac-07 + poc-isolation-01 + proxy-04 regression)
- RBAC-01 + RBAC-02 + RBAC-03 satisfied
- Git commit: `feat(13-01): land tenant-workload-editor ClusterRole + human-tenant-owner SAs (RBAC-01 + RBAC-02 + RBAC-03 GREEN; D-13-04 D-13-05 D-13-12-allowlist-addition)`
</success_criteria>

<output>
After completion, create `.planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-01-SUMMARY.md` documenting:
- ClusterRole D-13-04 grant matrix (positive verbs + 4 denial categories) with live kubectl auth can-i verification output
- Per-tenant human SA + Tenant CR additive owners[] verbatim diff (existing entries preserved)
- containerRegistries.allowed += docker.io justification (Pitfall 13-P1 + Phase 9 N7 fixture non-regression)
- issue-tenant-kubeconfig.sh hint-only edit (Phase 10 PROXY-01..03 inheritance non-regressing)
- Wave 0 bats GREEN-confirmed; cite tests
- Hand-off note for Plan 13-02: rbac-03-static-tenant-co-ownership.bats remains RED after this plan because Group + platform-owner-SA entries are deferred to Plan 13-02
</output>
</content>
</invoke>