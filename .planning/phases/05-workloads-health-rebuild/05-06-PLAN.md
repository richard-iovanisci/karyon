---
phase: 05-workloads-health-rebuild
plan: 06
type: execute
wave: 5
depends_on: [05-02, 05-03, 05-04, 05-05]
files_modified: []
autonomous: false
requirements: [DEP-01, DEP-02, DEP-03, DEP-04, HEALTH-01, HEALTH-02, HEALTH-03, HEALTH-04, HEALTH-05, HEALTH-06, HEALTH-07, DESTROY-01, DESTROY-02, DESTROY-03, DESTROY-04, DESTROY-05, DESTROY-06]
requirements_addressed: [DEP-01, DEP-02, DEP-03, DEP-04, HEALTH-01, HEALTH-02, HEALTH-03, HEALTH-04, HEALTH-05, HEALTH-06, HEALTH-07, DESTROY-01, DESTROY-02, DESTROY-03, DESTROY-04, DESTROY-05, DESTROY-06]
nyquist_compliant: true
tags: [live, destructive, timing, verification]

must_haves:
  truths:
    - "Full live destructive proof runs only after user confirms target machine, warm CUDA cache, GitHub credentials, and pushed manifests are ready"
    - "task rebuild is invoked exactly ONCE in this plan (Task 2); the live state-checker bats in Task 3 reads /tmp/karyon-rebuild.log and post-rebuild kubeconfig state without re-executing rebuild"
    - "task rebuild exits 0 and prints elapsed seconds <= 1200"
    - "task health-check exits 0 after rebuild"
    - "podinfo Deployment Available=True on spoke-apps and GPU Job logs contain NVIDIA-SMI on spoke-ml"
    - "Strict chain ran in order, proven by ordered step markers in /tmp/karyon-rebuild.log without re-running the chain"
---

<objective>
Run the final live destructive proof for Phase 5. This plan does not modify
source files. It proves that the strict rebuild chain works on the target dev
box with warm CUDA cache, finishes in under 20 minutes, and ends with
`task health-check` green.

Per BLOCKER #3 from checker round 1, `task rebuild` runs EXACTLY ONCE in this
plan (Task 2). Task 3 then runs the live state-checker bats
(`phase5-02-live.bats`) which reads `/tmp/karyon-rebuild.log` and checks
post-rebuild cluster state — it does NOT re-execute `task rebuild` itself.
This eliminates the previous double-rebuild that consumed ~40 minutes and
destroyed clusters between runs.
</objective>

<threat_model>
**ASVS Level:** 1
**Block on:** high

- **T-05-15 Unapproved live destructive run:** Final verification destroys and
  recreates clusters. Mitigation: blocking human checkpoint before `task rebuild`.
- **T-05-16 Secret exposure in logs:** Live commands may reference `.env`-backed
  Flux/GitHub state. Mitigation: capture logs to local temp files only and do not
  commit timing/log artifacts.
- **T-05-17 Incomplete proof:** Rebuild without final health/workload checks can
  falsely pass. Mitigation: require `task health-check`, podinfo Available=True,
  GPU Job logs with `NVIDIA-SMI`, and live Bats state-checker suite.
- **T-05-21 Double-execution timing inflation:** Running rebuild twice (once
  manually, once inside the bats) destroys clusters between runs and inflates
  observed timing. Mitigation: Task 2 runs rebuild once; Task 3 reads the log
  written by that single run.
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm live destructive preconditions</name>
  <files></files>
  <read_first>
    - .planning/phases/05-workloads-health-rebuild/05-CONTEXT.md
    - scripts/rebuild.sh
    - scripts/health-check.sh
    - tests/bats/phase5-02-live.bats
  </read_first>
  <action>
Pause and present the live destructive checkpoint. Do not run `task rebuild`
until the user confirms:

- They are on the target WSL2 dev box with Docker/k3d/GPU available.
- The CUDA image cache is warm (`docker images karyon/k3s-cuda` shows the image).
- `.env` is populated with placeholder-safe local GitHub values.
- All Phase 5 files from Plans 05-02..05-05 are committed and pushed if Flux is
  expected to reconcile origin/main.
- They accept that this will destroy and recreate all k3d clusters.
- They understand `task rebuild` will run EXACTLY ONCE (Task 2) and the live
  bats in Task 3 will only verify the resulting state and log — not re-run
  rebuild.
  </action>
  <verify>
    <automated>docker images karyon/k3s-cuda --format '{{.Repository}}' | grep -q '^karyon/k3s-cuda$'</automated>
  </verify>
  <acceptance_criteria>
    - User explicitly confirms live destructive rebuild may proceed.
    - `docker images karyon/k3s-cuda --format '{{.Repository}}'` returns at least one `karyon/k3s-cuda` line.
    - `git status --short` is reviewed before running live rebuild.
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 2: Run task rebuild once and verify timing/order from log</name>
  <files></files>
  <read_first>
    - scripts/rebuild.sh
    - Taskfile.yml
    - scripts/destroy.sh
    - scripts/deploy-examples.sh
    - scripts/health-check.sh
  </read_first>
  <action>
Run `task rebuild` EXACTLY ONCE (BLOCKER #3 fix from checker round 1):

```bash
task rebuild
```

If non-interactive execution is needed after explicit human confirmation, run:

```bash
KARYON_REBUILD_APPROVED=yes task rebuild
```

The command must complete successfully. `scripts/rebuild.sh` writes the
following to `/tmp/karyon-rebuild.log` (per Plan 05-05 Task 2 contract):
- ordered step markers `>>> step destroy`, `>>> step create-clusters`,
  `>>> step bootstrap-flux`, `>>> step register-spokes`,
  `>>> step deploy-examples`, `>>> step health-check`
- a final line `rebuild elapsed seconds: N`

Do NOT re-run `task rebuild` to satisfy the verify block. The verify block
parses the log written by THIS single run.

If the final health is green but elapsed seconds exceed 1200, keep the failure;
do not override the hard SLO.
  </action>
  <verify>
    <automated>test -f /tmp/karyon-rebuild.log && grep -E '^rebuild elapsed seconds: [0-9]+' /tmp/karyon-rebuild.log | awk '{print $4}' | tail -1 | awk '$1<=1200{exit 0} $1>1200{exit 1}' && for s in destroy create-clusters bootstrap-flux register-spokes deploy-examples health-check; do grep -F ">>> step ${s}" /tmp/karyon-rebuild.log >/dev/null || exit 1; done && for pair in 'destroy:create-clusters' 'create-clusters:bootstrap-flux' 'bootstrap-flux:register-spokes' 'register-spokes:deploy-examples' 'deploy-examples:health-check'; do prev="${pair%%:*}"; next="${pair##*:}"; pl=$(grep -nF ">>> step ${prev}" /tmp/karyon-rebuild.log | head -1 | cut -d: -f1); nl=$(grep -nF ">>> step ${next}" /tmp/karyon-rebuild.log | head -1 | cut -d: -f1); [ "$pl" -lt "$nl" ] || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `task rebuild` exits 0 (single execution; not re-run by the verify block).
    - `/tmp/karyon-rebuild.log` exists.
    - The log contains a line matching `^rebuild elapsed seconds: [0-9]+`.
    - Parsed elapsed seconds is `<= 1200` (gated by `awk '$1<=1200{exit 0} $1>1200{exit 1}'`).
    - The log contains all six step markers `>>> step destroy`..`>>> step health-check`.
    - The step markers appear in strict order (destroy before create-clusters before bootstrap-flux before register-spokes before deploy-examples before health-check), proven by the line-number comparison loop in the verify block.
    - No output indicates `docker system prune`, `docker builder prune`, or `docker image prune` was run.
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 3: Run live state-checker bats and final workload proofs</name>
  <files></files>
  <read_first>
    - scripts/health-check.sh
    - tests/bats/phase5-02-live.bats
  </read_first>
  <action>
Run the lightweight final proofs (each is a state read; nothing destructive):

```bash
task health-check
kubectl --context k3d-spoke-apps -n podinfo get deploy podinfo
kubectl --context k3d-spoke-ml -n gpu-smoke logs job/nvidia-smi
```

Then run the live/destructive Bats STATE CHECKER suite. This suite reads
`/tmp/karyon-rebuild.log` (written by Task 2's single rebuild) and checks
post-rebuild cluster state. It does NOT invoke `task rebuild`,
`scripts/rebuild.sh`, or any destructive script (BLOCKER #3 contract):

```bash
KARYON_LIVE_TESTS=1 KARYON_LIVE_TESTS_DESTRUCTIVE=1 bats tests/bats/phase5-02-live.bats
```
  </action>
  <verify>
    <automated>task health-check && kubectl --context k3d-spoke-ml -n gpu-smoke logs job/nvidia-smi | grep -F NVIDIA-SMI && KARYON_LIVE_TESTS=1 KARYON_LIVE_TESTS_DESTRUCTIVE=1 bats tests/bats/phase5-02-live.bats</automated>
  </verify>
  <acceptance_criteria>
    - `task health-check` exits 0.
    - `kubectl --context k3d-spoke-apps -n podinfo get deploy podinfo -o jsonpath='{.status.conditions[?(@.type=="Available")].status}'` returns `True`.
    - `kubectl --context k3d-spoke-ml -n gpu-smoke logs job/nvidia-smi` contains `NVIDIA-SMI`.
    - `KARYON_LIVE_TESTS=1 KARYON_LIVE_TESTS_DESTRUCTIVE=1 bats tests/bats/phase5-02-live.bats` exits 0.
    - The live bats run does NOT invoke `task rebuild` or any destructive script (verified by grep contract in Plan 01 Task 5).
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `task rebuild` (run once in Task 2)
- `task health-check`
- `KARYON_LIVE_TESTS=1 KARYON_LIVE_TESTS_DESTRUCTIVE=1 bats tests/bats/phase5-02-live.bats` (state checker only, reads /tmp/karyon-rebuild.log)
</verification>

<success_criteria>
- The rebuild SLO is proven on target machine (single `task rebuild` execution).
- /tmp/karyon-rebuild.log captures ordered step markers and elapsed seconds.
- Health-check is green at the end of rebuild.
- DEP, HEALTH, and DESTROY requirements have live proof.
- Timing is recorded in execution summary and log file, not committed as a tracked timing log.
</success_criteria>
</output>
