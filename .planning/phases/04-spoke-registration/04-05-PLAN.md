---
phase: 04-spoke-registration
plan: 05
type: execute
wave: 4
depends_on: [04-01, 04-02, 04-03, 04-04]
files_modified: []
autonomous: false
requirements: [SPOKE-01, SPOKE-02, SPOKE-03, SPOKE-04, SPOKE-05, SPOKE-06]
requirements_addressed: [SPOKE-01, SPOKE-02, SPOKE-03, SPOKE-04, SPOKE-05, SPOKE-06]
nyquist_compliant: true
tags: [live-execution, checkpoint, post-push, reconciliation, p18-trip-wire]

must_haves:
  truths:
    - "scripts/register-spokes-for-flux.sh executed cleanly against the live three-cluster k3d topology (k3d-hub-flux + k3d-spoke-ml + k3d-spoke-apps); exit code 0; D-13 4-gate verification passed for both spokes"
    - "On second invocation (idempotency proof), the script's per-spoke 3-part-AND skip-if-healthy fires and the sentinel-grep skips the patch — `info \"already done, skipping:\"` lines appear >=4 times in the second-run output"
    - "After user commits + pushes the Phase 4 changes (clusters/hub-flux/spokes/, clusters/hub-flux/flux-system/kustomization.yaml, clusters/spoke-{ml,apps}/, Taskfile.yml, docs/flux-hub-spoke.md, scripts/register-spokes-for-flux.sh, tests/bats/register-spokes-*.bats), Flux's source-controller pulls the new state from origin/main and the kustomize-controller reconciles the `flux-system` Kustomization (which now includes `- ../spokes` -> `clusters/hub-flux/spokes/{spoke-ml,spoke-apps}.yaml`)"
    - "Both `spoke-ml` and `spoke-apps` Flux Kustomizations on hub-flux/flux-system reach `Ready=True`"
    - "Each spoke Kustomization's `.status.inventory.entries[].id` contains the literal `karyon-spoke-{ml,apps}_karyon-spoke-id__ConfigMap` (literal double-underscore for core-API ConfigMap with empty group — RESEARCH.md inventory ID format adjustment)"
    - "Cross-cluster falsifier holds: ConfigMap `karyon-spoke-id` exists in `karyon-spoke-ml` ns ONLY on `k3d-spoke-ml` (NOT on hub, NOT on spoke-apps); same for spoke-apps mirror — proves manifests routed to the correct cluster (P18 catch)"
    - "ROADMAP success criteria 1, 2, 3, 4, 5 all verified true: (1) script idempotent; (2) hub-side <spoke>-kubeconfig Secrets have valid kubeconfig with k3d-DNS server URL + base64 CA + non-expiring token; (3) in-pod probe returns ok for both spokes; (4) clusters/hub-flux/spokes/<spoke>.yaml have spec.kubeConfig.secretRef.{name,key=value.yaml} explicit; (5) negative-proof of correct routing via cross-cluster ConfigMap absence on hub"
  artifacts: []
  key_links:
    - from: scripts/register-spokes-for-flux.sh (executed live)
      to: k3d-hub-flux/flux-system/spoke-ml-kubeconfig + spoke-apps-kubeconfig Secrets (imperative apply)
      via: "Idempotent run — D-13 4-gate verification gates on every run"
      pattern: "kubectl --context k3d-hub-flux -n flux-system get secret spoke-ml-kubeconfig spoke-apps-kubeconfig"
    - from: User git push origin main
      to: Flux source-controller GitRepository pull → kustomize-controller reconciles
      via: "1-2 min reconciliation latency OR `flux reconcile source git flux-system && flux reconcile kustomization flux-system` nudge"
      pattern: "flux --context k3d-hub-flux get kustomizations -n flux-system"
    - from: tests/bats/register-spokes-02-live.bats (Plan 04-01 deliverable, run with KARYON_LIVE_TESTS=1 KARYON_LIVE_TESTS_DESTRUCTIVE=1)
      to: Reconciliation negative-proof (.status.inventory + cross-cluster ConfigMap falsifier)
      via: "13 @tests covering Ready=True + inventory ID + cross-cluster falsifier"
      pattern: "karyon-spoke-ml_karyon-spoke-id__ConfigMap"
---

<objective>
Wave 4 live execution + post-push reconciliation gate. This is the only Phase 4 plan that mutates live cluster state. It runs the orchestration script twice (first to land state, second to prove idempotency), pauses for the user to commit + push, then runs the post-push live-destructive bats suite to verify the reconciliation-layer correctness (`.status.inventory` + cross-cluster falsifier per ROADMAP success #5).

Purpose: Close the Phase 4 loop. Plans 04-01..04-04 produced verified-static deliverables; Plan 04-05 proves the deliverables actually work end-to-end against a live three-cluster k3d topology. Phase 4's HIGH-risk profile (DNS + TLS + CA + RBAC + Secret-key all converge) means we treat live execution as a separate, gated step rather than rolling it into the script-write plan.

Output: No file modifications. Pure execution + verification + checkpoints. The plan is `autonomous: false` because Task 3 is a `checkpoint:human-verify` for the user-push step — the script intentionally does NOT auto-push (D-04), so reconciliation can only happen after the human pushes.

Failure modes:
- Task 1 fail: script exits non-zero on first live run. Likely cause: D-13 4-gate verification caught a credential-layer fault (P18, missing CA, missing token, etc.). User inspects the script's `Fix:` remediation message and reruns.
- Task 2 fail: script's second run is NOT a no-op. Likely cause: idempotency-by-presence check missed a guard. User inspects the second-run stdout for the `info "already done, skipping:"` lines.
- Task 4 fail: post-push live bats fails. Likely cause: user has not pushed yet (the bats setup() emits a skip-with-message in this case), OR Flux has not reconciled yet (the `flux reconcile` nudge in the script Summary helps), OR the cross-cluster falsifier failed (P18 silent misroute landed manifests on the wrong cluster — user inspects the bats output for which @test failed and follows the remediation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-spoke-registration/04-CONTEXT.md
@.planning/phases/04-spoke-registration/04-RESEARCH.md
@.planning/phases/04-spoke-registration/04-PATTERNS.md
@.planning/phases/04-spoke-registration/04-VALIDATION.md
@scripts/register-spokes-for-flux.sh
@tests/bats/register-spokes-01-static.bats
@tests/bats/register-spokes-02-live.bats
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: First live run of scripts/register-spokes-for-flux.sh — D-13 4-gate verification</name>
  <files></files>
  <read_first>
    - scripts/register-spokes-for-flux.sh (the Plan 04-03 deliverable — the executor confirms it exists, executable, and passed bats static suite via Plan 04-04)
    - tests/bats/register-spokes-01-static.bats (must be 100% green from Plan 04-04 Task 4 — pre-condition for live execution)
    - .planning/phases/04-spoke-registration/04-CONTEXT.md D-13 lines 68-73 (4-gate verification: presence + decode + auth roundtrip + node-name negative-proof)
    - .planning/phases/04-spoke-registration/04-RESEARCH.md §Validation Architecture (lines 925-963) AND §Open Question 5 (lines 895-898 — flux reconcile nudge guidance)
  </read_first>
  <action>
**Pre-conditions** (the executor MUST verify before running the script):
1. `kubectl config current-context` returns `k3d-hub-flux`. If not, run `kubectl config use-context k3d-hub-flux` first. The script enforces this gate but failing fast at the executor level produces a clearer error.
2. `bats tests/bats/register-spokes-01-static.bats --tap | grep -c '^not ok'` returns 0 (Plan 04-04 Task 4 left this state). If non-zero, return to Plan 04-04 — the static contract is not met yet.
3. All 3 k3d clusters are Ready: `kubectl --context k3d-hub-flux get nodes && kubectl --context k3d-spoke-ml get nodes && kubectl --context k3d-spoke-apps get nodes` all show a single Ready node.
4. Phase 3 flux-system controllers are Ready on hub: `flux --context k3d-hub-flux check` exits 0 (4 controllers Available).

**Run the script:**

```bash
bash scripts/register-spokes-for-flux.sh
```

Capture exit code AND full stdout+stderr to a log:

```bash
LOG="$(mktemp -t register-spokes-first-run.XXXXXX.log)"
bash scripts/register-spokes-for-flux.sh > "$LOG" 2>&1
RC=$?
echo "Exit code: $RC"
echo "Log: $LOG"
tail -30 "$LOG"
```

Expected behavior:
- `Section "Preflight gate"`: emits `pass "preflight green"`.
- `Section "Context check"`: emits `pass "kubectl context: k3d-hub-flux"`.
- `Section "spoke-ml"`: emits multiple `pass` lines for the 7 subsections (RBAC apply, token wait, kubeconfig assembly, hub Secret apply, hub-side Kustomization YAML, spoke seed tree, 4-gate verification).
- `Section "spoke-apps"`: same shape, `spoke-apps` substituted.
- `Section "Hub-side Kustomization wire-up"`: either `info "already done, skipping: spokes mount sentinel present"` (if Plan 04-04 Task 1 already landed the patch) OR `pass "applied: '- ../spokes' under resources:"` (if the executor's environment has not yet pulled Plan 04-04's edits into the working tree — though Plan 04-04 is a hard dependency, this branch should not fire).
- `Section "Summary"`: `0 passed, 0 warnings, 0 failed` is wrong — should be many passes; if `failed > 0`, the script exited 1 and Task 1 fails.
- Final `info` line: the exact `git add ... && git commit -m '...' && git push` reminder + `flux reconcile` nudge.

If `RC != 0`, do NOT proceed to Task 2. Read the log for the offending `fail` message; the message includes a `Fix:` remediation. Common causes:
- `flux-reconciler-token never populated on <spoke> after 10s`: SA-Secret race; rerun after 30s.
- `auth roundtrip failed for <spoke>`: in-pod wget got a non-`ok` response; check that the kustomize-controller pod is Ready (`kubectl --context k3d-hub-flux -n flux-system get deploy kustomize-controller`).
- `node-name negative-proof FAILED ... P18 SILENT MISROUTE caught`: the kubeconfig points at the wrong server. Inspect the Secret content via the `kubectl get secret <spoke>-kubeconfig -o jsonpath='{.data.value\.yaml}' | base64 -d` line in the fail message. Re-run after diagnosis.
  </action>
  <verify>
    <automated>set +e; LOG="$(mktemp -t register-spokes-first.XXXXXX.log)"; bash scripts/register-spokes-for-flux.sh > "$LOG" 2>&1; RC=$?; echo "EXIT=$RC"; echo "=== TAIL ==="; tail -30 "$LOG"; echo "=== PASS COUNT ==="; grep -c '✓' "$LOG"; echo "=== FAIL COUNT ==="; grep -c '✗' "$LOG"; echo "=== LOG: $LOG ==="; [ "$RC" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - Pre-condition 1: `kubectl config current-context` returned `k3d-hub-flux` before the script ran.
    - Pre-condition 2: Plan 04-04 Task 4 left `bats tests/bats/register-spokes-01-static.bats` 100% green; verified by `bats tests/bats/register-spokes-01-static.bats --tap | grep -c '^not ok'` returning 0.
    - Pre-condition 3: All 3 k3d clusters Ready; `flux --context k3d-hub-flux check` exits 0.
    - Script exit code is 0: `bash scripts/register-spokes-for-flux.sh; echo $?` ends with `0` on stdout.
    - Hub Secret spoke-ml-kubeconfig exists: `kubectl --context k3d-hub-flux -n flux-system get secret spoke-ml-kubeconfig -o jsonpath='{.data.value\.yaml}'` returns non-empty base64 content.
    - Hub Secret spoke-apps-kubeconfig exists: same form for spoke-apps; non-empty.
    - Spoke-side flux-reconciler SA exists on each spoke: `kubectl --context k3d-spoke-ml -n flux-system get serviceaccount flux-reconciler -o name` returns `serviceaccount/flux-reconciler`; same for spoke-apps.
    - Spoke-side flux-reconciler-cluster-admin CRB exists on each spoke; `roleRef.name` is `cluster-admin`.
    - Spoke-side flux-reconciler-token Secret has data populated: `kubectl --context k3d-spoke-ml -n flux-system get secret flux-reconciler-token -o jsonpath='{.data.token}'` returns non-empty; same for `data.ca\.crt`; same for spoke-apps.
    - clusters/hub-flux/flux-system/kustomization.yaml contains `# KARYON SPOKES MOUNT` AND `- ../spokes` AND `# FLUX PATCH SURFACE` — verify with three grep -F checks each exiting 0.
    - The script's stdout (captured to $LOG) contains the literal `node-name proof ok:` for both spokes (D-13 step 4 passed; P18 trip-wire NOT triggered).
    - The script's stdout contains the literal `auth roundtrip ok:` for both spokes (D-13 step 3 passed).
    - The script's stdout contains the literal `presence ok:` for both spokes (D-13 step 1 passed).
    - The script's stdout contains the literal `decode ok:` for both spokes (D-13 step 2 passed).
    - The script's stdout does NOT contain any `✗` (fail) characters: `grep -c '✗' "$LOG"` returns 0.
    - Pass count >= 14 on first run: `grep -c '✓' "$LOG"` returns >=14 (preflight + context + 6 per-spoke passes × 2 spokes + sentinel patch = ~14 minimum).
    - Final summary line shows `0 failed`: `grep -F '0 failed' "$LOG"` exits 0.
  </acceptance_criteria>
  <done>scripts/register-spokes-for-flux.sh executed cleanly on the live three-cluster topology; D-13 4-gate verification passed for both spokes (P18 trip-wire NOT triggered); hub-side Secrets landed; spoke-side RBAC landed; sentinel patch verified present (idempotent if Plan 04-04 already wrote it). Live state matches the static contract.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Second live run — idempotency proof (D-04 / D-07 inheritance)</name>
  <files></files>
  <read_first>
    - scripts/register-spokes-for-flux.sh (verify the per-spoke 3-part-AND skip + sentinel-grep skip + file-exists skip are present in the script body via grep)
    - .planning/phases/04-spoke-registration/04-CONTEXT.md D-07 inheritance from Phase 3 (the 3-part-AND skip-if-healthy/fall-through pattern)
  </read_first>
  <action>
Run the script a SECOND time with no state changes between runs. The script must be a no-op (idempotent).

```bash
LOG="$(mktemp -t register-spokes-second-run.XXXXXX.log)"
bash scripts/register-spokes-for-flux.sh > "$LOG" 2>&1
RC=$?
echo "Exit code: $RC"
echo "=== Skip messages (idempotency proof) ==="
grep 'already done, skipping:' "$LOG"
echo "=== End ==="
```

Expected behavior:
- Exit code 0.
- Multiple `info "already done, skipping:"` lines:
  - Per spoke: `info "already done, skipping: ${spoke}-kubeconfig (Secret present, decodes, auth ok)"` (3-part-AND skip fires).
  - Sentinel patch: `info "already done, skipping: spokes mount sentinel present in clusters/hub-flux/flux-system/kustomization.yaml"`.
- The 3-part-AND skip means the per-spoke RBAC apply, token wait, kubeconfig assembly, hub Secret apply, and 4-gate verification are ALL skipped on the second run — only the cheap idempotency probes run. This is the load-bearing performance characteristic: `task rebuild` (Phase 5 DESTROY-05) calls `task register-spokes` as the 4th step in the chain; if the script is NOT idempotent, the 20-min rebuild SLO is violated.
- Total runtime of second run < 5 seconds (vs. ~10-15 seconds for the first run).

Note on patch idempotency: if Plan 04-04 Task 1 already wrote the sentinel + `- ../spokes` patch to kustomization.yaml BEFORE the first script run, the first run's `Section "Hub-side Kustomization wire-up"` ALREADY emits the skip message; the second run is just a no-op repeat. If Plan 04-04 Task 1 happened AFTER the first script run (executor ran them in a different order), the first run wrote the patch and the second run skips it — both code paths converge on the same final state.
  </action>
  <verify>
    <automated>set +e; LOG="$(mktemp -t register-spokes-second.XXXXXX.log)"; bash scripts/register-spokes-for-flux.sh > "$LOG" 2>&1; RC=$?; echo "EXIT=$RC"; echo "=== SKIP MESSAGES ==="; grep -F 'already done, skipping:' "$LOG"; SKIP_COUNT=$(grep -cF 'already done, skipping:' "$LOG"); echo "SKIP_COUNT=$SKIP_COUNT"; echo "=== FAIL COUNT ==="; grep -c '✗' "$LOG"; echo "=== LOG: $LOG ==="; [ "$RC" -eq 0 ] && [ "$SKIP_COUNT" -ge 3 ]</automated>
  </verify>
  <acceptance_criteria>
    - Script exit code is 0 (idempotent — no state mutation, no failure).
    - Stdout contains at least 3 `already done, skipping:` lines (per-spoke 3-part-AND skip × 2 spokes + sentinel patch skip = 3 minimum).
    - Stdout contains the literal `already done, skipping: spoke-ml-kubeconfig` (proves the 3-part-AND for spoke-ml fired).
    - Stdout contains the literal `already done, skipping: spoke-apps-kubeconfig` (proves the 3-part-AND for spoke-apps fired).
    - Stdout contains the literal `already done, skipping: spokes mount sentinel present` (proves the sentinel-grep skip fired).
    - Stdout does NOT contain `applied:` for the per-spoke RBAC sections (`grep -F 'applied: spoke-ml RBAC' "$LOG"` exits NON-zero AND `grep -F 'applied: spoke-apps RBAC' "$LOG"` exits NON-zero — proving the apply was skipped).
    - Final summary shows `0 failed`: `grep -F '0 failed' "$LOG"` exits 0.
    - No `✗` (fail) characters: `grep -c '✗' "$LOG"` returns 0.
    - Cluster state UNCHANGED between Task 1 and Task 2: `kubectl --context k3d-hub-flux -n flux-system get secret spoke-ml-kubeconfig spoke-apps-kubeconfig -o jsonpath='{.items[*].metadata.resourceVersion}'` returns the SAME resourceVersions as captured at the end of Task 1 (or, if the executor cannot capture between-task resourceVersions, the Secrets simply still exist with non-empty data.value\.yaml — which is sufficient proof of no-regression).
  </acceptance_criteria>
  <done>Second run of scripts/register-spokes-for-flux.sh exits 0 with skip-messages firing; no state change; idempotency proven for `task rebuild` chain (Phase 5 hand-off). Per-spoke 3-part-AND skip fired for both spokes; sentinel patch skip fired.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User commits + pushes the Phase 4 changes</name>
  <files>(no file modifications by the executor — the user runs git add/commit/push manually; this is a checkpoint:human-verify task)</files>
  <action>
This is a `checkpoint:human-verify` task. The execute-plan agent MUST NOT run `git add`, `git commit`, or `git push` itself — D-04 forbids automated git mutations from any Phase 4 deliverable, and the script's Summary info-line already emits the exact commands for the user to run. The agent's ONLY action here is to PRESENT the checkpoint contract to the human (the contents of `<what-built>` and `<how-to-verify>` below) and PAUSE for the resume signal.

Do NOT proceed past this checkpoint without an `approved`/`pushed`/`pushed and reconciled` resume signal.
  </action>
  <verify>
    <automated>flux --context k3d-hub-flux get kustomizations -n flux-system 2>&1 | grep -E '(spoke-ml|spoke-apps).*True' | wc -l | xargs -I{} bash -c '[ {} -ge 2 ] || (echo "Both spoke Kustomizations must be Ready=True before resuming. Got: {} Ready=True spoke Kustomizations." && exit 1)'</automated>
  </verify>
  <done>The user pasted "approved" / "pushed" / "pushed and reconciled" as the resume signal AFTER `flux get kustomizations` confirmed both `spoke-ml` and `spoke-apps` are `Ready=True`. The Phase 4 changes are on origin/main and Flux has reconciled the new spec.kubeConfig-bearing Kustomizations.</done>
  <what-built>
    Tasks 1 + 2 verified the live cluster state matches the Phase 4 contract: hub-side `<spoke>-kubeconfig` Secrets exist, spoke-side RBAC is applied, the kustomization.yaml patch is in place, and the script is idempotent. The local Git checkout has 12+ files modified or created across Phase 4 (the script + the 9 manifest YAMLs + the 2 bats files + the kustomization.yaml patch + the Taskfile entry + the doc fill).
    
    Critical: the bearer-token-bearing `<spoke>-kubeconfig` Secrets on hub-flux are applied IMPERATIVELY only — they are NOT part of the Git checkout. The committed files are the declarative GitOps tree; the runtime Secrets are out-of-band. This is per D-04 / RESEARCH.md Anti-Patterns; the gitleaks pre-commit hook (Phase 6 REPO-04) provides defense-in-depth at commit time.
  </what-built>
  <how-to-verify>
    The user MUST commit + push the Phase 4 changes BEFORE Task 4 can run. The following sequence is the contract:
    
    1. **Inspect the diff** to confirm no secrets leaked into the working tree:
       ```bash
       git status
       git diff
       # Verify NO file under clusters/ or scripts/ contains a base64 bearer token,
       # a real kubeconfig, or any 'value.yaml:' field with content beyond the
       # declarative manifests we authored.
       ```
       Expected: 12 files changed (the 9 manifests from Plan 04-02, the script from Plan 04-03, the 2 bats files from Plan 04-01, the 3 wire-up edits from Plan 04-04 — kustomization.yaml + Taskfile.yml + docs/flux-hub-spoke.md). NO bearer tokens, NO base64-encoded kubeconfig blobs, NO `data.value.yaml: <b64>` lines.
    
    2. **Stage + commit** with the commit message matching what the script's Summary info-line emitted:
       ```bash
       git add clusters/hub-flux/spokes \
               clusters/hub-flux/flux-system/kustomization.yaml \
               clusters/spoke-ml clusters/spoke-apps \
               Taskfile.yml \
               docs/flux-hub-spoke.md \
               scripts/register-spokes-for-flux.sh \
               tests/bats/register-spokes-01-static.bats \
               tests/bats/register-spokes-02-live.bats
       git commit -m 'feat(04): register spokes for Flux reconciliation (SPOKE-01..06)'
       ```
    
    3. **Push to origin/main**:
       ```bash
       git push origin main
       ```
       Expected: push succeeds; no PR review gate (this is the user's own dev workflow); origin/main now contains all Phase 4 deliverables.
    
    4. **Trigger Flux reconciliation** (optional but speeds up Task 4 by 1-2 minutes):
       ```bash
       flux --context k3d-hub-flux reconcile source git flux-system
       flux --context k3d-hub-flux reconcile kustomization flux-system
       ```
       Expected: both commands exit 0 and emit `flux: reconciliation completed`.
    
    5. **Verify Flux picked up the new tree**:
       ```bash
       flux --context k3d-hub-flux get kustomizations -n flux-system
       ```
       Expected: 3 Kustomizations listed: `flux-system` (Ready=True), `spoke-ml` (Ready=True after a few seconds), `spoke-apps` (Ready=True after a few seconds). If `spoke-ml` or `spoke-apps` shows `Ready=Unknown` or `Ready=False`, wait 1 minute and recheck — first reconciliation can take up to 60 seconds.
    
    Once both spoke Kustomizations show `Ready=True`, type "approved" or "pushed and reconciled" to resume. If anything in steps 1-5 fails, abort and diagnose; do NOT proceed to Task 4 with a broken state.
  </how-to-verify>
  <resume-signal>Type "approved", "pushed", or "pushed and reconciled" once `flux get kustomizations` shows `spoke-ml` and `spoke-apps` both `Ready=True`. If diagnosis revealed a P18 silent misroute (the cross-cluster falsifier in Task 4 will fail), abort with a description of which spoke routed wrong and what the kubeconfig content looks like for diagnosis.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Run the post-push live-destructive bats suite — full reconciliation contract</name>
  <files></files>
  <read_first>
    - tests/bats/register-spokes-02-live.bats (the suite created in Plan 04-01 — 13 @tests gated by require_live_destructive())
    - .planning/phases/04-spoke-registration/04-VALIDATION.md (lines 50, 66 — pinning of inventory ID literals)
    - .planning/phases/04-spoke-registration/04-RESEARCH.md §Sources lines 834-843 (inventory ID format <ns>_<name>__<kind> for core API)
  </read_first>
  <action>
**Pre-condition:** Task 3 checkpoint resumed with "approved" / "pushed" — meaning the user has pushed AND `flux get kustomizations` shows both spoke Kustomizations Ready=True. If either is NOT Ready=True, the live bats will fail loudly (or skip-with-message via the unpushed-commits guard in setup()).

Run the live destructive bats suite with both gate env vars set:

```bash
KARYON_LIVE_TESTS=1 KARYON_LIVE_TESTS_DESTRUCTIVE=1 bats tests/bats/register-spokes-02-live.bats --tap
```

Expected behavior: every @test passes (13 @tests, no skips, no failures).

The 13 @tests verify:
1. SPOKE-04 (live): `spoke-ml-kubeconfig` Secret has `data.value.yaml` populated on hub.
2. SPOKE-04 (live): `spoke-apps-kubeconfig` Secret has `data.value.yaml` populated on hub.
3. SPOKE-01 (live): `flux-reconciler` SA + `flux-reconciler-cluster-admin` CRB exist on `k3d-spoke-ml`.
4. SPOKE-01 (live): same on `k3d-spoke-apps`.
5. SPOKE-02 (live): `flux-reconciler-token` Secret has `data.token` AND `data.ca.crt` populated on `k3d-spoke-ml`.
6. SPOKE-02 (live): same on `k3d-spoke-apps`.
7. SPOKE-05 (live): `spoke-ml` Kustomization on hub has `Ready=True`.
8. SPOKE-05 (live): `spoke-apps` Kustomization on hub has `Ready=True`.
9. SPOKE-05 (live): `spoke-ml` inventory contains `karyon-spoke-ml_karyon-spoke-id__ConfigMap` literal (RESEARCH.md inventory ID format adjustment).
10. SPOKE-05 (live): `spoke-apps` inventory contains `karyon-spoke-apps_karyon-spoke-id__ConfigMap`.
11. ROADMAP success #5: cross-cluster falsifier — `karyon-spoke-id` ConfigMap exists on `k3d-spoke-ml` (NOT on hub) with `data.spoke=spoke-ml`.
12. ROADMAP success #5: cross-cluster falsifier — same for spoke-apps; ConfigMap NOT on hub, NOT on spoke-ml's `karyon-spoke-apps` ns (which doesn't exist), exists on spoke-apps with `data.spoke=spoke-apps`.
13. SPOKE-03/06 (live): in-pod wget /readyz with the spoke-ml bearer token returns `ok` (auth roundtrip).
14. SPOKE-03/06 (live): in-pod wget /api/v1/nodes returns `k3d-spoke-ml-server-0` (NOT `k3d-hub-flux-server-0`) — node-name negative-proof.

(Test count was 13 in Plan 04-01; some tests have multiple assertions — the count varies depending on exactly how the executor laid them out. Tolerance: 12-15 @tests.)

If ANY @test fails:
- @tests 1-2 fail (Secrets missing data.value.yaml): the script's Section 4 hub Secret apply did not work; rerun the script.
- @tests 3-6 fail (spoke-side RBAC missing): the script's apply_spoke_rbac() did not work; rerun against the failing spoke.
- @tests 7-10 fail (Kustomizations not Ready / inventory missing): Flux has not reconciled yet, OR the user has not pushed (the setup() unpushed-commits guard will skip if it detects this), OR the spec.kubeConfig wiring is wrong (P18 — but Task 1 already caught this via D-13). Run `flux --context k3d-hub-flux reconcile kustomization flux-system` and wait 60s, then rerun the bats.
- @tests 11-12 fail (cross-cluster falsifier — ConfigMap wrong location): P18 silent misroute! The kubeconfig for one of the spokes is routing manifests to the wrong cluster. Inspect the Secret content via `kubectl --context k3d-hub-flux -n flux-system get secret <spoke>-kubeconfig -o jsonpath='{.data.value\.yaml}' | base64 -d` and verify the `server:` line matches the expected `https://k3d-<spoke>-server-0:6443`. This is exactly the failure mode D-13 step 4 was designed to catch IN-SCRIPT — if it fired here in the bats but not in Task 1's script run, something is asymmetric (e.g., the in-pod probe used a stale token but the bats uses a fresh one).
- @tests 13-14 fail (in-pod wget probe): the kustomize-controller pod may have restarted; check its Ready state.
  </action>
  <verify>
    <automated>set +e; LOG="$(mktemp -t register-spokes-live-bats.XXXXXX.log)"; KARYON_LIVE_TESTS=1 KARYON_LIVE_TESTS_DESTRUCTIVE=1 bats tests/bats/register-spokes-02-live.bats --tap > "$LOG" 2>&1; RC=$?; echo "EXIT=$RC"; echo "=== TAP SUMMARY ==="; grep -E '^(ok|not ok|1\.\.)' "$LOG"; echo "=== NOT-OK COUNT ==="; grep -c '^not ok' "$LOG"; echo "=== SKIPPED COUNT ==="; grep -c '# skip' "$LOG"; echo "=== LOG: $LOG ==="; [ "$RC" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `bats tests/bats/register-spokes-02-live.bats --tap | grep -c '^not ok'` returns 0 (no failures).
    - `bats tests/bats/register-spokes-02-live.bats --tap | grep -c '# skip'` returns 0 (no skips — the unpushed-commits setup() guard did not fire, meaning the user pushed correctly per Task 3).
    - `bats tests/bats/register-spokes-02-live.bats --tap | grep -c '^ok'` returns >=12.
    - `flux --context k3d-hub-flux get kustomizations -n flux-system` shows `spoke-ml` and `spoke-apps` both Ready=True (paranoia recheck).
    - The cross-cluster ConfigMap falsifier holds: `kubectl --context k3d-hub-flux -n karyon-spoke-ml get configmap karyon-spoke-id 2>&1` exits NON-zero with `NotFound` in the error; `kubectl --context k3d-spoke-ml -n karyon-spoke-ml get configmap karyon-spoke-id -o jsonpath='{.data.spoke}'` returns `spoke-ml`; same shape for spoke-apps mirror.
    - Inventory ID format verified: `kubectl --context k3d-hub-flux -n flux-system get kustomization spoke-ml -o json | jq -r '.status.inventory.entries[].id' | grep -F 'karyon-spoke-ml_karyon-spoke-id__ConfigMap'` exits 0.
    - All 5 ROADMAP success criteria for Phase 4 verified true: (1) script idempotent (Task 2 proved); (2) hub Secrets have valid kubeconfig with k3d-DNS server URL + base64 CA + non-expiring token (Task 1 D-13 step 2 proved + bats @test 1 proved); (3) in-pod probe returns ok for both spokes (Task 1 D-13 step 3 proved + bats @test 13 proved); (4) clusters/hub-flux/spokes/<spoke>.yaml have spec.kubeConfig.secretRef.{name,key=value.yaml} explicit (Plan 04-02 + Plan 04-01 static bats proved); (5) negative-proof of correct routing (Task 1 D-13 step 4 proved at credential layer + bats @tests 9-12 proved at reconciliation layer).
  </acceptance_criteria>
  <done>Live destructive bats suite passes 100%; ROADMAP Phase 4 success criteria 1-5 fully verified; reconciliation-layer correctness proven; cross-cluster ConfigMap falsifier holds. Phase 4 is operationally complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script execution → live three-cluster topology | Tasks 1+2 mutate cluster state via the user's local kubectl context (cluster-admin on all three clusters by k3d default). |
| Local Git → origin/main (Task 3 user push) | The user pushes the local checkout to GitHub. Phase 6 REPO-04 (gitleaks pre-commit) is the canonical defense against PAT/secret leakage; this plan retains the warn-only gitleaks-advisory section from Phase 3 in scripts/bootstrap-flux.sh + scripts/register-spokes-for-flux.sh as a belt-and-suspenders advisory. |
| User push → Flux source-controller pull → kustomize-controller reconcile → spoke apiservers (via spec.kubeConfig) | The reconciliation chain is hub-only Flux topology (ADR-004); cross-cluster apply happens via the kubeconfig Secrets that contain bearer tokens with cluster-admin on each spoke. |
| Live bats suite (Task 4) → kustomize-controller in-pod exec | bats @tests 13-14 extract the spoke's bearer token AND inject it as a wget Authorization header inside the kustomize-controller pod. Token appears in the pod's process arg list briefly during the wget invocation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-25 | Information Disclosure | User accidentally commits a hub Secret YAML or any file containing a real bearer token | mitigate | (1) The script writes hub Secrets imperatively only (D-04 / Plan 04-03); no Secret YAML is written to disk under clusters/. (2) The Plan 04-01 static bats negative-grep on `git push` in script body. (3) Plan 04-04's doc explicitly warns the user that hub Secrets are never committed. (4) Phase 6 REPO-04 gitleaks pre-commit (lands BEFORE first push per ROADMAP parallelization) is the canonical defense. (5) Task 3's checkpoint instructions explicitly direct the user to `git diff` and verify no bearer token / kubeconfig content is in the staged changes. |
| T-04-26 | Tampering | User pushes the wrong commit (e.g., includes uncommitted Phase 5 changes by mistake) | mitigate | Task 3's checkpoint instructions explicitly enumerate the 12 files to `git add`; the user's `git status` review is the gate. The bats live suite's setup() detects unpushed-commits and skip-with-message; if the user pushes the WRONG state, bats Task 4 will fail loudly on the @tests pinning the inventory ID format. |
| T-04-27 | Spoofing | An attacker on the user's machine (with cluster-admin already, since k3d is local) edits the kubeconfig Secret on hub between Task 1 and Task 2 to point at a malicious server | accept | Local-machine attacker with cluster-admin on hub-flux already has full control; Phase 4 cannot mitigate this further. RESEARCH.md security domain documents this as accepted lab-scope risk. |
| T-04-28 | Denial of Service | Flux reconciliation does not complete within Task 3's 1-2 minute window | mitigate | Task 3 checkpoint includes the optional `flux reconcile source git flux-system && flux reconcile kustomization flux-system` nudge to force immediate reconciliation rather than waiting for the natural interval. The bats live suite's setup() detects the unpushed-commits case and skip-with-message rather than timing out indefinitely. |
| T-04-29 | Repudiation | The script's stdout-only output is not persisted as an audit log | accept | Lab scope; user's terminal scrollback is the audit trail. Tasks 1+2 capture the script output to `mktemp` log files for diagnostic purposes, but they are ephemeral by design. |
| T-04-30 | Elevation of Privilege | Phase 4 grants flux-reconciler SA cluster-admin on each spoke (load-bearing per REQ-SPOKE-01) | accept | Locked design; namespace-scoped narrowing is v2. |

The live execution stage is the highest-risk Phase 4 surface — all DNS + TLS + CA + RBAC + Secret-key concerns converge here. The defenses are layered: D-13 in-script verification (Task 1), idempotency proof (Task 2), human review at push (Task 3), and post-push reconciliation contract (Task 4). Any single layer catching a fault aborts the chain before it propagates.
</threat_model>

<verification>
After Tasks 1-4 complete:

1. Hub Secrets present with non-empty data.value.yaml: `kubectl --context k3d-hub-flux -n flux-system get secret spoke-ml-kubeconfig spoke-apps-kubeconfig` returns both Secrets; `-o jsonpath='{.items[*].data.value\.yaml}'` returns non-empty for both.

2. Spoke RBAC present on each spoke: `kubectl --context k3d-spoke-ml -n flux-system get sa flux-reconciler` AND `kubectl --context k3d-spoke-ml get clusterrolebinding flux-reconciler-cluster-admin` AND `kubectl --context k3d-spoke-ml -n flux-system get secret flux-reconciler-token` all exit 0; same for spoke-apps.

3. kustomization.yaml patched: `grep -F '# KARYON SPOKES MOUNT' clusters/hub-flux/flux-system/kustomization.yaml` AND `grep -F '- ../spokes' clusters/hub-flux/flux-system/kustomization.yaml` AND `grep -F '# FLUX PATCH SURFACE' clusters/hub-flux/flux-system/kustomization.yaml` all exit 0.

4. Both spoke Kustomizations Ready=True: `flux --context k3d-hub-flux get kustomizations -n flux-system` shows `spoke-ml Ready=True` and `spoke-apps Ready=True`.

5. Inventory ID format verified: `kubectl --context k3d-hub-flux -n flux-system get kustomization spoke-ml -o json | jq -r '.status.inventory.entries[].id' | grep -F 'karyon-spoke-ml_karyon-spoke-id__ConfigMap'` exits 0; same shape for spoke-apps.

6. Cross-cluster falsifier holds: `karyon-spoke-id` ConfigMap exists ONLY on the targeted spoke (NOT on hub) for both spokes.

7. Idempotency: rerunning `bash scripts/register-spokes-for-flux.sh` produces >=3 `already done, skipping:` lines and exits 0.

8. Live destructive bats: `KARYON_LIVE_TESTS=1 KARYON_LIVE_TESTS_DESTRUCTIVE=1 bats tests/bats/register-spokes-02-live.bats --tap | grep -c '^not ok'` returns 0.

9. ROADMAP Phase 4 success criteria 1-5 all verified true (enumerated in Task 4 acceptance criteria).
</verification>

<success_criteria>
- Phase 4 is operationally complete: live three-cluster topology has hub-flux reconciling into spoke-ml and spoke-apps via spec.kubeConfig; both spoke Kustomizations Ready=True; cross-cluster ConfigMap falsifier holds.
- Script idempotency proven (Task 2): second run is a fast no-op with >=3 skip-messages; `task rebuild` (Phase 5) chain unblocked.
- D-13 4-gate in-script verification passed (Task 1) AND post-push live destructive bats passed (Task 4) — full Nyquist sampling per VALIDATION.md (static + in-script + post-push).
- P18 silent-misroute trip-wires (in-script node-name probe + bats cross-cluster falsifier) BOTH passed clean — no manifests landed on the wrong cluster.
- User has committed + pushed all Phase 4 deliverables to origin/main (Task 3 checkpoint resumed).
- ROADMAP Phase 4 success criteria 1-5 fully verified.
- Phase 4 is unblocked for Phase 5 (DEP-01..04 + HEALTH-01..07 + DESTROY-01..06 — workloads + health + rebuild chain).
</success_criteria>

<output>
After completion, create `.planning/phases/04-spoke-registration/04-05-SUMMARY.md` with:
- Live execution outcomes (first-run + second-run pass/fail counts; idempotency proof)
- Post-push reconciliation latency (time from `git push` to both Kustomizations Ready=True; informs Phase 5 SLO planning)
- Bats live destructive suite results (13 @tests, all green)
- ROADMAP success criteria 1-5 verification map
- Hand-off to Phase 5 (workloads + health + rebuild — `task register-spokes` is now invocable as the 4th step in DESTROY-05's chain)
- Hand-off to Phase 6 (DOCS-09 ADR-004 hub-only-Flux can land alongside; gitleaks REPO-04 still pending)
</output>
</content>
</invoke>