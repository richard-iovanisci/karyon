---
phase: 04-spoke-registration
plan: 04
type: execute
wave: 3
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - clusters/hub-flux/flux-system/kustomization.yaml
  - Taskfile.yml
  - docs/flux-hub-spoke.md
autonomous: true
requirements: [SPOKE-04, SPOKE-05, DOCS-03]
requirements_addressed: [SPOKE-04, SPOKE-05, DOCS-03]
nyquist_compliant: true
tags: [wire-up, sentinel-patch, taskfile, docs, integration]

must_haves:
  truths:
    - "clusters/hub-flux/flux-system/kustomization.yaml carries `# KARYON SPOKES MOUNT` sentinel followed by `- ../spokes` line under the `resources:` block (D-02 sentinel-guarded patch)"
    - "The Phase 3 `# FLUX PATCH SURFACE` comment block at the top of the file is preserved (non-regression — D-13 inheritance from Phase 3)"
    - "The bootstrap-managed `- gotk-components.yaml` and `- gotk-sync.yaml` resource entries are preserved (Flux bootstrap re-bootstraps must not break)"
    - "`kubectl --context k3d-hub-flux kustomize clusters/hub-flux/flux-system/` builds successfully and emits Flux Kustomization documents that include both the bootstrap-managed core controllers AND the Phase 4 spokes index"
    - "Taskfile.yml has a new `register-spokes` task entry: `desc: Register spoke clusters for Flux reconciliation via spec.kubeConfig (SPOKE-01..06)`; `cmds: - bash scripts/register-spokes-for-flux.sh` (single-line invocation per CONTEXT.md domain — full REPO-05 reorg stays Phase 6)"
    - "`docs/flux-hub-spoke.md` has the Phase 4 stub (`<!-- Phase 4: hub-spoke reconciliation -->`) replaced with a real `## Phase 4: Hub-spoke reconciliation` section that names the 3 artifacts (per-spoke RBAC + hub-side `<spoke>-kubeconfig` Secret + hub-side Flux Kustomization), reframes P18 per RESEARCH.md (silent-misroute = `spec.kubeConfig` ENTIRELY ABSENT, NOT key typo), and points to the wget-based in-pod probe + bats live test as defenses"
    - "After this plan, `bats tests/bats/register-spokes-01-static.bats --tap` shows ALL static @tests passing (the patch + Taskfile + doc are now present; Plans 04-01/04-02/04-03 already landed the script + manifests + bats files)"
  artifacts:
    - path: clusters/hub-flux/flux-system/kustomization.yaml
      provides: "Phase 3 D-13 patch surface — Phase 4 ADDS sentinel-guarded `- ../spokes` to mount the spokes/ directory; preserves all Phase 3 content (FLUX PATCH SURFACE comment, gotk-components.yaml, gotk-sync.yaml, kustomize.config.k8s.io/v1beta1 apiVersion)"
      contains: "# KARYON SPOKES MOUNT"
    - path: Taskfile.yml
      provides: "register-spokes task entry — single-line bash invocation per Phase 3 stub posture (CONTEXT.md §domain line 22; full REPO-05 reorganization is Phase 6)"
      contains: "register-spokes:"
    - path: docs/flux-hub-spoke.md
      provides: "Phase 4 hub-spoke reconciliation section — fills the Phase 3 stub with the 3-artifact contract + P18 reframe (per RESEARCH.md adjustment 2: silent misroute = block omission, NOT key typo) + node-name probe rationale + bats live test pointer; medium depth per CONTEXT.md Claude's Discretion"
      contains: "## Phase 4: Hub-spoke reconciliation"
  key_links:
    - from: clusters/hub-flux/flux-system/kustomization.yaml
      to: clusters/hub-flux/spokes/kustomization.yaml (Plan 04-02 deliverable)
      via: "`- ../spokes` resource reference under `resources:` block; kustomize 5.x permits parent-dir reference without --load-restrictor (RESEARCH.md Pitfall 7 verified live)"
      pattern: "- ../spokes"
    - from: Taskfile.yml
      to: scripts/register-spokes-for-flux.sh (Plan 04-03 deliverable)
      via: "task register-spokes invokes `bash scripts/register-spokes-for-flux.sh` — single-line cmds entry"
      pattern: "bash scripts/register-spokes-for-flux.sh"
    - from: docs/flux-hub-spoke.md (Phase 4 section)
      to: ROADMAP success #5 (negative-proof) + bats live test (Plan 04-01 register-spokes-02-live.bats)
      via: "Doc explains the in-pod node-name probe (script-side, D-13 step 4) AND points the user at the post-push live bats test for `.status.inventory` cross-cluster falsifier"
      pattern: "register-spokes-02-live.bats"
---

<objective>
Wave 3 integration: wire the Plan 04-02 manifests + Plan 04-03 script into the existing GitOps tree, Taskfile, and docs. This is a 3-file plan with three independent edits — none mutate cluster state, all are pure file-write operations.

Purpose: Land the connective tissue. Plans 04-01 (bats), 04-02 (manifests), and 04-03 (script) created the pieces in isolation; Plan 04-04 makes them addressable from the standard surfaces (`task register-spokes`, the kustomize-build at `clusters/hub-flux/flux-system/`, and the docs the user reads). After this plan lands, Plan 04-01's static bats suite passes 100% — the in-script verification gates and the post-push reconciliation gates are gated to Plan 04-05 (live execution).

Output: 3 file modifications totaling ~80 added lines (~5 line patch on kustomization.yaml, ~5 line append on Taskfile.yml, ~70 line section fill on docs/flux-hub-spoke.md). Idempotency: all three are sentinel/section-guarded, so re-running them is a no-op.

Risk: medium — the kustomization.yaml patch is the only file that's also written by the Plan 04-03 script. The script uses sentinel-grep to detect prior patches, so this plan's manual edit is fully compatible with the script's auto-edit. The kustomization.yaml MUST end up in the same final state regardless of which side wrote it (the bats static @test pins both the sentinel literal AND the `- ../spokes` line independently of position).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-spoke-registration/04-CONTEXT.md
@.planning/phases/04-spoke-registration/04-RESEARCH.md
@.planning/phases/04-spoke-registration/04-PATTERNS.md
@.planning/phases/04-spoke-registration/04-VALIDATION.md
@clusters/hub-flux/flux-system/kustomization.yaml
@Taskfile.yml
@docs/flux-hub-spoke.md
@tests/bats/register-spokes-01-static.bats
@scripts/register-spokes-for-flux.sh

<interfaces>
Current state of the three files BEFORE this plan (snapshots from the executor's read_first scan):

**clusters/hub-flux/flux-system/kustomization.yaml** (19 lines, ends with `- gotk-sync.yaml`):
```yaml
# ──────────────────────────────────────────────────────────────────────────
# FLUX PATCH SURFACE
#
# This file IS the supported patch surface for the hub-flux bootstrap.
# Edits here SURVIVE re-bootstrap — add `patches:`, `configMapGenerator:`,
# `images:` etc. here to customize the 4 core controllers.
#
# Peer files in this directory — gotk-components.yaml, gotk-sync.yaml —
# are bootstrap-managed and WILL be overwritten by `flux bootstrap`.
# Do NOT hand-edit them.
#
# See: docs/flux-hub-spoke.md
# ──────────────────────────────────────────────────────────────────────────
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- gotk-components.yaml
- gotk-sync.yaml
```

**Taskfile.yml** (current state — Phase 3 stub with 4 tasks: build-image, create-clusters, delete-clusters, bootstrap-flux):
```yaml
version: '3'
tasks:
  build-image: ...
  create-clusters: ...
  delete-clusters: ...
  bootstrap-flux:
    desc: Bootstrap Flux GitOps on k3d-hub-flux from clusters/hub-flux/ (FLUX-01..04)
    cmds:
      - bash scripts/bootstrap-flux.sh
```

**docs/flux-hub-spoke.md** (126 lines, ends with the Phase 4 stub on the last line):
```markdown
## Rule 3: `--path` is effectively immutable after first run
... (lines 97-115) ...
## Rerun behavior
... (lines 117-124) ...
<!-- Phase 4: hub-spoke reconciliation (spec.kubeConfig, <spoke>-kubeconfig Secrets, value.yaml key) -->
```

Pattern E1 (PATTERNS.md lines 366-396) — sentinel-guarded patch idempotency. The Plan 04-03 script ALREADY implements this; this plan reaches the SAME final state via direct edit (manual or via running the script first then committing the change).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Patch clusters/hub-flux/flux-system/kustomization.yaml — sentinel + ../spokes</name>
  <files>clusters/hub-flux/flux-system/kustomization.yaml</files>
  <read_first>
    - clusters/hub-flux/flux-system/kustomization.yaml (the FILE BEING MODIFIED — read it first to confirm pre-state matches the snapshot in `<interfaces>` above; if not, the executor must re-derive the patch)
    - .planning/phases/04-spoke-registration/04-CONTEXT.md D-02 (lines 39 — sentinel-guarded patch under `resources:` adds `- ../spokes`; sentinel literal is `# KARYON SPOKES MOUNT` per CONTEXT.md Claude's Discretion line 83)
    - .planning/phases/04-spoke-registration/04-PATTERNS.md Pattern E1 lines 366-396 (the awk-insert idiom; preserves the FLUX PATCH SURFACE comment block + adds new sentinel under resources)
    - .planning/phases/04-spoke-registration/04-RESEARCH.md §Pitfall 7 lines 538-547 (kustomize 5.x permits `../spokes` parent-dir reference — no --load-restrictor flag needed)
    - scripts/register-spokes-for-flux.sh (the Plan 04-03 deliverable — confirm the script's awk insert produces the SAME final state)
  </read_first>
  <action>
Edit `clusters/hub-flux/flux-system/kustomization.yaml` to add `# KARYON SPOKES MOUNT` sentinel + `- ../spokes` line under the `resources:` block.

The expected final state (after this edit):

```yaml
# ──────────────────────────────────────────────────────────────────────────
# FLUX PATCH SURFACE
#
# This file IS the supported patch surface for the hub-flux bootstrap.
# Edits here SURVIVE re-bootstrap — add `patches:`, `configMapGenerator:`,
# `images:` etc. here to customize the 4 core controllers.
#
# Peer files in this directory — gotk-components.yaml, gotk-sync.yaml —
# are bootstrap-managed and WILL be overwritten by `flux bootstrap`.
# Do NOT hand-edit them.
#
# See: docs/flux-hub-spoke.md
# ──────────────────────────────────────────────────────────────────────────
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- gotk-components.yaml
- gotk-sync.yaml
# KARYON SPOKES MOUNT
- ../spokes
```

Constraints:
- Preserve the entire FLUX PATCH SURFACE comment block at the top (lines 1-13 of pre-state) — it is part of Phase 3 D-13 contract; Phase 3's bats live test `tests/bats/bootstrap-flux-01-idempotent.bats` line 124 still asserts this literal in the kustomization.yaml.
- Preserve `apiVersion: kustomize.config.k8s.io/v1beta1` and `kind: Kustomization` (lines 14-15 of pre-state).
- Preserve the bootstrap-managed `- gotk-components.yaml` and `- gotk-sync.yaml` lines (lines 17-18 of pre-state) — `flux bootstrap` regenerates these on re-bootstrap; removing them breaks Flux self-management.
- Insert `# KARYON SPOKES MOUNT` (the D-02 sentinel) AND `- ../spokes` together as two adjacent lines under the `resources:` block (after the bootstrap-managed entries).
- Use the `Edit` tool (not `Write` — preserves the file's existing content). String to replace: the line `- gotk-sync.yaml`. New content: `- gotk-sync.yaml\n# KARYON SPOKES MOUNT\n- ../spokes`.
- Do NOT add `--load-restrictor` flag references anywhere — kustomize 5.x permits `- ../spokes` by default (RESEARCH.md Pitfall 7).
- After editing, verify with `kubectl --context k3d-hub-flux kustomize clusters/hub-flux/flux-system/` that the build succeeds.

Idempotency: if the file ALREADY has the sentinel + `- ../spokes` lines (e.g., because Plan 04-03's script was run first), DO NOT re-add them — the executor checks `grep -qF '# KARYON SPOKES MOUNT' clusters/hub-flux/flux-system/kustomization.yaml` and skips the edit if true.
  </action>
  <verify>
    <automated>grep -F -- '# KARYON SPOKES MOUNT' clusters/hub-flux/flux-system/kustomization.yaml && grep -F -- '- ../spokes' clusters/hub-flux/flux-system/kustomization.yaml && grep -F -- '# FLUX PATCH SURFACE' clusters/hub-flux/flux-system/kustomization.yaml && grep -F -- '- gotk-components.yaml' clusters/hub-flux/flux-system/kustomization.yaml && grep -F -- '- gotk-sync.yaml' clusters/hub-flux/flux-system/kustomization.yaml && kubectl --context k3d-hub-flux kustomize clusters/hub-flux/flux-system/ >/dev/null 2>&1 && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - File `clusters/hub-flux/flux-system/kustomization.yaml` exists.
    - `grep -F -- '# KARYON SPOKES MOUNT' clusters/hub-flux/flux-system/kustomization.yaml` exits 0 (D-02 sentinel landed).
    - `grep -F -- '- ../spokes' clusters/hub-flux/flux-system/kustomization.yaml` exits 0 (the spokes mount).
    - `grep -F -- '# FLUX PATCH SURFACE' clusters/hub-flux/flux-system/kustomization.yaml` exits 0 (Phase 3 D-13 invariant preserved — non-regression).
    - `grep -F -- '- gotk-components.yaml' clusters/hub-flux/flux-system/kustomization.yaml` exits 0 (bootstrap-managed entry preserved).
    - `grep -F -- '- gotk-sync.yaml' clusters/hub-flux/flux-system/kustomization.yaml` exits 0 (bootstrap-managed entry preserved).
    - `grep -F -- 'apiVersion: kustomize.config.k8s.io/v1beta1' clusters/hub-flux/flux-system/kustomization.yaml` exits 0 (apiVersion preserved).
    - `kubectl --context k3d-hub-flux kustomize clusters/hub-flux/flux-system/` exits 0 (the kustomize build succeeds — proves `- ../spokes` is a valid resource reference under kustomize 5.x).
    - Bats Phase-3 non-regression: `bats tests/bats/bootstrap-flux-01-idempotent.bats -f 'D-13'` exits 0 (the FLUX PATCH SURFACE sentinel grep still matches).
    - Bats Phase-4 D-02 @test: `bats tests/bats/register-spokes-01-static.bats -f 'D-02'` exits 0 (both `# KARYON SPOKES MOUNT` and `- ../spokes` AND `# FLUX PATCH SURFACE` greps succeed).
    - File line count between 18 and 25 lines: `wc -l clusters/hub-flux/flux-system/kustomization.yaml | awk '{print $1}'` returns a value in [18, 25].
  </acceptance_criteria>
  <done>kustomization.yaml has the sentinel-guarded `- ../spokes` patch landed; FLUX PATCH SURFACE comment block + bootstrap-managed entries preserved; kustomize build succeeds; Phase-3 non-regression bats passes; Phase-4 D-02 bats passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append register-spokes task to Taskfile.yml</name>
  <files>Taskfile.yml</files>
  <read_first>
    - Taskfile.yml (the FILE BEING MODIFIED — read to confirm pre-state has 4 tasks: build-image, create-clusters, delete-clusters, bootstrap-flux; the new entry appends after bootstrap-flux)
    - .planning/phases/04-spoke-registration/04-PATTERNS.md Pattern T1 lines 898-917 (single-task entry shape; mirrors Phase 3's bootstrap-flux entry verbatim)
    - .planning/phases/04-spoke-registration/04-CONTEXT.md §domain line 22 (single-line invocation; full REPO-05 reorganization stays Phase 6)
  </read_first>
  <action>
Append a `register-spokes` task entry to `Taskfile.yml` AFTER the existing `bootstrap-flux:` entry. Use the `Edit` tool — find the last line of the bootstrap-flux entry (`      - bash scripts/bootstrap-flux.sh`) and append the new task entry after it.

The new entry to add (verbatim — match Phase 3's bootstrap-flux entry shape):

```yaml

  register-spokes:
    desc: Register spoke clusters for Flux reconciliation via spec.kubeConfig (SPOKE-01..06)
    cmds:
      - bash scripts/register-spokes-for-flux.sh
```

(Note: the leading blank line is intentional — the existing tasks each have a blank line separator between them per Phase 3 stub style.)

Constraints:
- Use the `Edit` tool, not `Write` — preserves the existing 4 tasks.
- Indentation: 2 spaces for the task name (`  register-spokes:`), 4 spaces for `desc` and `cmds` (`    desc:`, `    cmds:`), 6 spaces + `- ` for the cmd entry. Match Phase 3's bootstrap-flux exactly.
- The desc line ends with `(SPOKE-01..06)` — pins the requirement IDs in the user-facing description.
- The cmds list contains exactly ONE entry: `bash scripts/register-spokes-for-flux.sh`. Do NOT add `flux reconcile` or `git push` follow-on commands here — the script's Summary info-line emits those instructions for the user to run manually (per CONTEXT.md D-04 / RESEARCH.md Open Question 5).
- Do NOT modify any of the 4 existing tasks (build-image, create-clusters, delete-clusters, bootstrap-flux); they are out-of-scope for Phase 4.
- Do NOT add any new top-level keys (`vars:`, `env:`, etc.) — the full Taskfile.yml reorganization is REPO-05 in Phase 6.
- After editing, verify with `task --list` (if `task` is on PATH) OR `grep` that the new entry parses.

Idempotency: if `register-spokes:` already exists in the file (e.g., earlier run of this plan), DO NOT re-add — `grep -qF 'register-spokes:' Taskfile.yml` skips the edit if true.
  </action>
  <verify>
    <automated>grep -F -- 'register-spokes:' Taskfile.yml && grep -F -- 'bash scripts/register-spokes-for-flux.sh' Taskfile.yml && grep -F -- 'SPOKE-01..06' Taskfile.yml && grep -F -- 'bootstrap-flux:' Taskfile.yml && (command -v task >/dev/null 2>&1 && task --list 2>&1 | grep -F register-spokes || echo 'task CLI not on PATH; falling back to grep validation') && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - File `Taskfile.yml` contains `register-spokes:` (the new task entry): `grep -F -- 'register-spokes:' Taskfile.yml` exits 0.
    - File contains `bash scripts/register-spokes-for-flux.sh` (the cmds entry): `grep -F -- 'bash scripts/register-spokes-for-flux.sh' Taskfile.yml` exits 0.
    - File contains the SPOKE-01..06 requirement reference in the desc: `grep -F -- 'SPOKE-01..06' Taskfile.yml` exits 0.
    - File still contains all 4 pre-existing tasks: `grep -F -- 'build-image:' Taskfile.yml`, `grep -F -- 'create-clusters:' Taskfile.yml`, `grep -F -- 'delete-clusters:' Taskfile.yml`, `grep -F -- 'bootstrap-flux:' Taskfile.yml` all exit 0.
    - File line count between 25 and 35 lines (Phase 3 stub was 22 lines; +5 lines for register-spokes entry = 27): `wc -l Taskfile.yml | awk '{print $1}'` returns a value in [25, 35].
    - File parses as valid YAML: `python3 -c "import yaml; yaml.safe_load(open('Taskfile.yml'))" 2>&1 | head -3` exits 0 OR yields no error message (no `python3` on PATH is acceptable; fall back to `yq eval . Taskfile.yml >/dev/null` if yq is on PATH).
    - If `task` is on PATH: `task --list` includes a line containing `register-spokes`. (If `task` is NOT on PATH — the executor's PATH may not have asdf shims active in non-interactive bash — skip this gate and rely on the grep validation above.)
    - Bats static @test for Taskfile entry: `bats tests/bats/register-spokes-01-static.bats -f 'REPO-05 hand-off'` exits 0 (the @test pinning `register-spokes:` and `bash scripts/register-spokes-for-flux.sh` literals passes).
  </acceptance_criteria>
  <done>Taskfile.yml has the new register-spokes task entry; all 4 pre-existing tasks preserved; Phase-4 REPO-05 hand-off bats @test flips green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fill the Phase 4 stub in docs/flux-hub-spoke.md</name>
  <files>docs/flux-hub-spoke.md</files>
  <read_first>
    - docs/flux-hub-spoke.md (the FILE BEING MODIFIED — read to confirm the Phase 4 stub `<!-- Phase 4: hub-spoke reconciliation ... -->` is on the last line; the executor REPLACES the stub line with the new section)
    - .planning/phases/04-spoke-registration/04-CONTEXT.md §specifics lines 271-324 (verbatim Phase 4 doc section template — medium-depth, 3-artifact contract + P18 callout + verification commands)
    - .planning/phases/04-spoke-registration/04-RESEARCH.md §Pitfall 2 lines 478-492 (P18 reframe — the silent-misroute fault is `spec.kubeConfig` block ENTIRELY ABSENT, NOT key typo; doc MUST reflect this)
    - .planning/phases/04-spoke-registration/04-PATTERNS.md Pattern D1 lines 922-1024 (verbatim Phase 4 doc fill content; doc-rule self-analog from Rule 2 lines 52-95)
    - .planning/phases/04-spoke-registration/04-CONTEXT.md Claude's Discretion line 89 (medium depth — REQ-IDs + value.yaml contract + P18 callout + node-name negative-proof rationale)
  </read_first>
  <action>
Use the `Edit` tool to replace the line `<!-- Phase 4: hub-spoke reconciliation (spec.kubeConfig, <spoke>-kubeconfig Secrets, value.yaml key) -->` with the full Phase 4 section.

The replacement content (copy verbatim from PATTERNS.md Pattern D1 lines 962-1022 — RESEARCH.md adjustments already integrated; P18 reframe in place):

````markdown
## Phase 4: Hub-spoke reconciliation

After `bash scripts/register-spokes-for-flux.sh` runs, the hub knows how to
reconcile manifests into each spoke. Three artifacts make this work:

1. **Per-spoke RBAC.** `flux-reconciler` SA + cluster-admin CRB + legacy
   `flux-reconciler-token` Secret in `flux-system` on each spoke. The legacy
   Secret has indefinite lifetime — chosen over `kubectl create token`
   (TokenRequest) because token expiry is not a useful failure mode in a lab.

2. **Hub-side `<spoke>-kubeconfig` Secret.** Lives in `flux-system` on
   `k3d-hub-flux`, key `value.yaml` (the explicit key REQ-SPOKE-04 locks).
   Contains the spoke's CA-data + bearer token + `https://k3d-<spoke>-server-0:6443`
   server URL. Applied imperatively (never committed — bearer token).

3. **Hub-side Flux `Kustomization` per spoke.** Lives at
   `clusters/hub-flux/spokes/<spoke>.yaml`, mounted into the existing
   flux-system Kustomization via the patch surface (`- ../spokes` added to
   `clusters/hub-flux/flux-system/kustomization.yaml`'s `resources:`). MUST
   include `spec.kubeConfig.secretRef.{name,key}` — both fields explicit. The
   `key: value.yaml` is belt-and-suspenders against future Flux defaults.

### Silent-misroute pitfall (P18)

**The real failure mode is a Kustomization YAML that omits `spec.kubeConfig`
entirely.** When that block is absent, the kustomize-controller reconciles via
its own in-cluster ServiceAccount, lands manifests on the hub instead of the
spoke, and reports `Ready=True`. Verified empirically against Flux 2.8.6
(2026-04-27): a wrong / missing `secretRef.key` produces `Ready=False` (loud);
a missing `spec.kubeConfig` block silently misroutes to hub.

Two defenses:

- Every spoke Kustomization YAML in `clusters/hub-flux/spokes/` includes both
  `spec.kubeConfig.secretRef.name` AND `key: value.yaml` explicitly. Do NOT
  remove either.
- `scripts/register-spokes-for-flux.sh` runs an in-pod node-name probe per
  spoke after creating the Secret: from inside `kustomize-controller` on hub,
  `wget --no-check-certificate --header='Authorization: Bearer <TOKEN>' https://k3d-<spoke>-server-0:6443/api/v1/nodes`
  MUST return a node named `k3d-<spoke>-server-0` (NOT `k3d-hub-flux-server-0`).
  If the probe fails, the script aborts before declaring success. (The
  kustomize-controller image v1.8.4 ships no `kubectl`, hence the wget form.)

### Verification

After committing + pushing the new `clusters/hub-flux/spokes/`, `clusters/spoke-ml/`,
`clusters/spoke-apps/` trees:

```bash
flux --context k3d-hub-flux get kustomizations -n flux-system
# spoke-ml and spoke-apps both Ready=True
```

Live/destructive bats: `KARYON_LIVE_TESTS=1 KARYON_LIVE_TESTS_DESTRUCTIVE=1 \
bats tests/bats/register-spokes-02-live.bats` — asserts the full
`.status.inventory` negative-proof (the seed `karyon-spoke-id` ConfigMap
appears in each spoke Kustomization's inventory and lives ONLY on the
targeted spoke).
````

Constraints:
- Use the `Edit` tool — replace the literal stub line with the full section. Do NOT use `Write` (would overwrite the file).
- Replacement preserves all 126 lines of pre-state EXCEPT the last stub line (line 126); after the edit, the file should be ~190 lines.
- The section heading is `## Phase 4: Hub-spoke reconciliation` (h2; matches the existing `## Rule 1`, `## Rule 2`, `## Rule 3`, `## Rerun behavior` heading style).
- The P18 callout MUST use the RESEARCH.md reframed framing: "spec.kubeConfig block ENTIRELY ABSENT" is the real silent-misroute fault; "wrong / missing secretRef.key produces Ready=False (loud)". This corrects the CONTEXT.md original framing per RESEARCH.md adjustment 2.
- The wget invocation in the doc MUST use `--no-check-certificate` (matches the script per D-14 ADJUSTED — kustomize-controller image lacks the kubectl that would otherwise let us validate the CA).
- The verification commands at the end MUST point to BOTH the manual `flux get kustomizations` proof AND the bats live test (`tests/bats/register-spokes-02-live.bats` with the destructive double-gate).
- Idempotency: if the file ALREADY contains `## Phase 4: Hub-spoke reconciliation`, DO NOT re-edit — `grep -qF '## Phase 4: Hub-spoke reconciliation' docs/flux-hub-spoke.md` skips the edit if true.
- After editing, the file should still parse as valid Markdown (no broken fenced code blocks, no orphan `<!-- ... -->` HTML comments).
  </action>
  <verify>
    <automated>grep -F -- '## Phase 4: Hub-spoke reconciliation' docs/flux-hub-spoke.md && ! grep -F -- '<!-- Phase 4: hub-spoke reconciliation' docs/flux-hub-spoke.md && grep -F -- 'spec.kubeConfig' docs/flux-hub-spoke.md && grep -iF -- 'silent' docs/flux-hub-spoke.md && grep -F -- 'wget --no-check-certificate' docs/flux-hub-spoke.md && grep -F -- 'register-spokes-02-live.bats' docs/flux-hub-spoke.md && grep -F -- 'k3d-hub-flux-server-0' docs/flux-hub-spoke.md && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/flux-hub-spoke.md` no longer contains the Phase 4 stub: `grep -F '<!-- Phase 4: hub-spoke reconciliation' docs/flux-hub-spoke.md` exits NON-zero.
    - File contains the new Phase 4 section heading: `grep -F -- '## Phase 4: Hub-spoke reconciliation' docs/flux-hub-spoke.md` exits 0.
    - File mentions `spec.kubeConfig`: `grep -F -- 'spec.kubeConfig' docs/flux-hub-spoke.md` exits 0.
    - File mentions `silent`: `grep -iF 'silent' docs/flux-hub-spoke.md` exits 0 (the P18 callout heading + body).
    - File mentions `value.yaml`: `grep -F -- 'value.yaml' docs/flux-hub-spoke.md` exits 0 (key contract).
    - File mentions the wget probe form: `grep -F -- 'wget --no-check-certificate' docs/flux-hub-spoke.md` exits 0 (D-14 ADJUSTED — wget-based per RESEARCH.md adjustment 1).
    - File mentions the bats live test: `grep -F -- 'register-spokes-02-live.bats' docs/flux-hub-spoke.md` exits 0.
    - File mentions the forbidden node literal: `grep -F -- 'k3d-hub-flux-server-0' docs/flux-hub-spoke.md` exits 0 (P18 negative-proof rationale).
    - File mentions the expected node pattern: `grep -F -- 'k3d-<spoke>-server-0' docs/flux-hub-spoke.md` exits 0.
    - File preserves the Phase 3 doc content: `grep -F -- '## Rule 1: ' docs/flux-hub-spoke.md` exits 0; `grep -F -- '## Rule 2: ' docs/flux-hub-spoke.md` exits 0; `grep -F -- '## Rule 3: ' docs/flux-hub-spoke.md` exits 0; `grep -F -- '## Rerun behavior' docs/flux-hub-spoke.md` exits 0 (Phase-3 non-regression).
    - File line count between 175 and 200 lines: `wc -l docs/flux-hub-spoke.md | awk '{print $1}'` returns a value in [175, 200] (pre-state was 126; new section is ~70 lines).
    - The bats Phase 4 doc @tests pass: `bats tests/bats/register-spokes-01-static.bats -f 'DOCS-03'` exits 0 (both the stub-removal @test AND the silent-misroute mention @test).
    - The bats SPOKE-04 @test for the doc passes: `bats tests/bats/register-spokes-01-static.bats -f 'SPOKE-04: value.yaml literal in docs'` exits 0.
  </acceptance_criteria>
  <done>docs/flux-hub-spoke.md Phase 4 section is filled; P18 reframe matches RESEARCH.md adjustment 2 ("block omission is the silent fault, NOT key typo"); wget-based probe is documented (D-14 ADJUSTED); bats live test pointer present; Phase 3 doc content preserved (non-regression). Plan 04-01's DOCS-03 @tests + SPOKE-04 doc @test flip green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Run the full static bats suite — verify Plan 04-01 fully passes</name>
  <files></files>
  <read_first>
    - tests/bats/register-spokes-01-static.bats (the suite created in Plan 04-01)
    - clusters/hub-flux/flux-system/kustomization.yaml (just patched in Task 1)
    - Taskfile.yml (just appended in Task 2)
    - docs/flux-hub-spoke.md (just filled in Task 3)
  </read_first>
  <action>
Run the full static bats suite to confirm ALL @tests pass after Tasks 1-3 complete. This is the integration gate that proves Plans 04-01 + 04-02 + 04-03 + 04-04 collectively land all the static contracts:

```bash
bats tests/bats/register-spokes-01-static.bats --tap
```

Expected output: every line is `ok N - <test name>`; no `not ok` lines.

If ANY @test fails, do NOT proceed to Plan 04-05. The failure indicates one of:
- A literal mismatch between Plan 04-02 manifests / Plan 04-03 script / Plan 04-04 wire-up and the bats grep contract.
- A missing file or section in the wire-up.
- A regression in Phase 3 invariants (check that `# FLUX PATCH SURFACE` is still present in clusters/hub-flux/flux-system/kustomization.yaml).

Diagnostic: run `bats tests/bats/register-spokes-01-static.bats --tap | grep '^not ok'` to enumerate failures, then fix the offending file. This task is a NO-OP from a file-modification standpoint — its job is to assert the integration is clean.

Phase-3 non-regression check (paranoid — make sure the kustomization.yaml patch did not break Phase 3's bats):

```bash
bats tests/bats/bootstrap-flux-01-idempotent.bats --tap | grep -E '^(ok|not ok)' | head -10
```

The pure-static @tests in Phase 3's suite must all still pass (the destructive `FLUX-04 + D-13` @test will skip without KARYON_LIVE_TESTS env vars — that's fine). Specifically:
- `FLUX-01: scripts/bootstrap-flux.sh invokes flux bootstrap github` → ok
- `FLUX-02: scripts/bootstrap-flux.sh hard-codes --path clusters/hub-flux` → ok
- `D-13: scripts/bootstrap-flux.sh carries the FLUX PATCH SURFACE sentinel literal` → ok (the script still has it; the kustomization.yaml ALSO still has it post-Task-1)
  </action>
  <verify>
    <automated>echo "=== Phase 4 static suite ==="; bats tests/bats/register-spokes-01-static.bats --tap 2>&1 | grep -E '^(ok|not ok|1\.\.)' | tail -30; echo ""; echo "=== Phase 4 not-ok count ==="; bats tests/bats/register-spokes-01-static.bats --tap 2>&1 | grep -c '^not ok' | xargs -I{} echo "FAILURES: {}"; echo ""; echo "=== Phase 3 non-regression (static only — destructive @tests skip) ==="; bats tests/bats/bootstrap-flux-01-idempotent.bats --tap 2>&1 | grep -E '^(ok|not ok|1\.\.)' | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `bats tests/bats/register-spokes-01-static.bats --tap | grep -c '^not ok'` returns 0 (zero failures across the full Phase 4 static suite).
    - `bats tests/bats/register-spokes-01-static.bats --tap | grep -c '^ok'` returns >=20 (matches the @test count from Plan 04-01 — all greens).
    - `bats tests/bats/bootstrap-flux-01-idempotent.bats --tap | grep -c '^not ok'` returns 0 (Phase 3 non-regression — the @tests that don't require live state still pass; the destructive `FLUX-04 + D-13 (live/destructive, combined)` @test SKIPs without KARYON_LIVE_TESTS, which counts as `ok ... # skip` in TAP and will not match `^not ok`).
    - `bats tests/bats/register-spokes-02-live.bats --tap | grep -c '^ok.*# skip'` returns >=12 (the live suite skips cleanly without KARYON_LIVE_TESTS / KARYON_LIVE_TESTS_DESTRUCTIVE — confirms the gate works).
    - No regressions in any other Phase 1/2/3 bats suites (smoke check): `bats tests/bats/preflight-*.bats tests/bats/cluster-*.bats tests/bats/bootstrap-flux-*.bats --tap 2>&1 | grep -c '^not ok'` returns a value at or below the pre-Task-1 baseline (the Plan 04-01 / 04-02 / 04-03 deliverables should not have broken any prior bats; if any prior bats already had failures, this gate is "do not regress").
  </acceptance_criteria>
  <done>Plan 04-01 static suite reaches 100% green; Phase-3 non-regression confirmed; the live suite skips cleanly. The static contract for Phase 4 is fully met. Plan 04-05 (live execution) is unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 04-04 file edits → Git commit (user's local checkout) | All three edits are pure file writes; no cluster state mutated. The user reviews + commits these edits via the `git add ... && git commit && git push` reminder emitted by Plan 04-03's script Summary (the doc and Taskfile edits land in the same commit per the user's discretion). |
| clusters/hub-flux/flux-system/kustomization.yaml → flux bootstrap re-run | Phase 3 D-13 verified that flux bootstrap does NOT rewrite this file when its `resources:` block is consistent with the bootstrap flag set. Adding `- ../spokes` to user-managed `resources:` entries is consistent with that guarantee. RESEARCH.md Pitfall 6 covers the defense (sentinel-grep idempotency in Plan 04-03's script). |
| docs/flux-hub-spoke.md → public repo | Doc content is public-safe — no secrets, no internal hostnames beyond the documented `k3d-<name>-server-0` Docker DNS aliases (which are intentionally part of the contract per REQ-SPOKE-03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-20 | Tampering | Plan 04-04's kustomization.yaml patch could remove the FLUX PATCH SURFACE comment block (Phase 3 D-13 invariant) | mitigate | Task 1's acceptance criteria explicitly assert `grep -F '# FLUX PATCH SURFACE' clusters/hub-flux/flux-system/kustomization.yaml` exits 0 AFTER the edit. Task 4 runs Phase 3's bats suite to detect any non-regression. |
| T-04-21 | Tampering | Plan 04-04's Taskfile edit could accidentally remove or modify a Phase 1/2/3 task | mitigate | Task 2's acceptance criteria explicitly assert all 4 pre-existing tasks (build-image, create-clusters, delete-clusters, bootstrap-flux) still grep cleanly after the edit. Use `Edit` not `Write` to avoid overwriting. |
| T-04-22 | Tampering | Plan 04-04's doc fill could overwrite Phase 3 doc content (rules, rerun behavior) | mitigate | Task 3's acceptance criteria assert all 4 Phase 3 H2 headings (`## Rule 1`, `## Rule 2`, `## Rule 3`, `## Rerun behavior`) still grep cleanly after the edit. Use `Edit` (replacing only the stub line) not `Write`. |
| T-04-23 | Information Disclosure | Doc could leak operational details that enable attacks (e.g., exact bearer token format) | accept | The doc describes the architectural contract (SA + CRB + Secret + kubeconfig + spec.kubeConfig); it does NOT include real bearer tokens, real cluster CA-data, or secrets. Public-safe by design. |
| T-04-24 | Spoofing | A malicious PR could substitute `- ../spokes` with `- ../malicious-spokes` to redirect Flux | accept | Pre-existing repo-trust assumption: anyone with PR-merge access could inject malicious manifests anywhere in the tree. Phase 6 REPO-04 (gitleaks) and REPO-07 (CI lint via kubeconform + markdownlint + shellcheck) are the broader defenses. Phase 4's bats static suite specifically pins `- ../spokes` literal as a non-regression gate. |

The wire-up is observational and integrative; it enforces nothing at runtime. The actual security controls live in (a) Plan 04-01's static bats suite catching drift, (b) Plan 04-03's in-script verification gates catching credential-layer faults, and (c) Plan 04-05's live destructive bats catching reconciliation-layer faults post-push.
</threat_model>

<verification>
After Tasks 1-4 complete:

1. `clusters/hub-flux/flux-system/kustomization.yaml` contains both `# FLUX PATCH SURFACE` AND `# KARYON SPOKES MOUNT` AND `- ../spokes` AND the bootstrap-managed `- gotk-components.yaml` + `- gotk-sync.yaml` entries.
2. `Taskfile.yml` has 5 task entries (build-image, create-clusters, delete-clusters, bootstrap-flux, register-spokes); the new entry has `desc:` mentioning SPOKE-01..06 and `cmds:` invoking `bash scripts/register-spokes-for-flux.sh`.
3. `docs/flux-hub-spoke.md` has the `## Phase 4: Hub-spoke reconciliation` section filled with the 3-artifact contract + P18 reframe (block omission, NOT key typo) + wget probe rationale + bats live test pointer.
4. `bats tests/bats/register-spokes-01-static.bats --tap | grep -c '^not ok'` returns 0 — full Plan 04-01 static suite green.
5. `bats tests/bats/bootstrap-flux-01-idempotent.bats --tap | grep -c '^not ok'` returns 0 — Phase-3 non-regression preserved.
6. `kubectl --context k3d-hub-flux kustomize clusters/hub-flux/flux-system/` exits 0 — kustomize build succeeds with the `- ../spokes` parent-dir reference (kustomize 5.x permits per RESEARCH.md Pitfall 7).

NOT verified by this plan:
- Live execution of `bash scripts/register-spokes-for-flux.sh` against the three-cluster topology — that is Plan 04-05.
- Flux reconciles the new `clusters/hub-flux/spokes/`, `clusters/spoke-ml/`, `clusters/spoke-apps/` trees — that requires the user to commit + push, which is Plan 04-05's checkpoint.
</verification>

<success_criteria>
- All 3 file modifications applied: kustomization.yaml patched (sentinel + ../spokes), Taskfile.yml appended (register-spokes task), docs/flux-hub-spoke.md filled (Phase 4 section).
- Plan 04-01 static bats suite reaches 100% green.
- Phase-3 bats non-regression: bootstrap-flux-01-idempotent.bats static @tests still pass (FLUX PATCH SURFACE sentinel preserved).
- All edits are idempotent: re-running this plan produces no further changes (sentinel-grep / heading-grep skips fire on every file).
- No cluster state mutated; no scripts executed beyond bats invocations for verification.
</success_criteria>

<output>
After completion, create `.planning/phases/04-spoke-registration/04-04-SUMMARY.md` with:
- 3 files modified (paths + line-count delta)
- bats static suite final state: 100% green
- Phase-3 non-regression: confirmed
- RESEARCH.md adjustments integrated in doc fill: P18 reframe (line ref), wget probe rationale (line ref)
- Hand-off to Plan 04-05 (live execution + post-push reconciliation gate; checkpoint:human-verify for the user-push step)
</output>
</content>
</invoke>