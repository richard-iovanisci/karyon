---
phase: 06-repo-hygiene-docs-adrs
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/bats/repo-hygiene-01-static.bats
  - tests/bats/repo-hygiene-02-taskfile.bats
  - tests/bats/repo-hygiene-03-ci.bats
  - tests/bats/docs-01-readme-order.bats
  - tests/bats/docs-02-architecture.bats
  - tests/bats/docs-04-gpu-notes.bats
  - tests/bats/docs-05-runbook.bats
  - tests/bats/docs-adr-template.bats
autonomous: true
requirements: []
requirements_addressed: []
tags:
  - phase-6
  - wave-0
  - bats
  - validation-scaffold
must_haves:
  truths:
    - "Bats authors who follow this phase have parse-clean test scaffolds for every NEW REPO/DOCS contract before any implementation lands."
    - "The 8 NEW bats files implement the literal patterns from 06-PATTERNS.md (existence + literal-presence + ordered-literal grep) verbatim."
    - "Each bats test file is invoked as part of the phase's quick-run command and parse-checks via `bats --count`."
    - "A synthetic `tests/repo-hygiene/known-bad.sh` fixture is used IN-MEMORY by the gitleaks proof in Plan 01, NEVER committed."
    - "The repo is already public; no test runs gitleaks against history during Wave 0 — that is Plan 01's one-shot scan."
  artifacts:
    - path: "tests/bats/repo-hygiene-01-static.bats"
      provides: "Static REPO-02 / REPO-03 / REPO-04 contract: .gitignore patterns, .env.example keys, hooks/pre-commit existence + invocation"
      contains: "@test \"REPO-02"
    - path: "tests/bats/repo-hygiene-02-taskfile.bats"
      provides: "Static REPO-05 contract: Taskfile.yml is a thin wrapper (no kubectl/docker/k3d/flux outside cmds:)"
      contains: "@test \"REPO-05"
    - path: "tests/bats/repo-hygiene-03-ci.bats"
      provides: "Static REPO-07 contract: 5 CI job names + fetch-depth: 0 in gitleaks job"
      contains: "@test \"REPO-07"
    - path: "tests/bats/docs-01-readme-order.bats"
      provides: "Static DOCS-01 contract: 10 fresh-clone-path section headings appear in locked order"
      contains: "@test \"DOCS-01"
    - path: "tests/bats/docs-02-architecture.bats"
      provides: "Static DOCS-02 contract: ```mermaid block + required nodes (hub-flux/spoke-ml/spoke-apps/6443/6444/6445/k8s-net/172.30.0.0/16/spec.kubeConfig)"
      contains: "@test \"DOCS-02"
    - path: "tests/bats/docs-04-gpu-notes.bats"
      provides: "Static DOCS-04 contract: 3-part GPU contract keywords + RTX 5090 + sm_120 + 12.8"
      contains: "@test \"DOCS-04"
    - path: "tests/bats/docs-05-runbook.bats"
      provides: "Static DOCS-05 contract: 7 step markers in order + at least one timing in seconds + Common failures section"
      contains: "@test \"DOCS-05"
    - path: "tests/bats/docs-adr-template.bats"
      provides: "Static DOCS-06..10 contract: 4 sections + Status: Accepted in each ADR"
      contains: "@test \"DOCS-06"
  key_links:
    - from: "tests/bats/repo-hygiene-*.bats and docs-*.bats"
      to: "tests/bats/test_helper.bash"
      via: "load 'test_helper'"
      pattern: "load 'test_helper'"
    - from: "all NEW bats files"
      to: "${REPO_ROOT}/<file-under-test>"
      via: "REPO_ROOT-rooted file paths from test_helper.bash"
      pattern: "\\$\\{REPO_ROOT\\}"
---

<objective>
Wave 0 validation scaffold per 06-VALIDATION.md. Author 8 NEW bats test files
that encode every observable contract for REPO-02..05/07 and DOCS-01/02/04/05/
06..10 BEFORE any implementation lands in Wave 1+. Each test parses cleanly
(via `bats --count`) and red-fails on missing implementation; once Wave 1+ ship
the artifacts, the same suites turn green without modification.

Purpose: Per Nyquist gate — every executor task in Waves 1-3 has a pre-existing
`<automated>` test command. No "implement and test in the same plan" anti-pattern.

Output: 8 bats files committed; `bats --count tests/bats/repo-hygiene-*.bats
tests/bats/docs-*.bats` reports `8` and parses with no syntax errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-repo-hygiene-docs-adrs/06-CONTEXT.md
@.planning/phases/06-repo-hygiene-docs-adrs/06-RESEARCH.md
@.planning/phases/06-repo-hygiene-docs-adrs/06-PATTERNS.md
@.planning/phases/06-repo-hygiene-docs-adrs/06-VALIDATION.md
@tests/bats/test_helper.bash
@tests/bats/destroy-rebuild-01-static.bats
@tests/bats/bootstrap-flux-02-docs.bats
@tests/bats/health-check-01-static.bats

<interfaces>
<!-- Conventions and helpers downstream tests must use. From tests/bats/test_helper.bash: -->

```bash
load 'test_helper'  # MUST be first non-comment line after shebang

# Provided globals (from test_helper.bash):
${REPO_ROOT}        # absolute path to repo root
${SCRIPTS_DIR}      # absolute path to scripts/
${IMAGES_DIR}       # absolute path to images/

# Provided helpers (sourced via preflight-lib.sh):
have <cmd>          # command -v <cmd>
require_live        # skip unless KARYON_LIVE_TESTS=1
require_live_destructive  # skip unless KARYON_LIVE_TESTS=1 + KARYON_LIVE_TESTS_DESTRUCTIVE=1
```

```bash
# Three project grep idioms (see 06-PATTERNS.md "Shared Patterns" section).

# 1. Existence + literal presence:
[ -f "$FILE" ]
run grep -F -- '<literal>' "$FILE"
[ "$status" -eq 0 ]

# 2. Negative-grep with comment-filter:
run bash -c "grep -v '^[[:space:]]*#' '$FILE' | grep -F -- '<forbidden>'"
[ "$status" -ne 0 ]

# 3. Ordered-literal grep:
previous=0
for literal in 'A' 'B' 'C'; do
  line="$(grep -nF -- "$literal" "$FILE" | head -1 | cut -d: -f1)"
  [ -n "$line" ]
  [ "$line" -gt "$previous" ]
  previous="$line"
done
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 4 repo-hygiene bats files (REPO-02..05/07)</name>
  <files>tests/bats/repo-hygiene-01-static.bats, tests/bats/repo-hygiene-02-taskfile.bats, tests/bats/repo-hygiene-03-ci.bats</files>
  <read_first>
    - tests/bats/destroy-rebuild-01-static.bats lines 1-44 (REPO-05 stub at 202-208 + DESTROY-04 grep at 148-181 — analog patterns to mirror verbatim)
    - tests/bats/test_helper.bash (provides REPO_ROOT)
    - .planning/phases/06-repo-hygiene-docs-adrs/06-PATTERNS.md "tests/bats/repo-hygiene-01-static.bats" through "tests/bats/repo-hygiene-03-ci.bats" sections
    - .planning/phases/06-repo-hygiene-docs-adrs/06-CONTEXT.md `<decisions>` D-16 (literal .gitignore patterns) and D-09..D-12 (CI job names)
    - .planning/phases/06-repo-hygiene-docs-adrs/06-RESEARCH.md §Pattern 2 (CI workflow with 5 job names + `fetch-depth: 0`)
  </read_first>
  <action>
Create three bats test files, each starting with `#!/usr/bin/env bats` shebang and `load 'test_helper'` followed by a `setup()` block that declares paths via `${REPO_ROOT}/...`.

**File A: `tests/bats/repo-hygiene-01-static.bats`** — covers REPO-02 (.gitignore D-16 expansion), REPO-03 (.env.example contract), REPO-04 (hooks/pre-commit) per 06-VALIDATION.md row "06-W0-01" and 06-PATTERNS.md analog `destroy-rebuild-01-static.bats:13-44`.

`setup()` declares: `GITIGNORE="${REPO_ROOT}/.gitignore"`, `ENVEXAMPLE="${REPO_ROOT}/.env.example"`, `HOOK="${REPO_ROOT}/hooks/pre-commit"`, `GITLEAKS_TOML="${REPO_ROOT}/.gitleaks.toml"`.

Tests (all using grep idiom 1 from `<interfaces>`):

- `@test "REPO-02 (D-16): .gitignore retains existing entries and adds Phase 6 expansions"`: assert literals `.env`, `kubeconfig`, `*.kubeconfig`, `*.pem`, `*.key`, `*.crt`, `flux-system/identity*`, `.DS_Store`, `*.swp`, `.idea/`, `.vscode/` each present via `run grep -F -- '<literal>' "$GITIGNORE"; [ "$status" -eq 0 ]`. Also assert `.planning/` is NOT in the file via `run grep -E '^\.planning/' "$GITIGNORE"; [ "$status" -ne 0 ]` (REPO-02 SC #1 + D-15 — `.planning/` is intentionally tracked).
- `@test "REPO-03: .env.example documents all three required keys with placeholder values"`: assert literals `GITHUB_OWNER=`, `GITHUB_REPO=`, `GITHUB_TOKEN=`, and the placeholder `ghp_REPLACE_WITH_PAT_WITH_repo_SCOPE` each present.
- `@test "REPO-04 (D-06): hooks/pre-commit exists and is executable bash with strict mode + preflight-lib sourcing"`: assert `[ -f "$HOOK" ]`, `[ -x "$HOOK" ]`, `run grep -E '^#!/usr/bin/env bash' "$HOOK"; [ "$status" -eq 0 ]`, `run grep -E '^set -euo pipefail' "$HOOK"; [ "$status" -eq 0 ]`, `run grep -F -- 'preflight-lib.sh' "$HOOK"; [ "$status" -eq 0 ]`.
- `@test "REPO-04 (D-06): hooks/pre-commit invokes the canonical gitleaks subcommand"`: assert `run grep -F -- 'gitleaks git --pre-commit' "$HOOK"; [ "$status" -eq 0 ]` AND `run grep -F -- '--redact' "$HOOK"; [ "$status" -eq 0 ]` AND `run grep -F -- '--staged' "$HOOK"; [ "$status" -eq 0 ]` AND `run grep -F -- '--verbose' "$HOOK"; [ "$status" -eq 0 ]`.
- `@test "REPO-04: hooks/pre-commit gates on gitleaks presence with remediation message"`: assert `run grep -F -- 'have gitleaks' "$HOOK"; [ "$status" -eq 0 ]` AND `run grep -F -- 'install-tools.sh' "$HOOK"; [ "$status" -eq 0 ]` (so the remediation message names the install path).
- `@test "REPO-04 (D-08): .gitleaks.toml exists with allow-rules for placeholders"`: assert `[ -f "$GITLEAKS_TOML" ]`, `run grep -F -- '[allowlist]' "$GITLEAKS_TOML"; [ "$status" -eq 0 ]`, `run grep -F -- 'ghp_REPLACE_WITH_PAT_WITH_repo_SCOPE' "$GITLEAKS_TOML"; [ "$status" -eq 0 ]`, `run grep -F -- '\.env\.example' "$GITLEAKS_TOML"; [ "$status" -eq 0 ]`.

**File B: `tests/bats/repo-hygiene-02-taskfile.bats`** — covers REPO-05 thin-wrapper assertion per 06-VALIDATION.md row "06-W0-02" and 06-PATTERNS.md analog `destroy-rebuild-01-static.bats:202-208`.

`setup()` declares `TASKFILE="${REPO_ROOT}/Taskfile.yml"`.

Tests:

- `@test "REPO-05: Taskfile.yml exists and lists every Phase 1-5 task"`: assert `[ -f "$TASKFILE" ]`. For literal in `build-image:`, `create-clusters:`, `delete-clusters:`, `bootstrap-flux:`, `register-spokes:`, `deploy-examples:`, `fix-dns:`, `health-check:`, `destroy:`, `rebuild:`, `preflight:` — assert each present via grep idiom 1.
- `@test "REPO-05: Taskfile.yml is a thin wrapper — every cmds: entry calls bash scripts/<name>.sh and nothing else"`: extract every line under any `cmds:` block via awk. Implementation: `run bash -c "awk '/^[[:space:]]+cmds:/{flag=1; next} /^  [a-z]/{flag=0} flag && /^[[:space:]]*-/' '$TASKFILE' | grep -vE '^[[:space:]]*-[[:space:]]+bash scripts/[a-z][a-z0-9-]+\\.sh\$'"`. Assert `[ "$status" -ne 0 ]` (no non-conforming lines).
- `@test "REPO-05: Taskfile.yml has no inline kubectl|docker|k3d|flux invocations outside cmds:"`: implementation: `run bash -c "grep -vE '^[[:space:]]*(#|cmds:|-[[:space:]]+bash scripts/)' '$TASKFILE' | grep -vE '^[[:space:]]*$' | grep -E '\\b(kubectl|docker|k3d|flux)[[:space:]]+'"`. Assert `[ "$status" -ne 0 ]`.

**File C: `tests/bats/repo-hygiene-03-ci.bats`** — covers REPO-07 workflow structure per 06-VALIDATION.md row "06-W0-03" and 06-PATTERNS.md analog `destroy-rebuild-01-static.bats:46-63`.

`setup()` declares `CI_FILE="${REPO_ROOT}/.github/workflows/ci.yml"`.

Tests:

- `@test "REPO-07: .github/workflows/ci.yml exists with correct top-level shape"`: assert `[ -f "$CI_FILE" ]`, `run grep -E '^name: CI' "$CI_FILE"; [ "$status" -eq 0 ]`, `run grep -F -- 'pull_request:' "$CI_FILE"; [ "$status" -eq 0 ]`, `run grep -F -- 'permissions:' "$CI_FILE"; [ "$status" -eq 0 ]`, `run grep -F -- 'contents: read' "$CI_FILE"; [ "$status" -eq 0 ]`.
- `@test "REPO-07 (D-09): all five required jobs present"`: assert each of the 5 literal job IDs `shellcheck:`, `markdownlint:`, `kubeconform:`, `prune-lint-bats:`, `gitleaks-scan:` present via grep idiom 1.
- `@test "REPO-07 (Pitfall 3 / T-06-PAT-01): gitleaks job uses fetch-depth: 0"`: implementation: `run bash -c "awk '/gitleaks-scan:/{flag=1} flag' '$CI_FILE' | grep -F 'fetch-depth: 0'"`. Assert `[ "$status" -eq 0 ]`.
- `@test "REPO-07 (D-12): prune-lint-bats job runs the existing destroy-rebuild-01-static.bats"`: assert `run grep -F -- 'bats tests/bats/destroy-rebuild-01-static.bats' "$CI_FILE"; [ "$status" -eq 0 ]`.
- `@test "REPO-07 (D-10): kubeconform schema-location includes the datreeio CRDs catalog"`: assert `run grep -F -- 'datreeio/CRDs-catalog' "$CI_FILE"; [ "$status" -eq 0 ]`. AND assert `run grep -F -- '{{.Group}}/{{.ResourceKind}}_{{.ResourceAPIVersion}}.json' "$CI_FILE"; [ "$status" -eq 0 ]`.
- `@test "REPO-07 (T-06-CI-01): all uses: lines pin to a 40-character commit SHA"`: implementation: `run bash -c "grep -E '^[[:space:]]+- uses:' '$CI_FILE' | grep -vE '@[a-f0-9]{40}'"`. Assert `[ "$status" -ne 0 ]` — no `uses:` line lacks a 40-char SHA.

DO NOT execute the bats files at this stage; they are expected to RED-FAIL until Waves 1-3 ship the artifacts. The acceptance gate is `bats --count` parses cleanly.
  </action>
  <verify>
    <automated>bats --count tests/bats/repo-hygiene-01-static.bats tests/bats/repo-hygiene-02-taskfile.bats tests/bats/repo-hygiene-03-ci.bats</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist: `[ -f tests/bats/repo-hygiene-01-static.bats ]`, `[ -f tests/bats/repo-hygiene-02-taskfile.bats ]`, `[ -f tests/bats/repo-hygiene-03-ci.bats ]`
    - Each file starts with literal `#!/usr/bin/env bats` (verify: `head -1 <file>` matches exactly)
    - Each file contains literal `load 'test_helper'` (verify: `grep -F "load 'test_helper'" <file>` exits 0)
    - `bats --count tests/bats/repo-hygiene-*.bats` returns a non-zero integer (12+ across the three files: 6 in -01, 3 in -02, 5 in -03 = 14)
    - `bash -c "shellcheck -s bash tests/bats/repo-hygiene-*.bats || true"` does NOT produce SC1090/SC1091 errors that bats does not already emit on existing files (regression check — compare to existing `destroy-rebuild-01-static.bats`)
    - `grep -F "REPO-02" tests/bats/repo-hygiene-01-static.bats` exits 0 (test naming convention satisfied)
    - `grep -F "REPO-05" tests/bats/repo-hygiene-02-taskfile.bats` exits 0
    - `grep -F "REPO-07" tests/bats/repo-hygiene-03-ci.bats` exits 0
  </acceptance_criteria>
  <done>Three bats files committed; `bats --count tests/bats/repo-hygiene-*.bats` parses without errors and reports >= 14 total tests; tests will RED-FAIL on the missing artifacts (expected).</done>
</task>

<task type="auto">
  <name>Task 2: Author 5 docs bats files (DOCS-01/02/04/05 + DOCS-06..10)</name>
  <files>tests/bats/docs-01-readme-order.bats, tests/bats/docs-02-architecture.bats, tests/bats/docs-04-gpu-notes.bats, tests/bats/docs-05-runbook.bats, tests/bats/docs-adr-template.bats</files>
  <read_first>
    - tests/bats/health-check-01-static.bats lines 1-50 (ordered-literal grep analog for DOCS-01 README order)
    - tests/bats/bootstrap-flux-02-docs.bats lines 1-50 (multi-keyword grep analog for DOCS-02 / DOCS-04)
    - tests/bats/destroy-rebuild-01-static.bats lines 129-146 (step-marker order grep analog for DOCS-05)
    - .planning/phases/06-repo-hygiene-docs-adrs/06-PATTERNS.md "tests/bats/docs-01-readme-order.bats" through "tests/bats/docs-adr-template.bats" sections
    - .planning/phases/06-repo-hygiene-docs-adrs/06-CONTEXT.md `<decisions>` D-01 (README order), D-02 (ADR template), D-03 (Mermaid), D-04 (runbook layout)
    - .planning/phases/06-repo-hygiene-docs-adrs/06-RESEARCH.md §Validation Architecture lines 1220-1224 (per-doc keyword lists)
  </read_first>
  <action>
Create five bats test files. Each starts with `#!/usr/bin/env bats` and `load 'test_helper'`. Each `setup()` block declares the file under test via `${REPO_ROOT}/...`.

**File A: `tests/bats/docs-01-readme-order.bats`** — covers DOCS-01 per 06-VALIDATION.md row "06-W0-04" and 06-PATTERNS.md `health-check-01-static.bats:20-37` analog.

`setup()`: `README="${REPO_ROOT}/README.md"`.

- `@test "DOCS-01: README.md exists and starts with an h1 (MD041)"`: assert `[ -f "$README" ]`, `run head -1 "$README"; echo "$output" | grep -E '^# '` exits 0.
- `@test "DOCS-01 (D-01): README walks the locked fresh-clone path in order"`: ordered-literal grep over the 10 section keywords IN ORDER: `Windows prereqs`, `WSL config`, `Docker install`, `NVIDIA setup`, `tools install`, `cluster creation`, `Flux bootstrap`, `spoke registration`, `example deploy`, `teardown`. Use grep idiom 3 verbatim from `<interfaces>`. Match case-insensitively (`grep -niF` instead of `grep -nF`) to allow Title Case heading variants.
- `@test "DOCS-01: README links to all v1 docs (architecture, gpu-notes, rebuild-runbook, flux-hub-spoke, wsl-networking) and the 5 ADRs"`: for literal in `docs/architecture.md`, `docs/gpu-notes.md`, `docs/rebuild-runbook.md`, `docs/flux-hub-spoke.md`, `docs/wsl-networking.md`, `docs/adr/0001-single-wsl2-shared-docker-network.md`, `docs/adr/0002-k3d-over-kind.md`, `docs/adr/0003-flux-over-argocd.md`, `docs/adr/0004-hub-only-flux-control-plane.md`, `docs/adr/0005-kubernetes-version-pin.md` — assert each present via `grep -F`.
- `@test "DOCS-01: README contains a 60-second TL;DR ahead of any installation step"`: implementation: `run bash -c "tldr_line=\$(grep -niE '(tl;dr|quick ?start|60.second)' '$README' | head -1 | cut -d: -f1); first_install=\$(grep -niE '(windows prereqs|wsl config)' '$README' | head -1 | cut -d: -f1); [ -n \"\$tldr_line\" ] && [ -n \"\$first_install\" ] && [ \"\$tldr_line\" -lt \"\$first_install\" ]"`. Assert `[ "$status" -eq 0 ]`.

**File B: `tests/bats/docs-02-architecture.bats`** — covers DOCS-02 per 06-VALIDATION.md row "06-W0-05" and 06-PATTERNS.md `bootstrap-flux-02-docs.bats:17-34` analog.

`setup()`: `DOC="${REPO_ROOT}/docs/architecture.md"`.

- `@test "DOCS-02 (Pitfall 4): docs/architecture.md starts with an h1 before the Mermaid block (MD041 safety)"`: `[ -f "$DOC" ]`. Implementation: `run bash -c "h1_line=\$(grep -nE '^# ' '$DOC' | head -1 | cut -d: -f1); mermaid_line=\$(grep -nE '^\`\`\`mermaid' '$DOC' | head -1 | cut -d: -f1); [ -n \"\$h1_line\" ] && [ -n \"\$mermaid_line\" ] && [ \"\$h1_line\" -lt \"\$mermaid_line\" ]"`. Assert `[ "$status" -eq 0 ]`.
- `@test "DOCS-02 (D-03): docs/architecture.md contains a fenced mermaid block"`: assert `run grep -E '^\`\`\`mermaid' "$DOC"; [ "$status" -eq 0 ]`.
- `@test "DOCS-02 (D-03): Mermaid topology contains all 3 clusters with API ports"`: for literal in `hub-flux`, `spoke-ml`, `spoke-apps`, `6443`, `6444`, `6445` — assert each present via `grep -F`.
- `@test "DOCS-02 (D-03): Mermaid topology contains the k8s-net 172.30.0.0/16 network and spec.kubeConfig arrows"`: for literal in `k8s-net`, `172.30.0.0/16`, `spec.kubeConfig` — assert each present.
- `@test "DOCS-02 (D-03): Mermaid topology lists the four flux-system controllers"`: for literal in `source-controller`, `kustomize-controller`, `helm-controller`, `notification-controller` — assert each present.

**File C: `tests/bats/docs-04-gpu-notes.bats`** — covers DOCS-04 per 06-VALIDATION.md row "06-W0-06" and 06-PATTERNS.md `bootstrap-flux-02-docs.bats:17-34` analog.

`setup()`: `DOC="${REPO_ROOT}/docs/gpu-notes.md"`.

- `@test "DOCS-04: docs/gpu-notes.md exists and starts with an h1"`: `[ -f "$DOC" ]`, `run head -1 "$DOC"; echo "$output" | grep -E '^# '` exits 0.
- `@test "DOCS-04: gpu-notes covers the 3-part k3d GPU contract"`: for literal in `default-runtime`, `nvidia/cuda`, `config.toml.tmpl` — assert each present (`grep -F`).
- `@test "DOCS-04: gpu-notes covers the RTX 5090 / sm_120 / CUDA 12.8+ floor"`: for literal in `RTX 5090`, `sm_120`, `12.8` — assert each present (`grep -F`).

**File D: `tests/bats/docs-05-runbook.bats`** — covers DOCS-05 per 06-VALIDATION.md row "06-W0-07" and 06-PATTERNS.md `destroy-rebuild-01-static.bats:129-146` analog.

`setup()`: `DOC="${REPO_ROOT}/docs/rebuild-runbook.md"`.

- `@test "DOCS-05: docs/rebuild-runbook.md exists and starts with an h1"`: `[ -f "$DOC" ]`, `head -1 "$DOC" | grep -E '^# '` exits 0.
- `@test "DOCS-05 (D-04): rebuild runbook lists the 7 step names in strict order"`: ordered-literal grep over `>>> step preflight`, `>>> step destroy`, `>>> step create-clusters`, `>>> step bootstrap-flux`, `>>> step register-spokes`, `>>> step deploy-examples`, `>>> step health-check`. Use grep idiom 3 verbatim. (The runbook embeds the `>>> step ...` markers verbatim from `scripts/rebuild.sh:49-79` to keep one source of truth.)
- `@test "DOCS-05 (specifics): rebuild runbook references the Phase 5 190-second baseline"`: assert `run grep -E '\b190\b' "$DOC"; [ "$status" -eq 0 ]` AND `run grep -iE '(seconds|s\b)' "$DOC"; [ "$status" -eq 0 ]`.
- `@test "DOCS-05 (D-04): rebuild runbook has a Common failures appendix"`: assert `run grep -iE '^## (common failures|failure modes)' "$DOC"; [ "$status" -eq 0 ]`. AND failure-mode keywords `fix-dns`, `nvidia.com/gpu`, `tls-san` each present via `grep -iF`.

**File E: `tests/bats/docs-adr-template.bats`** — covers DOCS-06..10 per 06-VALIDATION.md rows "06-W0-08" + "06-DOCS-06..10" and 06-PATTERNS.md `bootstrap-flux-02-docs.bats:17-34` analog.

`setup()`:
```
ADR_DIR="${REPO_ROOT}/docs/adr"
ADRS=(
  "0001-single-wsl2-shared-docker-network.md"
  "0002-k3d-over-kind.md"
  "0003-flux-over-argocd.md"
  "0004-hub-only-flux-control-plane.md"
  "0005-kubernetes-version-pin.md"
)
```

- `@test "DOCS-06..10 (D-02): all five ADR files exist with locked slugs"`: for each entry in $ADRS — assert `[ -f "${ADR_DIR}/<slug>" ]`.
- `@test "DOCS-06..10 (D-02): each ADR contains the 4 Nygard sections in order"`: for each ADR file, ordered-literal grep over `## Status`, `## Context`, `## Decision`, `## Consequences` (grep idiom 3, full-string `-F`).
- `@test "DOCS-06..10 (D-02): each ADR has Status: Accepted in the first 10 lines"`: implementation per ADR: `run bash -c "head -n 10 '${ADR_DIR}/<slug>' | grep -F 'Accepted'"; [ "$status" -eq 0 ]`.
- `@test "DOCS-06: ADR-001 captures single-WSL2 + shared k8s-net Docker network"`: `[ -f "${ADR_DIR}/0001-single-wsl2-shared-docker-network.md" ]`. For keyword in `WSL2`, `k8s-net`, `Docker` — assert each present (`grep -iF`).
- `@test "DOCS-07: ADR-002 captures k3d-over-Kind decision"`: `[ -f "${ADR_DIR}/0002-k3d-over-kind.md" ]`. For keyword in `k3d`, `Kind`, `--gpus all` — assert each present.
- `@test "DOCS-08: ADR-003 captures Flux-over-ArgoCD with ApplicationSet trade-off + migration note"`: `[ -f "${ADR_DIR}/0003-flux-over-argocd.md" ]`. For keyword in `Flux`, `ArgoCD`, `ApplicationSet`, `migration` — assert each present (`grep -iF`).
- `@test "DOCS-09: ADR-004 captures hub-only Flux control plane"`: `[ -f "${ADR_DIR}/0004-hub-only-flux-control-plane.md" ]`. For keyword in `hub`, `spec.kubeConfig`, `controller` — assert each present.
- `@test "DOCS-10 (specifics): ADR-005 pins k3s v1.34.6-k3s1 and CUDA 12.8+, notes current line + RTX 5090 sm_120 floor"`: `[ -f "${ADR_DIR}/0005-kubernetes-version-pin.md" ]`. For literal in `v1.34.6-k3s1`, `12.8`, `RTX 5090`, `sm_120`, `current line` — assert each present (`grep -iF`).
  </action>
  <verify>
    <automated>bats --count tests/bats/docs-01-readme-order.bats tests/bats/docs-02-architecture.bats tests/bats/docs-04-gpu-notes.bats tests/bats/docs-05-runbook.bats tests/bats/docs-adr-template.bats</automated>
  </verify>
  <acceptance_criteria>
    - All five files exist as listed in `<files>`
    - Each starts with literal `#!/usr/bin/env bats` (verify: `head -1 <file>` matches)
    - Each contains `load 'test_helper'`
    - `bats --count` returns a non-zero integer summing to >= 18 across the five files (4 in -01, 5 in -02, 3 in -04, 4 in -05, 8 in -adr-template = 24)
    - `grep -F "DOCS-01" tests/bats/docs-01-readme-order.bats` exits 0
    - `grep -F "DOCS-02" tests/bats/docs-02-architecture.bats` exits 0
    - `grep -F "DOCS-04" tests/bats/docs-04-gpu-notes.bats` exits 0
    - `grep -F "DOCS-05" tests/bats/docs-05-runbook.bats` exits 0
    - `grep -F "DOCS-06" tests/bats/docs-adr-template.bats` exits 0
    - `grep -F "DOCS-10" tests/bats/docs-adr-template.bats` exits 0
    - The ordered-literal grep idiom in DOCS-01 covers all 10 fresh-clone-path sections (`grep -c "for literal in" tests/bats/docs-01-readme-order.bats` >= 1)
  </acceptance_criteria>
  <done>Five bats files committed; total test count across all 8 NEW bats files (Tasks 1+2) is >= 32; all parse cleanly; tests will RED-FAIL on missing implementation (expected).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a Wave 0 test-scaffold plan; no application surface, no live services, no secret material. Boundary analysis: text files written into `tests/bats/`. Read by future `bats` invocations. No runtime threat surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (n/a) | (n/a) | bats scaffold files | accept | Test scaffolds carry no application surface. The downstream Plan 01 + Plan 07 plans inherit T-06-PAT-01 / T-06-CI-01 mitigations directly. |
</threat_model>

<verification>
Run `bats --count tests/bats/repo-hygiene-*.bats tests/bats/docs-*.bats`. Expected output: a non-zero integer (>= 32 tests). All 8 files exist as committed git objects. Tests will RED-FAIL on missing implementation files; that is the intended Wave 0 contract per Nyquist.

Manual sanity:
```
bats tests/bats/repo-hygiene-01-static.bats || true   # expected: many failures, no parse errors
bats tests/bats/docs-adr-template.bats || true        # expected: failures on missing ADR files
```
</verification>

<success_criteria>
- 8 NEW bats files exist on disk under `tests/bats/`.
- `bats --count` parses every file (no `1..0` parse errors).
- Tests use only the patterns documented in 06-PATTERNS.md "Shared Patterns" section (idioms 1-3) plus `head`/`awk`/`grep` literals from the analog files.
- No test invokes `KARYON_LIVE_TESTS=1` paths or attempts to run gitleaks / kubeconform / shellcheck (those belong to Plans 01 and 07).
- File count: exactly 8 NEW bats files (not 7, not 9).
- Plan committed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-repo-hygiene-docs-adrs/06-00-SUMMARY.md` recording:
- 8 bats files created with line counts and test counts per file
- Total test count across all 8 files
- Verification: `bats --count` output (showing all parse cleanly)
- Notes: All tests RED-FAIL by design until Wave 1+ ship the artifacts; this is the Nyquist gate.
</output>
