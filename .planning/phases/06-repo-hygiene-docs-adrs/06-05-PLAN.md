---
phase: 06-repo-hygiene-docs-adrs
plan: 05
type: execute
wave: 3
depends_on: [00, 01, 02, 03, 04]
files_modified:
  - README.md
  - prereqs.sh
  - starter.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-03
requirements_addressed:
  - DOCS-01
  - DOCS-03
tags:
  - phase-6
  - wave-3
  - readme
  - cleanup
must_haves:
  truths:
    - "DOCS-01 (D-01): README.md is a hybrid quickstart + reference — 60-second TL;DR up top + the locked fresh-clone path (Windows prereqs → WSL config → Docker install → NVIDIA setup → tools install → cluster creation → Flux bootstrap → spoke registration → example deploy → teardown) → per-task reference sections."
    - "DOCS-01: README cross-links to docs/architecture.md, docs/gpu-notes.md, docs/rebuild-runbook.md, docs/flux-hub-spoke.md, docs/wsl-networking.md, and the 5 ADRs under docs/adr/."
    - "DOCS-03 (verify-only): docs/flux-hub-spoke.md was shipped in Phase 3 — confirm exists and the README cross-links to it. NO rewrite of the file."
    - "Cleanup (D-13): prereqs.sh is removed from repo root via git rm. README references `task preflight` / `bash scripts/preflight.sh` as the canonical entry point."
    - "Cleanup (D-14): starter.md is removed from repo root via git rm. README + PROJECT.md / REQUIREMENTS.md / ROADMAP.md are now the only top-level entry points."
    - "After this plan, the repo root has no competing entry-point files — there is exactly one README.md and the planning artifacts under .planning/."
  artifacts:
    - path: "README.md"
      provides: "DOCS-01: hybrid quickstart + reference walkthrough + cross-link index"
      contains: "task rebuild"
      min_lines: 90
  key_links:
    - from: "README.md"
      to: "docs/architecture.md (Plan 03), docs/gpu-notes.md (Plan 03), docs/rebuild-runbook.md (Plan 06), docs/flux-hub-spoke.md (Phase 3 ✅), docs/wsl-networking.md (Phase 1 ✅)"
      via: "relative markdown links"
      pattern: "docs/[a-z-]+\\.md"
    - from: "README.md"
      to: "docs/adr/0001..0005-*.md"
      via: "relative markdown links"
      pattern: "docs/adr/000[1-5]"
---

<objective>
Author the README and remove the two superseded entry-point files. README is
a hybrid quickstart + reference (D-01): TL;DR + locked fresh-clone path + a
per-task reference section keyed off Taskfile.yml + a cross-reference index
to all v1 docs and ADRs.

Cleanup deletes: `prereqs.sh` (D-13, fully superseded by `scripts/preflight.sh`)
and `starter.md` (D-14, fully absorbed into PROJECT.md / REQUIREMENTS.md /
ROADMAP.md). Both files remain accessible via `git log`; removing them
prevents new contributors from landing on stale guidance.

Purpose: DOCS-01 is the single highest-impact artifact in Phase 6 — it is the
first file any new reader sees. The fresh-clone path is the contract this
project ships with; if a reader can run `task rebuild` from a fresh Windows
11 + WSL2 install by following the README in order, the lab succeeds.
DOCS-03 is verify-only (the file landed in Phase 3); README cross-links from
its Flux bootstrap section ensure the verify-only contract is satisfied.

Output: README.md committed; 2 deletions committed; bats DOCS-01 contract
green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-repo-hygiene-docs-adrs/06-CONTEXT.md
@.planning/phases/06-repo-hygiene-docs-adrs/06-RESEARCH.md
@.planning/phases/06-repo-hygiene-docs-adrs/06-PATTERNS.md
@Taskfile.yml
@docs/flux-hub-spoke.md
@docs/wsl-networking.md
@docs/architecture.md
@docs/gpu-notes.md
@docs/adr/0001-single-wsl2-shared-docker-network.md
@docs/adr/0002-k3d-over-kind.md
@docs/adr/0003-flux-over-argocd.md
@docs/adr/0004-hub-only-flux-control-plane.md
@docs/adr/0005-kubernetes-version-pin.md

<interfaces>
<!-- The 10 fresh-clone-path section keywords (D-01 + bats `tests/bats/docs-01-readme-order.bats`).
Each is grep-matched case-insensitively (`grep -niF`) so the heading prose may
use Title Case or any reasonable phrasing as long as the literal substring
appears in order:

  1. "Windows prereqs"
  2. "WSL config"
  3. "Docker install"
  4. "NVIDIA setup"
  5. "tools install"
  6. "cluster creation"
  7. "Flux bootstrap"
  8. "spoke registration"
  9. "example deploy"
  10. "teardown"

The bats test grep-orders these literals; reordering them breaks DOCS-01.
-->

<!-- Taskfile.yml (after Plan 04) has 11 tasks:
  preflight, build-image, create-clusters, delete-clusters, bootstrap-flux,
  register-spokes, deploy-examples, fix-dns, health-check, destroy, rebuild

  Per-task reference section per D-01 should cover all 11. -->

<!-- 5 ADRs after Wave 2:
  docs/adr/0001-single-wsl2-shared-docker-network.md (Plan 01)
  docs/adr/0002-k3d-over-kind.md (Plan 02)
  docs/adr/0003-flux-over-argocd.md (Plan 02)
  docs/adr/0004-hub-only-flux-control-plane.md (Plan 02)
  docs/adr/0005-kubernetes-version-pin.md (Plan 02)
-->

<!-- 5 v1 docs after Wave 2 (DOCS-05 lands in Plan 06 — forward reference is fine):
  docs/architecture.md (Plan 03; DOCS-02)
  docs/flux-hub-spoke.md (Phase 3 ✅; DOCS-03 verify-only)
  docs/gpu-notes.md (Plan 03; DOCS-04)
  docs/rebuild-runbook.md (Plan 06; DOCS-05 — forward link is OK; Plan 06 lands in same wave as this plan)
  docs/wsl-networking.md (Phase 1 ✅; HOST-07)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author README.md as hybrid quickstart + reference</name>
  <files>README.md</files>
  <read_first>
    - .planning/PROJECT.md "What This Is" + "Core Value" + "Context" sections (TL;DR source content; the README's 60-second pitch should compress to 3-5 lines from this material)
    - .planning/ROADMAP.md §Phase 6 success criteria (DOCS-01 SC #3: locked fresh-clone path order — verbatim source)
    - Taskfile.yml (mandatory pre-read; per-task reference sections must list 11 tasks in the same order as the file)
    - docs/flux-hub-spoke.md lines 1-30 (analog: top-of-file h1 + orientation paragraph + table style for structured rules)
    - docs/wsl-networking.md lines 1-30 (analog: bold lead-in for hard rules; Step 1 / Step 2 numbered procedural style)
    - .planning/phases/06-repo-hygiene-docs-adrs/06-PATTERNS.md "README.md" section
    - .planning/phases/06-repo-hygiene-docs-adrs/06-CONTEXT.md `<specifics>` 401-414 (locked fresh-clone path in literal order)
    - .planning/phases/06-repo-hygiene-docs-adrs/06-RESEARCH.md §Pitfall 7 lines 891-902 (branch-protection one-time setup — small block in README per Open Question 3)
    - tests/bats/docs-01-readme-order.bats (the contract this README must satisfy)
  </read_first>
  <action>
Use `Write` to create `README.md`. Content (synthesizes D-01 + the 10-section ordering literal + cross-link index):

```markdown
# karyon

A reproducible local Kubernetes lab — three k3d clusters (Flux hub + GPU
spoke + apps spoke) on Windows 11 + WSL2 — that you can rebuild from zero
in under 20 minutes.

## TL;DR

`task rebuild` performs a scorched-earth teardown and reaches a healthy
hub-spoke Flux GitOps environment with a CUDA-capable spoke in under 20
minutes, every time. On a warm CUDA image cache, the chain (destroy →
create-clusters → bootstrap-flux → register-spokes → deploy-examples →
health-check) ran in 190 seconds end-to-end on the author's dev box.

If `task rebuild` is green, every layer of the lab is green. That is the
contract this project ships with.

***

## Quick Start (Fresh Clone)

These ten steps walk a clean Windows 11 + WSL2 dev box to a running lab.
Each step has a corresponding [Per-Task Reference](#per-task-reference)
section below with details and recovery procedures.

> **Hardware floor:** RTX 5090 (Blackwell, sm_120) + 16+ cores + 40+ GB RAM
> + 100+ GB free disk. The dev box was tuned to 20 cores / 52 GB / ample
> NVMe; downscale at your own risk and re-time `task rebuild` accordingly.

### 1. Windows prereqs

Install WSL2 + an Ubuntu 24.04 distro. The `config/wslconfig` template at
the repo root pins `52 GB RAM / 20 CPU` for WSL — copy it (or merge with
your existing `%USERPROFILE%\.wslconfig`) and run `wsl --shutdown` for the
limits to take effect. NVIDIA driver 570.x or newer is required on the
Windows side for the RTX 5090 + sm_120 floor (see [`docs/gpu-notes.md`](docs/gpu-notes.md)).

### 2. WSL config

Inside the Ubuntu distro, copy `config/wsl.conf` to `/etc/wsl.conf` (the
template enables `systemd=true`). Re-shutdown WSL once. Confirm:

```bash
systemctl is-system-running   # expected: running (or degraded; not "offline")
```

WSL2 networking modes are documented in [`docs/wsl-networking.md`](docs/wsl-networking.md);
v1 requires NAT mode. Mirrored mode + Docker custom networks have unresolved
upstream issues.

### 3. Docker install

Run the Docker Engine installer (NOT Docker Desktop integration — see
[`docs/adr/0001-single-wsl2-shared-docker-network.md`](docs/adr/0001-single-wsl2-shared-docker-network.md)):

```bash
bash scripts/install-docker.sh
```

This sets `default-runtime: nvidia` and an explicit `dns` array in
`/etc/docker/daemon.json` (HOST-04 — required for the 3-part k3d GPU
contract).

### 4. NVIDIA setup

Install the NVIDIA Container Toolkit:

```bash
bash scripts/install-nvidia-container-toolkit.sh
```

Confirm GPU pass-through:

```bash
docker run --rm --gpus all nvcr.io/nvidia/cuda:12.8.0-base-ubuntu22.04 nvidia-smi
# Expected: NVIDIA-SMI output naming an RTX 5090 device.
```

### 5. tools install

Install asdf and the pinned toolchain (`.tool-versions` carries the line:
`kubectl 1.35.0`, `helm 3.20.2`, `flux2 2.8.6`, `k3d 5.8.3`, `task 3.50.0`,
plus `gitleaks 8.30.1` for the pre-commit hook):

```bash
bash scripts/install-tools.sh
source ~/.bashrc            # picks up the new asdf shim path
```

This step also wires the local git pre-commit hook (REPO-04) by setting
`git config --local core.hooksPath hooks` — see
[Repo Hygiene & CI](#repo-hygiene--ci) below.

Verify with `task preflight` (read-only, idempotent):

```bash
task preflight              # expected: exits 0 with all green checks
```

### 6. cluster creation

Build the custom k3s+CUDA image (cached across rebuilds), then create the
three clusters on the shared `k8s-net` Docker bridge:

```bash
cp .env.example .env        # populate GITHUB_OWNER, GITHUB_REPO, GITHUB_TOKEN
task build-image
task create-clusters
```

`hub-flux` lands on API port 6443, `spoke-ml` on 6444 (`--gpus all`),
`spoke-apps` on 6445. Topology is rendered in [`docs/architecture.md`](docs/architecture.md).

### 7. Flux bootstrap

Bootstrap Flux GitOps on `hub-flux`:

```bash
task bootstrap-flux
```

This runs `flux bootstrap github --path clusters/hub-flux` (the path is
hard-coded — see [`docs/flux-hub-spoke.md`](docs/flux-hub-spoke.md) for the
patch-surface contract).

### 8. spoke registration

Register the two spokes for hub-only Flux reconciliation:

```bash
task register-spokes
```

Provisions a `flux-reconciler` ServiceAccount + cluster-admin
ClusterRoleBinding on each spoke, embeds the kubeconfig (server + CA + token)
into a `flux-system/<spoke>-kubeconfig` Secret on the hub under key
`value.yaml`, and authors hub-side `Kustomization` resources with explicit
`spec.kubeConfig.secretRef`. Rationale: [`docs/adr/0004-hub-only-flux-control-plane.md`](docs/adr/0004-hub-only-flux-control-plane.md).

### 9. example deploy

Deploy podinfo and a GPU smoke-test:

```bash
task deploy-examples
task health-check
```

Manifests live under `examples/podinfo/` and `examples/gpu-smoke-test/`;
they reconcile through Flux into their target spokes. `task health-check`
verifies clusters, controllers, Kustomizations, in-hub DNS, GPU capacity,
TLS SANs, and the workload proofs.

### 10. teardown

Either remove just the clusters (preserves the CUDA image cache for the
next `task rebuild`):

```bash
task destroy                # type "yes" to confirm
```

…or do the full destructive rebuild loop (preserves the CUDA cache, then
exercises the entire chain end-to-end):

```bash
task rebuild                # type "yes" to confirm
```

***

## Per-Task Reference

Each task is a thin wrapper around a script under `scripts/`. All logic
lives in the scripts; `Taskfile.yml` is the orchestration surface.

| Task | Script | What it does | REQ-IDs |
|------|--------|--------------|---------|
| `preflight` | `scripts/preflight.sh` | Read-only environment check; idempotent; exits 0 / 1 | PRE-01..14 |
| `build-image` | `scripts/build-image.sh` | Build the custom k3s+CUDA image (Ubuntu 22.04 + k3s + nvidia-device-plugin) | IMG-01..06 |
| `create-clusters` | `scripts/create-clusters.sh` | Idempotently create `k8s-net` + three k3d clusters | CLU-01..05 |
| `delete-clusters` | `scripts/delete-clusters.sh` | Remove clusters + `k8s-net`, preserve `karyon/k3s-cuda` image | CLU-06, IMG-06 |
| `bootstrap-flux` | `scripts/bootstrap-flux.sh` | Bootstrap Flux GitOps on `hub-flux` | FLUX-01..05 |
| `register-spokes` | `scripts/register-spokes-for-flux.sh` | Provision spoke kubeconfig Secrets + hub-side Kustomizations | SPOKE-01..06 |
| `deploy-examples` | `scripts/deploy-examples.sh` | Reconcile podinfo (apps) + GPU smoke-test (ml) | DEP-01..04 |
| `fix-dns` | `scripts/fix-coredns.sh` | Stop/start `hub-flux` to refresh stale CoreDNS NodeHosts | HEALTH-07 |
| `health-check` | `scripts/health-check.sh` | Verify clusters, Flux, spokes, DNS, GPU capacity, TLS SANs | HEALTH-01..06 |
| `destroy` | `scripts/destroy.sh` | Confirm + remove clusters + `k8s-net`; CUDA cache preserved | DESTROY-01..04 |
| `rebuild` | `scripts/rebuild.sh` | Full destroy → create → bootstrap → register → deploy → health-check; < 20 min SLO | DESTROY-05..06 |

`task --list` shows all tasks; `task --dry <name>` previews the script
invocation without running it.

***

## Why These Choices

Architectural decisions are recorded in [`docs/adr/`](docs/adr/):

- **Single-WSL2 + shared Docker network** — [`adr/0001-single-wsl2-shared-docker-network.md`](docs/adr/0001-single-wsl2-shared-docker-network.md)
- **k3d over Kind** — [`adr/0002-k3d-over-kind.md`](docs/adr/0002-k3d-over-kind.md)
- **Flux over ArgoCD (with ApplicationSet trade-off)** — [`adr/0003-flux-over-argocd.md`](docs/adr/0003-flux-over-argocd.md)
- **Hub-only Flux control plane** — [`adr/0004-hub-only-flux-control-plane.md`](docs/adr/0004-hub-only-flux-control-plane.md)
- **k3s `v1.34.6-k3s1` + CUDA 12.8+ pins** — [`adr/0005-kubernetes-version-pin.md`](docs/adr/0005-kubernetes-version-pin.md)

For deeper reference:

- Topology diagram: [`docs/architecture.md`](docs/architecture.md)
- 3-part k3d GPU contract + Blackwell floor: [`docs/gpu-notes.md`](docs/gpu-notes.md)
- Flux patch-surface contract: [`docs/flux-hub-spoke.md`](docs/flux-hub-spoke.md)
- WSL2 networking modes (mirrored vs NAT): [`docs/wsl-networking.md`](docs/wsl-networking.md)
- Rebuild procedure + timings + failure-mode appendix: [`docs/rebuild-runbook.md`](docs/rebuild-runbook.md)

***

## Repo Hygiene & CI

This is a public-safe repository. No real tokens, kubeconfigs, CA keys, or
client certs are committed. The substitution contract is the gitignored
`.env` populated locally from the committed `.env.example` template.

A `gitleaks` pre-commit hook (configured by
`bash scripts/install-tools.sh`, wired through `git config --local
core.hooksPath hooks`) blocks commits that would leak secrets. A GitHub
Actions workflow ([`.github/workflows/ci.yml`](.github/workflows/ci.yml))
runs the same gitleaks scan plus shellcheck, kubeconform, markdownlint, and
the existing prune-lint bats test on every push and pull-request.

### One-time branch-protection setup

After CI runs green at least once on `main`, configure branch protection:

1. GitHub repo → Settings → Branches → Add branch protection rule
2. Branch name pattern: `main`
3. ☑ Require status checks to pass before merging
4. ☑ Require branches to be up to date before merging
5. Status checks required: `shellcheck`, `markdownlint`, `kubeconform`,
   `prune-lint-bats`, `gitleaks-scan`
6. ☑ Do not allow bypassing the above settings (recommended)

***

## Project Surface

- `scripts/` — bash automation (idempotent, fail-fast, source `lib/preflight-lib.sh` for shared helpers)
- `images/` — `Dockerfile.k3s-cuda` + `config.toml.tmpl` + the `nvidia-device-plugin` manifest
- `clusters/hub-flux/` — Flux bootstrap manifests + spoke `Kustomization` resources
- `clusters/spoke-{ml,apps}/` — per-spoke Kustomize trees (namespace, configmap, etc.)
- `examples/` — workload manifests reconciled into spokes (podinfo, gpu-smoke-test)
- `docs/` — reference docs (this README + the files cross-linked above) and ADRs
- `tests/bats/` — bats-core static and live tests; live tests gated by `KARYON_LIVE_TESTS=1`
- `Taskfile.yml` — the single orchestration surface; runs scripts, never inline logic
- `hooks/` — git hooks (currently `pre-commit` for gitleaks; wired via `core.hooksPath`)
- `.planning/` — the GSD v1 planning state (intentionally tracked; NOT a build artifact)

***

## License

(TODO: add a LICENSE file when the project ships v1.)
```

CRITICAL ordering: the 10 keywords (`Windows prereqs`, `WSL config`,
`Docker install`, `NVIDIA setup`, `tools install`, `cluster creation`,
`Flux bootstrap`, `spoke registration`, `example deploy`, `teardown`) MUST
each appear in a section heading IN THAT ORDER. The bats test
`docs-01-readme-order.bats` greps line numbers and verifies monotonic
ordering. Re-read tests/bats/docs-01-readme-order.bats after writing the
README to confirm each literal is present.
  </action>
  <verify>
    <automated>bats tests/bats/docs-01-readme-order.bats</automated>
  </verify>
  <acceptance_criteria>
    - `[ -f README.md ]` true
    - `head -1 README.md` matches `^# ` (MD041 — first line is h1)
    - The 10 fresh-clone path keywords each appear in the file: for k in 'Windows prereqs' 'WSL config' 'Docker install' 'NVIDIA setup' 'tools install' 'cluster creation' 'Flux bootstrap' 'spoke registration' 'example deploy' 'teardown'; do `grep -niF "$k" README.md` exits 0
    - The 10 keywords appear IN ORDER: ordered-literal grep idiom matches monotonically increasing line numbers
    - All 5 docs cross-links present: `docs/architecture.md`, `docs/gpu-notes.md`, `docs/rebuild-runbook.md`, `docs/flux-hub-spoke.md`, `docs/wsl-networking.md`
    - All 5 ADR cross-links present: `docs/adr/0001-single-wsl2-shared-docker-network.md`, `docs/adr/0002-k3d-over-kind.md`, `docs/adr/0003-flux-over-argocd.md`, `docs/adr/0004-hub-only-flux-control-plane.md`, `docs/adr/0005-kubernetes-version-pin.md`
    - TL;DR section present BEFORE the first install step (line of TL;DR < line of "Windows prereqs")
    - `wc -l < README.md` returns >= 90
    - Per-task reference table lists all 11 Taskfile tasks (each task name from `yq '.tasks | keys[]' Taskfile.yml` appears in README.md)
    - `bats tests/bats/docs-01-readme-order.bats` is fully green
    - NO dangling reference to the to-be-deleted `prereqs.sh` (negative grep): `grep -F 'prereqs.sh' README.md` exits NON-zero (D-13 cleanup) — the README references `task preflight` / `bash scripts/preflight.sh` going forward
  </acceptance_criteria>
  <done>README.md committed; bats `DOCS-01` tests fully green; all cross-references resolve to real files (which exist after Plans 01-04 land).</done>
</task>

<task type="auto">
  <name>Task 2: Delete prereqs.sh and starter.md from repo root (D-13 + D-14)</name>
  <files>prereqs.sh, starter.md</files>
  <read_first>
    - .planning/phases/06-repo-hygiene-docs-adrs/06-CONTEXT.md `<decisions>` D-13 + D-14 (verbatim cleanup mandates)
    - .planning/phases/06-repo-hygiene-docs-adrs/06-RESEARCH.md §Runtime State Inventory lines 706-720 (confirms no scripts reference prereqs.sh / starter.md by name)
    - README.md (Task 1 output — confirm it references `task preflight` / `bash scripts/preflight.sh`, NEVER `prereqs.sh`)
  </read_first>
  <action>
**Step 1 — Verify no other tracked file references the to-be-deleted files**:

```bash
git grep -l 'prereqs\.sh' -- ':!.planning/' ':!docs/'   # expected: only the file itself, if any
git grep -l 'starter\.md' -- ':!.planning/' ':!docs/'   # expected: only the file itself, if any
```

(Grep excludes `.planning/` because historical CONTEXT.md / RESEARCH.md
references are intentional — they document why the file was deleted. Excludes
`docs/` for the same reason — ADRs may reference the historical content.)

If any non-`.planning` non-`docs` file still references either path, STOP and
flag the dependency. The likely candidates would be the README (already
checked in Task 1's negative grep) or a script that hasn't been refactored.

**Step 2 — Verify scripts/preflight.sh exists** (mandatory before deleting `prereqs.sh`):

```bash
[ -f scripts/preflight.sh ]    # MUST be true — D-13 supersession requires the replacement to exist
[ -x scripts/preflight.sh ]    # MUST be true
```

**Step 3 — `git rm` both files**:

```bash
git rm prereqs.sh
git rm starter.md
```

(Use `git rm`, not `rm`, so the deletion is staged for the commit.)

**Step 4 — Verify the deletion took effect**:

```bash
[ ! -f prereqs.sh ]    # MUST be true
[ ! -f starter.md ]    # MUST be true
git status --porcelain | grep '^D ' | wc -l   # expected: 2
```

**Step 5 — Confirm the existing prune-lint bats test still passes** (sanity
check — `prereqs.sh` was never under `scripts/`, so `tests/bats/destroy-rebuild-01-static.bats:148-200` should not be affected, but verify):

```bash
bats tests/bats/destroy-rebuild-01-static.bats   # all green
```

**Step 6 — Update `.planning/STATE.md` reference** if it still names
`prereqs.sh` as a "starting point" for any phase (it does in row 80: "Phase 1:
Port existing `prereqs.sh` as `scripts/preflight.sh`..."). DO NOT modify
STATE.md in this plan — that is a historical record. If a developer reading
STATE.md later wonders why `prereqs.sh` is gone, they will find this plan's
summary and `git log -- prereqs.sh` will show the full deletion history.
  </action>
  <verify>
    <automated>bash -c '[ ! -f prereqs.sh ] && [ ! -f starter.md ]'</automated>
  </verify>
  <acceptance_criteria>
    - `[ ! -f prereqs.sh ]` true
    - `[ ! -f starter.md ]` true
    - `git status --porcelain | grep -F 'D  prereqs.sh'` exits 0 (file staged for deletion)
    - `git status --porcelain | grep -F 'D  starter.md'` exits 0 (file staged for deletion)
    - `grep -RF 'prereqs.sh' --include='*.sh' --include='*.yml' --include='*.md' --include='Taskfile.yml' --exclude-dir='.planning' --exclude-dir='docs' .` exits NON-zero (no remaining references in working code or top-level docs)
    - `grep -RF 'starter.md' --include='*.sh' --include='*.yml' --include='*.md' --include='Taskfile.yml' --exclude-dir='.planning' --exclude-dir='docs' .` exits NON-zero
    - `[ -f scripts/preflight.sh ]` true (replacement still exists)
    - `bats tests/bats/destroy-rebuild-01-static.bats` is green (regression check)
  </acceptance_criteria>
  <done>prereqs.sh and starter.md removed from working tree and staged for deletion; the cleanup is committed as part of this plan; no stale references remain in working code or top-level docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan ships a public-facing README (the first file every reader sees) and removes two superseded entry-point files. The README itself contains no secret material — placeholder strings only. Pre-commit hook from Plan 01 gates the commit.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (n/a) | (n/a) | README.md | accept | No application surface. Plan 01 gitleaks pre-commit hook would catch any accidentally-embedded token (e.g., a real PAT in the .env.example walkthrough section). The README references `ghp_REPLACE_WITH_PAT_WITH_repo_SCOPE` only by reference (link to .env.example), not by literal — no false-positive risk. |
| (n/a) | (n/a) | prereqs.sh / starter.md deletion | accept | `git log -- <path>` retains historical content. The deletion is reversible via `git checkout <SHA>~1 -- <path>`. No live system depends on these files (verified by grep in Task 2 Step 1). |
</threat_model>

<verification>
```bash
bats tests/bats/docs-01-readme-order.bats        # all DOCS-01 tests green
bats tests/bats/destroy-rebuild-01-static.bats   # regression: prune-lint test still green

[ ! -f prereqs.sh ]         # gone
[ ! -f starter.md ]         # gone
[ -f README.md ]            # present
[ -f scripts/preflight.sh ] # replacement still in place

# Manual: render README on GitHub UI / local previewer; confirm cross-links resolve.
```

Manual:
- Open `README.md` in `glow` or GitHub UI; confirm the 10 walkthrough sections
  render in order with proper headings, and every cross-link resolves to a
  real file (after Wave 2 + Plan 06 land for `docs/rebuild-runbook.md`).
- The `git status` output shows: 1 new file (README.md), 2 deletions
  (prereqs.sh, starter.md). Nothing else from this plan.
</verification>

<success_criteria>
- README.md committed at the repo root.
- prereqs.sh and starter.md deleted (`git rm`).
- bats `DOCS-01` tests fully green (10 keywords in order; all docs/ADR cross-links present).
- Regression: `bats tests/bats/destroy-rebuild-01-static.bats` still green.
- README.md does NOT reference `prereqs.sh` or `starter.md` (negative grep — those paths are gone).
- No working code (under `scripts/`, `Taskfile.yml`, etc.) references the deleted files.
- Plan committed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-repo-hygiene-docs-adrs/06-05-SUMMARY.md` recording:
- README.md line count + cross-link inventory
- 2 file deletions (prereqs.sh, starter.md)
- bats output confirming DOCS-01 + regression checks all green
- Note that `git log -- prereqs.sh` and `git log -- starter.md` retain historical content for any future archeology
</output>
