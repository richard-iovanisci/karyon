---
phase: 06-repo-hygiene-docs-adrs
plan: 02
type: execute
wave: 2
depends_on: [00, 01]
files_modified:
  - docs/adr/0002-k3d-over-kind.md
  - docs/adr/0003-flux-over-argocd.md
  - docs/adr/0004-hub-only-flux-control-plane.md
  - docs/adr/0005-kubernetes-version-pin.md
autonomous: true
requirements:
  - DOCS-07
  - DOCS-08
  - DOCS-09
  - DOCS-10
requirements_addressed:
  - DOCS-07
  - DOCS-08
  - DOCS-09
  - DOCS-10
tags:
  - phase-6
  - wave-2
  - adrs
must_haves:
  truths:
    - "DOCS-07 (D-02): docs/adr/0002-k3d-over-kind.md captures the locked k3d-over-Kind decision in the Nygard 4-section template; Status: Accepted."
    - "DOCS-08 (D-02): docs/adr/0003-flux-over-argocd.md captures the locked Flux-over-ArgoCD decision INCLUDING the ApplicationSet trade-off and the migration note (DOCS-08 SC explicit)."
    - "DOCS-09 (D-02): docs/adr/0004-hub-only-flux-control-plane.md captures the hub-only control plane decision and the spec.kubeConfig reconciliation pattern that Phase 4 validated."
    - "DOCS-10 (D-02 + specifics): docs/adr/0005-kubernetes-version-pin.md captures k3s v1.34.6-k3s1 + CUDA 12.8+ pins, EXPLICITLY notes the 'current line' framing (NOT LTS), and notes the CUDA 12.8+ floor is forced by RTX 5090 sm_120 Blackwell."
    - "All four ADRs use the locked filename slugs (D-02 + ROADMAP) — slugs are non-negotiable; downstream README cross-links rely on them."
    - "Each ADR's content draws verbatim decision wording from PROJECT.md 'Key Decisions' — no paraphrasing of the locked rationale, only synthesis into the Nygard template."
  artifacts:
    - path: "docs/adr/0002-k3d-over-kind.md"
      provides: "ADR-002: k3d-over-Kind decision"
      contains: "## Decision"
      min_lines: 30
    - path: "docs/adr/0003-flux-over-argocd.md"
      provides: "ADR-003: Flux-over-ArgoCD with ApplicationSet trade-off + migration note"
      contains: "ApplicationSet"
      min_lines: 35
    - path: "docs/adr/0004-hub-only-flux-control-plane.md"
      provides: "ADR-004: hub-only Flux + spec.kubeConfig reconciliation pattern"
      contains: "spec.kubeConfig"
      min_lines: 30
    - path: "docs/adr/0005-kubernetes-version-pin.md"
      provides: "ADR-005: k3s v1.34.6-k3s1 + CUDA 12.8+ pins"
      contains: "v1.34.6-k3s1"
      min_lines: 30
  key_links:
    - from: "all 4 ADRs"
      to: ".planning/PROJECT.md \"Key Decisions\" section"
      via: "verbatim decision wording"
      pattern: "(k3d|Flux|hub-only|v1\\.34\\.6-k3s1)"
    - from: "docs/adr/0004-hub-only-flux-control-plane.md"
      to: "docs/flux-hub-spoke.md (DOCS-03 ✅)"
      via: "cross-link in Consequences section"
      pattern: "flux-hub-spoke\\.md"
---

<objective>
Author the four remaining v1 Architecture Decision Records using the locked
Nygard 4-section template (D-02): Status / Context / Decision / Consequences.
Decisions are already locked in PROJECT.md "Key Decisions"; this plan converts
them into the on-disk ADR artifacts of record. ADR-001 already shipped in
Plan 01.

Purpose: DOCS-07/08/09/10. Decisions in PROJECT.md are records of fact;
ADRs are records of intent that travel with the code. Every ADR cross-links
to PROJECT.md (the canonical record) and links from the Phase 6 README to
the ADRs (Plan 05).

Output: 4 NEW Markdown files under docs/adr/ with locked slugs; bats
docs-adr-template covers each.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-repo-hygiene-docs-adrs/06-CONTEXT.md
@.planning/phases/06-repo-hygiene-docs-adrs/06-RESEARCH.md
@.planning/phases/06-repo-hygiene-docs-adrs/06-PATTERNS.md
@.planning/phases/02-cluster-layer/02-CONTEXT.md
@.planning/phases/04-spoke-registration/04-CONTEXT.md
@docs/flux-hub-spoke.md
@docs/adr/0001-single-wsl2-shared-docker-network.md

<interfaces>
<!-- Nygard 4-section ADR template (D-02; from 06-RESEARCH.md §Pattern 3 lines 552-591):

# N. <Title Case Title>

## Status

Accepted (YYYY-MM-DD)   <!-- D-02 default -->

## Context

<problem statement, candidates, why it matters>

## Decision

<the locked decision, copied from PROJECT.md "Key Decisions" verbatim where possible>

## Consequences

**Easier:**
- bullet 1
- bullet 2

**Harder:**
- bullet 1
- bullet 2

**No-change:**
- bullet 1
- bullet 2

  Conventions:
  - h1 = `# N. <Title>` (N is the ADR number stripped of leading zeros: 2, 3, 4, 5)
  - exactly four sections (## Status, ## Context, ## Decision, ## Consequences)
  - no "Considered Options", no MADR pros/cons (D-02 explicit)
  - status default = `Accepted (2026-04-22)` (the date PROJECT.md was last
    updated for v0.18; researcher confirmed this matches the locked-decision
    timestamp)
  - cross-links via relative paths: `../flux-hub-spoke.md`, `../wsl-networking.md`, `0001-...md`
-->

<!-- PROJECT.md "Key Decisions" verbatim (lines 94-108):

  - Single WSL2 Ubuntu 24.04 distro for all clusters → ADR-001 (shipped Plan 01)
  - Docker Engine installed natively in WSL → ADR-001 (shipped Plan 01)
  - k3d over Kind → ADR-002 (this plan)
  - Flux over ArgoCD → ADR-003 (this plan; trade-off explicit)
  - Hub-only Flux control plane (v1) → ADR-004 (this plan)
  - Pin k3s to rancher/k3s:v1.34.6-k3s1; pin CUDA base to 12.8+ → ADR-005 (this plan)
-->

<!-- Phase 4 P18 silent-misroute defense (from REQUIREMENTS.md SPOKE-04..05 +
04-CONTEXT.md): Hub-only Flux uses spec.kubeConfig.secretRef pointing at a
flux-system Secret named `<spoke>-kubeconfig` with key `value.yaml`. Omitting
spec.kubeConfig falls back silently to in-cluster SA → wrong cluster. ADR-004
captures this. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ADR-002 (k3d over Kind) and ADR-005 (Kubernetes version pin)</name>
  <files>docs/adr/0002-k3d-over-kind.md, docs/adr/0005-kubernetes-version-pin.md</files>
  <read_first>
    - .planning/PROJECT.md "Key Decisions" rows 3 and 6 (k3d + version pin — verbatim source)
    - .planning/REQUIREMENTS.md §CUDA Image (IMG-01..06) and §Cluster Layer (CLU-01..06) — pin justifications
    - .planning/phases/02-cluster-layer/02-CONTEXT.md (D-13 SAN verify, D-14 cache-preservation, D-15 device-plugin v0.17.4 override — context for both ADRs)
    - .planning/phases/06-repo-hygiene-docs-adrs/06-RESEARCH.md §Pattern 3 lines 552-591 (k3d-over-Kind exemplar verbatim — copy structure, adapt content)
    - .planning/phases/06-repo-hygiene-docs-adrs/06-CONTEXT.md `<specifics>` 416-417 (ADR-005 must explicitly note 'current line' pin and CUDA 12.8+ floor forced by RTX 5090 sm_120 Blackwell)
    - docs/adr/0001-single-wsl2-shared-docker-network.md (Plan 01 output — analog for tone, length, cross-link conventions)
  </read_first>
  <action>
**Step 1 — Create `docs/adr/0002-k3d-over-kind.md`** (DOCS-07).

Use `Write`. Content (Nygard 4-section template; structure adapted verbatim from 06-RESEARCH.md §Pattern 3 lines 552-591):

```markdown
# 2. k3d Over Kind

## Status

Accepted (2026-04-22)

## Context

This is a local Kubernetes lab on Windows 11 + WSL2 with an RTX 5090 GPU. The
cluster runtime needs to support: GPU pass-through, multi-cluster on a shared
Docker network, explicit network control for cross-cluster reachability, and
rebuild-from-zero in under 20 minutes. The two main candidates are k3d
(k3s-on-Docker) and Kind (Kubernetes-in-Docker).

Both are local-only Docker-based runtimes; both are well-supported by the
Kubernetes community. They differ on three dimensions that matter to this
lab: GPU pass-through, idle resource overhead, and explicit network control
for the hub-spoke topology described in ADR-004.

## Decision

Use k3d for all three clusters in the lab (`hub-flux`, `spoke-ml`,
`spoke-apps`).

## Consequences

**Easier:**
- Official GPU support via the `--gpus all` flag at cluster create time. Kind
  has no first-class GPU integration; the workaround paths are fragile and
  break on RTX 5090 / sm_120.
- Lower idle RAM per cluster (~250 MB for k3s vs ~500 MB for upstream
  Kubernetes control plane). Three clusters on one dev box benefit
  proportionally.
- Explicit `--network k8s-net` keeps all three clusters on a shared Docker
  bridge — required for the hub-only Flux topology where `kustomize-controller`
  on `hub-flux` reaches `spoke-ml-server-0` and `spoke-apps-server-0` by
  Docker embedded DNS hostnames (see `0004-hub-only-flux-control-plane.md`).
- The `--k3s-arg '--tls-san=...'` pattern lets us pin TLS SANs at create time
  to those Docker DNS hostnames. Kind's equivalent (`kubeadmConfigPatches`)
  is more verbose and harder to retrofit.
- Ships a Helm controller-equivalent (Flux's `helm-controller`) layered cleanly
  on top; k3d does not embed Helm 2 / Tiller historical baggage.

**Harder:**
- k3s has a smaller community than upstream Kubernetes; some upstream tools
  assume vanilla `kubectl` semantics that k3s deviates from in subtle ways
  (none surfaced for v1, but a future risk).
- The custom `karyon/k3s-cuda` image (Phase 2 IMG-01..06) is more complex to
  maintain than a stock Kind image — the 3-part k3d GPU contract
  (`/etc/docker/daemon.json` `default-runtime: nvidia` + Ubuntu 22.04 base
  image + containerd `config.toml.tmpl` setting `default_runtime_name = "nvidia"`)
  is lab-specific. See `../gpu-notes.md`.

**No-change:**
- Both runtimes are local-only. Cloud cluster migration (EKS / GKE / AKS) is
  explicitly out of scope (see REQUIREMENTS.md "Out of Scope").
- Both runtimes are dev-box-only; production hardening is out of scope.
```

**Step 2 — Create `docs/adr/0005-kubernetes-version-pin.md`** (DOCS-10).

CRITICAL content requirements per CONTEXT.md `<specifics>` 416-417:
- MUST explicitly note `v1.34.6-k3s1` is a "current line" pin (NOT LTS).
- MUST note the CUDA 12.8+ floor is forced by RTX 5090 sm_120 Blackwell.

```markdown
# 5. Kubernetes Version Pin (k3s + CUDA)

## Status

Accepted (2026-04-22)

## Context

Two version axes pin together for the karyon lab: the k3s line that runs in
every cluster and the CUDA base image used for the GPU smoke-test on
`spoke-ml`. Both must be reproducible across rebuilds; both have constraints
imposed by the dev box's hardware (RTX 5090 / sm_120 Blackwell).

The k3s pin governs Kubernetes API surface, k3s subsystems (CoreDNS, Traefik,
local-path-provisioner, etc.), and image-tag stability. The CUDA pin governs
which compiled kernels (`sm_*` sets) the smoke-test container can dispatch.

There are two flavors of pin available for k3s: an LTS line (e.g.,
`rancher/k3s:vX.Y.<patch>-k3s1` against the current Kubernetes LTS) and a
"current line" pin (the latest minor that the project is shipping toolchain
support against). For CUDA, NVIDIA's RTX 5090 (Blackwell, sm_120) requires
CUDA 12.8 or newer at the runtime; nothing earlier supports compiled kernels
for sm_120.

## Decision

1. Pin k3s to `rancher/k3s:v1.34.6-k3s1` (Kubernetes 1.34 line, k3s patch 1).
   This is a "current line" pin — NOT LTS. The line tracks current upstream
   Kubernetes, which is the line for which Flux v2.8.x and the helm-controller
   image set ship maintained tooling.
2. Pin the CUDA base image used by the GPU smoke-test to
   `nvcr.io/nvidia/cuda:12.8.0-base-ubuntu22.04`. The custom k3s+CUDA image
   used by `spoke-ml` is FROM `nvidia/cuda:12.8.1-base-ubuntu22.04` (a
   minor-revision newer base; Phase 2 IMG-01).
3. Both pins are checked-in: k3s line in `.tool-versions`-derived references
   and `scripts/create-clusters.sh`; CUDA base in `images/Dockerfile.k3s-cuda`
   and `examples/gpu-smoke-test/job.yaml`.

## Consequences

**Easier:**
- Reproducible cluster behavior across rebuilds; `task rebuild` lands the
  same image every time.
- Flux compatibility is guaranteed for the duration of v1 (Flux v2.8.x ships
  for Kubernetes 1.30..1.35 — the pinned line is comfortably mid-window).
- The CUDA 12.8+ floor unblocks RTX 5090 sm_120 Blackwell GPU support — the
  smoke-test pod's `nvidia-smi` output names a CUDA 12.8 device, which is
  the lowest version that can host sm_120 kernels (REQUIREMENTS.md DEP-03).

**Harder:**
- "Current line" pins receive no upstream LTS support. When 1.34 approaches
  EOL (~12-18 months out), the pin must be bumped to whatever the current
  line is at that time. Tracked as `KUBE-01` in v2 REQUIREMENTS.md (policy
  for bumping the line — trigger, testing steps, migration notes).
- The CUDA 12.8+ floor disqualifies older PyTorch / framework wheels that
  haven't caught up to sm_120 yet. This is why the GPU smoke-test runs only
  `nvidia-smi` (DEP-03) — it avoids coupling the lab's health to PyTorch
  nightly availability for sm_120.
- The nvidia-device-plugin had to be pinned to v0.17.4 instead of
  REQUIREMENTS.md's v0.19.0+ at Phase 2 research time (no stable v0.19.x
  release compatible with Blackwell at that moment). Phase-scoped override
  recorded in PROJECT.md "Key Decisions" + Phase 2 D-15. See
  `../gpu-notes.md`.

**No-change:**
- Helm 3.20.x stays pinned via `.tool-versions`; Helm 4.x migration is out
  of scope (TOOL-01 in v2).
- Cloud-cluster migration is out of scope (REQUIREMENTS.md "Out of Scope");
  the pin is dev-box-bound and not portable to managed Kubernetes.
```
  </action>
  <verify>
    <automated>bats tests/bats/docs-adr-template.bats -f '0002|DOCS-07|0005|DOCS-10'</automated>
  </verify>
  <acceptance_criteria>
    - `[ -f docs/adr/0002-k3d-over-kind.md ]` true
    - `head -1 docs/adr/0002-k3d-over-kind.md` matches `^# 2\.`
    - `grep -F '## Status' docs/adr/0002-k3d-over-kind.md` exits 0
    - `grep -F '## Context' docs/adr/0002-k3d-over-kind.md` exits 0
    - `grep -F '## Decision' docs/adr/0002-k3d-over-kind.md` exits 0
    - `grep -F '## Consequences' docs/adr/0002-k3d-over-kind.md` exits 0
    - `head -10 docs/adr/0002-k3d-over-kind.md | grep -F 'Accepted'` exits 0
    - `grep -iF 'k3d' docs/adr/0002-k3d-over-kind.md` exits 0
    - `grep -iF 'Kind' docs/adr/0002-k3d-over-kind.md` exits 0
    - `grep -F '--gpus all' docs/adr/0002-k3d-over-kind.md` exits 0
    - `wc -l < docs/adr/0002-k3d-over-kind.md` returns >= 30
    - `[ -f docs/adr/0005-kubernetes-version-pin.md ]` true
    - `head -1 docs/adr/0005-kubernetes-version-pin.md` matches `^# 5\.`
    - `grep -F '## Status' docs/adr/0005-kubernetes-version-pin.md` exits 0
    - `grep -F '## Context' docs/adr/0005-kubernetes-version-pin.md` exits 0
    - `grep -F '## Decision' docs/adr/0005-kubernetes-version-pin.md` exits 0
    - `grep -F '## Consequences' docs/adr/0005-kubernetes-version-pin.md` exits 0
    - `head -10 docs/adr/0005-kubernetes-version-pin.md | grep -F 'Accepted'` exits 0
    - `grep -F 'v1.34.6-k3s1' docs/adr/0005-kubernetes-version-pin.md` exits 0
    - `grep -iF 'current line' docs/adr/0005-kubernetes-version-pin.md` exits 0 (CONTEXT.md `<specifics>` mandate)
    - `grep -F 'RTX 5090' docs/adr/0005-kubernetes-version-pin.md` exits 0
    - `grep -F 'sm_120' docs/adr/0005-kubernetes-version-pin.md` exits 0
    - `grep -F '12.8' docs/adr/0005-kubernetes-version-pin.md` exits 0
  </acceptance_criteria>
  <done>ADR-002 and ADR-005 committed; bats `DOCS-07` and `DOCS-10` tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Author ADR-003 (Flux over ArgoCD with ApplicationSet trade-off + migration note) and ADR-004 (Hub-only Flux control plane)</name>
  <files>docs/adr/0003-flux-over-argocd.md, docs/adr/0004-hub-only-flux-control-plane.md</files>
  <read_first>
    - .planning/PROJECT.md "Key Decisions" rows 4 and 5 (Flux+ApplicationSet trade-off + hub-only — verbatim source)
    - .planning/PROJECT.md "Constraints" line 83 (ApplicationSet trade-off explicit)
    - .planning/REQUIREMENTS.md §Spoke Registration (SPOKE-01..06) and §Flux Hub Bootstrap (FLUX-01..05)
    - .planning/phases/04-spoke-registration/04-CONTEXT.md (P18 silent-misroute defense — ADR-004 must capture)
    - .planning/phases/06-repo-hygiene-docs-adrs/06-PATTERNS.md "docs/adr/0001..0005-*.md" section
    - docs/flux-hub-spoke.md (DOCS-03 ✅; ADR-004 cross-links here in Consequences)
    - docs/adr/0001-single-wsl2-shared-docker-network.md (analog for length, tone, cross-link convention)
  </read_first>
  <action>
**Step 1 — Create `docs/adr/0003-flux-over-argocd.md`** (DOCS-08).

CRITICAL content requirements per REQUIREMENTS.md DOCS-08 + PROJECT.md Constraints line 83:
- MUST capture the ApplicationSet trade-off (no clean Flux equivalent for ArgoCD's cluster-generator-with-label-targeting).
- MUST include a migration note (what would change if a future milestone moves to ArgoCD).

```markdown
# 3. Flux Over ArgoCD

## Status

Accepted (2026-04-22)

## Context

The lab needs a GitOps controller stack to reconcile manifests under
`clusters/` and `examples/` into the three k3d clusters. Two well-established
candidates: Flux v2 (CNCF Graduated, controller set: `source-controller`,
`kustomize-controller`, `helm-controller`, `notification-controller`) and
ArgoCD (CNCF Graduated, server + repo-server + application-controller +
applicationset-controller). Both reconcile a Git repo into a Kubernetes
cluster; both support hub-spoke topologies; both have mature Helm + Kustomize
support.

The lab has 2-3 spokes today and a hub-only control plane (see
`0004-hub-only-flux-control-plane.md`). The CRD set installed on the hub
matters because every controller increases the hub's footprint. Spoke
multiplicity is low — a feature like ArgoCD's `ApplicationSet` cluster
generator with label-based targeting (one declaration → many clusters) is
worth less here than a per-spoke explicit Kustomization is.

## Decision

Use Flux v2 as the GitOps controller stack on `hub-flux`. Author 2-3 explicit
hub-side `Kustomization` resources per spoke under `clusters/hub-flux/spokes/`,
each with `spec.kubeConfig` pointing at a kubeconfig Secret in `flux-system`.

## Consequences

**Easier:**
- Smaller controller footprint on the hub: 4 Flux controllers vs ArgoCD's
  full server+UI+repo-server+applicationset-controller stack. Fewer Pods and
  less idle RAM. Pertinent on a single dev box.
- `flux bootstrap github` is a single, idempotent command (FLUX-01..04). No
  cluster-config-only-via-UI gotchas.
- Tighter integration with `kustomize-controller`'s native `spec.kubeConfig`
  remote-cluster reconciliation (the hub-only pattern this lab uses; see
  ADR-004). Direct, no `Application` resource layer.
- The Phase 4 P18 silent-misroute defense (explicit `spec.kubeConfig.secretRef`
  with `key: value.yaml`) is documented in `docs/flux-hub-spoke.md` and
  enforced in `clusters/hub-flux/spokes/spoke-{ml,apps}.yaml`.

**Harder:**
- No clean Flux equivalent of ArgoCD's `ApplicationSet` cluster generator
  with label-based targeting. For 2-3 spokes (the v1 scope), authoring
  per-spoke explicit Kustomizations is acceptable; the lab's current pattern
  is `clusters/hub-flux/spokes/spoke-ml.yaml` + `clusters/hub-flux/spokes/spoke-apps.yaml`.
  At higher spoke counts (~5+), the duplication starts to add real
  maintenance cost.
- No web UI baked in. The lab compensates with `flux check`, `flux get
  kustomizations -A`, `flux logs`, and `task health-check`. A web UI
  (e.g., `weave-gitops`, `headlamp`) can be layered later but is out of
  scope for v1.

**No-change:**
- Both controllers are CNCF Graduated and well-supported. Flux choice is
  reversible at a future milestone if the spoke count grows enough that
  ApplicationSet pays for itself.

## Migration note

If a future milestone (e.g., when the lab grows to 5+ spokes) reverses this
decision, the migration is non-trivial but bounded:

1. Replace `clusters/hub-flux/flux-system/` with the ArgoCD installation
   manifests (likely via Helm chart `argo/argo-cd`).
2. Translate each `Kustomization` under `clusters/hub-flux/spokes/` to an
   `Application` (or, more efficiently, to a single `ApplicationSet` with a
   `clusters` generator targeting all spoke labels).
3. Re-author `register-spokes-for-flux.sh` to provision spokes as ArgoCD
   Cluster registrations (which use a Secret with `type: Opaque` and the
   `argocd.argoproj.io/secret-type: cluster` label) instead of Flux
   kubeconfig Secrets.
4. The `spec.kubeConfig` defense-in-depth pattern (Phase 4 P18) does not
   exist in ArgoCD; the equivalent is the cluster-registration model itself.
   Re-document the silent-misroute defense for ArgoCD before the migration
   ships.
5. The Helm `HelmRelease` resources from `helm-controller` translate to
   `Application` resources with `source.helm` blocks; semantics are
   equivalent for the lab's current scope.

The migration is documented as v2-eligible (not v1 scope); concrete trigger
is "spoke count exceeds 5" or "ApplicationSet semantic features become
load-bearing for new use cases."
```

**Step 2 — Create `docs/adr/0004-hub-only-flux-control-plane.md`** (DOCS-09).

CRITICAL content per REQUIREMENTS.md SPOKE-01..06 + Phase 4 04-CONTEXT.md:
- MUST capture the spec.kubeConfig reconciliation pattern.
- MUST capture the P18 silent-misroute defense (omitted spec.kubeConfig falls back to in-cluster SA → wrong cluster).
- Cross-link to `../flux-hub-spoke.md` (DOCS-03 ✅).

```markdown
# 4. Hub-Only Flux Control Plane

## Status

Accepted (2026-04-22)

## Context

The lab has three k3d clusters: `hub-flux` (control plane home), `spoke-ml`
(GPU workloads), and `spoke-apps` (general-purpose workloads). Two
deployment topologies are common for multi-cluster GitOps:

1. **Per-cluster controllers**: Run a Flux controller stack on every cluster,
   each reconciling a different path of the same Git repo. Independent
   blast radius per cluster; redundant controller footprint per cluster.
2. **Hub-only controllers**: Run Flux on a single hub cluster; have the
   hub's `kustomize-controller` and `helm-controller` reconcile manifests
   into spokes via `spec.kubeConfig` and a remote kubeconfig Secret.

The Phase 4 implementation work (SPOKE-01..06) surfaced one significant
risk in the hub-only pattern: P18 silent-misroute. If a Flux Kustomization
omits `spec.kubeConfig`, the controller falls back to its own in-cluster
ServiceAccount and reconciles the manifests into the HUB cluster (where the
controller runs) while reporting `Ready=True`. The Kustomization appears
green; the workloads land in the wrong cluster.

## Decision

Run Flux only on `hub-flux`. Author hub-side Flux `Kustomization` resources
under `clusters/hub-flux/spokes/<spoke>.yaml` with `spec.kubeConfig.secretRef`
pointing at a kubeconfig Secret in the `flux-system` namespace, key
`value.yaml`. Each spoke's `kubeconfig` Secret is provisioned by
`scripts/register-spokes-for-flux.sh` from a `kubernetes.io/service-account-token`-typed
Secret on the spoke (legacy Secret-backed SA token; not `kubectl create
token` — see SPOKE-02).

## Consequences

**Easier:**
- One controller stack to operate, observe, upgrade, and restart. Fewer
  moving parts overall; lower aggregate idle RAM (helpful on a single
  dev box).
- Spokes carry only their workloads (no GitOps controller footprint, no
  reconcile loops). They can be torn down and recreated without losing
  GitOps state on the hub.
- The `spec.kubeConfig` reconciliation pattern is documented in
  `../flux-hub-spoke.md` (DOCS-03 ✅) — the patch surface and immutability
  rules carry through unchanged from a vanilla Flux install.

**Harder:**
- The P18 silent-misroute risk is real and not visible in `kubectl` output
  alone. Defense in depth requires:
  1. Every spoke Kustomization MUST include `spec.kubeConfig.secretRef.name:
     <spoke>-kubeconfig` AND `spec.kubeConfig.secretRef.key: value.yaml`.
     Both fields are explicit; the key defaults to `value.yaml` but is
     pinned for clarity (Phase 4 SPOKE-04).
  2. Negative-proof tests in `tests/bats/register-spokes-02-live.bats`
     verify that `.status.inventory` of each spoke Kustomization contains
     at least one resource (e.g., a `ConfigMap` named `karyon-spoke-id`)
     that exists ONLY on the spoke and NOT on the hub — proves
     reconciliation landed on the right cluster, not the hub (P18).
  3. The hub's `kustomize-controller` has cluster-admin equivalent on every
     spoke (via the ServiceAccount → ClusterRoleBinding chain in
     SPOKE-01). This is acceptable for lab scope but would not be acceptable
     in production (a v2 concern would be per-namespace RBAC).
- DNS dependence: the hub's `kustomize-controller` reaches each spoke's
  apiserver by Docker embedded DNS hostname (`k3d-<spoke>-server-0`).
  Stale CoreDNS NodeHosts after Docker / WSL restart manifest as
  reconcile failures on the hub side. The lab ships `task fix-dns`
  (HEALTH-07) as the canonical workaround.

**No-change:**
- Per-cluster controllers remain feasible (and may be preferred at higher
  spoke counts). Reversal is bounded: install Flux on each spoke, point
  each spoke's `flux-system` Kustomization at the corresponding
  `clusters/<spoke>/` path, remove the hub-side `clusters/hub-flux/spokes/`
  tree.
- Adopting ArgoCD (see `0003-flux-over-argocd.md` migration note) would
  rework the hub-only model into ArgoCD's cluster-registration pattern;
  the same defense-in-depth principles apply.
```
  </action>
  <verify>
    <automated>bats tests/bats/docs-adr-template.bats -f '0003|DOCS-08|0004|DOCS-09'</automated>
  </verify>
  <acceptance_criteria>
    - `[ -f docs/adr/0003-flux-over-argocd.md ]` true
    - `head -1 docs/adr/0003-flux-over-argocd.md` matches `^# 3\.`
    - `grep -F '## Status' docs/adr/0003-flux-over-argocd.md` exits 0
    - `grep -F '## Context' docs/adr/0003-flux-over-argocd.md` exits 0
    - `grep -F '## Decision' docs/adr/0003-flux-over-argocd.md` exits 0
    - `grep -F '## Consequences' docs/adr/0003-flux-over-argocd.md` exits 0
    - `head -10 docs/adr/0003-flux-over-argocd.md | grep -F 'Accepted'` exits 0
    - `grep -iF 'Flux' docs/adr/0003-flux-over-argocd.md` exits 0
    - `grep -iF 'ArgoCD' docs/adr/0003-flux-over-argocd.md` exits 0
    - `grep -F 'ApplicationSet' docs/adr/0003-flux-over-argocd.md` exits 0 (REQUIREMENTS.md DOCS-08 mandate)
    - `grep -iF 'migration' docs/adr/0003-flux-over-argocd.md` exits 0 (REQUIREMENTS.md DOCS-08 mandate)
    - `wc -l < docs/adr/0003-flux-over-argocd.md` returns >= 35
    - `[ -f docs/adr/0004-hub-only-flux-control-plane.md ]` true
    - `head -1 docs/adr/0004-hub-only-flux-control-plane.md` matches `^# 4\.`
    - `grep -F '## Status' docs/adr/0004-hub-only-flux-control-plane.md` exits 0
    - `grep -F '## Context' docs/adr/0004-hub-only-flux-control-plane.md` exits 0
    - `grep -F '## Decision' docs/adr/0004-hub-only-flux-control-plane.md` exits 0
    - `grep -F '## Consequences' docs/adr/0004-hub-only-flux-control-plane.md` exits 0
    - `head -10 docs/adr/0004-hub-only-flux-control-plane.md | grep -F 'Accepted'` exits 0
    - `grep -iF 'hub' docs/adr/0004-hub-only-flux-control-plane.md` exits 0
    - `grep -F 'spec.kubeConfig' docs/adr/0004-hub-only-flux-control-plane.md` exits 0 (load-bearing pattern)
    - `grep -iF 'controller' docs/adr/0004-hub-only-flux-control-plane.md` exits 0
    - `grep -F 'flux-hub-spoke.md' docs/adr/0004-hub-only-flux-control-plane.md` exits 0 (cross-link to DOCS-03 ✅)
    - All four ADRs (0001..0005 once Plan 01 + this plan land) collectively pass `bats tests/bats/docs-adr-template.bats` (full suite green).
  </acceptance_criteria>
  <done>ADR-003 and ADR-004 committed; bats `DOCS-08` and `DOCS-09` tests green; combined with Plan 01's ADR-001 and Task 1's ADR-002/005, the full `bats tests/bats/docs-adr-template.bats` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a documentation-authoring plan; no application surface, no live services, no secret material handled. Boundary analysis: Markdown files written into `docs/adr/`. Read by humans and by the bats template-coverage test.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (n/a) | (n/a) | ADR markdown files | accept | ADR documents have no application surface. They CAPTURE security decisions made elsewhere (e.g., ADR-004's P18 silent-misroute defense narrative reflects the mitigation already shipped in Phase 4). The pre-commit hook (Plan 01) gates these markdown files like any other commit; if any decision text accidentally embeds a real token / kubeconfig, gitleaks will catch it on commit. |
</threat_model>

<verification>
```bash
# Combined gate for all DOCS-06..10 (1 from Plan 01 + 4 from this plan):
bats tests/bats/docs-adr-template.bats   # expected: all tests green

# Per-ADR spot check:
for adr in docs/adr/000*.md; do
  echo "== $adr =="
  head -1 "$adr"           # expected: # N. <Title>
  grep -c '^## ' "$adr"    # expected: 4 (Status, Context, Decision, Consequences)
done
```

Manual:
- Open each ADR in a Markdown previewer (or GitHub UI). Confirm: 4 sections, no
  obvious typos, prose accurately captures the locked decision.
- Cross-references resolve: `../flux-hub-spoke.md`, `../gpu-notes.md` (the
  latter lands in Plan 03; cross-links from this plan's ADR-002/005 are forward
  references that activate after Wave 2 settles).
</verification>

<success_criteria>
- 4 NEW Markdown files committed under docs/adr/ with locked slugs (0002, 0003, 0004, 0005).
- All four files use the Nygard 4-section template (Status / Context / Decision / Consequences) with `Status: Accepted (2026-04-22)`.
- `bats tests/bats/docs-adr-template.bats` is fully green (all DOCS-06..10 tests pass — combined with Plan 01's ADR-001).
- ADR-003 contains literal `ApplicationSet` and the word `migration` (REQUIREMENTS.md DOCS-08 mandate).
- ADR-005 contains literals `v1.34.6-k3s1`, `current line`, `RTX 5090`, `sm_120`, `12.8` (CONTEXT.md `<specifics>` 416-417 mandate).
- ADR-004 cross-links to `flux-hub-spoke.md` (DOCS-03 ✅) and contains the literal `spec.kubeConfig` (P18 defense narrative).
- Plan committed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-repo-hygiene-docs-adrs/06-02-SUMMARY.md` recording:
- 4 ADRs created with line counts
- Notes on which CONTEXT.md `<specifics>` mandates each ADR satisfied
- bats output confirming all DOCS-07/08/09/10 tests green
</output>
