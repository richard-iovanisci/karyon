---
phase: 08-capsule-capsule-proxy-bare-minimum-install
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified: []
autonomous: true
requirements:
  - CAP-01
  - CAP-02
  - CAP-03

must_haves:
  truths:
    - "Both Flux v2 HelmReleases (capsule:0.12.4 operator + capsule-proxy:0.12.0) reach Ready=True via `flux get helmrelease ... --context k3d-hub-flux` within the reconcile budget (operator: ≤10 min cold start; proxy: ≤5 min after operator)"
    - "capsule-controller-manager pod is Running on spoke-capsule (CAP-01 secondary signal)"
    - "≥7 capsule.clastix.io CRDs registered on spoke-capsule + each of the 7 expected names (tenants, capsuleconfigurations, globaltenantresources, tenantresources, resourcepools, resourcepoolclaims, tenantowners) is present (CAP-03)"
    - "NodePort 30443 reachable from WSL host — TLS handshake succeeds + curl returns HTTP code matching [1-5][0-9][0-9] (CAP-02 per OQ-1 reframe; NOT literal /healthz returns 200)"
    - "ADR-004 invariant preserved — kubectl --context=k3d-spoke-capsule get pods -n flux-system returns no Flux controllers (Domain boundary item #4)"
    - "FOLDED CARRYOVER (Phase 7 verification item 1b) — outer poc-capsule Kustomization reports Ready=True via `flux get kustomization poc-capsule -n flux-system --context k3d-hub-flux` against a post-Phase-7 source revision (NOT 1f2da1d3 close-out commit)"
    - "Phase 7 P31 isolation regression — `tests/bats/poc-isolation-01-static.bats` GREEN (Phase 8's new files under `pocs/capsule/operator/` + `pocs/capsule/proxy/` did NOT introduce `spoke-capsule` mentions in any v0.18 script)"
    - "Phase 7 P40 / P18 invariant regression — `clusters/hub-flux/pocs/capsule.yaml` retains `secretRef.name: spoke-capsule-kubeconfig` AND `key: value.yaml` (`tests/bats/poc-mount-01-static.bats` GREEN)"
    - "If outer `poc-capsule` Kustomization is NOT Ready=True, the verifier surfaces this as a Phase 7 SEAM defect (gap-close to Phase 7 with `/gsd-plan-phase 7 --gaps`), NOT a Phase 8 install defect"
  artifacts: []
  key_links:
    - from: "tests/bats/capsule-install-06-live-helm.bats (Plan 08-00)"
      to: "spoke-capsule cluster + hub-flux helm-controller observation"
      via: "flux get helmrelease capsule + capsule-proxy + kubectl get pod app.kubernetes.io/name=capsule"
      pattern: "Ready=True"
    - from: "tests/bats/capsule-install-08-live-proxy-reachable.bats (Plan 08-00)"
      to: "https://127.0.0.1:30443"
      via: "curl -k -s -o /dev/null -w '%{http_code}' --max-time 5"
      pattern: "^[1-5][0-9][0-9]$"
    - from: "tests/bats/capsule-install-10-live-flux-observable.bats (Plan 08-00)"
      to: "outer Flux Kustomization poc-capsule on hub-flux"
      via: "flux get kustomization poc-capsule -n flux-system --context k3d-hub-flux"
      pattern: "poc-capsule.*True"
    - from: "tests/bats/poc-isolation-01-static.bats (Phase 7 — UNCHANGED)"
      to: "scripts/{create-clusters,register-spokes-for-flux,health-check,destroy,delete-clusters,rebuild}.sh"
      via: "comment-filtered grep for spoke-capsule"
      pattern: "grep -v '^[[:space:]]*#'"
---

<objective>
Verify Phase 8 install end-to-end against the live spoke-capsule cluster + hub-flux. Plan 08-03 is a verifier-only plan: it modifies NO production files and authors NO new bats files. It runs the 5 live bats authored by Plan 08-00 (`capsule-install-{06..10}-live-*.bats`) plus reruns the Phase 7 static regression bats verbatim, then surfaces a Phase 7 SEAM defect distinction if the outer `poc-capsule` Kustomization is NOT Ready=True.

Purpose: Close the FOLDED CARRYOVER (Phase 7 verification item 1b — hub-Flux observable reconcile of `poc-capsule` Kustomization). Confirm CAP-01/CAP-02/CAP-03 live signals AND ADR-004 invariant. The Pitfall 8 reframe (CAP-02 reachability via HTTP `[1-5][0-9][0-9]`, NOT literal /healthz returns 200) was encoded in Plan 08-00's bats; Plan 08-03 just executes them.

Output: Live verification log surfaced in 08-03-SUMMARY.md. NO files created. NO yaml modified. The verifier discriminates between three failure modes:
  1. Phase 8 install defect (HelmRelease Ready=False with chart-related error) → gap-close `/gsd-plan-phase 8 --gaps`
  2. Phase 7 SEAM defect (outer `poc-capsule` Ready=False, GitRepository revision stuck on `1f2da1d3`) → gap-close `/gsd-plan-phase 7 --gaps`
  3. Push gate not satisfied (origin/main behind local main; Phase 11 VAL-05 owns) → surface as "carryover-deferred", NOT a Phase 8 fail

Plan 08-03 is NOT autonomous in the sense of "Claude does this with no prerequisite live state" — it presupposes the milestone owner has (a) raised inotify limit per Phase 7 Human Verification §1 (`sudo sysctl -w fs.inotify.max_user_instances=1024`), (b) run `bash scripts/poc/capsule/create-cluster.sh` + `bash scripts/register-poc-cluster.sh spoke-capsule` to bring spoke-capsule + the kubeconfig Secret up, AND (c) pushed Plan 08-01 + Plan 08-02 commits to origin/main so hub-flux's GitRepository sees them. If any precondition is unmet, the live bats `skip` cleanly with a clear reason.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-capsule-capsule-proxy-bare-minimum-install/08-CONTEXT.md
@.planning/phases/08-capsule-capsule-proxy-bare-minimum-install/08-RESEARCH.md
@.planning/phases/08-capsule-capsule-proxy-bare-minimum-install/08-PATTERNS.md
@.planning/phases/08-capsule-capsule-proxy-bare-minimum-install/08-VALIDATION.md
@.planning/phases/08-capsule-capsule-proxy-bare-minimum-install/08-00-PLAN.md
@.planning/phases/08-capsule-capsule-proxy-bare-minimum-install/08-01-PLAN.md
@.planning/phases/08-capsule-capsule-proxy-bare-minimum-install/08-02-PLAN.md

# Phase 7 verification status — folded carryover details:
@.planning/phases/07-foundation-poc-seam-spoke-capsule-cluster-eks-capsule-doc/07-VERIFICATION.md
@.planning/todos/pending/2026-04-29-phase-7-hub-flux-observable-reconcile.md

# Live bats this plan executes (created by Plan 08-00):
@tests/bats/capsule-install-06-live-helm.bats
@tests/bats/capsule-install-07-live-crds.bats
@tests/bats/capsule-install-08-live-proxy-reachable.bats
@tests/bats/capsule-install-09-live-adr-004.bats
@tests/bats/capsule-install-10-live-flux-observable.bats

# Phase 7 regression bats — MUST stay 100% GREEN (rerun verbatim):
@tests/bats/poc-mount-01-static.bats
@tests/bats/poc-isolation-01-static.bats
@tests/bats/poc-cluster-01-static.bats

# v0.18 invariant regression — full project suite must stay GREEN:
@tests/bats/

<interfaces>
<!-- Flux CLI invocations used by live bats (verified 2026-04-29 against flux v2.8.6) -->
flux get helmrelease <name> -n <namespace> --context <ctx>
  Output columns: NAME  REVISION  SUSPENDED  READY  MESSAGE
  Ready=True parsing: grep -qE '<name>[[:space:]]+.*True'

flux get kustomization <name> -n <namespace> --context <ctx>
  Output columns: NAME  REVISION  SUSPENDED  READY  MESSAGE
  Same parsing as helmrelease

flux get sources git flux-system --context k3d-hub-flux
  Output includes the current GitRepository revision sha

flux reconcile source git flux-system --context k3d-hub-flux
  Force-syncs the GitRepository (used as priming step before observation; documented in poc-capsule.md)

<!-- Verification decision tree -->
1. Run live bats `bats tests/bats/capsule-install-{06..10}-live-*.bats`.
2. If all skip with cluster-info reasons → "preconditions not met; milestone owner must raise inotify + create-cluster + register-poc-cluster + push commits".
3. If FILE 10 (`capsule-install-10-live-flux-observable.bats`) shows the outer `poc-capsule` NOT Ready=True AND skip reason is "git log origin/main..main not empty" or "1f2da1d3 still" → push-gate surface (Phase 11 VAL-05 owns; not a Phase 8 fail).
4. If FILE 10 outer `poc-capsule` Ready=False AND push-gate satisfied → Phase 7 SEAM defect (gap-close `/gsd-plan-phase 7 --gaps`).
5. If FILE 6 (operator HR Ready=False) OR FILE 6 (proxy HR Ready=False) AND outer `poc-capsule` Ready=True → Phase 8 install defect (gap-close `/gsd-plan-phase 8 --gaps`).
6. If all 10 live bats GREEN → Phase 8 closes; close FOLDED CARRYOVER per `resolves_phase: 8` and execute-phase.md `close_phase_todos` step.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all 5 live Phase 8 bats — observe HelmRelease Ready=True + CRDs + proxy reachability + ADR-004 + folded carryover</name>
  <files>(none — verification-only task)</files>
  <read_first>
    @.planning/phases/08-capsule-capsule-proxy-bare-minimum-install/08-RESEARCH.md (§"Pattern 5: Phase 7 carryover assertion as load-bearing acceptance" lines 614-642 — verifier observation source-of-truth list)
    @.planning/phases/08-capsule-capsule-proxy-bare-minimum-install/08-CONTEXT.md (domain boundary items #1-#5 — Phase 8 success bar)
    @tests/bats/capsule-install-06-live-helm.bats (CAP-01 + CAP-02 live HR Ready=True)
    @tests/bats/capsule-install-07-live-crds.bats (CAP-03 live ≥7 CRDs)
    @tests/bats/capsule-install-08-live-proxy-reachable.bats (CAP-02 live NodePort reachability — Pitfall 8 reframe)
    @tests/bats/capsule-install-09-live-adr-004.bats (Domain boundary item #4)
    @tests/bats/capsule-install-10-live-flux-observable.bats (FOLDED CARRYOVER — outer poc-capsule Ready=True)
    @.planning/todos/pending/2026-04-29-phase-7-hub-flux-observable-reconcile.md (folded carryover requirement)
  </read_first>
  <action>
This task runs live observability bats. Cluster preconditions handled by each bats file's `setup()` (cluster-info gate per RESEARCH §Pattern 4 — auto-skip when cluster unreachable, no human intervention required for skip path).

1. **(Optional priming step — only if milestone owner has not yet seen Ready=True via this verifier)**
   Force-sync the GitRepository on hub-flux so it picks up Plans 08-01 + 08-02 commits:
   ```bash
   if kubectl --context=k3d-hub-flux cluster-info >/dev/null 2>&1; then
     flux reconcile source git flux-system --context k3d-hub-flux
     flux reconcile kustomization poc-capsule -n flux-system --context k3d-hub-flux --with-source || true
   fi
   ```
   This is documented in `docs/poc-capsule.md` per Claude's Discretion §"Reconcile loop noise tolerance window" (Plan 08-00 also says `--with-source` is OK as a force path, NOT required for CI).

2. **Run all 5 live bats — capture outcome:**
   ```bash
   bats tests/bats/capsule-install-{06..10}-live-*.bats 2>&1 | tee /tmp/cap8-live.log
   ```

3. **Categorize each test outcome (in 08-03-SUMMARY.md):**
   - For each `@test`: ok / not ok / skip
   - For each skip: extract the skip reason from the bats output
   - For each `not ok`: extract the assertion message + the `flux get`/`kubectl get`/`curl` output

4. **Apply the verification decision tree (08-03-SUMMARY.md must surface the conclusion):**

   **Path A — All 5 live bats files report only `ok` or `skip` (no `not ok`):**
     - If all `skip` → preconditions not met. Surface to milestone owner: "Plan 08-03 cannot verify until (a) inotify limit raised on WSL host, (b) `bash scripts/poc/capsule/create-cluster.sh && bash scripts/register-poc-cluster.sh spoke-capsule` run to bring cluster + Secret up, (c) Plan 08-01 + 08-02 commits pushed to origin/main." Phase 8 stays in `verifying` status.
     - If all `ok` → Phase 8 install verified end-to-end. CAP-01 + CAP-02 + CAP-03 + Domain item #4 + FOLDED CARRYOVER all GREEN. Proceed to Task 2 + 3.

   **Path B — File 10 (`capsule-install-10-live-flux-observable.bats`) reports `# skip` with reason "Local commits not pushed to origin/main" or "1f2da1d3":**
     - This is the push-gate surface. Phase 11 VAL-05 owns the one-shot history scan + first-push gate. Surface: "Phase 8 verifier defers to Phase 11 VAL-05 for outer-Kustomization-Ready=True observation. Not a Phase 8 fail. The script-internal P18 falsifier in `register-poc-cluster.sh verify_credential_layer()` already passed in Phase 7 (stronger guarantee than Flux runtime observation)." Mark `resolves_phase: 8` todo as **deferred-to-VAL-05** (NOT closed). Phase 8 status: `closed-with-deferred-carryover`.

   **Path C — File 10 reports `not ok` (outer `poc-capsule` Ready=False) AND push-gate is satisfied (git log origin/main..main is empty AND GitRepository revision past 1f2da1d3):**
     - This is a Phase 7 SEAM defect. Surface to milestone owner: "Outer `poc-capsule` Kustomization is NOT reaching Ready=True against the post-Phase-7 source revision. The hub-side credential is failing to route the apiserver call through to spoke-capsule. This is a Phase 7 seam defect — gap-close back to Phase 7 with `/gsd-plan-phase 7 --gaps`. Inspect: `flux describe kustomization poc-capsule -n flux-system --context k3d-hub-flux` for the failure reason." Phase 8 cannot close.

   **Path D — File 6 reports `not ok` for HelmRelease Ready=True OR File 7 reports `not ok` for CRDs OR File 8 reports `not ok` for proxy reachability:**
     - Phase 8 install defect. Surface to milestone owner: "Phase 8's HelmRelease/CRD/proxy install is not reaching the success bar. The outer `poc-capsule` is GREEN (seam works) but the chart install or proxy chart values are wrong. Gap-close: `/gsd-plan-phase 8 --gaps`. Inspect: `flux describe helmrelease capsule -n capsule-system --context k3d-hub-flux`, `flux describe helmrelease capsule-proxy -n capsule-system --context k3d-hub-flux`."

   **Path E — File 9 reports `not ok` (ADR-004 invariant violated — Flux controllers found in spoke-capsule's flux-system ns):**
     - Critical regression. Phase 8 has accidentally installed Flux on spoke-capsule. Should NOT happen with Phase 8's hub-only HelmRelease pattern; investigate immediately. Surface as Phase 8 install defect with high severity.

5. **Record live bats output verbatim in 08-03-SUMMARY.md** under "## Live Verification Log" so the milestone owner can audit.
  </action>
  <verify>
    <automated>bats tests/bats/capsule-install-{06..10}-live-*.bats 2>&1 | tee /tmp/cap8-live.log | tail -3 ; echo "--- summary ---" ; grep -cE '^(ok|not ok|# skip)' /tmp/cap8-live.log ; ! grep -q '^not ok' /tmp/cap8-live.log || echo "FAIL: live bats failures detected — see decision tree in PLAN action"</automated>
  </verify>
  <done>
All 5 live bats run. Each `@test` outcome is captured (ok / not ok / skip with reason). The verification decision tree (Paths A-E) surfaces the right next action in 08-03-SUMMARY.md. If clusters are unreachable (preconditions unmet), the verifier reports "Path A — all skip" cleanly and Phase 8 stays in `verifying` status pending milestone owner action. If a Phase 7 SEAM defect is detected, the gap-close path is explicit (`/gsd-plan-phase 7 --gaps`). If all GREEN, Phase 8 closes and the FOLDED CARRYOVER auto-closes per `resolves_phase: 8`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rerun Phase 7 static regression bats verbatim — confirm P31 isolation + P40 / P18 invariant unchanged</name>
  <files>(none — verification-only task; reruns existing bats)</files>
  <read_first>
    @tests/bats/poc-isolation-01-static.bats (Phase 7 P31 / D-12 invariant — zero `spoke-capsule` mentions in 6 v0.18 scripts)
    @tests/bats/poc-mount-01-static.bats (Phase 7 P40 / P18 invariant — outer Kustomization secretRef + key: value.yaml)
    @tests/bats/poc-cluster-01-static.bats (Phase 7 CAPCLU-01..04 cluster-shape contract)
    @.planning/phases/08-capsule-capsule-proxy-bare-minimum-install/08-CONTEXT.md (domain boundary — Phase 8's new files live ONLY under `pocs/capsule/operator/` + `pocs/capsule/proxy/` + the modified `pocs/capsule/kustomization.yaml` — none of these are in v0.18 scripts' grep set)
  </read_first>
  <action>
This task confirms Phase 8 introduced ZERO regressions to Phase 7's invariants. NO files modified.

1. **Rerun Phase 7 static regression bats verbatim (THESE FILES ARE UNCHANGED):**
   ```bash
   bats tests/bats/poc-mount-01-static.bats \
        tests/bats/poc-isolation-01-static.bats \
        tests/bats/poc-cluster-01-static.bats 2>&1 | tee /tmp/phase7-regression-final.log
   ```
   EXPECT: every `@test` reports `ok` (Phase 7 close-out had 271/271 GREEN, including these). Plan 08-00 + 08-01 + 08-02 introduce ZERO `spoke-capsule` mentions in v0.18 scripts (`scripts/{create-clusters,register-spokes-for-flux,health-check,destroy,delete-clusters,rebuild}.sh`) AND zero edits to `clusters/hub-flux/pocs/capsule.yaml`. If ANY of the 3 Phase 7 bats files report `not ok`, Phase 8 has accidentally regressed Phase 7's invariants.

2. **Per-invariant verbose check (in case of failure):**
   ```bash
   # P31 — explicit grep over the 6 v0.18 scripts (excluding comments)
   for f in scripts/{create-clusters,register-spokes-for-flux,health-check,destroy,delete-clusters,rebuild}.sh; do
     hits=$(grep -v '^[[:space:]]*#' "$f" | grep -cF -- 'spoke-capsule' || true)
     echo "$f: $hits hits (must be 0)"
     [[ "$hits" -eq 0 ]]
   done

   # P40 / P18 — exact yq query on outer Kustomization
   yq eval '.spec.kubeConfig.secretRef.name == "spoke-capsule-kubeconfig" and .spec.kubeConfig.secretRef.key == "value.yaml"' clusters/hub-flux/pocs/capsule.yaml
   # MUST return "true"

   # P40 / P18 — outer Kustomization NOT modified by Phase 8 (file unchanged)
   git diff --name-only origin/main..HEAD -- clusters/hub-flux/pocs/capsule.yaml
   # MUST return empty (file is unchanged in any Phase 8 commit)
   ```

3. **Anti-grep — Phase 8 scope-boundary cross-check (final pass):**
   ```bash
   # No CapsuleConfiguration CR (defer Phase 9 — D-08-08)
   ! grep -rqF 'kind: CapsuleConfiguration' pocs/capsule/

   # No Tenant CR (defer Phase 9 — TEN-01..06)
   ! grep -rqF 'kind: Tenant' pocs/capsule/

   # No lockdown flag patches (defer Phase 9 — TEN-05)
   ! grep -rqF 'no-cross-namespace-refs' pocs/capsule/
   ! grep -rqF 'no-remote-bases' pocs/capsule/
   ! grep -rqF 'default-service-account' pocs/capsule/
   ```

4. **Surface results in 08-03-SUMMARY.md under "## Phase 7 Regression Confirmation".**
  </action>
  <verify>
    <automated>bats tests/bats/poc-mount-01-static.bats tests/bats/poc-isolation-01-static.bats tests/bats/poc-cluster-01-static.bats 2>&1 | tail -1 | grep -E '0 failures' && yq eval '.spec.kubeConfig.secretRef.name == "spoke-capsule-kubeconfig" and .spec.kubeConfig.secretRef.key == "value.yaml"' clusters/hub-flux/pocs/capsule.yaml | grep -q '^true$' && ! grep -rqF 'kind: CapsuleConfiguration' pocs/capsule/ && ! grep -rqF 'kind: Tenant' pocs/capsule/ && ! grep -rqF 'no-cross-namespace-refs' pocs/capsule/</automated>
  </verify>
  <done>
The 3 Phase 7 static regression bats files (poc-{mount,isolation,cluster}-01-static) report `0 failures` after Plan 08-02 commits. The 6 v0.18 scripts have ZERO `spoke-capsule` mentions (P31 / D-12 inheritance unbroken). `clusters/hub-flux/pocs/capsule.yaml` retains both `secretRef.name: spoke-capsule-kubeconfig` AND `key: value.yaml` AND was NOT touched by any Phase 8 commit (P40 / P18 inheritance unbroken). Phase 8 scope-boundary anti-greps all clean (no CapsuleConfiguration CR, no Tenant CR, no lockdown flag patches anywhere in `pocs/capsule/`).
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full project bats suite + summarize Phase 8 close status (folded carryover decision + Phase 7 SEAM defect routing)</name>
  <files>(none — verification-only task; runs existing bats and writes summary)</files>
  <read_first>
    @.planning/phases/08-capsule-capsule-proxy-bare-minimum-install/08-CONTEXT.md (folded carryover — `resolves_phase: 8` auto-close discipline)
    @.planning/todos/pending/2026-04-29-phase-7-hub-flux-observable-reconcile.md (folded carryover requirement; auto-closes when Phase 8 verifier observes outer poc-capsule Ready=True)
    @.planning/phases/08-capsule-capsule-proxy-bare-minimum-install/08-VALIDATION.md (§"Validation Sign-Off" checklist)
  </read_first>
  <action>
1. **Run full project bats suite (all 271+ v0.18 + Phase 7 + Phase 8 tests):**
   ```bash
   bats tests/bats/ 2>&1 | tee /tmp/full-suite.log | tail -5
   ```
   EXPECT: `0 failures` (live bats auto-skip when clusters unreachable; static bats all GREEN). If ANY non-skip `not ok` is reported, Phase 8 cannot close — surface the failures in 08-03-SUMMARY.md.

2. **Compose `08-03-SUMMARY.md` with the following sections:**

   **`## Phase 8 Close Status`** — One of:
     - `closed-clean` (all live bats GREEN; FOLDED CARRYOVER auto-closes per `resolves_phase: 8`)
     - `closed-with-deferred-carryover` (Path B — push-gate surfaced; FOLDED CARRYOVER deferred to Phase 11 VAL-05; not a Phase 8 fail)
     - `verifying` (Path A — preconditions unmet; milestone owner action required)
     - `gap-close-phase-7` (Path C — Phase 7 SEAM defect; route to `/gsd-plan-phase 7 --gaps`)
     - `gap-close-phase-8` (Path D or E — Phase 8 install defect; route to `/gsd-plan-phase 8 --gaps`)

   **`## Live Verification Log`** — Verbatim output of all 5 live bats from Task 1.

   **`## Phase 7 Regression Confirmation`** — Outcome of Task 2 (3 bats files, 6 v0.18 scripts grep, outer Kustomization yq, scope-boundary anti-greps).

   **`## Full Project Suite Result`** — Outcome of step 1 above (e.g., `271/271 + 10 Phase 8 = 281/281 with N skipped` or whatever the actual numbers are).

   **`## FOLDED CARRYOVER Resolution`** — Explicit:
     - "FOLDED CARRYOVER (Phase 7 verification item 1b — hub Flux observable reconcile of poc-capsule Kustomization, todo `2026-04-29-phase-7-hub-flux-observable-reconcile.md`) is **<resolution>**."
     - resolution = `auto-closed` (Path A all-GREEN), `deferred-to-VAL-05` (Path B push-gate), `pending-cluster-precondition` (Path A all-skip), `routed-to-phase-7-gap-close` (Path C), or `blocked-by-phase-8-defect` (Paths D/E).
     - The todo file at `.planning/todos/pending/2026-04-29-phase-7-hub-flux-observable-reconcile.md` is auto-handled by execute-phase.md's `close_phase_todos` step on `closed-clean`. NO manual file edits in this plan.

   **`## Outcome — Required Operator Action (if any)`** — Specific next command for the milestone owner if Phase 8 is not `closed-clean`. Examples:
     - For `verifying`: "Run `sudo sysctl -w fs.inotify.max_user_instances=1024 && bash scripts/poc/capsule/create-cluster.sh && bash scripts/register-poc-cluster.sh spoke-capsule && git push origin main` then rerun `bats tests/bats/capsule-install-*-live-*.bats`."
     - For `gap-close-phase-7`: "Run `/gsd-plan-phase 7 --gaps`."
     - For `gap-close-phase-8`: "Run `/gsd-plan-phase 8 --gaps`."

3. **Surface the 08-03-SUMMARY.md path** in the final stdout so execute-phase.md can pick it up.
  </action>
  <verify>
    <automated>bats tests/bats/ 2>&1 | tail -1 | grep -E '[0-9]+ failures' | head -1; test -f .planning/phases/08-capsule-capsule-proxy-bare-minimum-install/08-03-SUMMARY.md && grep -cE '^## ' .planning/phases/08-capsule-capsule-proxy-bare-minimum-install/08-03-SUMMARY.md | awk '{ if ($1 < 5) { exit 1 } }' && echo OK</automated>
  </verify>
  <done>
Full project bats suite runs (271+ v0.18 + 7 Phase 7 + 10 Phase 8 = ~290 tests with N skipped depending on cluster state) and reports `0 failures`. `08-03-SUMMARY.md` exists with at least 5 H2 sections (Phase 8 Close Status, Live Verification Log, Phase 7 Regression Confirmation, Full Project Suite Result, FOLDED CARRYOVER Resolution, Outcome). The Phase 8 close status is one of the 5 enumerated states; FOLDED CARRYOVER resolution is explicit. If status is `closed-clean`, execute-phase.md `close_phase_todos` will auto-close `.planning/todos/pending/2026-04-29-phase-7-hub-flux-observable-reconcile.md`. NO production yaml or bats files modified by Plan 08-03.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Phase 8 Plan 03 — Live Verifier)

| Boundary | Description |
|----------|-------------|
| Verifier (test host) → hub-flux apiserver | `kubectl --context=k3d-hub-flux` + `flux get ...` invocations; read-only observation. |
| Verifier → spoke-capsule apiserver | `kubectl --context=k3d-spoke-capsule get pods/crds` invocations; read-only. |
| Verifier → WSL2 host TCP/IP layer | `curl -k https://127.0.0.1:30443/api` reachability probe; no auth credentials sent. |
| Verifier → git remote (origin/main) | `git log origin/main..main` push-gate check; read-only against local mirror of origin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-Ver-01 | Information Disclosure | `flux get` output may include sensitive cluster info if rerouted to wrong cluster | accept | Live bats use explicit `--context=k3d-{cluster}` (CONTEXT D-08-09 default — never reads ambient KUBECONFIG). Single-host POC; only milestone owner has access. |
| T-08-Ver-02 | Denial of Service | Verifier `flux reconcile source git --with-source` priming step takes ≤30s; could perturb running clusters | mitigate | Priming step is OPTIONAL (Task 1 step 1). Default verifier path uses passive `flux get` observation. The bats retry budget (3 × 30s = 90s) tolerates eventual consistency without forced reconciles. |
| T-08-Ver-03 | Tampering | Verifier accidentally writes a file (modifies bats / yaml / SUMMARY format) | mitigate | Plan 08-03 frontmatter `files_modified: []` (NO production file changes). Only writes are SUMMARY.md (allowed; outside the production paths). Static bats and yaml are read-only inputs to this plan. |
| T-08-04 | Information Disclosure | Tenant kubeconfig leak (P29 — DORMANT in Plan 08-03; verifier has no kubeconfig-shaped artifacts; full closure Phase 10 + Phase 11) | accept | Verifier reads cluster state via `kubectl --context`, not via kubeconfig files in repo. P29 dormant. |
| T-08-05 | Elevation of Privilege | Tenant escape via Flux missing spec.serviceAccountName (P27 — DORMANT in Plan 08-03; no tenant Kustomizations to verify; full closure Phase 9 + Phase 11) | accept | Verifier has zero tenant-related observability. P27 dormant. |

NO `block` severity threats. Phase 8 Plan 03 has 1 LOW-severity ACCEPTED threat (T-08-Ver-01 read-only flux observation; single-host POC) and 2 LOW-severity MITIGATED threats (T-08-Ver-02..03). Two DORMANT inheritance points (T-08-04, T-08-05) close in Phase 9-11.
</threat_model>

<verification>
- All 5 Phase 8 live bats files (`capsule-install-{06..10}-live-*.bats`) execute and produce a clear ok / not ok / skip per `@test`
- The verification decision tree (Paths A-E) categorizes the outcome and surfaces the right next action
- Phase 7 static regression bats (`poc-{mount,isolation,cluster}-01-static.bats`) report 0 failures (P31 / P40 / P18 inheritance unbroken)
- Anti-grep lints for Phase 8 scope-boundary all clean (no CapsuleConfiguration CR / Tenant CR / lockdown flag patches in `pocs/capsule/`)
- Full project bats suite (`bats tests/bats/`) reports 0 failures (live tests skip on unreachable clusters; static tests all GREEN)
- `08-03-SUMMARY.md` exists with at least 5 H2 sections covering close status, live log, Phase 7 regression, full suite result, FOLDED CARRYOVER resolution, and operator action
- `git status --short` shows ONLY `.planning/phases/08-capsule-capsule-proxy-bare-minimum-install/08-03-SUMMARY.md` as the new file from this plan (no production yaml / bats / scripts modified)
</verification>

<success_criteria>
Phase 8 verifier produces a categorical outcome:
  - `closed-clean` (best path) — all live bats GREEN, FOLDED CARRYOVER auto-closed, Phase 8 ships.
  - `closed-with-deferred-carryover` — push-gate Phase 11 owns the first-push observation; Phase 8 ships with explicit deferral.
  - `verifying` — preconditions unmet; milestone owner action queued.
  - `gap-close-phase-7` — Phase 7 SEAM defect detected; route back via `/gsd-plan-phase 7 --gaps`.
  - `gap-close-phase-8` — Phase 8 install defect detected; route via `/gsd-plan-phase 8 --gaps`.

The FOLDED CARRYOVER (Phase 7 verification item 1b — hub Flux observable reconcile of poc-capsule Kustomization, todo `2026-04-29-phase-7-hub-flux-observable-reconcile.md`) is RESOLVED in one of the 5 ways above. ADR-004 invariant (no Flux on spoke-capsule) is positively verified live.
</success_criteria>

<output>
After completion, create `.planning/phases/08-capsule-capsule-proxy-bare-minimum-install/08-03-SUMMARY.md` with the 6 H2 sections enumerated in Task 3 step 2. Plan 08-03 produces NO other artifacts. The verifier's outcome routes the milestone owner's next action.
</output>
