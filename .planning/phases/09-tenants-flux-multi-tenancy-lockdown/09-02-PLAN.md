---
phase: 09-tenants-flux-multi-tenancy-lockdown
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - pocs/capsule/tenants/kustomization.yaml
  - pocs/capsule/tenants/alpha.yaml
  - pocs/capsule/tenants/bravo.yaml
  - pocs/capsule/tenants/alpha-app/kustomization.yaml
  - pocs/capsule/tenants/bravo-app/kustomization.yaml
  - pocs/capsule/kustomization.yaml
  - scripts/register-poc-cluster.sh
autonomous: true
requirements: [TEN-04, TEN-06]

must_haves:
  truths:
    - "Hub-side per-tenant inner Kustomizations land at pocs/capsule/tenants/{alpha,bravo}.yaml — each is a multi-doc YAML containing Namespace + GitRepository + Kustomization (in tenant-<name> namespace on hub)"
    - "Each tenant inner Kustomization has spec.serviceAccountName=gitops-reconciler (P27 LOAD-BEARING DEFENSE per RESEARCH Gap 3)"
    - "Each tenant inner Kustomization has spec.kubeConfig.secretRef.name=spoke-capsule-kubeconfig + key=value.yaml (P40/P18 inheritance) AND lives in same namespace as the Secret (RESEARCH Gap 1 hard same-namespace constraint)"
    - "Each tenant inner Kustomization has spec.dependsOn[0].name=poc-capsule + namespace=flux-system (TEN-06 P32 chain)"
    - "Each tenant inner Kustomization has spec.wait=true at TOP-LEVEL (RESEARCH Gap 9 correction; NOT inside dependsOn entry)"
    - "Pitfall-7 placeholder dirs alpha-app/ + bravo-app/ exist with kustomization.yaml resources: [] (Phase 11 may populate with workloads)"
    - "pocs/capsule/kustomization.yaml updated to resources: [operator/, proxy/, tenants/] (preserves Phase 7+8 head comment + appends Phase 9 update note)"
    - "scripts/register-poc-cluster.sh extended with mirror_kubeconfig_to_tenant_namespaces function that imperatively mirrors spoke-capsule-kubeconfig Secret into tenant-{alpha,bravo} ns on hub-flux (RESEARCH Gap 1 mitigation — Flux's kubeConfig.secretRef has UNCONDITIONAL same-namespace constraint)"
    - "register-poc-cluster.sh extension is idempotent (re-run safe; mirrors existing apply_hub_secret pattern; uses yq strip of metadata.{resourceVersion,uid,creationTimestamp})"
  artifacts:
    - path: "pocs/capsule/tenants/alpha.yaml"
      provides: "Multi-doc YAML: Namespace tenant-alpha + GitRepository tenant-alpha-source + Kustomization tenant-alpha-app (with kubeConfig + serviceAccountName=gitops-reconciler [P27] + dependsOn=poc-capsule [P32] + wait:true TOP-LEVEL)"
      contains: "kind: Kustomization"
    - path: "pocs/capsule/tenants/bravo.yaml"
      provides: "Multi-doc YAML mirror for bravo (same shape; alpha → bravo substitution)"
      contains: "tenant-bravo"
    - path: "pocs/capsule/tenants/alpha-app/kustomization.yaml"
      provides: "Pitfall-7 placeholder; resources: [] (Phase 11 may populate with workloads)"
      contains: "resources: []"
    - path: "pocs/capsule/tenants/bravo-app/kustomization.yaml"
      provides: "Pitfall-7 placeholder mirror"
      contains: "resources: []"
    - path: "pocs/capsule/tenants/kustomization.yaml"
      provides: "Aggregator: resources: [alpha.yaml, bravo.yaml, alpha-app/, bravo-app/]"
      contains: "alpha.yaml"
    - path: "pocs/capsule/kustomization.yaml"
      provides: "Phase 9 UPDATE — adds tenants/ to resources list (preserve Phase 7+8 head comment + append Phase 9 update note)"
      contains: "tenants/"
    - path: "scripts/register-poc-cluster.sh"
      provides: "EXTENSION — mirror_kubeconfig_to_tenant_namespaces function + invocation in main flow; mirrors spoke-capsule-kubeconfig into tenant-{alpha,bravo} ns on hub-flux (RESEARCH Gap 1 mitigation)"
      contains: "mirror_kubeconfig_to_tenant_namespaces"
  key_links:
    - from: "pocs/capsule/tenants/alpha.yaml (Kustomization doc)"
      to: "pocs/capsule/tenants/alpha-app/kustomization.yaml"
      via: "spec.path: ./pocs/capsule/tenants/alpha-app"
      pattern: "path:.*pocs/capsule/tenants/alpha-app"
    - from: "pocs/capsule/tenants/alpha.yaml (Kustomization doc)"
      to: "Secret tenant-alpha/spoke-capsule-kubeconfig (mirrored by register-poc-cluster.sh extension)"
      via: "spec.kubeConfig.secretRef.name: spoke-capsule-kubeconfig (must exist in same namespace as the Kustomization — tenant-alpha — per RESEARCH Gap 1)"
      pattern: "secretRef.*spoke-capsule-kubeconfig"
    - from: "pocs/capsule/tenants/alpha.yaml (Kustomization doc)"
      to: "GitRepository tenant-alpha/tenant-alpha-source"
      via: "spec.sourceRef: {kind: GitRepository, name: tenant-alpha-source} (same namespace as Kustomization — tenant-alpha — to survive --no-cross-namespace-refs lockdown)"
      pattern: "sourceRef.*tenant-alpha-source"
    - from: "pocs/capsule/tenants/alpha.yaml (Kustomization doc)"
      to: "clusters/hub-flux/pocs/capsule.yaml (poc-capsule outer K)"
      via: "spec.dependsOn: [{name: poc-capsule, namespace: flux-system}] — P32 chain blocks until operator HelmRelease Ready"
      pattern: "dependsOn.*poc-capsule"
---

<objective>
Plan 09-02 — Hub-side per-tenant inner Flux Kustomizations + per-tenant GitRepositories + per-tenant hub Namespaces + Pitfall-7 placeholder dirs + outer pocs/capsule/kustomization.yaml update + register-poc-cluster.sh Secret-mirror extension.

Lands the hub-targeted control plane (CRs that materialize on hub-flux's apiserver and target spoke-capsule via spec.kubeConfig + spec.serviceAccountName impersonation): per-tenant Namespace + GitRepository + Kustomization (3-doc YAML per tenant), per-tenant Pitfall-7 placeholder dirs (alpha-app/, bravo-app/), the tenants/ aggregator, and the outer pocs/capsule/kustomization.yaml resource list update. Critically, extends `scripts/register-poc-cluster.sh` to imperatively mirror the `spoke-capsule-kubeconfig` Secret into both `tenant-alpha` and `tenant-bravo` namespaces on hub-flux per RESEARCH Gap 1 — Flux's kubeConfig.secretRef has UNCONDITIONAL same-namespace constraint (NOT gated by --no-cross-namespace-refs flag).

Purpose: TEN-04 P27 LOAD-BEARING (every tenant inner K declares spec.serviceAccountName explicitly per RESEARCH §Gap 3 root cause analysis). TEN-06 P32 (each per-tenant inner K dependsOn poc-capsule with TOP-LEVEL spec.wait:true per RESEARCH Gap 9). Without the Secret mirror (D-09-03a per RESEARCH Gap 1), the per-tenant inner Ks reach Ready=False with "secret 'spoke-capsule-kubeconfig' not found" — verifier tenants-09-live-inner-k-ready.bats catches this.

Output: 6 new yaml files + 2 modified yaml files + 1 modified shell script (register-poc-cluster.sh extension); tenants-02-static-p27.bats turns GREEN; tenants-04 turns FULLY GREEN; outer pocs/capsule/ Kustomize tree resolves with tenants/ subdir.

This plan honors RESEARCH.md corrections to CONTEXT.md verbatim:
- Flux kubeConfig.secretRef has UNCONDITIONAL same-namespace constraint (RESEARCH Gap 1) — Plan 09-02 mirrors Secret per tenant ns via register-poc-cluster.sh extension
- spec.wait: true is TOP-LEVEL (NOT inside dependsOn entry) per RESEARCH Gap 9
- GitHub owner is hard-coded `richard-iovanisci` per RESEARCH Gap 10 Option A (avoids cross-namespace ConfigMap mirror)
- P27 LOAD-BEARING: every tenant inner K MUST set spec.serviceAccountName explicitly (RESEARCH §Gap 3 — flag fallback IS belt-and-suspenders, not the primary defense)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-CONTEXT.md
@.planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-RESEARCH.md
@.planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-PATTERNS.md
@pocs/capsule/kustomization.yaml
@pocs/capsule/spoke/kustomization.yaml
@scripts/register-poc-cluster.sh
@clusters/hub-flux/pocs/capsule-spoke.yaml

<interfaces>
<!-- Existing patterns the executor will mirror. -->
<!-- Executor copies these patterns directly — RESEARCH §"Code Examples" provides verbatim target shapes. -->

From clusters/hub-flux/pocs/capsule-spoke.yaml (analog for inner K with kubeConfig — minus the dependsOn shape, which Plan 09-01 already added):
```yaml
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: poc-capsule-spoke
  namespace: flux-system
spec:
  interval: 5m
  path: ./pocs/capsule/spoke
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  kubeConfig:
    secretRef:
      name: spoke-capsule-kubeconfig
      key: value.yaml
```

From scripts/register-poc-cluster.sh (existing analog: apply_hub_secret function — Plan 09-02 adds a sibling mirror_kubeconfig_to_tenant_namespaces function):
```bash
# ---------- apply_hub_secret: D-11 token-safe imperative apply ----------
# --from-file=value.yaml=/dev/stdin only — NEVER --from-literal (would expose
# token in argv / shell history / process listings). NEVER tee. NEVER echo.
apply_hub_secret() {
  local spoke="$1" payload="$2"
  printf '%s\n' "${payload}" |
    kubectl --context "${HUB_CTX}" -n "${FLUX_NS}" create secret generic "${spoke}-kubeconfig" \
      --from-file=value.yaml=/dev/stdin --dry-run=client -o yaml |
    kubectl --context "${HUB_CTX}" -n "${FLUX_NS}" apply -f -
}
```

Constants already defined in scripts/register-poc-cluster.sh:
- HUB_CTX="k3d-hub-flux"
- FLUX_NS="flux-system"
- HUB_KUST_FILE points to clusters/hub-flux/flux-system/kustomization.yaml

From pocs/capsule/kustomization.yaml (existing Phase 8 — Phase 9 modifies in-place):
```yaml
# pocs/capsule/kustomization.yaml
# Phase 7 placeholder so the outer Flux Kustomization
# (clusters/hub-flux/pocs/capsule.yaml, spec.path: ./pocs/capsule) reconciles
# successfully against an empty resource list. Phase 8 will replace `resources: []`
# with `[operator, proxy, config]` once the HelmReleases land.
#
# REQ-CAPCLU-02 (Pitfall 7 — without this file the outer Kustomization reports
# Ready=False; path not found).
#
# Phase 8 update (D-08-05): operator/ and proxy/ subdir refs added — the outer
# poc-capsule Flux Kustomization now reconciles operator HelmRelease + proxy
# HelmRelease + their per-chart OCIRepositories under capsule-system on
# spoke-capsule via spoke-capsule-kubeconfig. Phase 9 will append config/ + tenants/.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- operator/
- proxy/
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create per-tenant alpha.yaml + bravo.yaml multi-doc YAML files + alpha-app/ + bravo-app/ Pitfall-7 placeholders + tenants/ aggregator + update outer pocs/capsule/kustomization.yaml</name>
  <files>
    pocs/capsule/tenants/alpha.yaml,
    pocs/capsule/tenants/bravo.yaml,
    pocs/capsule/tenants/alpha-app/kustomization.yaml,
    pocs/capsule/tenants/bravo-app/kustomization.yaml,
    pocs/capsule/tenants/kustomization.yaml,
    pocs/capsule/kustomization.yaml
  </files>
  <read_first>
    - .planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-CONTEXT.md (D-09-01 directory layout lines 36-65; D-09-03 transitional Tenant inner K shape lines 88-115; D-09-03b per-tenant GitRepository lines 117-130; D-09-06 dependsOn chain lines 224-231)
    - .planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-RESEARCH.md §"Code Examples" lines 923-1031 (verbatim target shapes for alpha.yaml multi-doc + Pitfall-7 placeholders + tenants aggregator + kustomization.yaml update) + §Gap 1 lines 137-181 (kubeConfig.secretRef same-namespace constraint) + §Gap 9 wait:true correction + §Gap 10 GitHub owner Option A (hard-code) + §Gap 3 P27 root cause comment block lines 268-283
    - .planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-PATTERNS.md lines 314-501 (alpha.yaml multi-doc analog mapping + outer kustomization.yaml diff guidance)
    - pocs/capsule/kustomization.yaml (existing Phase 8 — Phase 9 modifies in-place; preserve head comment block)
    - clusters/hub-flux/pocs/capsule-spoke.yaml (analog for Kustomization-with-kubeConfig shape)
    - pocs/capsule/spoke/kustomization.yaml (analog for Pitfall-7 placeholder pattern)
  </read_first>
  <action>
Create the 5 new yaml files + modify pocs/capsule/kustomization.yaml. Use these exact shapes from RESEARCH §"Code Examples" + RESEARCH-corrected forms (Gap 9 top-level wait; Gap 1 same-namespace; Gap 10 hard-coded owner):

**File 1: `pocs/capsule/tenants/alpha.yaml`** (RESEARCH §"Code Examples" lines 925-1002 verbatim — multi-doc with 3 docs):
```yaml
# pocs/capsule/tenants/alpha.yaml
# TEN-04 — hub-targeted: per-tenant Namespace (tenant-alpha on hub) +
# per-tenant GitRepository + per-tenant inner Kustomization.
#
# D-09-02 — hub-side tenant ns mirrors spoke-side name (required so the
# Kustomization CR has a home AND so Flux's same-namespace impersonation
# rule resolves: SA gitops-reconciler in tenant-alpha on TARGET cluster).
#
# CRITICAL — RESEARCH §Gap 1: spec.kubeConfig.secretRef.name "spoke-capsule-kubeconfig"
# MUST exist in tenant-alpha namespace on hub (NOT just flux-system). Plan 09-02 extends
# scripts/register-poc-cluster.sh to imperatively mirror the Secret. Without the mirror,
# the Kustomization reaches Ready=False with `secret "spoke-capsule-kubeconfig" not found`.
# Verifier tests/bats/tenants-09-live-inner-k-ready.bats catches this.
apiVersion: v1
kind: Namespace
metadata:
  name: tenant-alpha
--- 
# TEN-04 — per-tenant GitRepository. Public HTTPS URL; no secretRef per Phase 6
# REPO-01..04 (karyon repo is public). Lives in tenant-alpha namespace so the
# Kustomization's sourceRef survives --no-cross-namespace-refs lockdown.
#
# RESEARCH §Gap 10 — owner is hard-coded (Option A) per plan-time recommendation;
# postBuild substituteFrom would require a cross-namespace ConfigMap mirror.
# Until Phase 11 VAL-05 first push, GitRepository reaches Ready=False with
# "failed to fetch: revision main doesn't exist on remote" (acceptable for Phase 9).
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: tenant-alpha-source
  namespace: tenant-alpha
spec:
  interval: 5m
  url: https://github.com/richard-iovanisci/karyon
  ref:
    branch: main
--- 
# TEN-04 — per-tenant inner Kustomization. CARRIES THE LOAD-BEARING P27 DEFENSE.
#
# P27 LOAD-BEARING DEFENSE — every tenant Flux Kustomization MUST set
# spec.serviceAccountName explicitly. Per Flux v2.8 kustomize-controller v1 API
# (api/v1/kustomization_types.go KubeConfig field comment): when spec.kubeConfig
# is set AND spec.serviceAccountName is empty, kustomize-controller falls through
# to the --default-service-account flag's value (default=`default`). For cross-
# cluster reconciles, this means impersonating system:serviceaccount:<ns>:default
# on the target cluster — a no-permission SA that fails closed BUT only if the
# kubeConfig Secret's principal isn't already cluster-admin. With karyon's
# spoke-capsule-kubeconfig (Phase 7 Pattern 3 — embedded cluster-admin token),
# if BOTH serviceAccountName is empty AND the kubeConfig principal is used,
# kustomize-controller applies as cluster-admin on spoke, bypassing Capsule's
# webhooks (which deliberately exempt cluster-admin / kube-system identities).
# Hence: explicit serviceAccountName is the primary structural defense; the
# --default-service-account flag is belt-and-suspenders.
#
# spec.kubeConfig.secretRef.key: "value.yaml" — P40/P18 inheritance (silent-misroute
# defense from Phase 7; mirrored from spoke-{apps,ml} pattern).
#
# spec.dependsOn: [{name: poc-capsule, namespace: flux-system}] — TEN-06 P32:
# blocks until Capsule operator HelmRelease is Ready=True (CRDs registered).
#
# spec.wait: true is TOP-LEVEL per RESEARCH Gap 9 correction (NOT inside dependsOn entry).
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: tenant-alpha-app
  namespace: tenant-alpha
spec:
  interval: 5m
  path: ./pocs/capsule/tenants/alpha-app
  prune: true
  wait: true
  timeout: 5m
  sourceRef:
    kind: GitRepository
    name: tenant-alpha-source
    # Same namespace (tenant-alpha) — survives --no-cross-namespace-refs
  kubeConfig:
    secretRef:
      name: spoke-capsule-kubeconfig
      # Same namespace (tenant-alpha) — required by Flux v2.8 contract
      # (RESEARCH §Gap 1). scripts/register-poc-cluster.sh extension mirrors Secret imperatively.
      key: value.yaml
  serviceAccountName: gitops-reconciler  # P27 LOAD-BEARING DEFENSE
  dependsOn:
    - name: poc-capsule
      namespace: flux-system
```

**File 2: `pocs/capsule/tenants/bravo.yaml`** — verbatim copy of alpha.yaml with `alpha → bravo` substitution throughout:
- Namespace doc: `metadata.name: tenant-bravo`
- GitRepository doc: `metadata.name: tenant-bravo-source` + `metadata.namespace: tenant-bravo`
- Kustomization doc: `metadata.name: tenant-bravo-app` + `metadata.namespace: tenant-bravo` + `spec.path: ./pocs/capsule/tenants/bravo-app` + `spec.sourceRef.name: tenant-bravo-source`
- ALL comment blocks substituted alpha → bravo

**File 3: `pocs/capsule/tenants/alpha-app/kustomization.yaml`** (RESEARCH §"Code Examples" lines 1006-1016):
```yaml
# pocs/capsule/tenants/alpha-app/kustomization.yaml
# D-09-01 / Pitfall-7 placeholder — empty resource list so the inner Flux
# Kustomization (tenant-alpha-app, defined in pocs/capsule/tenants/alpha.yaml)
# reconciles successfully against an empty resource set.
#
# Mirrors Phase 7 D-06 / Phase 8 D-08-12 spoke/ placeholder pattern.
# Phase 11 (workload demonstration) may populate.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
```

**File 4: `pocs/capsule/tenants/bravo-app/kustomization.yaml`** — verbatim copy of file 3 with `alpha → bravo` in comment.

**File 5: `pocs/capsule/tenants/kustomization.yaml`** (RESEARCH §"Code Examples" lines 1019-1031):
```yaml
# pocs/capsule/tenants/kustomization.yaml
# Aggregator for hub-targeted tenant resources.
#
# D-09-01 — tenants subdir under pocs/capsule/. Hub-targeted: parent
# pocs/capsule/kustomization.yaml resolves [operator/, proxy/, tenants/] under the
# poc-capsule outer K (NO kubeConfig per D-08-12 split-path; CRs land on hub apiserver).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - alpha.yaml
  - bravo.yaml
  - alpha-app/
  - bravo-app/
```

**File 6: `pocs/capsule/kustomization.yaml`** (modify in-place; preserve Phase 7+8 head comment + append Phase 9 update note per PATTERNS.md lines 446-500):
```yaml
# pocs/capsule/kustomization.yaml
# Phase 7 placeholder so the outer Flux Kustomization
# (clusters/hub-flux/pocs/capsule.yaml, spec.path: ./pocs/capsule) reconciles
# successfully against an empty resource list. Phase 8 will replace `resources: []`
# with `[operator, proxy, config]` once the HelmReleases land.
#
# REQ-CAPCLU-02 (Pitfall 7 — without this file the outer Kustomization reports
# Ready=False; path not found).
#
# Phase 8 update (D-08-05): operator/ and proxy/ subdir refs added — the outer
# poc-capsule Flux Kustomization now reconciles operator HelmRelease + proxy
# HelmRelease + their per-chart OCIRepositories under capsule-system on
# spoke-capsule via spoke-capsule-kubeconfig.
#
# Phase 9 update (D-09-01): tenants/ subdir added — hub-targeted per-tenant
# inner Flux Kustomization CRs + per-tenant Namespaces + per-tenant GitRepositories.
# CapsuleConfiguration + Tenant CRs land via the spoke-targeted poc-capsule-spoke
# outer K (path ./pocs/capsule/spoke).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- operator/
- proxy/
- tenants/
```

After creating/modifying the 6 files:
1. Run `kustomize build pocs/capsule/` to verify the full Kustomize tree resolves (operator HR + OCIRepo + proxy HR + OCIRepo + 4 tenant resources from tenants/alpha.yaml + 4 tenant resources from tenants/bravo.yaml = 12 CRs)
2. Run `kustomize build pocs/capsule/tenants/` independently to verify per-tenant tree resolves (8 CRs: 2 Namespaces + 2 GitRepositories + 2 Kustomizations + 0 from placeholder dirs)
3. Verify yq assertions on each tenant inner K:
   - `yq eval 'select(.kind == "Kustomization") | .spec.serviceAccountName' pocs/capsule/tenants/alpha.yaml` returns `gitops-reconciler` (P27)
   - `yq eval 'select(.kind == "Kustomization") | .spec.wait' pocs/capsule/tenants/alpha.yaml` returns `true` (TOP-LEVEL per RESEARCH Gap 9)
   - `yq eval 'select(.kind == "Kustomization") | .spec.kubeConfig.secretRef.name' pocs/capsule/tenants/alpha.yaml` returns `spoke-capsule-kubeconfig`
   - `yq eval 'select(.kind == "Kustomization") | .spec.kubeConfig.secretRef.key' pocs/capsule/tenants/alpha.yaml` returns `value.yaml`
   - `yq eval 'select(.kind == "Kustomization") | .spec.dependsOn[0].name' pocs/capsule/tenants/alpha.yaml` returns `poc-capsule`
   - `yq eval 'select(.kind == "Kustomization") | .spec.dependsOn[0].namespace' pocs/capsule/tenants/alpha.yaml` returns `flux-system`
4. Run `bats tests/bats/tenants-02-static-p27.bats` — expect GREEN (P27 yq lint passes for both tenant inner Ks)
5. Run `bats tests/bats/tenants-04-static-dependson.bats` — expect FULLY GREEN (capsule-spoke + per-tenant inner K dependsOn)
  </action>
  <verify>
    <automated>cd "${REPO_ROOT:-/home/rich/code/gsd/karyon}" && kustomize build pocs/capsule/tenants/ > /tmp/tenants-build.yaml 2>&1 && grep -c '^kind: ' /tmp/tenants-build.yaml | grep -qx '6' && yq eval 'select(.kind == "Kustomization") | .spec.serviceAccountName' pocs/capsule/tenants/alpha.yaml | grep -qx 'gitops-reconciler' && yq eval 'select(.kind == "Kustomization") | .spec.wait' pocs/capsule/tenants/alpha.yaml | grep -qx 'true' && yq eval 'select(.kind == "Kustomization") | .spec.serviceAccountName' pocs/capsule/tenants/bravo.yaml | grep -qx 'gitops-reconciler' && yq eval '.resources' pocs/capsule/kustomization.yaml | grep -F 'tenants/' && bats tests/bats/tenants-02-static-p27.bats tests/bats/tenants-04-static-dependson.bats</automated>
  </verify>
  <done>
    6 hub-side yaml files created/modified. `kustomize build pocs/capsule/tenants/` resolves to 6 CRs (2 Namespace + 2 GitRepository + 2 Kustomization; alpha-app/ + bravo-app/ are empty placeholders). RESEARCH Gap 9 wait:true is at TOP-LEVEL spec.wait. RESEARCH Gap 1 same-namespace constraint is satisfied (Kustomization in tenant-alpha references tenant-alpha-source GitRepo + spoke-capsule-kubeconfig Secret — both in tenant-alpha ns; Secret-mirror lands in Task 2). P27 yq lint passes (every Kustomization in pocs/capsule/tenants/*.yaml has serviceAccountName=gitops-reconciler non-null). tenants-02 + tenants-04 turn GREEN.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend scripts/register-poc-cluster.sh with mirror_kubeconfig_to_tenant_namespaces function (RESEARCH Gap 1 mitigation)</name>
  <files>
    scripts/register-poc-cluster.sh
  </files>
  <read_first>
    - scripts/register-poc-cluster.sh (existing — preserve full file structure; add new function + invocation; analog: apply_hub_secret function lines 187-196)
    - .planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-CONTEXT.md (D-09-03a research-flagged ambiguity lines 115-116; the open research question is RESOLVED by RESEARCH Gap 1 — Flux v2.8 has UNCONDITIONAL same-namespace constraint regardless of flag)
    - .planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-RESEARCH.md §Gap 1 lines 137-181 (Flux contract citation + recommended mitigation function shape)
    - .planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-PATTERNS.md lines 682-733 (register-poc-cluster.sh extension function shape)
    - tests/bats/poc-isolation-01-static.bats (P31 contract — Phase 9 changes to scripts/register-poc-cluster.sh MUST NOT introduce spoke-capsule mentions in v0.18 scripts; THIS script is in scripts/ but v0.18-isolation bats explicitly excludes register-poc-cluster.sh from V018_SCRIPTS array)
  </read_first>
  <action>
Modify `scripts/register-poc-cluster.sh` to add a new function `mirror_kubeconfig_to_tenant_namespaces` and invoke it in the main flow. The existing structure (set -euo pipefail, constants, apply_hub_secret, ensure_hub_pocs_mount, verify_credential_layer, etc.) is PRESERVED VERBATIM.

Per PATTERNS.md lines 700-720 + RESEARCH §Gap 1 lines 162-175, ADD this function block between `apply_hub_secret()` and `ensure_hub_pocs_mount()` (insertion point: after line 196 in current file):

```bash
# ---------- mirror_kubeconfig_to_tenant_namespaces: RESEARCH §Gap 1 ----------
# Mirror spoke-capsule-kubeconfig Secret into per-tenant namespaces on hub-flux.
# Required because Flux's kubeConfig.secretRef has UNCONDITIONAL same-namespace
# constraint (RESEARCH §Gap 1 — Flux v2.8 docs + Go API source citation). The
# tenant namespaces tenant-alpha + tenant-bravo host the per-tenant inner
# Kustomization CRs (defined in pocs/capsule/tenants/<name>.yaml), which
# reference this Secret. Without the mirror, those CRs reach Ready=False with
# `secret "spoke-capsule-kubeconfig" not found`.
#
# Idempotent: re-run safe (kubectl apply + dry-run client + yq strip metadata).
# yq strip is required: a Secret applied verbatim from another ns rejects with
# "field is immutable" on resourceVersion / uid / creationTimestamp conflict.
#
# Operates only on tenant-{alpha,bravo} on k3d-hub-flux. Does NOT touch spoke
# clusters. Phase 11 VAL-04 SLO regression is unaffected (this function is
# only called by register-poc-cluster.sh, which is NEVER invoked by task rebuild
# per P31 isolation contract — verified by tests/bats/poc-isolation-01-static.bats).
mirror_kubeconfig_to_tenant_namespaces() {
  local tenant
  for tenant in alpha bravo; do
    # Ensure the per-tenant namespace exists on hub-flux (Plan 09-02 commits
    # this namespace as part of pocs/capsule/tenants/<name>.yaml, but Flux's
    # reconcile of that file may not have happened yet at register-time).
    kubectl --context="${HUB_CTX}" create namespace "tenant-${tenant}" \
      --dry-run=client -o yaml | kubectl --context="${HUB_CTX}" apply -f -
    # Mirror the spoke-capsule-kubeconfig Secret from flux-system into the
    # per-tenant namespace. yq strip metadata fields that cause conflict on apply.
    # shellcheck disable=SC2016 # yq expression intentionally uses single quotes
    kubectl --context="${HUB_CTX}" -n "${FLUX_NS}" get secret spoke-capsule-kubeconfig -o yaml \
      | yq eval '.metadata.namespace = "tenant-'"${tenant}"'"
                 | del(.metadata.resourceVersion, .metadata.uid, .metadata.creationTimestamp)' - \
      | kubectl --context="${HUB_CTX}" apply -f -
  done
}
```

Then ADD invocation in the main flow. Per PATTERNS.md + the existing main flow at lines 375-409, INSERT between `apply_hub_secret "${POC_CLUSTER}" "${kubeconfig_yaml}"` (line 400) and `pass "applied: ${POC_CLUSTER} hub Secret"` (line 401):

```bash
# Mirror spoke-capsule-kubeconfig into per-tenant namespaces on hub-flux
# (RESEARCH §Gap 1 — Flux's kubeConfig.secretRef has UNCONDITIONAL same-namespace
# constraint). Without this mirror, Phase 9 per-tenant inner Kustomizations fail
# with `secret "spoke-capsule-kubeconfig" not found`.
section "Mirror spoke-capsule-kubeconfig to per-tenant namespaces (RESEARCH §Gap 1)"
mirror_kubeconfig_to_tenant_namespaces
pass "mirrored: spoke-capsule-kubeconfig → tenant-alpha + tenant-bravo on ${HUB_CTX}"
```

Insertion point: search for the line `apply_hub_secret "${POC_CLUSTER}" "${kubeconfig_yaml}"` and insert AFTER its `pass` line, BEFORE the next `section` block (`section "Hub-side FLUX PATCH SURFACE patch"`).

After modifying the script:
1. Verify shellcheck passes: `shellcheck -x scripts/register-poc-cluster.sh` exits 0 (no errors; warnings tolerated per Phase 1-5 follow-up)
2. Verify the new function is grep-findable: `grep -F 'mirror_kubeconfig_to_tenant_namespaces' scripts/register-poc-cluster.sh` returns 2 hits (definition + invocation)
3. Verify the script doesn't BREAK Phase 7 P31 isolation contract: `bats tests/bats/poc-isolation-01-static.bats` — this script is NOT in V018_SCRIPTS array (V018_SCRIPTS contains: create-clusters, register-spokes-for-flux, health-check, destroy, delete-clusters, rebuild — register-poc-cluster.sh is NOT v0.18); P31 bats stays GREEN.
4. Run shellcheck on the diff: `shellcheck -x scripts/register-poc-cluster.sh` — expect exit 0 (per repo's existing shellcheck severity gate).
5. Test idempotency by simulating (do not run live yet; live exercise in Plan 09-04 verifier): `bash -n scripts/register-poc-cluster.sh` — should parse without syntax errors.
  </action>
  <verify>
    <automated>cd "${REPO_ROOT:-/home/rich/code/gsd/karyon}" && bash -n scripts/register-poc-cluster.sh && grep -c -F 'mirror_kubeconfig_to_tenant_namespaces' scripts/register-poc-cluster.sh | grep -qx '2' && grep -F 'tenant-alpha' scripts/register-poc-cluster.sh > /dev/null && grep -F 'tenant-bravo' scripts/register-poc-cluster.sh > /dev/null && bats tests/bats/poc-isolation-01-static.bats && shellcheck -x scripts/register-poc-cluster.sh 2>&1 | grep -v 'note' | head -20 || echo "shellcheck output: see above"</automated>
  </verify>
  <done>
    scripts/register-poc-cluster.sh extended with mirror_kubeconfig_to_tenant_namespaces function. The function is invoked in main flow between apply_hub_secret and ensure_hub_pocs_mount sections. yq strip pattern handles immutable metadata fields. P31 isolation contract preserved (bats tests/bats/poc-isolation-01-static.bats GREEN). shellcheck has no new errors (severity warning still acceptable per v0.18 follow-up). Idempotent re-run safe (kubectl apply + yq strip pattern).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify Wave 2 hub-side state + commit (live Secret-mirror exercise deferred to Plan 09-04 verifier)</name>
  <files>
    (no files modified — verification only)
  </files>
  <read_first>
    - .planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-VALIDATION.md lines 56-57 (per-task verification map for 09-02)
    - .planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-CONTEXT.md (D-09-09 plan ordering — Plan 09-02 reconcile blocked until Plan 09-03 lockdown lands; live Secret-mirror exercise is Plan 09-04 verifier scope)
  </read_first>
  <action>
Verify the static contract bats turn GREEN for the new yaml shapes. Live observation (per-tenant inner K Ready=True; Secret-mirror successful on hub-flux) is intentionally DEFERRED to Plan 09-04 verifier because:
1. Plan 09-03 lockdown patches must land BEFORE the per-tenant inner Ks attempt to reconcile (they will reconcile through the lockdown-patched kustomize-controller).
2. The Secret-mirror function in register-poc-cluster.sh is invoked by `bash scripts/register-poc-cluster.sh spoke-capsule` — that script must be re-run as part of Plan 09-04 verifier setup, not as part of this plan's deliverable.

Steps:
1. Run static bats:
   ```bash
   bats tests/bats/tenants-02-static-p27.bats     # GREEN expected (P27 yq lint over pocs/capsule/tenants/*.yaml)
   bats tests/bats/tenants-04-static-dependson.bats  # GREEN expected (capsule-spoke + per-tenant Ks dependsOn)
   bats tests/bats/poc-isolation-01-static.bats   # GREEN expected (P31 — register-poc-cluster.sh extension preserves invariant)
   ```
2. Run kustomize build verification:
   ```bash
   kustomize build pocs/capsule/ > /tmp/full-build.yaml
   grep -c '^kind: ' /tmp/full-build.yaml
   # Expected: 12 CRs (2 OCIRepository + 2 HelmRelease + 2 Namespace + 2 GitRepository + 2 Kustomization + 2 placeholder kustomization.yaml resolved to 0 = 10 + 2 from placeholders evaluating to nothing = 10)
   # Concretely: operator/ resolves to 1 OCIRepo + 1 HR; proxy/ resolves to 1 OCIRepo + 1 HR; tenants/ resolves to 2 ns + 2 gitrepo + 2 kustomization + alpha-app/empty + bravo-app/empty
   # Total: 10 CRs
   ```
3. Verify the directory tree:
   ```bash
   ls pocs/capsule/tenants/
   # Expected: alpha-app/  alpha.yaml  bravo-app/  bravo.yaml  kustomization.yaml
   ls pocs/capsule/tenants/alpha-app/
   # Expected: kustomization.yaml
   ```

Commit:
```bash
gsd-sdk query commit "feat(09-02): hub-side per-tenant inner Kustomizations + Secret-mirror extension (TEN-04 P27 + TEN-06 P32; D-09-01/03/06; RESEARCH Gap 1/9/10)" \
  pocs/capsule/tenants/alpha.yaml \
  pocs/capsule/tenants/bravo.yaml \
  pocs/capsule/tenants/alpha-app/kustomization.yaml \
  pocs/capsule/tenants/bravo-app/kustomization.yaml \
  pocs/capsule/tenants/kustomization.yaml \
  pocs/capsule/kustomization.yaml \
  scripts/register-poc-cluster.sh
```

NO push to origin/main per D-08-13.

Verify commit landed: `git log -1 --oneline | grep -F '09-02'`.
  </action>
  <verify>
    <automated>cd "${REPO_ROOT:-/home/rich/code/gsd/karyon}" && bats tests/bats/tenants-02-static-p27.bats tests/bats/tenants-04-static-dependson.bats tests/bats/poc-isolation-01-static.bats && kustomize build pocs/capsule/ > /tmp/full-build.yaml 2>&1 && [ -f pocs/capsule/tenants/alpha.yaml ] && [ -f pocs/capsule/tenants/bravo.yaml ] && [ -f pocs/capsule/tenants/alpha-app/kustomization.yaml ] && [ -f pocs/capsule/tenants/bravo-app/kustomization.yaml ] && grep -F 'mirror_kubeconfig_to_tenant_namespaces' scripts/register-poc-cluster.sh > /dev/null && git log -1 --oneline | grep -F '09-02'</automated>
  </verify>
  <done>
    7 files committed (5 new yaml + 1 modified yaml + 1 modified shell script). Static bats tenants-02 + tenants-04 GREEN. P31 isolation bats GREEN. `kustomize build pocs/capsule/` resolves cleanly. NO push to origin per D-08-13. Live Secret-mirror exercise + per-tenant inner K Ready=True deferred to Plan 09-04 verifier.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hub kustomize-controller → tenant-{alpha,bravo} ns on hub | Per-tenant inner K CRs live in tenant-<name> ns; kustomize-controller (cluster-admin SA) applies them on hub apiserver |
| Hub kustomize-controller (impersonating gitops-reconciler) → spoke apiserver | Per-tenant inner K reconciles via spec.kubeConfig: spoke-capsule-kubeconfig (cluster-admin token) + spec.serviceAccountName: gitops-reconciler (impersonation downgrade — primary structural defense per P27) |
| register-poc-cluster.sh imperative apply → hub apiserver | Secret-mirror operation (read spoke-capsule-kubeconfig from flux-system; write to tenant-{alpha,bravo}) — token never logged, never tee'd; same D-11 safety pattern as existing apply_hub_secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-P27 (PRIMARY MITIGATION HERE) | Elevation of Privilege | pocs/capsule/tenants/{alpha,bravo}.yaml Kustomization doc | mitigate | Every tenant inner Kustomization has spec.serviceAccountName: gitops-reconciler explicit-set per RESEARCH §Gap 3 root cause analysis. Without this, kustomize-controller would fall through to either the kubeConfig Secret's principal (cluster-admin token → bypass Capsule) or the --default-service-account flag's value (default SA). Static bats tenants-02-static-p27.bats yq-asserts every Kustomization in pocs/capsule/tenants/*.yaml has non-null serviceAccountName + a synthetic broken fixture (tests/fixtures/p27-broken/) proves the lint catches missing SA. Phase 11 VAL-01 N10 negative-RBAC test verifies live denial. |
| T-9-IMPERSONATION (PRIMARY MITIGATION HERE) | Spoofing | pocs/capsule/tenants/{alpha,bravo}.yaml Kustomization doc spec.serviceAccountName | mitigate | gitops-reconciler SA exists on spoke in tenant-<name> namespace (Plan 09-01 Task 1). Flux's same-namespace impersonation rule resolves: SA in `tenant-alpha` on TARGET cluster (spoke-capsule). Plan 09-01 ships the SA BEFORE Plan 09-02 deploys per-tenant inner Ks. Live observation: tenants-09-live-inner-k-ready.bats verifies inner K reaches Ready=True (or skips on pre-push GitRepository state). |
| T-9-CRD-RACE (REFERENCED) | Tampering | pocs/capsule/tenants/{alpha,bravo}.yaml Kustomization doc spec.dependsOn | mitigate | Each tenant inner K declares spec.dependsOn: [{name: poc-capsule, namespace: flux-system}] + spec.wait: true (TOP-LEVEL per RESEARCH Gap 9). This blocks tenant inner K apply until poc-capsule outer K reaches Ready=True (HelmRelease applied → operator running → CRDs registered on spoke). Static bats tenants-04 grep-asserts the dependsOn shape. |
| T-9-SECRET-MIRROR-LEAK | Information Disclosure | scripts/register-poc-cluster.sh extension | mitigate | The mirror function uses `kubectl get secret -o yaml | yq | kubectl apply` pattern — token bytes flow through stdin only, never argv, never tee, never logged. Mirrors the existing apply_hub_secret D-11 safety pattern. Idempotent: re-run does not produce additional log output beyond standard kubectl apply messages. The mirrored Secret lives only on hub-flux (k3d-hub-flux), never copied to spoke. |
| T-9-SECRET-MIRROR-IMMUTABLE | Denial of Service | scripts/register-poc-cluster.sh extension yq strip block | mitigate | yq strip pattern `del(.metadata.resourceVersion, .metadata.uid, .metadata.creationTimestamp)` removes immutable metadata fields that would otherwise cause kubectl apply to reject with "field is immutable". Without the strip, the second-and-subsequent invocations would fail. Verified by bash -n syntax check; Plan 09-04 verifier exercises the live re-run. |
| T-9-PUBLIC-REPO-AUTH (Pre-push state) | Authentication failure (acceptable) | pocs/capsule/tenants/{alpha,bravo}.yaml GitRepository doc | accept | Until Phase 11 VAL-05 first push, the GitRepository CRs reach Ready=False with "failed to fetch: revision main doesn't exist on remote". Per RESEARCH §Gap 10 + tenants-09-live-inner-k-ready.bats skip-on-pre-push pattern, this is acceptable for Phase 9. After Phase 11 push, GitRepositories reach Ready=True and inner Ks reconcile their (still-empty) paths. |
| T-9-P31-ISOLATION (Regression) | Tampering | scripts/register-poc-cluster.sh | mitigate | The new mirror function is added to register-poc-cluster.sh, NOT to v0.18 scripts (create-clusters, register-spokes-for-flux, health-check, destroy, delete-clusters, rebuild). P31 contract: tests/bats/poc-isolation-01-static.bats greps V018_SCRIPTS for "spoke-capsule" / "register-poc-cluster" / "scripts/poc/" mentions; register-poc-cluster.sh is NOT in V018_SCRIPTS array. P31 bats stays GREEN. |
</threat_model>

<verification>
At end of plan, run:
```bash
cd "${REPO_ROOT:-/home/rich/code/gsd/karyon}"
bats tests/bats/tenants-02-static-p27.bats          # GREEN expected
bats tests/bats/tenants-04-static-dependson.bats    # GREEN expected (FULLY — capsule-spoke + per-tenant Ks)
bats tests/bats/poc-isolation-01-static.bats        # GREEN expected (P31 invariant)
kustomize build pocs/capsule/ | grep -c '^kind: '   # 10 CRs total
yq eval 'select(.kind == "Kustomization") | .spec.serviceAccountName' pocs/capsule/tenants/alpha.yaml  # gitops-reconciler
yq eval 'select(.kind == "Kustomization") | .spec.wait' pocs/capsule/tenants/alpha.yaml  # true (TOP-LEVEL)
yq eval 'select(.kind == "Kustomization") | .spec.kubeConfig.secretRef.key' pocs/capsule/tenants/alpha.yaml  # value.yaml (P40/P18)
grep -F 'mirror_kubeconfig_to_tenant_namespaces' scripts/register-poc-cluster.sh  # 2 hits
```

NO push to origin/main per D-08-13.

Live observation (Secret mirrored to tenant-{alpha,bravo} ns; per-tenant inner Ks Ready=True OR skip pre-push GitRepository) deferred to Plan 09-04 verifier.
</verification>

<success_criteria>
- 5 new yaml files exist under `pocs/capsule/tenants/` (alpha.yaml + bravo.yaml + alpha-app/kustomization.yaml + bravo-app/kustomization.yaml + kustomization.yaml)
- pocs/capsule/kustomization.yaml updated to add tenants/ to resources list (preserves Phase 7+8 head comment + appends Phase 9 update note)
- scripts/register-poc-cluster.sh extended with mirror_kubeconfig_to_tenant_namespaces function + invocation in main flow
- Each tenant inner Kustomization in alpha.yaml + bravo.yaml has: spec.serviceAccountName=gitops-reconciler (P27) + spec.kubeConfig.secretRef.{name,key}={spoke-capsule-kubeconfig,value.yaml} (P40/P18) + spec.dependsOn[0].name=poc-capsule + spec.dependsOn[0].namespace=flux-system + spec.wait=true (TOP-LEVEL per RESEARCH Gap 9)
- GitHub URL hard-coded to `https://github.com/richard-iovanisci/karyon` per RESEARCH Gap 10 Option A
- All 3 docs in alpha.yaml have `metadata.namespace: tenant-alpha` (RESEARCH Gap 1 same-namespace constraint)
- All 3 docs in bravo.yaml have `metadata.namespace: tenant-bravo`
- `kustomize build pocs/capsule/` resolves cleanly (10 CRs total)
- tenants-02-static-p27.bats GREEN; tenants-04-static-dependson.bats FULLY GREEN
- P31 isolation invariant preserved (bats poc-isolation-01-static.bats GREEN)
- shellcheck on register-poc-cluster.sh has no new errors
- Single commit landed locally; NO push to origin (D-08-13 deferral)
</success_criteria>

<output>
After completion, create `.planning/phases/09-tenants-flux-multi-tenancy-lockdown/09-02-SUMMARY.md` per the GSD summary template documenting:
- 7 files modified/created (5 new yaml + 1 modified yaml + 1 modified shell script)
- RESEARCH Gap 1 mitigation: register-poc-cluster.sh extension mirrors spoke-capsule-kubeconfig per tenant ns
- RESEARCH Gap 9 correction applied: spec.wait at TOP-LEVEL on per-tenant inner Ks
- P27 LOAD-BEARING DEFENSE in place: every tenant inner K has spec.serviceAccountName=gitops-reconciler
- tenants-02 + tenants-04 turned GREEN
- Forward-pointer: Plan 09-03 lands lockdown patches (high regression risk; pre/post task health-check rerun); Plan 09-04 verifier observes full reconcile chain (live Secret-mirror via re-running register-poc-cluster.sh + tenant inner K Ready)
</output>
</content>
</invoke>
