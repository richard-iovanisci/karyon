---
phase: 12
plan: 07
type: execute
wave: 2
depends_on: ["12-01", "12-02", "12-03", "12-04", "12-05", "12-06"]
files_modified:
  - .planning/v0.19-MILESTONE-AUDIT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/12-v0-19-close-out-reconciliation/12-VALIDATION.md
autonomous: false
requirements: []
tags: [bookkeeping, audit, phase-close, gate, v0.19]

must_haves:
  truths:
    - "`/gsd-audit-milestone v0.19` regenerates `.planning/v0.19-MILESTONE-AUDIT.md` and returns `status: passed` (success criterion 7 — phase-close gate)"
    - "If the audit returns `tech_debt` due to ADDON-01/02 being scored as `unsatisfied` (RESEARCH.md Open Question 3 / A1), apply the contingency: confirm via `audit.cjs` source whether `Future Requirements` REQ-IDs are excluded; if not, present the operator-decision checkpoint inline before declaring SC7 satisfied"
    - "Phase 12 self-close lands: ROADMAP.md Phase 12 row `Planned → Complete` + Completed cell `— → 2026-05-11` + Phase Summary checkbox `[ ] → [x]`; STATE.md `completed_phases: 5 → 6`, `completed_plans: 27 → 33`, `percent: 82 → 100`; Phase 12 VALIDATION.md frontmatter `nyquist_compliant: false → true` (Phase 12 has no Wave-0 scaffold so wave_0_complete: n/a stays as comment or the field stays `false` per VALIDATION.md §Wave 0 Requirements `None`); REQUIREMENTS.md has no remaining v0.19 `[ ]` mandatory checkboxes"
  artifacts:
    - path: .planning/v0.19-MILESTONE-AUDIT.md
      provides: "Regenerated audit report (status: passed) — replaces the 2026-05-07 tech_debt snapshot"
      contains: "status: passed"
    - path: .planning/ROADMAP.md
      provides: "Phase 12 self-close — Progress table row Complete + Phase Summary checkbox [x]"
      contains: "| 12. v0.19 Close-out Reconciliation | v0.19 | 7/7 | Complete | 2026-05-11 |"
    - path: .planning/STATE.md
      provides: "Phase 12 completion + milestone-ready advance"
      contains: "completed_phases: 6"
    - path: .planning/phases/12-v0-19-close-out-reconciliation/12-VALIDATION.md
      provides: "Phase 12 VALIDATION ledger updated at phase-close (per VALIDATION.md §Validation Sign-Off final bullet)"
      contains: "nyquist_compliant: true"
  key_links:
    - from: .planning/v0.19-MILESTONE-AUDIT.md
      to: ".planning/REQUIREMENTS.md + .planning/ROADMAP.md + .planning/STATE.md + per-phase VERIFICATION.md + per-phase VALIDATION.md"
      via: "Audit re-run reads all 5 source classes; verifies 0 stale checkboxes, 0 contradictions, 5/5 Nyquist compliant"
      pattern: "status: passed"
---

<objective>
**Wave 2 gate.** Re-run `/gsd-audit-milestone v0.19` AFTER all Wave 1 plans
(12-01 through 12-06) complete, verify the audit returns `passed`, and then
flip the Phase 12 self-close markers across ROADMAP.md + STATE.md +
REQUIREMENTS.md + 12-VALIDATION.md. This is the FINAL plan of Phase 12 and
the entire v0.19 milestone close-out reconciliation.

Purpose: Success Criterion 7 ("`/gsd-audit-milestone v0.19` returns `passed`")
is the canonical phase gate. The audit reads REQUIREMENTS.md, ROADMAP.md,
STATE.md, and each phase's VERIFICATION.md + SUMMARY.md + VALIDATION.md and
cross-references. After Wave 1 lands (REQ checkboxes flipped, STATE updated,
11-VERIFICATION superseded, 5 VALIDATION ledgers closed, stale todo moved),
the audit re-run should report `passed` with zero remaining `tech_debt`.

Output:
1. Regenerated `.planning/v0.19-MILESTONE-AUDIT.md` (overwritten by the
   audit workflow, status: passed).
2. Phase 12 self-close edits to ROADMAP.md, STATE.md, REQUIREMENTS.md (no
   v0.19 REQ-IDs to flip — but `## Traceability` table updated to reflect
   Phase 12 closure), and 12-VALIDATION.md.
3. A human-verify checkpoint to confirm the audit output before declaring
   the phase complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v0.19-MILESTONE-AUDIT.md
@.planning/phases/12-v0-19-close-out-reconciliation/12-VALIDATION.md
@.planning/phases/12-v0-19-close-out-reconciliation/12-RESEARCH.md
@.planning/phases/12-v0-19-close-out-reconciliation/12-PATTERNS.md
</context>

<truths>
- T-1: HEAD has advanced past audit HEAD; live state is the source of truth. The 2026-05-07 audit was at HEAD 8415ab66; this plan runs the audit at the post-Wave-1 HEAD (which will be 7 commits past 83b81bb5).
- T-5: Phase 12 has no own Wave-0 / Nyquist scaffold per 12-VALIDATION.md §Wave 0 Requirements `None`. At phase-close, this plan flips its own VALIDATION ledger using the lighter shape (no `finalized_evidence:` for a bookkeeping phase that has no bats scaffold — but `status: final` + `nyquist_compliant: true` + `wave_0_complete: true` per the conventional terminal state; alternatively keep wave_0_complete as `n/a` style with explanation).
- RESEARCH.md A1 / Open Question 3: ADDON-01/02 disposition in the audit re-run is the known sharp edge. The audit MAY report them as `unsatisfied` even though they're in Future Requirements. The contingency is documented inline.
- Audit re-run is invoked via `/gsd-audit-milestone v0.19` per RESEARCH.md §"Audit Re-run Mechanism". The workflow reads ROADMAP, REQUIREMENTS, STATE, per-phase VERIFICATION/SUMMARY/VALIDATION, spawns gsd-integration-checker, and overwrites `.planning/v0.19-MILESTONE-AUDIT.md` with the new audit report.
- Per the workflow, `passed` requires: all REQ-IDs satisfied; no critical gaps; minimal tech debt. RESEARCH.md §"Likely-failing audit pass currently and why" lists the 6 reasons the current audit fails — Wave 1 of Phase 12 closes 5 of those (REQ checkboxes, Progress table contradiction (already closed), STATE.md stale, 11-* artifacts pre-date 11-07, stale todo, 4 Nyquist ledgers). The 6th — ADDON-01/02 scoring — depends on the audit logic's Future Requirements handling.
</truths>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight check — verify all Wave 1 acceptance criteria hold before audit re-run</name>
  <files>(read-only verification of files modified by 12-01..12-06)</files>
  <read_first>
    - All Plan 12-01..12-06 SUMMARY.md files (confirm each Wave-1 plan reported its done state)
    - .planning/REQUIREMENTS.md (REQ checkboxes)
    - .planning/STATE.md (frontmatter)
    - .planning/phases/11-validation-graduation-adr-008/11-VERIFICATION.md + 11-PHASE-VERIFICATION.md (supersedure blocks)
    - .planning/phases/{07,08,09,10,11}-*/[01]*-VALIDATION.md (status: final)
    - .planning/todos/completed/2026-04-29-phase-7-hub-flux-observable-reconcile.md (move + appendix)
  </read_first>
  <action>
    Run consolidated grep checks to confirm Wave 1 landed:

    ```bash
    # Wave 1 success state — 7 grep checks
    test $(grep -cE "^- \[x\] \*\*(POC|CAPCLU|EKSDOC|CAP)-" .planning/REQUIREMENTS.md) -ge 12 && \
    test $(grep -cE "^- \[x\] \*\*TEN-" .planning/REQUIREMENTS.md) -eq 6 && \
    test $(grep -cE "^last_activity: 2026-05-11" .planning/STATE.md) -eq 1 && \
    test $(grep -cE "^## Supersedure — 2026-05-11$" .planning/phases/11-validation-graduation-adr-008/11-VERIFICATION.md) -eq 1 && \
    test $(grep -cE "^## Supersedure — 2026-05-11$" .planning/phases/11-validation-graduation-adr-008/11-PHASE-VERIFICATION.md) -eq 1 && \
    test $(for f in .planning/phases/0{7,8,9}-*/[01]*-VALIDATION.md .planning/phases/1{0,1}-*/[01]*-VALIDATION.md; do grep -l "^status: final$" "$f"; done | wc -l) -eq 5 && \
    test -f .planning/todos/completed/2026-04-29-phase-7-hub-flux-observable-reconcile.md && \
    test ! -f .planning/todos/pending/2026-04-29-phase-7-hub-flux-observable-reconcile.md
    ```

    If ANY check fails: HALT. Phase 12 has a regression in Wave 1; report which check failed and direct to revision mode for the affected Plan 12-NN.

    If ALL checks pass: proceed to Task 2.
  </action>
  <verify>
    <automated>
test $(grep -cE "^- \[x\] \*\*(POC|CAPCLU|EKSDOC|CAP)-" .planning/REQUIREMENTS.md) -ge 12 && \
test $(grep -cE "^- \[x\] \*\*TEN-" .planning/REQUIREMENTS.md) -eq 6 && \
test $(grep -cE "^last_activity: 2026-05-11" .planning/STATE.md) -eq 1 && \
test -f .planning/todos/completed/2026-04-29-phase-7-hub-flux-observable-reconcile.md
    </automated>
  </verify>
  <acceptance_criteria>
    - 12+ POC/CAPCLU/EKSDOC/CAP REQ-IDs are `[x]` (Plan 12-01 Task 1 closure check)
    - 6 TEN-* REQ-IDs are `[x]` (Plan 12-01 Tasks 1+2 closure check)
    - STATE.md `last_activity: 2026-05-11` (Plan 12-02 Task 1)
    - Both supersedure blocks present in 11-* files (Plan 12-03)
    - 5 VALIDATION.md files report `status: final` (Plan 12-04)
    - Phase 7 hub-flux observable-reconcile todo moved to completed/ (Plan 12-05)
  </acceptance_criteria>
  <done>
    All Wave 1 closure conditions verified; safe to invoke the audit re-run.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run `/gsd-audit-milestone v0.19` and verify the regenerated audit reports `passed`</name>
  <files>.planning/v0.19-MILESTONE-AUDIT.md</files>
  <read_first>
    - .planning/v0.19-MILESTONE-AUDIT.md (current — will be overwritten by the workflow)
    - ~/.claude/get-shit-done/workflows/audit-milestone.md (the audit workflow specification)
    - .planning/phases/12-v0-19-close-out-reconciliation/12-RESEARCH.md §"Audit Re-run Mechanism" + §"Pitfall 5"
  </read_first>
  <action>
    Invoke the audit workflow:

    ```
    /gsd-audit-milestone v0.19
    ```

    This regenerates `.planning/v0.19-MILESTONE-AUDIT.md`. After the workflow completes, read the frontmatter status:

    ```bash
    head -10 .planning/v0.19-MILESTONE-AUDIT.md
    ```

    **Expected primary case:** Frontmatter `status: passed`; `gaps.requirements: []` or contains only ADDON-01/02 (which are Future Requirements / v0.20 candidates, not v0.19 mandatory); `tech_debt: []` (empty); `nyquist.overall: 5/5` (4 phases newly closed + Phase 9 already-closed).

    **Contingency 1 — Audit returns `tech_debt` only because of ADDON-01/02:**
    Per RESEARCH.md §"Pitfall 5" and §"Audit Re-run Mechanism", the audit logic may include `Future Requirements` REQ-IDs in its scoring. If `gaps.requirements:` contains ONLY ADDON-01/02 AND `tech_debt:` lists no other items, the operator must decide whether `tech_debt` is acceptable as the close-out state OR whether to drop ADDON-01/02 from REQUIREMENTS.md altogether (moving to `.planning/milestones/v0.19-archive/` or removing entirely).

    Read `~/.claude/get-shit-done/bin/lib/audit.cjs` (or equivalent) to determine the audit's Future Requirements handling logic BEFORE proceeding to the operator checkpoint. If the audit excludes Future Requirements, status should already be `passed`; if it doesn't, the ADDON-01/02 issue blocks SC7 strict reading.

    **Contingency 2 — Audit returns `tech_debt` for non-ADDON reasons:**
    Print the audit's `gaps.requirements`, `gaps.integration`, `gaps.flows`, and `tech_debt` arrays. Identify the missed item from Wave 1. Open a revision-mode task on the affected Plan 12-NN. Do not proceed to Task 3 until the audit returns `passed`.

    **Contingency 3 — Audit returns `gaps_found`:**
    Same as Contingency 2 but more severe. Investigate immediately; this means a Wave 1 plan did not fully close its target gap.

    Read the new audit's status and emit a one-line summary to the SUMMARY.md notable observations section.
  </action>
  <verify>
    <automated>head -10 .planning/v0.19-MILESTONE-AUDIT.md | grep -E "^status: (passed|tech_debt|gaps_found)$"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/v0.19-MILESTONE-AUDIT.md` is overwritten (`git diff --stat .planning/v0.19-MILESTONE-AUDIT.md` shows changes)
    - Frontmatter `status:` is either `passed` (primary case) OR `tech_debt` with ADDON-01/02 as the sole gap (Contingency 1)
    - If status is `tech_debt` for non-ADDON reasons: revision mode triggered; this task remains incomplete until revision lands
    - SUMMARY notes the audit status + gaps/tech_debt arrays verbatim
  </acceptance_criteria>
  <done>
    Audit regenerated; status is `passed` OR `tech_debt` is acceptable per Contingency 1 + checkpoint approval (Task 4 handles the human decision).
  </done>
</task>

<task type="auto">
  <name>Task 3: Phase 12 self-close — flip ROADMAP, STATE, 12-VALIDATION, REQUIREMENTS Traceability table</name>
  <files>
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/phases/12-v0-19-close-out-reconciliation/12-VALIDATION.md
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/ROADMAP.md (Phase Summary line 44 + Progress table line 167 — the two rows for Phase 12)
    - .planning/STATE.md frontmatter (updated by Plan 12-02; flip completed_phases / completed_plans / percent + status to ready_to_archive)
    - .planning/phases/12-v0-19-close-out-reconciliation/12-VALIDATION.md (Phase 12 own ledger — see 12-VALIDATION.md §Validation Sign-Off final bullet "nyquist_compliant: true set in frontmatter at phase close")
    - .planning/REQUIREMENTS.md §Traceability table lines ~150-180 — verify whether Phase 12 needs a row added (planning context says no new REQ-IDs but the Traceability table convention may need a "Phase 12: bookkeeping" note)
  </read_first>
  <action>
    **Edit 1 — ROADMAP.md Phase Summary line 44.**

    `old_string`:
    `- [ ] **Phase 12: v0.19 Close-out Reconciliation** — Gap closure phase created by `/gsd-plan-milestone-gaps` 2026-05-11. Reconcile REQUIREMENTS.md stale checkboxes (15 REQs), refresh ROADMAP Progress table, update STATE.md, supersede stale Phase 11 verification artifacts, close 4 Nyquist VALIDATION ledgers, resolve TEN-01 `forceTenantPrefix` spec deviation per D-11-07, archive Phase 7 carryover todo. (Replaces the original Phase 12 `capsule-addon-fluxcd` trial; addon trial deferred to v0.20.)`

    `new_string`:
    `- [x] **Phase 12: v0.19 Close-out Reconciliation** — Gap closure phase created by `/gsd-plan-milestone-gaps` 2026-05-11; completed 2026-05-11 (7/7 plans). Reconciled REQUIREMENTS.md (18 v0.19 mandatory checkboxes flipped; TEN-01 spec amended per D-11-07), STATE.md (last_activity past Plan 11-07; Phase 12 entered), 11-VERIFICATION.md + 11-PHASE-VERIFICATION.md (Supersedure 2026-05-11 blocks appended; disposition `passed`), Nyquist VALIDATION ledgers (phases 7, 8, 9, 10, 11 → status: final), Phase 7 hub-flux observable-reconcile todo (moved to completed/). Audit re-run returned `passed` per 12-07-SUMMARY.md. (Replaces the original Phase 12 `capsule-addon-fluxcd` trial; addon trial deferred to v0.20.)`

    **Edit 2 — ROADMAP.md Progress table line 167.**

    `old_string`:
    `| 12. v0.19 Close-out Reconciliation | v0.19 | 0/0 | Planned | — |`

    `new_string`:
    `| 12. v0.19 Close-out Reconciliation | v0.19 | 7/7 | Complete | 2026-05-11 |`

    **Edit 3 — STATE.md frontmatter (advance counters past Phase 12 completion + flip status).**

    `old_string` (Plan 12-02's resulting frontmatter):
    ```
    ---
    gsd_state_version: 1.0
    milestone: v0.19
    milestone_name: — Capsule Multi-Tenancy POC
    status: executing
    stopped_at: Phase 12 in progress — v0.19 close-out reconciliation
    last_updated: "2026-05-11T00:00:00.000Z"
    last_activity: 2026-05-11 -- Plan 11-07 closed (G-06 diagnostics resolved, HEAD 8415ab66); Phase 12 close-out reconciliation entered
    progress:
      total_phases: 6
      completed_phases: 5
      total_plans: 33
      completed_plans: 27
      percent: 82
    ---
    ```

    `new_string`:
    ```
    ---
    gsd_state_version: 1.0
    milestone: v0.19
    milestone_name: — Capsule Multi-Tenancy POC
    status: ready_to_archive
    stopped_at: Phase 12 complete — v0.19 close-out reconciliation done; milestone ready for /gsd-complete-milestone v0.19
    last_updated: "2026-05-11T12:00:00.000Z"
    last_activity: 2026-05-11 -- Phase 12 complete; audit re-run passed; v0.19 milestone ready to archive
    progress:
      total_phases: 6
      completed_phases: 6
      total_plans: 33
      completed_plans: 33
      percent: 100
    ---
    ```

    **Edit 4 — STATE.md body Current Position block.**

    `old_string`:
    ```
    ## Current Position

    Phase: 12 (v0-19-close-out-reconciliation) — EXECUTING
    Plan: 1 of 6 (12-01 in flight; clusters A-F run as Wave 1 parallel; gate 12-06 in Wave 2)
    Status: Executing Phase 12 close-out reconciliation (book-keeping; mutates only `.planning/*.md`)
    Last activity: 2026-05-11 -- Plan 11-07 closed (HEAD 8415ab66); Phase 12 entered
    ```

    `new_string`:
    ```
    ## Current Position

    Phase: 12 (v0-19-close-out-reconciliation) — COMPLETE
    Plan: 7 of 7 (all plans landed)
    Status: v0.19 milestone ready to archive via /gsd-complete-milestone v0.19
    Last activity: 2026-05-11 -- Phase 12 complete; v0.19-MILESTONE-AUDIT.md regenerated to `passed`
    ```

    **Edit 5 — 12-VALIDATION.md frontmatter (Phase 12's own ledger close).**

    `old_string`:
    ```
    ---
    phase: 12
    slug: v0-19-close-out-reconciliation
    status: draft
    nyquist_compliant: false
    wave_0_complete: false
    created: 2026-05-11
    ---
    ```

    `new_string`:
    ```
    ---
    phase: 12
    slug: v0-19-close-out-reconciliation
    status: final
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-05-11
    finalized: 2026-05-11
    finalized_evidence: "Phase 12 is a book-keeping reconciliation phase with no own Wave-0 scaffold (per 12-VALIDATION.md §Wave 0 Requirements `None`; mutates only `.planning/*.md` files; verification surface IS file content). Phase gate `/gsd-audit-milestone v0.19` returns `passed` per 12-07-SUMMARY.md (audit re-run 2026-05-11). nyquist_compliant set true because every Wave-1 plan's `<acceptance_criteria>` is grep-verifiable and was verified during execution; wave_0_complete set true because no Wave 0 was required per the phase character (book-keeping; T-5 in 12-07 plan)."
    ---
    ```

    **Edit 6 — REQUIREMENTS.md Traceability table (add a Phase 12 row if convention allows; otherwise add a footer note).**

    Per RESEARCH.md §"Architecture Patterns" §A4 (commit cadence) and Plan 12-01 Task 1 design (Phase 12 closes REQs against existing implementation evidence; no new REQ-IDs), the Traceability table can either:
    - (a) Be unchanged (the table tracks mappings, and Phase 12 didn't add mappings; the existing rows are still correct).
    - (b) Add a single footer note: "Phase 12 closed all 18 v0.19 mandatory REQ checkboxes against existing implementation evidence at HEAD 8415ab66 per v0.19-MILESTONE-AUDIT.md (audit re-run status: passed). No new REQ-IDs added."

    **Use option (b)** for traceability completeness. Append after the existing Total line (REQUIREMENTS.md line 180):

    `old_string` (the existing Total line):
    `**Total:** 27 requirements across 5 mandatory phases (Phases 7-11). Phase 12 is the close-out reconciliation phase added 2026-05-11; it has no new REQ-IDs. ADDON-01 and ADDON-02 (originally Phase 12) are deferred to v0.20 — see Future Requirements §"Deferred from v0.19".`

    `new_string`:
    `**Total:** 27 requirements across 5 mandatory phases (Phases 7-11). Phase 12 is the close-out reconciliation phase added 2026-05-11; it has no new REQ-IDs. ADDON-01 and ADDON-02 (originally Phase 12) are deferred to v0.20 — see Future Requirements §"Deferred from v0.19".

    Phase 12 closed all 18 v0.19 mandatory REQ checkboxes (POC-01..04, CAPCLU-01..04, EKSDOC-01, CAP-01..03, TEN-01..06) against the implementation evidence at HEAD 8415ab66 per .planning/v0.19-MILESTONE-AUDIT.md (audit re-run 2026-05-11 status: passed). No new REQ-IDs added by Phase 12.`
  </action>
  <verify>
    <automated>
test $(grep -cE "^- \[x\] \*\*Phase 12:" .planning/ROADMAP.md) -eq 1 && \
test $(grep -cE "^\| 12\. .*\| v0.19 \| 7/7 \| Complete \| 2026-05-11 \|$" .planning/ROADMAP.md) -eq 1 && \
test $(grep -c "^status: ready_to_archive$" .planning/STATE.md) -eq 1 && \
test $(grep -c "^  completed_phases: 6$" .planning/STATE.md) -eq 1 && \
test $(grep -c "^  percent: 100$" .planning/STATE.md) -eq 1 && \
test $(grep -c "^status: final$" .planning/phases/12-v0-19-close-out-reconciliation/12-VALIDATION.md) -eq 1
    </automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 12 Phase Summary checkbox is `[x]`: `grep -c "^- \[x\] \*\*Phase 12:" .planning/ROADMAP.md` returns 1
    - ROADMAP.md Phase 12 Progress table row is Complete 2026-05-11: `grep -c "^| 12\..*7/7 | Complete | 2026-05-11 |$" .planning/ROADMAP.md` returns 1
    - STATE.md `status: ready_to_archive`; completed_phases: 6; completed_plans: 33; percent: 100
    - 12-VALIDATION.md `status: final`; nyquist_compliant: true; wave_0_complete: true; finalized_evidence present
    - REQUIREMENTS.md Traceability footer note about Phase 12 closure present
  </acceptance_criteria>
  <done>
    Phase 12 self-close edits landed; milestone is ready_to_archive; audit returns `passed`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — confirm audit status and self-close edits</name>
  <what-built>
    Tasks 1-3 verified Wave 1 closure, re-ran the audit, and applied Phase 12 self-close edits. The operator must confirm the audit's status decision before this plan reports DONE.
  </what-built>
  <how-to-verify>
    1. Read the audit output:
       ```
       head -20 .planning/v0.19-MILESTONE-AUDIT.md
       ```
       Expect `status: passed` OR `status: tech_debt` with ONLY ADDON-01/02 in gaps.requirements (and tech_debt: [] otherwise).

    2. If `passed` — approve and proceed.

    3. If `tech_debt` for ADDON-01/02 only (RESEARCH.md Open Question 3 / A1):
       Decide which path:
       - **Path X (accept):** Acknowledge ADDON-01/02 as acceptable v0.20-deferred tech_debt; declare SC7 satisfied with `tech_debt: <ADDON-only>` per the criterion's lenient reading (deferred-not-blocker = acceptable; per RESEARCH.md §"Conditions for `passed`").
       - **Path Y (purge):** Remove ADDON-01/02 from REQUIREMENTS.md entirely (move definitions to `.planning/milestones/v0.19-archive/`); re-run the audit; expect `passed`.

    4. If `tech_debt` for non-ADDON reasons OR `gaps_found`:
       Open revision mode on the affected Plan 12-NN; do NOT mark this task done.

    5. Verify the Phase 12 self-close edits:
       - `grep "^- \[x\] \*\*Phase 12:" .planning/ROADMAP.md` returns 1
       - `grep "status: ready_to_archive" .planning/STATE.md` returns 1
       - `grep "status: final" .planning/phases/12-v0-19-close-out-reconciliation/12-VALIDATION.md` returns 1

    6. After approval, the orchestrator may proceed to `/gsd-complete-milestone v0.19` to archive the milestone.
  </how-to-verify>
  <resume-signal>
    Type "approved" OR "path-x" OR "path-y" (per the audit outcome). Describe any issues to trigger revision mode.
  </resume-signal>
  <acceptance_criteria>
    - Operator confirms audit status decision (`passed`, `path-x`, or `path-y`)
    - All grep checks in step 5 return their expected values
    - SUMMARY.md notable observations records the operator's resume signal verbatim
  </acceptance_criteria>
  <done>
    Operator approval received; Phase 12 closed; v0.19 milestone ready to archive.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Audit re-run + bookkeeping flips; no new code paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07-01 | N/A | Audit re-run + Phase 12 self-close edits | accept | No new threats — documentation reconciliation only; existing v0.19 security posture per `11-VERIFICATION.md` §VAL-01 through VAL-05 (now with Supersedure 2026-05-11 reflecting post-fix state) is unchanged. Audit workflow reads files only; does not introduce new attack surface. |
</threat_model>

<verification>
- Task 1: All Wave 1 acceptance criteria hold.
- Task 2: `head -10 .planning/v0.19-MILESTONE-AUDIT.md` shows `status: passed` (primary) or `status: tech_debt` with ADDON-only gaps (Contingency 1 with operator approval).
- Task 3: All 6 self-close grep checks pass.
- Task 4: Operator approves with resume signal recorded.
</verification>

<success_criteria>
This plan satisfies ROADMAP Phase 12 Success Criterion 7 ("Re-run `/gsd-audit-milestone v0.19` returns `passed`"). Combined with Plans 12-01..12-06, the plan-set satisfies all 7 success criteria of Phase 12.
</success_criteria>

<output>
After completion, create `.planning/phases/12-v0-19-close-out-reconciliation/12-07-SUMMARY.md` per `~/.claude/get-shit-done/templates/summary.md`. requirements-completed: [] (no REQ-IDs; bookkeeping plan). Notable Observations:
- Audit re-run status (passed / tech_debt / gaps_found) + verbatim output frontmatter
- Operator resume signal (approved / path-x / path-y / revision-trigger)
- Final v0.19 milestone state (ready_to_archive; ready for /gsd-complete-milestone v0.19)
</output>
