---
phase: 12
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [POC-01, POC-02, POC-03, POC-04, CAPCLU-01, CAPCLU-02, CAPCLU-03, CAPCLU-04, EKSDOC-01, CAP-01, CAP-02, CAP-03, TEN-01, TEN-02, TEN-03, TEN-04, TEN-05, TEN-06]
tags: [bookkeeping, requirements, close-out, v0.19]

must_haves:
  truths:
    - "All 18 stale `[ ]` checkboxes for v0.19 mandatory REQ-IDs (POC-01..04, CAPCLU-01..04, EKSDOC-01, CAP-01..03, TEN-01..06) are flipped to `[x]` in REQUIREMENTS.md"
    - "TEN-01 spec text reflects the live `forceTenantPrefix: false` repair from Plan 11-07 D-11-07 (path-a amendment per RESEARCH.md Open Question 4)"
    - "ADDON-01 and ADDON-02 stay `[ ]` (deferred to v0.20 per Future Requirements section)"
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: "Updated REQ ledger with v0.19 mandatory checkboxes flipped and TEN-01 spec amended"
      contains: "forceTenantPrefix: false per Plan 11-07 D-11-07"
  key_links:
    - from: .planning/REQUIREMENTS.md
      to: pocs/capsule/spoke/tenant-crs/{alpha,bravo}.yaml
      via: "TEN-01 spec text now matches live YAML at HEAD 8415ab66 (commit faecf14)"
      pattern: "forceTenantPrefix: false"
---

<objective>
Flip the 18 stale `[ ]` checkboxes for v0.19 mandatory REQ-IDs in
`.planning/REQUIREMENTS.md` to `[x]` and amend TEN-01 spec text to reflect the
live `forceTenantPrefix: false` repair from Plan 11-07 D-11-07. This is Cluster
A of the Phase 12 close-out reconciliation (RESEARCH.md §"Wave / Parallelism
Opportunity" Wave 1).

Purpose: REQUIREMENTS.md is the canonical milestone REQ ledger. The 18 v0.19
mandatory checkboxes (POC-01..04, CAPCLU-01..04, EKSDOC-01, CAP-01..03,
TEN-01..06) have all been WIRED at code/integration level per the
2026-05-07 milestone audit and Plan 11-07 closeout, but the ledger lags
reality.

Output: REQUIREMENTS.md with all 18 v0.19 mandatory checkboxes set to `[x]`
and TEN-01 spec body amended inline; PROXY-01..03 + VAL-01..06 stay `[x]`
unchanged; ADDON-01..02 stay `[ ]` (deferred to v0.20).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v0.19-MILESTONE-AUDIT.md
@.planning/phases/12-v0-19-close-out-reconciliation/12-RESEARCH.md
@.planning/phases/12-v0-19-close-out-reconciliation/12-PATTERNS.md
</context>

<truths>
- T-1: HEAD has advanced past audit HEAD (audit: 8415ab66; HEAD: 83b81bb5). Live state is the source of truth.
- T-4: TEN-01 resolution is via REQUIREMENTS.md amend (path-a, per RESEARCH.md Open Question 4 recommendation), NOT ADR-008 amendment. Lower blast radius; REQUIREMENTS.md is the canonical spec.
- The 18 IDs above are the actual `[ ]` count on REQUIREMENTS.md lines 30-63 — the roadmap prose says "15" but the literal grep returns 18 (the phase character note in the planning context acknowledged this).
- ADDON-01..02 (lines 105-106 Future Requirements) MUST stay `[ ]` — they are deferred to v0.20.
- The 9 future-requirement `[ ]` lines (Inherited from v0.18 close, Future Requirements §"Surfaced by v0.19 research") MUST stay `[ ]`.
</truths>

<tasks>

<task type="auto">
  <name>Task 1: Flip 17 same-line REQ checkboxes (all v0.19 mandatory except TEN-01)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 30-63 (the v0.19 mandatory REQ section — confirms current `[ ]` state and exact line contents)
    - .planning/v0.19-MILESTONE-AUDIT.md §1 "Requirements Coverage" table lines 102-129 (the WIRED evidence for each REQ-ID — confirms all 27 mandatory at code/integration level)
    - .planning/phases/12-v0-19-close-out-reconciliation/12-PATTERNS.md §A "REQUIREMENTS.md checkbox flip" (verbatim diff pattern from commit 4cd1837)
  </read_first>
  <action>
    Use the Edit tool 17 times (one Edit call per REQ-ID) to flip `- [ ] **<ID>**` to `- [x] **<ID>**`. Preserve every other byte of each line exactly. Per the 12-PATTERNS.md §A pattern (commit 4cd1837 verbatim), the rest of the line stays unchanged.

    Exact 17 edits (line numbers per REQUIREMENTS.md HEAD `83b81bb5` — confirmed via `grep -n "^- \[ \] \*\*\(POC\|CAPCLU\|EKSDOC\|CAP\|TEN\)-"`):

    Line 30: replace `- [ ] **POC-01**` with `- [x] **POC-01**`
    Line 31: replace `- [ ] **POC-02**` with `- [x] **POC-02**`
    Line 32: replace `- [ ] **POC-03**` with `- [x] **POC-03**`
    Line 33: replace `- [ ] **POC-04**` with `- [x] **POC-04**`
    Line 37: replace `- [ ] **CAPCLU-01**` with `- [x] **CAPCLU-01**`
    Line 38: replace `- [ ] **CAPCLU-02**` with `- [x] **CAPCLU-02**`
    Line 39: replace `- [ ] **CAPCLU-03**` with `- [x] **CAPCLU-03**`
    Line 40: replace `- [ ] **CAPCLU-04**` with `- [x] **CAPCLU-04**`
    Line 44: replace `- [ ] **EKSDOC-01**` with `- [x] **EKSDOC-01**`
    Line 50: replace `- [ ] **CAP-01**` with `- [x] **CAP-01**`
    Line 51: replace `- [ ] **CAP-02**` with `- [x] **CAP-02**`
    Line 52: replace `- [ ] **CAP-03**` with `- [x] **CAP-03**`
    Line 59: replace `- [ ] **TEN-02**` with `- [x] **TEN-02**`
    Line 60: replace `- [ ] **TEN-03**` with `- [x] **TEN-03**`
    Line 61: replace `- [ ] **TEN-04**` with `- [x] **TEN-04**`
    Line 62: replace `- [ ] **TEN-05**` with `- [x] **TEN-05**`
    Line 63: replace `- [ ] **TEN-06**` with `- [x] **TEN-06**`

    **Skip line 58 (TEN-01) in this task — that line is handled in Task 2 because it requires both the checkbox flip AND a spec text amendment in the SAME Edit call.**

    DO NOT touch ADDON-01 / ADDON-02 (Future Requirements lines 105-106).
    DO NOT touch lines 98-118 (Future Requirements + research deferrals — those stay `[ ]`).
    DO NOT touch line 30+ checkboxes already `[x]` (PROXY-01..03 + VAL-01..06).

    Because the Edit tool requires unique `old_string`, use enough surrounding text on each Edit call so `old_string` matches exactly one line. For each ID, an unambiguous `old_string` is the full bold-id literal (e.g., `- [ ] **POC-01**`). All 17 are unique because each ID is unique.
  </action>
  <verify>
    <automated>grep -c "^- \[x\] \*\*\(POC\|CAPCLU\|EKSDOC\|CAP\)-" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*POC-0[1-4]\*\*" .planning/REQUIREMENTS.md` returns 4
    - `grep -c "^- \[x\] \*\*CAPCLU-0[1-4]\*\*" .planning/REQUIREMENTS.md` returns 4
    - `grep -c "^- \[x\] \*\*EKSDOC-01\*\*" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[x\] \*\*CAP-0[1-3]\*\*" .planning/REQUIREMENTS.md` returns 3
    - `grep -c "^- \[x\] \*\*TEN-0[2-6]\*\*" .planning/REQUIREMENTS.md` returns 5
    - `grep -c "^- \[ \] \*\*\(POC\|CAPCLU\|EKSDOC\|CAP\|TEN\)-" .planning/REQUIREMENTS.md` returns 1 (only TEN-01 still `[ ]` at this point — Task 2 handles it)
    - `grep -c "^- \[ \] \*\*ADDON-0[12]\*\*" .planning/REQUIREMENTS.md` returns 2 (ADDON-01/02 stay `[ ]`)
  </acceptance_criteria>
  <done>
    17 of 18 v0.19 mandatory REQ-IDs checked `[x]`; ADDON-01/02 unchanged; Future Requirements section unchanged; line 58 (TEN-01) still `[ ]` pending Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip TEN-01 checkbox + amend spec text inline (forceTenantPrefix: true → false annotation)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md line 58 (the current TEN-01 line — confirm exact wording)
    - .planning/v0.19-MILESTONE-AUDIT.md §"Spec-vs-code deviation (TEN-01)" (lines 210-213 of the audit body — "forceTenantPrefix: false per Plan 11-07 D-11-07 live repair...commit faecf14")
    - .planning/phases/11-validation-graduation-adr-008/11-07-SUMMARY.md (canonical evidence for the live repair — references D-11-07 and commit faecf14)
    - .planning/phases/12-v0-19-close-out-reconciliation/12-RESEARCH.md §"Example 2" (the exact replacement string verbatim)
    - .planning/phases/12-v0-19-close-out-reconciliation/12-PATTERNS.md §B "REQUIREMENTS.md TEN-01 spec text amend" (the same Example 2 in PATTERN form)
  </read_first>
  <action>
    Use ONE Edit tool call against REQUIREMENTS.md line 58. Per RESEARCH.md Example 2 and 12-PATTERNS.md §B:

    `old_string` (entire current line 58, verbatim):
    `- [ ] **TEN-01** — Two `Tenant` custom resources (`alpha`, `bravo`) exist with disjoint `owners` arrays (each kind ServiceAccount, registered as `<tenant-ns>:gitops-reconciler`); namespace prefix enforcement enabled (`forceTenantPrefix: true`)`

    `new_string` (verbatim replacement; flipped checkbox + amended spec text + inline citation):
    `- [x] **TEN-01** — Two `Tenant` custom resources (`alpha`, `bravo`) exist with disjoint `owners` arrays (each kind ServiceAccount, registered as `<tenant-ns>:gitops-reconciler`); namespace prefix enforcement DISABLED (`forceTenantPrefix: false` per Plan 11-07 D-11-07 live repair — preserves established `tenant-alpha` / `tenant-bravo` home namespace names; see `11-07-SUMMARY.md` and commit `faecf14`)`

    Differences from old_string:
    - `[ ]` → `[x]` (checkbox flipped)
    - `enabled (`forceTenantPrefix: true`)` → `DISABLED (`forceTenantPrefix: false` per Plan 11-07 D-11-07 live repair — preserves established `tenant-alpha` / `tenant-bravo` home namespace names; see `11-07-SUMMARY.md` and commit `faecf14`)`
    - All other bytes identical.

    This is path-a per RESEARCH.md Open Question 4 (REQUIREMENTS.md amend, NOT ADR-008 amendment). No edits to docs/adr/0008-*.md.
  </action>
  <verify>
    <automated>grep -c "TEN-01.*forceTenantPrefix: false.*Plan 11-07 D-11-07" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*TEN-01\*\*" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[ \] \*\*TEN-01\*\*" .planning/REQUIREMENTS.md` returns 0
    - `grep -c "TEN-01.*forceTenantPrefix: false.*Plan 11-07 D-11-07.*commit \`faecf14\`" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[ \] \*\*\(POC\|CAPCLU\|EKSDOC\|CAP\|TEN\)-" .planning/REQUIREMENTS.md` returns 0 (combined with Task 1: all 18 v0.19 mandatory REQ-IDs now `[x]`)
    - `grep -c "^- \[ \] \*\*ADDON-0[12]\*\*" .planning/REQUIREMENTS.md` returns 2 (Future Requirements unchanged)
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md TEN-01 row is `[x]`, body amended to reflect live `forceTenantPrefix: false` with inline D-11-07 + faecf14 citation. All 18 v0.19 mandatory REQ-IDs are `[x]`. ADDON-01/02 + research-deferred items unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation reconciliation only; no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | N/A | REQUIREMENTS.md text edits | accept | No new threats — documentation reconciliation only; existing v0.19 security posture per `11-VERIFICATION.md` §VAL-01 through VAL-05 is unchanged. No auth, no input validation, no crypto, no data flow introduced or modified. |
</threat_model>

<verification>
- All 18 mandatory v0.19 REQ-ID checkboxes are `[x]`: `grep -c "^- \[x\] \*\*\(POC\|CAPCLU\|EKSDOC\|CAP\|TEN\)-" .planning/REQUIREMENTS.md` returns 18.
- Zero v0.19 mandatory REQ-IDs remain `[ ]`: `grep -c "^- \[ \] \*\*\(POC\|CAPCLU\|EKSDOC\|CAP\|TEN\)-" .planning/REQUIREMENTS.md` returns 0.
- ADDON-01..02 remain `[ ]` (Future Requirements): `grep -c "^- \[ \] \*\*ADDON-0[12]\*\*" .planning/REQUIREMENTS.md` returns 2.
- TEN-01 spec amended inline: `grep -c "TEN-01.*forceTenantPrefix: false" .planning/REQUIREMENTS.md` returns 1.
- File does not lose any prior `[x]` state (PROXY-01..03 + VAL-01..06 stay `[x]`): `grep -c "^- \[x\] \*\*\(PROXY\|VAL\)-" .planning/REQUIREMENTS.md` returns 9.
</verification>

<success_criteria>
This plan satisfies ROADMAP Phase 12 Success Criterion 1 ("`.planning/REQUIREMENTS.md`: 15 stale `[ ]` checkboxes flipped to `[x]` ... TEN-01 spec text amended").
</success_criteria>

<output>
After completion, create `.planning/phases/12-v0-19-close-out-reconciliation/12-01-SUMMARY.md` per `~/.claude/get-shit-done/templates/summary.md`. requirements-completed: [POC-01, POC-02, POC-03, POC-04, CAPCLU-01, CAPCLU-02, CAPCLU-03, CAPCLU-04, EKSDOC-01, CAP-01, CAP-02, CAP-03, TEN-01, TEN-02, TEN-03, TEN-04, TEN-05, TEN-06].
</output>
