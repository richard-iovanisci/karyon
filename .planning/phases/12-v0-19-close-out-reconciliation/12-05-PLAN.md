---
phase: 12
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/todos/pending/2026-04-29-phase-7-hub-flux-observable-reconcile.md
  - .planning/todos/completed/2026-04-29-phase-7-hub-flux-observable-reconcile.md
autonomous: true
requirements: []
tags: [bookkeeping, todos, close-out, v0.19]

must_haves:
  truths:
    - "Stale Phase 7 hub-flux observable-reconcile todo moved from `.planning/todos/pending/` to `.planning/todos/completed/`"
    - "Moved file has a `## 2026-05-11 closure (Phase 12 v0.19 close-out reconciliation)` appendix that cites 11-07-SUMMARY.md + 11-07-G06-DIAGNOSTICS.md as the resolution evidence (Option B per 12-PATTERNS.md §G)"
    - "Pure `git mv` (no body content modification of file content above the appendix) — Option B reuses the pre-existing `2026-04-29 status update` precedent in the file"
  artifacts:
    - path: .planning/todos/completed/2026-04-29-phase-7-hub-flux-observable-reconcile.md
      provides: "Closed Phase 7 carryover todo with auto-close-target-Phase-8 condition met by Plan 11-07 (G-04 + G-06 closure)"
      contains: "## 2026-05-11 closure"
  key_links:
    - from: .planning/todos/completed/2026-04-29-phase-7-hub-flux-observable-reconcile.md
      to: .planning/phases/11-validation-graduation-adr-008/11-07-SUMMARY.md
      via: "Closure appendix cites the canonical post-fix evidence at HEAD 8415ab66"
      pattern: "11-07-SUMMARY.md"
---

<objective>
Move `.planning/todos/pending/2026-04-29-phase-7-hub-flux-observable-reconcile.md`
to `.planning/todos/completed/` and append a `## 2026-05-11 closure` block
citing Plan 11-07 as the auto-close-condition resolver. This is Cluster F of
the Phase 12 close-out reconciliation (RESEARCH.md §"Wave / Parallelism
Opportunity" Wave 1).

Purpose: The todo carries `resolves_phase: 8` and was waiting for Phase 8
verifier to observe outer `poc-capsule` Kustomization Ready=True. Phase 8 did
observe Ready=True momentarily (commit `32e0b2fa`) but masked by empty
inventory (G-04). Plan 11-06 (G-04 closure) + Plan 11-07 (G-06 closure)
lifted that mask at HEAD `8415ab66` — all 4 outer Flux Kustomizations are now
Ready=True with applied manifests. The Phase 7 1b deferred verification is
now observable.

Output: File moved from pending/ → completed/ (preserves original commit
history via `git mv`); appended H2 closure block cites 11-07-SUMMARY.md and
11-07-G06-DIAGNOSTICS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/v0.19-MILESTONE-AUDIT.md
@.planning/todos/pending/2026-04-29-phase-7-hub-flux-observable-reconcile.md
@.planning/phases/11-validation-graduation-adr-008/11-07-SUMMARY.md
@.planning/phases/12-v0-19-close-out-reconciliation/12-RESEARCH.md
@.planning/phases/12-v0-19-close-out-reconciliation/12-PATTERNS.md
</context>

<truths>
- The pending todo already has a `2026-04-29 status update — fragile pass, NOT resolved` section that explicitly stipulates "Todo stays `pending`... It will auto-close only when Phase 8 verifier reports `passed` after G-03 + G-04 gap fixes land." (Audit confirms this; the pending file body has the precedent inline.)
- Plans 11-06 + 11-07 closed G-04 (capsule-system Namespace on hub + PostBuild relocation to HR postRenderers) + G-06 (helm-controller SA + flux-reconciler impersonation correction).
- At HEAD `8415ab66`: poc-capsule, poc-capsule-spoke, poc-capsule-spoke-rbac, poc-capsule-spoke-tenants all Ready=True per 11-07-SUMMARY.md.
- 12-PATTERNS.md §G "Pending todo close" recommends Option B for THIS specific todo (file has inline status-update precedent; closure block records the resolution event).
- Option A precedent `089ccbf` was a pure `git mv` (`similarity index 100%`) for the gitleaks-planning-doc-allowlist todo; that one was a simpler case without an existing inline status-update precedent.
</truths>

<tasks>

<task type="auto">
  <name>Task 1: Move stale Phase 7 hub-flux observable-reconcile todo from pending/ to completed/</name>
  <files>
    .planning/todos/pending/2026-04-29-phase-7-hub-flux-observable-reconcile.md
    .planning/todos/completed/2026-04-29-phase-7-hub-flux-observable-reconcile.md
  </files>
  <read_first>
    - .planning/todos/pending/2026-04-29-phase-7-hub-flux-observable-reconcile.md (full file — current contents, including the pre-existing inline `2026-04-29 status update` section that flags the auto-close condition)
    - .planning/todos/completed/2026-04-29-phase-7-gitleaks-planning-doc-allowlist.md (verify the pattern — precedent file, demonstrates Option A pure-`git mv` shape)
    - .planning/phases/12-v0-19-close-out-reconciliation/12-PATTERNS.md §G "Pending todo close"
  </read_first>
  <action>
    Use the Bash tool to run `git mv` (preserves history; matches `089ccbf` precedent for the move operation itself).

    Command:
    ```
    git mv .planning/todos/pending/2026-04-29-phase-7-hub-flux-observable-reconcile.md \
           .planning/todos/completed/2026-04-29-phase-7-hub-flux-observable-reconcile.md
    ```

    Verify the move succeeded with:
    ```
    test ! -f .planning/todos/pending/2026-04-29-phase-7-hub-flux-observable-reconcile.md && \
    test -f .planning/todos/completed/2026-04-29-phase-7-hub-flux-observable-reconcile.md
    ```

    Do NOT modify file body in this task. Body append happens in Task 2 (separate Edit call for clean diff).
  </action>
  <verify>
    <automated>test ! -f .planning/todos/pending/2026-04-29-phase-7-hub-flux-observable-reconcile.md && test -f .planning/todos/completed/2026-04-29-phase-7-hub-flux-observable-reconcile.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f .planning/todos/pending/2026-04-29-phase-7-hub-flux-observable-reconcile.md` exits 0 (file no longer in pending/)
    - `test -f .planning/todos/completed/2026-04-29-phase-7-hub-flux-observable-reconcile.md` exits 0 (file now in completed/)
    - `git status --porcelain .planning/todos/` shows the rename as a single move (similarity index ≥ 90%) — NOT as a delete+add pair
  </acceptance_criteria>
  <done>
    File physically moved from pending/ to completed/ via `git mv`. Body unchanged at this point.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append closure block to the moved file (Option B per 12-PATTERNS.md §G)</name>
  <files>.planning/todos/completed/2026-04-29-phase-7-hub-flux-observable-reconcile.md</files>
  <read_first>
    - .planning/todos/completed/2026-04-29-phase-7-hub-flux-observable-reconcile.md (the FILE AFTER MOVE — re-read to find tail anchor)
    - .planning/phases/11-validation-graduation-adr-008/11-07-SUMMARY.md (canonical evidence — `## Result` section lists the 4 Kustomizations Ready=True at HEAD 8415ab66; both HelmReleases Ready=True; healthz HTTP 200)
    - .planning/phases/12-v0-19-close-out-reconciliation/12-RESEARCH.md §"Example 5: Pending todo close pattern" (verbatim closure-block text)
  </read_first>
  <action>
    Use the Edit tool to append a closure block at the END of the file.

    The pending file currently ends with a `## Cross-references` H2 section (last 4 lines pre-move):
    ```
    ## Cross-references

    - Phase 7 split rationale: `.planning/phases/07-foundation-poc-seam-spoke-capsule-cluster-eks-capsule-doc/07-HUMAN-UAT.md` §1b
    - P18 script-internal falsifier (resolves 1a): `scripts/register-poc-cluster.sh` `verify_credential_layer()` function
    - Phase 8 plan when written: should include this acceptance criterion in CAP-01 / CAP-02 must_haves
    ```

    Use the unique trailing string "Phase 8 plan when written: should include this acceptance criterion in CAP-01 / CAP-02 must_haves" as the tail anchor.

    `old_string` (last line of file):
    ```
    - Phase 8 plan when written: should include this acceptance criterion in CAP-01 / CAP-02 must_haves
    ```

    `new_string`:
    ```
    - Phase 8 plan when written: should include this acceptance criterion in CAP-01 / CAP-02 must_haves

    ---

    ## 2026-05-11 closure (Phase 12 v0.19 close-out reconciliation)

    **Resolved.** The auto-close target was Phase 8 (run 2026-04-30) which observed `poc-capsule` outer Kustomization Ready=True only with masked empty inventory (see existing "2026-04-29 status update — fragile pass, NOT resolved" section above). The masking was lifted by Plan 11-06 (G-04 capsule-system Namespace on hub + PostBuild patch relocated from Kustomization `spec.patches` to HelmRelease `spec.postRenderers`) and Plan 11-07 (spoke-side `capsule-system` Namespace + `helm-controller` SA + flux-reconciler impersonation correction + new `poc-capsule-spoke-rbac` top-level Flux Kustomization). At HEAD `8415ab66`, per `11-07-SUMMARY.md`:

    - `poc-capsule`, `poc-capsule-spoke`, `poc-capsule-spoke-rbac`, `poc-capsule-spoke-tenants` all Ready=True
    - `capsule` and `capsule-proxy` HelmReleases Ready=True
    - `curl -sk https://127.0.0.1:30443/healthz` returns HTTP 200
    - CI passed for pushed HEAD `8415ab6655faba324ee1809fdb22b08380f42a38`

    This is the **observable post-push hub-Flux reconcile of `poc-capsule`** that Phase 7's 1b deferred verification was waiting for. Todo closed manually as part of Phase 12 close-out reconciliation (auto-close target Phase 8 completed 2026-04-30 with the fragile-pass override that 11-06 + 11-07 subsequently lifted).

    Cross-references:
    - `.planning/phases/11-validation-graduation-adr-008/11-07-SUMMARY.md` — canonical post-fix evidence
    - `.planning/phases/11-validation-graduation-adr-008/11-07-G06-DIAGNOSTICS.md` — empirical pre/post-fix evidence for G-06
    ```

    Note: the move from pending/ to completed/ DOES change the file path; the appendix is added to the file AT ITS NEW PATH (completed/). The Edit tool operates on file path, so use `.planning/todos/completed/2026-04-29-phase-7-hub-flux-observable-reconcile.md` as the target.
  </action>
  <verify>
    <automated>grep -c "^## 2026-05-11 closure (Phase 12 v0.19 close-out reconciliation)$" .planning/todos/completed/2026-04-29-phase-7-hub-flux-observable-reconcile.md && grep -c "11-07-SUMMARY.md" .planning/todos/completed/2026-04-29-phase-7-hub-flux-observable-reconcile.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## 2026-05-11 closure (Phase 12 v0.19 close-out reconciliation)$" .planning/todos/completed/2026-04-29-phase-7-hub-flux-observable-reconcile.md` returns 1
    - `grep -c "8415ab66" .planning/todos/completed/2026-04-29-phase-7-hub-flux-observable-reconcile.md` returns ≥2
    - `grep -c "11-07-SUMMARY.md" .planning/todos/completed/2026-04-29-phase-7-hub-flux-observable-reconcile.md` returns ≥1
    - `grep -c "11-07-G06-DIAGNOSTICS.md" .planning/todos/completed/2026-04-29-phase-7-hub-flux-observable-reconcile.md` returns 1
    - `grep -c "## Problem$" .planning/todos/completed/2026-04-29-phase-7-hub-flux-observable-reconcile.md` returns 1 (original H2 preserved)
    - `grep -c "## Solution$" .planning/todos/completed/2026-04-29-phase-7-hub-flux-observable-reconcile.md` returns 1 (original H2 preserved)
    - `grep -c "## Cross-references$" .planning/todos/completed/2026-04-29-phase-7-hub-flux-observable-reconcile.md` returns 1 (original H2 preserved)
  </acceptance_criteria>
  <done>
    Closure appendix added to completed/ file; original problem/solution/cross-references sections preserved; canonical post-fix evidence (11-07-SUMMARY.md + 11-07-G06-DIAGNOSTICS.md) cited.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | File move + appendix; no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-01 | N/A | todo file move + appendix | accept | No new threats — documentation reconciliation only; existing v0.19 security posture per `11-VERIFICATION.md` §VAL-01 through VAL-05 is unchanged. |
</threat_model>

<verification>
- `test ! -f .planning/todos/pending/2026-04-29-phase-7-hub-flux-observable-reconcile.md` exits 0.
- `test -f .planning/todos/completed/2026-04-29-phase-7-hub-flux-observable-reconcile.md` exits 0.
- Moved file has closure appendix referencing 11-07-SUMMARY.md + 11-07-G06-DIAGNOSTICS.md + HEAD 8415ab66.
- `git log --diff-filter=R --name-only` confirms file rename history preserved.
</verification>

<success_criteria>
This plan satisfies ROADMAP Phase 12 Success Criterion 5 ("`.planning/todos/pending/2026-04-29-phase-7-hub-flux-observable-reconcile.md` resolved ... removed from pending or archived to `.planning/todos/completed/`").
</success_criteria>

<output>
After completion, create `.planning/phases/12-v0-19-close-out-reconciliation/12-05-SUMMARY.md` per `~/.claude/get-shit-done/templates/summary.md`. requirements-completed: [] (no REQ-IDs).
</output>
