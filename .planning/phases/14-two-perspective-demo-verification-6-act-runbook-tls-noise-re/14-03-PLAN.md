---
phase: 14-two-perspective-demo-verification-6-act-runbook-tls-noise-re
plan: 03
type: execute
wave: 3
depends_on:
  - 14-00
  - 14-01
  - 14-02
files_modified:
  - .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VALIDATION.md
autonomous: false
requirements:
  - RUNBOOK-04
requirements_addressed:
  - RUNBOOK-04
tags:
  - capsule
  - demo
  - runbook
  - uat
  - validation

must_haves:
  truths:
    - "Milestone-owner stopwatched cold-read of `docs/poc-capsule-demo.md` against live spoke-capsule completes in ≤ 15 minutes (RUNBOOK-04 UAT contract)"
    - "Pre-demo checklist fully reset: `kubectl delete tenant charlie --ignore-not-found` ran cleanly; both kubeconfigs minted fresh; cluster reachable (D-14-10)"
    - "All 6 acts executed in order; every expected output observed against live cluster matches the runbook's Expected output block (no surprise mismatches)"
    - "UAT duration + rough edges captured in `14-VALIDATION.md` Manual-Only Verifications table (D-14-10)"
    - "`14-VALIDATION.md` frontmatter flipped: `status: draft` → `status: final`; `nyquist_compliant: true`; `wave_0_complete: true`"
    - "If UAT > 15 min on first pass: documented + tightened until ≤ 15 min (D-14-10 pass criterion)"
    - "If verbatim-string drift discovered: noted in carry-forward for Phase 15 retrospective; runbook Expected outputs adjusted if Drift-C-equivalent (minor formatting) or escalated if Drift-A/B (Phase 13 primitive drift)"
  artifacts:
    - path: ".planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VALIDATION.md"
      provides: "Phase 14 validation ledger with UAT result + final frontmatter flip"
      contains: "nyquist_compliant: true"
  key_links:
    - from: ".planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VALIDATION.md"
      to: "docs/poc-capsule-demo.md + bats live evidence logs"
      via: "Manual-Only Verifications table row updated with UAT timing + rough-edge notes"
      pattern: "RUNBOOK-04"
---

<objective>
Plan 14-03 — Milestone-owner UAT cold-read of `docs/poc-capsule-demo.md` against the live `spoke-capsule` cluster + final flip of `14-VALIDATION.md` frontmatter to close Phase 14 against the Nyquist ledger.

Purpose: RUNBOOK-04 is the load-bearing UAT — stopwatched single-pass cold read by a human reading the runbook with no prior context. Pass criterion: duration ≤ 15:00 + zero unexplained failures + all expected outputs match. This is the Phase 14 close gate; Phase 15 (milestone close-out) depends on it.

Output: Updated `14-VALIDATION.md` with `status: final`, `nyquist_compliant: true`, `wave_0_complete: true` + Manual-Only Verifications row populated with stopwatched UAT result + carry-forward notes (if any).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-CONTEXT.md
@.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-RESEARCH.md
@.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VALIDATION.md
@.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-00-SUMMARY.md
@.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-01-SUMMARY.md
@.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-02-SUMMARY.md
@.planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VALIDATION.md

# The UAT target artifact
@docs/poc-capsule-demo.md

<interfaces>
<!-- UAT contract per D-14-10 + RUNBOOK-04 + Phase 13 VALIDATION.md sibling pattern. -->

## UAT pass criteria (D-14-10 + 14-VALIDATION.md Manual-Only Verifications row)
- Duration ≤ 15:00 cold-read (no prior context allowed; runbook is the only input)
- Zero unexplained failures (every act's Expected output observed within reasonable tolerance for transient log timestamps / pod ages / etc.)
- All 6 acts executed in documented order

## Pre-UAT reset (must run before stopwatch starts)
```bash
# Confirm cluster reachable
kubectl --context k3d-spoke-capsule get nodes
# Recover DNS if needed
task fix-dns-poc-capsule 2>/dev/null || true
# Idempotent charlie reset
kubectl --kubeconfig=/tmp/karyon-tenants/platform-owner.kubeconfig delete tenant charlie --ignore-not-found 2>/dev/null || true
# Clean kubeconfig dir (force fresh mint inside the UAT)
rm -rf /tmp/karyon-tenants/{platform-owner,alpha-owner,bravo-owner}.kubeconfig
```

## 14-VALIDATION.md frontmatter flip (target final state)
```yaml
---
phase: 14
slug: two-perspective-demo-verification-6-act-runbook-tls-noise-re
status: final
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-19
finalized: <YYYY-MM-DD>
---
```

## Manual-Only Verifications row update (target shape)
| Behavior | Requirement | Why Manual | Test Instructions | UAT Result |
|---|---|---|---|---|
| Cold-read UAT ≤ 15 min by milestone owner | RUNBOOK-04 | Stopwatch-bounded human read | (preserved verbatim from current 14-VALIDATION.md row 1) | Duration: MM:SS; Rough edges: <list or "none">; First-pass pass/fail; Plan 14-03 commit SHA |

## Per-Task Verification Map update (flip ⬜ → ✅ for row 14-04-*)
Row in current `14-VALIDATION.md`:
```
| 14-04-* | 04 | 3 | RUNBOOK-04 | — | Milestone-owner cold-read ≤ 15 min ... | manual UAT | stopwatched cold-read; results recorded | ❌ W0 | ⬜ pending |
```
Becomes:
```
| 14-03-* | 03 | 3 | RUNBOOK-04 | — | Milestone-owner cold-read ≤ 15 min ... | manual UAT | stopwatched cold-read; results recorded | ✅ | ✅ green |
```
(Plan numbering: VALIDATION.md was drafted assuming Plan 04 final; final plan count is 14-00..14-03 = 4 plans. Update the row to match.)

## Validation Sign-Off section (target — all boxes checked)
- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (11 anticipated bats files landed in Plan 14-00)
- [x] No watch-mode flags (bats has no watch mode)
- [x] Feedback latency < 120s
- [x] `nyquist_compliant: true` set after Wave 0 GREEN
- [x] `wave_0_complete: true` set after Wave 0 plan commits
- [x] RUNBOOK-04 UAT result recorded in Manual-Only Verifications table after cold-read pass

**Approval:** approved (planner authored / executor flipped status / verifier signed off)
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT operator → cluster | Stopwatched cold-read mutates cluster state (provisions + deletes charlie); demo-only blast radius |
| 14-VALIDATION.md → milestone audit | Final-state ledger feeds Phase 15 milestone close + `/gsd-audit-milestone v0.20` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Tampering | UAT delete-tenant-charlie reset may run against a busy cluster | accept | Idempotent `--ignore-not-found`; cluster is k3d POC (no production blast radius); alpha + bravo Tenant CRs untouched by Phase 14 by construction |
| T-14-03-02 | Repudiation | UAT result is human-attested; without timing + rough-edge capture, no audit trail | mitigate | Manual-Only Verifications row records duration + rough edges + commit SHA; `14-VALIDATION.md` git history is the audit artifact |
| T-14-03-03 | Information disclosure | UAT operator may paste live kubeconfig content into the SUMMARY by mistake | mitigate | SUMMARY template instructs operator to record only the placeholder env-var names + write paths, NOT contents; P29 gitleaks rule catches accidental paste |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Stopwatched cold-read UAT of docs/poc-capsule-demo.md against live spoke-capsule (RUNBOOK-04 / D-14-10)</name>
  <what-built>
    Plans 14-00 / 14-01 / 14-02 landed all the Phase 14 artifacts: 11 bats files (all GREEN), `docs/poc-capsule-demo.md` (250-400 lines, 6 acts), `docs/capsule-on-eks.md` three-tier append. RUNBOOK-04 — the ≤ 15 minute cold-read UAT — is the load-bearing close gate. Without it Phase 14 cannot mark `status: final`.
  </what-built>
  <how-to-verify>
    **Setup (NOT counted toward the 15-min stopwatch):**

    1. Open a fresh terminal. Ensure no prior context loaded (open a new pane / tab).
    2. Confirm cluster reachable + reset idempotently:
       ```bash
       kubectl --context k3d-spoke-capsule get nodes
       # If NotReady: task fix-dns-poc-capsule
       # Clean any stale charlie from prior runs:
       bash scripts/poc/capsule/issue-platform-owner-kubeconfig.sh \
         --duration 30m --write-to /tmp/karyon-tenants/uat-reset.kubeconfig 2>/dev/null
       kubectl --kubeconfig=/tmp/karyon-tenants/uat-reset.kubeconfig delete tenant charlie --ignore-not-found 2>/dev/null
       rm -f /tmp/karyon-tenants/uat-reset.kubeconfig /tmp/karyon-tenants/{platform-owner,alpha-owner,bravo-owner}.kubeconfig
       ```
    3. Open `docs/poc-capsule-demo.md` in a markdown viewer / editor. DO NOT skim it — the reader executes it cold.

    **Stopwatch start:**

    4. Note the start time (wall clock or `date +%T`).
    5. Execute the runbook EXACTLY in order:
       - Pre-demo checklist (Steps 1-5 — fence-block commands)
       - Act 1 (architecture talk-through — no commands; visual / verbal)
       - Act 2 (Tenant LIST + CRB Forbidden)
       - Act 3 (`new-tenant.sh charlie` provision)
       - Act 4 (SEED-02 ≤ 60s wait)
       - Act 5 (5a..5e — tenant-owner narrow + platform-owner co-owner)
       - Act 6 (cleanup `delete tenant charlie`)
    6. As you execute each act, compare the observed output against the runbook's `### Expected output` block. Note any drift (verbatim mismatches, render glitches, tolerance excursions like pod-age timestamps).

    **Stopwatch stop:**

    7. Note the stop time the moment Act 6's `tenant.capsule.clastix.io "charlie" deleted` confirmation appears.
    8. Compute duration (stop - start).

    **Result recording:**

    9. Pass criteria:
       - Duration ≤ 15:00 (D-14-10)
       - All 6 acts executed cleanly
       - Expected output blocks matched (modulo tolerable noise: timestamps, pod ages, transient resource IDs)
    10. Decision tree:
        - **PASS first-try:** Type "approved" + report duration + report rough edges (or "none").
        - **FAIL first-try (duration > 15:00 or unexpected failures):**
          - If duration > 15:00 but acts succeeded: identify which sections bloated. Options:
            - (a) Collapse Act 1 narrative (verbal-only act; can be tightened in Plan 14-03 Task 2 below as a Drift-C-equivalent edit before final UAT pass).
            - (b) Pre-mint kubeconfigs as a one-liner Taskfile entry (rejected — D-14 Discretion prohibits adding `task demo-capsule` per P31).
            - (c) Add pre-demo cluster-warmup snippet (image pull pre-load) to checklist.
          - If specific act failed (e.g., Forbidden string drift): triage per Plan 14-02 Drift policy (A/B = escalate; C = minor runbook edit).
          - Document the failure in the SUMMARY + decide go/no-go for re-run.
        - **PASS after revision:** Re-run the cold UAT with a different reader if possible (the original reader is no longer cold). Otherwise document "second-pass after revision" in the result.
  </how-to-verify>
  <resume-signal>Type "approved" + report stopwatched duration (MM:SS) + rough-edges list (or "none"), OR describe failures and request revision-pass plan.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flip 14-VALIDATION.md to final — record UAT result + flip frontmatter + check Validation Sign-Off boxes</name>
  <files>.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VALIDATION.md</files>
  <behavior>
    - Frontmatter: `status: draft` → `status: final`; `nyquist_compliant: false` → `nyquist_compliant: true`; `wave_0_complete: false` → `wave_0_complete: true`; add `finalized: <YYYY-MM-DD>` field
    - Per-Task Verification Map: every row's Status column flips from `⬜ pending` to `✅ green` (or `⚠️` with explanation for the carry-forward Phase 8 G-04 inheritance if it surfaces in Phase 14 sampling)
    - Manual-Only Verifications table row 1 (Cold-read UAT) gains a `UAT Result` column populated with: `Duration: MM:SS; Rough edges: <list>; First-pass: PASS|FAIL; Re-pass: <if applicable>; Plan 14-03 commit SHA: <sha>`
    - Validation Sign-Off section: all 8 checklist items flip from `[ ]` to `[x]`
    - Approval line: `**Approval:** pending` → `**Approval:** approved (planner authored / executor flipped status / verifier signed off — Plan 14-03 UAT pass <date>)`
    - Plan-number reference in Per-Task Verification Map: row `14-04-*` is RENAMED to `14-03-*` to match the final plan count (14-00..14-03 = 4 plans, NOT 5)
  </behavior>
  <read_first>
    - .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VALIDATION.md (current state — draft frontmatter + ⬜ pending Status column + unchecked Sign-Off boxes)
    - .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VALIDATION.md (final-state sibling pattern — verbatim model for the frontmatter flip + Sign-Off rendering)
    - Task 1 (above) — UAT result captured at the resume-signal
  </read_first>
  <action>
    Edit `.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VALIDATION.md` to flip it to final state per `<interfaces>` "14-VALIDATION.md frontmatter flip" + "Validation Sign-Off section (target)" + "Manual-Only Verifications row update".

    Specific edits required (use Edit tool to preserve byte-level fidelity of unchanged content):

    1. **Frontmatter block** — replace verbatim:
       FROM (current state, lines 1-8):
       ```yaml
       ---
       phase: 14
       slug: two-perspective-demo-verification-6-act-runbook-tls-noise-re
       status: draft
       nyquist_compliant: false
       wave_0_complete: false
       created: 2026-05-19
       ---
       ```
       TO:
       ```yaml
       ---
       phase: 14
       slug: two-perspective-demo-verification-6-act-runbook-tls-noise-re
       status: final
       nyquist_compliant: true
       wave_0_complete: true
       created: 2026-05-19
       finalized: <YYYY-MM-DD>
       ---
       ```
       Substitute `<YYYY-MM-DD>` with `$(date +%Y-%m-%d)` at edit time.

    2. **Per-Task Verification Map table** — for each existing row (`14-00-*`, `14-01-*`, `14-02-*`, `14-03-*`, `14-04-*`):
       - Update Status column from `⬜ pending` to `✅ green` (or `⚠️ flaky` with explanation if Phase 8 G-04 inheritance surfaced in sampling — refer to Plan 14-02 full-suite evidence log).
       - RENAME `14-04-*` row to `14-03-*` and change its Plan column from `04` to `03` (final plan count is 14-00..14-03 = 4 plans, NOT 5 — Wave 2 plans 14-02 and Wave 3 plan 14-03; original VALIDATION.md anticipated 5 but planner consolidated to 4).
       - File Exists column from `❌ W0` to `✅` for all rows.

    3. **Manual-Only Verifications table** — extend row 1 with the UAT Result column populated from Task 1 resume-signal output. Format:
       ```
       | Cold-read UAT ≤ 15 min by milestone owner | RUNBOOK-04 | Stopwatch-bounded human read | (preserved verbatim instructions) | Duration: MM:SS; Rough edges: <list or "none">; First-pass: PASS\|FAIL; Re-pass: <if applicable, else "n/a">; Plan 14-03 commit SHA: <sha> |
       ```
       Substitute UAT data from Task 1.

    4. **Validation Sign-Off section** — flip all 8 `- [ ]` boxes to `- [x]`:
       - All tasks have automated verify or Wave 0 dependencies
       - Sampling continuity: no 3 consecutive tasks without automated verify
       - Wave 0 covers all MISSING references
       - No watch-mode flags
       - Feedback latency < 120s
       - `nyquist_compliant: true` set
       - `wave_0_complete: true` set
       - RUNBOOK-04 UAT result recorded

    5. **Approval line** at the bottom:
       FROM: `**Approval:** pending (planner fills the Per-Task Verification Map; executor flips status; verifier sets `status: final`)`
       TO: `**Approval:** approved (planner authored Plans 14-00..14-03 / executor flipped 11 bats GREEN + docs landed / verifier signed off — Plan 14-03 UAT pass <YYYY-MM-DD>)`

    Verification spot-checks after edit:
    ```bash
    grep -E "^status:" .planning/phases/14-*/14-VALIDATION.md       # expect: status: final
    grep -E "^nyquist_compliant:" .planning/phases/14-*/14-VALIDATION.md  # expect: nyquist_compliant: true
    grep -E "^wave_0_complete:" .planning/phases/14-*/14-VALIDATION.md    # expect: wave_0_complete: true
    grep -c '⬜ pending' .planning/phases/14-*/14-VALIDATION.md          # expect: 0
    grep -c '✅ green' .planning/phases/14-*/14-VALIDATION.md            # expect: ≥ 4 (one per plan row)
    grep -c '- \[x\]' .planning/phases/14-*/14-VALIDATION.md             # expect: ≥ 8 (Sign-Off boxes)
    grep -c '- \[ \]' .planning/phases/14-*/14-VALIDATION.md             # expect: 0 in Sign-Off section
    ```

    Commit: `docs(14-03): finalize 14-VALIDATION.md — UAT passed (MM:SS); status: final; nyquist_compliant: true; wave_0_complete: true`.
  </action>
  <verify>
    <automated>test -f .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VALIDATION.md && grep -qE "^status:[[:space:]]*final" .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VALIDATION.md && grep -qE "^nyquist_compliant:[[:space:]]*true" .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VALIDATION.md && grep -qE "^wave_0_complete:[[:space:]]*true" .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VALIDATION.md && grep -qF "Approval:** approved" .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VALIDATION.md && PENDING=$(grep -c '⬜ pending' .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VALIDATION.md || echo 0); [ "$PENDING" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter has `status: final` + `nyquist_compliant: true` + `wave_0_complete: true` + `finalized: <date>`
    - Per-Task Verification Map: zero `⬜ pending` rows; all rows show `✅ green` (or `⚠️` with rationale for documented carry-forward only)
    - Plan-numbering reconciled: `14-04-*` row RENAMED to `14-03-*` (final plan count = 4)
    - Manual-Only Verifications row 1 has UAT Result column populated with Duration + Rough edges + First-pass result + commit SHA
    - All 8 Validation Sign-Off checkboxes flipped to `[x]`
    - Approval line shows `approved (planner ... / executor ... / verifier ... — Plan 14-03 UAT pass <date>)`
    - Single commit with subject `docs(14-03): finalize 14-VALIDATION.md`
  </acceptance_criteria>
  <done>Phase 14 validation ledger closed. `14-VALIDATION.md` is the audit artifact for `/gsd-audit-milestone v0.20` in Phase 15.</done>
</task>

</tasks>

<verification>
- Task 1 UAT checkpoint approved by milestone-owner with duration ≤ 15:00 (or documented PASS-after-revision)
- Task 2 14-VALIDATION.md flipped to `status: final` + nyquist_compliant + wave_0_complete = true
- All 8 Sign-Off boxes checked
- Plan numbering reconciled (4 plans, not 5)
- Carry-forward notes (if any) captured in Manual-Only Verifications row for Phase 15 retrospective
</verification>

<success_criteria>
- RUNBOOK-04 contract verified: cold-read ≤ 15 min by milestone-owner against live spoke-capsule
- 14-VALIDATION.md in final state — ready for `/gsd-audit-milestone v0.20` consumption
- Phase 14 close gate held: 11 REQs (DEMO-01..06 + RUNBOOK-01..05) all verified live + documented
- Ready for Phase 15 v0.20 close-out + retrospective
</success_criteria>

<output>
After completion, create `.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-03-SUMMARY.md` capturing:
- UAT duration (MM:SS) + first-pass-or-second outcome
- Rough edges list (input for Phase 15 retrospective)
- Carry-forward items (if any drift surfaced)
- 14-VALIDATION.md git diff stat (frontmatter + status column + sign-off + approval changes)
- Phase 14 close declaration: 11/11 REQs verified live; ready for milestone close-out
</output>
