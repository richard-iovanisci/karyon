---
phase: 14-two-perspective-demo-verification-6-act-runbook-tls-noise-re
plan: 01
type: execute
wave: 1
depends_on:
  - 14-00
files_modified:
  - docs/poc-capsule-demo.md
  - docs/capsule-on-eks.md
autonomous: true
requirements:
  - RUNBOOK-01
  - RUNBOOK-02
  - RUNBOOK-03
  - RUNBOOK-05
requirements_addressed:
  - RUNBOOK-01
  - RUNBOOK-02
  - RUNBOOK-03
  - RUNBOOK-05
tags:
  - capsule
  - demo
  - runbook
  - docs

must_haves:
  truths:
    - "`docs/poc-capsule-demo.md` exists with frontmatter (status: Active) + h1 + status blockquote + companion-docs cross-links (D-14-04)"
    - "6 ordered `## Act N — ` sections (Act 1 architecture → Act 2 platform-owner ceiling → Act 3 provision charlie live → Act 4 SEED auto-propagation → Act 5 two perspectives → Act 6 cleanup) per RUNBOOK-01"
    - "Every act ends with `### Expected output` fenced ```text``` block containing verbatim canonical strings from RESEARCH Q2/Q3/Q4/Q5/Q6 — NEVER invented (D-14-06)"
    - "Pre-demo checklist section with executable shell snippets (kubectl/bash/task) — NOT narrative bullets — covering cluster reachable, fix-dns, kubeconfig minting, charlie reset, task sanity (D-14-05 / RUNBOOK-02)"
    - "Known noise appendix after Act 6 — 2-paragraph TLS chatter explanation containing verbatim `tls: bad certificate` literal + ADR-008 cross-link + `capsule-controller-manager` literal (D-14-01 / RUNBOOK-03)"
    - "Forward pointers section cross-linking docs/poc-capsule.md, docs/capsule-on-eks.md (with §\"Three-tier permission model on EKS\" anchor), docs/adr/0008-capsule-multi-tenancy-graduation.md"
    - "Marquee tenant name `charlie` verbatim in Act 3 (`new-tenant.sh charlie`) + Act 4 (tenant-charlie SEED observation) + Act 6 (cleanup `delete tenant charlie`) — single canonical name (D-14-02 / D-14-03 / RUNBOOK-05)"
    - "DEMO-06 headline Forbidden action surfaced in Act 2: `kubectl create clusterrolebinding po-cluster-admin-attempt --clusterrole=cluster-admin --user=platform-owner` (D-14-07)"
    - "`docs/capsule-on-eks.md` gains `## Three-tier permission model on EKS` section (~15 lines) with explicit Tier 1 / Tier 2 / Tier 3 labels mapped to AWS IAM admin / EKS IRSA + capsule-platform-owner / IRSA + tenant-workload-editor (RUNBOOK-03 cross-link target; RESEARCH Q1)"
    - "Total runbook line count 250–350 (within 15-min cold-read budget per RESEARCH Q9; RUNBOOK-04 enabling constraint)"
    - "Zero new `spoke-capsule` mentions in default-path scripts (P31 preserved — Phase 14 is docs-only)"
    - "Zero bearer tokens / kubeconfig contents embedded in runbook — only placeholder env vars `$PO_KC`, `$TO_ALPHA_KC`, `$TO_BRAVO_KC` (P29 defense)"
  artifacts:
    - path: "docs/poc-capsule-demo.md"
      provides: "6-act demo runbook + pre-demo checklist + Known noise appendix + Forward pointers"
      min_lines: 250
      contains: "## Act 1"
    - path: "docs/capsule-on-eks.md"
      provides: "Existing EKS rough-cut EXTENDED with explicit three-tier mapping section (RUNBOOK-03 cross-link target)"
      contains: "## Three-tier permission model on EKS"
  key_links:
    - from: "docs/poc-capsule-demo.md"
      to: "scripts/poc/capsule/{issue-platform-owner-kubeconfig.sh, issue-tenant-kubeconfig.sh, new-tenant.sh}"
      via: "executable shell snippets in Pre-demo checklist + Act 3"
      pattern: "scripts/poc/capsule/(issue-platform-owner-kubeconfig|issue-tenant-kubeconfig|new-tenant)\\.sh"
    - from: "docs/poc-capsule-demo.md"
      to: "docs/adr/0008-capsule-multi-tenancy-graduation.md"
      via: "markdown link in Known noise appendix"
      pattern: "adr/0008"
    - from: "docs/poc-capsule-demo.md"
      to: "docs/capsule-on-eks.md (anchor: Three-tier permission model on EKS)"
      via: "markdown link in Forward pointers section"
      pattern: "capsule-on-eks\\.md"
    - from: "docs/poc-capsule-demo.md"
      to: "docs/poc-capsule.md (operator runbook for fix-dns / recovery)"
      via: "markdown link in companion-docs blockquote + Forward pointers"
      pattern: "poc-capsule\\.md"
---

<objective>
Plan 14-01 — Lands `docs/poc-capsule-demo.md` as the load-bearing 6-act demo runbook + appends `## Three-tier permission model on EKS` section to `docs/capsule-on-eks.md` as the RUNBOOK-03 production-translation cross-link target.

Purpose: Deliver the single canonical demo artifact a teammate can execute cold in ≤ 15 minutes (RUNBOOK-04 UAT in Plan 14-03). Turn 5 of the Wave-0 RED `runbook-*-static-*.bats` files GREEN. RUNBOOK-04 (≤ 15-min UAT) stays open for Plan 14-03 since it requires human cold-read.

Output: 1 NEW file (`docs/poc-capsule-demo.md`, 250–400 lines) + 1 MODIFIED file (`docs/capsule-on-eks.md` gains ~15-line three-tier section). All 5 RUNBOOK-* static bats turn GREEN in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-CONTEXT.md
@.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-RESEARCH.md
@.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-PATTERNS.md
@.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VALIDATION.md

# Analog docs
@docs/poc-capsule.md
@docs/rebuild-runbook.md
@docs/capsule-on-eks.md

# Wave 0 falsifier bats — these tests define GREEN
@tests/bats/runbook-01-static-acts.bats
@tests/bats/runbook-02-static-checklist.bats
@tests/bats/runbook-03-static-tls-noise.bats
@tests/bats/runbook-03-static-eks-three-tier.bats
@tests/bats/runbook-05-static-charlie.bats

# Scripts the runbook invokes
@scripts/poc/capsule/issue-platform-owner-kubeconfig.sh
@scripts/poc/capsule/issue-tenant-kubeconfig.sh
@scripts/poc/capsule/new-tenant.sh

<interfaces>
<!-- Verbatim strings + analog snippets the executor MUST copy literally. No invention. -->

## docs/poc-capsule-demo.md frontmatter (verbatim PATTERNS lines 467-474)

```yaml
---
status: Active
audience: demo presenter + audience
purpose: 6-act Capsule POC demo walkthrough — three-tier permission model end-to-end.
scope: Phase 14 verification + demo runbook. Single canonical document; companion to docs/poc-capsule.md (operator runbook) and docs/capsule-on-eks.md (EKS translation).
---
```

## H1 + status blockquote + companion-docs (verbatim PATTERNS lines 478-482)

```markdown
# spoke-capsule POC Demo Walkthrough

> **Status:** Active (Phase 14 — Two-Perspective Demo Verification).
> **Companion docs:** [`docs/poc-capsule.md`](poc-capsule.md) (operator runbook — host-restart recovery + kubeconfig delivery contracts), [`docs/capsule-on-eks.md`](capsule-on-eks.md) (EKS translation rough-cut), [`docs/adr/0008-capsule-multi-tenancy-graduation.md`](adr/0008-capsule-multi-tenancy-graduation.md) (POC graduation = DEFER framing).
```

## RESEARCH Q6 verbatim Forbidden strings — USE LITERALLY in Expected output blocks

### Act 2 CRB Forbidden (D-14-07 headline):
```
error: failed to create clusterrolebinding: clusterrolebindings.rbac.authorization.k8s.io is forbidden: User "system:serviceaccount:capsule-system:platform-owner" cannot create resource "clusterrolebindings" in API group "rbac.authorization.k8s.io" at the cluster scope
```

### Act 5 Secret Forbidden:
```
error: failed to create secret secrets is forbidden: User "system:serviceaccount:tenant-alpha:human-tenant-owner" cannot create resource "secrets" in API group "" in the namespace "tenant-alpha"
```

## RESEARCH Q3 verbatim exec probe (Act 5 positive control):
- Command: `kubectl --kubeconfig=$TO_ALPHA_KC -n tenant-alpha exec hello-world -- sh -c "echo ok-from-shell"`
- Output: `ok-from-shell`

## RESEARCH Q4 verbatim logs probe (Act 5):
- Command: `kubectl --kubeconfig=$TO_ALPHA_KC -n tenant-alpha logs hello-world --tail=3`
- Output (verbatim live 2026-05-19):
  ```
  2026/05/19 22:41:37 [notice] 1#1: start worker process 47
  2026/05/19 22:41:37 [notice] 1#1: start worker process 48
  2026/05/19 22:41:37 [notice] 1#1: start worker process 49
  ```

## RESEARCH Q5 verbatim namespace + SEED-02 probe (Act 4):
- Probe: `kubectl --context k3d-spoke-capsule -n tenant-charlie wait pod/hello-world --for=condition=Ready --timeout=60s`
- Namespace name: `tenant-charlie` (ONLY ns — NOT `charlie-app1`)

## RESEARCH Q2 verbatim TLS noise log line (Known noise appendix):
```
2026/05/19 23:50:49 http: TLS handshake error from 10.42.0.1:39986: remote error: tls: bad certificate
```

## Pre-demo checklist executable snippet block (RESEARCH Q7 + Q8 + D-14-05) — embed verbatim in `## Pre-demo checklist` section:

```
# Step 1: cluster reachable?
kubectl --context k3d-spoke-capsule get nodes
# If not Ready, recover DNS:
task fix-dns-poc-capsule

# Step 2: mint platform-owner kubeconfig (Tier 2)
bash scripts/poc/capsule/issue-platform-owner-kubeconfig.sh \
  --duration 2h --write-to /tmp/karyon-tenants/platform-owner.kubeconfig
export PO_KC=/tmp/karyon-tenants/platform-owner.kubeconfig

# Step 3: mint tenant-owner kubeconfigs (Tier 3)
bash scripts/poc/capsule/issue-tenant-kubeconfig.sh alpha human-tenant-owner \
  --duration 2h --write-to /tmp/karyon-tenants/alpha-owner.kubeconfig
export TO_ALPHA_KC=/tmp/karyon-tenants/alpha-owner.kubeconfig

bash scripts/poc/capsule/issue-tenant-kubeconfig.sh bravo human-tenant-owner \
  --duration 2h --write-to /tmp/karyon-tenants/bravo-owner.kubeconfig
export TO_BRAVO_KC=/tmp/karyon-tenants/bravo-owner.kubeconfig

# Step 4: idempotent reset (charlie may exist from previous demo)
kubectl --kubeconfig=$PO_KC delete tenant charlie --ignore-not-found

# Step 5: task sanity
task -l | grep poc
```

## docs/capsule-on-eks.md append — verbatim section (Q1 RESEARCH + PATTERNS lines 553-563)

Append AFTER existing `## Required permissions / RBAC` table (lines 35-43), BEFORE next `## ` heading. The verbatim section text:

```
## Three-tier permission model on EKS

The Phase 13 three-tier model translates to EKS as follows. Each tier preserves the same RBAC shape as the k3d POC; the platform-specific carriers change.

| Tier | k3d POC carrier | EKS carrier | Permission ceiling |
|---|---|---|---|
| **Tier 1 — Cloud Ops** | k3d host operator (full cluster control) | AWS account / IAM admin holding `cluster-admin` on the EKS cluster | `cluster-admin` (nodes, csr, clusterrolebindings) |
| **Tier 2 — Karyon Platform Team** | `capsule-platform-owners` group + `platform-owner@capsule-system` SA | EKS IRSA-bound IAM role + cluster-scoped `capsule-platform-owner` ClusterRole | Tenant CR CRUD + namespace LIST; NO cluster-admin, NO nodes, NO csr, NO clusterrolebinding writes |
| **Tier 3 — Tenant Developers** | `human-tenant-owner@tenant-<name>` SA + Tenant `spec.owners[]` co-ownership | IRSA-bound IAM role per tenant SA (existing pattern above) | `tenant-workload-editor` ClusterRole scoped to own tenant namespaces |

The IRSA TrustPolicy YAML (Tier 3 carrier) appears verbatim below in §"Verbatim YAML #1". Tier 2's IRSA shape is identical except the role's namespace-scope is cluster-wide and the bound ClusterRole is `capsule-platform-owner` (NOT `tenant-workload-editor`).
```

## Known noise appendix verbatim text (D-14-01 / PATTERNS lines 515-531):

```
## Known noise — `tls: bad certificate` chatter

**What it is.** The capsule-controller-manager pod emits a `tls: bad certificate` log line ~1Hz throughout normal operation, visible via:

(fenced bash block)
kubectl --context k3d-spoke-capsule -n capsule-system logs deploy/capsule-controller-manager --tail=30 | grep tls

Example:

(fenced text block)
2026/05/19 23:50:49 http: TLS handshake error from 10.42.0.1:39986: remote error: tls: bad certificate

This is the apiserver's webhook-handshake retry loop against capsule-controller-manager using a self-signed cert it doesn't trust. It does NOT affect tenant boundary enforcement or `GlobalTenantResource` propagation (verified empirically via Phase 13 + this phase's DEMO-01..06 bats).

**Why document-only.** Per [ADR-008](adr/0008-capsule-multi-tenancy-graduation.md) the Capsule POC is `DEFER` for production graduation; the chatter is cosmetic, not functional. Fixing it pulls in cert-manager / CA-distribution work that the lean v0.20 milestone is explicitly scope-guarded against. A production EKS cut would wire AWS Certificate Manager or cert-manager controller-managed certs cleanly; see [`docs/capsule-on-eks.md`](capsule-on-eks.md) §"Phase 11 Evidence — Cert source".
```

## Forward pointers verbatim text (PATTERNS lines 687-691):

```
## Forward pointers

- [`docs/poc-capsule.md`](poc-capsule.md) §"Host restart recovery" — operator-side fix-dns + cluster-stop/start recovery procedures.
- [`docs/capsule-on-eks.md`](capsule-on-eks.md) §"Three-tier permission model on EKS" — production translation of the three-tier carriers (Cloud Ops / Karyon Platform Team / Tenant Developers).
- [`docs/adr/0008-capsule-multi-tenancy-graduation.md`](adr/0008-capsule-multi-tenancy-graduation.md) — POC graduation = DEFER framing (cited in "Known noise" appendix).
```

## Line budget per section (RESEARCH Q9 — DO NOT exceed ~400 total)

| Section | Target |
|---|---|
| Frontmatter | 6-8 |
| H1 + status blockquote | 6-8 |
| Pre-demo checklist | 30-40 |
| Act 1 (architecture + three-tier) | 30-40 |
| Act 2 (platform-owner + CRB Forbidden) | 25-35 |
| Act 3 (provision charlie live) | 25-35 |
| Act 4 (SEED ≤ 60s) | 20-30 |
| Act 5 (two perspectives — densest) | 40-60 |
| Act 6 (cleanup) | 15-25 |
| Known noise | 15-25 |
| Forward pointers | 10-15 |
| **Total** | **~216-313** |
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runbook reader → kubeconfig files at `/tmp/karyon-tenants/*` | Runbook directs minting to `/tmp`; never commits payload contents |
| Runbook publication → public-repo gitleaks | `docs/` is git-tracked + publicly visible; must not embed secrets |
| Demo audience → kubectl exec shell payload | `sh -c "echo ok-from-shell"` only — no destructive commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Information disclosure | Runbook embeds command examples with kubeconfig paths | mitigate | Runbook uses placeholder env vars (`$PO_KC`, `$TO_ALPHA_KC`, `$TO_BRAVO_KC`) — NEVER inline kubeconfig contents or bearer tokens. P29 gitleaks rule fires on accidental paste; verify via `gitleaks detect --no-banner --redact` post-write |
| T-14-01-02 | Tampering | Runbook directs operator to `bash scripts/poc/capsule/*.sh` | accept | Scripts are repo-tracked + reviewed; runbook references them by canonical repo-relative path; no curl-pipe-bash |
| T-14-01-03 | Elevation of privilege | Runbook Act 3 invokes `new-tenant.sh charlie` cold (D-14-03 live provisioning) | accept | `new-tenant.sh` validates name regex + uses platform-owner kubeconfig (NOT cluster-admin) per Phase 13 RBAC-05; Act 6 cleanup unwinds via `delete tenant charlie` |
| T-14-01-04 | Denial of service | Runbook Act 6 `kubectl delete tenant charlie` removes a tenant during the demo | accept | `charlie` is demo-only; pre-demo checklist resets idempotently; alpha + bravo untouched |
| T-14-01-05 | Spoofing | Runbook quotes verbatim SA identity strings in Expected output | accept | Strings are the apiserver's own User-identity output (RESEARCH Q6); embedding identifiers is the docs-standard pattern (poc-capsule.md does similar). No tokens, no signatures |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write docs/poc-capsule-demo.md — frontmatter + h1 + Pre-demo checklist + 6 acts + Known noise + Forward pointers (250-400 lines)</name>
  <files>docs/poc-capsule-demo.md</files>
  <behavior>
    - Frontmatter has `status: Active`
    - H1 in first 20 lines
    - 6 ordered `## Act N — ` sections (1 → 6 monotonically increasing line numbers)
    - Each act has `### Expected output` fenced block (≥ 6 total)
    - `## Pre-demo checklist` section contains `task fix-dns-poc-capsule`, `issue-platform-owner-kubeconfig.sh`, `issue-tenant-kubeconfig.sh alpha human-tenant-owner`, `issue-tenant-kubeconfig.sh bravo human-tenant-owner`, `delete tenant charlie --ignore-not-found`
    - `## Known noise` section appears AFTER `## Act 6 —`; contains verbatim `tls: bad certificate`, `capsule-controller-manager`, `adr/0008` or `ADR-008` cross-link
    - `charlie` literal between Act 3 and Act 4 line numbers
    - `charlie` literal after Act 6 line number
    - `new-tenant.sh charlie` + `tenant-charlie` literals present
    - Act 2 contains verbatim `kubectl create clusterrolebinding po-cluster-admin-attempt --clusterrole=cluster-admin --user=platform-owner` (D-14-07) AND verbatim `clusterrolebindings.rbac.authorization.k8s.io is forbidden` (Q6)
    - Act 5 Expected output contains verbatim `human-tenant-owner` AND `ok-from-shell`
    - 200 ≤ line count ≤ 450 (15-min cold-read budget)
    - Zero bearer tokens / kubeconfig contents (P29 — only `$PO_KC`, `$TO_ALPHA_KC`, `$TO_BRAVO_KC` placeholders)
  </behavior>
  <read_first>
    - .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-PATTERNS.md (`docs/poc-capsule-demo.md` composite analog lines 461-533; Shared patterns lines 654-691)
    - .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-RESEARCH.md Q2 (TLS verbatim), Q3 (exec probe), Q4 (logs probe), Q5 (charlie namespace), Q6 (Forbidden verbatim), Q7 (task fix-dns), Q8 (kubeconfig minting), Q9 (line budget)
    - docs/poc-capsule.md (analog frontmatter lines 1-6, h1 + status blockquote lines 8-11, Quickstart cadence lines 29-67, Cross-references lines 184-189)
    - docs/rebuild-runbook.md (analog `## Step N — Title (~Nsec)` cadence lines 45-88)
    - tests/bats/runbook-01-static-acts.bats (Wave-0 falsifier — defines RUNBOOK-01 GREEN)
    - tests/bats/runbook-02-static-checklist.bats (Wave-0 falsifier — defines required checklist literals)
    - tests/bats/runbook-03-static-tls-noise.bats (Wave-0 falsifier — defines TLS appendix shape)
    - tests/bats/runbook-05-static-charlie.bats (Wave-0 falsifier — defines `charlie` placement)
    - scripts/poc/capsule/issue-platform-owner-kubeconfig.sh (head + Usage line)
    - scripts/poc/capsule/issue-tenant-kubeconfig.sh (head + Usage line)
    - scripts/poc/capsule/new-tenant.sh (head + Usage line)
  </read_first>
  <action>
    Write `docs/poc-capsule-demo.md` per the structure below. Every fenced code block + every Expected output block uses verbatim strings from `<interfaces>` above — NEVER invent output strings.

    Section order (load-bearing):

    1. **Frontmatter** — verbatim from `<interfaces>` "docs/poc-capsule-demo.md frontmatter".

    2. **H1 + status blockquote + companion-docs blockquote** — verbatim from `<interfaces>` "H1 + status blockquote + companion-docs".

    3. **`## Pre-demo checklist`** (D-14-05 RUNBOOK-02) — single fenced ```bash``` block containing all 5 steps from `<interfaces>` "Pre-demo checklist executable snippet block" verbatim. After the fenced block, add a 1-paragraph note: "All kubeconfig writes are constrained to `${TMPDIR:-/tmp}/karyon-tenants/` per Phase 10 BL-01/BL-02 hardening; tokens never touch git (P29 + `.gitleaks.toml` kubeconfig-bearer-token rule)."

    4. **`## Act 1 — Architecture + three-tier permission model`** (RUNBOOK-01 act 1):
       - Paragraph 1: "This demo walks the spoke-capsule POC three-tier permission model end-to-end against a live k3d cluster. The model exists to validate the production constraint that the Karyon Platform Team does NOT hold cloud-account cluster-admin."
       - Paragraph 2: Articulate Tier 1 / Tier 2 / Tier 3 explicitly. Tier 1 = Cloud Ops, out of scope here, AWS IAM admin / EKS cluster-admin holder. Tier 2 = Karyon Platform Team, `platform-owner@capsule-system` SA + `capsule-platform-owners` group, co-owner on every Tenant, lacks cluster-admin. Tier 3 = Tenant Developers, `human-tenant-owner@tenant-<name>` SA, bound to `tenant-workload-editor` ClusterRole, narrower than k8s `edit`.
       - Paragraph 3 (forward-pointer): "Production EKS translation lives in [`docs/capsule-on-eks.md`](capsule-on-eks.md) §\"Three-tier permission model on EKS\"."
       - End with `### Expected output` block containing: `(No commands in this act — articulate the three-tier model verbally to the audience.)`

    5. **`## Act 2 — Platform-owner role + ceiling demo`** (RUNBOOK-01 act 2 / D-14-07 headline):
       - 2 narrative paragraphs about Tier-2 LIST visibility + ceiling.
       - Single fenced bash block:
         - `kubectl --kubeconfig=$PO_KC get tenants`
         - `kubectl --kubeconfig=$PO_KC create clusterrolebinding po-cluster-admin-attempt --clusterrole=cluster-admin --user=platform-owner`
       - `### Expected output` fenced text block — Tenant list table snippet + verbatim Q6 CRB Forbidden line.

    6. **`## Act 3 — Provision a new tenant live (charlie)`** (RUNBOOK-01 act 3 / D-14-02 + D-14-03):
       - Narrative: platform-owner RBAC-05 lifecycle.
       - Fenced bash block:
         - `bash scripts/poc/capsule/new-tenant.sh charlie --kubeconfig=$PO_KC`
         - `kubectl --kubeconfig=$PO_KC get tenant charlie`
         - `kubectl --kubeconfig=$PO_KC get namespace tenant-charlie`
       - `### Expected output` — tenant created + namespace materialized.

    7. **`## Act 4 — GlobalTenantResource auto-seed observation (≤ 60s)`** (RUNBOOK-01 act 4 / SEED-02):
       - Narrative: 60s propagation contract.
       - Fenced bash block:
         - `kubectl --context k3d-spoke-capsule -n tenant-charlie wait pod/hello-world --for=condition=Ready --timeout=60s`
         - `kubectl --context k3d-spoke-capsule -n tenant-charlie get pod hello-world,service hello-world`
       - `### Expected output` — `pod/hello-world condition met` + pod/service Running tables.

    8. **`## Act 5 — Two perspectives (tenant-owner narrow + platform-owner co-owner)`** (RUNBOOK-01 act 5; DEMO-02 + DEMO-03 + DEMO-04 + DEMO-05; densest):
       - Narrative: switch to tenant-owner, demonstrate scoped LIST + exec/logs + Forbidden Secret + cross-tenant Forbidden; switch back to platform-owner co-owner LIST/EXEC.
       - Fenced bash block (sub-segments 5a..5e):
         - 5a: `kubectl --kubeconfig=$TO_ALPHA_KC get namespaces` (scoped LIST)
         - 5b: `kubectl --kubeconfig=$TO_ALPHA_KC -n tenant-alpha exec hello-world -- sh -c "echo ok-from-shell"` + `kubectl --kubeconfig=$TO_ALPHA_KC -n tenant-alpha logs hello-world --tail=3`
         - 5c: `kubectl --kubeconfig=$TO_ALPHA_KC -n tenant-alpha create secret generic demo-deny --from-literal=k=v` (Forbidden)
         - 5d: `kubectl --kubeconfig=$TO_ALPHA_KC get pods -n tenant-bravo` (cross-tenant Forbidden)
         - 5e: `kubectl --kubeconfig=$PO_KC -n tenant-alpha exec hello-world -- sh -c "hostname"` (platform-owner co-owner success)
       - `### Expected output` — combined block with sub-comments `# 5a`..`# 5e`, embedding verbatim Q3 `ok-from-shell`, Q4 worker process lines, Q6 Secret Forbidden, plus cross-tenant pods-list Forbidden (User string `human-tenant-owner`), plus 5e platform-owner hostname success.

    9. **`## Act 6 — Cleanup (delete charlie)`** (RUNBOOK-01 act 6 / RBAC-05 delete half):
       - Narrative: platform-owner deletes; Capsule unmaterializes.
       - Fenced bash block:
         - `kubectl --kubeconfig=$PO_KC delete tenant charlie`
         - `kubectl --kubeconfig=$PO_KC get namespace tenant-charlie`
       - `### Expected output` — `tenant.capsule.clastix.io "charlie" deleted` + `Error from server (NotFound): namespaces "tenant-charlie" not found`.

    10. **`## Known noise — \`tls: bad certificate\` chatter`** (D-14-01 / RUNBOOK-03) — verbatim per `<interfaces>` "Known noise appendix verbatim text". Translate the (fenced bash block) and (fenced text block) placeholders into actual triple-backtick fenced code blocks of language `bash` and `text` respectively.

    11. **`## Forward pointers`** — verbatim from `<interfaces>` "Forward pointers verbatim text".

    Post-write spot checks:
    ```bash
    # Line count (RESEARCH Q9 budget)
    wc -l docs/poc-capsule-demo.md
    # Section structure
    grep -n '^## ' docs/poc-capsule-demo.md
    # Verbatim Q6 + Q2 + Q3 presence
    grep -c "clusterrolebindings.rbac.authorization.k8s.io is forbidden" docs/poc-capsule-demo.md
    grep -c "tls: bad certificate" docs/poc-capsule-demo.md
    grep -c "ok-from-shell" docs/poc-capsule-demo.md
    grep -c "new-tenant.sh charlie" docs/poc-capsule-demo.md
    # Gitleaks (only kubeconfig env-var placeholders; no actual tokens)
    gitleaks detect --no-banner --redact --no-git docs/poc-capsule-demo.md 2>&1 | grep -iE 'leaks found|no leaks' || echo "(gitleaks not installed)"
    ```
    Line count target: 250-400. Above 450 → collapse Act 1 / Forward pointers. Below 200 → expand Act 5 narrative.

    Run the 4 Wave-0 falsifier bats to confirm GREEN:
    ```bash
    bats tests/bats/runbook-01-static-acts.bats tests/bats/runbook-02-static-checklist.bats tests/bats/runbook-03-static-tls-noise.bats tests/bats/runbook-05-static-charlie.bats --duration
    ```
    All 4 must exit 0.

    Single commit: `docs(14-01): land docs/poc-capsule-demo.md 6-act demo runbook (RUNBOOK-01/02/03/05)`.
  </action>
  <verify>
    <automated>test -f docs/poc-capsule-demo.md && bats tests/bats/runbook-01-static-acts.bats && bats tests/bats/runbook-02-static-checklist.bats && bats tests/bats/runbook-03-static-tls-noise.bats && bats tests/bats/runbook-05-static-charlie.bats && grep -qF "clusterrolebindings.rbac.authorization.k8s.io is forbidden" docs/poc-capsule-demo.md && grep -qF "ok-from-shell" docs/poc-capsule-demo.md && grep -qF "tls: bad certificate" docs/poc-capsule-demo.md && grep -qF "new-tenant.sh charlie" docs/poc-capsule-demo.md && grep -qF "tenant-charlie" docs/poc-capsule-demo.md && grep -qE "^status:[[:space:]]*Active" docs/poc-capsule-demo.md && LINES=$(wc -l < docs/poc-capsule-demo.md); [ "$LINES" -ge 200 ] && [ "$LINES" -le 450 ]</automated>
  </verify>
  <acceptance_criteria>
    - `docs/poc-capsule-demo.md` exists
    - Frontmatter has `status: Active`
    - H1 in first 20 lines
    - 6 ordered `## Act N — ` sections; strict line-number ascending (verified by runbook-01-static-acts.bats strict-order loop)
    - ≥ 6 `### Expected output` fenced blocks
    - Verbatim Q6 string `clusterrolebindings.rbac.authorization.k8s.io is forbidden` present (Act 2)
    - Verbatim Q6 `human-tenant-owner` present (Act 5)
    - Verbatim Q3 `ok-from-shell` present (Act 5)
    - Verbatim Q2 `tls: bad certificate` present (Known noise)
    - `## Known noise` line > `## Act 6 —` line
    - `## Pre-demo checklist` appears BEFORE `## Act 1 —`
    - `new-tenant.sh charlie` + `tenant-charlie` + `delete tenant charlie --ignore-not-found` literals present
    - `runbook-01-static-acts.bats` GREEN
    - `runbook-02-static-checklist.bats` GREEN
    - `runbook-03-static-tls-noise.bats` GREEN
    - `runbook-05-static-charlie.bats` GREEN
    - Line count between 200 and 450
    - Zero bearer tokens / kubeconfig payloads (P29; gitleaks-clean spot check)
  </acceptance_criteria>
  <done>4 of 5 RUNBOOK-* Wave-0 RED bats turn GREEN. `runbook-03-static-eks-three-tier.bats` flips in Task 2. RUNBOOK-04 UAT remains for Plan 14-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append "Three-tier permission model on EKS" section to docs/capsule-on-eks.md (RUNBOOK-03 cross-link target)</name>
  <files>docs/capsule-on-eks.md</files>
  <behavior>
    - Existing `docs/capsule-on-eks.md` content preserved byte-for-byte except the inserted section
    - New section header `## Three-tier permission model on EKS` exists
    - Section contains explicit `Tier 1`, `Tier 2`, `Tier 3` labels
    - Section contains `IRSA`, `tenant-workload-editor`, `capsule-platform-owner` literals
    - Placement: AFTER existing `## Required permissions / RBAC` table, BEFORE next existing `## ` heading
    - File grows ~15 lines
  </behavior>
  <read_first>
    - .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-PATTERNS.md (`docs/capsule-on-eks.md (MODIFY — append ~15 lines)` section lines 537-563)
    - .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-RESEARCH.md Q1 (existing doc analysis — gap confirmed; Tier labels do NOT yet appear)
    - docs/capsule-on-eks.md (read full file — locate `## Required permissions / RBAC` table around line 35 + identify next `## ` heading)
    - tests/bats/runbook-03-static-eks-three-tier.bats (Wave-0 falsifier — defines GREEN: section header + 3 Tier labels + 3 carrier literals)
  </read_first>
  <action>
    Append the verbatim `## Three-tier permission model on EKS` section per `<interfaces>` "docs/capsule-on-eks.md append".

    Procedure:
    1. Read `docs/capsule-on-eks.md` and locate the line range of `## Required permissions / RBAC` (RESEARCH Q1 confirms ~lines 35-43).
    2. Locate the line number of the NEXT `## ` heading after the RBAC table (e.g., `## Topology (EKS target)` per RESEARCH Q1).
    3. Use the Edit tool to insert the new section BETWEEN the existing RBAC table and the next `## ` heading. Preserve the surrounding bytes exactly.

    The verbatim section text (copy literally from `<interfaces>` "docs/capsule-on-eks.md append" — the entire markdown block starting with `## Three-tier permission model on EKS` through the trailing paragraph about IRSA TrustPolicy).

    Post-edit verification:
    ```bash
    grep -n '^## ' docs/capsule-on-eks.md
    ```
    The new section header must appear between `## Required permissions / RBAC` and the pre-existing next `## ` heading.

    Run the Wave-0 falsifier:
    ```bash
    bats tests/bats/runbook-03-static-eks-three-tier.bats
    ```
    Must exit 0.

    Single commit: `docs(14-01): append three-tier permission model section to docs/capsule-on-eks.md (RUNBOOK-03 cross-link target)`.
  </action>
  <verify>
    <automated>test -f docs/capsule-on-eks.md && bats tests/bats/runbook-03-static-eks-three-tier.bats && grep -qE "^## Three-tier permission model" docs/capsule-on-eks.md && grep -qF "Tier 1" docs/capsule-on-eks.md && grep -qF "Tier 2" docs/capsule-on-eks.md && grep -qF "Tier 3" docs/capsule-on-eks.md && grep -qF "IRSA" docs/capsule-on-eks.md && grep -qF "tenant-workload-editor" docs/capsule-on-eks.md && grep -qF "capsule-platform-owner" docs/capsule-on-eks.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/capsule-on-eks.md` contains `## Three-tier permission model on EKS` heading
    - All three labels `Tier 1`, `Tier 2`, `Tier 3` present in the file
    - All three carriers `IRSA`, `tenant-workload-editor`, `capsule-platform-owner` present
    - Section placement: line number of new header > line number of `## Required permissions / RBAC`; new header line number < line number of the previously-next `## ` heading
    - `runbook-03-static-eks-three-tier.bats` GREEN
    - Pre-existing content preserved byte-for-byte except the inserted section (verified by git diff showing only the appended ~15 lines)
  </acceptance_criteria>
  <done>5th and final RUNBOOK-* Wave-0 RED bats flips GREEN. RUNBOOK-03 cross-link target landed. Phase 14 docs deliverables complete.</done>
</task>

</tasks>

<verification>
- After both tasks: all 5 `runbook-*-static-*.bats` files GREEN
- `docs/poc-capsule-demo.md` line count 250-400 (within 15-min cold-read budget)
- `docs/capsule-on-eks.md` grew by ~15 lines (no other edits)
- Zero new `spoke-capsule` mentions in default-path scripts (P31; no script edits in this plan)
- Zero bearer tokens / kubeconfig contents in docs (P29; gitleaks-clean)
- Wave 0 demo-*-live-*.bats remain unchanged from Plan 14-00 (no edits to bats files in Plan 14-01)
</verification>

<success_criteria>
- 1 NEW file (`docs/poc-capsule-demo.md`) + 1 MODIFIED file (`docs/capsule-on-eks.md`) committed
- 5 Wave-0 RED runbook-* static bats turn GREEN
- All verbatim RESEARCH Q2/Q3/Q4/Q5/Q6 strings present in the runbook (no invented strings)
- D-14-01 (TLS document-only) honored: Known noise appendix lands AFTER Act 6 with ADR-008 cross-link
- D-14-02 + D-14-03 (charlie live provision) honored: `new-tenant.sh charlie` cold in Act 3
- D-14-04 (single doc) honored: 6 ordered Acts in one file
- D-14-05 (executable checklist) honored: fenced bash snippets, no narrative bullets
- D-14-06 (Expected output per act) honored: ≥ 6 Expected output fenced blocks
- D-14-07 (Act 2 headline Forbidden) honored: CRB attempt + verbatim Q6 output
- D-14-09 (capsule-proxy:30443 routing) honored: Act 5 5a/5d narrative references proxy:30443 LIST filtering
- RUNBOOK-04 (≤ 15-min UAT) deferred to Plan 14-03 (requires human cold-read)
</success_criteria>

<output>
After completion, create `.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-01-SUMMARY.md` capturing:
- `docs/poc-capsule-demo.md` line count + section structure (`grep -n '^## '` output)
- `docs/capsule-on-eks.md` git diff stat (lines added/removed)
- All 5 RUNBOOK-* static bats GREEN status snapshot
- Verbatim-string spot-check grep results
- Gitleaks spot-check result
- Carry-forward note: RUNBOOK-04 UAT pending Plan 14-03
</output>
