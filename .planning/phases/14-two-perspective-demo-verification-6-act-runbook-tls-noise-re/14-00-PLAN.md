---
phase: 14-two-perspective-demo-verification-6-act-runbook-tls-noise-re
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/bats/demo-01-live-platform-owner-list.bats
  - tests/bats/demo-02-live-tenant-owner-scoped-list.bats
  - tests/bats/demo-03-live-pod-ops.bats
  - tests/bats/demo-04-live-tenant-secret-forbidden.bats
  - tests/bats/demo-05-live-cross-tenant-forbidden.bats
  - tests/bats/demo-06-live-platform-owner-cluster-admin-denials.bats
  - tests/bats/runbook-01-static-acts.bats
  - tests/bats/runbook-02-static-checklist.bats
  - tests/bats/runbook-03-static-tls-noise.bats
  - tests/bats/runbook-03-static-eks-three-tier.bats
  - tests/bats/runbook-05-static-charlie.bats
autonomous: true
requirements:
  - DEMO-01
  - DEMO-02
  - DEMO-03
  - DEMO-04
  - DEMO-05
  - DEMO-06
  - RUNBOOK-01
  - RUNBOOK-02
  - RUNBOOK-03
  - RUNBOOK-05
requirements_addressed:
  - DEMO-01
  - DEMO-02
  - DEMO-03
  - DEMO-04
  - DEMO-05
  - DEMO-06
  - RUNBOOK-01
  - RUNBOOK-02
  - RUNBOOK-03
  - RUNBOOK-05
tags:
  - capsule
  - demo
  - runbook
  - bats
  - wave0
  - nyquist

must_haves:
  truths:
    - "11 NEW bats files exist under tests/bats/ covering DEMO-01..06 + RUNBOOK-01/02/03/05 contracts (per D-14-08)"
    - "All 11 NEW bats files RED before docs/poc-capsule-demo.md is authored (Nyquist gate; D-13-15 inheritance)"
    - "Wave 0 bats lock the falsifier matrix: two-kubeconfig live RBAC perspective (DEMO-01..06), static markdown shape for runbook (RUNBOOK-01/02/05), TLS-noise appendix shape (RUNBOOK-03 D-14-01), EKS three-tier append shape (RUNBOOK-03 Q1)"
    - "Marquee tenant name `charlie` (D-14-02) appears verbatim in runbook-05-static bats grep targets — single canonical name across runbook + bats"
    - "Forbidden-string verbatim captures from RESEARCH Q6 (CRB + secret) are baked into demo-04 + demo-06 bats — no invented strings"
    - "Phase 13 inheritance still live: capsule-platform-owner ClusterRole + tenant-workload-editor ClusterRole + GlobalTenantResource + capsule-proxy NodePort 30443 + CapsuleConfiguration.userGroups (preflight probe verifies)"
  artifacts:
    - path: "tests/bats/demo-01-live-platform-owner-list.bats"
      provides: "DEMO-01 platform-owner kubectl get tenants + get namespaces + get pods -A live falsifier"
      contains: "PO_KUBECONFIG"
    - path: "tests/bats/demo-02-live-tenant-owner-scoped-list.bats"
      provides: "DEMO-02 tenant-owner scoped LIST through capsule-proxy NodePort 30443 (alpha + bravo @test groups)"
      contains: "ALPHA_KUBECONFIG"
    - path: "tests/bats/demo-03-live-pod-ops.bats"
      provides: "DEMO-03 positive control — kubectl get/logs/exec on SEED-01 hello-world (tenant-owner own ns + platform-owner all ns)"
      contains: "ok-from-shell"
    - path: "tests/bats/demo-04-live-tenant-secret-forbidden.bats"
      provides: "DEMO-04 kubectl create secret Forbidden under tenant-owner (RBAC-01 boundary + RESEARCH Q6 verbatim User string)"
      contains: "human-tenant-owner"
    - path: "tests/bats/demo-05-live-cross-tenant-forbidden.bats"
      provides: "DEMO-05 alpha→bravo + bravo→alpha cross-tenant LIST Forbidden (symmetric mirror)"
      contains: "Forbidden|forbidden|cannot list"
    - path: "tests/bats/demo-06-live-platform-owner-cluster-admin-denials.bats"
      provides: "DEMO-06 platform-owner 3-action denial matrix (CRB + nodes -o yaml + csr) with verbatim CRB Forbidden string from RESEARCH Q6"
      contains: "clusterrolebindings.rbac.authorization.k8s.io is forbidden"
    - path: "tests/bats/runbook-01-static-acts.bats"
      provides: "RUNBOOK-01 docs/poc-capsule-demo.md exists; 6 ordered Act sections; each act has Expected output block (D-14-04 + D-14-06)"
      contains: "## Act"
    - path: "tests/bats/runbook-02-static-checklist.bats"
      provides: "RUNBOOK-02 pre-demo checklist executable snippets — fix-dns, kubeconfig minting, charlie reset (D-14-05)"
      contains: "task fix-dns-poc-capsule"
    - path: "tests/bats/runbook-03-static-tls-noise.bats"
      provides: "RUNBOOK-03 + D-14-01 TLS-noise appendix — verbatim 'tls: bad certificate' literal + ADR-008 cross-link + appears AFTER Act 6"
      contains: "tls: bad certificate"
    - path: "tests/bats/runbook-03-static-eks-three-tier.bats"
      provides: "RUNBOOK-03 Q1 docs/capsule-on-eks.md explicit Tier 1/2/3 labels + IRSA + tenant-workload-editor + capsule-platform-owner cross-mapping"
      contains: "Three-tier permission model"
    - path: "tests/bats/runbook-05-static-charlie.bats"
      provides: "RUNBOOK-05 + D-14-02 + D-14-03 — charlie verbatim in Act 3 + Act 6 + `new-tenant.sh charlie` literal + tenant-charlie namespace literal"
      contains: "new-tenant.sh charlie"
  key_links:
    - from: "tests/bats/demo-*-live-*.bats"
      to: "scripts/poc/capsule/issue-platform-owner-kubeconfig.sh + issue-tenant-kubeconfig.sh"
      via: "setup_file mint + KARYON_BATS_TMPDIR per-suite isolation"
      pattern: "issue-(platform-owner|tenant)-kubeconfig\\.sh"
    - from: "tests/bats/demo-*-live-*.bats"
      to: "k3d-spoke-capsule cluster (kubectl --kubeconfig=$KC against apiserver + capsule-proxy:30443)"
      via: "live kubectl probes"
      pattern: "kubectl --kubeconfig="
    - from: "tests/bats/runbook-*-static-*.bats"
      to: "docs/poc-capsule-demo.md (RED until Plan 14-01) + docs/capsule-on-eks.md (RED until Plan 14-01 append)"
      via: "grep -F + grep -nE strict-order assertions"
      pattern: "REPO_ROOT/docs/(poc-capsule-demo|capsule-on-eks)\\.md"
---

<objective>
Plan 14-00 — Wave 0 RED bats scaffold. Lands ALL 11 NEW bats files covering DEMO-01..06 + RUNBOOK-01/02/03/05 contracts as failing tests BEFORE the runbook is authored or the EKS doc appended. Plus a Phase 13 inheritance preflight probe verifying the live cluster primitives (ClusterRoles + GlobalTenantResource + capsule-proxy + CapsuleConfiguration.userGroups) all still hold.

Purpose: Lock the falsifier matrix in advance per Nyquist gate (D-13-15 + D-09-08 inheritance). Every executor task in Wave 1+ has a pre-existing automated test command before implementation lands. Per D-14-08: new `demo-*-live-*.bats` + `runbook-*-static-*.bats` files keyed to REQs (preference for new files over extending Phase 13 rbac/negative-rbac).

Output: 11 NEW bats files + 1 commit + Phase 13 inheritance preflight log. All 11 bats RED end-of-plan (live demo-* tests skip — fail with "RED until 14-02 lands" when cluster reachable but PO/tenant kubeconfig minting succeeds and assertions fire; static runbook-* tests fail because `docs/poc-capsule-demo.md` does not yet exist).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-CONTEXT.md
@.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-RESEARCH.md
@.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-PATTERNS.md
@.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-VALIDATION.md
@.planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-CONTEXT.md

# Analog bats files (read for naming + assertion shape conventions)
@tests/bats/rbac-08-live-platform-owner-grant-matrix.bats
@tests/bats/rbac-05-live-tenant-owner-grant-matrix.bats
@tests/bats/rbac-06-live-tenant-kubeconfig.bats
@tests/bats/negative-rbac-01-cross-tenant-list.bats
@tests/bats/seed-03-live-fresh-tenant.bats
@tests/bats/seed-01-static-globaltenantresource.bats
@tests/bats/rbac-01-static-tenant-workload-editor.bats
@tests/bats/docs-05-runbook.bats
@tests/bats/docs-06-eks.bats
@tests/bats/test_helper.bash

<interfaces>
<!-- Key literals + patterns the executor needs verbatim from RESEARCH Q6 + PATTERNS.md. -->
<!-- Executor copies these EXACTLY — no inventing Forbidden strings or section names. -->

## Verbatim RESEARCH Q6 Forbidden strings (use literally — NEVER reword)

### DEMO-06 (platform-owner attempting ClusterRoleBinding create):
```
error: failed to create clusterrolebinding: clusterrolebindings.rbac.authorization.k8s.io is forbidden: User "system:serviceaccount:capsule-system:platform-owner" cannot create resource "clusterrolebindings" in API group "rbac.authorization.k8s.io" at the cluster scope
```

bats grep targets (literal):
- `clusterrolebindings.rbac.authorization.k8s.io is forbidden`
- `system:serviceaccount:capsule-system:platform-owner`

### DEMO-04 (tenant-owner attempting Secret create in own namespace):
```
error: failed to create secret secrets is forbidden: User "system:serviceaccount:tenant-alpha:human-tenant-owner" cannot create resource "secrets" in API group "" in the namespace "tenant-alpha"
```

bats grep targets (literal):
- `forbidden:`
- `human-tenant-owner`

## Verbatim RESEARCH Q2 TLS noise log line (use literally in runbook-03 grep):
```
2026/05/19 23:50:49 http: TLS handshake error from 10.42.0.1:39986: remote error: tls: bad certificate
```

bats grep target (literal):
- `tls: bad certificate`

## Canonical setup_file pattern (verbatim from rbac-08; PATTERNS.md "Shared: Per-suite TMPDIR")

```bash
setup_file() {
  if ! kubectl --context=k3d-spoke-capsule cluster-info >/dev/null 2>&1; then
    skip "k3d-spoke-capsule cluster not reachable; skipping live bats"
  fi
  export KARYON_BATS_TMPDIR="${TMPDIR:-/tmp}/karyon-tenants/bats-${BATS_RUN_TMPDIR##*/}"
  mkdir -p "$KARYON_BATS_TMPDIR"
  bash "${REPO_ROOT}/scripts/poc/capsule/issue-platform-owner-kubeconfig.sh" \
    --duration=2h --write-to "${KARYON_BATS_TMPDIR}/po.kubeconfig" >/dev/null 2>&1
  export PO_KUBECONFIG="${KARYON_BATS_TMPDIR}/po.kubeconfig"
}

teardown_file() {
  rm -rf "$KARYON_BATS_TMPDIR"
}

setup() {
  if ! kubectl --context=k3d-spoke-capsule cluster-info >/dev/null 2>&1; then
    skip "k3d-spoke-capsule cluster not reachable; skipping live bats"
  fi
  [ -f "${PO_KUBECONFIG:-/dev/null}" ] || skip "platform-owner kubeconfig not minted (setup_file failed; expected RED pre-implementation)"
}
```

## Canonical bats header convention (D-13-15 Nyquist gate; load-bearing `RED until ...` line)

```bash
#!/usr/bin/env bats
# tests/bats/<filename>.bats
# Phase 14 / Wave 0 (D-14-08 + D-13-15 Nyquist gate)
# <REQ-ID> — <one-line behavior>.
# RED until <Plan N-NN> lands <what>.

load 'test_helper'
```

## Phase 14 file path conventions:
- New demo runbook (RED until 14-01):    docs/poc-capsule-demo.md
- EKS doc (existing; appended in 14-01): docs/capsule-on-eks.md
- Existing operator runbook (untouched): docs/poc-capsule.md
- Existing ADR-008 (cross-link target):  docs/adr/0008-capsule-multi-tenancy-graduation.md
- Tenant-owner SA name:                  human-tenant-owner (RBAC-02 / Phase 13)
- Platform-owner SA fully-qualified:     system:serviceaccount:capsule-system:platform-owner
- Marquee tenant name (D-14-02):         charlie
- Marquee tenant namespace (RESEARCH Q5): tenant-charlie  (NOT charlie-app1 — single ns per new-tenant.sh)
- hello-world Pod name + exec probe:     hello-world  +  sh -c "echo ok-from-shell"  (RESEARCH Q3)
- hello-world logs probe:                logs hello-world --tail=3  →  grep "worker process"  (RESEARCH Q4)
- capsule-proxy NodePort:                30443 (D-14-09 / Phase 10 inheritance)

## Forbidden-grep idiom (Pitfall 11-P3 permissive)
```bash
echo "$output" | grep -qE 'Forbidden|forbidden|cannot create resource|cannot list'
```
Plus verbatim-User sub-grep for DEMO-04 / DEMO-06 per RESEARCH Q6 (above).

## Self-invalidating grep gate defense (planner-antipatterns rule)
For any static-grep bats whose own comments contain the literal being asserted (e.g., `charlie` in runbook-05 comments), filter comments first:
```bash
run bash -c "grep -v '^[[:space:]]*#' '$DOC' | grep -F 'charlie'"
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bats process → spoke-capsule apiserver | bats mints + uses kubeconfigs; apiserver is the trust anchor |
| bats process → local filesystem (`${TMPDIR}/karyon-tenants/`) | Kubeconfigs land owner-private; never git-tracked |
| static-bats process → `docs/*.md` files | Read-only grep; no kubectl reach |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-00-01 | Information disclosure | Per-suite kubeconfigs minted into `${TMPDIR}/karyon-tenants/bats-${BATS_RUN_TMPDIR##*/}/` | mitigate | Path-locked via `--write-to` validator in `issue-*-kubeconfig.sh` (BL-01/BL-02 inheritance); P29 `.gitleaks.toml` kubeconfig-bearer-token rule fires on any leak attempt; `teardown_file` removes the directory on suite completion |
| T-14-00-02 | Spoofing | `system:serviceaccount:tenant-alpha:human-tenant-owner` Forbidden literal embedded in bats grep target | accept | The User string is captured from a verbatim live probe (RESEARCH Q6); embedding it in a test is the standard pattern (rbac-06 line 49 precedent). No bearer token, no kubeconfig payload — just the SA identity string |
| T-14-00-03 | Tampering | bats `setup_file` invokes `bash scripts/poc/capsule/issue-*-kubeconfig.sh` | mitigate | Scripts are repo-tracked, peer-reviewed; bats reads them by path (no arbitrary download); writes constrained to `${TMPDIR}/karyon-tenants/` per Phase 10 BL-01/BL-02 |
| T-14-00-04 | Denial of service | Live bats against `spoke-capsule` create+delete probe resources (Forbidden tests don't mutate; positive-control tests use existing hello-world Pod) | accept | Cluster is a POC k3d; cluster-stop/start recovery is 30s via `task fix-dns-poc-capsule`. No production blast radius |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight Phase 13 inheritance verification (ClusterRoles + GlobalTenantResource + capsule-proxy + CapsuleConfiguration.userGroups)</name>
  <files>(no file output — STATE / inspection only; informational stdout to /tmp/14-00-preflight.log)</files>
  <read_first>
    - .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-CONTEXT.md (Phase 13 inheritance section — Phase 13 RBAC + SEED + DELIVERY GREEN live state must hold before Phase 14 verifies)
    - .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-RESEARCH.md (Q2-Q6 verbatim live probes captured 2026-05-19)
    - .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-CONTEXT.md (Phase 13 land state — what should now exist on the cluster)
  </read_first>
  <action>
    Run the following preflight probes against `k3d-spoke-capsule` to confirm Phase 13 inheritance still holds before Phase 14 implementation lands. Capture stdout + stderr to `/tmp/14-00-preflight.log` for SUMMARY citation.

    ```bash
    set +e  # capture failures, don't abort
    LOG=/tmp/14-00-preflight.log
    : > "$LOG"

    # Probe 1: cluster reachable
    echo "=== Probe 1: cluster-info ===" | tee -a "$LOG"
    kubectl --context=k3d-spoke-capsule cluster-info 2>&1 | tee -a "$LOG"

    # Probe 2: CapsuleConfiguration userGroups inheritance (Phase 11 D-11-02 — required for capsule-proxy LIST filter)
    echo "=== Probe 2: CapsuleConfiguration.userGroups ===" | tee -a "$LOG"
    kubectl --context=k3d-spoke-capsule get capsuleconfiguration default \
      -o jsonpath='{.spec.userGroups}' 2>&1 | tee -a "$LOG"
    echo "" | tee -a "$LOG"
    # Expected to contain: capsule.clastix.io, system:serviceaccounts, system:authenticated

    # Probe 3: tenant-workload-editor ClusterRole (Phase 13 RBAC-01 inheritance)
    echo "=== Probe 3: tenant-workload-editor ClusterRole ===" | tee -a "$LOG"
    kubectl --context=k3d-spoke-capsule get clusterrole tenant-workload-editor -o name 2>&1 | tee -a "$LOG"

    # Probe 4: capsule-platform-owner ClusterRole + dual-subject CRB (Phase 13 RBAC-04 inheritance)
    echo "=== Probe 4: capsule-platform-owner ClusterRole + CRB ===" | tee -a "$LOG"
    kubectl --context=k3d-spoke-capsule get clusterrole capsule-platform-owner -o name 2>&1 | tee -a "$LOG"
    kubectl --context=k3d-spoke-capsule get clusterrolebinding capsule-platform-owner \
      -o jsonpath='{.subjects}' 2>&1 | tee -a "$LOG"
    echo "" | tee -a "$LOG"
    # Expected: ClusterRole exists; CRB subjects[] contains Group: capsule-platform-owners + SA platform-owner

    # Probe 5: platform-owner SA in capsule-system (Phase 13 D-13-09)
    echo "=== Probe 5: platform-owner SA ===" | tee -a "$LOG"
    kubectl --context=k3d-spoke-capsule -n capsule-system get sa platform-owner -o name 2>&1 | tee -a "$LOG"

    # Probe 6: GlobalTenantResource hello-world-seeded reconciled (Phase 13 SEED-01)
    echo "=== Probe 6: GlobalTenantResource hello-world-seeded ===" | tee -a "$LOG"
    kubectl --context=k3d-spoke-capsule get globaltenantresource hello-world-seeded -o name 2>&1 | tee -a "$LOG"

    # Probe 7: hello-world Pod nginx:alpine image in tenant-alpha (Phase 13 D-13-02 + RESEARCH Q3 — DEMO-03 enabler)
    echo "=== Probe 7: hello-world Pod image (DEMO-03 enabler) ===" | tee -a "$LOG"
    kubectl --context=k3d-spoke-capsule -n tenant-alpha get pod hello-world \
      -o jsonpath='{.spec.containers[*].image}' 2>&1 | tee -a "$LOG"
    echo "" | tee -a "$LOG"
    # Expected: docker.io/library/nginx:alpine

    # Probe 8: capsule-proxy NodePort 30443 (D-14-09 / Phase 10)
    echo "=== Probe 8: capsule-proxy Service NodePort ===" | tee -a "$LOG"
    kubectl --context=k3d-spoke-capsule -n capsule-system get svc capsule-proxy \
      -o jsonpath='{.spec.ports[?(@.nodePort)].nodePort}' 2>&1 | tee -a "$LOG"
    echo "" | tee -a "$LOG"
    # Expected: 30443

    # Probe 9: alpha + bravo tenants present with capsule-platform-owners group in spec.owners (RBAC-06)
    echo "=== Probe 9: alpha + bravo tenants + capsule-platform-owners co-ownership ===" | tee -a "$LOG"
    kubectl --context=k3d-spoke-capsule get tenant alpha bravo \
      -o jsonpath='{range .items[*]}{.metadata.name}{": owners="}{range .spec.owners[*]}{.kind}{"/"}{.name}{","}{end}{"\n"}{end}' 2>&1 | tee -a "$LOG"
    # Expected: alpha and bravo each list Group/capsule-platform-owners

    # Probe 10: Forbidden CRB still fires under platform-owner kubeconfig (verbatim RESEARCH Q6 spot-check)
    echo "=== Probe 10: Forbidden CRB live spot-check ===" | tee -a "$LOG"
    KARYON_PREFLIGHT_TMP="${TMPDIR:-/tmp}/karyon-tenants/preflight-14-00"
    mkdir -p "$KARYON_PREFLIGHT_TMP"
    bash scripts/poc/capsule/issue-platform-owner-kubeconfig.sh \
      --duration=30m --write-to "$KARYON_PREFLIGHT_TMP/po.kubeconfig" >>"$LOG" 2>&1
    kubectl --kubeconfig="$KARYON_PREFLIGHT_TMP/po.kubeconfig" create clusterrolebinding preflight-14-00-attempt \
      --clusterrole=cluster-admin --user=preflight-check 2>&1 | tee -a "$LOG"
    rm -rf "$KARYON_PREFLIGHT_TMP"
    # Expected: error containing 'clusterrolebindings.rbac.authorization.k8s.io is forbidden'

    # FAIL FAST CHECK
    FAIL=0
    grep -qF 'system:serviceaccounts' "$LOG" || { echo "BLOCKER: Probe 2 — CapsuleConfiguration.userGroups missing system:serviceaccounts (Phase 11 D-11-02 drift)" | tee -a "$LOG"; FAIL=1; }
    grep -qF 'clusterrole.rbac.authorization.k8s.io/tenant-workload-editor' "$LOG" || { echo "BLOCKER: Probe 3 — tenant-workload-editor ClusterRole missing (Phase 13 RBAC-01 drift)" | tee -a "$LOG"; FAIL=1; }
    grep -qF 'clusterrole.rbac.authorization.k8s.io/capsule-platform-owner' "$LOG" || { echo "BLOCKER: Probe 4 — capsule-platform-owner ClusterRole missing (Phase 13 RBAC-04 drift)" | tee -a "$LOG"; FAIL=1; }
    grep -qF 'globaltenantresource.capsule.clastix.io/hello-world-seeded' "$LOG" || { echo "BLOCKER: Probe 6 — GTR hello-world-seeded missing (Phase 13 SEED-01 drift)" | tee -a "$LOG"; FAIL=1; }
    grep -qF 'docker.io/library/nginx:alpine' "$LOG" || { echo "BLOCKER: Probe 7 — hello-world Pod nginx:alpine image missing (DEMO-03 exec/logs probes will fail without BusyBox sh)" | tee -a "$LOG"; FAIL=1; }
    grep -qF '30443' "$LOG" || { echo "BLOCKER: Probe 8 — capsule-proxy NodePort 30443 missing (D-14-09 / Phase 10 drift)" | tee -a "$LOG"; FAIL=1; }
    grep -qF 'capsule-platform-owners' "$LOG" || { echo "BLOCKER: Probe 9 — alpha/bravo Tenant CRs missing capsule-platform-owners co-ownership (RBAC-06 drift)" | tee -a "$LOG"; FAIL=1; }
    grep -qF 'clusterrolebindings.rbac.authorization.k8s.io is forbidden' "$LOG" || { echo "BLOCKER: Probe 10 — Forbidden CRB string drifted (RESEARCH Q6 invalidated; runbook expected-output blocks will fail UAT)" | tee -a "$LOG"; FAIL=1; }

    [ "$FAIL" -eq 0 ] || { echo "Phase 13 inheritance preflight FAILED — investigate before Wave 1+. Log: $LOG"; exit 1; }
    echo "Phase 13 inheritance preflight PASSED — Phase 14 may proceed."
    ```

    Read-only (probe 10 attempts a create that MUST 403; no cluster mutation possible on success). No script edits, no manifest edits, no Helm changes.
  </action>
  <verify>
    <automated>test -f /tmp/14-00-preflight.log && grep -qF 'clusterrolebindings.rbac.authorization.k8s.io is forbidden' /tmp/14-00-preflight.log && grep -qF 'docker.io/library/nginx:alpine' /tmp/14-00-preflight.log && grep -qF '30443' /tmp/14-00-preflight.log && grep -qF 'capsule-platform-owners' /tmp/14-00-preflight.log</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/14-00-preflight.log` exists and contains output from probes 1-10
    - Probe 2 output contains all three groups: `capsule.clastix.io`, `system:serviceaccounts`, `system:authenticated`
    - Probe 3 + 4 confirm `tenant-workload-editor` + `capsule-platform-owner` ClusterRoles both present
    - Probe 6 confirms `globaltenantresource.capsule.clastix.io/hello-world-seeded` present
    - Probe 7 confirms `docker.io/library/nginx:alpine` image (DEMO-03 BusyBox-sh enabler)
    - Probe 8 confirms NodePort `30443`
    - Probe 9 confirms `capsule-platform-owners` Group in alpha + bravo Tenant `spec.owners`
    - Probe 10 confirms verbatim RESEARCH Q6 Forbidden string `clusterrolebindings.rbac.authorization.k8s.io is forbidden` still fires
    - If ANY probe fails the fail-fast check, task exits 1 and Phase 14 is blocked
  </acceptance_criteria>
  <done>Phase 13 inheritance baseline verified — Phase 14 implementation can proceed against a known-stable Phase 13 land state. Preflight log captured for SUMMARY citation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Land Wave 0 RED bats files — DEMO live (6 files) + RUNBOOK static (5 files) = 11 files total</name>
  <files>
    tests/bats/demo-01-live-platform-owner-list.bats,
    tests/bats/demo-02-live-tenant-owner-scoped-list.bats,
    tests/bats/demo-03-live-pod-ops.bats,
    tests/bats/demo-04-live-tenant-secret-forbidden.bats,
    tests/bats/demo-05-live-cross-tenant-forbidden.bats,
    tests/bats/demo-06-live-platform-owner-cluster-admin-denials.bats,
    tests/bats/runbook-01-static-acts.bats,
    tests/bats/runbook-02-static-checklist.bats,
    tests/bats/runbook-03-static-tls-noise.bats,
    tests/bats/runbook-03-static-eks-three-tier.bats,
    tests/bats/runbook-05-static-charlie.bats
  </files>
  <behavior>
    - Test 1 (demo-01): platform-owner kubeconfig LIST returns alpha + bravo tenants + tenant-alpha/tenant-bravo namespaces; live-skip without cluster
    - Test 2 (demo-02): alpha-tenant-owner kubeconfig sees ONLY tenant-alpha/alpha-* via proxy:30443; bravo-tenant-owner symmetric mirror
    - Test 3 (demo-03): tenant-owner `exec hello-world -- sh -c "echo ok-from-shell"` succeeds; `logs hello-world --tail=3` contains "worker process"; platform-owner same probes across all tenant namespaces
    - Test 4 (demo-04): tenant-owner `create secret` Forbidden with verbatim User string `human-tenant-owner`; idempotent cleanup
    - Test 5 (demo-05): alpha→bravo pod LIST Forbidden; bravo→alpha symmetric mirror Forbidden
    - Test 6 (demo-06): platform-owner CRB create Forbidden with verbatim `clusterrolebindings.rbac.authorization.k8s.io is forbidden`; `get nodes -o yaml` Forbidden; `get certificatesigningrequests` Forbidden
    - Test 7 (runbook-01): `docs/poc-capsule-demo.md` exists; h1 present; `status: Active` frontmatter; 6 ordered `## Act N — ` sections; ≥6 `### Expected output` blocks
    - Test 8 (runbook-02): `## Pre-demo checklist` section; references `task fix-dns-poc-capsule`, `issue-platform-owner-kubeconfig.sh`, `issue-tenant-kubeconfig.sh alpha human-tenant-owner` + bravo equivalent, `delete tenant charlie --ignore-not-found`
    - Test 9 (runbook-03 tls): verbatim `tls: bad certificate` literal in doc; `ADR-008` or `adr/0008` cross-link; `## Known noise` section appears AFTER `## Act 6 —`; `capsule-controller-manager` literal present
    - Test 10 (runbook-03 eks-tier): `docs/capsule-on-eks.md` contains `## Three-tier permission model` section; explicit `Tier 1`, `Tier 2`, `Tier 3` labels; `IRSA`, `tenant-workload-editor`, `capsule-platform-owner` literals
    - Test 11 (runbook-05): `charlie` between `## Act 3 —` and `## Act 4 —`; `charlie` after `## Act 6 —`; `new-tenant.sh charlie` literal; `tenant-charlie` namespace literal — all comment-filtered to defeat self-invalidating grep gate
  </behavior>
  <read_first>
    - .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-PATTERNS.md (every bats file's analog + verbatim code excerpt — lines 28-457 cover all 11 files)
    - .planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-RESEARCH.md (Q2 TLS verbatim + Q3 exec probe + Q4 logs probe + Q5 tenant-charlie ns + Q6 Forbidden verbatim + Q10 bats fixture pattern)
    - tests/bats/rbac-08-live-platform-owner-grant-matrix.bats (analog for demo-01 + demo-06 — single-PO kubeconfig setup_file lines 12-25; auth-can-i ceiling matrix; comment-block convention lines 1-9)
    - tests/bats/rbac-05-live-tenant-owner-grant-matrix.bats (analog for demo-02 + demo-03 dual-kubeconfig setup_file lines 10-22)
    - tests/bats/rbac-06-live-tenant-kubeconfig.bats (analog for demo-04 Forbidden-create-secret idiom lines 44-53 — verbatim)
    - tests/bats/negative-rbac-01-cross-tenant-list.bats (analog for demo-05 cross-tenant Forbidden + demo-02 scoped-LIST pattern lines 52-72)
    - tests/bats/seed-03-live-fresh-tenant.bats (analog for kubeconfig-mint + teardown_file structure)
    - tests/bats/seed-01-static-globaltenantresource.bats (analog for runbook-02 + runbook-03-tls static file-exists + grep -F literal idiom)
    - tests/bats/docs-05-runbook.bats (analog for runbook-01 ordered-section strict-order grep loop lines 14-37)
    - tests/bats/docs-06-eks.bats (analog for runbook-03-eks multi-literal grep loop lines 33-39 + 41-53)
    - tests/bats/rbac-01-static-tenant-workload-editor.bats (analog for runbook-05 comment-filtered grep idiom lines 180-186)
    - tests/bats/test_helper.bash (load + REPO_ROOT export contract — every new bats `load 'test_helper'`)
  </read_first>
  <action>
    Create 11 NEW bats files at the paths listed above. Every file uses the EXACT analog patterns + verbatim literals from `<interfaces>` and PATTERNS.md.

    CRITICAL GLOBAL RULES — apply to EVERY bats file:
    - Shebang: `#!/usr/bin/env bats`
    - `load 'test_helper'` (provides `REPO_ROOT`)
    - Header comment block (load-bearing `RED until ...` line per D-13-15):
      ```
      # tests/bats/<filename>.bats
      # Phase 14 / Wave 0 (D-14-08 + D-13-15 Nyquist gate)
      # <REQ-ID> — <one-line behavior>.
      # RED until <Plan N-NN> lands <what>.
      ```
    - Live bats: `setup_file` + `teardown_file` + `setup` with double cluster-info skip gate (PATTERNS.md "Shared: Cluster-info skip gate"). Mint kubeconfigs into `${TMPDIR:-/tmp}/karyon-tenants/bats-${BATS_RUN_TMPDIR##*/}/`. Skip per @test if kubeconfig file missing (graceful Wave 0 RED behavior — bats reports SKIP with explanatory message, NOT cryptic mint failure).
    - Static bats: setup() defines `DOC` var only; no cluster gate.
    - `@test` description format: `"<REQ-ID> [/ <decision-tag>]: <human assertion>"` — e.g., `"DEMO-04 (alpha boundary): tenant-owner cannot create secret in own namespace"`.
    - Comment-filtered grep where bats own comments might match (runbook-05-static-charlie.bats — comments mention `charlie`): use `bash -c "grep -v '^[[:space:]]*#' '$DOC' | grep -F '...'"` per PATTERNS.md "Shared: Comment-filtered grep".
    - Forbidden assertion idiom (Pitfall 11-P3): `echo "$output" | grep -qE 'Forbidden|forbidden|cannot create resource|cannot list'`.
    - For DEMO-04 + DEMO-06, ALSO assert the verbatim RESEARCH Q6 substrings (literally as shown in `<interfaces>`).

    === DEMO live bats (6 files) ===

    1. **`tests/bats/demo-01-live-platform-owner-list.bats`** — analog: rbac-08 (PATTERNS lines 28-86). Use platform-owner-only `setup_file` (mint PO_KUBECONFIG only). `@test`s:
       - `"DEMO-01 (Tier-2 platform-owner): kubectl get tenants returns alpha + bravo"` — `kubectl --kubeconfig="$PO_KUBECONFIG" get tenants.capsule.clastix.io -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}'`; assert status 0 and output contains both `alpha` and `bravo` via `grep -qx`.
       - `"DEMO-01 (Tier-2 platform-owner): kubectl get namespaces sees tenant-alpha + tenant-bravo"` — same shape probing namespaces.
       - `"DEMO-01 (Tier-2 platform-owner): kubectl get pods -A across tenant namespaces succeeds"` — `kubectl --kubeconfig="$PO_KUBECONFIG" get pods --all-namespaces`; assert status 0 + output contains `hello-world`.

    2. **`tests/bats/demo-02-live-tenant-owner-scoped-list.bats`** — analog: rbac-05 + negative-rbac-01 (PATTERNS lines 89-124). Use dual-kubeconfig `setup_file` minting ALPHA_KUBECONFIG + BRAVO_KUBECONFIG via `issue-tenant-kubeconfig.sh <alpha|bravo> human-tenant-owner`. `@test`s:
       - `"DEMO-02 (alpha scoped LIST): tenant-owner via proxy:30443 sees only own tenant ns"` — verbatim PATTERNS lines 112-120 (scoped-LIST regex walks output, rejects any non-`tenant-alpha|alpha-` ns).
       - `"DEMO-02 (bravo scoped LIST): tenant-owner via proxy:30443 sees only own tenant ns"` — symmetric mirror swapping ALPHA→BRAVO + regex `^(tenant-bravo|bravo-)`.
       - `"DEMO-02 (alpha scoped LIST): kubectl get tenants returns only alpha"` — `kubectl --kubeconfig="$ALPHA_KUBECONFIG" get tenants -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}'`; assert output contains `alpha` and does NOT contain `bravo` (`! echo "$output" | grep -qx bravo`).
       - `"DEMO-02 (bravo scoped LIST): kubectl get tenants returns only bravo"` — symmetric mirror.

    3. **`tests/bats/demo-03-live-pod-ops.bats`** — analog: PATTERNS lines 127-149 + RESEARCH Q3/Q4 verbatim probes. Use 3-kubeconfig `setup_file` (PO + ALPHA + BRAVO; needed for both tenant-owner own-ns probes AND platform-owner cross-tenant probes per DEMO-03 contract). `@test`s:
       - `"DEMO-03 (tenant-owner alpha exec): kubectl exec hello-world -- sh -c 'echo ok-from-shell'"` — verbatim PATTERNS lines 135-140; assert status 0 + `grep -qF "ok-from-shell"`.
       - `"DEMO-03 (tenant-owner bravo exec): kubectl exec hello-world -- sh -c 'echo ok-from-shell'"` — symmetric mirror against `BRAVO_KUBECONFIG` / `tenant-bravo`.
       - `"DEMO-03 (tenant-owner alpha logs): kubectl logs hello-world --tail=3 contains 'worker process'"` — verbatim PATTERNS lines 142-145.
       - `"DEMO-03 (tenant-owner bravo logs)"` — symmetric mirror.
       - `"DEMO-03 (platform-owner cross-tenant exec into tenant-alpha): succeeds (RBAC-06 co-owner)"` — `kubectl --kubeconfig="$PO_KUBECONFIG" -n tenant-alpha exec hello-world -- sh -c "echo ok-from-shell"`; assert status 0.
       - `"DEMO-03 (platform-owner cross-tenant logs against tenant-bravo): succeeds"` — symmetric mirror via PO into bravo.

    4. **`tests/bats/demo-04-live-tenant-secret-forbidden.bats`** — analog: rbac-06 lines 44-53 + RESEARCH Q6 (PATTERNS lines 153-173). Use dual-kubeconfig `setup_file` (ALPHA + BRAVO). `@test`s:
       - `"DEMO-04 (alpha boundary): tenant-owner cannot create secret in own namespace"` — verbatim PATTERNS lines 159-170, including the Pitfall 11-P3 permissive grep + the verbatim `human-tenant-owner` sub-grep from RESEARCH Q6 + idempotent cleanup `kubectl delete secret demo-deny -n tenant-alpha --ignore-not-found`.
       - `"DEMO-04 (bravo boundary): tenant-owner cannot create secret in own namespace"` — symmetric mirror against `BRAVO_KUBECONFIG` / `tenant-bravo`.

    5. **`tests/bats/demo-05-live-cross-tenant-forbidden.bats`** — analog: negative-rbac-01 N1/N2/N3 + RESEARCH (PATTERNS lines 177-194). Use dual-kubeconfig `setup_file` (ALPHA + BRAVO). `@test`s:
       - `"DEMO-05 (alpha→bravo): cross-tenant pod LIST denied"` — verbatim PATTERNS lines 183-187.
       - `"DEMO-05 (bravo→alpha): cross-tenant pod LIST denied (symmetric mirror)"` — verbatim PATTERNS lines 189-193.
       - `"DEMO-05 (alpha→bravo): cross-tenant namespace GET denied"` — `kubectl --kubeconfig="$ALPHA_KUBECONFIG" get namespace tenant-bravo`; assert status non-zero + permissive Forbidden grep.
       - `"DEMO-05 (bravo→alpha): cross-tenant namespace GET denied (symmetric mirror)"`.

    6. **`tests/bats/demo-06-live-platform-owner-cluster-admin-denials.bats`** — analog: rbac-08 + RESEARCH Q6 verbatim CRB string (PATTERNS lines 198-228). Use platform-owner-only `setup_file` (PO_KUBECONFIG only). `@test`s:
       - `"DEMO-06 (Tier-2 ceiling): platform-owner cannot create ClusterRoleBinding (headline)"` — verbatim PATTERNS lines 208-215, including the verbatim RESEARCH Q6 grep `grep -qF "clusterrolebindings.rbac.authorization.k8s.io is forbidden"` + `grep -qF "system:serviceaccount:capsule-system:platform-owner"`.
       - `"DEMO-06 (Tier-2 ceiling): platform-owner cannot read nodes -o yaml"` — verbatim PATTERNS lines 217-220.
       - `"DEMO-06 (Tier-2 ceiling): platform-owner cannot list certificatesigningrequests"` — verbatim PATTERNS lines 223-226.

    === RUNBOOK static bats (5 files) ===

    7. **`tests/bats/runbook-01-static-acts.bats`** — analog: docs-05-runbook lines 14-37 (PATTERNS lines 232-279). `setup` defines `DOC="${REPO_ROOT}/docs/poc-capsule-demo.md"`. `@test`s verbatim PATTERNS lines 244-278:
       - File exists; h1 line in head -20.
       - `status: Active` frontmatter (case-insensitive grep).
       - Strict-ordered 6 acts: `## Act 1 — ` through `## Act 6 — ` — line numbers strictly ascending (verbatim strict-order loop PATTERNS lines 256-272).
       - `### Expected output` count ≥ 6.

    8. **`tests/bats/runbook-02-static-checklist.bats`** — analog: seed-01-static + PATTERNS lines 287-329. Set `DOC` to demo runbook. `@test`s:
       - `## Pre-demo checklist` section exists (case-insensitive `^## Pre-demo checklist`).
       - `task fix-dns-poc-capsule` literal present.
       - `issue-platform-owner-kubeconfig.sh` literal present.
       - Both `issue-tenant-kubeconfig.sh alpha human-tenant-owner` + `issue-tenant-kubeconfig.sh bravo human-tenant-owner` literals present (loop).
       - `delete tenant charlie --ignore-not-found` literal present (D-14-03 idempotency).

    9. **`tests/bats/runbook-03-static-tls-noise.bats`** — analog: seed-01-static + RESEARCH Q2 (PATTERNS lines 338-371). Set `DOC` to demo runbook. `@test`s:
       - Verbatim `tls: bad certificate` literal (grep -F).
       - `ADR-008` or `adr/0008` cross-link (case-insensitive grep -iE).
       - `## Known noise` section line number > `## Act 6 —` line number (PATTERNS lines 357-364 verbatim — appears AFTER act 6 per D-14-01).
       - `capsule-controller-manager` literal present.

    10. **`tests/bats/runbook-03-static-eks-three-tier.bats`** — analog: docs-06-eks (PATTERNS lines 380-411). Set `DOC="${REPO_ROOT}/docs/capsule-on-eks.md"` (different target file — this asserts the Plan 14-01 EKS-doc append). `@test`s verbatim PATTERNS lines 387-411:
       - File exists.
       - `^## Three-tier permission model` section exists (case-insensitive).
       - All three labels `Tier 1`, `Tier 2`, `Tier 3` present (literal loop).
       - All three carriers `IRSA`, `tenant-workload-editor`, `capsule-platform-owner` present (literal loop).

    11. **`tests/bats/runbook-05-static-charlie.bats`** — analog: rbac-01-static comment-filter idiom (PATTERNS lines 421-456). Set `DOC` to demo runbook. `@test`s verbatim PATTERNS lines 428-456:
       - `charlie` between Act 3 and Act 4 line numbers (`sed -n "${act3_line},${act4_line}p"` then `grep -F charlie`) — comment-filter idiom NOT required here because we're slicing by line range, not whole-file grep.
       - `charlie` after Act 6 line number (`tail -n +${act6_line}` then `grep -F charlie`).
       - `new-tenant.sh charlie` literal present (D-14-03 verbatim provisioning command).
       - `tenant-charlie` namespace literal present (RESEARCH Q5).

    After writing all 11 files, run the suite to confirm Wave 0 RED behavior:
    ```bash
    bats tests/bats/demo-*.bats tests/bats/runbook-*.bats --duration 2>&1 | tail -40
    ```
    Expected outcome per Nyquist gate:
    - All 5 `runbook-*-static-*.bats` files FAIL (RED) because `docs/poc-capsule-demo.md` does not yet exist + `docs/capsule-on-eks.md` lacks the three-tier section.
    - All 6 `demo-*-live-*.bats` files run but most `@test`s SKIP (kubeconfigs mint OK; assertions run; they may PASS opportunistically against the live Phase 13 inheritance if the cluster is reachable AND charlie has not yet been provisioned — that's fine, Wave 0 contract is "RED bats _land_ before runbook lands", not "every assertion must fail"). The runbook-*-static-* RED is the load-bearing Wave 0 falsifier.

    Commit the 11 bats files in a single commit: `test(14-00): Wave 0 RED bats scaffold for DEMO-01..06 + RUNBOOK-01/02/03/05 (D-14-08 Nyquist gate)`.
  </action>
  <verify>
    <automated>test -f tests/bats/demo-01-live-platform-owner-list.bats && test -f tests/bats/demo-02-live-tenant-owner-scoped-list.bats && test -f tests/bats/demo-03-live-pod-ops.bats && test -f tests/bats/demo-04-live-tenant-secret-forbidden.bats && test -f tests/bats/demo-05-live-cross-tenant-forbidden.bats && test -f tests/bats/demo-06-live-platform-owner-cluster-admin-denials.bats && test -f tests/bats/runbook-01-static-acts.bats && test -f tests/bats/runbook-02-static-checklist.bats && test -f tests/bats/runbook-03-static-tls-noise.bats && test -f tests/bats/runbook-03-static-eks-three-tier.bats && test -f tests/bats/runbook-05-static-charlie.bats && grep -qF "RED until" tests/bats/demo-01-live-platform-owner-list.bats && grep -qF "RED until" tests/bats/runbook-01-static-acts.bats && grep -qF "clusterrolebindings.rbac.authorization.k8s.io is forbidden" tests/bats/demo-06-live-platform-owner-cluster-admin-denials.bats && grep -qF "tls: bad certificate" tests/bats/runbook-03-static-tls-noise.bats && grep -qF "new-tenant.sh charlie" tests/bats/runbook-05-static-charlie.bats && bats tests/bats/runbook-01-static-acts.bats; [ $? -ne 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - All 11 files exist at the listed paths
    - Every file has the Phase 14 / Wave 0 header comment block with load-bearing `RED until ...` line
    - Every file `load 'test_helper'` (single-quoted, no `../` prefix)
    - Live bats files (6) use `setup_file` with `KARYON_BATS_TMPDIR="${TMPDIR:-/tmp}/karyon-tenants/bats-${BATS_RUN_TMPDIR##*/}"` + matching `teardown_file rm -rf`
    - Live bats files include both `setup_file` AND `setup` cluster-info skip gates
    - `demo-06` file contains verbatim RESEARCH Q6 substring `clusterrolebindings.rbac.authorization.k8s.io is forbidden`
    - `demo-04` file contains verbatim RESEARCH Q6 substring `human-tenant-owner`
    - `runbook-03-static-tls-noise.bats` contains verbatim `tls: bad certificate`
    - `runbook-03-static-eks-three-tier.bats` targets `docs/capsule-on-eks.md` (NOT the demo runbook — this asserts the Plan 14-01 EKS-doc append)
    - `runbook-05-static-charlie.bats` contains `new-tenant.sh charlie` literal + `tenant-charlie` namespace literal
    - `bats tests/bats/runbook-01-static-acts.bats` exits non-zero (RED — doc does not yet exist)
    - `bats tests/bats/runbook-05-static-charlie.bats` exits non-zero (RED)
    - `bats tests/bats/runbook-03-static-tls-noise.bats` exits non-zero (RED)
    - `bats tests/bats/runbook-03-static-eks-three-tier.bats` exits non-zero (RED — docs/capsule-on-eks.md lacks Tier 1/2/3 labels per RESEARCH Q1)
    - Single git commit with subject `test(14-00): Wave 0 RED bats scaffold for DEMO-01..06 + RUNBOOK-01/02/03/05`
  </acceptance_criteria>
  <done>11 NEW bats files landed RED. Falsifier matrix locked. Wave 1 (runbook draft) cannot land without first turning runbook-* static bats GREEN; Wave 2 (DEMO live bats GREEN) cannot land without the same scaffold. Nyquist gate held.</done>
</task>

</tasks>

<verification>
- Both tasks complete in order: preflight passes BEFORE bats land (preflight is the inheritance contract for the bats to even be writable against the right primitives)
- `bats tests/bats/demo-*.bats tests/bats/runbook-*.bats --duration` runs without parse errors (every file is syntactically valid bats)
- `runbook-*` static bats all RED (5/5)
- `demo-*` live bats files all exist and are runnable (assertions may opportunistically pass; the load-bearing Wave 0 falsifier is the static-runbook RED contract)
- `/tmp/14-00-preflight.log` exists with all 10 probe outputs
- Single commit lands all 11 bats files
- No edits to any non-bats file (Phase 14 Wave 0 is bats-only; docs land in Wave 1)
</verification>

<success_criteria>
- 11 new bats files at the documented paths, every file conforming to the canonical header + setup pattern from PATTERNS.md
- Preflight log confirms Phase 13 inheritance still live: ClusterRoles, GTR, capsule-proxy NodePort, Tenant co-ownership, Forbidden CRB string
- RED-bats-first Nyquist gate held — runbook-* bats fail because target docs don't exist yet
- D-14-08 honored: 11 new files (not extensions of Phase 13 rbac-*); per-REQ traceability via filename
- D-14-02 honored: `charlie` literal embedded in runbook-05-static grep targets
- D-14-01 honored: `tls: bad certificate` verbatim literal embedded in runbook-03-static-tls grep targets
- RESEARCH Q6 verbatim Forbidden strings (no invented strings) embedded in demo-04 + demo-06 grep targets
</success_criteria>

<output>
After completion, create `.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-00-SUMMARY.md` capturing:
- Preflight log evidence (probe-by-probe pass/fail)
- 11 bats files committed (paths + commit SHA)
- Wave 0 RED status snapshot (which bats currently fail and why — load-bearing for Wave 1 plan to chase GREEN)
- Phase 13 inheritance baseline citation
</output>
