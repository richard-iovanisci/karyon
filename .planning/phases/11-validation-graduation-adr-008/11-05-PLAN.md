---
phase: 11
plan: 05
type: execute
wave: 4
depends_on: ["11-04"]
files_modified:
  - docs/adr/0008-capsule-multi-tenancy-graduation.md
  - docs/capsule-on-eks.md
  - tests/bats/docs-adr-template.bats
autonomous: false
requirements: [VAL-06]

must_haves:
  truths:
    - "docs/adr/0008-capsule-multi-tenancy-graduation.md exists with Status: Accepted (<outcome>) per Nygard 4-section template + 3 sub-sections in Decision (What we proved / What we did not prove / Trade-offs)"
    - "ADR-008 outcome (adopt | defer | reject | replaced by ADR-009) is mechanically derived from 11-VERIFICATION.md's GREEN/RED counts via D-11-14 evidence-bound rubric"
    - "ADR-008 cross-references docs/capsule-on-eks.md and Plans 11-04 11-VERIFICATION.md for evidence trail"
    - "docs/capsule-on-eks.md frontmatter Status: Reviewed (was: Draft) + reviewed/reviewed_against/reviewed_by frontmatter keys added"
    - "docs/capsule-on-eks.md has 4 targeted revision blocks per D-11-15: cert source, IRSA, proxy LIST scope, push-gate translation"
    - "tests/bats/docs-adr-template.bats ADRS array extended to include 0008; the 4 Nygard sections + Status: Accepted lint passes for ADR-008"
  artifacts:
    - path: "docs/adr/0008-capsule-multi-tenancy-graduation.md"
      provides: "v0.19 graduation ADR (D-11-14 evidence-bound outcome)"
      contains: "## Decision"
      min_lines: 80
    - path: "docs/capsule-on-eks.md"
      provides: "Rolled-forward EKSDOC-01 with bats-evidence revisions (D-11-15)"
      contains: "Status: Reviewed"
    - path: "tests/bats/docs-adr-template.bats"
      provides: "Lint extended for 6 ADRs (0001-0005 + 0008); ADR-008 keyword test added"
      contains: "0008-capsule-multi-tenancy-graduation.md"
  key_links:
    - from: "docs/adr/0008-capsule-multi-tenancy-graduation.md ## Decision"
      to: "11-VERIFICATION.md VAL-01..05 GREEN counts + carryover state"
      via: "D-11-14 evidence-bound rubric mechanical mapping"
      pattern: "VAL-"
    - from: "docs/capsule-on-eks.md frontmatter"
      to: "Status: Reviewed + reviewed_against: ADR-008"
      via: "D-11-15 frontmatter rollforward"
      pattern: "reviewed_against"
    - from: "tests/bats/docs-adr-template.bats ADRS array"
      to: "0008-capsule-multi-tenancy-graduation.md (NEW entry)"
      via: "Array literal extension at lines 11-17"
      pattern: "0008-"
---

<objective>
Write the v0.19 closing graduation ADR-008 per D-11-14 evidence-bound rubric (consuming 11-VERIFICATION.md's GREEN counts) and roll EKSDOC-01 forward from Status: Draft to Status: Reviewed per D-11-15 (4 targeted revision blocks). Extend the existing docs-adr-template.bats lint to cover 0008. This is the final plan of milestone v0.19.

Purpose: VAL-06 is the milestone-closing acceptance criterion. The ADR-008 outcome ∈ {adopt, defer, reject, replaced by ADR-009} gates Phase 12 (capsule-addon-fluxcd Trial — OPTIONAL); the EKSDOC rollforward bridges the POC evidence to the EKS prod-translation team-presentation artifact. Both files committed = milestone closes.

Output: 1 new ADR file + 1 EKSDOC rollforward + 1 lint extension; v0.19 milestone closes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-validation-graduation-adr-008/11-CONTEXT.md
@.planning/phases/11-validation-graduation-adr-008/11-RESEARCH.md
@.planning/phases/11-validation-graduation-adr-008/11-PATTERNS.md
@.planning/phases/11-validation-graduation-adr-008/11-VERIFICATION.md
@docs/adr/0004-hub-only-flux-control-plane.md
@docs/capsule-on-eks.md
@tests/bats/docs-adr-template.bats

<interfaces>
<!-- ADR-008 structure (Nygard 4-section + 3 sub-sections in Decision) per D-11-14. -->
<!-- The lint at tests/bats/docs-adr-template.bats lines 26-43 enforces 4 sections in order. -->

**IMPORTANT — embedded frontmatter escape:** YAML frontmatter delimiters in the template snippets below appear as ` ---` (single leading space). This is purely an escape so the planner's frontmatter validator does not pick up the embedded snippet. When you write the actual files (the EKSDOC frontmatter and the ADR-008 file), **strip the leading space so the lines become `---`** at column 0 (standard markdown frontmatter delimiters).

ADR template (verified by docs-adr-template.bats):
- Line 1: `# 8. Capsule Multi-Tenancy POC Graduation`
- Within first 10 lines: `Accepted` literal (line 49 of lint)
- 4 sections in this order (line 26-43 of lint):
  - `## Status`
  - `## Context`
  - `## Decision`
  - `## Consequences`

D-11-14 rubric table (CONTEXT.md lines 286-294 verbatim):
| Outcome | Required evidence |
|---------|-------------------|
| ADOPT | All 12/12 N1-N12 + VAL-02..05 + zero open BLOCKER carryovers |
| DEFER | ≥1 of: workaround documented OR upstream chart issue OR Phase 12 addon trial would clarify |
| REJECT | ≥1 of: tenant isolation breached OR VAL-04 SLO regressed OR webhook recovery non-deterministic |
| REPLACED BY ADR-009 | Mid-validation discovery of fundamentally different POC |

D-11-15 EKSDOC-01 frontmatter rollforward (verbatim from CONTEXT.md specifics lines 561-569):
```yaml
 ---
Status: Reviewed
audience: Mixed Engineering + Cloud Ops
purpose: Team-presentation primer for the Karyon v0.19 Capsule POC translated to an EKS target.
source-of-truth: .planning/research/SUMMARY.md (topology), .planning/research/FEATURES.md (CRDs), .planning/research/STACK.md (chart pins).
reviewed: 2026-MM-DD
reviewed_against: ADR-008 (Capsule Multi-Tenancy POC Graduation)
reviewed_by: karyon milestone owner
 ---
```

D-11-15 4 targeted revision blocks (CONTEXT.md lines 326-330):
1. Cert source section: Pitfall 10-P1 finding (capsule-proxy chart's tls.crt-only certgen Job; EKS needs cert-manager or ALB-side TLS)
2. IRSA section: Phase 10 D-10-01 TokenRequest portability vs --service-account-max-token-expiration clamping (k3s does not clamp; EKS clamps at 24h default)
3. Proxy LIST scope: Phase 11 N1-N9 evidence on which kinds capsule-proxy filters (namespace LIST yes; cluster-scoped resources via upstream RBAC not proxy)
4. Push-gate translation: Phase 11 D-11-01..04 patterns as EKS analogues (Argo CD post-render kustomization patches; ServerSideApply for cluster-admin-bypass-webhook semantics)

Existing lint extension shape (tests/bats/docs-adr-template.bats):
- ADRS array at lines 11-17: append `"0008-capsule-multi-tenancy-graduation.md"` as 6th entry
- Add new @test asserting ADR-008 contains keywords: `Capsule`, `tenant`, `graduation`, `ADR-004` (cross-reference)
</interfaces>

<canonical_decisions>
<!-- D-11-14 + D-11-15 LOCKED. The outcome decision is mechanical from 11-VERIFICATION.md. -->
<!-- Operator confirms the outcome via Task 1 checkpoint (only 'replaced by ADR-009' is fully discretionary). -->
</canonical_decisions>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: CHECKPOINT -- Operator confirms ADR-008 outcome from 11-VERIFICATION.md evidence</name>
  <files>(no files modified by this checkpoint -- operator decision feeds Task 2 ADR write)</files>
  <action>HALT execution and present the D-11-14 evidence-bound rubric below to the operator. Read 11-VERIFICATION.md ## Carryover to Plan 11-05 section first. The agent presents the 4 outcome options + the rubric mapping; the operator selects one. Operator's selection is consumed verbatim by Task 2 (ADR-008 outcome line).</action>
  <decision>
    Which graduation outcome does the Phase 11 bats evidence support per the D-11-14 evidence-bound rubric?
  </decision>
  <context>
    Plan 11-04's 11-VERIFICATION.md records the GREEN/RED counts for VAL-01..05. The D-11-14 rubric maps these mechanically to one of four outcomes. Three of the four are evidence-deterministic; the fourth ("REPLACED BY ADR-009") requires operator judgment about a competing POC.
    
    Read .planning/phases/11-validation-graduation-adr-008/11-VERIFICATION.md `## Carryover to Plan 11-05` section. Apply the rubric:
  </context>
  <options>
    <option id="adopt">
      <name>ADOPT</name>
      <pros>All 12/12 N1-N12 PASS; VAL-02..05 PASS; zero open BLOCKERS. Capsule graduates to v0.20 default platform; Phase 12 capsule-addon-fluxcd Trial unlocked as adoption acceleration; spoke-capsule promoted from POC to v1 spoke.</pros>
      <cons>Commits karyon to Capsule's specific failure modes (Pitfall 11-P1 chart-level RBAC patch as workaround; Pitfall 11-P2 cluster-admin webhook bypass assumption; Pitfall 11-P3 quota error string permissive grep). All accepted as POC-to-prod trade-offs in the Trade-offs sub-section.</cons>
    </option>
    <option id="defer">
      <name>DEFER</name>
      <pros>≥1 documented workaround (e.g., D-11-01 PostBuild patch is a workaround pending upstream chart fix) OR Phase 12 capsule-addon-fluxcd Trial would clarify the architectural shape. v0.20 reruns Phase 11 with the addon pattern. Capsule + addon could be the canonical Phase 12+ pattern.</pros>
      <cons>Phase 12 still runs (gated on adopt OR defer); spoke-capsule stays POC-isolated; v0.20 starts with additional unfinished work.</cons>
    </option>
    <option id="reject">
      <name>REJECT</name>
      <pros>Evidence-driven: ≥1 of N1-N12 RED for tenant-isolation reason (security invariant breached) OR VAL-04 SLO regressed (>230s) OR webhook recovery non-deterministic. spoke-capsule torn down as part of v0.19 close; multi-tenancy strategy returns to design phase. Phase 12 SKIPPED (gate condition not met).</pros>
      <cons>v0.19 milestone effort largely informational only; alternative POC (vCluster, HNC, Crossplane) becomes v0.20+ research.</cons>
    </option>
    <option id="replaced-by-adr-009">
      <name>REPLACED BY ADR-009</name>
      <pros>Mid-validation discovery: a fundamentally different POC (Hierarchical Namespaces / vCluster / Crossplane multi-tenant / kcp) is identified that supersedes Capsule's value proposition. ADR-008 records the comparison + points forward to ADR-009 (which Plan 11-05 will NOT write — it's a forward pointer for v0.20).</pros>
      <cons>Discretionary judgment; requires operator naming of the replacement POC. ADR-008 still ships as a recorded comparison (not a no-op).</cons>
    </option>
  </options>
  <verify>Operator selected one of the 4 outcomes; selection logged for Task 2 ADR write.</verify>
  <done>Operator typed one of: adopt | defer | reject | replaced-by-adr-009 (with replacement POC name); Task 2 reads the selection and writes ADR-008 with the chosen outcome.</done>
  <resume-signal>Select: adopt | defer | reject | replaced-by-adr-009 (with replacement POC name)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write docs/adr/0008-capsule-multi-tenancy-graduation.md per chosen outcome</name>
  <files>docs/adr/0008-capsule-multi-tenancy-graduation.md</files>
  <read_first>
    - docs/adr/0004-hub-only-flux-control-plane.md (canonical structural analog — long Decision, layered Consequences with Easier/Harder/No-change blocks)
    - docs/adr/0001-single-wsl2-shared-docker-network.md head (verify the `# N. Title` heading style + Status pattern)
    - .planning/phases/11-validation-graduation-adr-008/11-VERIFICATION.md (FRESH — read AFTER Plan 11-04 wrote it; the GREEN counts are the evidence)
    - .planning/phases/11-validation-graduation-adr-008/11-CONTEXT.md (D-11-14 lines 286-294 — outcome rubric; D-11-15 lines 326-330 — EKSDOC rollforward)
    - .planning/phases/11-validation-graduation-adr-008/11-PATTERNS.md ("docs/adr/0008-capsule-multi-tenancy-graduation.md" section lines 700-826 — verbatim shape)
    - .planning/phases/11-validation-graduation-adr-008/11-RESEARCH.md (lines 720-806 — verbatim ADR-008 template)
    - tests/bats/docs-adr-template.bats (the lint shape ADR-008 must satisfy)
  </read_first>
  <tdd>true</tdd>
  <behavior>
    - File starts with `# 8. Capsule Multi-Tenancy POC Graduation`
    - 4 Nygard sections in this order: `## Status`, `## Context`, `## Decision`, `## Consequences`
    - `## Status` block contains `Accepted (YYYY-MM-DD)` + `Outcome: <ADOPT|DEFER|REJECT|REPLACED BY ADR-009>` line
    - `Accepted` literal appears in the first 10 lines (lint requirement)
    - `## Decision` has 3 sub-sections: `### What we proved`, `### What we did not prove`, `### Trade-offs`
    - `## Decision` includes the 4-row D-11-14 rubric table with the chosen row marked Y, others marked N
    - `## Consequences` has conditional outcome blocks (If ADOPT / If DEFER / If REJECT / If REPLACED BY ADR-009) — only the chosen branch needs full elaboration; others can be brief
    - File ends with cross-reference to docs/capsule-on-eks.md (D-11-15 forward pointer)
    - File contains keywords: `Capsule`, `tenant`, `graduation`, `ADR-004` (lint requirement Task 4 will add)
  </behavior>
  <action>
Use the Write tool to create `docs/adr/0008-capsule-multi-tenancy-graduation.md`. Replace `<OUTCOME>` with the operator-chosen value from Task 1 (ADOPT / DEFER / REJECT / REPLACED BY ADR-009) in the indicated places. Replace `<YYYY-MM-DD>` with today's date. Replace `<X>/12` with the actual N count from 11-VERIFICATION.md.

For the rubric table, mark the chosen row's `<Y/N>` cell as `Y` and the other 3 as `N`. The "What we proved" sub-section pulls VAL-01..05 GREEN counts from 11-VERIFICATION.md verbatim.

```markdown
# 8. Capsule Multi-Tenancy POC Graduation

## Status

Accepted (<YYYY-MM-DD>)

Outcome: <ADOPT | DEFER | REJECT | REPLACED BY ADR-009 -- replacement POC: <name>>

## Context

Milestone v0.19 (Capsule Multi-Tenancy POC) ran Phases 7-11 to validate
[Capsule](https://capsule.clastix.io/) v0.12.4 as a karyon-managed multi-tenancy
pattern on the dedicated isolated POC spoke (`spoke-capsule`), without polluting
the default v1 topology or `task rebuild` path.

Five mandatory phases shipped:

- **Phase 7** -- POC seam (`# KARYON POC MOUNT` sentinel + `scripts/poc/capsule/`
  isolation per P31; `spoke-capsule` k3d cluster on shared `k8s-net`; EKSDOC-01
  rough-cut design + `.gitleaks.toml` + `.gitignore` leak defenses).
- **Phase 8** -- Capsule operator + capsule-proxy install via Flux HelmRelease +
  OCIRepository (split-path D-08-12 architectural seam: hub-targeted outer K
  for HelmRelease + OCIRepository CRs; spoke-targeted outer K for the rendered
  spoke-side resources via `spec.kubeConfig`).
- **Phase 9** -- Tenant CRs (alpha, bravo) + multi-tenancy lockdown patches on
  hub-flux Flux controllers (`--no-cross-namespace-refs`, `--no-remote-bases`,
  `--default-service-account=default`); P27 defense in tenant inner Kustomizations.
- **Phase 10** -- Tenant kubeconfig issuance via Kubernetes TokenRequest API +
  capsule-proxy LIST filter validation (Pitfall 10-P3 corrected qualitative falsifier).
- **Phase 11** -- Negative RBAC bats suite (N1-N12) + webhook failure mode
  (`task fail-capsule-webhook -- 0|1`) + clean teardown (`task destroy-poc -- capsule
  [--full]`) + `task rebuild` SLO regression test + first push gitleaks history scan +
  this graduation ADR.

This ADR records the v0.19 graduation decision based on Phase 11's empirical evidence
(see `.planning/phases/11-validation-graduation-adr-008/11-VERIFICATION.md`).

## Decision

The graduation outcome is **<OUTCOME>** per the D-11-14 evidence-bound rubric:

| Outcome | Required evidence | Matched? |
|---------|-------------------|----------|
| **ADOPT** | All 12/12 N1-N12 PASS + VAL-02 webhook recovery PASS + VAL-03 clean teardown PASS (no stuck namespaces post-90s) + VAL-04 < 230s + CID unchanged + zero gitleaks findings + ZERO open BLOCKER carryovers (all D-08-13 push-gate items resolved) | <Y or N> |
| **DEFER** | >= 1 of: N1-N12 has a RED-by-design (workaround documented, not bug) OR >= 1 unresolved upstream chart issue (e.g., D-11-01 PostBuild patch is a workaround) OR Phase 12 capsule-addon-fluxcd Trial would clarify the architectural shape | <Y or N> |
| **REJECT** | >= 1 of: N1-N12 RED for a tenant-isolation reason (silent escape -- security invariant breached) OR VAL-04 SLO regressed > 230s (POC pollutes v1 SLO) OR webhook recovery non-deterministic (VAL-02 flakes more than 2/3 runs) | <Y or N> |
| **REPLACED BY ADR-009** | Mid-validation discovery: a fundamentally different POC supersedes Capsule's value proposition for karyon's lab use-case | <Y or N> |

### What we proved

Per `.planning/phases/11-validation-graduation-adr-008/11-VERIFICATION.md`:

- **VAL-01 (Negative RBAC):** <X>/12 N1-N12 falsifiers PASS (cross-tenant pod LIST denied;
  cluster-wide LIST filtered to own tenant; cross-tenant secret read denied; ClusterRoleBinding
  escalation denied; namespace prefix violation denied; tenant quota violation denied;
  registry violation denied; direct apiserver bypass on `:6446` returns 403 + 'cannot list
  resource'; cross-tenant `sourceRef` denied; missing `serviceAccountName` falls through to
  no-permission default SA; webhook-down rejects workload create; recovery succeeds).
- **VAL-02 (Webhook failure recovery):** `task fail-capsule-webhook -- 0` rejects tenant
  pod create within 5s; `task fail-capsule-webhook -- 1` recovers within 90s rollout-status
  timeout; both halves bats-asserted via N11 + N12.
- **VAL-03 (Clean teardown):** `task destroy-poc -- capsule` performs the 5-step canonical
  sequence (suspend tenant inner Ks + spoke-targeted outer K -> delete Tenant CRs -> poll
  namespace cascade with auto force-delete PVC finalizers after 60s -> suspend operator + config
  outer Ks -> optional `--full` cluster delete). Post-teardown: zero `capsule.clastix.io/tenant`
  labeled namespaces remain; `# KARYON POC MOUNT` sentinel + `../pocs` mount line stay verbatim
  in `clusters/hub-flux/flux-system/kustomization.yaml`.
- **VAL-04 (SLO regression):** `task rebuild` completes in <X>s (budget: 230s);
  `k3d-spoke-capsule-server-0` container ID unchanged across rebuild; `task health-check`
  exits 0; Phase 7 P31 lint inheritance (zero `spoke-capsule` mentions in v0.18 scripts) PASSES.
- **VAL-05 (Gitleaks history scan):** First push to origin/main triggered CI gitleaks-scan
  (fetch-depth: 0 full history) and post-push `gitleaks git --log-opts="--all"` -- both
  returned 0 findings. D-11-04 `.planning/*.md` allowlist effective.

### What we did not prove

- **HA Capsule** -- single-replica only on spoke-capsule; multi-replica + leader election
  semantics not validated.
- **OIDC integration** -- TokenRequest API + bearer tokens only; no external IDP (Dex, Keycloak,
  Auth0) tested.
- **Multi-spoke federation** -- Capsule has no native cross-cluster Tenant; spoke-capsule is
  isolated, not part of the hub-flux spoke topology.
- **Hub-side Capsule** -- POC kept on isolated spoke per ADR-004; hub-side install (control-plane
  self-tenancy) is a different threat model + a separate ADR.
- **Real ingress / TLS for capsule-proxy** -- NodePort 30443 + self-signed cert (chart-managed
  controller-less certgen Job); no ingress-nginx, cert-manager, or AWS Certificate Manager
  validated.
- **Production secret management** -- TokenRequest API with `--duration=1h` default; no Vault,
  SOPS, or age integration.
- **GPU + Capsule interaction** -- spoke-capsule has no GPU; spoke-ml is the GPU-bearing spoke
  but not Capsule-managed.
- **Capsule on Kind / minikube / k3s-on-bare-metal** -- POC validates k3d only.

### Trade-offs

- **TokenRequest semantics on k3s vs IRSA on EKS** -- k3s does NOT clamp at 24h
  (Phase 10 D-10-02 reframe; verified live up to 720h). EKS clamps at 24h default
  via `--service-account-max-token-expiration`. Portability requires forking the
  duration logic for prod.
- **Chart-level RBAC patch (D-11-01)** is a workaround pending upstream
  capsule-proxy chart fix -- the chart-rendered Role lacks `secrets create`
  permission needed by the bundled `kube-webhook-certgen` Job; karyon extends it
  via Flux PostBuild patches (`clusters/hub-flux/pocs/capsule.yaml` spec.patches).
- **CapsuleConfiguration userGroups list (D-11-02)** is opinionated -- chart
  default is `[capsule.clastix.io]` only; karyon adds `system:serviceaccounts`
  + `system:authenticated` to make capsule-proxy LIST recognize SA-token-authenticated
  identities as Capsule-owned.
- **PVC strand auto force-delete after 60s (D-11-11)** is acceptable for POC
  iteration -- production teardown would need a `--preserve-data` flag.
- **Pitfall 11-P2 (cluster-admin webhook bypass)** -- `pocs/capsule/spoke/tenants/{alpha,bravo}/namespace.yaml`
  carry `capsule.clastix.io/tenant=<name>` labels; if the Capsule webhook rejects the
  cluster-admin Flux apply (system:masters bypass NOT in effect for the flux-reconciler SA),
  the operator-runbook fallback (`kubectl --as=alpha create namespace tenant-alpha`) applies.
  Empirical observation in 11-VERIFICATION.md.

## Consequences

**Outcome-conditional.** Karyon proceeds per the chosen graduation outcome:

### If ADOPT:

- v0.20 incorporates Capsule into the default `task rebuild` chain (P31 lifted; spoke-capsule
  promoted from POC to v1 spoke).
- `task rebuild` SLO budget updated for the additional capsule-system + tenant namespaces.
- Phase 12 (capsule-addon-fluxcd v0.2.3 Trial) runs as adoption acceleration; D-09-03
  forward-pointer (tenant inner K kubeConfig source swap from `spoke-capsule-kubeconfig`
  to per-tenant proxy-kubeconfigs) absorbed by Phase 12.
- ADR-008 references in `docs/capsule-on-eks.md` (EKSDOC-01) become canonical translation
  guidance.

### If DEFER:

- v0.20 reruns Phase 11 with the capsule-addon-fluxcd pattern.
- spoke-capsule stays on persistent isolated cluster; not promoted to v1.
- Phase 12 (capsule-addon-fluxcd Trial) runs as a gated re-evaluation pass.

### If REJECT:

- spoke-capsule torn down as part of v0.19 close (`task destroy-poc -- capsule --full`).
- No v0.20 Capsule carryover; multi-tenancy strategy returns to the design phase.
- Phase 12 is SKIPPED (gate condition not met).
- `docs/capsule-on-eks.md` (EKSDOC-01) annotated as superseded.

### If REPLACED BY ADR-009:

- ADR-009 is named with the replacement POC and the comparison evidence is preserved here
  for historical record.
- This POC's artifacts archived under `.planning/milestones/v0.19-archive/`.
- v0.20+ work resumes against the replacement architecture.

### Cross-references

- `docs/adr/0004-hub-only-flux-control-plane.md` -- the load-bearing v0.18 ADR this POC inherits
  (NO Flux runs on `spoke-capsule`; hub-only reconciliation via `spec.kubeConfig`).
- `docs/capsule-on-eks.md` (EKSDOC-01) -- Status: Reviewed (rolled forward by Plan 11-05);
  contains 4 targeted revision blocks informed by Phase 11 bats evidence (cert source, IRSA,
  proxy LIST scope, push-gate translation).
- `.planning/phases/11-validation-graduation-adr-008/11-VERIFICATION.md` -- the audit trail
  feeding the D-11-14 rubric.
```

Use the Write tool. ALL `<placeholder>` values must be replaced with operator-chosen values + actual VERIFICATION.md numbers (do NOT commit a template).
  </action>
  <verify>
    <automated>set -e
cd /home/rich/code/gsd/karyon
ADR=docs/adr/0008-capsule-multi-tenancy-graduation.md
[ -f "$ADR" ]
# Title literal
head -1 "$ADR" | grep -qF '# 8. Capsule Multi-Tenancy POC Graduation'
# Accepted appears in first 10 lines
head -n 10 "$ADR" | grep -qF 'Accepted'
# 4 Nygard sections in order
prev_line=0
for s in '## Status' '## Context' '## Decision' '## Consequences'; do
  line=$(grep -nF -- "$s" "$ADR" | head -1 | cut -d: -f1)
  [ -n "$line" ] || { echo "MISSING section: $s"; exit 1; }
  [ "$line" -gt "$prev_line" ] || { echo "OUT OF ORDER: $s at line $line, previous was $prev_line"; exit 1; }
  prev_line=$line
done
# 3 Decision sub-sections
grep -qF -- '### What we proved' "$ADR"
grep -qF -- '### What we did not prove' "$ADR"
grep -qF -- '### Trade-offs' "$ADR"
# Outcome line present (one of the 4 outcomes)
grep -qE 'Outcome: (ADOPT|DEFER|REJECT|REPLACED BY ADR-009)' "$ADR"
# 4-row rubric table present
grep -qF -- '**ADOPT**' "$ADR"
grep -qF -- '**DEFER**' "$ADR"
grep -qF -- '**REJECT**' "$ADR"
grep -qF -- '**REPLACED BY ADR-009**' "$ADR"
# Cross-refs present
grep -qF -- 'docs/capsule-on-eks.md' "$ADR"
grep -qF -- '0004-hub-only-flux-control-plane' "$ADR"
grep -qF -- '11-VERIFICATION.md' "$ADR"
# Required keywords for Task 4 lint
for kw in 'Capsule' 'tenant' 'graduation' 'ADR-004'; do
  grep -qF -- "$kw" "$ADR"
done
# No literal '<placeholder>' remains
if grep -qF -- '<X>' "$ADR"; then echo "TEMPLATE PLACEHOLDER NOT REPLACED: <X>"; exit 1; fi
if grep -qF -- '<YYYY-MM-DD>' "$ADR"; then echo "TEMPLATE DATE NOT REPLACED"; exit 1; fi
if grep -qF -- '<OUTCOME>' "$ADR"; then echo "TEMPLATE OUTCOME NOT REPLACED"; exit 1; fi
if grep -qF -- '<Y or N>' "$ADR"; then echo "TEMPLATE RUBRIC NOT REPLACED"; exit 1; fi
echo "OK: ADR-008 lands with 4 Nygard sections + 3 Decision sub-sections + outcome chosen + no placeholders"</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 docs/adr/0008-capsule-multi-tenancy-graduation.md` returns `# 8. Capsule Multi-Tenancy POC Graduation`
    - `head -n 10 docs/adr/0008-capsule-multi-tenancy-graduation.md | grep -F 'Accepted'` exits 0
    - 4 Nygard sections (`## Status`, `## Context`, `## Decision`, `## Consequences`) appear in this order
    - 3 Decision sub-sections present (`### What we proved`, `### What we did not prove`, `### Trade-offs`)
    - Outcome line matches `^Outcome: (ADOPT|DEFER|REJECT|REPLACED BY ADR-009)`
    - 4-row D-11-14 rubric table present (all 4 outcome rows mentioned in bold)
    - Cross-references to docs/capsule-on-eks.md, 0004-hub-only-flux-control-plane, 11-VERIFICATION.md
    - Required keywords: `Capsule`, `tenant`, `graduation`, `ADR-004`
    - NO `<placeholder>` literals remain (all replaced with actuals)
  </acceptance_criteria>
  <done>ADR-008 written per D-11-14 outcome rubric + Nygard 4-section template + 3 Decision sub-sections; cross-references in place; no template placeholders remain</done>
</task>

<task type="auto">
  <name>Task 3: Roll docs/capsule-on-eks.md forward to Status: Reviewed with 4 D-11-15 revision blocks</name>
  <files>docs/capsule-on-eks.md</files>
  <read_first>
    - docs/capsule-on-eks.md (full file — read completely; identify the existing H2 sections for cert source / IRSA / proxy / push-gate; the 4 revision blocks insert into or supplement these sections)
    - .planning/phases/11-validation-graduation-adr-008/11-CONTEXT.md (D-11-15 lines 326-330; <specifics> lines 561-569 frontmatter)
    - .planning/phases/11-validation-graduation-adr-008/11-PATTERNS.md ("docs/capsule-on-eks.md" section lines 1121-1172)
    - .planning/phases/11-validation-graduation-adr-008/11-VERIFICATION.md (the bats evidence cited in the revision blocks)
    - docs/adr/0008-capsule-multi-tenancy-graduation.md (just-written; ADR-008 cross-reference must point here)
  </read_first>
  <tdd>true</tdd>
  <behavior>
    - Frontmatter `Status: Draft` becomes `Status: Reviewed`
    - Frontmatter gains 3 new keys: `reviewed: <YYYY-MM-DD>`, `reviewed_against: ADR-008 (Capsule Multi-Tenancy POC Graduation)`, `reviewed_by: karyon milestone owner`
    - 4 targeted revision blocks added (cert source, IRSA, proxy LIST scope, push-gate translation) — each as a `> **Phase 11 evidence (ADR-008 cross-reference):**` blockquote
    - Existing doc content preserved (the 4 revision blocks are insertions/appends, not rewrites)
    - File parses as markdown (no broken frontmatter)
  </behavior>
  <action>
**Step 1 — Update frontmatter:**

Use Edit. Replace the existing frontmatter (lines 1-7):
```yaml
 ---
Status: Draft
audience: Mixed Engineering + Cloud Ops
purpose: Team-presentation primer for the Karyon v0.19 Capsule POC translated to an EKS target.
source-of-truth: .planning/research/SUMMARY.md (topology), .planning/research/FEATURES.md (CRDs), .planning/research/STACK.md (chart pins).
status-rollover: Phase 11 graduation ADR-008 rolls Status from Draft to Reviewed with corrections informed by bats evidence.
 ---
```

with:
```yaml
 ---
Status: Reviewed
audience: Mixed Engineering + Cloud Ops
purpose: Team-presentation primer for the Karyon v0.19 Capsule POC translated to an EKS target.
source-of-truth: .planning/research/SUMMARY.md (topology), .planning/research/FEATURES.md (CRDs), .planning/research/STACK.md (chart pins).
reviewed: <YYYY-MM-DD>
reviewed_against: ADR-008 (Capsule Multi-Tenancy POC Graduation)
reviewed_by: karyon milestone owner
 ---
```

Replace `<YYYY-MM-DD>` with today's date.

**Step 2 — Update the inline status line (just under the heading):**

Find the line `> **Status:** Draft` (likely near line 11) and replace with `> **Status:** Reviewed (rolled forward by ADR-008 in Plan 11-05).`

**Step 3 — Append the 4 D-11-15 revision blocks:**

Locate the existing H2 sections for: (a) cert source / TLS, (b) IRSA / authentication, (c) proxy / LIST scope, (d) push-gate / bootstrap. If a section doesn't exist verbatim, add the revision block at the end of the closest matching section OR append at end-of-file under a new `## Phase 11 Evidence (ADR-008 cross-references)` H2.

Insert these 4 blockquote blocks (each in its corresponding section OR all under the new H2):

```markdown
> **Phase 11 evidence (ADR-008 cross-reference) -- Cert source:** capsule-proxy chart's
> `kube-webhook-certgen` Job emits `tls.crt` only; the chart self-signs the root with
> `tls.crt` doubling as CA; **NO `ca.crt` key is generated** (Pitfall 10-P1, verified
> empirically). EKS translation requires cert-manager (controller-managed) or AWS
> Certificate Manager (ALB-side termination); the controller-less certgen Job pattern
> doesn't translate cleanly to EKS without operational state-tracking.

> **Phase 11 evidence (ADR-008 cross-reference) -- IRSA:** The POC's TokenRequest pattern
> translates cleanly to IRSA on EKS (both are short-lived bearer tokens via apiserver-side
> lifecycle). The `--service-account-max-token-expiration` clamp behavior differs across
> distros: k3s does NOT clamp (Phase 10 D-10-02 verified live up to 720h); EKS clamps at
> 24h default. Production translation must adapt the duration logic.

> **Phase 11 evidence (ADR-008 cross-reference) -- Proxy LIST scope:** Negative RBAC bats
> N1-N9 empirically verified the proxy-edge LIST filter scope:
> - capsule-proxy filters: namespace LIST (verified by N1, N2, N3 with cross-tenant kubeconfigs).
> - Cluster-scoped resources (ClusterRole, ClusterRoleBinding, nodes) are filtered by
>   upstream RBAC NOT proxy (verified by N4 -- ClusterRoleBinding escalation denied at
>   apiserver auth, not at proxy edge).

> **Phase 11 evidence (ADR-008 cross-reference) -- Push-gate translation (EKS analogues
> for v0.19 push-gate fixes):**
> - **Flux PostBuild patch (D-11-01)** maps to Argo CD's post-render kustomization patches
>   (or `argocd-application-controller` JSON patches via `argocd.argoproj.io/sync-options`).
> - **CapsuleConfiguration repo-side update (D-11-02)** is portable as-is -- single
>   declarative source-of-truth.
> - **Tenant home namespace label (D-11-03)** requires Argo CD's
>   `argocd.argoproj.io/sync-options: ServerSideApply` to handle the cluster-admin-bypass-webhook
>   semantics (system:masters bypass is identical across distros -- verified Pitfall 11-P2).
```

Use the Edit tool for all changes (frontmatter + inline status line). Use Write only if the doc is largely empty or needs major restructuring (it should not be — it's a Phase 7 deliverable already). For the 4 revision blocks, use Edit to insert each in the closest matching section, OR Edit to append a new H2 at end-of-file containing all 4 blocks.
  </action>
  <verify>
    <automated>set -e
cd /home/rich/code/gsd/karyon
EKS=docs/capsule-on-eks.md
[ -f "$EKS" ]
# Frontmatter Status: Reviewed
yq eval --front-matter=extract '.Status' "$EKS" | grep -qx 'Reviewed'
# New frontmatter keys
yq eval --front-matter=extract '.reviewed' "$EKS" | grep -qE '^[0-9]{4}-[0-9]{2}-[0-9]{2}$'
yq eval --front-matter=extract '.reviewed_against' "$EKS" | grep -qF 'ADR-008'
yq eval --front-matter=extract '.reviewed_by' "$EKS" | grep -qF 'karyon milestone owner'
# 4 revision blocks present
grep -qF -- '**Phase 11 evidence (ADR-008 cross-reference) -- Cert source:**' "$EKS"
grep -qF -- '**Phase 11 evidence (ADR-008 cross-reference) -- IRSA:**' "$EKS"
grep -qF -- '**Phase 11 evidence (ADR-008 cross-reference) -- Proxy LIST scope:**' "$EKS"
grep -qF -- '**Phase 11 evidence (ADR-008 cross-reference) -- Push-gate translation' "$EKS"
# No literal placeholder
if grep -qF -- '<YYYY-MM-DD>' "$EKS"; then echo "DATE PLACEHOLDER NOT REPLACED"; exit 1; fi
# Existing keys preserved
yq eval --front-matter=extract '.audience' "$EKS" | grep -qF 'Mixed Engineering + Cloud Ops'
yq eval --front-matter=extract '.purpose' "$EKS" | grep -qF 'Team-presentation primer'
echo "OK: docs/capsule-on-eks.md rolled forward to Status: Reviewed with 4 D-11-15 revision blocks"</automated>
  </verify>
  <acceptance_criteria>
    - `yq eval --front-matter=extract '.Status' docs/capsule-on-eks.md` returns `Reviewed`
    - `yq eval --front-matter=extract '.reviewed' docs/capsule-on-eks.md` returns a YYYY-MM-DD date
    - `yq eval --front-matter=extract '.reviewed_against' docs/capsule-on-eks.md` contains `ADR-008`
    - `yq eval --front-matter=extract '.reviewed_by' docs/capsule-on-eks.md` returns `karyon milestone owner`
    - 4 revision blockquotes present (Cert source, IRSA, Proxy LIST scope, Push-gate translation), each marked `**Phase 11 evidence (ADR-008 cross-reference)**`
    - Existing frontmatter keys preserved (audience, purpose, source-of-truth)
    - No literal `<YYYY-MM-DD>` placeholder remains
  </acceptance_criteria>
  <done>EKSDOC-01 rolled forward from Status: Draft to Status: Reviewed; 4 D-11-15 revision blocks added with bats-evidence cross-references to ADR-008; existing content preserved</done>
</task>

<task type="auto">
  <name>Task 4: Extend tests/bats/docs-adr-template.bats lint to cover ADR-008</name>
  <files>tests/bats/docs-adr-template.bats</files>
  <read_first>
    - tests/bats/docs-adr-template.bats (full file — understand the ADRS array + the 3 generic @tests + the 5 per-ADR keyword @tests)
    - docs/adr/0008-capsule-multi-tenancy-graduation.md (the file the new lint covers)
    - .planning/phases/11-validation-graduation-adr-008/11-PATTERNS.md (lines 821-827 — note on existing lint compatibility)
  </read_first>
  <tdd>true</tdd>
  <behavior>
    - ADRS array gains `"0008-capsule-multi-tenancy-graduation.md"` as 6th entry
    - The 3 generic @tests (existence, 4 sections, Status: Accepted in first 10 lines) automatically apply to ADR-008 via the loop
    - A new @test asserts ADR-008 contains keywords: Capsule, tenant, graduation, ADR-004
    - All existing per-ADR @tests (DOCS-06..10) preserved verbatim
    - bats lint passes for all 6 ADRs (0001-0005 + 0008)
  </behavior>
  <action>
Edit `tests/bats/docs-adr-template.bats`. Make 2 changes:

**Change 1 — Extend the ADRS array (lines 11-17 of current file):**

Replace:
```bash
  ADRS=(
    "0001-single-wsl2-shared-docker-network.md"
    "0002-k3d-over-kind.md"
    "0003-flux-over-argocd.md"
    "0004-hub-only-flux-control-plane.md"
    "0005-kubernetes-version-pin.md"
  )
```

with:
```bash
  ADRS=(
    "0001-single-wsl2-shared-docker-network.md"
    "0002-k3d-over-kind.md"
    "0003-flux-over-argocd.md"
    "0004-hub-only-flux-control-plane.md"
    "0005-kubernetes-version-pin.md"
    "0008-capsule-multi-tenancy-graduation.md"
  )
```

**Change 2 — Append a new per-ADR keyword @test at end of file (after the existing DOCS-10 @test):**

Append:
```bash

@test "VAL-06 (D-11-14): ADR-008 captures Capsule graduation with ADR-004 cross-reference" {
  f="${ADR_DIR}/0008-capsule-multi-tenancy-graduation.md"
  [ -f "$f" ]
  for keyword in \
    'Capsule' \
    'tenant' \
    'graduation' \
    'ADR-004' \
    'D-11-14' ; do
    run grep -iF -- "$keyword" "$f"
    [ "$status" -eq 0 ]
  done
}
```

Use the Edit tool for both changes (2 separate Edit calls or 1 atomic).
  </action>
  <verify>
    <automated>set -e
cd /home/rich/code/gsd/karyon
LINT=tests/bats/docs-adr-template.bats
[ -f "$LINT" ]
# ADR-008 entry in ADRS array
grep -qF -- '0008-capsule-multi-tenancy-graduation.md' "$LINT"
# New per-ADR keyword @test
grep -qF -- 'VAL-06 (D-11-14)' "$LINT"
# Existing entries preserved
grep -qF -- '0001-single-wsl2-shared-docker-network.md' "$LINT"
grep -qF -- '0005-kubernetes-version-pin.md' "$LINT"
grep -qF -- 'DOCS-06' "$LINT"
grep -qF -- 'DOCS-10' "$LINT"
# bats lint passes for all 6 ADRs
bats tests/bats/docs-adr-template.bats
echo "OK: docs-adr-template.bats extended for ADR-008; all 6 ADR lints pass"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF '0008-capsule-multi-tenancy-graduation.md' tests/bats/docs-adr-template.bats` exits 0
    - `grep -qF 'VAL-06 (D-11-14)' tests/bats/docs-adr-template.bats` exits 0
    - All 5 existing ADR entries preserved (0001..0005)
    - All 5 existing per-ADR keyword tests (DOCS-06..10) preserved
    - `bats tests/bats/docs-adr-template.bats` exits 0 (the 3 generic @tests + 5 per-ADR @tests + 1 new ADR-008 @test all pass)
  </acceptance_criteria>
  <done>docs-adr-template.bats extended to cover ADR-008; the 3 generic Nygard @tests automatically apply via the ADRS loop; new VAL-06 keyword @test passes; all existing ADRs still pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADR-008 outcome decision → milestone close | The chosen outcome gates Phase 12; operator confirms via Task 1 checkpoint |
| docs-adr-template.bats lint extension → 5 existing ADRs | New @test added; existing @tests unchanged (regression-safe) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | R (Repudiation) | ADR-008 outcome subjective discretion | mitigate | D-11-14 rubric is mechanical for ADOPT/DEFER/REJECT; only REPLACED BY ADR-009 is discretionary. Operator confirms outcome via Task 1 checkpoint. |
| T-11-05-02 | I (Information Disclosure) | EKSDOC-01 rollforward references VERIFICATION.md GREEN counts | accept | Counts are aggregate metrics (no PII, no secrets); intentional cross-reference for prod-translation evidence trail |
</threat_model>

<verification>
- ADR-008 written with 4 Nygard sections + 3 Decision sub-sections + chosen outcome
- ADR-008 cross-references docs/capsule-on-eks.md, ADR-004, 11-VERIFICATION.md
- EKSDOC-01 rolled forward to Status: Reviewed with 4 D-11-15 revision blocks
- docs-adr-template.bats extended for ADR-008; all 6 ADR lints pass
- Milestone v0.19 closes (per ADR-008 outcome): Phase 12 unlocked OR skipped
</verification>

<success_criteria>
- [x] docs/adr/0008-capsule-multi-tenancy-graduation.md exists with Nygard 4-section + 3 sub-sections
- [x] ADR-008 outcome chosen via Task 1 checkpoint and mechanically derived from 11-VERIFICATION.md
- [x] ADR-008 contains keywords: Capsule, tenant, graduation, ADR-004, D-11-14
- [x] docs/capsule-on-eks.md frontmatter Status: Reviewed + reviewed/reviewed_against/reviewed_by keys
- [x] EKSDOC-01 has 4 targeted revision blocks (cert source, IRSA, proxy LIST scope, push-gate translation)
- [x] tests/bats/docs-adr-template.bats ADRS array extended; new @test for ADR-008
- [x] `bats tests/bats/docs-adr-template.bats` exits 0
- [x] No template placeholders remain in any of the 3 modified files
</success_criteria>

<output>
After completion, create `.planning/phases/11-validation-graduation-adr-008/11-05-SUMMARY.md` documenting:
- ADR-008 outcome chosen (with operator-confirmed reasoning)
- 11-VERIFICATION.md evidence cited (per-VAL GREEN counts)
- EKSDOC-01 frontmatter rollforward + 4 revision blocks added
- docs-adr-template.bats lint extension (1 new ADR entry + 1 new @test)
- All 6 ADR lints pass (0001-0005 + 0008)
- Milestone v0.19 status: closed (per ADR-008 outcome — Phase 12 OPTIONAL gate state)
</output>