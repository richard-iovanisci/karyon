---
phase: 11
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]  # 11-00 transitive via 11-01 + 11-02 (info_only checker note)
files_modified:
  - pocs/capsule/spoke/tenants/alpha/tenant.yaml
  - pocs/capsule/spoke/tenants/bravo/tenant.yaml
autonomous: true
requirements: [VAL-01, VAL-02]

must_haves:
  truths:
    - "alpha/tenant.yaml has spec.namespaceOptions.quota: 3 + spec.containerRegistries.allowed: [registry.example.io] (Capsule v1beta2 schema verified)"
    - "bravo/tenant.yaml mirrors alpha's quota + registry fixtures (symmetric tenants)"
    - "Live Tenant CRs on spoke-capsule reflect the new fixture state (kubectl reconcile via apply or Flux post-Plan-11-04 push)"
    - "All 5 negative-rbac-*.bats files run live (when cluster reachable) and turn GREEN: N1, N2, N3, N4, N5, N6 (with permissive grep), N7, N8, N9, N10, N11, N12"
    - "Anti-pattern lint stays GREEN (no `kubectl auth can-i` introduced anywhere)"
  artifacts:
    - path: "pocs/capsule/spoke/tenants/alpha/tenant.yaml"
      provides: "Tenant CR alpha extended with namespaceOptions.quota + containerRegistries (N6/N7 fixture)"
      contains: "namespaceOptions:"
    - path: "pocs/capsule/spoke/tenants/bravo/tenant.yaml"
      provides: "Tenant CR bravo extended with namespaceOptions.quota + containerRegistries (N6/N7 fixture; symmetric)"
      contains: "namespaceOptions:"
  key_links:
    - from: "alpha/tenant.yaml spec.namespaceOptions.quota"
      to: "negative-rbac-03-webhook-policy.bats N6 quota probe"
      via: "Capsule webhook quota enforcement on namespace count"
      pattern: "namespaceOptions"
    - from: "alpha/tenant.yaml spec.containerRegistries.allowed"
      to: "negative-rbac-03-webhook-policy.bats N7 registry probe"
      via: "Capsule webhook container-registry allowlist"
      pattern: "containerRegistries"
    - from: "Live Tenant CR (apply or Flux reconcile)"
      to: "Capsule webhooks enforcing N5/N6/N7"
      via: "Tenant spec → Capsule controller → ValidatingWebhookConfiguration"
      pattern: "tenant alpha"
---

<objective>
Land the N6 quota fixture + N7 container-registry fixture on both Tenant CRs (alpha, bravo). After the fixtures are applied to the live spoke-capsule cluster (via `kubectl apply`), run the full 5 negative-rbac-*.bats suite live and turn N1-N12 GREEN. Plan 11-04 owns the GitOps push event; this plan stages the live state via direct apply so the bats can run BEFORE first push.

Purpose: VAL-01 + VAL-02 require all 12 negative falsifiers GREEN. The Tenant CR fixture extensions (Claude's Discretion per CONTEXT.md) gate N6 (quota) and N7 (registry); without them N6 fails because no quota is enforced and N7 fails because all registries are allowed by default. The fixtures are minimal: 2 lines under spec for each Tenant.

Output: 2 Tenant CR YAMLs edited (mirror change); live Tenant CR state reflects new fixtures; 5 negative-rbac bats files GREEN (12 @tests across the partition).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-validation-graduation-adr-008/11-CONTEXT.md
@.planning/phases/11-validation-graduation-adr-008/11-RESEARCH.md
@.planning/phases/11-validation-graduation-adr-008/11-PATTERNS.md
@.planning/phases/11-validation-graduation-adr-008/11-VALIDATION.md
@pocs/capsule/spoke/tenants/alpha/tenant.yaml
@pocs/capsule/spoke/tenants/bravo/tenant.yaml
@scripts/poc/capsule/issue-tenant-kubeconfig.sh

<interfaces>
<!-- Capsule v1beta2 Tenant CR schema (verified by RESEARCH.md against pkg.go.dev). -->

Capsule v1beta2 Tenant.spec extension fields:
- `spec.namespaceOptions.quota: *int32` — namespace count quota (kubebuilder:Minimum=1)
- `spec.containerRegistries.allowed: []string` — allowed-registry list (string match)
- `spec.containerRegistries.allowedRegex: string` — regex variant (alternative to allowed)

Both fields are documented in:
- https://pkg.go.dev/github.com/projectcapsule/capsule/api/v1beta2 (verified 2026-05-05)
- https://projectcapsule.dev/docs/tenants/enforcement/ (verified 2026-05-05)

Existing tenant.yaml fields (PRESERVE):
- `spec.forceTenantPrefix: true` (top-level)
- `spec.owners: [...]` (SA + User multi-owner)
- `spec.resourceQuotas.items: [...]` (CPU/memory/pods)
- `spec.limitRanges.items: [...]` (Container default + defaultRequest)

Phase 9 TEN-02 namespace state (existing):
- tenant-alpha (home, created via Plan 9 namespace.yaml)
- alpha-app1 (created via Plan 9 TEN-02 self-serve impersonation)
- tenant-bravo (home)
- bravo-app1 (created via Plan 9 TEN-02)

So pre-N6 namespace count for alpha = 2 (tenant-alpha + alpha-app1). With quota:3, N6 creates alpha-app2 (3rd, OK) then alpha-app3 (4th, REJECTED).
</interfaces>

<canonical_decisions>
<!-- Claude's Discretion (CONTEXT.md lines 361-365) — fixtures recommended defaults: -->
<!--   quota: 3 (one home + two app namespaces fits Phase 9 + leaves N6 room) -->
<!--   containerRegistries.allowed: [registry.example.io] -->
<!-- These are NOT locked decisions; planner records the choice in this PLAN.md (already made). -->

<!-- Live state staging: per Plan 11-04 ordering, the imperative `helm uninstall capsule-proxy` -->
<!-- + first push happen IN PLAN 11-04. This Plan 11-03 stages live state via direct kubectl apply -->
<!-- so the bats run can validate N1-N12 GREEN BEFORE first push. Post-push (Plan 11-04 verifier), -->
<!-- the live state will reconcile from the same YAML (Flux apply is idempotent — same spec, same result). -->
</canonical_decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend alpha + bravo tenant.yaml with namespaceOptions.quota + containerRegistries fixtures, then apply live</name>
  <files>pocs/capsule/spoke/tenants/alpha/tenant.yaml, pocs/capsule/spoke/tenants/bravo/tenant.yaml</files>
  <read_first>
    - pocs/capsule/spoke/tenants/alpha/tenant.yaml (full current state — 58 lines; understand existing forceTenantPrefix + owners + resourceQuotas + limitRanges)
    - pocs/capsule/spoke/tenants/bravo/tenant.yaml (full current state — 58 lines; mirror)
    - .planning/phases/11-validation-graduation-adr-008/11-RESEARCH.md (lines 514-565 verified Capsule v1beta2 Tenant schema with namespaceOptions + containerRegistries)
    - .planning/phases/11-validation-graduation-adr-008/11-PATTERNS.md (lines 968-1048 — Tenant CR fixture extension pattern)
    - .planning/phases/11-validation-graduation-adr-008/11-CONTEXT.md (lines 361-362 — Claude's Discretion N6/N7 fixtures)
    - tests/bats/negative-rbac-03-webhook-policy.bats (the @tests these fixtures gate)
  </read_first>
  <tdd>true</tdd>
  <behavior>
    - alpha/tenant.yaml gains `spec.namespaceOptions.quota: 3` and `spec.containerRegistries.allowed: [registry.example.io]` with inline N6/N7 fixture comments
    - bravo/tenant.yaml mirrors the same additions
    - All existing fields preserved verbatim (forceTenantPrefix, owners, resourceQuotas, limitRanges, head comments)
    - Files parse cleanly via yq
    - Live cluster reflects the new spec after `kubectl --context=k3d-spoke-capsule apply -f` (idempotent — Capsule controller reconciles)
    - `kubectl get tenant alpha -o jsonpath='{.spec.namespaceOptions.quota}'` returns 3
    - `kubectl get tenant alpha -o jsonpath='{.spec.containerRegistries.allowed[0]}'` returns `registry.example.io`
  </behavior>
  <action>
**Step 1 — Edit `pocs/capsule/spoke/tenants/alpha/tenant.yaml`:**

Use Edit. Replace the closing comment block (lines 52-57 of current file):
```yaml
  # NOTE: namespaceOptions intentionally OMITTED — TEN-01 only requires
  # forceTenantPrefix (now top-level). namespaceOptions.quota (max namespace
  # count) is omitted; tenant can create unlimited namespaces inside prefix.
  # No additionalRoleBindings, storageClasses, ingressClasses, containerRegistries,
  # nodeSelector, networkPolicies — Capsule defaults to allow-all (per CONTEXT.md
  # Claude's Discretion).
```

with the fixture additions:
```yaml
  # N6 fixture (Phase 11 -- VAL-01 N6 quota falsifier; Claude's Discretion per CONTEXT.md):
  # tenant-alpha (home) + alpha-app1 (Phase 9 TEN-02) = 2; bats N6 creates alpha-app2 (3rd, OK)
  # then alpha-app3 (4th, REJECTED with Capsule webhook quota error per Pitfall 11-P3 permissive grep).
  # Schema: NamespaceOptions struct: Quota *int32 (kubebuilder:Minimum=1) -- verified RESEARCH.md.
  namespaceOptions:
    quota: 3
  # N7 fixture (Phase 11 -- VAL-01 N7 registry-violation falsifier; Claude's Discretion per CONTEXT.md):
  # bats N7 runs `kubectl run x --image=docker.io/library/nginx -n alpha-app1` and asserts
  # webhook rejects with registry-violation error (only registry.example.io is allowed).
  # Schema: AllowedListSpec struct: { allowed: []string, allowedRegex: string } -- verified RESEARCH.md.
  # NOTE: ContainerRegistries field is marked deprecated in v0.12.4 (use Enforcement.Registries)
  # but the deprecated form still works for POC purposes.
  containerRegistries:
    allowed:
      - registry.example.io
  # No additionalRoleBindings, storageClasses, ingressClasses, nodeSelector, networkPolicies --
  # Capsule defaults to allow-all (per CONTEXT.md Claude's Discretion).
```

**Step 2 — Edit `pocs/capsule/spoke/tenants/bravo/tenant.yaml`:**

Same edit, mirror for bravo (replace the trailing NOTE block with the fixture block, identical content — bravo gets `quota: 3` and `containerRegistries.allowed: [registry.example.io]` for symmetry).

**Step 3 — Apply both Tenant CRs to spoke-capsule:**

```bash
kubectl --context=k3d-spoke-capsule apply -f pocs/capsule/spoke/tenants/alpha/tenant.yaml
kubectl --context=k3d-spoke-capsule apply -f pocs/capsule/spoke/tenants/bravo/tenant.yaml
```

Wait ~5s for Capsule controller to reconcile (it materializes ResourceQuota into tenant namespaces).

Use Edit for steps 1+2; Bash for step 3.
  </action>
  <verify>
    <automated>set -e
cd /home/rich/code/gsd/karyon
# alpha/tenant.yaml
[ "$(yq eval '.spec.namespaceOptions.quota' pocs/capsule/spoke/tenants/alpha/tenant.yaml)" = "3" ]
[ "$(yq eval '.spec.containerRegistries.allowed | length' pocs/capsule/spoke/tenants/alpha/tenant.yaml)" = "1" ]
[ "$(yq eval '.spec.containerRegistries.allowed[0]' pocs/capsule/spoke/tenants/alpha/tenant.yaml)" = "registry.example.io" ]
# Existing fields preserved
[ "$(yq eval '.spec.forceTenantPrefix' pocs/capsule/spoke/tenants/alpha/tenant.yaml)" = "true" ]
[ "$(yq eval '.spec.owners | length' pocs/capsule/spoke/tenants/alpha/tenant.yaml)" = "2" ]
[ "$(yq eval '.spec.resourceQuotas.items | length' pocs/capsule/spoke/tenants/alpha/tenant.yaml)" = "1" ]
# bravo/tenant.yaml mirror
[ "$(yq eval '.spec.namespaceOptions.quota' pocs/capsule/spoke/tenants/bravo/tenant.yaml)" = "3" ]
[ "$(yq eval '.spec.containerRegistries.allowed[0]' pocs/capsule/spoke/tenants/bravo/tenant.yaml)" = "registry.example.io" ]
[ "$(yq eval '.spec.forceTenantPrefix' pocs/capsule/spoke/tenants/bravo/tenant.yaml)" = "true" ]
# Live cluster reflects new state (skip if cluster down)
if kubectl --context=k3d-spoke-capsule cluster-info >/dev/null 2>&1; then
  ALPHA_QUOTA=$(kubectl --context=k3d-spoke-capsule get tenant alpha -o jsonpath='{.spec.namespaceOptions.quota}' 2>/dev/null || echo "MISSING")
  [ "$ALPHA_QUOTA" = "3" ] || { echo "live alpha quota: $ALPHA_QUOTA (expected 3)"; exit 1; }
  ALPHA_REG=$(kubectl --context=k3d-spoke-capsule get tenant alpha -o jsonpath='{.spec.containerRegistries.allowed[0]}' 2>/dev/null || echo "MISSING")
  [ "$ALPHA_REG" = "registry.example.io" ] || { echo "live alpha registry: $ALPHA_REG"; exit 1; }
  BRAVO_QUOTA=$(kubectl --context=k3d-spoke-capsule get tenant bravo -o jsonpath='{.spec.namespaceOptions.quota}' 2>/dev/null || echo "MISSING")
  [ "$BRAVO_QUOTA" = "3" ] || { echo "live bravo quota: $BRAVO_QUOTA"; exit 1; }
fi
# anti-pattern lint stays GREEN
bats tests/bats/negative-rbac-anti-pattern-lint-static.bats
echo "OK: Tenant CRs extended with N6/N7 fixtures; live state synced (or cluster unavailable -- bats run in Task 2)"</automated>
  </verify>
  <acceptance_criteria>
    - `yq eval '.spec.namespaceOptions.quota' pocs/capsule/spoke/tenants/alpha/tenant.yaml` returns `3`
    - `yq eval '.spec.namespaceOptions.quota' pocs/capsule/spoke/tenants/bravo/tenant.yaml` returns `3`
    - `yq eval '.spec.containerRegistries.allowed[0]' pocs/capsule/spoke/tenants/alpha/tenant.yaml` returns `registry.example.io`
    - `yq eval '.spec.containerRegistries.allowed[0]' pocs/capsule/spoke/tenants/bravo/tenant.yaml` returns `registry.example.io`
    - `yq eval '.spec.forceTenantPrefix' pocs/capsule/spoke/tenants/alpha/tenant.yaml` returns `true` (existing field preserved)
    - `yq eval '.spec.owners | length' pocs/capsule/spoke/tenants/alpha/tenant.yaml` returns `2` (existing owners preserved)
    - `yq eval '.spec.resourceQuotas.items | length' pocs/capsule/spoke/tenants/alpha/tenant.yaml` returns `1` (existing resourceQuotas preserved)
    - When cluster reachable: `kubectl --context=k3d-spoke-capsule get tenant alpha -o jsonpath='{.spec.namespaceOptions.quota}'` returns `3`
    - When cluster reachable: `kubectl --context=k3d-spoke-capsule get tenant alpha -o jsonpath='{.spec.containerRegistries.allowed[0]}'` returns `registry.example.io`
    - `bats tests/bats/negative-rbac-anti-pattern-lint-static.bats` exits 0
  </acceptance_criteria>
  <done>Both tenant.yaml files extended with N6 quota + N7 registry fixtures; live Tenant CRs reflect the new spec (when cluster reachable); existing forceTenantPrefix + owners + resourceQuotas + limitRanges preserved</done>
</task>

<task type="auto">
  <name>Task 2: Run full 5 negative-rbac-*.bats live suite + record GREEN counts</name>
  <files>(no files modified — verification-only task; records outcome in next-task or SUMMARY commit)</files>
  <read_first>
    - tests/bats/negative-rbac-01-cross-tenant-list.bats
    - tests/bats/negative-rbac-02-escalation.bats
    - tests/bats/negative-rbac-03-webhook-policy.bats
    - tests/bats/negative-rbac-04-bypass-and-flux.bats
    - tests/bats/negative-rbac-05-webhook-down.bats
    - tests/bats/negative-rbac-anti-pattern-lint-static.bats
    - .planning/phases/11-validation-graduation-adr-008/11-CONTEXT.md (D-11-05 N1-N12 mapping; <decisions> section)
  </read_first>
  <behavior>
    - All 5 negative-rbac-*.bats files run live; each @test either PASSES or SKIPS (cluster down)
    - When cluster reachable, expected GREEN count is 12 (3+1+3+3+2 = 12 @tests across N1-N12)
    - Anti-pattern lint @test PASSES regardless (it's a static lint, not live)
    - If any @test RED, executor records the failure in 11-03-SUMMARY.md with the actual error string + recommended escalation (per Pitfall 11-P3 permissive grep adjustment, or Pitfall 11-P1 patch relocation if N7/N6 webhook errors mismatch)
  </behavior>
  <action>
**Step 1 — Pre-check live cluster state:**

```bash
kubectl --context=k3d-spoke-capsule cluster-info
```

If this fails, document in SUMMARY: live bats SKIPPED; full GREEN flip deferred to Plan 11-04 verifier. Do not attempt to bring up the cluster from this plan (Plan 11-04 owns live staging).

**Step 2 — Ensure capsule-controller-manager is Ready=1 (idempotent precondition):**

```bash
kubectl --context=k3d-spoke-capsule scale deploy capsule-controller-manager -n capsule-system --replicas=1 || true
kubectl --context=k3d-spoke-capsule rollout status deploy/capsule-controller-manager -n capsule-system --timeout=90s || true
```

**Step 3 — Run all 6 negative-rbac bats files (5 live + 1 static lint):**

```bash
bats tests/bats/negative-rbac-anti-pattern-lint-static.bats
bats tests/bats/negative-rbac-01-cross-tenant-list.bats
bats tests/bats/negative-rbac-02-escalation.bats
bats tests/bats/negative-rbac-03-webhook-policy.bats
bats tests/bats/negative-rbac-04-bypass-and-flux.bats
bats tests/bats/negative-rbac-05-webhook-down.bats
```

Capture exit codes + TAP output for each. The N11/N12 sequence in negative-rbac-05 invokes `task fail-capsule-webhook` which Plan 11-02 landed.

**Step 4 — Record outcomes:**

If all 12 @tests GREEN: proceed; Plan 11-04 verifier confirms post-push.

If N6 RED with `quota|namespace.*limit|exceeded` error not matched: this is Pitfall 11-P3 (the upstream Capsule v0.12.4 quota error string differs from the permissive grep). Record exact observed string in SUMMARY; the permissive grep already covers reasonable variants — if observed string is truly outside the regex, escalate by patching the bats grep to add the new variant.

If N7 RED: verify the live Tenant CR actually has containerRegistries.allowed (not silently rejected by Capsule webhook). If accepted but webhook doesn't fire, the chart may need a chart-side `enforcement.registries: true` flag — out of scope for Phase 11; document as Rule 3 deviation.

If N9/N10 RED with no Flux observable error: increase the polling budget or check that the Kustomization CR actually applied (cluster-admin apply, not via tenant kubeconfig).

If N11/N12 RED: verify `task fail-capsule-webhook -- 0` and `-- 1` work end-to-end via `task --list | grep fail-capsule-webhook` (Plan 11-02 must have landed Taskfile entries first).

If cluster reachable but bats RED for environmental reasons (Phase 8 G-04 BLOCKER inheritance, Capsule operator down, etc.), DO NOT block Plan 11-03 — record observation in SUMMARY and let Plan 11-04 verifier re-run after first-push reconciliation.
  </action>
  <verify>
    <automated>set -e
cd /home/rich/code/gsd/karyon
# Anti-pattern lint always passes (static)
bats tests/bats/negative-rbac-anti-pattern-lint-static.bats
# Live bats: skip if cluster down, otherwise run and capture results
if kubectl --context=k3d-spoke-capsule cluster-info >/dev/null 2>&1; then
  echo "Cluster reachable -- running live negative-rbac suite"
  # Each bats invocation; non-zero exit recorded but not necessarily fatal
  # (the executor records observations in SUMMARY; environmental failures fold into Plan 11-04 verifier)
  RC=0
  bats tests/bats/negative-rbac-01-cross-tenant-list.bats || RC=$?
  bats tests/bats/negative-rbac-02-escalation.bats || RC=$?
  bats tests/bats/negative-rbac-03-webhook-policy.bats || RC=$?
  bats tests/bats/negative-rbac-04-bypass-and-flux.bats || RC=$?
  bats tests/bats/negative-rbac-05-webhook-down.bats || RC=$?
  if [ "$RC" -ne 0 ]; then
    echo "WARN: at least one negative-rbac bats RED -- record in 11-03-SUMMARY.md and let Plan 11-04 verifier re-run post-push"
  else
    echo "OK: all negative-rbac bats GREEN locally"
  fi
else
  echo "Cluster NOT reachable -- live bats SKIPPED; Plan 11-04 verifier will run them after first-push reconcile"
fi</automated>
  </verify>
  <acceptance_criteria>
    - `bats tests/bats/negative-rbac-anti-pattern-lint-static.bats` exits 0 (always — static lint)
    - When cluster reachable: each negative-rbac-*.bats file runs to completion (no parse errors, no infinite loop)
    - When cluster reachable: ALL 12 N1-N12 @tests PASS locally (matches VAL-01 contract verbatim per ROADMAP success criterion #1). The N1-N12 negative-RBAC bats are independent of the push event — they exercise apiserver-level RBAC enforcement that does NOT depend on gitleaks scanning or post-push Flux reconcile. Negative-RBAC bats MUST be 12/12 GREEN locally BEFORE Plan 11-04 push.
    - `bats tests/bats/negative-rbac-{01..05}-*.bats` exits 0 across all 5 partition files
    - Total bats GREEN @test count = 12 of 12 across the 5 negative-rbac files (3+1+3+3+2 = 12)
    - 11-03-SUMMARY.md (created in next-task or commit) records the actual GREEN/RED count + any escalations needed
  </acceptance_criteria>
  <done>The 5 negative-rbac live bats files run; all 12 N1-N12 @tests GREEN locally (per VAL-01 contract = 12/12; matches ROADMAP success criterion #1). The negative-rbac bats are independent of the push event. If any @test RED, executor records the actual error string in SUMMARY for Plan 11-04 verifier follow-up but Plan 11-03 IS NOT considered complete until 12/12 GREEN locally; anti-pattern lint stays GREEN; no source files modified by this verification task</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tenant CR namespaceOptions.quota → Capsule webhook | Quota enforcement is webhook-side; bats N6 verifies via real CRUD probe (NOT SAR cache per D-11-05) |
| Tenant CR containerRegistries.allowed → Capsule webhook | Registry allowlist enforcement is webhook-side (admission control on Pod create) |
| Plan 11-03 direct kubectl apply → Live cluster state | Mutates live Tenant CR ahead of GitOps push; Plan 11-04 confirms Flux reconcile is idempotent (same spec from same source) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | T (Tampering) | Live kubectl apply pre-push | accept | Plan 11-04 push event reconciles from same YAML; idempotent (Flux apply with same spec is no-op); intentional staging per Plan 11-04 ordering |
| T-11-03-02 | E (Elevation of Privilege) | N4 ClusterRoleBinding evil probe (Plan 11-00 created the bats; Plan 11-03 runs it) | mitigate | Defensive cleanup in @test removes any accidentally-created CRB; the @test SHOULD fail to create it |
| T-11-03-03 | D (Denial of Service) | N11 webhook-down probe in negative-rbac-05 | mitigate | teardown_file() in negative-rbac-05.bats scales controller back to 1 + waits for rollout (Plan 11-00 design) |
</threat_model>

<verification>
- 2 Tenant CR YAMLs extended with N6 quota + N7 registry fixtures
- All existing fields preserved (forceTenantPrefix, owners, resourceQuotas, limitRanges, head comments)
- Live Tenant CRs (when cluster reachable) reflect the new spec
- Anti-pattern lint stays GREEN
- Live negative-rbac suite runs (12 @tests across 5 files); GREEN count recorded in SUMMARY
- Any RED outcomes recorded with observed error strings for Plan 11-04 follow-up
</verification>

<success_criteria>
- [x] alpha/tenant.yaml has spec.namespaceOptions.quota: 3 + spec.containerRegistries.allowed: [registry.example.io]
- [x] bravo/tenant.yaml mirror
- [x] All existing tenant.yaml fields preserved
- [x] Live cluster Tenant CR state reflects fixtures (when reachable)
- [x] Anti-pattern lint GREEN (no `kubectl auth can-i` introduced)
- [x] Negative-RBAC suite runs end-to-end; GREEN count = 12 of 12 across N1-N12 (matches VAL-01 contract per ROADMAP success criterion #1; failures block Plan 11-03 completion)
- [x] No source code modified beyond tenant.yaml files (the two scripts from Plan 11-02 + bats from Plan 11-00 + push-gate fixes from Plan 11-01 are the production surface; this plan is fixture + verification)
</success_criteria>

<output>
After completion, create `.planning/phases/11-validation-graduation-adr-008/11-03-SUMMARY.md` documenting:
- 2 tenant.yaml files edited (alpha + bravo) with N6/N7 fixtures
- Live `kubectl apply` outcome for both Tenant CRs
- Negative-RBAC bats GREEN/RED count per file (e.g., negative-rbac-01: 3/3 GREEN, negative-rbac-03: 3/3 GREEN, etc.)
- Any error strings observed (e.g., N6 quota error variant) + Pitfall 11-P3 / 11-P1 escalation flags for Plan 11-04
- Confirmation: anti-pattern lint stays GREEN; no `kubectl auth can-i` literal introduced
</output>