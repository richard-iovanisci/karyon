---
phase: 13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism
plan: 04
type: execute
wave: 4
depends_on:
  - "13-00"
  - "13-01"
  - "13-02"
  - "13-03"
files_modified:
  - .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VERIFICATION.md
  - .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VALIDATION.md
  - docs/poc-capsule.md
autonomous: true
requirements:
  - DELIVERY-01
  - DELIVERY-02
  - DELIVERY-03
tags:
  - capsule
  - verifier
  - delivery

must_haves:
  truths:
    - "Full Phase 13 bats suite (18 NEW + 6 inherited) runs to a documented disposition (GREEN or passed_with_overrides)"
    - "Phase 7 + 8 + 9 + 10 + 11 inheritance bats remain GREEN throughout Phase 13"
    - "DELIVERY-01 evidence: all 13 modified files live under `pocs/capsule/spoke/` or `scripts/poc/capsule/`; zero Taskfile.yml edits"
    - "DELIVERY-02 evidence: P31 static bats GREEN; SLO regression bats run (or documented as deferred per KARYON_REBUILD_APPROVED gate)"
    - "DELIVERY-03 evidence: git diff alpha + bravo Tenant CRs shows additive-only changes to spec.owners[]"
    - "Plan 13-04 commits: docs/poc-capsule.md appended with `## Platform-owner kubeconfig delivery contract` H2 section (1 paragraph forward-pointer; Phase 14 expands into runbook narrative)"
    - "`13-VERIFICATION.md` written with disposition + per-REQ evidence + carryover notes"
    - "`13-VALIDATION.md` frontmatter flipped: `status: final`, `nyquist_compliant: true`, `wave_0_complete: true`"
  artifacts:
    - path: ".planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VERIFICATION.md"
      provides: "Phase 13 verification ledger — per-REQ evidence + bats outcome matrix + carryover notes"
      contains: "passed"
      min_lines: 100
    - path: "docs/poc-capsule.md"
      provides: "EDITED — appended `## Platform-owner kubeconfig delivery contract` H2 section (D-13-09 + D-13-10 forward-pointer to Phase 14 runbook act 2)"
      contains: "Platform-owner kubeconfig"
    - path: ".planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VALIDATION.md"
      provides: "EDITED frontmatter — status: final, nyquist_compliant: true, wave_0_complete: true"
      contains: "nyquist_compliant: true"
  key_links:
    - from: ".planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VERIFICATION.md"
      to: "All Phase 13 plans (13-00..13-03) + their commits"
      via: "Per-REQ evidence rows citing bats file names + commit SHAs + live kubectl output snippets"
      pattern: "RBAC-01..06 + SEED-01..03 + DELIVERY-01..03 each have a row"
    - from: "docs/poc-capsule.md `## Platform-owner kubeconfig delivery contract` H2"
      to: "Phase 14 docs/poc-capsule-demo.md runbook act 2"
      via: "Forward-pointer in concluding sentence"
      pattern: "See Phase 14 docs/poc-capsule-demo.md runbook act 2"
---

<objective>
Plan 13-04 — Verifier + docs forward-pointer + VALIDATION.md sign-off.

Purpose: Rerun the full Phase 13 bats suite (18 NEW + 6 inherited Phase 7/8/9/10/11 regression gates) + verify all 12 REQs (RBAC-01..06 + SEED-01..03 + DELIVERY-01..03) against live evidence. Write `13-VERIFICATION.md` documenting the per-REQ evidence + bats matrix + carryover items. Append a 1-paragraph "Platform-owner kubeconfig delivery contract" H2 subsection to `docs/poc-capsule.md` (forward-pointer to Phase 14 runbook act 2; Phase 14 expands this into the full demo narrative). Flip VALIDATION.md frontmatter to `status: final, nyquist_compliant: true, wave_0_complete: true`.

Output: 1 NEW VERIFICATION.md + 1 EDITED docs/poc-capsule.md + 1 EDITED VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-CONTEXT.md
@.planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VALIDATION.md
@.planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-00-SUMMARY.md
@.planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-01-SUMMARY.md
@.planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-02-SUMMARY.md
@.planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-03-SUMMARY.md

# Analogs (REQUIRED reading)
@.planning/milestones/v0.19-phases/11-validation-graduation-adr-008/11-VERIFICATION.md
@.planning/milestones/v0.19-phases/10-tenant-owner-kubeconfigs-capsule-proxy-round-trip/10-VERIFICATION.md
@docs/poc-capsule.md

<interfaces>
<!-- Verification ledger conventions inherited from Phase 10 + Phase 11 -->

## 13-VERIFICATION.md structure (mirror Phase 10/11 close-out shape):
```markdown
---
phase: 13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism
status: passed | passed_with_overrides | failed
verifier: 13-04
verified: 2026-MM-DD
---

# Phase 13 — Verification Ledger

## Disposition

(passed / passed_with_overrides / failed — with rationale)

## REQ Coverage

| REQ ID | Status | Evidence (bats file + commit SHA + live observation) |
|--------|--------|-------------------------------------------------------|
| RBAC-01 | yes | bats rbac-01-static + rbac-05-live; commit SHA; kubectl auth can-i matrix passed |
| RBAC-02 | yes | bats rbac-06-live; commit SHA; auth can-i '*' '*' returns no; create secret Forbidden |
| RBAC-03 | yes | bats rbac-07-live; commit SHA; flux-reconciler + gitops-reconciler entries preserved verbatim |
| RBAC-04 | yes | bats rbac-08-live; commit SHA; platform-owner auth can-i '*' '*' returns no; nodes/csr Forbidden |
| RBAC-05 | yes | bats rbac-10-live; commit SHA; new-tenant.sh phase13-fresh creates/gets/patches/deletes |
| RBAC-06 | yes | bats rbac-09-live; commit SHA; LIST tenants + namespaces through proxy returns alpha + bravo |
| SEED-01 | yes | bats seed-02-live; commit SHA; 4 namespaces x 2 resources = 8 objects materialized with managed-by label |
| SEED-02 | yes | bats seed-03-live; commit SHA; phase13-fresh hello-world Pod Ready in N s (<=60s) |
| SEED-03 | yes | bats seed-04-live; commit SHA; zero Deployment/RS/CM materialized by GTR |
| DELIVERY-01 | yes | bats delivery-01-static + delivery-02-static; all new files under pocs/capsule/spoke/ or scripts/poc/capsule/ |
| DELIVERY-02 | yes or PARTIAL | bats poc-isolation-01-static GREEN; SLO regression bats delivery-05 either GREEN or deferred per KARYON_REBUILD_APPROVED gate |
| DELIVERY-03 | yes | bats delivery-03-live + delivery-04-live; git diff additive-only on Tenant CRs; all 3 Flux Ks Ready=True |

## Bats Matrix

| Category | GREEN | RED | SKIP | Total |
|----------|-------|-----|------|-------|
| Phase 13 NEW static | N | 0 | 0 | N |
| Phase 13 NEW live | N | 0 | 0 | N |
| Phase 7+8+9+10+11 inherited | N | 0 | 0 | N |
| Total | N | 0 | 0 | N |

## Inheritance Verification

- Phase 7 P31 isolation: bats tests/bats/poc-isolation-01-static.bats GREEN
- Phase 8 ADR-004 (no Flux on spoke-capsule): bats tests/bats/capsule-install-09-live-adr-004.bats GREEN
- Phase 9 P27 tenant Kustomization SA defense: bats tests/bats/p27-tenant-kustomization-sa-static.bats GREEN
- Phase 9 P32 dependsOn chain: bats tests/bats/tenants-04-static-dependson.bats GREEN
- Phase 9 N7 registry-rejection fixture (different registry): bats tests/bats/negative-rbac-*.bats GREEN
- Phase 10 PROXY-01..03: bats tests/bats/proxy-04-live-issue.bats + sibling proxy-* GREEN
- Phase 11 D-11-02 CapsuleConfiguration userGroups: bats tests/bats/push-gate-02-static-capsuleconfig.bats GREEN
- Phase 11 D-11-03 tenant ns label: bats tests/bats/push-gate-03-static-tenant-ns-label.bats GREEN

## Carryover & Deferred Items

- (any KARYON_REBUILD_APPROVED-gated SLO test that was deferred)
- (any RoleBinding numbering observation from A2 that the verifier confirmed live)
- (any docs/poc-capsule-demo.md / Phase 14 forward-pointers)

## Notable Observations

(Pitfall 13-P7 RoleBinding numbering — actual live observation post-Phase 13-02)
(A2/A3 assumption-confirm results)
(Any deviations from D-13-* discretion notes; planner choices)
```

## docs/poc-capsule.md APPEND target — new H2 section (mirror existing H2 shape from lines 146-189):
```markdown
## Platform-owner kubeconfig delivery contract

> **Status:** Active. Phase 13 — RBAC-04..06.

The `scripts/poc/capsule/issue-platform-owner-kubeconfig.sh` script mints a short-lived,
platform-owner kubeconfig that routes through capsule-proxy NodePort 30443. The
identity is the `ServiceAccount/platform-owner` in the `capsule-system` namespace
(D-13-06), bound to the existing `capsule-platform-owner` ClusterRole via the dual-
subject ClusterRoleBinding.

### Platform-owner kubeconfig quickstart

    bash scripts/poc/capsule/issue-platform-owner-kubeconfig.sh 2>/dev/null > /tmp/po.kubeconfig
    KUBECONFIG=/tmp/po.kubeconfig kubectl get tenants
    # alpha
    # bravo

### Platform-owner kubeconfig contract

| Property | Value |
|---|---|
| Output channel default | stdout (pure YAML kubeconfig); `preflight-lib` log lines on stderr |
| Optional file output | `--write-to <path>` — `<path>` MUST resolve under `${TMPDIR:-/tmp}/karyon-tenants/` (validated via `realpath -m` prefix check) |
| Server URL | `https://127.0.0.1:30443` (capsule-proxy NodePort) |
| Authentication | Bound SA token via `kubectl create token platform-owner -n capsule-system` (TokenRequest API); default `--duration=1h` |
| TLS trust | `certificate-authority-data` embedded in cluster block (read live from `capsule-proxy` Secret on spoke-capsule via `tls.crt` jsonpath) |
| Default namespace in context | UNSET (platform-owner ops are cluster-scoped — Tenant CR LIST, namespaces LIST) |
| Issued context name | `platform-owner-via-proxy` |

### Mandatory invariants

- P29 leak defense — same `.gitignore` glob `karyon-tenants/` covers `platform-owner.kubeconfig` under tmpdir-root; `.gitleaks.toml` kubeconfig-bearer-token rule active.
- P31 isolation — script lives under `scripts/poc/capsule/`; zero new `spoke-capsule` mentions in v0.18 default-path scripts.

### Cross-references

- REQ: RBAC-04 (platform-owner ceiling — no cluster-admin); RBAC-05 (Tenant CR CRUD); RBAC-06 (co-owner LIST/EXEC across tenant namespaces)
- Decisions: D-13-06 (SA + dual CRB), D-13-09 (script), D-13-10 (proxy routing)
- Forward-pointer: Phase 14 `docs/poc-capsule-demo.md` runbook act 2 expands this into the full demo narrative.
- EKS-translation: `docs/capsule-on-eks.md` — in EKS, the Group `capsule-platform-owners` binds to an IRSA-backed IdP group identity.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full Phase 13 bats suite + inheritance regression sweep; capture results</name>
  <files>(no file output — bats execution + log capture)</files>
  <read_first>
    - .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-00-SUMMARY.md (Wave 0 RED log path)
    - .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-01-SUMMARY.md (Plan 13-01 RBAC GREEN bats + commit SHA)
    - .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-02-SUMMARY.md (Plan 13-02 platform-owner GREEN bats + commit SHA)
    - .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-03-SUMMARY.md (Plan 13-03 SEED GREEN bats + commit SHA + /tmp/13-03-seed-02.log fresh-tenant latency)
    - .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VALIDATION.md (per-task verification map — all 19 task rows + 6 inherited)
  </read_first>
  <action>
    Refresh platform-owner kubeconfig + run full bats sweep. Capture results to `/tmp/13-04-verifier.log` for VERIFICATION.md citation.

    ```bash
    # Refresh platform-owner kubeconfig for fresh tokens:
    bash scripts/poc/capsule/issue-platform-owner-kubeconfig.sh \
      --duration=2h --write-to "${TMPDIR:-/tmp}/karyon-tenants/platform-owner.kubeconfig" 2>&1 | tail -3

    # === Phase 13 NEW bats — full suite ===
    echo "=== PHASE 13 NEW BATS ===" | tee /tmp/13-04-verifier.log
    bats tests/bats/rbac-{01,02,03,04,05,06,07,08,09,10}-*.bats \
         tests/bats/seed-{01,02,03,04}-*.bats \
         tests/bats/delivery-{01,02,03,04,05}-*.bats -x 2>&1 | tee -a /tmp/13-04-verifier.log

    # === Inheritance regression sweep ===
    echo "=== INHERITANCE REGRESSION ===" | tee -a /tmp/13-04-verifier.log
    # Phase 7 — P31 isolation
    bats tests/bats/poc-isolation-01-static.bats -x 2>&1 | tee -a /tmp/13-04-verifier.log
    # Phase 7 — POC seam mount sentinel
    bats tests/bats/poc-mount-01-static.bats -x 2>&1 | tee -a /tmp/13-04-verifier.log
    # Phase 8 — ADR-004 (no Flux on spoke-capsule)
    bats tests/bats/capsule-install-09-live-adr-004.bats -x 2>&1 | tee -a /tmp/13-04-verifier.log
    # Phase 8 — Helmrelease + chart static contracts
    bats tests/bats/capsule-install-01-static-helmrelease.bats \
         tests/bats/capsule-install-04-static-no-umbrella.bats -x 2>&1 | tee -a /tmp/13-04-verifier.log
    # Phase 9 — P27 + P32
    bats tests/bats/p27-tenant-kustomization-sa-static.bats \
         tests/bats/tenants-04-static-dependson.bats -x 2>&1 | tee -a /tmp/13-04-verifier.log
    # Phase 9 — negative RBAC (N7 registry-rejection fixture — must not regress with docker.io addition)
    bats tests/bats/negative-rbac-01-cross-tenant-list.bats \
         tests/bats/negative-rbac-02-escalation.bats \
         tests/bats/negative-rbac-03-webhook-policy.bats \
         tests/bats/negative-rbac-04-bypass-and-flux.bats \
         tests/bats/negative-rbac-05-webhook-down.bats \
         tests/bats/negative-rbac-anti-pattern-lint-static.bats -x 2>&1 | tee -a /tmp/13-04-verifier.log
    # Phase 10 — PROXY-01..03 regression (issue-tenant-kubeconfig.sh hint edit only)
    bats tests/bats/proxy-04-live-issue.bats -x 2>&1 | tee -a /tmp/13-04-verifier.log
    # Phase 11 — D-11-02 + D-11-03 (CapsuleConfiguration + tenant ns label)
    # If the bats files don't exist or have different names, mark as carryover.
    for f in tests/bats/push-gate-02-static-capsuleconfig.bats tests/bats/push-gate-03-static-tenant-ns-label.bats; do
      if [[ -f "$f" ]]; then
        bats "$f" -x 2>&1 | tee -a /tmp/13-04-verifier.log
      else
        echo "INHERITANCE-NOTE: $f not present; recording in carryover" | tee -a /tmp/13-04-verifier.log
      fi
    done
    # Phase 11 — spike artifact static (platform-owner ClusterRole rules + Group subject preservation)
    bats tests/bats/capsule-platform-owner-01-static-rbac.bats \
         tests/bats/capsule-platform-owner-02-live-rbac.bats -x 2>&1 | tee -a /tmp/13-04-verifier.log

    # === Tally ===
    echo "=== TALLY ===" | tee -a /tmp/13-04-verifier.log
    grep -E '^[0-9]+ tests, [0-9]+ failures' /tmp/13-04-verifier.log | tee -a /tmp/13-04-verifier.log
    grep -cE '^not ok' /tmp/13-04-verifier.log | xargs echo "RED count:" | tee -a /tmp/13-04-verifier.log
    grep -cE '^ok' /tmp/13-04-verifier.log | xargs echo "GREEN count:" | tee -a /tmp/13-04-verifier.log
    ```

    Two CARRYOVER conditions are acceptable (planner-discretion to mark disposition `passed_with_overrides`):
    1. `delivery-05-live-slo-regression.bats` — DEFERRED unless `KARYON_REBUILD_APPROVED=1` env was set (Phase 11 carryover pattern). Document as "DEFERRED per KARYON_REBUILD_APPROVED gate; carryover to ad-hoc verification" in VERIFICATION.md.
    2. `delivery-03-live-additive-only.bats` — depends on the base SHA being the right Phase 12 commit. If executor finds the diff returns unexpected removals, investigate: (a) was Plan 13-01 / 13-02 yq edit truly additive-only (the answer should be YES — both used `yq += []`)? (b) is the base SHA correct?

    If any other bats RED, do NOT proceed to Task 2 — investigate first.
  </action>
  <verify>
    <automated>test -f /tmp/13-04-verifier.log && grep -cE '^not ok' /tmp/13-04-verifier.log | grep -q '^0$\|^1$'</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/13-04-verifier.log` captured with full bats output
    - Phase 13 NEW bats: max 1 RED allowed (delivery-05 SLO is acceptable as DEFERRED with KARYON_REBUILD_APPROVED gate documented)
    - All inheritance bats GREEN (Phase 7 P31, Phase 8 ADR-004 + capsule-install statics, Phase 9 P27 + P32 + negative-RBAC suite, Phase 10 PROXY-01..03, Phase 11 spike + push-gate where applicable)
    - Tally summary captured in log (test count + failure count + green count)
  </acceptance_criteria>
  <done>Bats suite outcome documented; disposition determined (passed / passed_with_overrides).</done>
</task>

<task type="auto">
  <name>Task 2: Write 13-VERIFICATION.md + flip VALIDATION.md frontmatter to final</name>
  <files>
    .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VERIFICATION.md,
    .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VALIDATION.md
  </files>
  <read_first>
    - .planning/milestones/v0.19-phases/11-validation-graduation-adr-008/11-VERIFICATION.md (Phase 11 verifier analog — disposition + REQ coverage table + bats matrix + carryover + notable observations sections)
    - .planning/milestones/v0.19-phases/10-tenant-owner-kubeconfigs-capsule-proxy-round-trip/10-VERIFICATION.md (Phase 10 verifier analog — passed_with_overrides disposition + carryover-to-Phase-11 pattern)
    - .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VALIDATION.md (current frontmatter — flip status: draft -> final; nyquist_compliant: false -> true; wave_0_complete: false -> true)
    - All Plan 13-00..13-03 SUMMARY files (per-plan commit SHAs + bats GREEN claims + carryover notes)
    - /tmp/13-00-preflight.log (Plan 13-00 Task 1 inheritance baseline verification)
    - /tmp/13-04-verifier.log (Task 1 full bats output)
  </read_first>
  <action>
    **1. CREATE `.planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VERIFICATION.md`** following the structure in `<interfaces>` above.

    Required content sections:
    - Frontmatter: phase, status (passed / passed_with_overrides — based on Task 1 outcome), verifier: 13-04, verified date.
    - Disposition section with rationale (cite Task 1 tally).
    - REQ Coverage table — 12 rows (RBAC-01..06 + SEED-01..03 + DELIVERY-01..03). Each row cites:
      - Bats file name
      - Plan + commit SHA (use `git log --oneline -10` to find Plan 13-01/13-02/13-03 commits)
      - Live evidence snippet (e.g., `auth can-i '*' '*'` returns `no`; SEED-02 elapsed time from /tmp/13-03-seed-02.log)
    - Bats Matrix table (categorized by Phase 13 NEW static / NEW live / inherited; GREEN / RED / SKIP / Total counts from /tmp/13-04-verifier.log tally).
    - Inheritance Verification section — each Phase 7/8/9/10/11 regression gate cited as GREEN (specific bats file + brief observation).
    - Carryover & Deferred section — `delivery-05-live-slo-regression.bats` deferred unless `KARYON_REBUILD_APPROVED=1` was set; any RoleBinding numbering observations (A2 confirm or deviation).
    - Notable Observations section — capture Pitfall 13-P7 actual live RoleBinding indices post-Phase-13-02 (from `kubectl get rolebinding -A | grep -E 'capsule-(alpha|bravo)-' | sort -u | awk '{print $1}'`); confirm Group + SA each get default-clusterRole bindings.
    - Hand-off to Phase 14 section — list of forward-pointers (RUNBOOK-01..05 + DEMO-01..06 + OQ-1 TLS noise + OQ-5 marquee name).
    - Hand-off to Phase 15 section — close-out + retrospective expectations; REQUIREMENTS.md `[ ]` -> `[x]` flips against this VERIFICATION.md evidence.

    Use the Phase 11 11-VERIFICATION.md shape verbatim. Disposition `passed_with_overrides` is acceptable if delivery-05 is documented as deferred per gate.

    **2. EDIT `.planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VALIDATION.md`** — flip the frontmatter:

    Before (current):
    ```yaml
    ---
    phase: 13
    slug: rbac-narrowing-globaltenantresource-seed-delivery-mechanism
    status: draft
    nyquist_compliant: false
    wave_0_complete: false
    created: 2026-05-19
    ---
    ```

    After:
    ```yaml
    ---
    phase: 13
    slug: rbac-narrowing-globaltenantresource-seed-delivery-mechanism
    status: final
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-05-19
    finalized: 2026-MM-DD   # date of Plan 13-04 commit
    ---
    ```

    Plus update the "Per-Task Verification Map" table — flip each row's Status from "pending" to "green" (or flaky / red if any task is not satisfied — should be all green in a `passed` disposition).

    Plus update the "Validation Sign-Off" section at the bottom — check off each [ ] item.

    Plus update the "Wave 0 Requirements" section — flip each `- [ ]` to `- [x]` for the 19 NEW bats files.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VERIFICATION.md && grep -F 'passed' .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VERIFICATION.md && grep -F 'nyquist_compliant: true' .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VALIDATION.md && grep -F 'wave_0_complete: true' .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `13-VERIFICATION.md` exists with at least 100 lines
    - Contains frontmatter with `status: passed` or `status: passed_with_overrides`
    - REQ Coverage table has 12 rows (all 12 phase REQs cited)
    - Bats Matrix tallies match `/tmp/13-04-verifier.log` GREEN/RED counts
    - Inheritance Verification cites Phase 7+8+9+10+11 inheritance bats by name
    - Carryover section lists DEFERRED items (e.g., SLO regression gate)
    - `13-VALIDATION.md` frontmatter flipped: `status: final`, `nyquist_compliant: true`, `wave_0_complete: true`
    - All 19 Wave 0 bats checkboxes flipped to `- [x]`
    - All 19 Per-Task Verification Map rows flipped to green
  </acceptance_criteria>
  <done>Verification ledger written; VALIDATION.md final; Phase 13 closed.</done>
</task>

<task type="auto">
  <name>Task 3: Append `## Platform-owner kubeconfig delivery contract` H2 section to docs/poc-capsule.md (Phase 14 forward-pointer)</name>
  <files>docs/poc-capsule.md</files>
  <read_first>
    - docs/poc-capsule.md (existing H2 `## Tenant kubeconfig delivery contract` lines 146-189 — mirror this shape exactly)
    - .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-PATTERNS.md - "docs/poc-capsule.md (EDITED; APPEND Platform-owner kubeconfig H2 subsection)" section lines 1056-1119 (pattern shape verbatim)
    - .planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-CONTEXT.md - "Plan 13-04 verifier docs snippet" + D-13-09 + D-13-10
  </read_first>
  <action>
    APPEND the new H2 section AT THE END of `docs/poc-capsule.md` (after the existing "Tenant kubeconfig delivery contract" H2 + any closing sections; the H2 is a peer to existing H2s, NOT nested under any H1).

    Use the verbatim section from `<interfaces>` above. The 1-paragraph intro + Quickstart bash block + Contract table + Mandatory invariants + Cross-references shape mirrors the existing H2.

    DO NOT modify any existing content in `docs/poc-capsule.md` — this is a pure append.

    Verify after editing:
    ```bash
    grep -c '^## ' docs/poc-capsule.md
    # Should be at least 1 more H2 section than before
    grep -F 'Platform-owner kubeconfig delivery contract' docs/poc-capsule.md
    grep -F 'platform-owner-via-proxy' docs/poc-capsule.md
    grep -F 'See Phase 14' docs/poc-capsule.md
    ```

    Phase 14 owns the FULL runbook (`docs/poc-capsule-demo.md`). Plan 13-04's append is a SHORT forward-pointer (~30 lines of markdown) that gives Phase 14 something to anchor the demo narrative to.
  </action>
  <verify>
    <automated>grep -F 'Platform-owner kubeconfig delivery contract' docs/poc-capsule.md && grep -F 'platform-owner-via-proxy' docs/poc-capsule.md && grep -F 'See Phase 14' docs/poc-capsule.md && grep -F 'capsule-platform-owners' docs/poc-capsule.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/poc-capsule.md` contains a NEW H2 section `## Platform-owner kubeconfig delivery contract`
    - Section contains Status callout (`> **Status:** Active. Phase 13 — RBAC-04..06.`)
    - Section contains Quickstart bash block invoking `issue-platform-owner-kubeconfig.sh`
    - Section contains Contract table with at least: Output channel default, Server URL (https://127.0.0.1:30443), Authentication (TokenRequest API), TLS trust (tls.crt jsonpath), Default namespace in context (UNSET), Issued context name (platform-owner-via-proxy)
    - Section contains Mandatory invariants (P29 + P31)
    - Section contains Cross-references with forward-pointer to Phase 14 `docs/poc-capsule-demo.md` runbook act 2
    - Existing content (Tenant kubeconfig delivery contract H2 + everything else) preserved verbatim — `git diff` shows ONLY a single APPEND at the end of the file
    - No regression of pre-existing markdownlint baseline (count of lint warnings unchanged)
  </acceptance_criteria>
  <done>docs/poc-capsule.md has forward-pointer section ready for Phase 14 to expand into the full demo runbook narrative.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 13 verifier -> live cluster | Verifier runs bats against the live spoke-capsule cluster; misinterpreted bats output could falsely close Phase 13 |
| 13-VERIFICATION.md -> Phase 15 close-out | VERIFICATION.md is the evidence anchor for REQUIREMENTS.md `[ ]` -> `[x]` flips in Phase 15; falsified evidence here cascades to milestone close |
| docs/poc-capsule.md append -> Phase 14 runbook | Section is the forward-pointer anchor for Phase 14's runbook; if section is missing or wrong, Phase 14 has no anchor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Denial of Service / Scope Creep | P31 isolation regression — verifier asserts no new spoke-capsule mentions in v0.18 default-path scripts (re-runs inherited poc-isolation-01-static.bats) | mitigate | Task 1 explicitly reruns `bats tests/bats/poc-isolation-01-static.bats`; any failure blocks Phase 13 disposition `passed` and requires investigation. Task 3 explicitly does NOT modify any v0.18 script. |
| T-13-05 | Denial of Service | Flux SA owner pattern regression — verifier asserts via delivery-03-live and rbac-07-live | mitigate | Task 1 explicitly reruns both bats files; delivery-03 asserts no removals from spec.owners[] via git diff; rbac-07 asserts existing entries preserved via jsonpath query |
| T-13-01 | Information Disclosure | New `Platform-owner kubeconfig delivery contract` H2 section in docs/poc-capsule.md could expose token shape | mitigate | Section uses placeholder bash example (`> /tmp/po.kubeconfig`) — no real token or path content. Phase 7 D-14 leak defenses cover any operator-generated kubeconfig file. |
</threat_model>

<verification>
- Task 1: Bats suite tally captured in `/tmp/13-04-verifier.log`
- Task 2: `13-VERIFICATION.md` exists with at least 100 lines, at least 12 REQ rows, at least 1 inheritance verification section, at least 1 carryover section
- Task 2: `13-VALIDATION.md` frontmatter shows `status: final`, `nyquist_compliant: true`, `wave_0_complete: true`; all 19 Wave 0 checkboxes flipped to `[x]`; all 19 per-task rows flipped to green
- Task 3: `docs/poc-capsule.md` contains the new H2 with all required content (Quickstart, Contract table, Mandatory invariants, Cross-references)
- All inherited bats GREEN throughout the verifier run (Phase 7 P31, Phase 8 ADR-004, Phase 9 P27/P32/negative-RBAC, Phase 10 PROXY, Phase 11 spike + D-11-02/03)
</verification>

<success_criteria>
- 3 files committed: `13-VERIFICATION.md` (NEW), `13-VALIDATION.md` (EDITED frontmatter + checkboxes), `docs/poc-capsule.md` (APPEND H2)
- Phase 13 disposition `passed` or `passed_with_overrides` documented with rationale
- All 12 REQs (RBAC-01..06, SEED-01..03, DELIVERY-01..03) cited with live evidence + commit SHA
- Inheritance bats matrix GREEN
- VALIDATION.md frontmatter final
- Phase 14 forward-pointer landed in docs/poc-capsule.md
- Git commit: `docs(13-04): write 13-VERIFICATION.md + flip 13-VALIDATION.md final + append platform-owner kubeconfig H2 to docs/poc-capsule.md (DELIVERY-01 + DELIVERY-02 + DELIVERY-03 evidence; Phase 13 closed)`
</success_criteria>

<output>
After completion, create `.planning/phases/13-rbac-narrowing-globaltenantresource-seed-delivery-mechanism/13-04-SUMMARY.md` documenting:
- Disposition outcome (passed / passed_with_overrides) with rationale
- Bats tally (GREEN / RED / SKIP counts)
- 12 REQ coverage citations
- Inheritance bats GREEN per phase (7/8/9/10/11)
- Carryover items (SLO regression deferred unless KARYON_REBUILD_APPROVED set; A2 RoleBinding numbering observations)
- Phase 14 forward-pointers (RUNBOOK-01..05 + DEMO-01..06 + OQ-1 + OQ-5)
- Phase 15 hand-off (REQUIREMENTS.md flips + retrospective expectations)
- STATE.md update suggestion for next session: bump `last_activity`, advance Phase 13 complete in Performance Metrics table
</output>
</content>
</invoke>