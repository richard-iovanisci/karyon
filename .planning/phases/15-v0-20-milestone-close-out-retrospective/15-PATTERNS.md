# Phase 15: v0.20 Milestone Close-out + Retrospective — Pattern Map

**Mapped:** 2026-05-20
**Files analyzed:** 5 plan templates × multiple `.planning/*.md` edit surfaces (see classification table)
**Analogs found:** 5 / 5 (every Phase 15 plan inherits a Phase 12 analog with concrete excerpts)

> **Phase character (read first):** Phase 15 is a book-keeping / close-out phase. It edits ONLY
> `.planning/*.md` artifacts and writes a single live-rerun evidence log under the phase dir.
> NO source code is created or modified anywhere. The pattern map therefore does NOT find analog
> source files (there are none) — it finds analog **plan files** from v0.19 Phase 12 close-out and
> per-artifact analog **edit patterns** the planner can copy verbatim. Where Phase 15 introduces a
> truly novel artifact category (live-rerun evidence log), the analog is Phase 14's evidence-log
> idiom, not a Phase 12 plan.

---

## File Classification

### Phase 15 Plans (the "files to create")

| Plan | Role | Data Flow | Closest Plan-template Analog | Match Quality |
|------|------|-----------|------------------------------|---------------|
| `15-01-PLAN.md` (REQUIREMENTS flip + inline deviation amend) | bookkeeping plan / artifact-edit | request-response (grep-verifiable) | `12-01-PLAN.md` | exact |
| `15-02-PLAN.md` (STATE.md frontmatter + body advance) | bookkeeping plan / artifact-edit | request-response (grep-verifiable) | `12-02-PLAN.md` | exact |
| `15-03-PLAN.md` (ROADMAP.md Progress table + Phase Summary) | bookkeeping plan / artifact-edit | request-response (grep-verifiable) | `12-06-PLAN.md` | exact |
| `15-04-PLAN.md` (live regression rerun: bats subset + `task rebuild` SLO) | bookkeeping plan / verification + evidence-log | live-cluster batch + evidence-capture | Phase 14 `14-02-full-suite-evidence.log` + `14-03-uat-cold-run-evidence.log` shape (NO Phase 12 analog — Phase 12 did not run live bats) | partial (closest precedent for the evidence-log shape; no plan-template analog) |
| `15-05-PLAN.md` (audit + retrospective append + parking-lot reconcile + VALIDATION/VERIFICATION finalize) | bookkeeping plan / multi-artifact gate | sequential audit-gate | `12-07-PLAN.md` (audit gate) + `RETROSPECTIVE.md` v0.19 section (retrospective shape) + `12-VALIDATION.md` (book-keeping VALIDATION shape) | exact (audit-gate); exact (retrospective); exact (VALIDATION) |

### Phase 15 Modification Surface (the files each plan edits)

| Edit Target | Plan | Analog Edit Source |
|-------------|------|-------------------|
| `.planning/REQUIREMENTS.md` (11 `[ ]` → `[x]` flips; inline OQ-1..5 lock-in amendments; Traceability table) | 15-01 | `12-01-PLAN.md` Tasks 1+2+3 |
| `.planning/STATE.md` (frontmatter counters + Current Position block + Performance Metrics By-Phase rows) | 15-02 | `12-02-PLAN.md` Tasks 1+2 |
| `.planning/ROADMAP.md` (Phase Summary checkboxes + Progress table rows; in-progress mid-phase state, NOT close-state flip) | 15-03 | `12-06-PLAN.md` Tasks 1+2 |
| `.planning/phases/15-*/15-04-live-rerun-evidence.log` (NEW evidence-log artifact) | 15-04 | `14-02-full-suite-evidence.log` (full bats); `14-03-uat-cold-run-evidence.log` (cold-run section) |
| `.planning/v0.20-MILESTONE-AUDIT.md` (regenerated by `/gsd-audit-milestone v0.20`) | 15-05 | `12-07-PLAN.md` Task 2 (audit re-run invocation + contingency handling) |
| `.planning/RETROSPECTIVE.md` (append v0.20 section) | 15-05 | `.planning/RETROSPECTIVE.md` existing v0.18 + v0.19 sections (lines 5-58 + 62-106) |
| `.planning/PROJECT.md` (carry-forward parking lot reconciliation) | 15-05 | No Phase 12 analog (Phase 12 didn't touch PROJECT.md); novel-but-bounded: planner authors from CONTEXT.md D-15-05 directly |
| `.planning/STATE.md` (final phase-close: `ready_to_archive`, `percent: 100`) | 15-05 | `12-07-PLAN.md` Task 3 Edits 3+4 |
| `.planning/ROADMAP.md` (final phase-close: Phase 15 row `5/5 Complete`; Phase Summary `[x]`) | 15-05 | `12-07-PLAN.md` Task 3 Edits 1+2 |
| `.planning/phases/15-*/15-VALIDATION.md` (frontmatter finalize: status/nyquist/wave_0) | 15-05 | `12-VALIDATION.md` frontmatter (lines 1-10) |
| `.planning/phases/15-*/15-VERIFICATION.md` (verifier output) | 15-05 | `12-VERIFICATION.md` (frontmatter + Goal Achievement table) |

---

## Pattern Assignments

### Plan 15-01 — REQUIREMENTS.md (analog: `12-01-PLAN.md`)

**Analog:** `.planning/milestones/v0.19-phases/12-v0-19-close-out-reconciliation/12-01-PLAN.md`

#### Frontmatter shape (12-01 lines 1-28)

```yaml
---
phase: 12
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [POC-01, POC-02, POC-03, POC-04, CAPCLU-01, CAPCLU-02, CAPCLU-03, CAPCLU-04, EKSDOC-01, CAP-01, CAP-02, CAP-03, TEN-01, TEN-02, TEN-03, TEN-04, TEN-05, TEN-06]
tags: [bookkeeping, requirements, close-out, v0.19]

must_haves:
  truths:
    - "All 18 stale `[ ]` checkboxes for v0.19 mandatory REQ-IDs ... are flipped to `[x]` in REQUIREMENTS.md"
    - "TEN-01 spec text reflects the live `forceTenantPrefix: false` repair from Plan 11-07 D-11-07 (path-a amendment per RESEARCH.md Open Question 4)"
    - "REQUIREMENTS.md §v0.19 Close-out Reconciliation narrative (line 86) accurately reports 18 v0.19 mandatory REQ-IDs (not the stale '15')"
    - "ADDON-01 and ADDON-02 stay `[ ]` (deferred to v0.20 per Future Requirements section)"
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: "Updated REQ ledger with v0.19 mandatory checkboxes flipped, TEN-01 spec amended, ..."
      contains: "forceTenantPrefix: false per Plan 11-07 D-11-07"
  key_links:
    - from: .planning/REQUIREMENTS.md
      to: pocs/capsule/spoke/tenant-crs/{alpha,bravo}.yaml
      via: "TEN-01 spec text now matches live YAML at HEAD 8415ab66 (commit faecf14)"
      pattern: "forceTenantPrefix: false"
---
```

**Adapt for 15-01:** `phase: 15 / plan: 01 / requirements: [DEMO-01..06, RUNBOOK-01..05]` (11 IDs);
`tags: [bookkeeping, requirements, close-out, v0.20]`; truths list the 11 flips + OQ-1/2/3/4/5 lock-in
amendments per CONTEXT D-15-03 + RESEARCH §"Plan 15-01 — REQUIREMENTS.md edits". `key_links` cite
Phase 13 + Phase 14 `VERIFICATION.md` + the live YAML / live bats evidence.

#### Same-line checkbox-flip pattern (12-01 Task 1 lines 79-134)

```xml
<task type="auto">
  <name>Task 1: Flip 17 same-line REQ checkboxes (all v0.19 mandatory except TEN-01)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 30-63 (the v0.19 mandatory REQ section — confirms current `[ ]` state and exact line contents)
    - .planning/v0.19-MILESTONE-AUDIT.md §1 "Requirements Coverage" table lines 102-129
    - .planning/phases/12-v0-19-close-out-reconciliation/12-PATTERNS.md §A "REQUIREMENTS.md checkbox flip"
  </read_first>
  <action>
    Use the Edit tool 17 times (one Edit call per REQ-ID) to flip `- [ ] **<ID>**` to `- [x] **<ID>**`. Preserve every other byte of each line exactly.

    Exact 17 edits (line numbers per REQUIREMENTS.md HEAD `83b81bb5`):
    Line 30: replace `- [ ] **POC-01**` with `- [x] **POC-01**`
    ...
    Line 63: replace `- [ ] **TEN-06**` with `- [x] **TEN-06**`

    Because the Edit tool requires unique `old_string`, use enough surrounding text on each Edit call so `old_string` matches exactly one line. For each ID, an unambiguous `old_string` is the full bold-id literal (e.g., `- [ ] **POC-01**`).
  </action>
  <verify>
    <automated>test $(grep -cE "^- \[x\] \*\*(POC|CAPCLU|EKSDOC|CAP|TEN)-" .planning/REQUIREMENTS.md) -eq 17</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*POC-0[1-4]\*\*" .planning/REQUIREMENTS.md` returns 4
    - ...
    - `grep -cE "^- \[ \] \*\*(POC|CAPCLU|EKSDOC|CAP|TEN)-" .planning/REQUIREMENTS.md` returns 1 (only TEN-01 still `[ ]` at this point — Task 2 handles it)
  </acceptance_criteria>
  <done>
    17 of 18 v0.19 mandatory REQ-IDs checked `[x]`; ADDON-01/02 unchanged; ...
  </done>
</task>
```

**Adapt for 15-01:** loop the same pattern over the 11 Phase-15 lines (DEMO-01..06 at REQUIREMENTS.md
lines 64-69; RUNBOOK-01..05 at lines 75-79 per RESEARCH §"Plan 15-01 — REQUIREMENTS.md edits"). One
Edit call per REQ-ID; `old_string` = `- [ ] **<ID>**`; `new_string` = `- [x] **<ID>**`. Final grep gate:
`grep -cE "^- \[x\] \*\*(DEMO|RUNBOOK)-" .planning/REQUIREMENTS.md` returns 11; combined Phase-13+14:
`grep -cE "^- \[x\] \*\*(RBAC|SEED|DEMO|RUNBOOK|DELIVERY)-"` returns 23.

#### Inline spec-deviation amendment pattern (12-01 Task 2 lines 136-176 — THE TEN-01 EXEMPLAR)

```xml
<task type="auto">
  <name>Task 2: Flip TEN-01 checkbox + amend spec text inline (forceTenantPrefix: true → false annotation)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md line 58 (the current TEN-01 line — confirm exact wording)
    - .planning/v0.19-MILESTONE-AUDIT.md §"Spec-vs-code deviation (TEN-01)"
    - .planning/phases/11-validation-graduation-adr-008/11-07-SUMMARY.md
    - .planning/phases/12-v0-19-close-out-reconciliation/12-PATTERNS.md §B
  </read_first>
  <action>
    Use ONE Edit tool call against REQUIREMENTS.md line 58.

    `old_string` (entire current line 58, verbatim):
    `- [ ] **TEN-01** — Two `Tenant` custom resources ... (`forceTenantPrefix: true`)`

    `new_string` (verbatim replacement; flipped checkbox + amended spec text + inline citation):
    `- [x] **TEN-01** — Two `Tenant` custom resources ... DISABLED (`forceTenantPrefix: false` per Plan 11-07 D-11-07 live repair — preserves established `tenant-alpha` / `tenant-bravo` home namespace names; see `11-07-SUMMARY.md` and commit `faecf14`)`

    Differences from old_string:
    - `[ ]` → `[x]` (checkbox flipped)
    - `enabled (`forceTenantPrefix: true`)` → `DISABLED (`forceTenantPrefix: false` per Plan 11-07 D-11-07 live repair — preserves ...; see `11-07-SUMMARY.md` and commit `faecf14`)`
    - All other bytes identical.
  </action>
  <verify>
    <automated>grep -c "TEN-01.*forceTenantPrefix: false.*Plan 11-07 D-11-07" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*TEN-01\*\*" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "TEN-01.*forceTenantPrefix: false.*Plan 11-07 D-11-07.*commit \`faecf14\`" .planning/REQUIREMENTS.md` returns 1
  </acceptance_criteria>
</task>
```

**Adapt for 15-01 (deviation candidates per CONTEXT D-15-03 + RESEARCH §"Inline spec-deviation amendments required"):**

For each of the 4 OQ lock-in amendments, use ONE Edit call combining checkbox flip + inline citation:

1. **OQ-1 RUNBOOK-03 — TLS noise = `document-only`** (Phase 14 D-14-01) — RESEARCH says no amendment needed (text already says "document-only is the recommended default"); optionally add `(Phase 14 D-14-01)` inline anchor.
2. **OQ-2 RBAC-01 — Secrets policy = `read-only`** (Phase 13 D-13-01) — amend `resolved in discuss-phase per OQ-2` → `read-only per Phase 13 D-13-01`.
3. **OQ-3 SEED-03 — hello-world shape = `Pod + Service`** (Phase 13 D-13-02) — amend `OQ-3-resolved choice` → `Pod + Service with FQCI docker.io/library/nginx:alpine per Phase 13 D-13-02`.
4. **OQ-4 DELIVERY-01 — GitOps under `pocs/capsule/` (NO Taskfile entry)** (Phase 13 D-13-03) — amend `either GitOps... or task seed-capsule-demo` → `GitOps under pocs/capsule/; NO task seed-capsule-demo entry per Phase 13 D-13-03`.
5. **OQ-5 RUNBOOK-05 — new-tenant name = `charlie`, provision = `live`** (Phase 14 D-14-02 + D-14-03) — amend `'charlie' / 'demo' / 'team-foo' / other` → `'charlie' (per Phase 14 D-14-02; provision style = live per D-14-03)`.

Acceptance criteria mirror Task 2: each amend has a unique grep-able citation literal
(`grep -c "Phase 13 D-13-01"`, etc.) returning 1.

#### Narrative-drift correction pattern (12-01 Task 3 lines 178-214)

This pattern applies to the REQUIREMENTS.md Traceability table (RESEARCH says lines 132-160 area):
flip each Phase-15-flipped REQ row's `Pending` → `Complete` (mirrors Phase 13 rows 138-146 which
already say `Complete`). Use the same 1-Edit-per-line idiom as 12-01 Task 3 (verbatim old_string ↔
new_string with `Pending` → `Complete`).

---

### Plan 15-02 — STATE.md (analog: `12-02-PLAN.md`)

**Analog:** `.planning/milestones/v0.19-phases/12-v0-19-close-out-reconciliation/12-02-PLAN.md`

#### Frontmatter shape (12-02 lines 1-34)

```yaml
---
phase: 12
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
autonomous: true
requirements: []
tags: [bookkeeping, state, close-out, v0.19]

must_haves:
  truths:
    - "STATE.md frontmatter `last_activity` reflects Plan 11-07 closeout ..."
    - "STATE.md frontmatter `stopped_at` cleared / advanced past `Plan 11-07 Task 6 human-verify checkpoint`"
    - "STATE.md frontmatter `last_updated` set to current timestamp"
    - "STATE.md frontmatter progress counters reflect actual milestone-wide plan total (34 across phases 7-12) ..."
    - "STATE.md `Current Position` body block reflects Phase 12 execution context"
    - "STATE.md `Pending Todos` body block: ... bullets ... marked RESOLVED with explicit closure-commit citations"
---
```

**Adapt for 15-02:** `phase: 15 / plan: 02 / tags: [bookkeeping, state, close-out, v0.20]`. Truths
reflect RESEARCH §"Plan 15-02 — STATE.md edits": frontmatter `status: completed → executing` (Wave 1
mid-phase) / `last_activity → 2026-05-20 -- Phase 15 entered`; counters (`total_phases: 3`,
`total_plans: 14`, `completed_plans: 9 → 14`, `percent: 64 → 100` — the `100` lands in 15-05, not 15-02);
Performance Metrics By-Phase rows for 13/14/15 (RESEARCH lines 140-147); append v0.20 velocity-counter
line under v0.19 (RESEARCH line 152).

#### Frontmatter block-edit pattern (12-02 Task 1 lines 94-169)

```xml
<task type="auto">
  <name>Task 1: Update STATE.md frontmatter — bump last_activity, last_updated, stopped_at, progress counts past Plan 11-07 closeout (mid-Phase-12 state: 27/34 = 79%)</name>
  <files>.planning/STATE.md</files>
  <action>
    Use the Edit tool to rewrite the STATE.md frontmatter block (lines 1-15). The exact replacement is below.

    `old_string` (lines 1-15 verbatim from HEAD):
    ```
    ---
    gsd_state_version: 1.0
    milestone: v0.19
    milestone_name: — Capsule Multi-Tenancy POC
    status: executing
    stopped_at: Plan 11-07 Task 6 human-verify checkpoint
    last_updated: "2026-05-07T12:19:48.383Z"
    last_activity: 2026-05-07 -- Phase 11 execution started
    progress:
      total_phases: 6
      completed_phases: 4
      total_plans: 27
      completed_plans: 26
      percent: 96
    ---
    ```

    `new_string`:
    ```
    ---
    gsd_state_version: 1.0
    milestone: v0.19
    milestone_name: — Capsule Multi-Tenancy POC
    status: executing
    stopped_at: Phase 12 in progress — v0.19 close-out reconciliation
    last_updated: "2026-05-11T00:00:00.000Z"
    last_activity: 2026-05-11 -- Plan 11-07 closed (G-06 diagnostics resolved, HEAD 8415ab66); Phase 12 close-out reconciliation entered
    progress:
      total_phases: 6
      completed_phases: 5
      total_plans: 34
      completed_plans: 27
      percent: 79
    ---
    ```

    Rationale for each numeric: ... (see 12-02 Task 1 action body for the planner's verbatim arithmetic)
  </action>
</task>
```

**Adapt for 15-02:** Read the live STATE.md frontmatter (RESEARCH §"Plan 15-02 — STATE.md edits" notes
current frontmatter has `status: completed` + `percent: 100` which is PREMATURE). Mid-phase target:
`status: executing / stopped_at: Phase 15 in progress / last_activity: 2026-05-20 -- Phase 15 entered /
completed_plans: 9 / total_plans: 14 / percent: 64`. Plan 15-05 advances to `status: ready_to_archive /
completed_phases: 3 / completed_plans: 14 / percent: 100` (mirrors 12-07 Task 3 Edit 3 — see Plan 15-05
section below).

#### Body block + Pending Todos edit pattern (12-02 Task 2 lines 171-287)

```xml
<task type="auto">
  <name>Task 2: Update STATE.md body — Current Position block + mark 4 stale Pending Todos bullets RESOLVED with closure-commit citations</name>
  <action>
    **Edit call 1: Update Current Position block (STATE.md lines 28-34).**

    `old_string`:
    ```
    ## Current Position

    Phase: 11 (validation-graduation-adr-008) — EXECUTING
    Plan: 8 of 8 (11-07)
    Status: Awaiting Task 6 human verification checkpoint
    Last activity: 2026-05-07 -- Plan 11-07 automated Tasks 1-5 complete; Task 6 operator verification pending
    ```

    `new_string`:
    ```
    ## Current Position

    Phase: 12 (v0-19-close-out-reconciliation) — EXECUTING
    Plan: 1 of 7 (12-01 in flight; clusters A-F run as Wave 1 parallel; gate 12-07 in Wave 2)
    Status: Executing Phase 12 close-out reconciliation (book-keeping; mutates only `.planning/*.md`)
    Last activity: 2026-05-11 -- Plan 11-07 closed (HEAD 8415ab66); Phase 12 entered
    ```

    **Edit call 2+: Mark each stale Pending Todos bullet RESOLVED with closure-commit citation.**
    Bullet body pattern: `- **<original bullet title> (RESOLVED <YYYY-MM-DD>)** — <closure narrative>; closure summarized in commit `<sha> <commit subject>`. Phase X re-verification commit `<sha>` confirms <thing> closed. → `<source artifact>`.`
  </action>
</task>
```

**Adapt for 15-02:** Current Position block flip per RESEARCH line 154:

```
Phase: 15 (v0-20-close-out-retrospective) — EXECUTING
Plan: 1 of 5 (Wave 1 in flight: 15-01..15-04 file-disjoint; gate 15-05 in Wave 2)
Status: Phase 15 close-out reconciliation (book-keeping; mutates only `.planning/*.md`)
Last activity: 2026-05-20 -- Phase 15 entered
```

**Pending Todos work:** RESEARCH says Plan 15-02 has NO Pending Todos reconciliation work (unlike
12-02 which had 4 bullets to mark RESOLVED — they were already RESOLVED in Plan 12-02). The HUMAN-UAT
bullet stays unchanged. Plan 15-02 may append a status note about live-cluster-state prerequisite for
Plan 15-04 (Blockers/Concerns block at lines 150-156).

**Performance Metrics By-Phase table (RESEARCH §"Performance Metrics By-Phase table" lines 72-78):**
this is novel to 15-02 (Plan 12-02 didn't touch this section). Use the same Edit-with-verbatim-table-row
idiom as the 12-06 Progress-table edits below — one Edit call per row, `old_string` = `| 13 | TBD | TBD | TBD |`,
`new_string` = `| 13 (RBAC + SEED + DELIVERY) | 5 | <total> | <avg> |`. Planner may invoke `/gsd-stats` or
`/gsd-progress` to populate the numbers (CONTEXT Claude's Discretion).

---

### Plan 15-03 — ROADMAP.md (analog: `12-06-PLAN.md`)

**Analog:** `.planning/milestones/v0.19-phases/12-v0-19-close-out-reconciliation/12-06-PLAN.md`

#### Frontmatter shape (12-06 lines 1-33)

```yaml
---
phase: 12
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
autonomous: true
requirements: []
tags: [bookkeeping, roadmap, verification, close-out, v0.19]

must_haves:
  truths:
    - "ROADMAP.md Progress table is consistent with Phase Summary checkboxes ..."
    - "Phase 11 row: 8/8 plans Complete 2026-05-07"
    - "Phase 12 row: currently `Planned 0/7 —` (set by planner-init commit aca6600; will be flipped to Complete 7/7 by Plan 12-07 in Wave 2)"
    - "No within-document contradiction between Phase Summary (lines 39-44) and Progress table (lines 153-167)"
    - "ROADMAP.md narrative count ... reports `18 v0.19 mandatory` (not the stale `15`) ..."
---
```

**Adapt for 15-03:** `phase: 15 / plan: 03 / tags: [bookkeeping, roadmap, verification, close-out, v0.20]`.
Truths from RESEARCH §"Plan 15-03 — ROADMAP.md edits": Phase 14 row `0/4 Not started → 4/4 Complete
2026-05-19`; Phase 15 row `0/TBD Not started → 0/5 In progress -` (mid-phase) or `5/5 Complete 2026-05-21`
(Plan 15-05 close-flip); Phase Summary Phase 14 `[ ] → [x]`; Phase 15 `[ ]` stays unchanged in Plan 15-03
(15-05 flips it).

#### Verification-only pattern for already-consistent state (12-06 Task 1 lines 99-161)

This pattern applies when the live ROADMAP rows MAY already be correct (set by orchestrator init or
prior phase commits). Use grep-only verification first; do NOT silently auto-correct. From 12-06 Task 1:

```xml
<task type="auto">
  <name>Task 1: Verify ROADMAP.md Phase Summary + Progress table consistency (grep-only; no edits)</name>
  <action>
    Run the following grep verifications (use Bash tool):
    grep -E "^- \[x\] \*\*Phase (7|8|9|10|11):" .planning/ROADMAP.md | wc -l   # Expected: 5
    grep -c "^| 11\. .*| v0.19 | 8/8 | Complete | 2026-05-07 |$" .planning/ROADMAP.md   # Expected: 1
    grep -c "^| 12\. .*| v0.19 | 0/7 | Planned | — |$" .planning/ROADMAP.md   # Expected: 1

    If ALL the above checks return their expected values, write a one-line verification record to ...

    If ANY check fails (returns unexpected value), DO NOT silently auto-correct. Instead, document the surfaced contradiction in the SUMMARY's Notable Observations and add a Wave-1 follow-up edit ...
  </action>
</task>
```

**Adapt for 15-03:** Run analogous greps against ROADMAP.md Phase Summary lines 70-73 (Phase 13/14/15)
and Progress table lines 150-152 (Phase 13/14/15 rows). Expected mid-phase state per RESEARCH lines
166-170: Phase 13 `[x] / 5/5 Complete 2026-05-19`; Phase 14 `[ ] → [x] / 0/4 Not started → 4/4 Complete
2026-05-19`; Phase 15 `[ ] / 0/TBD Not started → 0/5 In progress -`.

#### Direct Edit row-replacement pattern (12-06 Task 2 lines 163-228)

```xml
<task type="auto">
  <name>Task 2: Correct ... at 3 sites (lines 44, 141, 143) ... </name>
  <action>
    Three Edit tool calls against ROADMAP.md, one per site. Each `old_string` is the verbatim current line; each `new_string` ...

    **Edit 2 — ROADMAP.md line 141 (Phase 12 Requirements line in Phase Details):**

    `old_string`:
    `**Requirements**: No new REQ-IDs (book-keeping phase). Closes the 15 stale `[ ]` checkboxes for ...`

    `new_string`:
    `**Requirements**: No new REQ-IDs (book-keeping phase). Closes the 18 v0.19 mandatory `[ ]` checkboxes for ... (= 4+4+1+3+6 = 18) against the existing implementation.`
  </action>
  <verify>
    <automated>
test $(grep -c "18 v0.19 mandatory REQs" .planning/ROADMAP.md) -ge 1 && \
test $(grep -c "15 stale \`\[ \]\` checkboxes" .planning/ROADMAP.md) -eq 0
    </automated>
  </verify>
</task>
```

**Adapt for 15-03:** RESEARCH §"Narrative-drift sweep" says v0.20 has NO equivalent count-drift in
ROADMAP.md (the milestone-level Phase Summary already cites `23 v0.20 mandatory` implicitly). Plan 15-03
should still sweep for any `0/4`-style stale counts and apply the same Edit-per-row idiom for the
Phase-14 row flip and (mid-phase) Phase-15 row state. Use exact-byte `old_string` matching the live
ROADMAP at edit time (planner reads first, then edits).

---

### Plan 15-04 — Live regression rerun (analog: Phase 14 evidence-log idiom; NO Phase 12 plan analog)

**Analog (evidence-log shape):**
- `.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-02-full-suite-evidence.log` (full bats — 79KB, 707 lines per `1..707` header)
- `.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-03-uat-cold-run-evidence.log` (cold-run + UAT structure)
- `.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-02-demo-live-evidence.log` (per-bats-suite subset)
- `.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/14-02-visceral-check-evidence.log` (single-assertion visceral check)

#### Evidence-log header pattern (14-02-full-suite-evidence.log lines 1-3)

```
=== Full bats suite (Plan 14-02 / 2026-05-19T20:51:33-04:00) ===
Cluster: k3d-spoke-capsule (Phase 13 + Phase 14 land state)
Runner: bats -T (1.11.0; --duration aliased to --timing per CLI help)
```

**Adapt for 15-04 `15-04-live-rerun-evidence.log`:** header per RESEARCH §"Evidence log shape" — run
metadata (UTC start/end, operator, branch, cluster context, HEAD commit), verbatim command + output
per bats invocation, summary table with GREEN/RED/SKIP per category, carry-forward inheritance analysis.

Concrete preamble per CONTEXT D-15-02 + RESEARCH §"Pre-demo checklist preamble":

```
=== Plan 15-04 live regression rerun (2026-05-21T<HH:MM>:00Z) ===
Cluster: k3d-spoke-capsule (Phase 13 + Phase 14 land state; Phase 15 close-out verification)
Branch: main @ <HEAD-sha>; origin/main must be at-or-past Phase 14 close commit
Runner: bats -T (1.11.0)
Operator: <operator>

--- Pre-flight checklist (per Phase 14 D-14-05) ---
$ kubectl --context k3d-spoke-capsule get nodes
<verbatim output>
$ flux --context k3d-hub-flux get kustomizations | grep capsule
<verbatim output>
$ kubectl --context k3d-spoke-capsule get tenants
<verbatim output>

--- Kubeconfig minting (per Phase 13 + Phase 14 scripts) ---
$ bash scripts/poc/capsule/issue-platform-owner-kubeconfig.sh --duration 2h --write-to /tmp/karyon-tenants/platform-owner.kubeconfig
<verbatim output; redact any kubeconfig contents per P29>
$ bash scripts/poc/capsule/issue-tenant-kubeconfig.sh alpha-owner alpha --write-to /tmp/karyon-tenants/tenant-alpha.kubeconfig
$ bash scripts/poc/capsule/issue-tenant-kubeconfig.sh bravo-owner bravo --write-to /tmp/karyon-tenants/tenant-bravo.kubeconfig

--- Bats subset (Phase 13 + Phase 14 live + static; per RESEARCH §"Plan 15-04 — Live regression rerun") ---
$ bats tests/bats/rbac-*.bats tests/bats/seed-*.bats tests/bats/delivery-{01,02,03,04}-*.bats tests/bats/demo-*.bats tests/bats/runbook-*.bats --duration
<verbatim output; expected ~207 tests GREEN>

--- Cluster-admin ceiling assertions ---
$ kubectl --kubeconfig=$PO_KC auth can-i '*' '*'
no
$ kubectl --kubeconfig=$TO_ALPHA_KC auth can-i '*' '*'
no
$ kubectl --kubeconfig=$TO_BRAVO_KC auth can-i '*' '*'
no

--- task rebuild SLO cold-run (DELIVERY-02 gate) ---
$ time KARYON_REBUILD_APPROVED=1 task rebuild
<verbatim output>
real    <duration>s    # must be < 230s
$ task health-check; echo $?
<verbatim output>
0

--- Summary ---
| Category                | Expected | GREEN | RED | SKIP |
| Phase 13 RBAC live      | ~28      | 28    | 0   | 0    |
| Phase 13 SEED live      | ~12      | 12    | 0   | 0    |
| Phase 13 DELIVERY live  | ~7       | 7     | 0   | 0    |
| Phase 14 DEMO live      | ~22      | 22    | 0   | 0    |
| Phase 14 RUNBOOK static | ~17      | 17    | 0   | 0    |
| Total                   | ~207     | 207   | 0   | 0    |

--- Carry-forward inheritance ---
- Phase 14 UAT auto-attested-with-carry-forward (Plan 14-03): RESOLVED by this Plan 15-04 live regression rerun + Plan 15-05 milestone-owner approval at audit gate.
- task rebuild SLO < 230s: PRESERVED (DELIVERY-02 + DELIVERY-03 regression gates).
```

#### Bats invocation pattern (14-02-full-suite-evidence.log line 4 onward — verbatim TAP output)

The `ok N <bats name> in <ms>ms` lines and `not ok` failure stanzas (with `# (in test file ...)` and
indented assertion-failure-text) are copy-paste from `bats -T` stdout. The `# skip ...` markers carry
the env-gate rationale (e.g., `KARYON_LIVE_TESTS=1` for live tests, `KARYON_REBUILD_APPROVED=1` for
DELIVERY-05). Plan 15-04 captures both `ok` and `not ok` verbatim.

#### Plan 15-04 plan-template (no Phase 12 analog — author from CONTEXT + RESEARCH directly)

Frontmatter:

```yaml
---
phase: 15
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/15-v0-20-milestone-close-out-retrospective/15-04-live-rerun-evidence.log
autonomous: false  # destructive task rebuild requires KARYON_REBUILD_APPROVED=1 + live cluster
requirements: [RBAC-03, RBAC-04, DELIVERY-02, DELIVERY-03]  # regression gates
tags: [bookkeeping, live-regression, bats, slo, close-out, v0.20]

must_haves:
  truths:
    - "Phase 13 + Phase 14 bats subset (~207 @tests) runs GREEN end-to-end against live spoke-capsule"
    - "kubectl auth can-i '*' '*' returns 'no' for platform-owner and both tenant-owner kubeconfigs (cluster-admin ceiling preserved)"
    - "task rebuild SLO cold-run < 230s; task health-check exit 0 (DELIVERY-02 + DELIVERY-03 regression gates)"
    - "Evidence captured verbatim in 15-04-live-rerun-evidence.log per Phase 14 14-02-full-suite-evidence.log shape"
  artifacts:
    - path: .planning/phases/15-v0-20-milestone-close-out-retrospective/15-04-live-rerun-evidence.log
      provides: "Live regression evidence (bats + cluster-admin denials + task rebuild SLO)"
      contains: "ok 207"  # or whatever the actual TAP green-count is at plan time
---
```

`autonomous: false` because the `task rebuild` cold-run is destructive (the env-gate `KARYON_REBUILD_APPROVED=1`
defends; see Pitfall 11-P5 in 12-RESEARCH). Tasks: (1) pre-flight checklist + kubeconfig mint
(non-destructive); (2) bats subset run; (3) cluster-admin denial assertions; (4) `task rebuild` cold-run
+ health-check; (5) write evidence log + summary table.

---

### Plan 15-05 — Audit + retrospective + parking lot + VALIDATION + VERIFICATION (multi-source analog)

**Analogs (multiple sources — Plan 15-05 is a multi-artifact gate):**
- **Audit gate + Phase self-close edits:** `12-07-PLAN.md` (entire plan; Tasks 1-4)
- **Retrospective append shape:** `.planning/RETROSPECTIVE.md` existing v0.18 section (lines 5-58) + v0.19 section (lines 62-106)
- **VALIDATION ledger finalize:** `.planning/milestones/v0.19-phases/12-v0-19-close-out-reconciliation/12-VALIDATION.md` (frontmatter lines 1-10; body §Wave 0 Requirements)
- **VERIFICATION shape:** `12-VERIFICATION.md` frontmatter + Goal Achievement table (lines 1-32)
- **PROJECT.md parking lot reconciliation:** NO Phase 12 analog (novel artifact category); planner authors from CONTEXT D-15-05 directly using `additive edits` to existing `## Carry-forward parking lot` section

#### Frontmatter shape (12-07 lines 1-40)

```yaml
---
phase: 12
plan: 07
type: execute
wave: 2
depends_on: ["12-01", "12-02", "12-03", "12-04", "12-05", "12-06"]
files_modified:
  - .planning/v0.19-MILESTONE-AUDIT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/12-v0-19-close-out-reconciliation/12-VALIDATION.md
autonomous: false
requirements: []
tags: [bookkeeping, audit, phase-close, gate, v0.19]

must_haves:
  truths:
    - "`/gsd-audit-milestone v0.19` regenerates `.planning/v0.19-MILESTONE-AUDIT.md` and returns `status: passed` ..."
    - "If the audit returns `tech_debt` due to ADDON-01/02 ... apply the contingency: ..."
    - "Phase 12 self-close lands: ROADMAP.md Phase 12 row `0/7 Planned → 7/7 Complete` ... STATE.md `completed_phases: 5 → 6`, `completed_plans: 27 → 34` ..."
---
```

**Adapt for 15-05:** `phase: 15 / plan: 05 / wave: 2 / depends_on: ["15-01", "15-02", "15-03", "15-04"]`.
`files_modified` expands to:
- `.planning/v0.20-MILESTONE-AUDIT.md`
- `.planning/ROADMAP.md`
- `.planning/STATE.md`
- `.planning/REQUIREMENTS.md` (Traceability footer)
- `.planning/RETROSPECTIVE.md` (append v0.20 section)
- `.planning/PROJECT.md` (parking lot)
- `.planning/phases/15-*/15-VALIDATION.md`
- `.planning/phases/15-*/15-VERIFICATION.md`

`autonomous: false` (human-verify checkpoint at audit gate per D-15-06).

#### Task 1 — Pre-flight verification (12-07 lines 96-170)

Exact-count grep gate over all Wave 1 outputs. Pattern:

```bash
# Wave 1 success state — exact-count grep gate
# Plan 15-01: 11 v0.20 mandatory REQ-IDs flipped to [x]
test $(grep -cE "^- \[x\] \*\*(DEMO|RUNBOOK)-" .planning/REQUIREMENTS.md) -eq 11 && \
# Plan 15-01: 4 OQ-1..5 lock-in citations landed
test $(grep -c "Phase 13 D-13-01" .planning/REQUIREMENTS.md) -ge 1 && \
test $(grep -c "Phase 13 D-13-02" .planning/REQUIREMENTS.md) -ge 1 && \
test $(grep -c "Phase 13 D-13-03" .planning/REQUIREMENTS.md) -ge 1 && \
test $(grep -c "Phase 14 D-14-02" .planning/REQUIREMENTS.md) -ge 1 && \
# Plan 15-02: STATE.md frontmatter advanced (mid-phase state pre-15-05-flip)
test $(grep -cE "^last_activity: 2026-05-20" .planning/STATE.md) -eq 1 && \
test $(grep -c "^  total_plans: 14$" .planning/STATE.md) -eq 1 && \
# Plan 15-03: ROADMAP.md Phase 14 row Complete; Phase 15 row in-progress mid-phase
test $(grep -cE "^- \[x\] \*\*Phase 14:" .planning/ROADMAP.md) -eq 1 && \
test $(grep -cE "^\| 14\. .*\| v0.20 \| 4/4 \| Complete" .planning/ROADMAP.md) -eq 1 && \
# Plan 15-04: live-rerun evidence log exists and has GREEN summary
test -f .planning/phases/15-v0-20-milestone-close-out-retrospective/15-04-live-rerun-evidence.log && \
test $(grep -c "^Total .*207 .*0 .*0$" .planning/phases/15-v0-20-milestone-close-out-retrospective/15-04-live-rerun-evidence.log) -ge 1

# If ANY check fails: HALT. Phase 15 has a regression in Wave 1; report which check failed.
```

#### Task 2 — Audit re-run + contingency (12-07 lines 172-220 — THE D-15-06 EXEMPLAR)

```xml
<task type="auto">
  <name>Task 2: Run `/gsd-audit-milestone v0.19` and verify the regenerated audit reports `passed`</name>
  <action>
    Invoke the audit workflow:
    /gsd-audit-milestone v0.19

    Read the new audit:
    head -10 .planning/v0.19-MILESTONE-AUDIT.md

    **Expected primary case:** Frontmatter `status: passed`; `gaps.requirements: []`; `tech_debt: []`; `nyquist.overall: 5/5`.

    **Contingency 1 — Audit returns `tech_debt` only because of ADDON-01/02:**
    Per RESEARCH.md §"Pitfall 5", ... operator must decide ...

    **Contingency 2 — Audit returns `tech_debt` for non-ADDON reasons:**
    Print the audit's gaps arrays. Identify the missed item from Wave 1. Open a revision-mode task on the affected Plan 12-NN. Do not proceed to Task 3 until the audit returns `passed`.

    **Contingency 3 — Audit returns `gaps_found`:**
    Same as Contingency 2 but more severe. Investigate immediately ...
  </action>
</task>
```

**Adapt for 15-05 per D-15-06:** Invoke `/gsd-audit-milestone v0.20`. Expected primary case: `status:
passed`; `gaps.requirements: []` (all 23 mandatory satisfied); `tech_debt: []`; `nyquist.overall: ≥
13/13` (Phases 1-12 + 13 + 14 + 15 = 15 phases counting v0.18 + v0.19 + v0.20; planner verifies actual
denominator at run time). **Contingency 1** (ADDON / parking-lot REQs): apply same Path-X / Path-Y
operator-decision pattern as 12-07 Task 4. **Contingency 2/3** (any non-acceptable failure): HARD BLOCK
per D-15-06 — milestone is NOT marked `ready_to_archive`; remediation lands as `/gsd-insert-phase 14.1`
or a new Plan 15-06; rerun audit.

#### Task 3 — Phase self-close edits (12-07 lines 222-396)

Six Edit calls landing the Phase-12 self-close markers. Edit 1: ROADMAP Phase Summary checkbox `[ ] → [x]`
+ body rewrite to past-tense summary. Edit 2: ROADMAP Progress table row `0/7 Planned — → 7/7 Complete
<date>`. Edit 3: STATE frontmatter `status: executing → ready_to_archive`, `completed_phases +1`,
`completed_plans → total`, `percent → 100`. Edit 4: STATE Current Position block `EXECUTING → COMPLETE`.
Edit 5: `<phase>-VALIDATION.md` frontmatter `status: draft → final` + `nyquist_compliant: true` +
`wave_0_complete: true` + add `finalized` + `finalized_evidence`. Edit 6: REQUIREMENTS.md Traceability
footer paragraph append.

**Adapt for 15-05:** Same 6 edits, retargeted at Phase 15:

```yaml
# Edit 5 — 15-VALIDATION.md frontmatter (mirrors 12-VALIDATION.md verbatim, adjusted IDs)
---
phase: 15
slug: v0-20-close-out-retrospective
status: final
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-20
finalized: 2026-05-21
finalized_evidence: "Phase 15 is a book-keeping reconciliation phase with no own Wave-0 scaffold (per 15-VALIDATION.md §Wave 0 Requirements `None`; mutates only `.planning/*.md` files + Plan 15-04 evidence log; verification surface IS file content + live bats GREEN). Phase gate `/gsd-audit-milestone v0.20` returns `passed` per 15-05-SUMMARY.md (audit re-run 2026-05-21). nyquist_compliant set true because every Wave-1 plan's <acceptance_criteria> is grep-verifiable and was verified during execution; wave_0_complete set true because no Wave 0 was required per the phase character (book-keeping; D-15-07)."
---
```

#### Task 4 — Human-verify checkpoint (12-07 lines 398-440)

```xml
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — confirm audit status and self-close edits</name>
  <how-to-verify>
    1. Read the audit output: head -20 .planning/v0.19-MILESTONE-AUDIT.md
    2. If `passed` — approve and proceed.
    3. If `tech_debt` for ADDON-01/02 only: Path X (accept) OR Path Y (purge).
    4. If `tech_debt` for non-ADDON reasons OR `gaps_found`: open revision mode; do NOT mark this task done.
    5. Verify the Phase self-close edits (5 grep checks)
    6. After approval, the orchestrator may proceed to `/gsd-complete-milestone v0.19` to archive.
  </how-to-verify>
  <resume-signal>
    Type "approved" OR "path-x" OR "path-y" (per the audit outcome). Describe any issues to trigger revision mode.
  </resume-signal>
</task>
```

**Adapt for 15-05:** Same checkpoint shape, retargeted at v0.20. Step 6: after approval, the orchestrator
may proceed to `/gsd-complete-milestone v0.20`.

#### Retrospective append shape (analog: `.planning/RETROSPECTIVE.md` v0.19 section lines 62-106)

Section header + sub-section cadence per D-15-04. Verbatim shape excerpt:

```markdown
## Milestone: v0.19 — Capsule Multi-Tenancy POC

**Shipped:** 2026-05-18
**Phases:** 6 (7-12) | **Plans:** 34 | **Tasks:** 76

### What Was Built

<3-6 bullets summarizing what shipped>

### What Worked

- **Wave 0 Nyquist gate** carried forward from v0.18: ...
- **Split-plan-from-execute discipline:** ...
- **D-letter decision codes** scaled cleanly across milestones: ...

### What Was Inefficient

- **Plan 09-03 landed on attempt 3** after two atomic rollbacks. ...

### Patterns Established

- **Split-Kustomization architecture (D-08-12):** ...
- **PostBuild over spec.patches (D-11-01 relocation):** ...
- **Evidence-bound graduation rubric (D-11-14):** ...
- **`/gsd-audit-milestone` re-run as Phase-close gate:** ...

### Key Lessons

1. **Wave 0 Nyquist gate continues to pay off** — verified across v0.18 and v0.19. ...

### Cost Observations

- **Model mix:** ...
- **Sessions:** ...
- **Notable:** ...
```

**Adapt for 15-05 (per D-15-04 sub-section cadence):**

```markdown
## Milestone: v0.20 — Capsule POC Demo & RBAC Polish

**Shipped:** 2026-05-21
**Phases:** 3 (13-15) | **Plans:** 14 | **Tasks:** <count>

### What Was Built

- `tenant-workload-editor` ClusterRole + tenant Tenant CR co-owner binding (Phase 13 RBAC-01..06)
- `GlobalTenantResource` propagation for hello-world seed (Phase 13 SEED-01..03)
- Platform-owner co-owner CRB pattern (LIST/WATCH/GET/LOGS/EXEC) (Phase 13 + 14)
- Two-perspective demo verification (platform-owner + tenant-owner) (Phase 14 DEMO-01..06)
- `docs/poc-capsule-demo.md` 6-act runbook + Known Noise appendix (Phase 14 RUNBOOK-01..05)
- OQ-1..5 resolutions: document-only TLS noise, read-only Secrets, Pod+Service hello-world, GitOps delivery, 'charlie' tenant + live provision

### What Worked

- Lean-milestone framing held (D-15-01 — 5 plans / 2 waves vs Phase 12's 7 plans / 2 waves)
- Additive-only constraint preserved alpha/bravo Flux reconciliation (RBAC-03 regression gate)
- Wave 0 RED bats Nyquist gate continues to pay off (carry-over from v0.18 + v0.19)
- UAT cold-read caught actual runbook friction (Phase 14 D-14-04)
- Phase 12 close-out template scaled cleanly to Phase 15 compressed form

### What Was Inefficient

- <pulled from Phase 13/14 SUMMARY.md + VERIFICATION.md per CONTEXT D-15-04>
- Capsule-controller TLS noise treated as cosmetic (OQ-1 document-only); cert-rotation layer fix deferred to graduation
- spoke-capsule cluster state-recovery patterns (`task fix-dns-poc-capsule` invocations)

### Patterns Established

- **Three-tier permission model:** Tier 1 (cluster-admin internal) / Tier 2 (platform-owner co-owner) / Tier 3 (tenant-workload-editor)
- **Co-owner CRB pattern** for platform-owner LIST/WATCH/GET/LOGS/EXEC
- **`new-tenant.sh` provisioning template** (per OQ-5 D-14-02 / D-14-03)
- **Demo runbook 6-act cadence** with executable pre-demo checklist + Known Noise appendix
- **Phase 15 5-plan compressed close-out** (vs Phase 12's 7-plan template)

### OQ Resolutions

| OQ | Resolution | Phase / D-code |
|----|-----------|----------------|
| OQ-1 (TLS noise) | document-only | Phase 14 D-14-01 |
| OQ-2 (Secrets policy) | read-only | Phase 13 D-13-01 |
| OQ-3 (hello-world shape) | Pod + Service | Phase 13 D-13-02 |
| OQ-4 (delivery mechanism) | GitOps under pocs/capsule/; NO Taskfile entry | Phase 13 D-13-03 |
| OQ-5 (new-tenant marquee name + provision style) | charlie + live | Phase 14 D-14-02 + D-14-03 |

### Carry-forward updates

- **Closed:** (none expected — v0.20 was purely additive)
- **Appended to parking lot:** TLS-fix-at-cert-layer (cert-manager wiring), `task demo-capsule` wrapper (revisit at graduation), <Phase 13 deferred-items.md items>

### Key Lessons

1. <…>

### Cost Observations

- <…>
```

#### PROJECT.md parking-lot reconciliation (NO analog — author from CONTEXT D-15-05 directly)

Per CONTEXT D-15-05 + RESEARCH integration points: additive edits to existing `## Carry-forward parking lot`
section. Retain existing entries (`capsule-addon-fluxcd`, G-04, `KARYON_PHASE11_STRICT_LIVE`,
`slo-regression-live`, markdownlint, gitleaks-proxy-06, Phase 1-5 shellcheck, first-push CI verification,
real secret management). Append (D-15-05):

- Fix `tls: bad certificate` capsule-controller chatter at cert-rotation layer (was OQ-1 document-only)
- `task demo-capsule` Taskfile wrapper (Phase 14 D-14 declined for P31 isolation; revisit at graduation)
- Any items from `.planning/phases/13-*/deferred-items.md` (read at plan time)

Edit idiom: append new bullets to the bottom of the parking-lot list using ONE Edit call per logical
addition. `old_string` = the literal last bullet text + trailing context; `new_string` = same + appended
new bullets. Verify with `grep -c "tls: bad certificate"` returns 1 + `grep -c "task demo-capsule"` returns
≥ 1 in PROJECT.md.

#### VERIFICATION.md shape (analog: 12-VERIFICATION.md lines 1-32)

Frontmatter:

```yaml
---
phase: 15-v0-20-close-out-retrospective
verified: 2026-05-21T00:00:00Z
status: passed
score: 5/5 ROADMAP Success Criteria verified; <N>/<N> plan-level must-haves verified
overrides_applied: 0
re_verification: false
---
```

Body sections (per 12-VERIFICATION header pattern):
- `# Phase 15: v0.20 Milestone Close-out + Retrospective — Verification Report`
- `## Goal Achievement` → `### ROADMAP Success Criteria (5/5)` table with columns `#`, `Success Criterion`,
  `Status`, `Evidence` (one row per ROADMAP SC 1-5)
- `### Plan-level Must-haves (<N>/<N>)` table per-plan sub-section (#### Plan 15-01 / Plan 15-02 / ...
  Plan 15-05) with truth-vs-evidence rows

---

## Shared Patterns

### Frontmatter date convention (applies to all 15-* plans)

**Source:** 12-PATTERNS §"Frontmatter date convention" (referenced in 12-02 Task 1)

- `last_updated` in ISO8601: `"YYYY-MM-DDTHH:mm:ss.sssZ"`
- `last_activity` in `YYYY-MM-DD --` prefix form
- `created` / `finalized` in plain `YYYY-MM-DD`
- ROADMAP Progress table date cells in plain `YYYY-MM-DD`

### Acceptance-criteria grep-verifiability convention (applies to ALL Phase 15 plans)

**Source:** Every Phase 12 plan; reinforced by 12-VALIDATION §"Per-Task Verification Map"
(book-keeping phase verification surface = file content; grep-checkable).

Every `<acceptance_criteria>` block uses literal grep counts with `-eq` (exact) semantics, not `-ge`
(over-counts indicate unintended additional matches and must be investigated, not silently accepted).
The 12-07 Task 1 pre-flight check is the exemplar: 14 chained `test $(grep -c ...) -eq <N>` assertions.

### Edit tool unique-old_string convention (applies to all `.planning/*.md` edits)

**Source:** 12-01 Task 1 action body ("Because the Edit tool requires unique `old_string`, use enough
surrounding text on each Edit call so `old_string` matches exactly one line").

For multi-bullet sections (e.g., REQ-ID flips, RESOLVED bullet rewrites, Performance Metrics rows),
the unique anchor is the bold ID literal (`**POC-01**`) or a unique multi-line window (Total paragraph
+ trailing blank + `---` separator + trailing blank + `*Last updated:` opener — 12-07 Task 3 Edit 6).

### `read_first` convention for `.planning/*.md`-editing plans

**Source:** Every Phase 12 plan; standard `<read_first>` shape:

```xml
<read_first>
  - <target file with exact line range — confirms current state and exact line contents>
  - <upstream-evidence file — confirms the change is justified>
  - <PATTERNS.md or RESEARCH.md section — provides the verbatim diff pattern>
  - <closure-commit citation source — for RESOLVED bullets or footer notes>
</read_first>
```

Phase 15 plans inherit verbatim — each `<read_first>` block reads (a) the target file's exact lines,
(b) the upstream Phase 13/14 evidence (`13-VERIFICATION.md`, `14-VERIFICATION.md`, `13-*-SUMMARY.md`,
`14-*-SUMMARY.md`), and (c) this PATTERNS.md section + 15-RESEARCH.md for the concrete diff.

### `<execution_context>` block (applies to all Phase 15 plans)

**Source:** Every Phase 12 plan (lines 65-67):

```xml
<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>
```

Phase 15 plans inherit verbatim — no adaptation needed.

### `<context>` block (applies to all Phase 15 plans)

**Source:** Every Phase 12 plan (lines 69-77) — context-file imports for the executor agent:

```xml
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v0.19-MILESTONE-AUDIT.md
@.planning/phases/12-v0-19-close-out-reconciliation/12-RESEARCH.md
@.planning/phases/12-v0-19-close-out-reconciliation/12-PATTERNS.md
</context>
```

**Adapt for 15-* plans:** Replace `v0.19-MILESTONE-AUDIT.md` with `v0.20-MILESTONE-AUDIT.md` (or
omit if it doesn't yet exist at plan-author time; Plan 15-05 Task 2 regenerates it). Replace
`12-RESEARCH.md` + `12-PATTERNS.md` with `15-RESEARCH.md` + `15-PATTERNS.md`. Add per-plan upstream
evidence imports (e.g., Plan 15-01 adds `@.planning/phases/13-*/13-VERIFICATION.md` +
`@.planning/phases/14-*/14-VERIFICATION.md`).

### `<threat_model>` block (applies to all Phase 15 plans)

**Source:** Every Phase 12 plan ends with a minimal threat model — Phase 15 is the same shape because
book-keeping introduces zero new attack surface:

```xml
<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation reconciliation only; no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-<plan>-01 | N/A | <surface> | accept | No new threats — documentation reconciliation only; existing v0.20 security posture per `14-VERIFICATION.md` is unchanged. |
</threat_model>
```

**Exception — Plan 15-04 (live regression rerun) threat model:** Plan 15-04 DOES mint kubeconfigs
+ run `task rebuild` (destructive). Add P29 mitigation note: "kubeconfigs minted to `/tmp/karyon-tenants/`;
evidence log redacts any kubeconfig contents per gitleaks defense". Add P31 mitigation: `task rebuild`
runs without `spoke-capsule` (k3d hub/spoke-ml/spoke-apps clusters only; `spoke-capsule` is a separate
isolated cluster per Phase 7 ADR-008 framing).

### `<output>` block (applies to all Phase 15 plans)

**Source:** Every Phase 12 plan ends with a SUMMARY.md output directive:

```xml
<output>
After completion, create `.planning/phases/12-v0-19-close-out-reconciliation/12-NN-SUMMARY.md` per `~/.claude/get-shit-done/templates/summary.md`. requirements-completed: [<IDs or empty for bookkeeping>].
</output>
```

**Adapt for 15-* plans:** Path becomes `.planning/phases/15-v0-20-milestone-close-out-retrospective/
15-NN-SUMMARY.md`. `requirements-completed` for 15-01 = `[DEMO-01, ..., DEMO-06, RUNBOOK-01, ..., RUNBOOK-05]`
(the 11 flipped IDs); 15-02 / 15-03 / 15-04 / 15-05 = `[]` (bookkeeping; no new REQ-IDs); 15-04 may
optionally cite the regression gates `[RBAC-03, RBAC-04, DELIVERY-02, DELIVERY-03]` as
`regression-gates-verified` if the SUMMARY template supports that key.

---

## No Analog Found

Files with no close match anywhere in the codebase (planner must author from CONTEXT.md + 15-RESEARCH.md
guidance directly):

| File / Surface | Rationale |
|----------------|-----------|
| `.planning/PROJECT.md` "Carry-forward parking lot" edits | Novel artifact category for Phase 15 — Phase 12 did NOT touch PROJECT.md. Planner authors per CONTEXT D-15-05: retain existing entries, append Phase 13/14-surfaced deferrals (TLS-fix-at-cert-layer, `task demo-capsule` wrapper, items from `13-*/deferred-items.md`). Edit idiom is the standard `additive bullet append` — one Edit call per logical addition with unique `old_string` anchor (last existing bullet + trailing context). |
| Plan 15-04 live-rerun plan-template | No Phase 12 analog — Phase 12 close-out did NOT run live bats (verification was grep-only on file content). The closest precedent is the Phase 14 evidence-log artifacts (above) which inform the OUTPUT shape, but no prior PLAN.md exists for a "run live bats subset + capture evidence log" pattern. Planner authors plan-template from CONTEXT D-15-02 + 15-RESEARCH §"Plan 15-04 — Live regression rerun" directly. |
| `task rebuild` SLO assertion in a `.planning/`-edit plan | Phase 12 did not exercise `task rebuild`. The closest precedent is v0.18 Phase 11 `delivery-05-live-slo-regression.bats` (env-gated by `KARYON_REBUILD_APPROVED=1`). Plan 15-04 may invoke either the bats wrapper (set the env-gate) or `task rebuild` directly with `time` wrapper. Both paths satisfy DELIVERY-02 per RESEARCH §"`delivery-05-live-slo-regression.bats` skip semantics". |

---

## Metadata

**Analog search scope:**
- `.planning/milestones/v0.19-phases/12-v0-19-close-out-reconciliation/` (7 plan files; 12-VALIDATION.md; 12-VERIFICATION.md; 12-RESEARCH.md; 12-PATTERNS.md)
- `.planning/phases/14-two-perspective-demo-verification-6-act-runbook-tls-noise-re/` (4 evidence-log files)
- `.planning/RETROSPECTIVE.md` (v0.18 + v0.19 sections lines 5-58 + 62-106)

**Files scanned:** ~25 (7 Phase 12 plans + 4 Phase 14 evidence logs + 4 cross-artifact references +
spot-reads of upstream artifacts to confirm context)

**Pattern extraction date:** 2026-05-20

**Confidence:** HIGH — every pattern reference cites a verbatim excerpt from a verified live artifact;
no abstractions or hypothetical patterns. The Phase 12 → Phase 15 mapping is 1:1 except for the three
"No Analog" cases explicitly listed above.
