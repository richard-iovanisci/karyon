---
phase: 02-cluster-layer
plan: 03
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - images/config.toml.tmpl
  - images/device-plugin-daemonset.yaml
  - tests/bats/cuda-image-04-device-plugin.bats
autonomous: true
requirements:
  - IMG-03
  - IMG-04
tags:
  - containerd
  - device-plugin
  - phase-2
  - wave-1

must_haves:
  truths:
    - "images/config.toml.tmpl exists with the verbatim body from 02-RESEARCH.md §2.2 (lines 395-431)"
    - "config.toml.tmpl uses the {{ template \"base\" . }} extension pattern (k3s docs explicitly recommend extension over hand-copy)"
    - "config.toml.tmpl overrides default_runtime_name = \"nvidia\" via [plugins.'io.containerd.cri.v1.runtime'.containerd] table re-declaration"
    - "config.toml.tmpl carries the 3-part GPU contract header comment (RESEARCH.md §2.2 lines 396-422)"
    - "images/device-plugin-daemonset.yaml exists with the verbatim body from 02-RESEARCH.md §2.3 (lines 454-533)"
    - "device-plugin-daemonset.yaml ships a RuntimeClass resource (apiVersion: node.k8s.io/v1, kind: RuntimeClass, name: nvidia, handler: nvidia) ABOVE the DaemonSet (separated by ---)"
    - "device-plugin-daemonset.yaml DaemonSet pins image: nvcr.io/nvidia/k8s-device-plugin:v0.17.4 (per planning context user lock — REQ-IMG-04 reads 'v0.19.0+' but user has approved v0.17.4 as the current-stable interpretation; do NOT amend REQUIREMENTS.md here)"
    - "DaemonSet pod spec sets runtimeClassName: nvidia (RuntimeClass-first flow per D-01)"
    - "DaemonSet container args include exactly four flags: --mig-strategy=none, --fail-on-init-error=true, --pass-device-specs=false, --device-list-strategy=envvar (RESEARCH.md §2.3 flag-rationale)"
    - "DaemonSet tolerates nvidia.com/gpu, node-role.kubernetes.io/master, node-role.kubernetes.io/control-plane (single-node spoke-ml taints)"
    - "DaemonSet uses priorityClassName: system-node-critical and securityContext drops ALL capabilities"
    - "tests/bats/cuda-image-04-device-plugin.bats has its static @test body activated (skip removed; real grep against the manifest pinning v0.17.4)"
  artifacts:
    - path: "images/config.toml.tmpl"
      provides: "containerd config template — third leg of 3-part k3d GPU contract; sets default_runtime_name = nvidia (IMG-03)"
      contains: "default_runtime_name = \"nvidia\""
    - path: "images/device-plugin-daemonset.yaml"
      provides: "NVIDIA device-plugin DaemonSet + RuntimeClass — baked into k3s auto-apply manifests dir for nvidia.com/gpu capacity at t=0 (IMG-04, IMG-05 baking)"
      contains: "image: nvcr.io/nvidia/k8s-device-plugin:v0.17.4"
    - path: "tests/bats/cuda-image-04-device-plugin.bats"
      provides: "Activated IMG-04 static check (image: nvcr.io/nvidia/k8s-device-plugin:v0.17.4)"
      contains: "v0.17.4"
  key_links:
    - from: "images/config.toml.tmpl"
      to: "k3s base template (ContainerdConfigTemplateV3)"
      via: "{{ template \"base\" . }} extension pattern documented at docs.k3s.io/advanced"
      pattern: "template \"base\""
    - from: "images/device-plugin-daemonset.yaml"
      to: "k3s auto-apply manifests dir /var/lib/rancher/k3s/server/manifests/"
      via: "Dockerfile COPY (Plan 02-02 already added the COPY line)"
      pattern: "COPY images/device-plugin-daemonset.yaml"
    - from: "DaemonSet spec.template.spec.runtimeClassName: nvidia"
      to: "RuntimeClass nvidia resource (top of file)"
      via: "Kubernetes RuntimeClass v1 resolution at pod admission"
      pattern: "runtimeClassName: nvidia"
---

<objective>
Create the two final image artifacts: images/config.toml.tmpl (containerd config override per 02-RESEARCH.md §2.2 — extends k3s base template, overrides default_runtime_name = nvidia) and images/device-plugin-daemonset.yaml (NVIDIA device-plugin DaemonSet + RuntimeClass, v0.17.4 pin per user-confirmed lock). Activate the IMG-04 static bats check.

Purpose: Completes the third leg of the 3-part k3d GPU contract (containerd template) and IMG-04/IMG-05 baking targets. With this plan complete, all four `images/*` files exist, and Plan 02-04 (build-image.sh) can successfully `docker build` the karyon/k3s-cuda image.

Output: Two static image-layer artifacts ready to be consumed by the Dockerfile's COPY lines (already present from Plan 02-02). One bats check activated (skip → real assertion).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cluster-layer/02-CONTEXT.md
@.planning/phases/02-cluster-layer/02-RESEARCH.md
@.planning/phases/02-cluster-layer/02-PATTERNS.md

<interfaces>
The config.toml.tmpl body is verbatim from 02-RESEARCH.md §2.2 (lines 395-431). Copy ALL of this into images/config.toml.tmpl:

```toml
# images/config.toml.tmpl
#
# Third leg of the 3-part k3d GPU contract (D-05 / IMG-03):
#   (1) /etc/docker/daemon.json default-runtime: nvidia  -- Phase 1 install-docker.sh
#   (2) FROM nvidia/cuda:12.8.1-base-ubuntu22.04         -- images/Dockerfile.k3s-cuda
#   (3) default_runtime_name = "nvidia" here             -- THIS FILE
#
# File path inside spoke-ml k3s nodes:
#   /var/lib/rancher/k3s/agent/etc/containerd/config.toml.tmpl
#
# k3s v1.34.x reads this at agent startup and renders it as
#   /var/lib/rancher/k3s/agent/etc/containerd/config.toml
# which containerd 2.0 then loads.
#
# {{ template "base" . }} pulls in the canonical k3s base template
# (ContainerdConfigTemplateV3 in k3s/pkg/agent/templates/templates.go).
# The base template already registers runtimes.nvidia when the
# nvidia-container-runtime binary is present in the image (it is —
# installed by `nvidia-ctk runtime configure --runtime=containerd`
# in images/Dockerfile.k3s-cuda).
#
# The base template emits default_runtime_name ONLY when
# .NodeConfig.DefaultRuntime is non-empty. We want it set
# unconditionally to "nvidia", so we override by re-declaring
# the [plugins.'...'.containerd] table after the base template
# renders.

{{ template "base" . }}

# Override: default_runtime_name = "nvidia" (unconditional).
# Required for GPU workloads to land on the nvidia runtime without
# needing runtimeClassName: nvidia in every Pod spec. Device-plugin
# daemonset still pins runtimeClassName: nvidia for explicitness.
[plugins.'io.containerd.cri.v1.runtime'.containerd]
  default_runtime_name = "nvidia"
```

The device-plugin-daemonset.yaml body is verbatim from 02-RESEARCH.md §2.3 (lines 454-533). Copy ALL of this into images/device-plugin-daemonset.yaml:

```yaml
# images/device-plugin-daemonset.yaml
#
# NVIDIA device-plugin v0.17.4 static manifest for spoke-ml.
# Baked into karyon/k3s-cuda at /var/lib/rancher/k3s/server/manifests/
# so nvidia.com/gpu capacity is up at t=0 (D-01 / IMG-05).
#
# Runtime flags chosen for a single-node GPU cluster:
#   --mig-strategy=none          (RTX 5090 / Blackwell has no MIG — default)
#   --fail-on-init-error=true    (surface GPU-chain breakage loud, not silently block)
#   --pass-device-specs=false    (default; avoid privileged cap drop unless CPUManager integration needed)
#   --device-list-strategy=envvar (default; RuntimeClass-first flow, no CDI)
#
# RuntimeClass resource created alongside the DaemonSet so
# Pods that need explicit nvidia runtime can set runtimeClassName: nvidia.
---
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: nvidia
handler: nvidia
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: nvidia-device-plugin-daemonset
  namespace: kube-system
spec:
  selector:
    matchLabels:
      name: nvidia-device-plugin-ds
  updateStrategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        name: nvidia-device-plugin-ds
    spec:
      # Schedule onto GPU nodes only. spoke-ml has exactly one server node
      # which carries the nvidia.com/gpu capacity. tolerations allow the
      # plugin pod to land even on a tainted GPU node.
      runtimeClassName: nvidia
      tolerations:
        - key: nvidia.com/gpu
          operator: Exists
          effect: NoSchedule
        # Also tolerate the node-role taint k3s adds to server nodes
        # in single-node clusters.
        - key: node-role.kubernetes.io/master
          operator: Exists
          effect: NoSchedule
        - key: node-role.kubernetes.io/control-plane
          operator: Exists
          effect: NoSchedule
      priorityClassName: "system-node-critical"
      containers:
        - image: nvcr.io/nvidia/k8s-device-plugin:v0.17.4
          name: nvidia-device-plugin-ctr
          args:
            - --mig-strategy=none
            - --fail-on-init-error=true
            - --pass-device-specs=false
            - --device-list-strategy=envvar
          env:
            - name: NVIDIA_VISIBLE_DEVICES
              value: all
            - name: NVIDIA_DRIVER_CAPABILITIES
              value: all
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: device-plugin
              mountPath: /var/lib/kubelet/device-plugins
      volumes:
        - name: device-plugin
          hostPath:
            path: /var/lib/kubelet/device-plugins
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create images/config.toml.tmpl (verbatim drop-in from 02-RESEARCH.md §2.2)</name>
  <files>images/config.toml.tmpl</files>
  <read_first>
    - .planning/phases/02-cluster-layer/02-RESEARCH.md §2.2 (lines 390-449) — the canonical body, including the rationale for {{ template "base" . }} extension over hand-copy
    - .planning/phases/02-cluster-layer/02-PATTERNS.md §`images/config.toml.tmpl` (lines 95-110) — pattern lock confirms verbatim drop-in only (do NOT hand-author)
    - .planning/phases/02-cluster-layer/02-CONTEXT.md §decisions D-05 — file path inside the image and rationale
  </read_first>
  <action>
Write `images/config.toml.tmpl` with the EXACT body from `<interfaces>` above (verbatim from 02-RESEARCH.md §2.2). Do NOT hand-author or rewrite. Do NOT copy the entire k3s base template inline — the `{{ template "base" . }}` extension pattern is documented at docs.k3s.io/advanced as the recommended approach.

Critical content guarantees:
- The body must contain literal `{{ template "base" . }}` (with the double-quotes and dot — both required by Go template syntax).
- The override block MUST use the v3 schema table syntax: `[plugins.'io.containerd.cri.v1.runtime'.containerd]` (NOT `[plugins.cri.containerd]` which is the old v2 schema). RESEARCH.md §1.2 explains that containerd 2.0 silently rejects v2 syntax in some cases.
- The line `default_runtime_name = "nvidia"` must appear exactly (with the double-quotes) — this is the literal grep target for IMG-03 verification (RESEARCH.md §5 row 1.4).
- Do NOT add a `runtimes.nvidia` block — RESEARCH.md §2.2 lines 442-446 explicitly notes that the k3s base template auto-detects nvidia-container-runtime and emits the runtime block automatically when nvidia-ctk runtime configure runs during image build (which it does, in the Dockerfile from Plan 02-02).

The header comment block (lines 1-29 of the body) documents the 3-part GPU contract and the template-extension rationale. Ship the header verbatim.
  </action>
  <verify>
    <automated>test -f images/config.toml.tmpl && grep -F -c '{{ template "base" . }}' images/config.toml.tmpl | grep -q '^1$' && grep -F -c 'default_runtime_name = "nvidia"' images/config.toml.tmpl | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f images/config.toml.tmpl` exits 0
    - Contains the base template extension call (literal): `grep -F -c '{{ template "base" . }}' images/config.toml.tmpl` returns `1`
    - default_runtime_name override present (literal, with double-quoted "nvidia"): `grep -F -c 'default_runtime_name = "nvidia"' images/config.toml.tmpl` returns `1`
    - v3-schema table heading present (literal): `grep -F -c "[plugins.'io.containerd.cri.v1.runtime'.containerd]" images/config.toml.tmpl` returns `1`
    - 3-part GPU contract documented in header: `grep -c '3-part k3d GPU contract\|3-part GPU contract' images/config.toml.tmpl` returns at least `1`
    - File path documented in header: `grep -c '/var/lib/rancher/k3s/agent/etc/containerd/config\.toml\.tmpl' images/config.toml.tmpl` returns at least `1`
    - NO redundant runtimes.nvidia hand-declaration: `grep -F -c "runtimes.'nvidia'" images/config.toml.tmpl` returns `0`
    - File length sane: `wc -l < images/config.toml.tmpl` returns at least `30`
  </acceptance_criteria>
  <done>images/config.toml.tmpl exists with verbatim 02-RESEARCH.md §2.2 body; the {{ template "base" . }} extension and default_runtime_name override are present; no redundant runtimes.nvidia hand-declaration.</done>
</task>

<task type="auto">
  <name>Task 2: Create images/device-plugin-daemonset.yaml (verbatim drop-in from 02-RESEARCH.md §2.3)</name>
  <files>images/device-plugin-daemonset.yaml</files>
  <read_first>
    - .planning/phases/02-cluster-layer/02-RESEARCH.md §2.3 (lines 450-541) — the canonical body, including the flag rationale block
    - .planning/phases/02-cluster-layer/02-PATTERNS.md §`images/device-plugin-daemonset.yaml` (lines 113-125) — pattern lock confirms verbatim drop-in
    - Planning context note on DEVICE_PLUGIN_VERSION = v0.17.4 user-approved lock (REQ-IMG-04 text reads "v0.19.0+" but user approved v0.17.4 as the current-stable interpretation)
  </read_first>
  <action>
Write `images/device-plugin-daemonset.yaml` with the EXACT body from `<interfaces>` above (verbatim from 02-RESEARCH.md §2.3). Source of truth is `NVIDIA/k8s-device-plugin/blob/v0.17.4/deployments/static/nvidia-device-plugin.yml` plus the RuntimeClass resource (k3d CUDA reference manifest pattern).

Per planning context user lock: `image: nvcr.io/nvidia/k8s-device-plugin:v0.17.4` (NOT v0.19.x). REQ-IMG-04 text reads "v0.19.0+" but the user has approved v0.17.4 as the current-stable interpretation. Do NOT amend REQUIREMENTS.md here — that's a docs change owned by Phase 6.

Non-negotiable elements (PATTERNS.md §device-plugin-daemonset.yaml):
- TWO YAML documents separated by `---` lines: a RuntimeClass first, then the DaemonSet.
- RuntimeClass: `apiVersion: node.k8s.io/v1`, `kind: RuntimeClass`, `metadata.name: nvidia`, `handler: nvidia`. NO spec field.
- DaemonSet: `apiVersion: apps/v1`, `kind: DaemonSet`, `metadata.namespace: kube-system`, `metadata.name: nvidia-device-plugin-daemonset`.
- Pod spec: `runtimeClassName: nvidia`, `priorityClassName: "system-node-critical"`.
- Container image: `nvcr.io/nvidia/k8s-device-plugin:v0.17.4` (literal — drift with Dockerfile ARG would be caught in code review; v2 will introduce a build-time substitution per RESEARCH.md §7 Open Question 1).
- Container args (exact list, in this order):
  - `--mig-strategy=none`
  - `--fail-on-init-error=true`
  - `--pass-device-specs=false`
  - `--device-list-strategy=envvar`
- Tolerations (3 entries): nvidia.com/gpu (NoSchedule), node-role.kubernetes.io/master (NoSchedule), node-role.kubernetes.io/control-plane (NoSchedule).
- securityContext: `allowPrivilegeEscalation: false`, `capabilities.drop: ["ALL"]`.
- Env: NVIDIA_VISIBLE_DEVICES=all, NVIDIA_DRIVER_CAPABILITIES=all.
- Volume: hostPath `/var/lib/kubelet/device-plugins` mounted at the same path.

Do NOT add `nodeSelector`, `affinity`, `resources`, `livenessProbe`, `readinessProbe`. Spoke-ml is single-node GPU; the manifest is intentionally minimal.
  </action>
  <verify>
    <automated>test -f images/device-plugin-daemonset.yaml && grep -F -c 'image: nvcr.io/nvidia/k8s-device-plugin:v0.17.4' images/device-plugin-daemonset.yaml | grep -q '^1$' && grep -F -c 'kind: RuntimeClass' images/device-plugin-daemonset.yaml | grep -q '^1$' && grep -F -c 'kind: DaemonSet' images/device-plugin-daemonset.yaml | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f images/device-plugin-daemonset.yaml` exits 0
    - DEVICE_PLUGIN_VERSION = v0.17.4 (per user lock): `grep -F -c 'image: nvcr.io/nvidia/k8s-device-plugin:v0.17.4' images/device-plugin-daemonset.yaml` returns `1`
    - RuntimeClass resource present: `grep -F -c 'kind: RuntimeClass' images/device-plugin-daemonset.yaml` returns `1`
    - DaemonSet resource present: `grep -F -c 'kind: DaemonSet' images/device-plugin-daemonset.yaml` returns `1`
    - Two-document YAML (separated by `---`): `grep -c '^---$' images/device-plugin-daemonset.yaml` returns `2`
    - RuntimeClass apiVersion correct: `grep -F -c 'apiVersion: node.k8s.io/v1' images/device-plugin-daemonset.yaml` returns `1`
    - DaemonSet apiVersion correct: `grep -F -c 'apiVersion: apps/v1' images/device-plugin-daemonset.yaml` returns `1`
    - DaemonSet name correct: `grep -F -c 'name: nvidia-device-plugin-daemonset' images/device-plugin-daemonset.yaml` returns `1`
    - Namespace correct: `grep -F -c 'namespace: kube-system' images/device-plugin-daemonset.yaml` returns `1`
    - runtimeClassName: nvidia in pod spec: `grep -F -c 'runtimeClassName: nvidia' images/device-plugin-daemonset.yaml` returns `1`
    - All four args present: `grep -F -c -- '--mig-strategy=none' images/device-plugin-daemonset.yaml` returns `1`; `grep -F -c -- '--fail-on-init-error=true' images/device-plugin-daemonset.yaml` returns `1`; `grep -F -c -- '--pass-device-specs=false' images/device-plugin-daemonset.yaml` returns `1`; `grep -F -c -- '--device-list-strategy=envvar' images/device-plugin-daemonset.yaml` returns `1`
    - Three tolerations present: `grep -c 'nvidia.com/gpu\|node-role.kubernetes.io/master\|node-role.kubernetes.io/control-plane' images/device-plugin-daemonset.yaml` returns at least `3`
    - drop: ["ALL"]: `grep -F -c 'drop: ["ALL"]' images/device-plugin-daemonset.yaml` returns `1`
    - hostPath device-plugins: `grep -F -c '/var/lib/kubelet/device-plugins' images/device-plugin-daemonset.yaml` returns at least `2` (volumeMount path + hostPath)
    - File length sane: `wc -l < images/device-plugin-daemonset.yaml` returns at least `60`
  </acceptance_criteria>
  <done>images/device-plugin-daemonset.yaml exists with verbatim 02-RESEARCH.md §2.3 body: RuntimeClass + DaemonSet (v0.17.4), four args, three tolerations, drop-ALL securityContext, hostPath device-plugins volume.</done>
</task>

<task type="auto">
  <name>Task 3: Activate IMG-04 static bats check (replace skip with real assertion)</name>
  <files>tests/bats/cuda-image-04-device-plugin.bats</files>
  <read_first>
    - tests/bats/cuda-image-04-device-plugin.bats (Wave 0 stub from Plan 02-01 Task 2)
    - images/device-plugin-daemonset.yaml (created in Task 2 above)
    - .planning/phases/02-cluster-layer/02-RESEARCH.md §5 verification table 1.5 (device-plugin manifest baked) — informs the IMG-04 grep pattern
    - Planning context note on DEVICE_PLUGIN_VERSION = v0.17.4 user-approved lock
  </read_first>
  <action>
Modify `tests/bats/cuda-image-04-device-plugin.bats`: REMOVE the `skip "awaiting implementation in plan 02-03 ..."` line and activate the placeholder grep assertion.

Final @test body must read:
```bash
@test "IMG-04: device-plugin manifest pins image v0.17.4" {
  run grep -F 'image: nvcr.io/nvidia/k8s-device-plugin:v0.17.4' "$MANIFEST"
  [ "$status" -eq 0 ]
}
```

Per planning context lock: v0.17.4 is the user-approved pin. REQ-IMG-04 text reads "v0.19.0+" but user has approved v0.17.4 as the current-stable interpretation. Do NOT change the assertion to v0.19.x.

Do NOT modify the setup() block, the load 'test_helper' line, the shebang, or the file header comment. Only the @test body changes.

Do NOT activate the cuda-image-03 (containerd) or cuda-image-05 (manifest path) bats checks here — those require the built image to exist (`docker run --entrypoint cat`) which doesn't happen until Plan 02-04 (build-image.sh) lands. Plan 02-04 owns those activations.
  </action>
  <verify>
    <automated>bats tests/bats/cuda-image-04-device-plugin.bats --tap 2>&1 | tee /tmp/bats-img04.log >/dev/null && grep -cE '^ok ' /tmp/bats-img04.log | grep -q '^1$' && ! grep -q '^not ok' /tmp/bats-img04.log</automated>
  </verify>
  <acceptance_criteria>
    - File retains `load 'test_helper'`: `grep -c "load 'test_helper'" tests/bats/cuda-image-04-device-plugin.bats` returns `1`
    - skip line removed: `grep -c 'skip "awaiting implementation' tests/bats/cuda-image-04-device-plugin.bats` returns `0`
    - Real grep assertion present: `grep -c 'k8s-device-plugin:v0.17.4' tests/bats/cuda-image-04-device-plugin.bats` returns at least `1`
    - Test passes green: `bats tests/bats/cuda-image-04-device-plugin.bats --tap 2>&1 | grep -c '^ok '` returns `1`
    - Zero failures: `bats tests/bats/cuda-image-04-device-plugin.bats --tap 2>&1 | grep -c '^not ok'` returns `0`
    - Phase 1 preflight regression-clean: `bats tests/bats/preflight-*.bats --tap 2>&1 | grep -c '^not ok'` returns `0`
    - IMG-01 / IMG-02 from Plan 02-02 remain green: `bats tests/bats/cuda-image-01-base.bats tests/bats/cuda-image-02-args.bats --tap 2>&1 | grep -c '^not ok'` returns `0`
  </acceptance_criteria>
  <done>IMG-04 static bats check passes green (no skip; real grep assertion verifying device-plugin manifest pins v0.17.4); Phase 1 + Plan 02-02 tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| containerd template input → k3s agent renderer | k3s renders the template into config.toml at agent startup; malformed TOML can DoS the agent (containerd refuses to start) |
| device-plugin DaemonSet pod → kubelet device-plugin socket | DaemonSet runs with hostPath /var/lib/kubelet/device-plugins; trust boundary between plugin pod and host kubelet |
| nvcr.io/nvidia/k8s-device-plugin:v0.17.4 image → spoke-ml node | Image pulled at cluster startup (k3s auto-apply); supply chain trust on nvcr.io accepted (NVIDIA-published) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | T (Tampering) | config.toml.tmpl override syntax | mitigate | Use v3-schema table heading `[plugins.'io.containerd.cri.v1.runtime'.containerd]`; acceptance criteria grep-verify the literal string; containerd 2.0 silently rejects v2 syntax — wrong heading would silently disable the override |
| T-02-13 | D (Denial of service) | malformed config.toml.tmpl | mitigate | Verbatim drop-in from RESEARCH.md §2.2 (no hand-author); k3s base template extension keeps custom surface minimal (3 lines override); upstream {{ template "base" . }} is well-tested |
| T-02-14 | E (Elevation of privilege) | device-plugin DaemonSet privileged surface | accept | DaemonSet must access GPU devices (hostPath /var/lib/kubelet/device-plugins is unavoidable); mitigated by drop ALL capabilities + allowPrivilegeEscalation: false; no privileged: true; runtimeClassName: nvidia gates pod admission to the nvidia runtime |
| T-02-15 | S (Spoofing) | nvcr.io/nvidia/k8s-device-plugin:v0.17.4 image | accept | NVIDIA-published image on official registry; v1 supply-chain trust accepted; cosign attestation + SBOM verification is a v2 concern (TOOL-02) |
| T-02-16 | I (Information disclosure) | env: NVIDIA_VISIBLE_DEVICES=all | accept | Plugin pod needs visibility into all GPUs; spoke-ml is single-node single-GPU; "all" is the canonical setting; operationally equivalent to per-GPU allocation in this scope |
| T-02-17 | T (Tampering) | DEVICE_PLUGIN_VERSION drift between Dockerfile ARG and manifest image tag | mitigate | Both files now pin v0.17.4 explicitly; activated bats check (Task 3) catches the literal pin in the manifest; planner-approved deferral of automated substitution to v2 (RESEARCH.md §7 OQ-1) |
</threat_model>

<verification>
After all 3 tasks complete:

```bash
# Files exist
test -f images/config.toml.tmpl && test -f images/device-plugin-daemonset.yaml

# IMG-03 grep target visible
grep -F 'default_runtime_name = "nvidia"' images/config.toml.tmpl

# IMG-04 grep target visible
grep -F 'image: nvcr.io/nvidia/k8s-device-plugin:v0.17.4' images/device-plugin-daemonset.yaml

# All four images/* files now exist (Plan 02-02 + Plan 02-03 = complete image-layer set)
ls images/
# Expected: Dockerfile.k3s-cuda  config.toml.tmpl  device-plugin-daemonset.yaml  image-tag.sh

# IMG-04 bats check green; static IMG-01/02/04 all passing
bats tests/bats/cuda-image-01-base.bats tests/bats/cuda-image-02-args.bats tests/bats/cuda-image-04-device-plugin.bats --tap

# Phase 1 + Wave 0 still green (no regression)
bats tests/bats/ --tap 2>&1 | grep -c 'not ok'
# Expected: 0
```
</verification>

<success_criteria>
- images/config.toml.tmpl exists with verbatim 02-RESEARCH.md §2.2 body, including {{ template "base" . }} extension and default_runtime_name = "nvidia" override
- images/device-plugin-daemonset.yaml exists with verbatim 02-RESEARCH.md §2.3 body: RuntimeClass + DaemonSet, v0.17.4 pin
- IMG-04 bats test passes green (no skip; real assertion)
- DEVICE_PLUGIN_VERSION pin = v0.17.4 (per planning-context user lock; cited in plan)
- All four images/* files now exist (image layer is complete; Plan 02-04 build-image.sh can run)
- Phase 1 preflight tests still green; Plan 02-02 IMG-01/02 tests still green
</success_criteria>

<output>
After completion, create `.planning/phases/02-cluster-layer/02-03-SUMMARY.md` documenting:
- Both files with line counts
- Confirmation of v0.17.4 pin per planning-context user lock (REQ-IMG-04 text "v0.19.0+" overridden)
- bats run output for cuda-image-04-device-plugin (green)
- Confirmation that all four images/* files now exist (ls listing)
- IMG-03/05 bats checks still skipped (require Plan 02-04 to land before they can be activated)
</output>
