---
phase: 03-flux-hub-bootstrap
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - Taskfile.yml
autonomous: true
requirements: [FLUX-01]
requirements_addressed: [FLUX-01]
tags: [taskfile, wave-1, flux-hub-bootstrap]

must_haves:
  truths:
    - "`Taskfile.yml` contains a `bootstrap-flux` task under the existing `tasks:` map that invokes `bash scripts/bootstrap-flux.sh`."
    - "The task's description line documents the Phase-3 REQ-IDs satisfied (FLUX-01..04)."
    - "The existing 3 tasks (`build-image`, `create-clusters`, `delete-clusters`) are untouched — diff is a strict append."
    - "`task --list` successfully parses the file and lists all 4 tasks (build-image, create-clusters, delete-clusters, bootstrap-flux)."
  artifacts:
    - path: "Taskfile.yml"
      provides: "Appended `bootstrap-flux` task entry; 4 tasks total"
      contains: "bootstrap-flux:"
  key_links:
    - from: "Taskfile.yml:bootstrap-flux"
      to: "scripts/bootstrap-flux.sh"
      via: "bash scripts/bootstrap-flux.sh command"
      pattern: "bash scripts/bootstrap-flux.sh"
---

<objective>
Append a single `bootstrap-flux` task entry to `Taskfile.yml`, preserving the existing 3 Phase-2 tasks verbatim. This makes `task bootstrap-flux` equivalent to `bash scripts/bootstrap-flux.sh` and exposes the Phase 3 deliverable through the same task-runner surface as Phase 2.

Purpose: Single orchestration surface. Every side-effecting Phase script is reachable via `task <name>` so Phase 5's `task rebuild` chain can compose them without shelling through bash paths.

Out of scope:
- `task rebuild` chain wiring — owned by Phase 5 (DESTROY-05/06).
- Full Taskfile.yml REPO-05 reorganization (task grouping, `desc:` hygiene audit, confirmation-required markers on destructive tasks) — owned by Phase 6.
- Any change to the existing `build-image` / `create-clusters` / `delete-clusters` entries.

Output: `Taskfile.yml` with one appended block; zero diff on the existing 3 entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-flux-hub-bootstrap/03-CONTEXT.md
@.planning/phases/03-flux-hub-bootstrap/03-PATTERNS.md
@Taskfile.yml

<interfaces>
<!-- Current state of Taskfile.yml (lines 1-17 — self-analog). -->
```yaml
version: '3'

tasks:
  build-image:
    desc: Build the karyon/k3s-cuda custom image (IMG-01..06)
    cmds:
      - bash scripts/build-image.sh

  create-clusters:
    desc: Create k8s-net + three k3d clusters (CLU-01..05)
    cmds:
      - bash scripts/create-clusters.sh

  delete-clusters:
    desc: Remove clusters + k8s-net; preserve karyon/k3s-cuda image (CLU-06, D-14)
    cmds:
      - bash scripts/delete-clusters.sh
```

<!-- Task convention (the existing 3 entries as the analog): -->
- Each task block: `<name>:` then `desc: <one-line summary with REQ-IDs in parens>` then `cmds:` then `- bash scripts/<script>.sh`
- 2-space indentation throughout (YAML)
- Blank line between task blocks
- Task name is the script name minus `.sh` extension (convention)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append bootstrap-flux task entry to Taskfile.yml</name>
  <files>Taskfile.yml</files>
  <read_first>
    - Taskfile.yml (read the current state in full — the entire file is 18 lines; confirm indentation, blank-line rhythm, and `desc:`/`cmds:`/command conventions before editing)
    - .planning/phases/03-flux-hub-bootstrap/03-PATTERNS.md §`Taskfile.yml` (the verbatim append skeleton)
    - .planning/phases/03-flux-hub-bootstrap/03-CONTEXT.md §code_context (Phase 3 appends one entry; REPO-05 reorg remains Phase 6)
  </read_first>
  <action>
Append the following YAML block to `Taskfile.yml` as the last task under the `tasks:` map. Use the Edit tool to insert after the final `- bash scripts/delete-clusters.sh` line (the current last line of the file).

The exact block to append (preserving the leading blank line for visual separation between task entries — matches the existing convention between `build-image` / `create-clusters` / `delete-clusters`):

```yaml

  bootstrap-flux:
    desc: Bootstrap Flux GitOps on k3d-hub-flux from clusters/hub-flux/ (FLUX-01..04)
    cmds:
      - bash scripts/bootstrap-flux.sh
```

Indentation: 2 spaces for the task name, 4 spaces for `desc:` and `cmds:`, 6 spaces for the `- bash ...` list item. Match the existing `delete-clusters:` block indentation exactly.

Do NOT:
- Rewrite the file from scratch (violates "strict append" invariant)
- Touch the existing `build-image` / `create-clusters` / `delete-clusters` blocks
- Add a trailing newline policy change (preserve the file's existing final-newline convention)
- Introduce `deps:` / `preconditions:` / `silent:` / `confirmation:` keys (those are Phase 5/6 concerns)
- Reorder the task entries (append-only)

After the edit, the file's first 18 lines must be byte-identical to the current file content (verifiable via `diff`). The new block starts at line 19 (or line 20 if the current file ends with a single blank line — check first).
  </action>
  <verify>
    <automated>bash -c 'cd /home/rich/code/gsd/karyon && grep -qF "bootstrap-flux:" Taskfile.yml && grep -qF "bash scripts/bootstrap-flux.sh" Taskfile.yml && grep -qF "Bootstrap Flux GitOps on k3d-hub-flux" Taskfile.yml && yq eval ".tasks | keys" Taskfile.yml 2>/dev/null | grep -c -E "build-image|create-clusters|delete-clusters|bootstrap-flux"'</automated>
  </verify>
  <acceptance_criteria>
    - File parses as valid YAML: `yq eval "." Taskfile.yml >/dev/null` exits 0 (or python equivalent if yq missing)
    - All 4 tasks present: `yq eval ".tasks | keys" Taskfile.yml` lists exactly `build-image`, `create-clusters`, `delete-clusters`, `bootstrap-flux` (order may vary — yq sorts alphabetically in some versions; the `keys` list length is 4)
    - Alternate: `grep -c '^  [a-z][a-z-]*:$' Taskfile.yml` equals 4 (4 task-name headers at 2-space indent followed by colon)
    - bootstrap-flux task block exists with correct shape: `grep -A3 '^  bootstrap-flux:$' Taskfile.yml` shows `desc:` + `cmds:` + `- bash scripts/bootstrap-flux.sh`
    - Command literal: `grep -qF '      - bash scripts/bootstrap-flux.sh' Taskfile.yml` (6-space indent prefix matches existing convention)
    - Description contains FLUX REQ-ID reference: `grep -qF 'FLUX-01..04' Taskfile.yml`
    - Existing tasks untouched: `grep -qF 'Build the karyon/k3s-cuda custom image (IMG-01..06)' Taskfile.yml` AND `grep -qF 'Create k8s-net + three k3d clusters (CLU-01..05)' Taskfile.yml` AND `grep -qF 'Remove clusters + k8s-net; preserve karyon/k3s-cuda image (CLU-06, D-14)' Taskfile.yml`
    - `task --list` succeeds (if `task` CLI is installed per `.tool-versions`): `task --list 2>/dev/null | grep -E '^(\* )?bootstrap-flux'` matches; 4 task entries listed total
    - No new top-level keys introduced: `grep -E '^[a-z]' Taskfile.yml` only matches `version:` and `tasks:` (the two existing top-level keys)
    - Version unchanged: `grep -qxF "version: '3'" Taskfile.yml`
  </acceptance_criteria>
  <done>
`Taskfile.yml` contains a new `bootstrap-flux` task that invokes `bash scripts/bootstrap-flux.sh` with a FLUX-01..04 attribution `desc:` line; the existing 3 tasks are byte-unchanged; `task --list` (if installed) reports 4 tasks.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| task runner → script | `task bootstrap-flux` spawns `bash scripts/bootstrap-flux.sh`; identical shell-out semantics to running the script directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-03 | T (Tampering) | Developer invokes `task bootstrap-flux` with the wrong kubectl context | mitigate (inherited) | The script's Section 2 kubectl context gate (Plan 02) is the single gate for this boundary; the Taskfile entry is a thin command invocation, no additional gates required. Phase 6 REPO-06 "destructive tasks named explicitly" audit may add a `confirmation:` prompt to bootstrap-flux if the UAT surfaces an accidental-invocation risk, but Phase 3 does not pre-solve this — `bootstrap-flux` is not destructive (idempotent, read-safe on a healthy cluster; fresh-bootstrap only creates resources). |
</threat_model>

<verification>
After Task 1 completes:

```bash
cd /home/rich/code/gsd/karyon && \
  yq eval '.tasks | keys' Taskfile.yml && \
  task --list 2>&1 | head -10
```

Expected:
- yq lists 4 task keys: `[build-image, bootstrap-flux, create-clusters, delete-clusters]`
- `task --list` shows 4 rows, each with the task name and its `desc:` content
- Zero diff on the existing 3 tasks — verify with `git diff Taskfile.yml` showing only added lines

(If `task` CLI is not on PATH, fall back to `yq eval` for the keys list.)
</verification>

<success_criteria>
- [ ] `Taskfile.yml` contains a `bootstrap-flux` task entry invoking `bash scripts/bootstrap-flux.sh`
- [ ] The 3 existing task entries (`build-image`, `create-clusters`, `delete-clusters`) are byte-unchanged
- [ ] YAML parses cleanly (`yq eval '.' Taskfile.yml`)
- [ ] `task --list` (when installed) reports 4 tasks
- [ ] No new top-level keys; `version: '3'` unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/03-flux-hub-bootstrap/03-04-SUMMARY.md` per the summary template, recording:
- File modified: Taskfile.yml — appended 5 lines (blank + task entry)
- 4 task entries total; 3 existing untouched
- Reference Plan 02 (script deliverable that `bootstrap-flux` invokes)
</output>
