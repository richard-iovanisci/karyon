---
phase: 03-flux-hub-bootstrap
plan: 06
type: execute
wave: 3
depends_on: ["03-05"]
files_modified:
  - tests/bats/bootstrap-flux-01-idempotent.bats
autonomous: false
gap_closure: true
requirements: [FLUX-04]
requirements_addressed: [FLUX-01, FLUX-04]
tags: [flux, bats, live-destructive, gap-closure, wave-3, flux-hub-bootstrap, human-verification]

must_haves:
  truths:
    - "The combined live/destructive @test in `tests/bats/bootstrap-flux-01-idempotent.bats` ALREADY contains the correct assertions for the post-Plan-03-05 script: it asserts run-1 exits 0, `${KUST_FILE}` exists, marker is present after run 1, run-2 exits 0 with the `already done, skipping: flux bootstrap` skip-message, and marker is preserved after run 2 (lines 101-125). After Plan 03-05 lands, no logical change to the @test is required — only a possible adjustment to the dirty-worktree precondition or to the `${KUST_FILE}` existence check timing if Plan 03-05's sync sub-step changes the order in which the marker becomes visible."
    - "The @test correctly gates on `require_live_destructive` (line 102) which double-gates on both `KARYON_LIVE_TESTS=1` AND `KARYON_LIVE_TESTS_DESTRUCTIVE=1` (per `tests/bats/test_helper.bash:45-49`). This guard is preserved verbatim — no test runs against the real cluster without both env vars set."
    - "A human verification step (autonomous: false checkpoint) runs the live destructive bats with both gates set, captures the TAP output, and asserts both invocations of the script exit 0 with the marker preserved. The checkpoint blocks the plan from completing until the human pastes back the captured TAP output."
    - "Before invoking the live test, a precondition step verifies that `git -C \"${REPO_ROOT}\" diff --quiet -- clusters/hub-flux` and `git -C \"${REPO_ROOT}\" diff --cached --quiet -- clusters/hub-flux` BOTH exit 0 — i.e. the `${FLUX_PATH}` worktree is clean — so the new Plan 03-05 dirty-worktree guard does NOT fire during the live proof. If the precondition fails, the human is told the exact `git status` and `git stash` commands to run before retrying."
  context:
    - "This plan does NOT replan the verification of FLUX-04. It executes the existing test contract Plan 03-01 already wrote, against the now-corrected script Plan 03-05 produced. The verification step in the test was reviewed against the post-fix script behavior and is correct AS-WRITTEN: the @test does not check for any specific local-pre-bootstrap state, it just runs the script twice and asserts the script's contract holds."
    - "An optional minor adjustment to the @test may be needed: if the developer's working tree happens to have `clusters/hub-flux/flux-system/kustomization.yaml` already committed locally before run 1, Plan 03-05's sync step is a fast-no-op (`pull --ff-only` returns up-to-date) and the marker check is identical to the original assertion. If the local checkout is missing the file, Plan 03-05 syncs it before the marker prepend, and the @test's existing `[ -f \"$KUST\" ]` assertion still holds AFTER run 1. No assertion change is needed in either case — Task 1 below VERIFIES this is the case and only edits the test if the executor finds an actual mismatch."
    - "Out-of-scope: re-deriving FLUX-04 truths or modifying any other Plan-03 artifact beyond the bats file (which itself only changes if the assertion-vs-post-fix-behavior check in Task 1 surfaces a real mismatch). The script `scripts/bootstrap-flux.sh` MUST NOT be modified in this plan — Plan 03-05 owns that file."
  artifacts:
    - path: "tests/bats/bootstrap-flux-01-idempotent.bats"
      provides: "Static + live/destructive contract for FLUX-01..04. After this plan, the live destructive @test is verified to be runnable end-to-end on a real k3d-hub-flux cluster via the human-verification checkpoint."
      contains: "FLUX-04 + D-13 (live/destructive, combined)"
      min_lines: 100
  key_links:
    - from: "tests/bats/bootstrap-flux-01-idempotent.bats"
      to: "scripts/bootstrap-flux.sh"
      via: "run bash \"$SCRIPT\" (twice, with \"$status\" eq 0 assertions)"
      pattern: "run bash \"\\\$SCRIPT\""
    - from: "tests/bats/bootstrap-flux-01-idempotent.bats"
      to: "tests/bats/test_helper.bash"
      via: "require_live_destructive gate"
      pattern: "require_live_destructive"
---

<objective>
Close the third Phase 3 verification gap (Gap C) — produce the LIVE destructive proof that bootstrap is idempotent end-to-end on a real k3d-hub-flux cluster, after Plans 03-05 (CR-01 + CR-02) have landed.

The static skip-logic gate is correct, the test contract is correct, but the proof has not been run because:
1. The first-run path was broken (Gap A / CR-01 — fixed by Plan 03-05 Task 2).
2. `.env` was sourced as shell (Gap B / CR-02 — fixed by Plan 03-05 Task 1).
3. The two destructive-gate envs (`KARYON_LIVE_TESTS=1 KARYON_LIVE_TESTS_DESTRUCTIVE=1`) require the human to set them before invocation — by design, since the test mutates the real cluster and pushes commits to the real GitHub repo.

Purpose: Run the destructive @test, capture deterministic exit-0 evidence on rerun, and confirm `# FLUX PATCH SURFACE` survives both invocations. This is the final evidence that closes FLUX-01 + FLUX-04 in the verification report.

Output: Captured TAP output from `bats tests/bats/bootstrap-flux-01-idempotent.bats --tap` with both gate envs set, pasted into `03-06-SUMMARY.md` and into a checkpoint resolution. No code changes unless Task 1 finds a genuine assertion mismatch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-flux-hub-bootstrap/03-CONTEXT.md
@.planning/phases/03-flux-hub-bootstrap/03-VERIFICATION.md
@.planning/phases/03-flux-hub-bootstrap/03-REVIEW.md
@.planning/phases/03-flux-hub-bootstrap/03-01-PLAN.md
@.planning/phases/03-flux-hub-bootstrap/03-01-SUMMARY.md
@.planning/phases/03-flux-hub-bootstrap/03-05-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify the live/destructive @test contract is correct against the post-Plan-03-05 script (no-op edit only if a real mismatch is found)</name>
  <files>tests/bats/bootstrap-flux-01-idempotent.bats</files>
  <read_first>
    - tests/bats/bootstrap-flux-01-idempotent.bats — read end-to-end. The live/destructive @test is at lines 101-125 — analyze every assertion line against the now-patched script.
    - scripts/bootstrap-flux.sh — read end-to-end AFTER Plan 03-05 has landed. Specifically inspect Section 5 (the marker prepend) — confirm that after Task 2 of Plan 03-05, the marker still ends up in `${KUST_FILE}` after run 1, and is still present after run 2. Confirm the D-07 skip-message string `already done, skipping: flux bootstrap` is unchanged.
    - .planning/phases/03-flux-hub-bootstrap/03-05-PLAN.md — read the truths and the Section 5 sync sub-step. The sync sub-step happens BEFORE the existing sentinel-grep, so the marker is prepended on run 1 (after the local checkout reflects bootstrap manifests).
    - .planning/phases/03-flux-hub-bootstrap/03-VERIFICATION.md — the "Human Verification Required" section (lines 137-148) is the source of truth for the expected live-test outcome.
    - tests/bats/test_helper.bash — verify `require_live_destructive` still requires both env vars (KARYON_LIVE_TESTS=1 AND KARYON_LIVE_TESTS_DESTRUCTIVE=1).
  </read_first>
  <action>
    Open `tests/bats/bootstrap-flux-01-idempotent.bats` and inspect the live/destructive @test (currently lines 101-125). For EACH of the following assertions, confirm whether it still holds after Plan 03-05:

    1. **Run 1 exits 0** — `[ "$status" -eq 0 ]` after `run bash "$SCRIPT"`.
       Expected: HOLDS. Plan 03-05 Task 2 added a sync step that produces `pass "fast-forward sync complete..."` and the marker prepend still runs. No new exit-1 paths on the happy path.

    2. **`[ -f "$KUST" ]`** after run 1.
       Expected: HOLDS. Plan 03-05 Task 2 ensures `${KUST_FILE}` exists locally after run 1 by syncing `origin/main` BEFORE the marker prepend.

    3. **`grep -F -- '# FLUX PATCH SURFACE' "$KUST"` exits 0** after run 1.
       Expected: HOLDS. Plan 03-05 does not change the marker block itself (D-13 invariant). After sync + prepend, the marker is in `${KUST_FILE}`.

    4. **Run 2 exits 0 AND output contains `already done, skipping: flux bootstrap`**.
       Expected: HOLDS. Plan 03-05 does not change the D-07 idempotency gate (Section 2 is unchanged). On a healthy hub, run 2 short-circuits at the same three-part AND.

    5. **`grep -F -- '# FLUX PATCH SURFACE' "$KUST"` exits 0** after run 2.
       Expected: HOLDS. Plan 03-05's Section 5 sync sub-step is wrapped in `if [[ ! -f "${KUST_FILE}" ]]` — on run 2 the file already exists, so the sync sub-step is a fast no-op AND the existing sentinel-grep idempotency gate prevents double-prepend. Marker survives.

    DECISION GATE:
    - **If all 5 assertions hold against the post-Plan-03-05 script** (the expected case): DO NOT EDIT THE TEST. The plan's Task 1 is "verify and document" only. Add a one-line comment INSIDE the `setup()` block (NOT inside the @test body, to avoid disturbing the existing assertions) recording that the contract was reviewed against the patched script. Specifically, INSERT a single line after the `setup() {` line:

      ```bash
        # 2026-04-26 (Plan 03-06): contract reviewed against post-Plan-03-05 script;
        # all live/destructive assertions hold without modification.
      ```

    - **If any assertion does NOT hold** (unexpected): pause and escalate. Do NOT edit the @test silently — the gap-closure scope of Plan 03-06 is to RUN the existing proof, not to redesign it. Open a separate `gsd-debug` invocation if the executor finds a real mismatch, then revisit this plan.

    Verify the file still parses with `bats --count`.

    Commit message (exact form):
    ```
    docs(03-06): note live/destructive contract reviewed against patched script
    ```

    (If the decision gate triggers an actual edit because of a real mismatch, the commit message becomes:
    ```
    fix(03-06): adjust live/destructive @test for post-Plan-03-05 sync timing
    ```
    — but this branch should NOT be needed and is documented only for completeness.)
  </action>
  <verify>
    <automated>bash -c 'set -e; cd /home/rich/code/gsd/karyon; bats --count tests/bats/bootstrap-flux-01-idempotent.bats && bats tests/bats/bootstrap-flux-01-idempotent.bats --tap | grep -F "ok " | wc -l'</automated>
  </verify>
  <acceptance_criteria>
    - `bats --count tests/bats/bootstrap-flux-01-idempotent.bats` returns `8` (7 static @tests + 1 live/destructive @test that skips without env gates) — same as before.
    - `bats tests/bats/bootstrap-flux-01-idempotent.bats --tap | grep -c '^ok '` returns `8` (all 7 static @tests pass; the live/destructive @test reports `ok ... # skip` which still counts as ok in TAP).
    - The verification comment in `setup()` is present:
      `grep -F 'contract reviewed against post-Plan-03-05 script' tests/bats/bootstrap-flux-01-idempotent.bats` exits 0.
    - The live/destructive @test body is byte-identical to its pre-Plan-03-06 form unless a documented mismatch was found (in which case the commit message reflects the `fix(03-06)` branch).
  </acceptance_criteria>
  <done>
    The live/destructive @test contract has been reviewed assertion-by-assertion against the patched script and confirmed to be correct without modification (or — only if a real mismatch was found — narrowly adjusted with a clear commit-message rationale). One commit is added with the appropriate message.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — run the live/destructive bootstrap idempotency proof on the real k3d-hub-flux cluster</name>
  <files>tests/bats/bootstrap-flux-01-idempotent.bats (read-only — invoked, not modified); scripts/bootstrap-flux.sh (read-only — invoked, not modified); clusters/hub-flux/flux-system/kustomization.yaml (mutated by flux bootstrap and by the script's Section 5 marker prepend during the live run, NOT a code-edit).</files>
  <action>
    This is a `checkpoint:human-verify` task. The execute-plan agent MUST NOT run the live destructive bats itself — both gating env vars (`KARYON_LIVE_TESTS=1` AND `KARYON_LIVE_TESTS_DESTRUCTIVE=1`) require explicit human consent because the test mutates the real `k3d-hub-flux` cluster AND pushes commits to the real GitHub repository.

    The agent's ONLY action here is to PRESENT the checkpoint to the human (the contents of `<what-built>` and `<how-to-verify>` below) and PAUSE for the resume signal. After the human pastes back the captured TAP output (and supporting outputs) per `<resume-signal>`, the agent records that evidence into `03-06-SUMMARY.md` per the `<output>` block at the bottom of this plan and creates the final commit with the message specified there.

    Do NOT proceed past this checkpoint without an `approved` resume signal that includes pasted live TAP output containing `ok 8 FLUX-04 + D-13 (live/destructive, combined)` and zero `not ok` lines.
  </action>
  <verify>
    <automated>bash -c 'set -e; cd /home/rich/code/gsd/karyon; test -f /tmp/karyon-03-06-live.tap && grep -F "ok 8 FLUX-04 + D-13 (live/destructive, combined)" /tmp/karyon-03-06-live.tap && [ "$(grep -c "^not ok" /tmp/karyon-03-06-live.tap)" -eq 0 ] && grep -F "# FLUX PATCH SURFACE" clusters/hub-flux/flux-system/kustomization.yaml'</automated>
  </verify>
  <done>
    The human ran the live destructive bats with both gate envs set; `/tmp/karyon-03-06-live.tap` contains `ok 8 FLUX-04 + D-13 (live/destructive, combined)` and zero `not ok` lines; `# FLUX PATCH SURFACE` is present in `clusters/hub-flux/flux-system/kustomization.yaml`; `flux get kustomizations -A` shows `flux-system Ready=True`. The TAP output and supporting evidence are pasted into the resume signal and committed to `03-06-SUMMARY.md`.
  </done>
  <what-built>
    Plan 03-05 patched `scripts/bootstrap-flux.sh` to (a) load `.env` as data, not as shell, and (b) sync the local checkout from `origin/main` before applying the patch-surface marker. Plan 03-06 Task 1 confirmed the existing live/destructive @test contract still applies. This checkpoint runs the actual destructive proof against the real `k3d-hub-flux` cluster — the only step the verifier (03-VERIFICATION.md "Human Verification Required" section) said must be human-driven because it mutates the cluster AND pushes commits to a real GitHub repository.
  </what-built>
  <how-to-verify>
    Run these steps in order. Stop and report any deviation:

    **Precondition checks (script-driven; safe to run anytime):**

    1. From the repo root (`/home/rich/code/gsd/karyon`), confirm the worktree under `clusters/hub-flux/` is clean:
       ```bash
       git -C . diff --quiet -- clusters/hub-flux && git -C . diff --cached --quiet -- clusters/hub-flux && echo "WORKTREE_CLEAN_UNDER_FLUX_PATH=YES" || echo "WORKTREE_DIRTY_UNDER_FLUX_PATH=YES — stash or commit first"
       ```
       Expected output: `WORKTREE_CLEAN_UNDER_FLUX_PATH=YES`.
       If dirty: run `git -C . status -- clusters/hub-flux` and either `git -C . stash push -- clusters/hub-flux` (recoverable) or `git -C . add clusters/hub-flux && git -C . commit -m 'wip: pre-bootstrap snapshot'`. Re-run the precondition check.

    2. Confirm `.env` has all three keys non-empty (does NOT print values):
       ```bash
       for k in GITHUB_OWNER GITHUB_REPO GITHUB_TOKEN; do
         if grep -E "^${k}=." .env >/dev/null 2>&1; then echo "${k}=PRESENT"; else echo "${k}=MISSING"; fi
       done
       ```
       Expected: all three lines say `=PRESENT`. NEVER print values — the variable name + presence/missing is sufficient.

    3. Confirm `k3d-hub-flux` cluster exists and is the current kubectl context:
       ```bash
       kubectl config current-context
       k3d cluster list | grep -F hub-flux
       ```
       Expected: `k3d-hub-flux` is the current context AND `hub-flux` appears in the cluster list with `1/1` servers running.

    **Live destructive run (mutates cluster + GitHub repo):**

    4. Run the destructive bats with both gates set, capturing TAP output:
       ```bash
       KARYON_LIVE_TESTS=1 KARYON_LIVE_TESTS_DESTRUCTIVE=1 \
         bats tests/bats/bootstrap-flux-01-idempotent.bats --tap | tee /tmp/karyon-03-06-live.tap
       ```
       Expected: every line starting with `ok ` and NO `not ok` lines. Look specifically for:
       - `ok 8 FLUX-04 + D-13 (live/destructive, combined): bootstrap is idempotent AND patch-surface marker survives re-run` — this is the line that proves Gap C closed.

    5. Independently confirm the marker landed and persisted:
       ```bash
       head -3 clusters/hub-flux/flux-system/kustomization.yaml
       ```
       Expected: the first non-empty line is `# ──────────────────────────────────────────────────────────────────────────` and the second is `# FLUX PATCH SURFACE`.

    6. Confirm Flux is healthy on the cluster:
       ```bash
       flux --context k3d-hub-flux check
       flux --context k3d-hub-flux get kustomizations -A
       ```
       Expected: `flux check` exits 0; the root `flux-system` Kustomization shows `Ready=True`. This closes the Phase 3 goal command from 03-VERIFICATION.md.

    7. Paste the full contents of `/tmp/karyon-03-06-live.tap` AND the `head -3` output AND the `flux get kustomizations -A` output into the resume signal below.
  </how-to-verify>
  <resume-signal>
    Reply with one of:

    - `approved — TAP shows ok 8 (FLUX-04 + D-13 combined) and all preconditions passed`, FOLLOWED by the pasted contents of `/tmp/karyon-03-06-live.tap` (full TAP output), the `head -3 clusters/hub-flux/flux-system/kustomization.yaml` output, and the `flux get kustomizations -A` output.

    - `precondition failed: <which step>` (1, 2, or 3 above) followed by the exact remediation taken; then re-run the checkpoint after fixing.

    - `live test failed: <not-ok line(s)>` followed by the full TAP output. This BLOCKS the plan from completing; escalate via `/gsd-debug 3` to triage the root cause before retrying.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → real `k3d-hub-flux` cluster | Tests mutate the live cluster: install Flux controllers, create the `flux-system` namespace, apply CRDs. Idempotent on rerun (D-07 gate), but NOT on first run when the cluster is empty. |
| Test runner → GitHub repository | First run pushes commits to `${GITHUB_OWNER}/${GITHUB_REPO}`'s `main` branch (the bootstrap commit creating `clusters/hub-flux/flux-system/{gotk-components.yaml,gotk-sync.yaml,kustomization.yaml}`). Visible in repo history, not destructive but not silent. |
| Live test execution → developer's local checkout | Plan 03-05's sync step pulls `origin/main` into the local working tree. Cleared by precondition step 1. |

## STRIDE Threat Register (ASVS L1; block on HIGH)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-06-01 | Tampering | Live destructive test running against the WRONG cluster | mitigate (HIGH) | Two-layer defense: (a) `require_live_destructive` requires BOTH `KARYON_LIVE_TESTS=1` AND `KARYON_LIVE_TESTS_DESTRUCTIVE=1` (test_helper.bash:45-49) — neither alone is sufficient; (b) the script itself's D-06 kubectl context gate (`scripts/bootstrap-flux.sh:70-76`) hard-fails if `current-context` is not `k3d-hub-flux`. Both gates must be intact before the human runs the checkpoint; the precondition step 3 explicitly verifies the kubectl context. ASVS V14.5.2 (segregation of test environments). |
| T-3-06-02 | Information Disclosure | Token leak via TAP output capture | mitigate (HIGH) | The bats `--tap` output captures stdout/stderr of the @test, NOT of the underlying script (the @test redirects via `>/dev/null 2>&1` for the first invocation and `run bash "$SCRIPT"` for the second — `run` captures into `$output`, but the static negative-grep @test enforces the script never funnels `$GITHUB_TOKEN` through any output helper). The `/tmp/karyon-03-06-live.tap` file is owner-readable by default; the precondition step explicitly forbids printing the values of `.env` keys (only key NAMES + PRESENT/MISSING). ASVS V7.4.1. |
| T-3-06-03 | Repudiation | Bootstrap commit pushed without developer awareness | accept | This is the documented, intended side-effect of `flux bootstrap github`. The precondition step 2 surfaces that `.env` keys are non-empty and the human must read the checkpoint description acknowledging the test "creates/updates the GitHub repo" (already in the @test header comment lines 96-97). No additional mitigation needed — single-developer lab, expected behavior. |
| T-3-06-04 | DoS | Live test hangs against an unreachable apiserver | accept | `kubectl wait --for=condition=Available --timeout=120s` is the longest single-step timeout in the script (D-10). Total worst-case wall-clock is bounded by 120s + flux-bootstrap-default-timeout (~2-3 min). Acceptable for a one-shot human-driven proof; no retry loop. |

## ASVS L1 Coverage

- V14.5.2 (Segregation of test environments) — addressed by T-3-06-01 double-gated `require_live_destructive` + script-level kubectl context gate.
- V7.4.1 (Secrets not logged) — addressed by T-3-06-02; reinforced by the precondition step's "key=PRESENT/MISSING only" pattern.
</threat_model>

<verification>
After the human checkpoint resolves with `approved`:

1. **TAP contains the FLUX-04 ok line:**
   ```bash
   grep -F 'ok 8 FLUX-04 + D-13 (live/destructive, combined)' /tmp/karyon-03-06-live.tap
   ```
   MUST exit 0.

2. **TAP contains zero `not ok` lines:**
   ```bash
   grep -c '^not ok' /tmp/karyon-03-06-live.tap
   ```
   MUST output `0`.

3. **Marker present in the bootstrap-managed file:**
   ```bash
   grep -F '# FLUX PATCH SURFACE' clusters/hub-flux/flux-system/kustomization.yaml
   ```
   MUST exit 0.

4. **Phase 3 goal command:**
   ```bash
   flux --context k3d-hub-flux get kustomizations -A
   ```
   MUST list the `flux-system` Kustomization with `Ready=True`.
</verification>

<success_criteria>
- The live/destructive @test executed end-to-end (not skipped) and reported `ok 8 ... FLUX-04 + D-13 ...` in TAP output.
- Both invocations of the script during the @test exited 0; the second invocation's output contained `already done, skipping: flux bootstrap`.
- `# FLUX PATCH SURFACE` is present in `clusters/hub-flux/flux-system/kustomization.yaml` after the live run.
- `flux --context k3d-hub-flux get kustomizations -A` reports `flux-system` `Ready=True` — closes the Phase 3 goal command.
- The captured TAP + marker check + flux-get output is committed to `03-06-SUMMARY.md` (a single commit at the end of the plan).
- Phase 3 verification re-run will move all three failed truths in 03-VERIFICATION.md to PASSED.
</success_criteria>

<output>
After the checkpoint resolves with `approved`, create `.planning/phases/03-flux-hub-bootstrap/03-06-SUMMARY.md` containing:
- Pasted full TAP output from `/tmp/karyon-03-06-live.tap`.
- Pasted output of `head -3 clusters/hub-flux/flux-system/kustomization.yaml`.
- Pasted output of `flux --context k3d-hub-flux get kustomizations -A`.
- Pasted output of `flux --context k3d-hub-flux check`.
- A short paragraph mapping each captured artifact to the failed truth in 03-VERIFICATION.md it closes (truths #1, #3, #4).
- A statement that Plans 03-05 + 03-06 collectively close all three Phase 3 verification gaps and the phase is ready for re-verification via `/gsd-verify-work 3`.

Final commit: `docs(03-06): record live/destructive idempotency proof and close Phase 3 gaps`.
</output>
