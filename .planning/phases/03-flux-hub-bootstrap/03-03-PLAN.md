---
phase: 03-flux-hub-bootstrap
plan: 03
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - docs/flux-hub-spoke.md
autonomous: true
requirements: [FLUX-05]
requirements_addressed: [FLUX-05]
tags: [docs, flux, wave-1, flux-hub-bootstrap]

must_haves:
  truths:
    - "`docs/flux-hub-spoke.md` exists at the repo root and is version-controlled."
    - "The doc explicitly states: `gotk-components.yaml` and `gotk-sync.yaml` are bootstrap-managed (do not hand-edit); `kustomization.yaml` IS the supported patch surface; `--path` is effectively immutable after first run."
    - "The doc contains a concrete, copy-pasteable `patches:` example bumping kustomize-controller's memory limit to 1Gi (D-15)."
    - "The doc references the exact skip-message contract from `scripts/bootstrap-flux.sh`: `already done, skipping: flux bootstrap` (D-16)."
    - "The doc contains an HTML-comment stub for the Phase 4 hub-spoke reconciliation section, left intentionally empty (D-14)."
    - "The 4 static @tests in `tests/bats/bootstrap-flux-02-docs.bats` flip from RED to GREEN after this plan lands."
  artifacts:
    - path: "docs/flux-hub-spoke.md"
      provides: "The Phase 3 patch-surface contract documentation (FLUX-05 / DOCS-03)"
      contains: "patch surface"
      min_lines: 50
  key_links:
    - from: "docs/flux-hub-spoke.md"
      to: "scripts/bootstrap-flux.sh"
      via: "cross-reference to the skip-message literal + the hard-coded --path clusters/hub-flux"
      pattern: "already done, skipping: flux bootstrap"
    - from: "docs/flux-hub-spoke.md"
      to: "clusters/hub-flux/flux-system/kustomization.yaml"
      via: "documents the in-file FLUX PATCH SURFACE comment as the user-visible contract"
      pattern: "kustomization.yaml"
---

<objective>
Create `docs/flux-hub-spoke.md` — the Phase-3-scoped patch-surface contract doc required by FLUX-05 / DOCS-03 / ROADMAP Phase 3 Success #4. Three rules, one concrete patch example, one reference to the script's skip-message, and an HTML-comment stub marker for Phase 4's forthcoming hub-spoke reconciliation section.

Purpose: Document the rules a future reader of `clusters/hub-flux/flux-system/` needs to know BEFORE hand-editing anything in that tree. Specifically correct the overly-broad "do not hand-edit" starter guidance by making explicit that `kustomization.yaml` IS the supported patch surface, and that `--path` is effectively immutable after first run.

Output: A new markdown file `docs/flux-hub-spoke.md` (~60-120 lines) that flips the 4 static @tests in `tests/bats/bootstrap-flux-02-docs.bats` from RED to GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-flux-hub-bootstrap/03-CONTEXT.md
@.planning/phases/03-flux-hub-bootstrap/03-RESEARCH.md
@.planning/phases/03-flux-hub-bootstrap/03-PATTERNS.md
@docs/wsl-networking.md
@tests/bats/bootstrap-flux-02-docs.bats

<interfaces>
<!-- From tests/bats/bootstrap-flux-02-docs.bats (landed by Plan 01) — the test contract. -->

The following 4 static @tests grep this file:

1. FLUX-05 / DOCS-03: 3 patch-surface rules. All of these literals MUST be present:
   - `gotk-components.yaml` (fixed string)
   - `gotk-sync.yaml` (fixed string)
   - `bootstrap-managed` (fixed string)
   - Case-insensitive regex match for `patch surface` (any capitalization OK)
   - `--path` (fixed string, exact dashes)
   - Case-insensitive match for `immutable` (any capitalization OK)

2. D-15: kustomize-controller memory patch example. All present:
   - `kustomize-controller` (fixed string)
   - `/spec/template/spec/containers/0/resources/limits/memory` (fixed JSON pointer literal)
   - `1Gi` (fixed string — must appear as the replacement value)

3. D-16: skip-message cross-reference. Exact literal `already done, skipping: flux bootstrap` must appear.

4. D-14: Phase 4 stub. The literal `<!-- Phase 4:` must appear (HTML comment marker).

<!-- From docs/wsl-networking.md — the only existing analog doc under docs/. -->
<!-- The shape to mirror: top-level `#` heading, short intro paragraph, then `## Rule/Section` subheadings. -->

Existing tone (docs/wsl-networking.md lines 1-9):
```markdown
# WSL2 Networking: Mirrored Mode, NAT Mode, and the Migration Procedure

## What Is mirrored Mode and Why It Breaks Docker Custom Networks

WSL2 supports two networking modes: **NAT** (the default prior to Windows 11 22H2) and
**mirrored** (introduced in Windows 11 22H2). In mirrored mode, Windows host NICs are mirrored
directly into the WSL2 VM — the WSL2 instance shares the same IP addresses and network interfaces
as the Windows host.
```

`docs/flux-hub-spoke.md` follows the same shape: H1 title, short intro, H2 subsections. No TOC (research Open Question 5 resolves as "keep flat").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/flux-hub-spoke.md with 3 rules + D-15 example + D-16 skip-message + D-14 Phase-4 stub</name>
  <files>docs/flux-hub-spoke.md</files>
  <read_first>
    - docs/wsl-networking.md (tone + structure analog — the only existing doc under docs/)
    - tests/bats/bootstrap-flux-02-docs.bats (confirm EXACT literals the tests grep for: `bootstrap-managed`, `gotk-components.yaml`, `gotk-sync.yaml`, `patch surface` (case-insensitive), `--path`, `immutable` (case-insensitive), `kustomize-controller`, `/spec/template/spec/containers/0/resources/limits/memory`, `1Gi`, `already done, skipping: flux bootstrap`, `<!-- Phase 4:`)
    - .planning/phases/03-flux-hub-bootstrap/03-CONTEXT.md §decisions D-13, D-14, D-15, D-16 + §specifics section skeleton (What flux bootstrap writes / Rule 1 / Rule 2 with D-15 example / Rule 3 / Rerun behavior / Phase 4 stub)
    - .planning/phases/03-flux-hub-bootstrap/03-RESEARCH.md Example 4 (the canonical docs skeleton — use as source material, lightly prose-polish for readability)
    - .planning/phases/03-flux-hub-bootstrap/03-RESEARCH.md §Common Pitfalls P2, P3, P9 (the "why" content behind each rule)
    - .planning/phases/03-flux-hub-bootstrap/03-PATTERNS.md §`docs/flux-hub-spoke.md` (the file-level analog assignment)
  </read_first>
  <action>
Create `docs/flux-hub-spoke.md` with the following exact content. Write the file directly (Write tool); do not use heredoc.

```markdown
# Flux Hub-Spoke Patch Surface

`scripts/bootstrap-flux.sh` runs `flux bootstrap github --path clusters/hub-flux` against
the `k3d-hub-flux` cluster. That single command creates (or reuses) the GitHub repo,
commits three files under `clusters/hub-flux/flux-system/`, installs the four Flux
controllers, and starts the self-reconciliation loop.

This document explains the three rules that govern hand-editing anything under
`clusters/hub-flux/flux-system/`. Rules 1 and 2 correct the starter's overly-broad
"do not hand-edit" guidance by naming the one file that IS the supported patch
surface. Rule 3 locks `--path` as a one-time decision.

## What `flux bootstrap` writes

On first invocation, `flux bootstrap github` commits three files under
`clusters/hub-flux/flux-system/`:

| File | Category | Hand-edit? |
|------|----------|------------|
| `gotk-components.yaml` | bootstrap-managed | **NO** — overwritten on every rerun |
| `gotk-sync.yaml` | bootstrap-managed | **NO** — overwritten on every rerun |
| `kustomization.yaml` | **patch surface** | **YES** — this is the supported edit point |

The bootstrap commit lands on the `main` branch of `$GITHUB_OWNER/$GITHUB_REPO`; Flux
reconciles it back into the cluster via the `flux-system` `Kustomization` resource
every minute.

## Rule 1: `gotk-components.yaml` and `gotk-sync.yaml` are bootstrap-managed

Do not hand-edit these files. `flux bootstrap` re-renders them from CLI flags on every
run; any hand-edits are silently reverted the next time the script runs. If you need to
change controller behavior (memory limits, image tags, annotations, replica counts, etc.),
apply the change via Rule 2.

## Rule 2: `kustomization.yaml` IS the supported patch surface

Hand-edits to `clusters/hub-flux/flux-system/kustomization.yaml` survive re-bootstrap.
`flux bootstrap` only rewrites this file when its `resources:` block diverges from what
the current CLI flag set would produce; the `patches:`, `configMapGenerator:`, `images:`,
and other Kustomize-native blocks you add here stay intact. The top-of-file
`# FLUX PATCH SURFACE` comment block that `scripts/bootstrap-flux.sh` prepends is part
of the same invariant.

Example — bump `kustomize-controller`'s memory limit from the default 1Gi default to an
explicit 1Gi declaration:

```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - gotk-components.yaml
  - gotk-sync.yaml
patches:
  - target:
      kind: Deployment
      name: kustomize-controller
      namespace: flux-system
    patch: |
      - op: replace
        path: /spec/template/spec/containers/0/resources/limits/memory
        value: 1Gi
```

To prove the patch survived re-bootstrap, run:

```bash
bash scripts/bootstrap-flux.sh
kubectl --context k3d-hub-flux -n flux-system get deploy kustomize-controller \
  -o jsonpath='{.spec.template.spec.containers[0].resources.limits.memory}'
# Expected: 1Gi
```

## Rule 3: `--path` is effectively immutable after first run

`scripts/bootstrap-flux.sh` hard-codes `--path clusters/hub-flux`. Do not change it.

Changing `--path` on a subsequent run does NOT migrate your existing tree — it creates a
divergent bootstrap commit under the new path, orphans the old `flux-system/` tree, and
leaves the cluster reconciling from the new directory with no history continuity. There
is no documented migration path back. Treat the initial `--path` as a locked decision,
captured in code as a `readonly` bash constant in `scripts/bootstrap-flux.sh` and
enforced at test time by `tests/bats/bootstrap-flux-01-idempotent.bats` (the FLUX-02
test rejects any `FLUX_PATH="${FLUX_PATH:-...}"` override pattern).

## Rerun behavior

Rerunning `scripts/bootstrap-flux.sh` on an already-bootstrapped hub is a fast no-op.
The script's Section 2 three-part gate checks that the `flux-system` namespace exists,
`flux check` exits 0, and all four controller Deployments report `Available=True`; if
all three are true, it logs `already done, skipping: flux bootstrap` and proceeds
directly to post-bootstrap verification plus the patch-surface marker idempotency check.
`flux bootstrap github` is not invoked a second time.

<!-- Phase 4: hub-spoke reconciliation (spec.kubeConfig, <spoke>-kubeconfig Secrets, value.yaml key) -->
```

Content notes for the executor:

1. **Every literal the bats @tests grep for is present above.** Specifically: `gotk-components.yaml`, `gotk-sync.yaml`, `bootstrap-managed` (appears both in the table and in the Rule 1 heading body), `patch surface` (in multiple places — doc title subsection, Rule 2 heading, table cell), `--path` (Rule 3 heading and body), `immutable` (Rule 3 heading), `kustomize-controller` (Rule 2 example target.name AND kubectl jsonpath), `/spec/template/spec/containers/0/resources/limits/memory` (Rule 2 example patch `path:`), `1Gi` (Rule 2 example `value:` AND expected jsonpath output), `already done, skipping: flux bootstrap` (Rerun behavior section — exact literal from the script), `<!-- Phase 4:` (HTML-comment stub at the bottom).

2. **Keep flat.** No table of contents, no front-matter. Four short sections. Matches research Open Question 5 recommendation.

3. **Phase-4 stub is an HTML comment, not content.** The `<!-- Phase 4: ... -->` line is intentionally empty content — Phase 4's planner will append the hub-spoke reconciliation section below it. The test asserts only the stub marker exists.

4. **Tone: imperative, explanatory, zero padding.** Mirrors `docs/wsl-networking.md` voice. Each rule states what, then why. No "best practices" sections; no "for production" caveats — this is a lab doc.

5. **Markdown code blocks use triple-backtick `yaml` and `bash`.** No indented-code-block syntax. GitHub renders fenced blocks correctly. The fenced YAML block must contain the exact `patches:` structure — shell-escape of backticks inside the Write tool's content parameter is not needed because Write accepts arbitrary content.

6. **Do NOT include a trailing `--path` doc about FLUX_PATH variable in Rule 3.** The reference to the bash readonly constant is informational; the test only greps for `--path` (literal dashes) and `immutable`.
  </action>
  <verify>
    <automated>bash -c 'cd /home/rich/code/gsd/karyon && [ -f docs/flux-hub-spoke.md ] && bats tests/bats/bootstrap-flux-02-docs.bats --tap 2>&1 | head -20'</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f docs/flux-hub-spoke.md`
    - Is a regular markdown file with H1 title: `head -1 docs/flux-hub-spoke.md | grep -qE '^# '`
    - Non-trivial size (50-200 lines is the healthy band): `wc -l docs/flux-hub-spoke.md` ≥ 50 AND ≤ 200
    - Rule 1 markers: `grep -qF 'gotk-components.yaml' docs/flux-hub-spoke.md` AND `grep -qF 'gotk-sync.yaml' docs/flux-hub-spoke.md` AND `grep -qF 'bootstrap-managed' docs/flux-hub-spoke.md`
    - Rule 2 marker (case-insensitive): `grep -qiE 'patch surface' docs/flux-hub-spoke.md`
    - Rule 3 markers: `grep -qF -- '--path' docs/flux-hub-spoke.md` AND `grep -qiF 'immutable' docs/flux-hub-spoke.md`
    - D-15 example literals all present: `grep -qF 'kustomize-controller' docs/flux-hub-spoke.md` AND `grep -qF '/spec/template/spec/containers/0/resources/limits/memory' docs/flux-hub-spoke.md` AND `grep -qF '1Gi' docs/flux-hub-spoke.md`
    - D-15 example uses fenced YAML block: `grep -qE '^\`\`\`yaml' docs/flux-hub-spoke.md`
    - D-16 skip-message cross-reference (EXACT literal): `grep -qF 'already done, skipping: flux bootstrap' docs/flux-hub-spoke.md`
    - D-14 Phase-4 HTML-comment stub: `grep -qF '<!-- Phase 4:' docs/flux-hub-spoke.md`
    - No front-matter / YAML header: `head -1 docs/flux-hub-spoke.md` does NOT equal `---`
    - No TOC section: `grep -qiE '^## (Table of Contents|TOC)' docs/flux-hub-spoke.md` returns non-zero (no match)
    - No broken markdown headings (orphan `#` on body lines): quick sanity check that rule headings are H2 (`^## `): `grep -c '^## ' docs/flux-hub-spoke.md` ≥ 4
    - All 4 static @tests in bootstrap-flux-02-docs.bats PASS: `bats tests/bats/bootstrap-flux-02-docs.bats --tap` shows 4 `ok` lines, no `not ok` lines, no skips
  </acceptance_criteria>
  <done>
`docs/flux-hub-spoke.md` exists, contains all Phase-3-scoped sections (3 rules + D-15 example + D-16 skip-message reference + D-14 Phase-4 stub), and flips all 4 static @tests in `tests/bats/bootstrap-flux-02-docs.bats` from RED to GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc → reader | Markdown file committed to public repo; informational content only, no code execution |
| doc → committed file state | `docs/flux-hub-spoke.md` is read-only from runtime's perspective; Phase 4 planner will append content below the `<!-- Phase 4: -->` stub |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-02 | I (Information Disclosure) | Doc accidentally including a real secret in a code example | mitigate | Review before commit that no example uses a real `GITHUB_TOKEN`, real `$GITHUB_OWNER` / `$GITHUB_REPO` (placeholders only), real cluster FQDN, or real CA-data. The D-15 example is self-contained YAML with no secret surface. |
| T-3-03 | T (Tampering) | Reader copy-pastes the D-15 example into the wrong kustomization.yaml (e.g., a spoke cluster) | accept | Phase 3 scope is hub-only; Phase 4 documents the spoke tree. Rule 2's example is unambiguously scoped to `namespace: flux-system` on hub-flux. Low-impact misapplication; reader-facing risk, not script-facing. |
</threat_model>

<verification>
After Task 1 completes:

```bash
cd /home/rich/code/gsd/karyon && bats tests/bats/bootstrap-flux-02-docs.bats --tap
```

Expected:
- 4 @tests PASS (`ok 1..ok 4`), no skips, no failures

Manual sanity check (readability, not test-enforced):
- Open the file in a markdown renderer (GitHub preview, VS Code preview)
- Confirm the YAML code block in Rule 2 renders with syntax highlighting
- Confirm the table in "What flux bootstrap writes" renders as a 3-column table
- Confirm no broken markdown links (there are none by design — no external refs)
</verification>

<success_criteria>
- [ ] `docs/flux-hub-spoke.md` exists, version-controlled via git
- [ ] 4 static @tests in `tests/bats/bootstrap-flux-02-docs.bats` pass after this plan lands
- [ ] Contains exactly the 3 rules + D-15 example + D-16 skip-reference + D-14 Phase-4 stub
- [ ] No TOC, no front-matter — flat structure per research recommendation
- [ ] Tone matches `docs/wsl-networking.md` — imperative, explanatory
- [ ] Phase-4 stub is an HTML comment, not prose — leaves the content slot for Phase 4's planner
</success_criteria>

<output>
After completion, create `.planning/phases/03-flux-hub-bootstrap/03-03-SUMMARY.md` per the summary template, recording:
- File created: docs/flux-hub-spoke.md (line count; section boundaries)
- bats status transition: Plan 01's 4 static @tests in bootstrap-flux-02-docs.bats RED → GREEN
- Reference Plan 02 (script) and Plan 04 (Taskfile) as parallel Wave 1 siblings
</output>
