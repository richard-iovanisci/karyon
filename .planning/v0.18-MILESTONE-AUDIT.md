---
milestone: v0.18
audited: 2026-04-29T02:30:00Z
status: tech_debt
scores:
  requirements: 81/81
  phases: 6/6
  integration: 9/9
  flows: 1/1
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 06-repo-hygiene-docs-adrs
    items:
      - "WR-01 (info-level, recorded in 06-REVIEW.md): scripts/install-tools.sh:42 asdf version-check regex `^0\\.(1[89]|[2-9])` never matches `asdf version v0.18.1...` output, breaking the documented idempotency contract for that section. Real correctness bug; ~5-30s wasted per re-run."
      - "WR-02 (info-level): .gitleaks.toml:13 path allowlist regex `\\.env\\.example` is unanchored. Recommend `^\\.env\\.example$`."
      - "WR-03 (info-level): scripts/install-tools.sh:151-163 Section 7 is dead code (Section 5's loop already includes gitleaks)."
      - "WR-04 (info-level): .gitignore `kubeconfig` line is a strict subset of `kubeconfig*` glob; redundant."
      - "06-07 first-push verification deferred: 5 CI jobs locally green, but actual GitHub-side push + branch-protection setup not exercised. Required loud-tests (gitleaks, prune-lint) and branch-protection config remain user follow-up."
      - "HOOKS-PATH-STATE (info-level operational caveat): current local clone's `core.hooksPath` is the default `.git/hooks`, NOT `hooks/`. Wiring code in scripts/install-tools.sh Section 8 is correct; user must run `bash scripts/install-tools.sh` (or `git config --local core.hooksPath hooks`) to activate the local-side hook. Post-push CI gitleaks-scan with `fetch-depth: 0` is the belt-and-suspenders catch."
      - "SUMMARY frontmatter `requirements-completed` missing in 06-02-SUMMARY.md (DOCS-07/08/09/10), 06-05-SUMMARY.md (DOCS-01), 06-06-SUMMARY.md (DOCS-05). Verifier confirmed via codebase evidence; cosmetic gap only."
  - phase: 02-cluster-layer
    items:
      - "Anti-pattern (recorded in 02-VERIFICATION.md): tests/bats/create-clusters-{02,04}.bats use `grep -c Ready` which substring-matches `NotReady`. Live re-confirmation was performed manually with `awk '$2 == \"Ready\"'`."
  - phase: meta
    items:
      - ".planning/REQUIREMENTS.md traceability table is out of date: many requirements still show `Pending` despite phase verification passing (HOST-01..07, TOOLS-01..03, PRE-01..14, IMG-06, CLU-06, REPO-01..07, DOCS-01/02/04/05/06/07/08/09/10). `gsd-sdk query phase.complete` returned `requirements_updated: false` — likely table format mismatch with SDK expectations. Recommend reconciling before milestone archive."
nyquist:
  compliant_phases: ["01-host-foundation", "02-cluster-layer", "04-spoke-registration"]
  partial_phases:
    - phase: "03-flux-hub-bootstrap"
      reason: "VALIDATION.md shows nyquist_compliant=false, wave_0_complete=false. Phase 3 verification is `passed` and all FLUX-01..05 satisfied; the validation file appears stale relative to actual phase completion."
    - phase: "05-workloads-health-rebuild"
      reason: "VALIDATION.md shows nyquist_compliant=true but wave_0_complete=false. Phase 5 verification is `passed` and all DEP/HEALTH/DESTROY satisfied; mixed flags suggest Wave 0 was never marked complete in the validation file."
    - phase: "06-repo-hygiene-docs-adrs"
      reason: "VALIDATION.md shows nyquist_compliant=false, wave_0_complete=false. Phase 6 verification is `passed`, all 56 phase-6 bats green (15 repo-hygiene + 24 docs + 17 destroy-rebuild regression), and `06-VALIDATION.md` Wave 0 task list landed via Plan 06-00 (8 NEW bats files / 39 tests / parses clean). VALIDATION.md frontmatter was not updated as plans completed."
  missing_phases: []
  overall: "Validation files (`*-VALIDATION.md`) are not auto-updated as plans complete. The frontmatter (`nyquist_compliant`, `wave_0_complete`) reflects the planning-time state, not the post-execution state. Empirically, all 6 phases passed verification with full bats suites green. Recommend a one-time reconciliation pass (or a future SDK feature) to refresh the VALIDATION.md frontmatter at `gsd-sdk query phase.complete` time."
---

# Milestone v0.18 Audit Report

## Summary

**Status:** `tech_debt` — all 81 requirements satisfied, all 6 phases verified passing, end-to-end `task rebuild` flow validated at 190s. No critical blockers. Several non-blocking debt items accumulated; recommend addressing before milestone archive.

| Score | Result |
|-------|--------|
| Requirements coverage | 81/81 satisfied |
| Phases verified | 6/6 (`status: passed` in all VERIFICATION.md) |
| Cross-phase integration | 9/9 wiring questions PASS |
| End-to-end flow | 1/1 (`task rebuild` chain validated, 190s wall-clock) |

## Requirements Coverage Matrix

All 81 milestone requirements are satisfied. Coverage was confirmed via 3-source cross-reference (REQUIREMENTS.md traceability table + per-plan SUMMARY.md `requirements-completed` frontmatter + per-phase VERIFICATION.md evidence tables).

| Phase | Requirements | Verified | SUMMARY Coverage | Notes |
|-------|--------------|----------|------------------|-------|
| 01-host-foundation | HOST-01..07, TOOLS-01..03, PRE-01..14 (24) | passed | 9/9 plans | All requirements have explicit SUMMARY frontmatter |
| 02-cluster-layer | IMG-01..06, CLU-01..06 (12) | passed | 5/7 plans (02-02/03 explicitly empty by plan design; closure attributed to 02-04/06) | All requirements covered |
| 03-flux-hub-bootstrap | FLUX-01..05 (5) | passed | 6/6 plans | All requirements covered |
| 04-spoke-registration | SPOKE-01..06, DOCS-03 (7) | passed | 5/5 plans | DOCS-03 was originally mapped to Phase 6 but Plan 04-04 satisfied it inline (verified) |
| 05-workloads-health-rebuild | DEP-01..04, HEALTH-01..07, DESTROY-01..06 (17) | passed | 6/6 plans | All requirements covered |
| 06-repo-hygiene-docs-adrs | REPO-01..07, DOCS-01..10 (16; DOCS-03 satisfied in Phase 4) | passed | 5/8 plans (06-02/05/06 missing frontmatter) | DOCS-01/05/07/08/09/10 satisfied via codebase evidence in VERIFICATION.md; SUMMARY frontmatter cosmetic gap only |

**Orphan detection:** 0 orphan requirements (all REQ-IDs present in REQUIREMENTS.md traceability are accounted for in at least one phase).

**Note on REQUIREMENTS.md traceability table:** the table itself shows many requirements as `Pending` despite phases verifying passing — `gsd-sdk query phase.complete` returns `requirements_updated: false` for most phases. This is a clerical/SDK gap, not a verification gap. The actual verification status is captured authoritatively in VERIFICATION.md files. Recommend reconciling REQUIREMENTS.md as part of `/gsd-complete-milestone`.

## Phase Verification Status

| Phase | VERIFICATION.md | Score | Notable Findings |
|-------|-----------------|-------|------------------|
| 01-host-foundation | passed | 5/5 must-haves | None |
| 02-cluster-layer | passed | 5/5 must-haves | Anti-pattern: `grep -c Ready` substring-matches `NotReady` (worked around manually with `awk`) |
| 03-flux-hub-bootstrap | passed | 5/5 must-haves | None |
| 04-spoke-registration | passed | 5/5 must-haves | DOCS-03 closure (Phase 4 absorbed Phase 6 verify-only deliverable) |
| 05-workloads-health-rebuild | passed | 5/5 must-haves | `task rebuild` empirically validated at 190s with warm CUDA cache |
| 06-repo-hygiene-docs-adrs | passed | 5/5 must-haves | Code review surfaced WR-01 (asdf regex bug) + 3 minor cleanups; first-push verification deferred |

## Cross-Phase Integration

The integration checker traced 9 cross-phase wiring questions and the full `task rebuild` end-to-end chain. **All 9 PASS, no blockers.** Detailed export ↔ consumer map confirms no orphan exports beyond two acceptable cases (`config/hwclock-resume.service` used only by `install-docker.sh`; `scripts/verify-flux-bootstrap-live.sh` is a verification-only artifact).

**E2E flow trace (`task rebuild`):**

| Step | Script | Cross-phase consumers verified |
|------|--------|-------------------------------|
| 1 | `preflight.sh` | `lib/preflight-lib.sh`, `.tool-versions`, `.env` |
| 2 | `destroy.sh` → `delete-clusters.sh` | `images/image-tag.sh` (CUDA cache survival check) |
| 3 | `create-clusters.sh` | `preflight.sh`, `images/image-tag.sh`, lazy `build-image.sh` |
| 3.5 | `kubectl config use-context k3d-hub-flux` | Explicit cross-phase glue (`rebuild.sh:66-67`) addressing the k3d cluster create last-context bug |
| 4 | `bootstrap-flux.sh` | `preflight.sh`, `.env` (`load_env_key`), `clusters/hub-flux/flux-system/kustomization.yaml` (FLUX PATCH SURFACE marker) |
| 5 | `register-spokes-for-flux.sh` | spoke kubectl contexts, hub Secret apply, writes `clusters/hub-flux/spokes/{spoke-ml,spoke-apps}.yaml`, edits flux-system kustomization to add `- ../spokes` mount |
| 6 | `deploy-examples.sh` | git visibility gate, `flux reconcile kustomization spoke-{apps,ml}` |
| 7 | `health-check.sh` | k3d cluster list, kubectl get/wait, flux check, openssl SAN probe, in-hub DNS probe |

**Empirical validation:** Phase 5 P06 verification recorded a 190-second wall-clock end-to-end (`/tmp/karyon-rebuild.log`).

## Tech Debt Summary

7 items across 3 phases (1 real correctness bug; 6 cosmetic / clerical / deferred):

**Phase 06 (5 items):**
1. **WR-01** (real bug) — asdf version regex never matches; breaks documented idempotency. `/gsd-code-review-fix 06` will auto-fix.
2. WR-02 — `.gitleaks.toml:13` unanchored allowlist regex.
3. WR-03 — `scripts/install-tools.sh` Section 7 dead code.
4. WR-04 — `.gitignore` redundant kubeconfig line.
5. 06-07 first-push verification deferred (CI green locally, GitHub-side push + branch-protection not exercised).
6. HOOKS-PATH-STATE — local clone needs `bash scripts/install-tools.sh` to activate hook (post-push CI catches what local would miss).
7. SUMMARY frontmatter `requirements-completed` missing in 06-02/05/06.

**Phase 02 (1 item):**
- `grep -c Ready` substring-matches `NotReady` in two test files (manually worked around).

**Meta (1 item):**
- REQUIREMENTS.md traceability table out of date — phases ship verification-passing but the table still shows requirements as `Pending`. Suggests an SDK or table-format mismatch in `gsd-sdk query phase.complete --requirements_updated`. Recommend reconciling before milestone archive.

## Nyquist Compliance

| Phase | VALIDATION.md | Compliant | Wave 0 Complete | Status |
|-------|---------------|-----------|-----------------|--------|
| 01-host-foundation | exists | true | true | COMPLIANT |
| 02-cluster-layer | exists | true | true | COMPLIANT |
| 03-flux-hub-bootstrap | exists | false | false | PARTIAL (frontmatter stale; phase verified passing) |
| 04-spoke-registration | exists | true | true | COMPLIANT |
| 05-workloads-health-rebuild | exists | true | false | PARTIAL (mixed flags; phase verified passing) |
| 06-repo-hygiene-docs-adrs | exists | false | false | PARTIAL (frontmatter stale; Wave 0 bats scaffold landed via 06-00, all 56 phase tests green) |

**Overall:** Validation files are not auto-updated as plans complete; frontmatter reflects planning-time state, not post-execution state. Empirically all 6 phases pass. No `/gsd-validate-phase` calls needed; recommend a one-time reconciliation pass on the 3 PARTIAL files at milestone archive time.

## Recommendations

1. **Optional cleanup before archive (recommended):**
   - `/gsd-code-review-fix 06` — auto-fix WR-01 (asdf regex), WR-02 (unanchored allowlist), WR-03 (dead Section 7), WR-04 (redundant gitignore line)
   - Reconcile REQUIREMENTS.md traceability table (phases marked Pending → Complete based on VERIFICATION.md status)
   - Backfill `requirements-completed` in 06-02/05/06 SUMMARY frontmatter
   - Refresh `nyquist_compliant` / `wave_0_complete` in 03/05/06 VALIDATION.md frontmatter

2. **Required follow-up (post-archive acceptable):**
   - Push to feature branch and verify 5 CI jobs run green on GitHub
   - Loud-test gitleaks (`tmp-leak.sh` with `ghp_AAAA…`) and prune-lint (deliberate `docker system prune` in destroy.sh) — both must go RED
   - Configure GitHub branch protection on `main` requiring all 5 status checks; verify it blocks PR merge on red CI

3. **Proceed:**
   - `/gsd-complete-milestone v0.18` — archive milestone, accept tech debt as documented; the WR-01 fix and CI verification can be addressed in v0.19's first phase or as a standalone polish phase.

---

*Milestone audit completed 2026-04-29.*
