# pocs/capsule/tenants/alpha-app/kustomization.yaml
# Placeholder — empty resource list so the inner Flux Kustomization
# (tenant-alpha-app, defined in pocs/capsule/tenants/alpha.yaml)
# reconciles successfully against an empty resource set (a missing path would
# leave it Ready=False, path not found). May be populated with tenant
# workloads later.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
