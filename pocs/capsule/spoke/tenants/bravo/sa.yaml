# pocs/capsule/spoke/tenants/bravo/sa.yaml
# The per-tenant ServiceAccount registered as Tenant owner via the
# combined name format `system:serviceaccount:tenant-bravo:gitops-reconciler` in
# tenant-crs/bravo.yaml's spec.owners[].
#
# gitops-reconciler SA. Capsule's auto-injected RoleBindings on owners
# (default clusterRoles: [admin, capsule-namespace-deleter]) give this SA full
# control inside any tenant-managed namespace; no additional RoleBinding needed.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: gitops-reconciler
  namespace: tenant-bravo
