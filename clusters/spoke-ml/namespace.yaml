# clusters/spoke-ml/namespace.yaml
# Seed manifest - distinct per spoke, falsifiable for the cross-cluster
# negative-proof (this Namespace exists ONLY on k3d-spoke-ml).
apiVersion: v1
kind: Namespace
metadata:
  name: karyon-spoke-ml
